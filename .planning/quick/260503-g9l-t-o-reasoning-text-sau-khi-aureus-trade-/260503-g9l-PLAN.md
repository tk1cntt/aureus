---
phase: quick-260503-g9l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/journal.py
  - services/aureus-trader/tests/test_journal.py
  - services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py
autonomous: true
requirements:
  - QUICK-260503-G9L
must_haves:
  truths:
    - "Reasoning Bank creates deterministic reasoning_text only after both aureus_trade_journal and aureus_trade_signal_snapshots rows are persisted/known."
    - "Generated reasoning_text uses only existing persisted/known data: strategy_name, symbol, direction, context_filters, active_signals, and persisted/inserted signal snapshot fields trend/CISD/TPO/session/ATR/EMA/BB."
    - "prompt_text and context_text remain copied-only from upstream aliases; no derived/fake prompt_text or context_text is generated."
    - "Reasoning insert and embedding enqueue remain non-blocking beyond current journal insertion path; Redis enqueue failure still returns True after journal insert."
    - "DB/runtime E2E proves aureus_trade_journal, aureus_trade_signal_snapshots, and aureus_reasoning_entries persist linked data with generated reasoning_text."
  artifacts:
    - path: "services/aureus-trader/journal.py"
      provides: "Deterministic reasoning_text builder and on_order_opened integration after journal and snapshot row ids exist."
      contains: "reasoning_text"
    - path: "services/aureus-trader/tests/test_journal.py"
      provides: "Unit coverage for generated reasoning_text, prompt/context preservation, and non-blocking enqueue behavior."
      contains: "test_on_strategy_match"
    - path: "services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py"
      provides: "Runtime DB E2E proving linked journal/snapshot/reasoning row with generated reasoning_text."
      contains: "reasoning_text"
  key_links:
    - from: "services/aureus-trader/journal.py:on_strategy_match"
      to: "aureus_trade_journal"
      via: "INSERT ... RETURNING id"
      pattern: "INSERT INTO aureus_trade_journal"
    - from: "services/aureus-trader/journal.py:on_strategy_match"
      to: "aureus_reasoning_entries.reasoning_text"
      via: "generated only after trade_journal_id and signal_snapshot_id exist"
      pattern: "reasoning_text"
    - from: "services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py"
      to: "aureus_trade_signal_snapshots"
      via: "on_order_opened runtime insert then joined select by trace_id"
      pattern: "signal_snapshot_id"
---

# Quick 260503-g9l: Generate deterministic reasoning_text after journal and snapshot data exist

## Objective

Tạo deterministic `reasoning_text` cho Reasoning Bank trong `services/aureus-trader/journal.py` khi upstream không gửi `reasoning` hoặc `rationale`.

Purpose: Reasoning Bank đang có schema và persistence nhưng runtime không có producer text. Cần tạo `reasoning_text` từ dữ liệu thật đã biết trong journal/signal snapshot, không tạo `prompt_text` hoặc `context_text` giả.

Output: Một thay đổi surgical trong trader journal, unit tests, và DB/runtime E2E chứng minh có row DB thật.

## Execution Context

- `CLAUDE.md`
- `RUN_SERVICES.md`
- `.planning/quick/260503-edn-ph-n-t-ch-th-c-t-data-hi-n-t-i-x-c-nh-re/260503-edn-DATA-FLOW-REPORT.md`

## Context

Relevant source:

- `services/aureus-trader/journal.py`
- `services/aureus-trader/tests/test_journal.py`
- `services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py`

Current evidence from `260503-edn-DATA-FLOW-REPORT.md`:

- `publish_strategy_match()` does not produce `reasoning_text`, `prompt_text`, `context_text`.
- `TradeJournalManager.on_strategy_match()` currently copies `reasoning`/`rationale`, `prompt_text`/`raw_prompt`, `context_text`/`raw_context` only.
- Embedding queue uses only non-empty `reasoning_text`, `prompt_text`, `context_text`.
- DB schema already supports raw text fields and embeddings.

Existing contracts:

```python
from journal import TradeJournalManager

async def TradeJournalManager.on_strategy_match(self, event: dict) -> bool
async def TradeJournalManager.on_order_opened(self, event: dict) -> bool
```

Existing journal insertion sequence:

1. Insert `aureus_trade_journal`, returning `id` as `result`.
2. If `result`, insert `aureus_reasoning_entries` with `trade_journal_id=result`.
3. Select embedding sources from inserted raw text fields.
4. Enqueue embedding job if sources exist and Redis exists.
5. Reasoning insert/enqueue exceptions are warnings and do not fail order flow.

## Assumptions

- No GitNexus MCP namespace appears available in current toolset. Executor must use `npx gitnexus ...` CLI if MCP unavailable, per `RUN_SERVICES.md` line 11: run `npx gitnexus` on current environment, not WSL.
- `on_strategy_match` cannot know database-generated `signal_snapshot_id` if `ORDER_OPENED` runs later. Therefore `on_strategy_match` must persist copied-only text fields without generated fallback, and `on_order_opened` must generate/finalize `reasoning_text` after inserting or finding the signal snapshot row.
- Keep current execute-order path. Do not move journal work into MT5 ACK/result path. Do not make Reasoning Bank required for MT5 dispatch.

## Decision Coverage Matrix

| Decision/Constraint | Plan | Task | Coverage | Notes |
|---|---:|---:|---|---|
| Deterministic `reasoning_text` after journal + snapshot rows persisted/known | 01 | 2 | Full | Builder called from `on_order_opened` after snapshot insert/link step. |
| Use real existing fields only | 01 | 1 | Full | Source only strategy/symbol/direction/context_filters/active_signals/signal_snapshot. |
| Preserve prompt/context copied-only behavior | 01 | 1,2 | Full | Tests assert missing upstream leaves `None`; provided upstream remains copied. |
| No execute-order gating change | 01 | 1,2 | Full | Keep warning-only reasoning/enqueue behavior; no blocking Redis requirement. |
| DB/runtime E2E required | 01 | 3 | Full | E2E writes and reads actual DB rows. |
| GitNexus impact analysis before edit | 01 | 1 | Full | Task starts with impact step. |

## Tasks

### Task 1: Add unit tests for post-snapshot generated reasoning_text and copy-only prompt/context

Type: auto  
TDD: true

Files:

- `services/aureus-trader/tests/test_journal.py`

Behavior:

- `on_strategy_match` without upstream reasoning creates reasoning entry with `reasoning_text is None` because snapshot row is not persisted yet.
- `on_order_opened` then persists/links `aureus_trade_signal_snapshots` and updates missing `reasoning_text` from real journal + snapshot values.
- Upstream reasoning provided earlier is preserved; post-snapshot generation must not overwrite it.
- Missing upstream `prompt_text` and `context_text` remain `None`.
- Redis enqueue failure remains non-blocking after generated reasoning text.

Action:

1. Add focused failing tests in existing `TestOnStrategyMatchInputValidation` or nearest Reasoning Bank test section.
2. Use existing fixtures/patterns, then call `on_strategy_match` followed by `on_order_opened` for same `trace_id`.
3. Use real values:
   - `strategy_name`: `TREND_CONT_BULL`
   - `symbol`: `XAUUSD`
   - `direction`: `BUY`
   - `context_filters`: `{"session": "london"}`
   - `signal_snapshot`: fields accepted by current snapshot code such as `trend`, `cisd_m15`, `tpo_shape`, `session`, `atr`, `ema21`, `ema55`, `bb_m5_up`, `bb_m5_dn`.
4. Assert `on_strategy_match` reasoning insert keeps `reasoning_text`, `prompt_text`, `context_text` null when upstream omitted.
5. Assert `on_order_opened` executes an update that sets generated `reasoning_text` only when existing `reasoning_text IS NULL`, and text includes strategy/symbol/direction/session/snapshot fact.
6. Assert upstream reasoning remains unchanged and is not overwritten.

Verify:

```bash
./.venv/bin/python -m pytest services/aureus-trader/tests/test_journal.py -q
```

Done:

- Tests fail before implementation and pass after implementation.
- Tests prove post-snapshot timing.
- Tests prove prompt/context are not derived.
- Tests prove generated reasoning can enqueue embeddings without blocking on Redis failure.

### Task 2: Generate/finalize reasoning_text after snapshot persistence in journal path

Type: auto  
TDD: true

Files:

- `services/aureus-trader/journal.py`

Behavior:

- If upstream `data.reasoning`, `data.rationale`, `match_data.reasoning`, or `match_data.rationale` exists, `on_strategy_match` stores it unchanged.
- If no upstream reasoning exists, `on_strategy_match` inserts `aureus_reasoning_entries.reasoning_text` as `NULL`.
- After `on_order_opened` persists or links `aureus_trade_signal_snapshots`, generate `reasoning_text` deterministically from available real fields:
  - `strategy_name`
  - `symbol`
  - `direction`
  - `context_filters`
  - `active_signals`
  - persisted/inserted signal snapshot fields: trend, CISD, TPO, session, ATR, EMA, BB
- Do not generate `prompt_text`.
- Do not generate `context_text`.
- Do not change dispatch/order/MT5 gating.

Action:

1. Before editing, run GitNexus impact analysis for modified symbols:
   - Preferred MCP if available: `gitnexus_impact({target: "TradeJournalManager.on_strategy_match", direction: "upstream"})` and `gitnexus_impact({target: "TradeJournalManager.on_order_opened", direction: "upstream"})`
   - If MCP unavailable, run CLI from repo root, not WSL: `npx gitnexus impact TradeJournalManager.on_strategy_match --direction upstream` and `npx gitnexus impact TradeJournalManager.on_order_opened --direction upstream`
   - Report blast radius in executor summary: direct callers, affected processes, risk.
2. In `services/aureus-trader/journal.py`, add a small private helper near journal code, for example `_build_reasoning_text(...)`. Keep output stable string, no timestamps/random data.
3. Keep `on_strategy_match` copied alias lookup as-is; do not generate fallback there.
4. In `on_order_opened`, after `aureus_trade_signal_snapshots` insert/link and `signal_snapshot_id` is known, update `aureus_reasoning_entries.reasoning_text` only for the same trace/journal where `reasoning_text IS NULL`.
5. Source fields from persisted journal row plus current snapshot data/columns in the same transaction. Include only present values. Normalize values safely with `json.dumps(..., sort_keys=True, default=str)` where needed.
6. If snapshot already exists and `ON CONFLICT DO NOTHING RETURNING id` returns `None`, fetch existing snapshot id for `trade_journal_id` before generation/linking.
7. After generated reasoning text is saved, enqueue embedding job for generated reasoning only if Redis exists. Enqueue failure logs warning and does not make `on_order_opened` fail.
8. Keep `prompt_text` and `context_text` exact current alias copy logic.

Verify:

```bash
./.venv/bin/python -m pytest services/aureus-trader/tests/test_journal.py -q
```

Done:

- `reasoning_text` is generated only after journal + snapshot ids exist.
- Generated text contains strategy, symbol, direction, context filters, and present signal/snapshot facts.
- Missing upstream prompt/context still persist as `NULL`.
- Existing non-blocking behavior remains unchanged.

### Task 3: Update DB/runtime E2E to prove generated reasoning_text with linked snapshot

Type: auto

Files:

- `services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py`

Behavior:

- E2E sends `STRATEGY_MATCH` without upstream `reasoning`, `rationale`, `prompt_text`, or `context_text`.
- E2E calls `on_strategy_match`, then `on_order_opened` so journal, snapshot, and reasoning rows are persisted and linked.
- E2E confirms generated `reasoning_text` exists from real strategy/symbol/direction/context/snapshot fields, while `prompt_text` and `context_text` stay null.

Action:

1. Modify E2E event in `verify_reasoning_bank_reuse_e2e.py` so `on_strategy_match` does not send `reasoning`, `rationale`, `prompt_text`, or `context_text`.
2. Keep real event fields used by builder:
   - `strategy_name`
   - `strategy_id`
   - `direction`
   - `symbol`
   - `score`
   - `active_signals`
   - `context_filters`
   - `signal_snapshot` with real existing keys that current snapshot code tolerates, aligned with current script's later `on_order_opened` payload.
3. Keep `on_order_opened` call so `aureus_trade_signal_snapshots` row is persisted and linked by current code.
4. Update assertions:
   - `row["reasoning_text"]` exists and contains strategy/symbol/direction/session/snapshot fact.
   - `row["prompt_text"] is None`.
   - `row["context_text"] is None`.
   - `row["trade_journal_id"] is not None`.
   - `row["signal_snapshot_id"] is not None`.
   - existing joined journal/snapshot assertions still pass.
5. Do not add new dependency, migration, service, or external API.
6. Before any commit, run change scope check:
   - Preferred MCP if available: `gitnexus_detect_changes()`
   - If MCP unavailable, run CLI from repo root, not WSL: `npx gitnexus detect-changes --scope all`

Verify:

```bash
./.venv/bin/python -m pytest services/aureus-trader/tests/test_journal.py -q
```

```bash
./.venv/bin/python services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py
```

If command fails because services/DB are down, use `RUN_SERVICES.md`:

```bash
./scripts/dev-service.sh
```

Then rerun E2E.

Done:

- Unit tests pass.
- E2E creates real DB rows and prints PASS.
- E2E confirms generated `reasoning_text`, null prompt/context, linked journal id, linked snapshot id.
- GitNexus detect changes reports only expected symbols/files.

## Threat Model

Trust boundaries:

| Boundary | Description |
|---|---|
| Redis STRATEGY_MATCH payload -> trader journal | Untrusted event data is normalized and persisted. |
| Trader journal -> TimescaleDB | Generated text and JSON fields cross into DB. |
| Trader journal -> Redis embedding queue | Generated text can be queued for async embedding. |

STRIDE threat register:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260503-g9l-01 | Tampering | `on_strategy_match` event data | mitigate | Use only already accepted event fields; do not trust prompt/context; normalize optional fields with safe serialization. |
| T-260503-g9l-02 | Denial of Service | Reasoning generation/enqueue | mitigate | Keep deterministic local string build only; no DB query/network call; keep existing warning-only enqueue failure path. |
| T-260503-g9l-03 | Information Disclosure | `reasoning_text` content | mitigate | Generate only trading signal facts already persisted/known; do not include raw prompts, hashes, credentials, env, or external payload dumps. |
| T-260503-g9l-04 | Repudiation | DB E2E evidence | mitigate | E2E writes unique trace_id and asserts linked `trade_journal_id`/`signal_snapshot_id`. |

## Verification

Run all gates:

```bash
./.venv/bin/python -m pytest services/aureus-trader/tests/test_journal.py -q
```

```bash
./.venv/bin/python services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py
```

```bash
npx gitnexus detect-changes --scope all
```

## Success Criteria

- `aureus_reasoning_entries.reasoning_text` no longer stays null after normal STRATEGY_MATCH plus ORDER_OPENED persist linked journal and signal snapshot rows.
- Generated text is deterministic and based only on real strategy/journal/signal snapshot fields.
- `prompt_text` and `context_text` remain null unless upstream provides them.
- Reasoning Bank/embedding path remains async/non-blocking relative to MT5 order flow.
- DB/runtime E2E proves persisted and linked journal/snapshot/reasoning data.

## Output

After completion, create:

- `.planning/quick/260503-g9l-t-o-reasoning-text-sau-khi-aureus-trade-/260503-g9l-SUMMARY.md`
