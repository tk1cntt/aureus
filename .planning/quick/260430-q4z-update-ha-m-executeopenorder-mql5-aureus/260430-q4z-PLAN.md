---
phase: 260430-q4z-update-ha-m-executeopenorder-mql5-aureus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
  - mql5/compile_aureusprovider_v2.log
autonomous: true
requirements:
  - Q4Z-01
must_haves:
  truths:
    - "ExecuteOpenOrder rejects an OPEN_ORDER when an open position already exists with the same symbol and magic number."
    - "ExecuteOpenOrder rejects an OPEN_ORDER when a pending order already exists with the same symbol and magic number."
    - "ExecuteOpenOrder still allows the same magic number to open orders on a different symbol."
    - "Reject happens before ACK, RecordCmdId, OrderCheck, OrderSend, and any ORDER_OPENED side effect."
    - "Provider socket/JSON/ACK/NACK/streaming behavior is unchanged except the new clear duplicate strategy reject."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "ExecuteOpenOrder same-symbol same-magic open-position/pending-order rejection"
      contains: "ExecuteOpenOrder"
    - path: "mql5/compile_aureusprovider_v2.log"
      provides: "MetaEditor compile verification log for AureusProvider_v2.mq5"
      contains: "0 errors, 0 warnings"
  key_links:
    - from: "ExecuteOpenOrder"
      to: "PositionsTotal/PositionGetTicket/PositionSelectByTicket"
      via: "pre-ACK scan for existing position with matching POSITION_SYMBOL and POSITION_MAGIC"
      pattern: "PositionsTotal\\(\\).*POSITION_SYMBOL.*POSITION_MAGIC"
    - from: "ExecuteOpenOrder"
      to: "OrdersTotal/OrderGetTicket/OrderSelect"
      via: "pre-ACK scan for pending order with matching ORDER_SYMBOL and ORDER_MAGIC"
      pattern: "OrdersTotal\\(\\).*ORDER_SYMBOL.*ORDER_MAGIC"
    - from: "duplicate strategy guard"
      to: "SendNACK"
      via: "clear reject reason before ACK/RecordCmdId/OrderSend"
      pattern: "SendNACK\\(cmdId, \"STRATEGY_ORDER_EXISTS\"\\)"
---

<objective>
Cập nhật `ExecuteOpenOrder` trong `mql5/AureusProvider_v2.mq5` để mỗi strategy, định danh bằng `magic`, chỉ có tối đa một open position hoặc pending order trên cùng một `symbol`.

Purpose: Ngăn một strategy vào trùng lệnh trên cùng symbol, nhưng vẫn cho phép cùng strategy/magic giao dịch nhiều symbol khác nhau.
Output: Một thay đổi surgical trong provider MQL5, có reject reason rõ ràng trước mọi order side effect, và compile MetaEditor sạch 0 errors / 0 warnings.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/mql5/AureusProvider_v2.mq5
@D:/Aureus/mql5/Build_Rules.md

<interfaces>
Existing relevant contracts in `mql5/AureusProvider_v2.mq5`:

```mql5
void ExecuteOpenOrder(const string &raw)
```

Current order acceptance sequence inside `ExecuteOpenOrder`:
1. Parse `cmdId`, `symbol`, `direction`, `orderType`, `magic`, etc.
2. Validate required fields.
3. Reject duplicate `cmdId` via `IsDuplicateCmd` + `SendNACK`.
4. Reject undeclared symbol by returning before ACK/order side effects.
5. Reject disabled trading via `SendNACK(cmdId, "TRADE_DISABLED")`.
6. Send `SendACK(cmdId)` and `RecordCmdId(cmdId)`.
7. Build `MqlTradeRequest`, run `OrderCheck`, then `OrderSend`.

The new same-symbol/same-magic guard must be inserted after required-field/idempotency/symbol checks have established a valid `cmdId`, `symbol`, and `magic`, and before `SendACK(cmdId)` and `RecordCmdId(cmdId)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze ExecuteOpenOrder blast radius and locate exact insertion point</name>
  <files>mql5/AureusProvider_v2.mq5</files>
  <action>Before editing any symbol, follow project GitNexus rules: run impact analysis for `ExecuteOpenOrder` with upstream direction and report blast radius in the execution summary. If GitNexus has no MQL5 symbol coverage or cannot resolve `ExecuteOpenOrder`, document that fallback explicitly, then use direct repo-relative searches for `ExecuteOpenOrder`, `OPEN_ORDER`, `SendACK`, `RecordCmdId`, `PushOrderFailed`, `SendNACK`, `PositionsTotal`, and `OrdersTotal` to confirm callers and nearby patterns. Do not edit yet in this task.</action>
  <verify>
    <automated>git grep -n "ExecuteOpenOrder\|OPEN_ORDER\|SendACK\|RecordCmdId\|OrdersTotal\|PositionsTotal" -- mql5/AureusProvider_v2.mq5</automated>
  </verify>
  <done>Blast radius is recorded; insertion point is confirmed as pre-ACK/pre-RecordCmdId/pre-OrderCheck/pre-OrderSend inside `ExecuteOpenOrder`; no unrelated files or symbols are selected for modification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add same-symbol same-magic active order guard</name>
  <files>mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - Existing open position where `POSITION_SYMBOL == symbol` and `POSITION_MAGIC == magic` causes `ExecuteOpenOrder` to send `NACK` with reason `STRATEGY_ORDER_EXISTS` and return before ACK/order side effects.
    - Existing pending order where `ORDER_SYMBOL == symbol` and `ORDER_MAGIC == magic` causes the same `STRATEGY_ORDER_EXISTS` reject before ACK/order side effects.
    - Existing position/order with the same `magic` but a different `symbol` does not reject.
    - Existing position/order with the same `symbol` but a different `magic` does not reject.
  </behavior>
  <action>Implement the smallest surgical change in `mql5/AureusProvider_v2.mq5`: add a provider-local helper if it keeps `ExecuteOpenOrder` readable, or inline a compact preflight block if that matches existing style better. The guard must scan both active positions and pending orders. For positions, iterate `PositionsTotal()`, get each ticket with `PositionGetTicket(i)`, select with `PositionSelectByTicket(ticket)` if needed, and compare `PositionGetString(POSITION_SYMBOL)` plus `PositionGetInteger(POSITION_MAGIC)`. For pending orders, iterate `OrdersTotal()`, get each ticket with `OrderGetTicket(i)`, select that ticket using the MQL5-supported `OrderSelect(ticket)` pattern, and compare `OrderGetString(ORDER_SYMBOL)` plus `OrderGetInteger(ORDER_MAGIC)`. Insert the guard after symbol allowlist validation and before `TerminalInfoInteger(TERMINAL_TRADE_ALLOWED)` or at least before `SendACK(cmdId)`; prefer before trade-enabled check because this is a command-level reject independent of terminal trading state. On match, call `SendNACK(cmdId, "STRATEGY_ORDER_EXISTS")`, optionally debug-log matched symbol/magic/ticket/type using existing `InpDebugMode` style, increment no order counters, do not call `RecordCmdId`, do not call `OrderCheck`, and do not call `OrderSend`. Preserve all provider socket/JSON/ACK/NACK/streaming semantics outside this reject path. Do not hardcode symbol lists, do not block same magic on different symbols, and do not change DCA, close-order, position-report, or streaming logic.</action>
  <verify>
    <automated>git diff -- mql5/AureusProvider_v2.mq5 && git grep -n "STRATEGY_ORDER_EXISTS\|OrdersTotal\|PositionsTotal\|SendACK(cmdId)\|RecordCmdId(cmdId)" -- mql5/AureusProvider_v2.mq5</automated>
  </verify>
  <done>`ExecuteOpenOrder` has a pre-side-effect guard for both open positions and pending orders matching the same `symbol` and `magic`; same `magic` on another symbol remains allowed because every match requires symbol equality.</done>
</task>

<task type="auto">
  <name>Task 3: Compile provider in current worktree and verify changed scope</name>
  <files>mql5/AureusProvider_v2.mq5, mql5/compile_aureusprovider_v2.log</files>
  <action>Compile using MetaEditor per `mql5/Build_Rules.md`, but use the current worktree path rather than hardcoded `D:/Aureus` to avoid compiling the wrong checkout. From bash, derive a Windows path with `pwd -W` (or an equivalent current-worktree absolute path) and compile `mql5/AureusProvider_v2.mq5` to `mql5/compile_aureusprovider_v2.log` using `E:\Openclaw\MetaTrader5\MetaEditor64.exe` as required by Build_Rules. Fix any compile errors or warnings caused by this change until the log reports 0 errors and 0 warnings. After compile, run `gitnexus_detect_changes()` as required by project rules; if GitNexus cannot map MQL5 changes, document the fallback and verify with `git diff -- mql5/AureusProvider_v2.mq5 mql5/compile_aureusprovider_v2.log` that only the expected provider source plus compile log changed.</action>
  <verify>
    <automated>cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"%CD%\mql5\AureusProvider_v2.mq5" /log:"%CD%\mql5\compile_aureusprovider_v2.log""</automated>
    <automated>git diff -- mql5/AureusProvider_v2.mq5 mql5/compile_aureusprovider_v2.log</automated>
  </verify>
  <done>MetaEditor compile log shows 0 errors and 0 warnings; scope check confirms only `mql5/AureusProvider_v2.mq5` and generated compile log changed; execution summary includes GitNexus impact/detect_changes results or the documented MQL5 fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway TCP JSON → provider order execution | Untrusted or duplicated OPEN_ORDER commands cross into MT5 trading side effects. |
| provider preflight → MT5 terminal state | Provider reads active positions and pending orders from terminal state before deciding whether to accept an order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q4Z-01 | Tampering | `ExecuteOpenOrder` OPEN_ORDER command | mitigate | Reject same `symbol` + same `magic` before `SendACK`, `RecordCmdId`, `OrderCheck`, and `OrderSend`. |
| T-Q4Z-02 | Denial of Service | Strategy execution capacity per symbol | mitigate | Scope rejection to exact `symbol` + `magic` only, so one symbol cannot block the same strategy/magic on other symbols. |
| T-Q4Z-03 | Repudiation | Reject reason reporting | mitigate | Use explicit NACK reason `STRATEGY_ORDER_EXISTS` so upstream logs can distinguish intentional strategy guard from trade-server failures. |
</threat_model>

<verification>
1. Static verification confirms the new guard is located before `SendACK(cmdId)` and `RecordCmdId(cmdId)` in `ExecuteOpenOrder`.
2. Static verification confirms both `PositionsTotal()` and `OrdersTotal()` scans compare both symbol and magic.
3. MetaEditor compile verification for current worktree provider returns 0 errors and 0 warnings.
4. GitNexus impact/detect_changes requirements are satisfied, or MQL5 index fallback is documented with direct search evidence.
</verification>

<success_criteria>
- Same `magic` + same `symbol` active position rejects new OPEN_ORDER with `STRATEGY_ORDER_EXISTS` before ACK/order side effects.
- Same `magic` + same `symbol` pending order rejects new OPEN_ORDER with `STRATEGY_ORDER_EXISTS` before ACK/order side effects.
- Same `magic` + different `symbol` remains allowed.
- Existing ACK/NACK/event/streaming semantics remain unchanged except the new reject path.
- `mql5/AureusProvider_v2.mq5` compiles with 0 errors and 0 warnings.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260430-q4z-update-ha-m-executeopenorder-mql5-aureus/260430-q4z-SUMMARY.md`.
</output>
