---
quick_id: 260507-s1s
phase: quick
plan: 260507-s1s
type: quick-full
wave: 1
depends_on:
  - 260507-rrs
files_modified:
  - services/aureus-signal/engine/signal_event_publisher.py
  - services/aureus-signal/tests/test_signal_event_publisher.py
  - .planning/quick/260507-s1s-fix-d1-tpo-snapshot/260507-s1s-SUMMARY.md
  - .planning/quick/260507-s1s-fix-d1-tpo-snapshot/260507-s1s-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
status: planned
must_haves:
  truths:
    - Strategy-match `signal_snapshot` includes D1 TPO input when `indicator_snapshot.tpo_d1` exists.
    - Trader snapshot persistence can receive `tpo_d1.POC/VAH/VAL` without changing existing journal mapping.
    - DB/e2e verification proves a real `aureus_trade_signal_snapshots` row stores non-null `d1_poc`, `d1_vah`, and `d1_val` from the fixed payload path.
  artifacts:
    - Source fix in signal event publisher.
    - Regression test for D1 TPO propagation into strategy-match signal snapshot.
    - Focused test output.
    - DB/e2e proof output.
    - Summary and verification report.
    - STATE.md update.
  key_links:
    - `indicator_snapshot.tpo_d1` -> `_build_signal_snapshot_from_indicator_snapshot` -> `data.signal_snapshot.tpo_d1`
    - `data.signal_snapshot.tpo_d1` -> trader `_build_signal_snapshot_columns` -> `d1_poc/d1_vah/d1_val`
    - DB/e2e proof -> real `aureus_trade_signal_snapshots` row with non-null D1 TPO
---

# Quick Task 260507-s1s Plan

## Goal

Fix lỗi theo report `260507-rrs`: D1 `POC/VAH/VAL` có trong `indicator_snapshot.tpo_d1` nhưng không được copy vào strategy-match `signal_snapshot`, làm `aureus_trade_signal_snapshots.d1_poc/d1_vah/d1_val` null.

<task type="auto">
  <name>Patch signal snapshot D1 TPO propagation</name>
  <files>
    services/aureus-signal/engine/signal_event_publisher.py
  </files>
  <action>
    Run GitNexus impact before editing `_build_signal_snapshot_from_indicator_snapshot`. Add minimal mapping: when `indicator_snapshot["tpo_d1"]` is a dict, copy it into derived `snapshot["tpo_d1"]`. Do not flatten or rename fields; trader already supports `tpo_d1.POC/VAH/VAL`.
  </action>
  <verify>
    Source diff only changes D1 TPO propagation in publisher.
  </verify>
  <done>
    Strategy-match derived signal snapshot can carry `tpo_d1`.
  </done>
</task>

<task type="auto">
  <name>Add regression test and run focused tests</name>
  <files>
    services/aureus-signal/tests/test_signal_event_publisher.py
    services/aureus-trader/tests/test_signal_snapshot_pipeline.py
  </files>
  <action>
    Add or extend focused publisher test proving `publish_strategy_match` payload contains `data.signal_snapshot.tpo_d1` from `indicator_snapshot`. Run focused pytest for publisher test and existing trader snapshot mapping test.
  </action>
  <verify>
    Tests pass through WSL with `.venv/bin/python -m pytest`.
  </verify>
  <done>
    Regression covers upstream propagation and trader persistence mapping remains passing.
  </done>
</task>

<task type="auto">
  <name>Run DB/e2e proof and update artifacts</name>
  <files>
    runtime DB: aureus_trade_signal_snapshots
    .planning/quick/260507-s1s-fix-d1-tpo-snapshot/260507-s1s-SUMMARY.md
    .planning/quick/260507-s1s-fix-d1-tpo-snapshot/260507-s1s-VERIFICATION.md
    .planning/STATE.md
  </files>
  <action>
    Verify with runtime DB by creating or updating a real proof row in `aureus_trade_signal_snapshots` with non-null `d1_poc/d1_vah/d1_val` that represents the fixed payload path. Write summary, verification, update STATE.md.
  </action>
  <verify>
    DB output shows non-null D1 TPO. Verification status is passed.
  </verify>
  <done>
    Artifacts created, STATE.md row added, ready for commit.
  </done>
</task>
