---
phase: 260430-tb3-update-ph-n-x-l-managepositionprofitbrea
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
autonomous: true
requirements:
  - QUICK-260430-TB3
must_haves:
  truths:
    - "ManagePositionProfitBreakEvent xử lý mỗi group theo symbol + magic + direction, không chỉ symbol + direction."
    - "earliest_open_time, total_profit, total_volume, weighted_price_sum được reset và tính riêng trong từng group symbol + magic + direction."
    - "ProcessPositionsByType chỉ nhận tickets và aggregate metrics thuộc đúng group hiện tại."
    - "Không đổi behavior database, không commit/generated .log/.ex5/snapshots/tmp/stable artifacts."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Corrective provider-safe ManagePositionProfitBreakEvent grouping"
      contains: "ManagePositionProfitBreakEvent"
  key_links:
    - from: "mql5/AureusProvider_v2.mq5::ManagePositionProfitBreakEvent"
      to: "mql5/AureusProvider_v2.mq5::ProcessPositionsByType"
      via: "tickets + aggregate metrics for exactly one symbol + magic + direction group"
      pattern: "PositionGetString\(POSITION_SYMBOL\).*PositionGetInteger\(POSITION_MAGIC\).*POSITION_TYPE"
---

<objective>
Sửa scope grouping trong `mql5/AureusProvider_v2.mq5::ManagePositionProfitBreakEvent()` để aggregation không còn có nguy cơ gom theo `symbol + direction` mà phải tách đầy đủ theo `symbol + magic + direction`.

Purpose: Provider v2 có thể có nhiều strategy/magic cùng symbol và cùng chiều. Nếu aggregate metrics bị trộn magic, profit/breakeven/close/modify có thể tác động sai strategy. Quick task này là corrective scope fix, chỉ chạm logic group hiện có.

Output: `AureusProvider_v2.mq5` được chỉnh surgical để mỗi group có `earliest_open_time`, `total_profit`, `total_volume`, `weighted_price_sum`, và `tickets[]` local theo đúng `symbol + magic + direction`.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/mql5/AureusProvider_v2.mq5
@D:/Aureus/.planning/quick/260430-sm5-base-tr-n-managepositionprofitbreakevent/260430-sm5-PLAN.md
@D:/Aureus/.planning/quick/260430-sm5-base-tr-n-managepositionprofitbreakevent/260430-sm5-SUMMARY.md

<interfaces>
Current relevant contracts in `mql5/AureusProvider_v2.mq5`:

```mql5
int FindContextIndex(string symbol);

void ProcessPositionsByType(string symbol,
                            long magic,
                            ENUM_POSITION_TYPE target_type,
                            const ulong &tickets[],
                            double total_profit,
                            double total_volume,
                            double weighted_price_sum,
                            datetime earliest_open_time);

void ManagePositionProfitBreakEvent();
```

Correct grouping invariant for this fix:

```mql5
PositionGetString(POSITION_SYMBOL) == symbol &&
PositionGetInteger(POSITION_MAGIC) == magic &&
(ENUM_POSITION_TYPE)PositionGetInteger(POSITION_TYPE) == type
```

Required local-per-group metrics:
- `tickets[]`: only tickets matching current `symbol + magic + type`.
- `total_profit`: sum `POSITION_PROFIT` only for tickets in that group.
- `total_volume`: sum `POSITION_VOLUME` only for tickets in that group.
- `weighted_price_sum`: sum `POSITION_PRICE_OPEN * POSITION_VOLUME` only for tickets in that group.
- `earliest_open_time`: minimum `POSITION_TIME` only within that group.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix ManagePositionProfitBreakEvent grouping by magic</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - Given two USDJPY BUY positions with different magic values, `ManagePositionProfitBreakEvent()` must produce two separate calls to `ProcessPositionsByType`, one per magic.
    - Given USDJPY magic 101 BUY and USDJPY magic 101 SELL, BUY and SELL remain separate groups.
    - Given USDJPY magic 101 BUY and EURUSD magic 101 BUY, symbols remain separate groups.
    - For each group, `earliest_open_time`, `total_profit`, `total_volume`, and `weighted_price_sum` are initialized before collecting that group and only updated after all three predicates match: symbol, magic, direction.
  </behavior>
  <action>Tuân thủ CLAUDE.md trước khi sửa symbol: chạy GitNexus impact cho `ManagePositionProfitBreakEvent` và `ProcessPositionsByType` nếu GitNexus/CLI nhận diện được; nếu MQL5 symbol không có trong index như quick 260430-sm5 thì ghi rõ fallback trong summary. Sửa surgical trong `ManagePositionProfitBreakEvent()` để cơ chế `already_processed` và inner collection đều so khớp đầy đủ `POSITION_SYMBOL`, `POSITION_MAGIC`, và `POSITION_TYPE`. Nếu code hiện tại đã có đủ ba điều kiện, không refactor rộng; chỉ thêm guard/log/comment tối thiểu hoặc chỉnh chỗ còn thiếu để làm invariant hiển nhiên. Không đổi `ProcessPositionsByType` signature trừ khi bắt buộc; không đụng DB, gateway order flow, DCA, hoặc các hàm không liên quan.</action>
  <verify>
    <automated>cd D:/Aureus && git diff -- mql5/AureusProvider_v2.mq5</automated>
    <automated>cd D:/Aureus && git diff --check -- mql5/AureusProvider_v2.mq5</automated>
  </verify>
  <done>`ManagePositionProfitBreakEvent()` không có đường nào collect/process group chỉ theo symbol + direction; mọi aggregate metric và tickets[] local theo symbol + magic + direction.</done>
</task>

<task type="auto">
  <name>Task 2: Verify aggregate locality and compile scope</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <action>Review diff bằng mắt và ghi vào summary checklist: `earliest_open_time`, `total_profit`, `total_volume`, `weighted_price_sum` đều được reset trong scope của từng group và chỉ cộng khi đủ symbol+magic+direction; `ProcessPositionsByType(symbol, magic, type, tickets, ...)` nhận đúng `magic` của group. Compile bằng MetaEditor nếu khả dụng theo `mql5/Build_Rules.md`; nếu không khả dụng, ghi rõ. Không stage/commit `.log`, `.ex5`, `snapshots/`, `tmp/`, `stable/` hoặc artifact generated. Chạy GitNexus detect changes nếu tool khả dụng; nếu CLI không có lệnh như summary 260430-sm5, ghi fallback và dùng `git status --short` + diff scoped review.</action>
  <verify>
    <automated>cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\AureusProvider_v2.mq5" /log:"D:\Aureus\mql5\AureusProvider_v2_compile.log"" || true</automated>
    <automated>cd D:/Aureus && git status --short</automated>
  </verify>
  <done>Compile attempt completed or unavailability documented; git status shows no generated artifacts staged; summary contains explicit aggregate-locality verification for earliest_open_time/total_profit/total_volume/weighted_price_sum.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider timer → MT5 trade server | Automated close/SL modify decisions cross into live MT5 trading operations. |
| Multiple strategy magic values → shared symbol positions | Different strategies can hold same symbol and same direction simultaneously. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260430-tb3-01 | Tampering | ManagePositionProfitBreakEvent grouping | mitigate | Require all grouping/processed/collection predicates to include `POSITION_SYMBOL`, `POSITION_MAGIC`, and `POSITION_TYPE`. |
| T-260430-tb3-02 | Elevation of Privilege | trade.PositionClose/PositionModify via ProcessPositionsByType | mitigate | Only pass tickets collected from the exact current group; never pass mixed-magic tickets into group actions. |
| T-260430-tb3-03 | Information Disclosure | Compile logs/artifacts | mitigate | Do not stage/commit `.log`, `.ex5`, snapshots, tmp, or stable artifacts. |
</threat_model>

<verification>
1. `git diff -- mql5/AureusProvider_v2.mq5` confirms only grouping-scope corrective changes.
2. `git diff --check -- mql5/AureusProvider_v2.mq5` passes.
3. Manual scoped review confirms no aggregate variable (`earliest_open_time`, `total_profit`, `total_volume`, `weighted_price_sum`) is shared across magic groups.
4. Compile via MetaEditor if available; document if unavailable.
5. No DB changes and no generated `.log`/`.ex5` artifacts committed.
</verification>

<success_criteria>
- `ManagePositionProfitBreakEvent()` aggregates by exact `symbol + magic + direction`.
- Same symbol/direction with different magic values is processed as separate groups.
- Same symbol/magic with BUY and SELL remains separate.
- Metrics and tickets passed to `ProcessPositionsByType` are group-local.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `D:/Aureus/.planning/quick/260430-tb3-update-ph-n-x-l-managepositionprofitbrea/260430-tb3-SUMMARY.md`.
</output>
