---
phase: 260425-u4u-th-c-hi-n-theo-xu-t-planning-quick-26042
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/tpo.py
  - services/aureus-signal/engine/signals/tpo_context.py
  - services/aureus-signal/engine/signals/tpo_detectors.py
  - services/aureus-signal/tests/test_tpo_signal.py
autonomous: true
requirements:
  - QUICK-260425-U4U
must_haves:
  truths:
    - "TPO block vẫn xuất shape/shape_confidence_pct/shape_scores_pct như visual-shape metadata D/B/p/b, không đổi nghĩa thành direction hay regime."
    - "Mỗi TPO block có distr = total_tpo_count / max_tpo_count và distribution_regime thuộc TREND/NORMAL/NEUTRAL/UNKNOWN."
    - "TPO context propagate distribution_regime theo từng timeframe riêng biệt với shape."
    - "Detector không biến setup invalid thành valid chỉ vì distribution_regime, không thay D1 bias, không đảo side long/short."
  artifacts:
    - path: "services/aureus-signal/engine/signals/tpo.py"
      provides: "TPO block fields for distr and distribution_regime while keeping shape metadata contract"
    - path: "services/aureus-signal/engine/signals/tpo_context.py"
      provides: "Per-timeframe distribution_regime propagation into TPO context"
    - path: "services/aureus-signal/engine/signals/tpo_detectors.py"
      provides: "Safe optional regime modifier/tagging only after primary price-relation setup is valid"
    - path: "services/aureus-signal/tests/test_tpo_signal.py"
      provides: "Regression tests for distr calculation, context propagation, non-emission, and D1 bias protection"
  key_links:
    - from: "services/aureus-signal/engine/signals/tpo.py"
      to: "services/aureus-signal/engine/signals/tpo_context.py"
      via: "TPO block distribution_regime consumed by TPOContextBuilder._build_timeframe"
      pattern: "distribution_regime"
    - from: "services/aureus-signal/engine/signals/tpo_context.py"
      to: "services/aureus-signal/engine/signals/tpo_detectors.py"
      via: "context['timeframes'][tf]['distribution_regime'] is metadata/context modifier only"
      pattern: "distribution_regime"
---

<objective>
Triển khai đề xuất advisory `260425-tpc` một cách surgical: giữ `_classify_shape` là visual-shape metadata, thêm `distr` và `distribution_regime` riêng từ count distribution, propagate qua TPO context, và khóa bằng test rằng regime không tự phát buy/sell hoặc thay D1 bias.

Purpose: hấp thụ phần đúng của report về `distr/daytype` mà không làm nhiễu semantics shape hoặc phá nguyên tắc detector lấy price relation làm primary.
Output: source/test cập nhật cho TPO block, context builder, detector safety, và regression tests.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260425-tpc-nh-gi-c-l-p-report-tpo-shape-distributio/260425-tpc-ARCHITECTURE-ADVISORY.md
@D:/Aureus/services/aureus-signal/engine/signals/tpo.py
@D:/Aureus/services/aureus-signal/engine/signals/tpo_context.py
@D:/Aureus/services/aureus-signal/engine/signals/tpo_detectors.py
@D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py

<interfaces>
Current contracts to preserve:
- `TPOSignal._build_tpo_block(session_df, symbol="") -> Optional[Dict[str, Any]]` currently emits `POC`, `VAH`, `VAL`, `shape`, `shape_confidence_pct`, `shape_scores_pct`.
- `TPOSignal._classify_shape(levels, counts, poc_idx) -> Tuple[str, float, Dict[str, float]]` must remain visual-shape classifier for D/B/p/b.
- `TPOContextBuilder._build_timeframe(block, close) -> Optional[Dict[str, Any]]` currently maps `POC/VAH/VAL` to lowercase `poc/vah/val`, keeps `shape`, and calculates price location/distances.
- TPO detectors currently validate price relation first and include reasons like `shape is not sufficient without price relation`; keep this invariant.
</interfaces>

Scope locks from user/advisory:
- Keep `_classify_shape` as visual-shape metadata; do not replace it with distribution regime.
- Add separate `distribution_regime` from `distr = total_tpo_count / max_tpo_count`.
- `distribution_regime` must not self-emit buy/sell, must not convert invalid setup to valid, and must not replace D1 bias.
- Keep changes surgical; no new dependency, no database work, no research phase.
- Before editing any function/class/method symbol, executor must run GitNexus impact analysis and report blast radius.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression tests for distribution regime semantics</name>
  <files>D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py</files>
  <behavior>
    - TPO block includes existing shape fields plus new `distr` and `distribution_regime`; existing shape tests must assert shape remains D/B/p/b metadata.
    - `distr` equals `sum(counts) / max(counts)` for normal, single-bin, multi-peak max, and zero/empty profile safe cases.
    - Missing/insufficient baseline produces `distribution_regime == "UNKNOWN"` and does not crash.
    - TPOContextBuilder propagates `distribution_regime` per D1/H1/M30 independently from `shape`.
    - A detector with invalid price relation remains invalid even when timeframe context has `distribution_regime == "TREND"`.
    - A D1 bullish/bearish conflict still blocks opposite setup even when `distribution_regime == "TREND"`.
  </behavior>
  <action>Before editing, run GitNexus impact analysis for the symbols that tests will exercise: `gitnexus_impact({target: "TPOSignal", direction: "upstream"})`, `gitnexus_impact({target: "TPOContextBuilder", direction: "upstream"})`, and impacted detector classes changed or asserted in tests. Report direct callers, affected processes, and risk level. Then update only `test_tpo_signal.py` with focused tests. Do not add broad replay/backtest harness here; this quick task only creates unit/regression coverage for the advisory invariants.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_tpo_signal.py -x</automated>
  </verify>
  <done>New tests fail for missing `distr`/`distribution_regime` before implementation and cover non-emission plus D1 bias protection. If pytest/service command fails due to environment or service startup, consult `D:/Aureus/RUN_SERVICES.md` before retrying.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit distr and distribution_regime without changing shape semantics</name>
  <files>D:/Aureus/services/aureus-signal/engine/signals/tpo.py, D:/Aureus/services/aureus-signal/engine/signals/tpo_context.py, D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py</files>
  <behavior>
    - `_classify_shape` still returns only D/B/p/b visual-shape metadata and all existing shape fixture tests remain valid.
    - `_build_tpo_block` computes `distr = total_tpo_count / max_tpo_count` from the same `counts` used to build the profile.
    - `distribution_regime` uses safe `UNKNOWN` when baseline/history is unavailable or when `max_tpo_count <= 0`; avoid static thresholds masquerading as validated rolling baseline.
    - TPOContextBuilder copies block `distribution_regime` and optionally `distr` into timeframe context without touching `_build_d1_bias` logic.
  </behavior>
  <action>Implement the smallest helper(s) needed in `TPOSignal` to calculate `distr` from `counts` and classify regime safely. Because this quick scope has no rolling baseline store for distribution intensity, default regime to `UNKNOWN` unless a defensible in-process baseline already exists in the touched code; do not invent broad persistence or static threshold semantics. Preserve all existing output keys and add new keys rather than renaming. Update tests' expected key sets accordingly. Do not change D1 bias relation checks in `TPOContextBuilder._build_d1_bias`.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_tpo_signal.py -x</automated>
  </verify>
  <done>`pytest` passes; TPO block includes `distr` numeric safe value and `distribution_regime`; shape output remains visual-shape metadata with D/B/p/b scores; context includes regime without changing D1 bias. If command fails due to environment, use `D:/Aureus/RUN_SERVICES.md`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep detector regime usage non-emissive and capped</name>
  <files>D:/Aureus/services/aureus-signal/engine/signals/tpo_detectors.py, D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py</files>
  <behavior>
    - `distribution_regime == "TREND"` never creates a valid setup without VAL reclaim/VAH reject/breakout acceptance/pullback confirmation.
    - `distribution_regime` never overrides D1 bullish/bearish conflict guards and never flips long/short side.
    - If adding detector usage, it is limited to a small post-valid score/tag/reason modifier with cap; `UNKNOWN` is no-op.
  </behavior>
  <action>Only modify detector code if needed to make regime handling explicit and safe. Prefer no-op propagation if existing detectors ignore regime and tests prove non-emission. If adding a modifier, apply it only inside `_valid(...)` paths after primary detector conditions are already satisfied, keep score boost/discount small and capped, and add reason text that regime is context metadata. Do not move conflict guards below modifier logic and do not use `TREND` as direction.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_tpo_signal.py -x</automated>
  </verify>
  <done>Detector tests prove invalid setups stay invalid under TREND, D1 bias guards still win, UNKNOWN is no-op, and any valid-setup score modifier remains capped and non-directional. Run `gitnexus_detect_changes()` before any commit to confirm only expected TPO symbols/flows changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Market data → TPO computation | OHLC dataframe/counts are untrusted runtime inputs and may be sparse, malformed, or zero-count. |
| TPO metadata → detector decisions | New regime metadata crosses from indicator context into signal decision logic; misuse could create false positives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260425-u4u-01 | Tampering | `TPOSignal._build_tpo_block` | mitigate | Guard zero/empty counts and `max_tpo_count <= 0`; emit safe `UNKNOWN` instead of dividing by zero or inferring a regime. |
| T-260425-u4u-02 | Information/Integrity | `TPOContextBuilder._build_timeframe` | mitigate | Propagate `distribution_regime` as separate field from `shape`; do not alter `_build_d1_bias`. |
| T-260425-u4u-03 | Elevation of Privilege | TPO detectors | mitigate | Tests must prove regime cannot promote invalid setup to valid, cannot override D1 conflict guards, and cannot imply long/short direction. |
</threat_model>

<verification>
Primary command:
`cd D:/Aureus/services/aureus-signal && pytest tests/test_tpo_signal.py -x`

If command fails because services/env are not running or dependencies are missing, read `D:/Aureus/RUN_SERVICES.md` and retry using the documented project service commands. Before committing, run `gitnexus_detect_changes()` as required by `D:/Aureus/CLAUDE.md`.
</verification>

<success_criteria>
- Existing TPO shape classifier contract remains intact and tested.
- `distr` is calculated from count distribution as `total_tpo_count / max_tpo_count` with safe handling for zero/empty inputs.
- `distribution_regime` exists as separate metadata with allowed values `TREND`, `NORMAL`, `NEUTRAL`, `UNKNOWN`; no unsupported baseline means `UNKNOWN`.
- TPO context carries regime per timeframe separately from shape.
- Detector tests prove regime is non-emissive, non-directional, and cannot replace D1 bias.
- All verification commands pass.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260425-u4u-th-c-hi-n-theo-xu-t-planning-quick-26042/260425-u4u-SUMMARY.md`.
</output>
