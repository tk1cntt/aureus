---
phase: quick-260425-055-tpo-service-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/tpo.py
  - services/aureus-signal/engine/signals/indicator_snapshot.py
  - services/aureus-signal/engine/telegram/alert_formatter.py
  - services/aureus-signal/tests/engine/signals/test_tpo.py
autonomous: true
requirements:
  - QUICK-260425-055-1D-TODAY-ONLY
  - QUICK-260425-055-1H-LAST-6-CACHED
  - QUICK-260425-055-M30-LAST-6-CACHED
  - QUICK-260425-055-TELEGRAM-SNAPSHOT-TPO
must_haves:
  truths:
    - "Indicator snapshot chứa TPO 1D chỉ của ngày hiện tại (VAH/VAL/POC)."
    - "TPO 1H luôn có dữ liệu 6 giờ gần nhất, giờ hiện tại tính realtime, các giờ trước lấy cache nếu có."
    - "TPO M30 luôn có dữ liệu 6 nến gần nhất, nến hiện tại tính realtime, các nến trước lấy cache nếu có."
    - "Telegram SIGNAL ALERT nhận đủ TPO 1D/1H/M30 với VAH/VAL/POC trong payload snapshot."
  artifacts:
    - path: "services/aureus-signal/engine/signals/tpo.py"
      provides: "Logic tính + cache TPO theo rule 1D/1H/M30"
    - path: "services/aureus-signal/engine/signals/indicator_snapshot.py"
      provides: "Map TPO 1D/1H/M30 vào indicator snapshot"
    - path: "services/aureus-signal/engine/telegram/alert_formatter.py"
      provides: "Render/send TPO fields trong SIGNAL ALERT"
    - path: "services/aureus-signal/tests/engine/signals/test_tpo.py"
      provides: "Automated tests cho rule cửa sổ thời gian + cache"
  key_links:
    - from: "tpo.py"
      to: "indicator_snapshot.py"
      via: "function call/returned structure"
      pattern: "tpo_1d|tpo_1h|tpo_m30"
    - from: "indicator_snapshot.py"
      to: "alert_formatter.py"
      via: "snapshot payload"
      pattern: "VAH|VAL|POC"
---

<objective>
Tối ưu cách tính TPO trong signal engine để đúng rule truy xuất theo timeframe (1D/1H/M30), giảm tính lại không cần thiết nhờ cache theo bucket đã đóng, và đảm bảo dữ liệu TPO đi xuyên suốt tới Telegram SIGNAL ALERT.

Purpose: Đồng bộ logic TPO với nhu cầu runtime hiện tại, tăng độ tin cậy snapshot, và tránh payload thiếu/không nhất quán ở cảnh báo Telegram.
Output: Logic TPO mới + mapping snapshot + formatter Telegram + test tự động cho các rule quan trọng.
</objective>

<execution_context>
@/d/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@/d/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/d/Aureus/.planning/STATE.md
@/d/Aureus/CLAUDE.md
@/d/Aureus/services/aureus-signal/engine/signals/tpo.py
@/d/Aureus/services/aureus-signal/engine/signals/indicator_snapshot.py
@/d/Aureus/services/aureus-signal/engine/telegram/alert_formatter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tối ưu engine TPO theo rule 1D/1H/M30 + chiến lược cache</name>
  <files>services/aureus-signal/engine/signals/tpo.py, services/aureus-signal/tests/engine/signals/test_tpo.py</files>
  <behavior>
    - Test 1: TPO 1D chỉ trả dữ liệu ngày hiện tại, không lẫn ngày trước.
    - Test 2: TPO 1H trả 6 giờ gần nhất; bucket giờ hiện tại tính realtime, 5 giờ đã đóng ưu tiên dùng cache.
    - Test 3: TPO M30 trả 6 bucket gần nhất; bucket hiện tại realtime, bucket đã đóng ưu tiên dùng cache.
    - Test 4: Cấu trúc output luôn có VAH/VAL/POC cho từng timeframe 1D/1H/M30.
  </behavior>
  <action>Điều chỉnh logic trong tpo.py để tách rõ rule theo timeframe: 1D = today-only window; 1H = sliding 6-hour window; M30 = sliding 6-bucket window. Thiết kế cache theo key bucket (timeframe + symbol + bucket_start), chỉ compute lại bucket đang mở; bucket đã đóng dùng cache nhằm giảm tính toán lặp. Không thay đổi API ngoài phạm vi TPO nếu không cần thiết.</action>
  <verify>
    <automated>pytest services/aureus-signal/tests/engine/signals/test_tpo.py -q</automated>
  </verify>
  <done>Logic TPO tạo đúng 3 nhánh 1D/1H/M30 với VAH/VAL/POC đầy đủ; test cho cửa sổ thời gian và cache đều pass.</done>
</task>

<task type="auto">
  <name>Task 2: Đẩy TPO 1D/1H/M30 vào Indicator Snapshot pipeline</name>
  <files>services/aureus-signal/engine/signals/indicator_snapshot.py</files>
  <action>Map output TPO mới vào indicator snapshot theo cấu trúc ổn định, bảo đảm có đủ 3 nhóm TPO (1D/1H/M30), mỗi nhóm gồm VAH/VAL/POC. Giữ naming nhất quán với pipeline hiện hữu để tránh vỡ downstream consumers.</action>
  <verify>
    <automated>pytest services/aureus-signal/tests -k "snapshot and tpo" -q</automated>
  </verify>
  <done>Indicator snapshot chứa trường TPO 1D/1H/M30 đầy đủ và không làm vỡ các test snapshot hiện có.</done>
</task>

<task type="auto">
  <name>Task 3: Gửi TPO vào Telegram SIGNAL ALERT và kiểm chứng tích hợp</name>
  <files>services/aureus-signal/engine/telegram/alert_formatter.py, services/aureus-signal/tests/engine/signals/test_tpo.py</files>
  <action>Cập nhật formatter/payload của SIGNAL ALERT để include TPO 1D/1H/M30 (VAH/VAL/POC) từ indicator snapshot. Bổ sung test tích hợp nhỏ để xác nhận payload/message có đủ 3 timeframe và không bị null key bắt buộc.</action>
  <verify>
    <automated>pytest services/aureus-signal/tests -k "telegram and tpo" -q</automated>
  </verify>
  <done>Telegram SIGNAL ALERT nhận và hiển thị đúng TPO 1D/1H/M30 (VAH/VAL/POC), không thiếu timeframe nào.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal engine -> telegram alert | Dữ liệu indicator snapshot đi vào kênh outbound message, cần tránh payload sai format/thiếu key |
| market data -> tpo computation | Dữ liệu OHLCV đầu vào có thể thiếu/không đều bucket, cần guard logic cửa sổ thời gian |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-055-01 | T | `tpo.py` | mitigate | Chuẩn hóa key cache theo bucket_start/timeframe/symbol, tránh ghi đè sai bucket |
| T-quick-055-02 | I | `alert_formatter.py` | mitigate | Validate presence của VAH/VAL/POC trước khi render payload/message |
| T-quick-055-03 | D | `tpo.py` | mitigate | Chỉ compute realtime cho bucket mở, tái dùng cache bucket đã đóng để giảm tải |
</threat_model>

<verification>
Chạy test TPO unit + snapshot pipeline + telegram payload format; xác nhận không có regression ở luồng signal snapshot hiện tại.
</verification>

<success_criteria>
- TPO 1D chỉ lấy ngày hiện tại, không có dữ liệu ngoài today.
- TPO 1H và M30 đều chỉ lấy 6 bucket gần nhất, với bucket đang chạy realtime và bucket trước đó ưu tiên cache.
- SIGNAL ALERT Telegram nhận đủ TPO 1D/1H/M30 (VAH/VAL/POC) trong indicator snapshot payload.
- Toàn bộ test liên quan pass bằng lệnh verify trong từng task.
</success_criteria>

<output>
After completion, create `/d/Aureus/.planning/quick/260425-055-t-i-mu-n-t-i-u-la-i-ca-ch-ti-nh-tpo-serv/260425-055-SUMMARY.md`
</output>
