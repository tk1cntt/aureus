---
phase: 260513-s4k-ki-m-tra-l-i-log-aureus-gateway-dev-v-ph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-gateway/main.py
autonomous: true
requirements:
  - QUICK-260513-S4K
must_haves:
  truths:
    - "Log aureus-gateway-dev được kiểm tra bằng docker logs qua WSL distro Aureus và phân loại Invalid JSON theo nguồn thực tế."
    - "GitNexus impact cho handle_tcp_client chạy trước mọi sửa symbol; nếu stale/error thì analyze trên môi trường hiện tại rồi retry trước khi fallback thủ công."
    - "Kết nối TCP healthcheck/probe không tạo warning Invalid JSON gây nhiễu log."
    - "Gateway vẫn xử lý TICK/CANDLE/order event JSON hợp lệ sau fix."
    - "Sau docker restart aureus-gateway-dev, ps/logs xác nhận service chạy và log mới sạch Invalid JSON không mong muốn hoặc warning được phân loại đúng."
  artifacts:
    - path: "services/aureus-gateway/main.py"
      provides: "TCP handler phân biệt empty/probe/non-JSON với payload JSON lỗi thật"
      contains: "handle_tcp_client"
    - path: "services/aureus-gateway/tests/test_tcp_invalid_json_classification.py"
      provides: "Regression tests cho phân loại TCP invalid JSON/probe"
      contains: "handle_tcp_client"
    - path: "docker-compose.dev.yml"
      provides: "healthcheck/probe hiện tại cho aureus-gateway-dev để đối chiếu nguyên nhân"
      contains: "aureus-gateway-dev"
  key_links:
    - from: "docker-compose.dev.yml"
      to: "services/aureus-gateway/main.py"
      via: "healthcheck opens TCP port 5556 without sending JSON"
      pattern: "s.connect(('127.0.0.1', 5556))"
    - from: "services/aureus-gateway/main.py"
      to: "Redis aureus streams/events"
      via: "process_message after json.loads"
      pattern: "await process_message\(r, data, source=\"TCP\"\)"
    - from: "GitNexus impact"
      to: "services/aureus-gateway/main.py"
      via: "impact handle_tcp_client before editing symbol"
      pattern: "gitnexus_impact\(\{target: \"handle_tcp_client\", direction: \"upstream\"\}\)"
---

<objective>
Kiểm tra log `aureus-gateway-dev`, xác định nguyên nhân warning `Invalid JSON`, sửa tối thiểu trong gateway TCP handler nếu warning đến từ healthcheck/probe/partial non-JSON, rồi restart container và verify log sạch.

Purpose: giảm nhiễu log mà không làm đổi flow MT5 JSON hợp lệ.
Output: fix nhỏ trong `services/aureus-gateway/main.py`, test regression, và bằng chứng verify bằng command `wsl -d Aureus`/Docker.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/RUN_SERVICES.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/services/aureus-gateway/main.py
@D:/Aureus/docker-compose.dev.yml

<interfaces>
Gateway entry points cần giữ nguyên:

```python
async def process_message(r: redis.Redis, data: dict, source: str = "ZMQ") -> bool:
    """Process a single TICK or CANDLE message. Returns True if processed."""

async def handle_tcp_client(reader: asyncio.StreamReader, writer: asyncio.StreamWriter,
                             r: redis.Redis):
    """Handle a single TCP client — reads newline-delimited JSON."""

async def run_tcp_listener(r: redis.Redis):
    """TCP listener on port 5556 for native MQL5 socket connections."""
```

Current dev healthcheck connects and closes port 5556 without JSON:

```yaml
healthcheck:
  test: ["CMD", "python", "-c", "import socket; s=socket.socket(); s.settimeout(2); s.connect(('127.0.0.1', 5556)); s.close(); print('ok')"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce log và chạy GitNexus impact trước sửa</name>
  <files>services/aureus-gateway/main.py, docker-compose.dev.yml</files>
  <behavior>
    - Log lines `Invalid JSON from` phải được đếm từ `docker logs --tail` hoặc `/app/logs/aureus-gateway.log` qua `wsl -d Aureus`.
    - Mỗi nhóm warning phải được phân loại: healthcheck/probe/empty connection, partial payload, hoặc JSON thật sai cú pháp.
    - Trước khi sửa `handle_tcp_client`, bắt buộc chạy GitNexus impact cho symbol này.
    - Nếu GitNexus stale/error, chạy `npx gitnexus analyze` trên môi trường hiện tại rồi retry impact; chỉ fallback thủ công sau khi ghi rõ lỗi analyze/impact retry.
  </behavior>
  <action>Chạy log inspect bằng WSL distro đúng: `wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && docker logs --tail 300 aureus-gateway-dev 2>&1"`. Đối chiếu timestamp warning với Docker healthcheck bằng `wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && docker inspect aureus-gateway-dev"` và healthcheck tại `docker-compose.dev.yml`. Xác định symbol sửa tối thiểu, gần như chắc là `handle_tcp_client`. Trước mọi sửa symbol, bắt buộc chạy `gitnexus_impact({target: "handle_tcp_client", direction: "upstream"})`. Nếu GitNexus báo stale hoặc error, chạy `wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && npx gitnexus analyze"`, rồi retry `gitnexus_impact({target: "handle_tcp_client", direction: "upstream"})`. Chỉ khi retry vẫn fail mới fallback thủ công và ghi rõ lỗi: `GitNexus impact handle_tcp_client failed after analyze: <error>` cùng blast radius thủ công từ file hiện tại (`run_tcp_listener -> handle_tcp_client -> process_message`). Không sửa code trong task này trước khi impact/retry hoàn tất.</action>
  <verify>
    <automated>wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && docker logs --tail 300 aureus-gateway-dev 2>&1 | grep -E 'Invalid JSON|handle_tcp_client|health|TCP Client connected' || true"</automated>
  </verify>
  <done>Có root cause định lượng: số dòng Invalid JSON, peer/source, và kết luận có/không liên quan healthcheck/probe. Có GitNexus impact `handle_tcp_client` hoặc lỗi impact/analyze/retry được ghi rõ trước fallback thủ công.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sửa tối thiểu TCP handler để phân loại probe/non-JSON đúng</name>
  <files>services/aureus-gateway/main.py, services/aureus-gateway/tests/test_tcp_invalid_json_classification.py</files>
  <behavior>
    - TCP line JSON hợp lệ vẫn gọi `process_message` và tăng `msg_count` khi process thành công.
    - Empty/whitespace line bị bỏ qua không warning.
    - Payload non-JSON ngắn/đặc trưng probe được log debug/info phân loại probe nếu nguyên nhân là healthcheck/probe, không warning `Invalid JSON`.
    - Payload giống JSON nhưng sai cú pháp vẫn warning `Invalid JSON` để không che lỗi MT5 thật.
  </behavior>
  <action>Chỉ sửa sau Task 1 impact/retry. Thêm helper nhỏ trong `services/aureus-gateway/main.py` nếu cần, ví dụ phân loại trước `json.loads(text)`: nếu `text` rỗng thì continue; nếu ký tự đầu không thuộc `{` hoặc `[` thì log `logger.debug` hoặc `logger.info` với nhãn `Non-JSON TCP probe/ignored` và continue; nếu bắt `json.JSONDecodeError` từ payload bắt đầu bằng `{` hoặc `[` thì giữ warning `Invalid JSON`. Không thay đổi Pydantic models, Redis keys, command subscriber, ZMQ listener, hay healthcheck nếu không cần. Test mới tập trung vào hàm phân loại/helper hoặc behavior TCP handler bằng mock reader/writer; không cần DB. Pytest bắt buộc chạy qua WSL distro Aureus từ `/mnt/d/Aureus` bằng project venv: `wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-gateway/tests/test_tcp_invalid_json_classification.py -q"`. Sau sửa, chạy `gitnexus_detect_changes()` trước finish; nếu GitNexus stale/error, chạy `wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && npx gitnexus analyze"` rồi retry detect_changes; nếu vẫn fail thì document lỗi chính xác.</action>
  <verify>
    <automated>wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-gateway/tests/test_tcp_invalid_json_classification.py -q"</automated>
  </verify>
  <done>Test chứng minh JSON hợp lệ không hỏng, non-JSON probe không tạo warning, JSON sai thật vẫn warning. GitNexus detect_changes chạy hoặc lỗi retry được ghi rõ.</done>
</task>

<task type="auto">
  <name>Task 3: Restart aureus-gateway-dev và verify ps/logs</name>
  <files>services/aureus-gateway/main.py</files>
  <action>Vì chỉ sửa `.py`, không rebuild. Restart container bằng `wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && docker restart aureus-gateway-dev"`. Không dùng `docker compose up -d --build`. Sau restart, verify container status bằng `docker ps` hoặc `docker compose -f docker-compose.dev.yml ps aureus-gateway-dev`, verify health/logs mới. Dùng `wsl -d Aureus` cho mọi command WSL. Không chạm database.</action>
  <verify>
    <automated>wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && docker restart aureus-gateway-dev && docker ps --filter name=aureus-gateway-dev && docker logs --since 2m aureus-gateway-dev 2>&1 | grep -E 'Invalid JSON|Non-JSON TCP probe|TCP listener started|Order event|Processed' || true"</automated>
  </verify>
  <done>`aureus-gateway-dev` Up sau `docker restart`, logs mới được kiểm tra, healthcheck/probe không còn tạo warning `Invalid JSON`; nếu warning còn, warning phải là payload JSON sai thật với raw/peer đủ để debug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5/healthcheck/probe -> TCP 5556 | Untrusted TCP bytes enter gateway parser |
| Gateway -> Redis | Validated events are published to Redis streams/channels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260513-S4K-01 | D | `handle_tcp_client` | mitigate | Ignore empty/non-JSON probe before expensive validation; keep loop alive. |
| T-260513-S4K-02 | T | `handle_tcp_client` | mitigate | Keep warning for malformed JSON-like payload starting with `{` or `[`; do not silently accept bad MT5 payload. |
| T-260513-S4K-03 | I | gateway logs | mitigate | Do not log full trading payload unless necessary; log peer, classification, and parse error only. |
| T-260513-S4K-04 | R | execution verification | mitigate | Use `wsl -d Aureus` and project venv pytest command so verification runs in same environment as service workflow. |
</threat_model>

<verification>
1. GitNexus impact for `handle_tcp_client` runs before any code edit; stale/error path runs `npx gitnexus analyze` on current environment then retries before manual fallback.
2. Unit test for TCP invalid JSON classification passes via `wsl -d Aureus -u root bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-gateway/tests/test_tcp_invalid_json_classification.py -q"`.
3. Because change is `.py`, restart uses only `docker restart aureus-gateway-dev`, not rebuild.
4. `docker ps`/compose ps confirms `aureus-gateway-dev` Up after restart.
5. `docker logs --since 2m aureus-gateway-dev` no longer shows healthcheck/probe as `Invalid JSON` warning.
6. Gateway still accepts valid MT5 JSON events through `process_message`.
</verification>

<success_criteria>
- Root cause of `Invalid JSON` recorded in executor summary with log counts and peer/source.
- All WSL commands use `wsl -d Aureus`; no `Ubuntu-24.04` commands remain.
- Minimal code change only in gateway parser/classification path unless evidence requires otherwise.
- GitNexus impact requested before editing `handle_tcp_client`; stale/error runs `npx gitnexus analyze` then retries; fallback only after exact error is recorded.
- Pytest uses `./.venv/bin/python -m pytest ...` from `/mnt/d/Aureus` through `wsl -d Aureus`.
- `.py` change deploy uses `docker restart aureus-gateway-dev` and verifies ps/logs; no `--build`.
- No DB files changed; no DB test required.
</success_criteria>

<output>
Sau hoàn thành, tạo `D:/Aureus/.planning/quick/260513-s4k-ki-m-tra-l-i-log-aureus-gateway-dev-v-ph/260513-s4k-SUMMARY.md`.
</output>
