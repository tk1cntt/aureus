---
phase: quick-260425-n8f-update-file-mql5-aureusprovider-mq5-khi-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider.mq5
autonomous: true
requirements:
  - QUICK-260425-N8F
must_haves:
  truths:
    - "Khi OPEN_ORDER fail do INVALID_PRICE/retcode 10015, log terminal hiển thị đủ entry_price, ask, bid để so sánh giá gửi với giá thị trường hiện tại."
    - "ORDER_FAILED JSON gửi về gateway có thêm các field diagnostic entry_price, ask, bid, không thay đổi semantics type/cmd_id/symbol/reason/retcode/t."
    - "Luồng gửi lệnh, retry, OrderCheck, OrderSend, SL/TP/volume calculation không bị đổi hành vi."
  artifacts:
    - path: "mql5/AureusProvider.mq5"
      provides: "Enhanced ORDER_FAILED diagnostics for INVALID_PRICE"
      contains: "entry_price"
  key_links:
    - from: "ExecuteOpenOrder"
      to: "PushOrderFailed"
      via: "failure branches pass request.price, current ask, current bid diagnostics"
      pattern: "PushOrderFailed\\(cmdId, symbol, .*entry_price"
    - from: "PushOrderFailed"
      to: "gateway ORDER_FAILED payload"
      via: "StringFormat JSON includes entry_price/ask/bid"
      pattern: "\\\"entry_price\\\".*\\\"ask\\\".*\\\"bid\\\""
---

<objective>
Cập nhật logging/payload lỗi ORDER_FAILED trong `mql5/AureusProvider.mq5` để khi gặp INVALID_PRICE/retcode 10015 có đủ `entry_price`, `ask`, `bid` hiện tại phục vụ chẩn đoán.

Purpose: Log hiện tại chỉ có `cmd_id`, `reason`, `retcode`, chưa đủ dữ liệu để biết giá entry gửi sang MT5 lệch thế nào so với ask/bid tại thời điểm fail.
Output: Một thay đổi surgical trong MQL5 provider, chỉ bổ sung diagnostic fields/log text, không đổi hành vi execute/retry/API ngoài việc thêm field vào ORDER_FAILED.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@D:/Aureus/mql5/AureusProvider.mq5

<interfaces>
Các symbol/liên kết chính trong file hiện tại:

```mql5
void PushOrderFailed(string cmdId, string symbol, string reason, int retcode)
```
Hiện đang build JSON:
```json
{"type":"ORDER_FAILED","cmd_id":"...","symbol":"...","reason":"...","retcode":10015,"t":...}
```
Cần mở rộng payload bằng số diagnostic `entry_price`, `ask`, `bid` và mở rộng log terminal tương ứng.

```mql5
void ExecuteOpenOrder(const string &raw)
```
Trong `ExecuteOpenOrder`, `entry_price`, `ask`, `bid` đã được tính trước các nhánh `OrderCheck`, `OrderSend`, và `result.retcode != TRADE_RETCODE_DONE`. Các nhánh fail này đang gọi `PushOrderFailed(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bổ sung diagnostic entry/ask/bid vào ORDER_FAILED</name>
  <files>mql5/AureusProvider.mq5</files>
  <action>
Trước khi sửa symbol/function, làm đúng rule dự án: chạy GitNexus impact analysis nếu tool khả dụng cho `PushOrderFailed` và `ExecuteOpenOrder`; nếu GitNexus MCP không khả dụng trong môi trường executor, dùng source-search fallback để liệt kê tất cả call sites của `PushOrderFailed` trong `mql5/AureusProvider.mq5` và ghi rõ limitation trong summary.

Sửa surgical trong `mql5/AureusProvider.mq5`:
1. Đổi chữ ký `PushOrderFailed` để nhận thêm diagnostic optional parameters dạng `double entryPrice = 0.0, double ask = 0.0, double bid = 0.0`. Giữ backward-compatible để các call site không có context giá vẫn compile và vẫn gửi field giá bằng `0.00000` hoặc giá được truyền vào.
2. Mở rộng JSON ORDER_FAILED trong `PushOrderFailed` thêm đúng ba field numeric: `"entry_price":%.5f`, `"ask":%.5f`, `"bid":%.5f`. Không đổi các field hiện có (`type`, `cmd_id`, `symbol`, `reason`, `retcode`, `t`).
3. Mở rộng `PrintFormat` log ORDER_FAILED để in thêm `entry_price=... ask=... bid=...`, để mẫu log INVALID_PRICE có đủ dữ liệu chẩn đoán.
4. Trong `ExecuteOpenOrder`, tại các nhánh fail sau khi đã có `entry_price`, `ask`, `bid`, truyền đúng `entry_price, ask, bid` vào `PushOrderFailed`: `OrderCheck` fail, `OrderSend` fail, `result.retcode != TRADE_RETCODE_DONE`, và hard terminal event gate nếu có. Nhánh `UNKNOWN_ORDER_TYPE` có thể truyền `entry_price, ask, bid` vì đã được lấy trước đó; các fail trước khi có giá giữ default.
5. Không thay đổi `request.price`, `entry_price`, `ask`, `bid`, volume, SL/TP, retcode mapping, retry/idempotency, ACK/NACK, hoặc order execution behavior.
  </action>
  <verify>
    <automated>cd D:/Aureus && python - <<'PY'
from pathlib import Path
p = Path('mql5/AureusProvider.mq5')
s = p.read_text(encoding='utf-8', errors='ignore')
assert 'entryPrice = 0.0' in s or 'entry_price = 0.0' in s
assert '\"entry_price\"' in s and '\"ask\"' in s and '\"bid\"' in s
assert 'ORDER_FAILED pushed: cmd_id=%s reason=%s retcode=%d' in s
assert 'PushOrderFailed(cmdId, symbol, RetcodeToReason(checkResult.retcode), checkResult.retcode, entry_price, ask, bid)' in s
assert 'PushOrderFailed(cmdId, symbol, RetcodeToReason(result.retcode), result.retcode, entry_price, ask, bid)' in s
print('ORDER_FAILED diagnostics present')
PY</automated>
  </verify>
  <done>`PushOrderFailed` JSON/log chứa `entry_price`, `ask`, `bid`; các failure path chính trong `ExecuteOpenOrder` truyền giá thực tế hiện có; không có thay đổi hành vi execute.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Kiểm tra scope và không đổi hành vi ngoài logging</name>
  <files>mql5/AureusProvider.mq5</files>
  <action>
Review diff thủ công để đảm bảo chỉ thay đổi diagnostic ORDER_FAILED. Xác nhận không sửa logic tính `entry_price`, `request.price`, `request.sl`, `request.tp`, `volume`, `OrderCheck`, `OrderSend`, `RecordCmdId`, ACK/NACK, hoặc retry behavior. Chạy `gitnexus_detect_changes()` trước khi commit nếu GitNexus khả dụng; nếu không khả dụng, ghi limitation và dùng `git diff -- mql5/AureusProvider.mq5` để xác nhận chỉ file này đổi.
  </action>
  <verify>
    <automated>cd D:/Aureus && git diff -- mql5/AureusProvider.mq5</automated>
  </verify>
  <done>Diff chỉ có thay đổi chữ ký/call-site `PushOrderFailed` và JSON/log diagnostic; không có DB change; không có thay đổi execution semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 provider → gateway socket | ORDER_FAILED JSON được gửi qua TCP socket nội bộ; dữ liệu diagnostic là giá market/order, không phải secret. |
| Gateway command → MT5 execution | OPEN_ORDER raw JSON vẫn là input không tin cậy, nhưng plan này không thay đổi parse/validation/execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260425-n8f-01 | I | `PushOrderFailed` payload/log | accept | `entry_price`, `ask`, `bid` là market diagnostics cần thiết để debug INVALID_PRICE; không chứa secret/credential/PII. |
| T-260425-n8f-02 | T | `ExecuteOpenOrder` failure paths | mitigate | Chỉ truyền read-only diagnostic values vào failure payload; không sửa `MqlTradeRequest` hoặc retry/order behavior. |
| T-260425-n8f-03 | D | ORDER_FAILED JSON size | accept | Thêm 3 numeric fields rất nhỏ, không đáng kể với socket payload hiện tại. |
</threat_model>

<verification>
- Automated static check trong Task 1 phải pass.
- `git diff -- mql5/AureusProvider.mq5` cho thấy chỉ thay đổi diagnostic ORDER_FAILED.
- Nếu có môi trường MetaEditor/MT5 compiler sẵn có, executor nên compile `AureusProvider.mq5`; nếu không có, ghi rõ chưa compile được do tool/runtime MT5 không khả dụng.
</verification>

<success_criteria>
- Log terminal dạng `[AureusProvider] ORDER_FAILED pushed: cmd_id=... reason=INVALID_PRICE retcode=10015 entry_price=... ask=... bid=...`.
- ORDER_FAILED JSON có thêm `entry_price`, `ask`, `bid` numeric fields.
- Không đổi order execution, retry/idempotency, ACK/NACK, SL/TP/volume calculation.
- Chỉ file `D:/Aureus/mql5/AureusProvider.mq5` được sửa cho implementation.
</success_criteria>

<output>
Sau khi hoàn tất, tạo summary tại `D:/Aureus/.planning/quick/260425-n8f-update-file-mql5-aureusprovider-mq5-khi-/260425-n8f-SUMMARY.md`.
</output>
