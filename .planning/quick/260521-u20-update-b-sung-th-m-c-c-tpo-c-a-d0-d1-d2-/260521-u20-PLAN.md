---
phase: quick-260521-u20-update-tpo-d0-d3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/tpo.py
  - services/aureus-signal/engine/indicator_snapshot.py
  - services/aureus-signal/engine/signal_event_publisher.py
  - services/aureus-trader/journal.py
  - services/aureus-db-writer/migrations/add_trade_signal_snapshots_tpo_d0_d3.sql
  - services/aureus-signal/tests/*
  - services/aureus-trader/tests/*
autonomous: true
requirements:
  - QUICK-260521-U20
must_haves:
  truths:
    - "Signal TPO snapshot có đủ D0, D1, D2, D3 theo nghĩa D0=hôm nay UTC, D1=hôm qua UTC, D2=hai ngày trước UTC, D3=ba ngày trước UTC."
    - "Trade snapshot lưu đủ POC/VAH/VAL/O/H/L/C cho D0-D3 vào bảng aureus_trade_signal_snapshots."
    - "Dữ liệu không còn dùng D1 hiện tại để đại diện hôm nay; hôm nay phải là D0."
    - "DB E2E với aureus_timescaledb_dev chứng minh cột mới được insert có giá trị thật."
  artifacts:
    - path: "services/aureus-signal/engine/signals/tpo.py"
      provides: "Tính TPO daily buckets D0-D3"
      contains: "tpo_d0"
    - path: "services/aureus-signal/engine/indicator_snapshot.py"
      provides: "Đưa tpo_d0..tpo_d3 vào indicator snapshot"
      contains: "tpo_d3"
    - path: "services/aureus-signal/engine/signal_event_publisher.py"
      provides: "Copy tpo_d0..tpo_d3 vào signal_snapshot"
      contains: "tpo_d2"
    - path: "services/aureus-trader/journal.py"
      provides: "Map tpo_d0..tpo_d3 vào aureus_trade_signal_snapshots insert columns"
      contains: "d3_poc"
    - path: "services/aureus-db-writer/migrations/add_trade_signal_snapshots_tpo_d0_d3.sql"
      provides: "Schema migration thêm cột TPO D0-D3"
      contains: "d0_poc"
  key_links:
    - from: "services/aureus-signal/engine/signals/tpo.py"
      to: "state.tpo_profile"
      via: "daily profile keys tpo_d0, tpo_d1, tpo_d2, tpo_d3"
      pattern: "tpo_d[0-3]"
    - from: "services/aureus-signal/engine/indicator_snapshot.py"
      to: "services/aureus-signal/engine/signal_event_publisher.py"
      via: "indicator snapshot tpo profile payload"
      pattern: "tpo_profile"
    - from: "services/aureus-signal/engine/signal_event_publisher.py"
      to: "services/aureus-trader/journal.py"
      via: "signal_snapshot fields copied unchanged"
      pattern: "tpo_d[0-3]"
    - from: "services/aureus-trader/journal.py"
      to: "aureus_trade_signal_snapshots"
      via: "insert column mapping"
      pattern: "d[0-3]_(poc|vah|val|open|high|low|close)"
---

<objective>
Bổ sung TPO daily buckets D0-D3 xuyên suốt từ signal engine đến trade snapshot DB.

Purpose: user cần lưu TPO của hôm nay, hôm qua, hai ngày trước, ba ngày trước tại thời điểm vào lệnh để phân tích trade snapshot đúng ngữ nghĩa ngày.
Output: source update, migration/schema update, tests, DB E2E xác nhận insert vào aureus_trade_signal_snapshots.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/RUN_SERVICES.md
@D:/Aureus/.planning/STATE.md

Điều tra hiện tại:
- Current D1 TPO nằm trong services/aureus-signal/engine/signals/tpo.py qua _compute_d1: day_start = now_ts - (now_ts % 86400), session day_start..now_ts. Đây thực chất là current UTC day running TPO.
- Path lưu hiện tại: indicator_snapshot.py đọc state.tpo_profile["tpo_d1"]; signal_event_publisher.py copy tpo_d1 vào signal_snapshot; services/aureus-trader/journal.py extract tpo_d1 vào d1_poc/d1_vah/d1_val/d1_open/d1_high/d1_low/d1_close rồi insert aureus_trade_signal_snapshots.
- User yêu cầu D0, D1, D2, D3: D0 hôm nay, D1 hôm qua, D2 hai ngày trước, D3 ba ngày trước. Lưu vào trade snapshot giống field TPO hiện tại.
- Vì liên quan database, bắt buộc test E2E với DB thật aureus_timescaledb_dev hoặc cách chạy chuẩn trong RUN_SERVICES.md.
- Trên Windows, tất cả verify dùng Python/Docker/psql phải chạy qua WSL theo dạng `wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ..."`; không chạy trực tiếp `cd D:/Aureus && python/docker/psql ...`.
- Surgical only. Không fallback rộng, không đổi behavior khác.
- Trước khi sửa symbol, bắt buộc chạy GitNexus impact upstream cho từng function/class/method sẽ sửa và báo blast radius.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tính và propagate TPO D0-D3 trong signal snapshot</name>
  <files>services/aureus-signal/engine/signals/tpo.py, services/aureus-signal/engine/indicator_snapshot.py, services/aureus-signal/engine/signal_event_publisher.py, services/aureus-signal/tests/*</files>
  <behavior>
    - Với now_ts nằm trong ngày N UTC, tpo_d0 dùng session từ N 00:00:00 UTC đến now_ts.
    - tpo_d1 dùng full UTC day N-1, tpo_d2 dùng full UTC day N-2, tpo_d3 dùng full UTC day N-3.
    - Existing current-day TPO không còn xuất hiện dưới key tpo_d1.
    - indicator snapshot và signal_snapshot giữ đủ tpo_d0, tpo_d1, tpo_d2, tpo_d3 payload keys.
  </behavior>
  <action>Chạy gitnexus_impact upstream cho các symbol sẽ sửa trong services/aureus-signal/engine/signals/tpo.py, services/aureus-signal/engine/indicator_snapshot.py, services/aureus-signal/engine/signal_event_publisher.py trước khi edit; báo direct callers, affected flows, risk. Sau đó đổi logic daily TPO: current-day running profile là tpo_d0; tạo thêm closed daily profiles tpo_d1/tpo_d2/tpo_d3 bằng UTC day boundaries lùi 1/2/3 ngày, dùng cùng cấu trúc profile hiện tại (poc/vah/val/open/high/low/close/shape nếu đang có). Không đổi H1/M30 cache/sliding behavior. Update indicator_snapshot.py đọc/copy cả 4 keys từ state.tpo_profile. Update signal_event_publisher.py copy cả 4 keys vào signal_snapshot. Thêm hoặc sửa tests hiện có để assert boundaries D0-D3 và propagation keys; nếu không có test phù hợp, tạo test nhỏ tập trung vào function hiện hữu, không build framework mới.</action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "test -d /mnt/d/Aureus || { echo 'Missing /mnt/d/Aureus; read RUN_SERVICES.md and set correct WSL repo path'; exit 2; }; cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-signal/tests -q -x"</automated>
  </verify>
  <done>state.tpo_profile, indicator snapshot, signal_snapshot đều có tpo_d0/tpo_d1/tpo_d2/tpo_d3 đúng ngữ nghĩa ngày UTC; tests signal pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mở rộng journal mapping và migration trade snapshot</name>
  <files>services/aureus-trader/journal.py, services/aureus-db-writer/migrations/add_trade_signal_snapshots_tpo_d0_d3.sql, services/aureus-trader/tests/*</files>
  <behavior>
    - journal.py extract POC/VAH/VAL/O/H/L/C cho từng tpo_d0, tpo_d1, tpo_d2, tpo_d3.
    - aureus_trade_signal_snapshots có columns d0_poc/d0_vah/d0_val/d0_open/d0_high/d0_low/d0_close đến d3_poc/d3_vah/d3_val/d3_open/d3_high/d3_low/d3_close.
    - Existing d1_* semantics đổi sang yesterday theo signal_snapshot tpo_d1; nếu DB đã có d1_* thì giữ tên nhưng đổi source key sang tpo_d1 yesterday, thêm d0_* và d2_/d3_* mới.
    - Missing TPO payload để NULL như pattern hiện tại, không fake/default value.
  </behavior>
  <action>Chạy gitnexus_impact upstream cho journal symbols sẽ sửa trước khi edit; báo blast radius. Dùng migration path thật `services/aureus-db-writer/migrations/`; tạo file concrete `services/aureus-db-writer/migrations/add_trade_signal_snapshots_tpo_d0_d3.sql` theo naming pattern descriptive hiện tại (`add_trade_journal.sql`, `optimize_trade_signal_snapshots_storage.sql`), không dùng path giả `services/aureus-trader/migrations`. Migration surgical: thêm cột d0_*, d2_*, d3_* nếu d1_* đã tồn tại; nếu d1_open/high/low/close chưa tồn tại trong branch này thì thêm đủ cột còn thiếu theo request. Update journal.py extraction/mapping/insert columns để lấy signal_snapshot["tpo_d0".."tpo_d3"] và map sang column prefix d0..d3. Không đổi unrelated journal lifecycle, trace_id, pending order mapping, reasoning bank logic. Thêm unit/integration test cho _build_signal_snapshot_columns hoặc helper tương đương để assert các cột d0-d3 nhận đúng numeric values từ payload.</action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "test -d /mnt/d/Aureus || { echo 'Missing /mnt/d/Aureus; read RUN_SERVICES.md and set correct WSL repo path'; exit 2; }; cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests -q -x"</automated>
  </verify>
  <done>Migration có tên file concrete đúng naming/order pattern hiện tại và có cột DB cần thiết; journal insert mapping đầy đủ d0-d3; tests trader pass; không có fallback/hardcode.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy DB E2E thật và kiểm scope GitNexus</name>
  <files>services/aureus-trader/tests/*, services/aureus-db-writer/migrations/add_trade_signal_snapshots_tpo_d0_d3.sql, RUN_SERVICES.md</files>
  <action>Thứ tự bắt buộc: (1) DB preflight/service start theo RUN_SERVICES.md: chạy mọi Python/Docker/psql qua WSL, kiểm tra `/mnt/d/Aureus` tồn tại trước; nếu không tồn tại, đọc RUN_SERVICES.md và dùng WSL repo path đúng, không chạy trực tiếp Windows host. (2) Áp dụng migration `services/aureus-db-writer/migrations/add_trade_signal_snapshots_tpo_d0_d3.sql` vào `aureus_timescaledb_dev`. (3) Tạo hoặc cập nhật một DB E2E test/command trong services/aureus-trader/tests để insert một trade snapshot bằng actual journal/snapshot path nếu khả dụng; nếu path đó cần quá nhiều runtime dependency, dùng direct snapshot insert helper hiện có và ghi rõ trong test comment vì mục tiêu là chứng minh DB schema + journal column values. Test phải dùng deterministic trace_id/test id cố định `tpo-d0-d3-e2e-plan-u20-fixed`; cleanup row có đúng trace_id/test id này trước và sau test; signal_snapshot chứa tpo_d0/tpo_d1/tpo_d2/tpo_d3 với POC/VAH/VAL/O/H/L/C khác nhau và expected values cố định. (4) Sau insert, query aureus_trade_signal_snapshots bằng đúng trace_id/test id `tpo-d0-d3-e2e-plan-u20-fixed`, không dùng ORDER BY created_at DESC LIMIT 1, rồi assert tất cả d0_*..d3_* values match expected values. (5) Sau code changes, chạy gitnexus_detect_changes() để xác nhận scope chỉ gồm TPO signal propagation, journal mapping, migration, tests. Nếu GitNexus báo index stale, chạy npx gitnexus analyze trên môi trường hiện tại trước rồi detect lại.</action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "test -d /mnt/d/Aureus || { echo 'Missing /mnt/d/Aureus; read RUN_SERVICES.md and set correct WSL repo path'; exit 2; }; cd /mnt/d/Aureus && docker ps --format '{{.Names}}' | grep -q aureus_timescaledb_dev && ./.venv/bin/python -m pytest services/aureus-trader/tests -q -x"</automated>
    <automated>wsl -d Aureus -e bash -lc "test -d /mnt/d/Aureus || { echo 'Missing /mnt/d/Aureus; read RUN_SERVICES.md and set correct WSL repo path'; exit 2; }; cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests -q -x -k 'tpo_d0_d3 and db'"</automated>
    <automated>wsl -d Aureus -e bash -lc "test -d /mnt/d/Aureus || { echo 'Missing /mnt/d/Aureus; read RUN_SERVICES.md and set correct WSL repo path'; exit 2; }; docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -v trace_id='tpo-d0-d3-e2e-plan-u20-fixed' -c \"SELECT trace_id,d0_poc,d0_vah,d0_val,d0_open,d0_high,d0_low,d0_close,d1_poc,d1_vah,d1_val,d1_open,d1_high,d1_low,d1_close,d2_poc,d2_vah,d2_val,d2_open,d2_high,d2_low,d2_close,d3_poc,d3_vah,d3_val,d3_open,d3_high,d3_low,d3_close FROM aureus_trade_signal_snapshots WHERE trace_id = :'trace_id';\""</automated>
  </verify>
  <done>DB E2E pass với aureus_timescaledb_dev; test insert row mới với deterministic trace_id/test id `tpo-d0-d3-e2e-plan-u20-fixed`, cleanup trước/sau bằng đúng id, query đúng row bằng trace_id/test id đó, assert đủ d0_poc/d0_vah/d0_val/d0_open/d0_high/d0_low/d0_close qua d3_poc/d3_vah/d3_val/d3_open/d3_high/d3_low/d3_close match expected values; không có verify nào dựa vào latest row; gitnexus_detect_changes scope đúng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal-engine to trader journal | signal_snapshot payload vượt service boundary; journal phải tolerate missing TPO keys without fake data |
| app to TimescaleDB | migration/insert thay đổi schema DB thật aureus_trade_signal_snapshots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260521-u20-01 | T | journal.py snapshot mapping | mitigate | Map explicit keys tpo_d0..tpo_d3 to explicit DB columns; no dynamic SQL column names from payload. |
| T-260521-u20-02 | I | aureus_trade_signal_snapshots | accept | TPO levels are market-derived trade context, no new secrets/PII. |
| T-260521-u20-03 | D | tpo.py daily profile calculation | mitigate | Keep closed-day windows bounded to 3 days; do not alter H1/M30 cache behavior; tests assert UTC day boundaries. |
| T-260521-u20-04 | R | DB E2E verification | mitigate | Insert/query with trace_id/test id and record command output in execution summary. |
</threat_model>

<verification>
- Signal tests pass: wsl -d Aureus -e bash -lc "test -d /mnt/d/Aureus || { echo 'Missing /mnt/d/Aureus; read RUN_SERVICES.md and set correct WSL repo path'; exit 2; }; cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-signal/tests -q -x"
- Trader tests pass: wsl -d Aureus -e bash -lc "test -d /mnt/d/Aureus || { echo 'Missing /mnt/d/Aureus; read RUN_SERVICES.md and set correct WSL repo path'; exit 2; }; cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests -q -x"
- DB E2E pass against aureus_timescaledb_dev with deterministic trace_id/test id `tpo-d0-d3-e2e-plan-u20-fixed`; cleanup trước/sau; query bằng đúng id; assert đủ d0_*..d3_* values.
- gitnexus_detect_changes() confirms only expected TPO snapshot flow changed.
</verification>

<success_criteria>
- D0 hôm nay UTC, D1 hôm qua UTC, D2 hai ngày trước UTC, D3 ba ngày trước UTC.
- signal_snapshot contains tpo_d0, tpo_d1, tpo_d2, tpo_d3.
- aureus_trade_signal_snapshots stores POC/VAH/VAL/O/H/L/C for D0-D3.
- Real DB row in aureus_timescaledb_dev proves insert works.
- No unrelated lifecycle/journal/MT5 behavior changed.
</success_criteria>

<output>
Sau khi xong, tạo D:/Aureus/.planning/quick/260521-u20-update-b-sung-th-m-c-c-tpo-c-a-d0-d1-d2-/260521-u20-SUMMARY.md
</output>
