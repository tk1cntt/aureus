---
quick_id: 260531-s85
mode: quick-full
status: planned
must_haves:
  truths:
    - FZ_CONT_BULL and FZ_CONT_BEAR definitions in seed_strategies.py remain canonical for CHOCH to BOS sequence and LIMIT execution expectations.
    - Unit tests must execute strategy sequence logic: CHOCH alone no trigger, CHOCH then BOS triggers, wrong/missing BOS no trigger for both strategies.
    - DB seed path must make both strategies available/active so they can run.
    - DB-related change must be verified with an E2E DB seed test that creates data.
  artifacts:
    - services/aureus-signal/tests/test_seed_strategies_fz_cont.py
    - services/aureus-signal/tests/test_seed_strategies_fz_cont_db.py
    - services/aureus-signal/engine/strategies/seed_strategies.py
    - .planning/quick/260531-s85-fz-cont-tests-db-seed/260531-s85-SUMMARY.md
  key_links:
    - services/aureus-signal/engine/strategies/seed_strategies.py
    - RUN_SERVICES.md
---

# Quick Task 260531-s85 Plan

## Goal

Implement unittest kiểm tra logic xử lý theo `FZ_CONT_BEAR` và `FZ_CONT_BULL` đang định nghĩa trong `services/aureus-signal/engine/strategies/seed_strategies.py`, đồng thời update/verify DB seed để 2 strategies này chạy được.

## Assumptions

- Không đổi semantics strategy nếu definitions hiện tại đã đúng.
- "Update db" nghĩa DB seed/upsert tạo `aureus_strategy_templates` và `aureus_symbol_strategies` active cho `FZ_CONT_BULL` và `FZ_CONT_BEAR`.
- Trước khi sửa symbol trong `seed_strategies.py`, executor phải chạy `gitnexus_impact({target: "seed_system_strategies", direction: "upstream"})` và báo blast radius. Nếu GitNexus tool unavailable, dừng trước khi sửa symbol.

## Tasks

### Task 1 — Add runtime unit coverage for FZ strategy logic

files:
- `services/aureus-signal/tests/test_seed_strategies_fz_cont.py`
- `services/aureus-signal/engine/strategies/seed_strategies.py`

action:
- Inspect existing strategy runtime tests and reuse project style.
- Seed/read `FZ_CONT_BULL` and `FZ_CONT_BEAR` configs from `seed_system_strategies`, not copied literals.
- Build existing strategy processor (`TemplateStrategy` or current equivalent) from seeded configs.
- Assert `FZ_CONT_BULL` config and runtime behavior:
  - sequence `choch_up` then `bos_up`, both required, max_wait 30
  - `choch_up` alone does not trigger
  - `choch_up` then `bos_up` triggers BUY LIMIT
  - wrong/missing BOS does not trigger
  - output uses `entry_method == FIRST_HIGH_LOW_PIVOT`, SL type `SECOND_HIGH_LOW_PIVOT`, early exit `choch_down`
- Assert `FZ_CONT_BEAR` config and runtime behavior:
  - sequence `choch_down` then `bos_down`, both required, max_wait 30
  - `choch_down` alone does not trigger
  - `choch_down` then `bos_down` triggers SELL LIMIT
  - wrong/missing BOS does not trigger
  - output uses `entry_method == FIRST_LOW_HIGH_PIVOT`, SL type `SECOND_LOW_HIGH_PIVOT`, early exit `choch_up`

verify:
- Run `cd D:/Aureus/services/aureus-signal && pytest tests/test_seed_strategies_fz_cont.py -q`.

done:
- Both FZ definitions are covered by unit tests matching canonical config and sequence processing behavior.

### Task 2 — Add DB E2E seed verification for FZ strategies

files:
- `services/aureus-signal/tests/test_seed_strategies_fz_cont_db.py`
- `services/aureus-signal/engine/strategies/seed_strategies.py`

action:
- Before any edit to `seed_system_strategies`, run `gitnexus_impact({target: "seed_system_strategies", direction: "upstream"})` and report direct callers, affected processes, risk level. If GitNexus unavailable, stop before editing `seed_strategies.py`.
- Add DB-backed pytest `services/aureus-signal/tests/test_seed_strategies_fz_cont_db.py` that connects to configured test DB, cleans FZ rows for test symbol, runs `seed_system_strategies`, and validates created data:
  - both template rows exist
  - both symbol strategy rows exist with `is_active = true`
  - configs are JSON with expected sequence/execution fields
- If existing seed logic misses either FZ strategy, make minimal surgical fix only in FZ blocks or seed activation path.
- If DB connection fails, read `D:/Aureus/RUN_SERVICES.md`, start required DB service, and rerun DB test. Do not mark done until DB E2E passes.

verify:
- Run `cd D:/Aureus/services/aureus-signal && pytest tests/test_seed_strategies_fz_cont_db.py -q`.

done:
- DB E2E test passes and proves rows are created/active for `FZ_CONT_BULL` and `FZ_CONT_BEAR`.

### Task 3 — Run targeted suite and document evidence

files:
- `.planning/quick/260531-s85-fz-cont-tests-db-seed/260531-s85-SUMMARY.md`

action:
- Run full targeted suite:
  - `cd D:/Aureus/services/aureus-signal && pytest tests/test_seed_strategies_fz_cont.py tests/test_seed_strategies_fz_cont_db.py -q`
- Before final commit, run `gitnexus_detect_changes()` if available; otherwise run `git diff -- services/aureus-signal/engine/strategies/seed_strategies.py services/aureus-signal/tests/test_seed_strategies_fz_cont.py services/aureus-signal/tests/test_seed_strategies_fz_cont_db.py` and confirm only expected files changed.
- Write summary with commands, results, changed files, DB E2E evidence, and GitNexus impact/detect status.

verify:
- Targeted suite passes.
- DB E2E passes.

done:
- SUMMARY.md exists and contains passing unit + DB E2E evidence.
