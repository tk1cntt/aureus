---
phase: quick-260424-1d1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/main.py
  - services/aureus-trader/dispatcher.py
  - services/aureus-trader/tests/test_dispatcher.py
autonomous: true
requirements:
  - QUICK-260424-1D1
must_haves:
  truths:
    - "Khi ORDER_OPENED được xác nhận trong dispatch_order, journal strategy_match được trigger ở đúng ngữ cảnh xử lý lệnh."
    - "trace_id của strategy event được bảo toàn để journal có thể liên kết đầy đủ strategy_match -> order_opened."
    - "Không còn gọi journal.on_strategy_match tại main.py sau khi enqueue."
  artifacts:
    - path: "services/aureus-trader/dispatcher.py"
      provides: "Điểm gọi journal.on_strategy_match(event) được chuyển vào nhánh final.type == ORDER_OPENED trong dispatch_order"
    - path: "services/aureus-trader/main.py"
      provides: "Loại bỏ fire-and-forget strategy_match cũ ở luồng nhận STRATEGY_MATCH"
    - path: "services/aureus-trader/tests/test_dispatcher.py"
      provides: "Test chứng minh dispatch_order truyền trace_id và gọi journal theo luồng mới"
  key_links:
    - from: "services/aureus-trader/main.py"
      to: "services/aureus-trader/dispatcher.py"
      via: "order_cmd payload phải mang đủ dữ liệu event/trace để dispatcher journal hóa"
      pattern: "enqueue_order\(order_cmd\)"
    - from: "services/aureus-trader/dispatcher.py::dispatch_order"
      to: "TradeJournalManager.on_strategy_match"
      via: "ORDER_OPENED branch"
      pattern: "final.get\(\"type\"\) == \"ORDER_OPENED\""
---

<objective>
Di chuyển logic `asyncio.create_task(journal.on_strategy_match(event))` từ `main.py` vào `dispatch_order` khi nhận `ORDER_OPENED`, đồng thời bổ sung luồng truyền `trace_id`/event context để journal ghi nhận đúng quan hệ strategy-order.

Purpose: Đồng bộ thời điểm journal hóa strategy_match với thời điểm lệnh thực sự mở thành công, tránh ghi sớm khi lệnh chưa mở.
Output: Luồng journal mới nằm trong dispatcher + test tự động xác nhận trace_id/context.
</objective>

<context>
@D:/Aureus/services/aureus-trader/main.py
@D:/Aureus/services/aureus-trader/dispatcher.py
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md

<interfaces>
From services/aureus-trader/dispatcher.py:
```python
class OrderDispatcher:
    async def dispatch_order(self, order: dict)
```

From services/aureus-trader/main.py:
```python
order_cmd = build_order_command(event)
enqueued = await dispatcher.enqueue_order(order_cmd)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Viết test RED cho luồng journal strategy_match trong ORDER_OPENED</name>
  <files>services/aureus-trader/tests/test_dispatcher.py</files>
  <behavior>
    - Test 1: Khi `dispatch_order` nhận final event `ORDER_OPENED`, dispatcher gọi `journal.on_strategy_match(...)` trước/sát với `journal.on_order_opened(...)` trong cùng ngữ cảnh xử lý order.
    - Test 2: payload truyền vào `on_strategy_match` có `trace_id` đúng như strategy event gốc (không mất khi đi qua order_cmd).
    - Test 3: Không gọi `on_strategy_match` nếu final result không phải `ORDER_OPENED`.
  </behavior>
  <action>Tạo/ cập nhật test dispatcher với mock redis + mock journal để mô phỏng ACK -> ORDER_OPENED. Test phải fail ban đầu do logic chưa chuyển.</action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -k "strategy_match or trace_id" -x</automated>
  </verify>
  <done>Có test mô tả đúng 3 hành vi trên và đang RED trước khi sửa production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chuyển điểm gọi journal từ main.py vào dispatch_order và bổ sung trace context</name>
  <files>services/aureus-trader/main.py, services/aureus-trader/dispatcher.py</files>
  <behavior>
    - `main.py` không còn gọi trực tiếp `journal.on_strategy_match(event)` ngay sau enqueue.
    - `dispatch_order` tại nhánh `final.get("type") == "ORDER_OPENED"` thực hiện journal strategy_match bằng context gắn với order đang xử lý.
    - `trace_id` được preserve end-to-end: strategy event -> order_cmd -> dispatcher final payload/strategy payload.
  </behavior>
  <action>
    1) Ở `main.py`, loại bỏ fire-and-forget cũ sau enqueue.
    2) Đảm bảo `order_cmd` mang đủ dữ liệu tối thiểu để dispatcher có thể gọi `on_strategy_match` (ít nhất trace_id và event context cần cho journal).
    3) Ở `dispatcher.py` trong nhánh ORDER_OPENED, gọi `journal.on_strategy_match(...)` rồi `journal.on_order_opened(final)` theo cùng trace context; không thêm abstraction mới ngoài scope quick task.
  </action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -k "strategy_match or trace_id" -x</automated>
  </verify>
  <done>Luồng journal strategy_match chỉ diễn ra khi ORDER_OPENED và trace_id được truyền đúng, test Task 1 chuyển GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy regression test focused cho trader journal flow</name>
  <files>services/aureus-trader/tests/test_dispatcher.py</files>
  <action>Chạy lại nhóm test liên quan dispatch + journal để đảm bảo không phá retry/ACK/ORDER_FAILED flow hiện có sau khi di chuyển logic.</action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -x</automated>
  </verify>
  <done>Tất cả test dispatcher pass; không có regression ở các nhánh non-ORDER_OPENED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis event -> trader service | Dữ liệu event không đáng tin cậy đi vào logic journal |
| trader -> journal manager | Payload nội bộ có thể thiếu/ sai trace_id nếu không preserve đúng |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260424-1d1-01 | T | services/aureus-trader/dispatcher.py | mitigate | Chỉ trigger on_strategy_match ở nhánh ORDER_OPENED đã qua ACK/result flow, tránh ghi nhận sai trạng thái |
| T-quick-260424-1d1-02 | R | services/aureus-trader/main.py + dispatcher.py | mitigate | Preserve trace_id xuyên suốt payload để truy vết được strategy_match/order_opened trong journal |
| T-quick-260424-1d1-03 | D | services/aureus-trader/dispatcher.py | accept | Vẫn dùng xử lý async hiện có; scope quick task không thay đổi cơ chế queue/retry để tránh mở rộng phạm vi |
</threat_model>

<verification>
- Focus test: `pytest services/aureus-trader/tests/test_dispatcher.py -k "strategy_match or trace_id" -x`
- Full dispatcher regression: `pytest services/aureus-trader/tests/test_dispatcher.py -x`
</verification>

<success_criteria>
- Không còn `asyncio.create_task(journal.on_strategy_match(event))` trong `main.py`.
- `dispatch_order` gọi journal strategy_match tại nhánh `ORDER_OPENED` và giữ đúng trace_id.
- Test tự động cho luồng mới pass, không làm hỏng các nhánh dispatch khác.
</success_criteria>

<output>
After completion, create `.planning/quick/260424-1d1-chuy-n-ph-n-x-ly-asyncio-create-task-jou/260424-1d1-SUMMARY.md`
</output>
