---
phase: 260512-9xw-ta-o-2-strategy-clone-t-trend-cont-bull-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/snapshot_utils.py
  - services/aureus-signal/engine/orders.py
  - services/aureus-signal/engine/simulated_orders.py
  - services/aureus-signal/engine/strategies/seed_strategies.py
  - services/aureus-signal/tests/test_entry_price_methods.py
  - services/aureus-signal/tests/test_strategy_seed_sync.py
autonomous: true
requirements:
  - QUICK-260512-9XW
must_haves:
  truths:
    - "Có 2 strategy clone mới từ TREND_CONT_BULL và TREND_CONT_BEAR, chỉ vào lệnh LIMIT sau CHOCH nếu tìm thấy FVG phù hợp."
    - "BUY clone lấy FVG bullish đầu tiên tính từ pivot LL tương ứng; SELL clone lấy FVG bearish đầu tiên tính từ pivot HH tương ứng."
    - "Không có FVG hợp lệ sau pivot CHOCH thì strategy bị reject, không tạo order, không ghi history duplicate."
    - "Entry type của 2 strategy clone là LIMIT, không ảnh hưởng MARKET TREND_CONT_BULL/TREND_CONT_BEAR hiện tại."
  artifacts:
    - path: "services/aureus-signal/engine/snapshot_utils.py"
      provides: "VALID_ENTRY_METHODS chứa ENTRY_FVG_FROM_CHOCH_PIVOT"
      contains: "ENTRY_FVG_FROM_CHOCH_PIVOT"
    - path: "services/aureus-signal/engine/orders.py"
      provides: "Live entry price method lấy FVG đầu tiên sau pivot HH/LL"
      contains: "_entry_fvg_from_choch_pivot"
    - path: "services/aureus-signal/engine/simulated_orders.py"
      provides: "Backtest/simulated entry price method tương thích ENTRY_FVG_FROM_CHOCH_PIVOT"
      contains: "ENTRY_FVG_FROM_CHOCH_PIVOT"
    - path: "services/aureus-signal/engine/strategies/seed_strategies.py"
      provides: "Seed 2 strategy clone TREND_CONT_FVG_BULL/TREND_CONT_FVG_BEAR"
      contains: "TREND_CONT_FVG_BULL"
    - path: "services/aureus-signal/tests/test_entry_price_methods.py"
      provides: "New regression tests cho FVG entry price và no-FVG rejection"
      contains: "ENTRY_FVG_FROM_CHOCH_PIVOT"
    - path: "services/aureus-signal/tests/test_strategy_seed_sync.py"
      provides: "New seed sync tests cho 2 strategy clone mới"
      contains: "TREND_CONT_FVG_BULL"
  key_links:
    - from: "services/aureus-signal/engine/strategies/seed_strategies.py"
      to: "services/aureus-signal/engine/snapshot_utils.py"
      via: "trade_execution.entry_method uses VALID_ENTRY_METHODS"
      pattern: "ENTRY_FVG_FROM_CHOCH_PIVOT"
    - from: "services/aureus-signal/engine/orders.py"
      to: "state_obj.swing_points/state_obj.fvgs"
      via: "_calculate_entry_price routes to _entry_fvg_from_choch_pivot"
      pattern: "elif method == \"ENTRY_FVG_FROM_CHOCH_PIVOT\""
    - from: "services/aureus-signal/engine/orders.py"
      to: "ORDER_REJECTED stream"
      via: "computed_entry None causes ENTRY_PRICE_UNAVAILABLE rejection"
      pattern: "ENTRY_PRICE_UNAVAILABLE"
---

# Kế hoạch quick 260512-9xw

## Mục tiêu

Tạo 2 strategy clone từ `TREND_CONT_BULL` và `TREND_CONT_BEAR`:

- `TREND_CONT_FVG_BULL`: sau `choch_up`, đặt `BUY LIMIT` tại FVG bullish đầu tiên được tìm thấy tính từ pivot `LL` tương ứng.
- `TREND_CONT_FVG_BEAR`: sau `choch_down`, đặt `SELL LIMIT` tại FVG bearish đầu tiên được tìm thấy tính từ pivot `HH` tương ứng.
- Nếu không có FVG phù hợp thì không vào lệnh.

Mục đích: strategy chỉ vào lệnh khi giá xảy ra CHOCH và có FVG, tránh clone market entry hiện tại.

## Ngữ cảnh

- `D:/Aureus/CLAUDE.md`: bắt buộc tiếng Việt, thay đổi surgical, GitNexus impact trước sửa symbol, `gitnexus_detect_changes()` trước commit.
- `D:/Aureus/services/aureus-signal/engine/strategies/seed_strategies.py`: đang có `TREND_CONT_BULL`, `TREND_CONT_BEAR`, `TREND_CONT_LIMIT_BULL`, `TREND_CONT_LIMIT_BEAR`.
- `D:/Aureus/services/aureus-signal/engine/orders.py`: live `SimulatedTradeManager._calculate_entry_price()` đang support `ENTRY_PIVOT_LIMIT`; nếu entry price `None` thì reject `ORDER_PLAN_INCOMPLETE` + `ENTRY_PRICE_UNAVAILABLE`.
- `D:/Aureus/services/aureus-signal/engine/simulated_orders.py`: simulated/backtest entry method cần cùng semantic để tránh lệch replay.
- `D:/Aureus/services/aureus-signal/engine/snapshot_utils.py`: `VALID_ENTRY_METHODS` whitelist entry method.
- `D:/Aureus/services/aureus-signal/tests/test_entry_price_methods.py`: sẽ tạo mới nếu chưa tồn tại; chứa regression tests cho entry methods/no-fallback.
- `D:/Aureus/services/aureus-signal/tests/test_strategy_seed_sync.py`: sẽ tạo mới nếu chưa tồn tại; chứa seed strategy sync/activation tests.

## Giả định executor phải kiểm tra trước khi sửa

- FVG trong `state_obj.fvgs` có field `direction`, `top`, `bottom`, `t` hoặc `t_start`, `state`.
- Swing point CHOCH pivot nằm trong `state_obj.swing_points`; BUY dùng `LL/is_high=False`, SELL dùng `HH/is_high=True`.
- "FVG đầu tiên tính từ pivot" nghĩa là FVG cùng hướng có timestamp lớn hơn hoặc bằng pivot timestamp, sort tăng dần theo time, lấy bản ghi đầu tiên chưa `BROKEN`.
- Entry price dùng midpoint FVG `(top + bottom) / 2` để đặt limit nằm trong vùng gap. Nếu codebase đã có convention khác cho FVG entry thì dùng convention hiện có, nhưng phải giữ đúng no-FVG-no-order.

## Tasks

### Task 1: Thêm entry method FVG từ CHOCH pivot, TDD trước

- **files**: `services/aureus-signal/engine/snapshot_utils.py`, `services/aureus-signal/engine/orders.py`, `services/aureus-signal/engine/simulated_orders.py`, `services/aureus-signal/tests/test_entry_price_methods.py`
- **action**: Trước khi sửa symbol, chạy GitNexus impact cho từng symbol sẽ sửa: `VALID_ENTRY_METHODS`, `SimulatedTradeManager._calculate_entry_price`, `_entry_pivot_limit` nếu copy pattern, và class/function tương ứng trong `simulated_orders.py`; báo blast radius trong summary. Tạo `services/aureus-signal/tests/test_entry_price_methods.py` nếu chưa tồn tại. Viết test fail trước trong `test_entry_price_methods.py`: (1) `ENTRY_FVG_FROM_CHOCH_PIVOT` nằm trong `VALID_ENTRY_METHODS`; (2) BUY có LL pivot và bullish FVG sau pivot trả midpoint FVG hoặc convention FVG entry hiện có; (3) SELL có HH pivot và bearish FVG sau pivot trả midpoint FVG/convention hiện có; (4) FVG trước pivot bị bỏ qua; (5) không có FVG hợp lệ trả `None`; (6) `process_triggers()` với entry method này và không FVG reject `ENTRY_PRICE_UNAVAILABLE`, không append order, không add Redis history. Sau test fail, thêm `ENTRY_FVG_FROM_CHOCH_PIVOT` vào whitelist, route `_calculate_entry_price()` live sang helper mới `_entry_fvg_from_choch_pivot(side, state_obj, fallback)`. Helper phải chọn pivot tương ứng `LL` cho BUY, `HH` cho SELL, lọc FVG cùng direction (`BULLISH`/`BEARISH` hoặc equivalent current code), `state != BROKEN`, timestamp FVG >= pivot timestamp, sort tăng dần, trả entry price trong vùng FVG. Không fallback current price khi thiếu pivot/FVG; trả `None` để không vào lệnh. Cập nhật `simulated_orders.py` cùng entry method, không để replay silently dùng current price.
- **verify**:
  - `cd D:/Aureus/services/aureus-signal && pytest tests/test_entry_price_methods.py -q`
  - `cd D:/Aureus/services/aureus-signal && python -m pytest tests/test_entry_price_methods.py::TestCalculateEntryPriceLive -q`
- **done**: Test entry method pass. Live và simulated đều có `ENTRY_FVG_FROM_CHOCH_PIVOT`. Không có FVG hợp lệ thì order bị reject, không tạo order.

### Task 2: Seed 2 strategy clone từ TREND_CONT_BULL/BEAR

- **files**: `services/aureus-signal/engine/strategies/seed_strategies.py`, `services/aureus-signal/tests/test_strategy_seed_sync.py`
- **action**: Chạy `gitnexus_impact({target: "seed_system_strategies", direction: "upstream"})` trước khi sửa seed. Tạo `services/aureus-signal/tests/test_strategy_seed_sync.py` nếu chưa tồn tại. Thêm 2 strategy seed mới, không đổi strategy cũ: `TREND_CONT_FVG_BULL` clone điều kiện `TREND_CONT_BULL` với sequence `choch_up`, `direction=BUY`, `entry_type=LIMIT`, `entry_method=ENTRY_FVG_FROM_CHOCH_PIVOT`, giữ `size_mode`, `size_value`, SL `PIVOT_POINT`, TP `RR_RATIO`, trailing/early_exits giống bull. `TREND_CONT_FVG_BEAR` clone điều kiện `TREND_CONT_BEAR` với sequence `choch_down`, `direction=SELL`, `entry_type=LIMIT`, `entry_method=ENTRY_FVG_FROM_CHOCH_PIVOT`, giữ SL/TP/trailing/early_exits giống bear. Description phải nói rõ: limit tại FVG đầu tiên sau CHOCH pivot; không có FVG thì không vào lệnh. Cập nhật test seed sync để assert 2 name mới tồn tại, active, direction đúng, entry_type LIMIT, entry_method `ENTRY_FVG_FROM_CHOCH_PIVOT`, sequence tag đúng, không làm thay đổi `TREND_CONT_BULL/BEAR` market entry hiện tại.
- **verify**:
  - `cd D:/Aureus/services/aureus-signal && pytest tests/test_strategy_seed_sync.py -q`
  - `cd D:/Aureus/services/aureus-signal && pytest tests/test_strategy_seed_sync.py tests/test_entry_price_methods.py -q`
- **done**: Seed có đủ 2 clone mới. Existing TREND_CONT_BULL/BEAR vẫn MARKET CURRENT. Clone mới LIMIT + FVG entry method.

### Task 3: Kiểm tra scope GitNexus và tạo summary

- **files**: `.planning/quick/260512-9xw-ta-o-2-strategy-clone-t-trend-cont-bull-/260512-9xw-SUMMARY.md`
- **action**: Chạy `gitnexus_detect_changes()` trước commit theo project rule, xác nhận changed symbols/files chỉ gồm entry method whitelist/helper/tests/seed. Nếu GitNexus báo index stale thì chạy `npx gitnexus analyze` từ `D:/Aureus` rồi chạy lại detect. Tạo summary tiếng Việt nêu: symbols đã impact, blast radius d=1, files đổi, test đã chạy, semantic no-FVG-no-order.
- **verify**:
  - `gitnexus_detect_changes()` output không báo scope ngoài dự kiến
  - `cd D:/Aureus && git diff -- services/aureus-signal/engine/snapshot_utils.py services/aureus-signal/engine/orders.py services/aureus-signal/engine/simulated_orders.py services/aureus-signal/engine/strategies/seed_strategies.py services/aureus-signal/tests/test_entry_price_methods.py services/aureus-signal/tests/test_strategy_seed_sync.py`
- **done**: Summary tồn tại, có blast radius + test commands + detect_changes result. Không commit nếu detect_changes chưa chạy.

## Threat model

| Threat | Thành phần | Xử lý |
|---|---|---|
| Sai entry price do FVG cũ/trước pivot | `_entry_fvg_from_choch_pivot` | Lọc timestamp FVG >= pivot timestamp, test FVG trước pivot bị bỏ qua |
| Vào lệnh khi không có FVG | `process_triggers()` entry method | Helper trả `None`; process reject `ENTRY_PRICE_UNAVAILABLE`; test no-order |
| Regression strategy cũ | `seed_strategies.py` | Không sửa `TREND_CONT_BULL/BEAR`; test assert vẫn MARKET/CURRENT |
| Live/replay lệch semantic | `orders.py` vs `simulated_orders.py` | Cập nhật cùng entry method, test cả whitelist/behavior |

## Verification tổng

Chạy tối thiểu:

```bash
cd D:/Aureus/services/aureus-signal && pytest tests/test_entry_price_methods.py tests/test_strategy_seed_sync.py -q
```

Trước commit:

```text
gitnexus_detect_changes()
```

## Success criteria

- Có `TREND_CONT_FVG_BULL` và `TREND_CONT_FVG_BEAR` trong seed.
- 2 clone chỉ tạo LIMIT order tại FVG sau CHOCH pivot phù hợp.
- Thiếu FVG thì reject, không vào lệnh.
- `TREND_CONT_BULL` và `TREND_CONT_BEAR` cũ không đổi semantic.
- Test entry method + seed sync pass.
- GitNexus impact/detect_changes được ghi trong summary.
