---
phase: 260425-kj9-th-c-hi-n-update-trend-theo-nh-ph-n-ti-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/trend.py
  - services/aureus-signal/tests/test_trend_o1.py
autonomous: true
requirements:
  - QUICK-260425-KJ9
must_haves:
  truths:
    - "TrendSignal uses hybrid scoring/voting nhẹ instead of the old EMA200 hard gate + raw OB count matrix."
    - "state_obj.htf_trend and returned event tag htf_trend keep downstream-compatible values BULLISH, BEARISH, or NEUTRAL."
    - "Returned data.regime keeps downstream-compatible semantics TREND_UP, TREND_DN, or SIDEWAYS."
    - "Trend decision can be explained by contribution fields from structure/CHOCH, EMA21/55 momentum, OB confidence, sweep/stop-hunt confirmation, and EMA200 macro penalty."
    - "Focused tests prove hybrid trend can turn bullish/bearish earlier than EMA200 hard gate while anti-false-break and weak-score cases stay NEUTRAL."
  artifacts:
    - path: "services/aureus-signal/engine/signals/trend.py"
      provides: "Hybrid trend detection implementation with compatibility-preserving output contract"
      contains: "class TrendSignal"
    - path: "services/aureus-signal/tests/test_trend_o1.py"
      provides: "Regression and behavior tests for the updated TrendSignal"
      contains: "hybrid"
  key_links:
    - from: "services/aureus-signal/engine/signals/trend.py"
      to: "state_obj.htf_trend"
      via: "calculate assigns canonical state field before returning"
      pattern: "state_obj\.htf_trend = htf_trend"
    - from: "services/aureus-signal/engine/signals/trend.py"
      to: "downstream htf_trend event consumers"
      via: "return payload tag/value contract"
      pattern: '"tag": "htf_trend"'
    - from: "services/aureus-signal/tests/test_trend_o1.py"
      to: "services/aureus-signal/engine/signals/trend.py"
      via: "pytest imports TrendSignal and asserts output semantics"
      pattern: "from engine.signals.trend import TrendSignal"
---

<objective>
Cập nhật TrendSignal theo advisory 260425-ka4: thay logic EMA200 hard gate + OB count cứng bằng hybrid scoring/voting nhẹ tận dụng structure/CHOCH, EMA21/55, OB recency/quality/status, sweep/stop-hunt và EMA200 macro penalty mềm.

Purpose: giảm `NEUTRAL` sai và độ trễ do EMA200 nhưng vẫn giữ compatibility với downstream strategy/Telegram/journal đang dùng `state_obj.htf_trend`, event tag `htf_trend`, values `BULLISH`/`BEARISH`/`NEUTRAL` và regime `TREND_UP`/`TREND_DN`/`SIDEWAYS`.
Output: `trend.py` được cập nhật surgical, kèm focused pytest regression/behavior tests.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260425-ka4-ph-n-ti-ch-planning-quick-260425-jre-ph-/260425-ka4-ARCHITECTURE-ADVISORY.md
@D:/Aureus/.planning/quick/260425-jre-ph-n-ti-ch-services-aureus-signal-engine/260425-jre-REPORT.md
@D:/Aureus/services/aureus-signal/engine/signals/trend.py
@D:/Aureus/services/aureus-signal/tests/test_trend_o1.py

<interfaces>
Executor must preserve these existing contracts:

From `services/aureus-signal/engine/signals/trend.py`:
```python
class TrendSignal(BaseSignal):
    signal_type = SignalType.INDICATOR
    def __init__(self, ema_period: int = 200): ...
    def calculate(self, df: pd.DataFrame, state_obj: Any, **kwargs) -> Optional[Dict[str, Any]]: ...
```

Existing return shape to keep compatible:
```python
{
    "tag": "htf_trend",
    "value": htf_trend,  # BULLISH | BEARISH | NEUTRAL
    "t": int(df.iloc[-1]["t"]),
    "data": {
        "regime": regime,  # TREND_UP | TREND_DN | SIDEWAYS
        ...
    }
}
```

Existing state fields/signals that advisory says to reuse when present:
- `state_obj.swing_points`: confirmed pivots with labels like `HH`, `HL`, `LH`, `LL`; may include CHOCH metadata.
- `state_obj.emas`: period map such as `{21: {"current": ..., "prev": ..., "slope": ...}, 55: {...}, 200: {...}}`.
- `state_obj.obs`: OB dicts with `ob_type`, `quality`, `body_ratio`, `t_start`, `t_breakout`, `status`, `mitigated`.
- Optional recent/transient event kwargs or state fields may contain `choch_up`/`choch_down`, `clean_breakout_*`, `sweep_*`, `stop_hunt_*`; use defensively and do not require a new dependency.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused hybrid trend tests first</name>
  <files>D:/Aureus/services/aureus-signal/tests/test_trend_o1.py</files>
  <behavior>
    - Hybrid bullish: when structure source is bullish (`HH`/`HL` or `choch_up`), EMA21/55 alignment/slope is bullish, recent/quality bullish OB exists, and price is still below EMA200, TrendSignal returns `BULLISH` with `data.regime == "TREND_UP"` instead of old EMA200-gated `NEUTRAL`.
    - Hybrid bearish: mirror bearish case returns `BEARISH` with `data.regime == "TREND_DN"` even when price is still above EMA200, because EMA200 is only a soft macro penalty.
    - Anti-false-break: bullish structure/momentum with opposing `STOP_HUNT`/`SWEEP` evidence or weak total score returns `NEUTRAL`/`SIDEWAYS`.
    - Compatibility: every non-None result still has tag `htf_trend`, value in `BULLISH|BEARISH|NEUTRAL`, and regime in `TREND_UP|TREND_DN|SIDEWAYS`; existing tests are updated only where their assertions encode the removed hard-gate behavior.
  </behavior>
  <action>Before editing, follow CLAUDE/GitNexus rules: run GitNexus impact analysis for `TrendSignal.calculate` (or the nearest indexed symbol if exact method lookup is unavailable) and report direct callers/affected flows/risk in the execution summary; if GitNexus MCP/CLI is unavailable, document the limitation and continue with a fallback note. Then update `test_trend_o1.py` with focused pytest cases for the advisory behavior above. Keep tests deterministic with small in-memory pandas DataFrames and a local MockState only; do not touch database/schema and do not create broad integration harnesses.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_trend_o1.py -q</automated>
  </verify>
  <done>Tests capture the new hybrid behavior and compatibility contract; they fail against the current hard-gated implementation before Task 2 and pass after Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement surgical hybrid scoring in TrendSignal</name>
  <files>D:/Aureus/services/aureus-signal/engine/signals/trend.py</files>
  <behavior>
    - Use structure/CHOCH as primary trend source: infer bullish from recent confirmed `HH`/`HL` sequence or `choch_up`, bearish from `LH`/`LL` sequence or `choch_down`.
    - Use EMA21/55 alignment and slope from `state_obj.emas` when available, falling back to df columns `ema_21`/`ema_55` or local EMA calculation only when enough close data exists; do not require 200 candles to emit a trend.
    - Use OB recency/quality/status confidence instead of raw count as the only gate: active, unmitigated, recent, higher-quality same-direction OB adds confidence; opposing OB/status reduces it.
    - Use sweep/stop-hunt/clean-breakout as confirmation or anti-false-break: same-direction clean breakout/status adds confidence; opposing sweep/stop-hunt/status penalizes or prevents flip when total score is weak.
    - Keep EMA200 as macro penalty/filter mềm: opposing EMA200 reduces score or raises threshold, but does not force `NEUTRAL` when structure + momentum + OB/sweep confidence are strong.
    - Keep output contract unchanged: set `state_obj.htf_trend`; return tag `htf_trend`; values limited to `BULLISH`, `BEARISH`, `NEUTRAL`; regimes limited to `TREND_UP`, `TREND_DN`, `SIDEWAYS`.
  </behavior>
  <action>Modify only `TrendSignal` internals in `trend.py`. Prefer small private helper methods inside the class only if they keep the logic simpler and testable; avoid new dependencies and avoid broad refactors. Preserve `__init__(ema_period: int = 200)` for downstream compatibility, but remove the early `len(df) < ema_period` hard-neutral behavior unless there is insufficient data for any scoring source. Add explainability fields under `data` such as `score`, `structure_score`, `ema_score`, `ob_score`, `sweep_score`, `ema200_penalty`, and keep existing `ema_ref`, `green_ob_count`, `red_ob_count`, `delta` if inexpensive for compatibility/debug. Use simple thresholds/hysteresis-style margin so weak or conflicting scores stay `NEUTRAL` rather than over-flipping. Do not modify database/schema or unrelated signal files.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_trend_o1.py -q</automated>
  </verify>
  <done>`TrendSignal.calculate` implements the advisory’s hybrid scoring/voting nhẹ, old downstream contract remains intact, explainability data exists, and all focused trend tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run scoped verification and change-scope checks</name>
  <files>D:/Aureus/services/aureus-signal/engine/signals/trend.py, D:/Aureus/services/aureus-signal/tests/test_trend_o1.py</files>
  <action>Run the focused trend pytest command. Then follow CLAUDE/GitNexus pre-commit rule by running `gitnexus_detect_changes()` if available; if unavailable, run fallback `git diff -- services/aureus-signal/engine/signals/trend.py services/aureus-signal/tests/test_trend_o1.py` and `git status --short` and document the limitation. Confirm only the two planned files changed. Because this quick task does not touch database/schema, no database e2e test is required.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_trend_o1.py -q</automated>
  </verify>
  <done>Focused pytest passes; change-scope check confirms only `trend.py` and `test_trend_o1.py` changed; execution summary includes GitNexus impact/detect_changes result or explicit fallback limitation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal state -> TrendSignal | `state_obj` fields such as `obs`, `emas`, `swing_points`, and transient event metadata may be absent, sparse, or malformed at runtime. |
| TrendSignal -> downstream consumers | `state_obj.htf_trend` and returned `htf_trend` payload feed strategy/Telegram/journal consumers expecting stable semantic strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260425-kj9-01 | Tampering | `TrendSignal.calculate` inputs from `state_obj` | mitigate | Read optional state defensively with `getattr`/type checks; ignore malformed OB/EMA/pivot entries instead of throwing or producing invalid trend strings. |
| T-260425-kj9-02 | Denial of Service | Hybrid scoring loop over `state_obj.obs` / `swing_points` | mitigate | Use bounded recent slices for pivots/OB scoring and avoid expensive full-history operations. |
| T-260425-kj9-03 | Information Disclosure | Explainability data in returned event payload | accept | Only numeric scores/counts/status-derived reason fields are added; no secrets or PII are involved. |
| T-260425-kj9-04 | Tampering | Downstream contract strings | mitigate | Tests assert `value` and `data.regime` remain in the existing allowed semantics. |
</threat_model>

<verification>
Primary command:

```bash
cd D:/Aureus/services/aureus-signal && pytest tests/test_trend_o1.py -q
```

Required executor checks:
1. GitNexus impact analysis before editing `TrendSignal.calculate` or documented unavailable-tool fallback.
2. Scoped pytest above passes.
3. GitNexus detect_changes before commit or documented fallback diff/status confirms only planned files changed.
4. No database/schema files changed; no DB e2e required for this quick task.
</verification>

<success_criteria>
- Hybrid scoring/voting nhẹ is implemented in `TrendSignal` exactly along the advisory: structure/CHOCH primary, EMA21/55 momentum filter, OB confidence, sweep/stop-hunt anti-false-break, EMA200 soft macro penalty.
- `state_obj.htf_trend`, event tag `htf_trend`, and output values/regime semantics remain downstream-compatible.
- Focused tests demonstrate earlier bullish/bearish detection than old EMA200 hard gate and NEUTRAL fallback for weak/conflicting evidence.
- Implementation is surgical: only `trend.py` and focused trend tests are modified.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260425-kj9-th-c-hi-n-update-trend-theo-nh-ph-n-ti-c/260425-kj9-SUMMARY.md`.
</output>
