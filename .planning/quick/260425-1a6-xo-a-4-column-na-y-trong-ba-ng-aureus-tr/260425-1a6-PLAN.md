---
phase: quick-260425-1a6-drop-4-trade-snapshot-columns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql
  - services/aureus-trader/journal.py
  - services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py
  - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
  - services/aureus-trader/tests/test_signal_snapshot_recompute.py
  - services/aureus-trader/tests/test_signal_snapshot_storage_policy.py
  - services/aureus-trader/tests/test_signal_snapshot_migration.py
  - services/aureus-db-writer/tests/test_signal_snapshot_migration.py
autonomous: true
requirements:
  - QUICK-260425-1A6-DROP-SIGNAL-SNAPSHOT-JSONB
  - QUICK-260425-1A6-DROP-CISD-DIRECTION
  - QUICK-260425-1A6-DROP-EMA21
  - QUICK-260425-1A6-DROP-EMA55
must_haves:
  truths:
    - "Bảng aureus_trade_signal_snapshots không còn 4 cột signal_snapshot, cisd_direction, ema21, ema55."
    - "Luồng ghi snapshot trade vẫn insert/update thành công sau khi bỏ 4 cột."
    - "Các script/test liên quan snapshot không còn phụ thuộc vào 4 cột đã xóa."
    - "Kiểm chứng e2e với database thật xác nhận migration + insert dữ liệu hoạt động."
  artifacts:
    - path: "services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql"
      provides: "DDL migration drop 4 columns khỏi bảng chính và archive + cập nhật function archive"
    - path: "services/aureus-trader/journal.py"
      provides: "SQL insert snapshot và mapping runtime không còn tham chiếu 4 cột đã xóa"
    - path: "services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py"
      provides: "Backfill script dùng schema mới, không select/insert 4 cột đã xóa"
    - path: "services/aureus-trader/tests/test_signal_snapshot_pipeline.py"
      provides: "Test pipeline theo schema mới"
    - path: "services/aureus-db-writer/tests/test_signal_snapshot_migration.py"
      provides: "Test migration xác nhận 4 cột bị remove"
  key_links:
    - from: "journal.py"
      to: "aureus_trade_signal_snapshots"
      via: "INSERT query"
      pattern: "INSERT INTO aureus_trade_signal_snapshots"
    - from: "optimize_trade_signal_snapshots_storage.sql"
      to: "aureus_archive_and_prune_trade_signal_snapshots"
      via: "archive INSERT column list"
      pattern: "aureus_trade_signal_snapshots_archive"
    - from: "backfill_signal_snapshots_canonical.py"
      to: "aureus_trade_signal_snapshots"
      via: "SELECT/INSERT column list"
      pattern: "signal_schema_version"
---

<objective>
Xóa 4 cột `signal_snapshot`, `cisd_direction`, `ema21`, `ema55` khỏi bảng `aureus_trade_signal_snapshots` và dọn toàn bộ source code liên quan để runtime không còn truy cập các cột này.

Purpose: Giảm dư thừa schema theo yêu cầu quick task, tránh drift giữa DB schema và code runtime.
Output: Migration drop cột + cleanup code/truy vấn + test cập nhật + kiểm chứng e2e DB pass.
</objective>

<execution_context>
@/d/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@/d/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/d/Aureus/.planning/STATE.md
@/d/Aureus/CLAUDE.md
@/d/Aureus/services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql
@/d/Aureus/services/aureus-trader/journal.py
@/d/Aureus/services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py
@/d/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py
@/d/Aureus/services/aureus-db-writer/tests/test_signal_snapshot_migration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cập nhật migration để drop 4 cột khỏi live/archive schema</name>
  <files>services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql, services/aureus-db-writer/tests/test_signal_snapshot_migration.py, services/aureus-trader/tests/test_signal_snapshot_migration.py</files>
  <behavior>
    - Test 1: Sau migration, information_schema.columns không còn `signal_snapshot`, `cisd_direction`, `ema21`, `ema55` trong `aureus_trade_signal_snapshots`.
    - Test 2: Bảng archive và function `aureus_archive_and_prune_trade_signal_snapshots` dùng column list mới, không tham chiếu 4 cột bị drop.
  </behavior>
  <action>Chỉnh migration hiện có theo hướng idempotent: thêm `ALTER TABLE ... DROP COLUMN IF EXISTS` cho cả bảng chính và bảng archive; cập nhật mọi `CREATE TABLE IF NOT EXISTS`, `ALTER TABLE ... ADD COLUMN`, `UPDATE ... SET`, và danh sách cột của function archive để loại bỏ 4 cột này. Không thay đổi cột khác ngoài phạm vi yêu cầu.</action>
  <verify>
    <automated>pytest services/aureus-db-writer/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py -q</automated>
  </verify>
  <done>Migration phản ánh đúng schema mới (không còn 4 cột) và test migration pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dọn code runtime/backfill không còn đọc-ghi 4 cột đã xóa</name>
  <files>services/aureus-trader/journal.py, services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py, services/aureus-trader/tests/test_signal_snapshot_pipeline.py, services/aureus-trader/tests/test_signal_snapshot_recompute.py, services/aureus-trader/tests/test_signal_snapshot_storage_policy.py</files>
  <behavior>
    - Test 1: Insert snapshot trong journal/backfill không lỗi SQL do mismatch số lượng/tên cột.
    - Test 2: Unit/integration tests snapshot pipeline/recompute/storage policy không còn assert vào 4 cột bị xóa.
  </behavior>
  <action>Loại bỏ tham chiếu tới `signal_snapshot`, `cisd_direction`, `ema21`, `ema55` trong câu SQL INSERT/SELECT và mapping phụ trợ của journal/backfill script; cập nhật test fixtures/assertions tương ứng để xác nhận behavior runtime vẫn đúng với schema mới. Giữ nguyên logic nghiệp vụ còn lại, chỉ chỉnh các phần vỡ do đổi schema.</action>
  <verify>
    <automated>pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py services/aureus-trader/tests/test_signal_snapshot_recompute.py services/aureus-trader/tests/test_signal_snapshot_storage_policy.py -q</automated>
  </verify>
  <done>Code trader không còn đọc-ghi 4 cột đã xóa và bộ test snapshot liên quan pass.</done>
</task>

<task type="auto">
  <name>Task 3: Kiểm chứng e2e với database thật cho migration + insert snapshot</name>
  <files>services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql, services/aureus-trader/journal.py</files>
  <action>Chạy môi trường DB theo RUN_SERVICES.md, apply migration trên database thật, sau đó chạy 1 test e2e có DB (hoặc test integration hiện có) để xác nhận luồng tạo dữ liệu snapshot vẫn insert thành công và bảng không còn 4 cột. Nếu command lỗi, dùng RUN_SERVICES.md để chọn lệnh khởi động/phục hồi đúng cho stack hiện tại.</action>
  <verify>
    <automated>docker compose -f docker-compose.dev.yml up -d timescaledb && pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q -k e2e</automated>
  </verify>
  <done>DB thật đã migrate thành công, insert snapshot tạo bản ghi thành công, và schema xác nhận không còn 4 cột mục tiêu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trader service -> PostgreSQL/TimescaleDB | Runtime SQL ghi snapshot đi qua boundary dữ liệu không tin cậy từ event payload |
| migration script -> production-like schema | DDL thay đổi cột có thể làm hỏng function/query nếu danh sách cột không đồng bộ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-1a6-01 | T | `optimize_trade_signal_snapshots_storage.sql` | mitigate | Dùng `DROP COLUMN IF EXISTS` + cập nhật đồng bộ mọi column list trong CREATE/ALTER/FUNCTION để tránh schema drift |
| T-quick-1a6-02 | D | `journal.py` INSERT snapshot | mitigate | Cập nhật chính xác số lượng và thứ tự placeholder theo schema mới, kiểm chứng bằng test pipeline có DB |
| T-quick-1a6-03 | R | backfill script | mitigate | Đồng bộ SELECT/INSERT list và giữ trace_id/trade_journal_id để đối soát dữ liệu sau migration |
</threat_model>

<verification>
- Unit/integration migration tests pass cho db-writer và trader.
- Snapshot pipeline/recompute/storage tests pass sau cleanup.
- E2E với database thật xác nhận migration + insert runtime không lỗi.
</verification>

<success_criteria>
- `aureus_trade_signal_snapshots` không còn 4 cột: `signal_snapshot`, `cisd_direction`, `ema21`, `ema55`.
- Code runtime/backfill không còn truy cập 4 cột này.
- Test migration + pipeline snapshot pass bằng các lệnh verify trong task.
- Có bằng chứng chạy với DB thật xác nhận tạo data snapshot thành công sau migration.
</success_criteria>

<output>
After completion, create `/d/Aureus/.planning/quick/260425-1a6-xo-a-4-column-na-y-trong-ba-ng-aureus-tr/260425-1a6-SUMMARY.md`
</output>
