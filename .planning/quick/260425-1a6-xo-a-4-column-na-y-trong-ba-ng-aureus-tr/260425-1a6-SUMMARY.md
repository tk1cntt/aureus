# Quick Task 260425-1a6 Summary

## Objective
Xóa 4 cột trong bảng `aureus_trade_signal_snapshots` và code liên quan:
- `signal_snapshot`
- `cisd_direction`
- `ema21`
- `ema55`

## GitNexus Impact Analysis
- `_build_signal_snapshot_columns` (`services/aureus-trader/journal.py`) → **LOW**
  - d=1: `_has_signal_payload`, `on_order_opened`
- `run_backfill` (`services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py`) → **LOW**
  - d=1: `_run`
- `on_strategy_match` (`services/aureus-trader/journal.py`) → **CRITICAL**
  - d=1: `run_trader` (`services/aureus-trader/main.py`)
  - Affected process group: trader runtime flows
- `recompute_batch` (`services/aureus-trader/recompute_evaluations.py`) → **CRITICAL**
  - d=1: `_run`
  - Affected process group: recompute pipeline

## Scope implemented
### 1) Migration / Schema
- `services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql`
  - Bỏ 4 cột khỏi `CREATE TABLE aureus_trade_signal_snapshots`.
  - Thêm `ALTER TABLE ... DROP COLUMN IF EXISTS` cho 4 cột trên bảng chính.
  - Bỏ logic/check constraint liên quan `signal_snapshot`.
  - Bỏ 4 cột khỏi `CREATE TABLE ..._archive`.
  - Thêm `ALTER TABLE ..._archive DROP COLUMN IF EXISTS` cho 4 cột.
  - Cập nhật function `aureus_archive_and_prune_trade_signal_snapshots` để archive theo schema mới.

### 2) Runtime / Backfill / Recompute
- `services/aureus-trader/journal.py`
  - Cập nhật INSERT snapshot trong `on_order_opened` để không còn ghi cột `signal_snapshot`.
  - Giữ mapping kỹ thuật (atr/ema_*/bb_*/cisd_*/session/candle_color_*) cho cột canonical.
- `services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py`
  - Bỏ đọc/ghi `signal_snapshot`, `cisd_direction`, `ema21`, `ema55`.
  - Insert chỉ theo schema mới (`trade_journal_id...timeframe/signal_schema_version/created_at`).
- `services/aureus-trader/recompute_evaluations.py`
  - Đồng bộ INSERT snapshot theo schema mới.
  - Thêm `timeframe`, `signal_schema_version`, `created_at` và conflict key `(trade_journal_id, signal_schema_version)`.

### 3) Tests updated
- `services/aureus-db-writer/tests/test_signal_snapshot_migration.py`
- `services/aureus-trader/tests/test_signal_snapshot_migration.py`
- `services/aureus-trader/tests/test_signal_snapshot_storage_policy.py`
  - Cập nhật required columns: bỏ 4 cột cũ.
- `services/aureus-trader/tests/test_signal_snapshot_recompute.py`
  - Cập nhật mock row + assertions theo insert signature mới.
- `services/aureus-trader/tests/test_signal_snapshot_pipeline.py`
  - Cập nhật assertions index args theo thứ tự params mới.
  - Cập nhật e2e insert query/verify cho schema mới (không dùng `signal_snapshot`).

## Verification
### Unit/Integration tests
- `pytest services/aureus-db-writer/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_signal_snapshot_storage_policy.py services/aureus-trader/tests/test_signal_snapshot_recompute.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q`
  - **20 passed, 1 skipped**

### E2E with DB (required for DB changes)
- Apply migration on dev DB:
  - `cat /mnt/d/Aureus/services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql | docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus`
- E2E test with real DB:
  - `pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py::test_signal_snapshot_e2e_db_real_persists_ema_cisd_bb_columns -q`
  - **1 passed**
- Schema verification (real DB):
  - `SELECT column_name FROM information_schema.columns WHERE table_name='aureus_trade_signal_snapshots' AND column_name IN ('signal_snapshot','cisd_direction','ema21','ema55')`
  - **0 rows**

## Notes
- `npx gitnexus detect_changes` vẫn báo `unknown command`; thay bằng `git diff --stat` + review file scope trước commit.