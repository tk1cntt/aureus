---
phase: 260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md
autonomous: true
requirements:
  - QUICK-260517-SF2
must_haves:
  truths:
    - "Xác định rõ nhánh ProcessLegacyPositionsByType đóng lệnh single stale profit khi positions_count == 1, age_seconds > 1800, net_profit > 0."
    - "Phân biệt được 3 case người dùng nêu: sideway không có lợi nhuận, âm rồi chạy hòa/chớm lời, và đang có lợi nhuận tốt cần giữ/không close sớm."
    - "Đề xuất xử lý không sửa code vội; nếu cần fix, phải nêu điều kiện an toàn cụ thể để executor xin xác nhận trước khi edit."
  artifacts:
    - path: ".planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md"
      provides: "Báo cáo phân tích root cause, case classification, solution proposal"
      contains: "ProcessLegacyPositionsByType"
  key_links:
    - from: "mql5/AureusProvider_v2.mq5"
      to: ".planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md"
      via: "Trích line logic ProcessLegacyPositionsByType và luồng ManagePositionProfitBreakEvent -> ProcessPositionsByType -> ProcessLegacyPositionsByType"
      pattern: "legacy_stale_profitable_single"
---

<objective>
Phân tích hàm `ProcessLegacyPositionsByType` trong `mql5/AureusProvider_v2.mq5`, giải thích vì sao case `positions_count == 1 && age_seconds > 1800 && net_profit > 0` đóng cả lệnh đang lời tốt, và đề xuất cách nhận dạng/ứng xử 3 trạng thái: sideway không có lợi nhuận, âm rồi hồi về hòa/chớm lời, lời tốt cần giữ.

Purpose: Người dùng cần quyết định logic quản lý lệnh legacy trước khi sửa code, tránh close nhầm lệnh đang chạy tốt.
Output: Báo cáo tiếng Việt tại `.planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md`; không sửa source nếu chưa có bằng chứng và xác nhận rõ.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/mql5/AureusProvider_v2.mq5

Ghi chú bắt buộc:
- Trao đổi và report bằng tiếng Việt markdown.
- Đây là plan phân tích/reporting. Không sửa `mql5/AureusProvider_v2.mq5` trừ khi người dùng xác nhận sau báo cáo.
- Nếu executor quyết định sửa symbol MQL5, trước khi edit phải chạy GitNexus impact theo project rule cho symbol bị sửa, báo blast radius, rồi mới sửa.

Interface chính đã đọc:
- `ManagePositionProfitBreakEvent()` group position theo symbol + magic + direction, tính `total_profit`, `total_volume`, `weighted_price_sum`, `earliest_open_time`, rồi gọi `ProcessPositionsByType(...)`.
- `ProcessPositionsByType(...)` resolve profile bằng `ResolveManagementProfile(magic, profile_fallback)`. Nếu profile không map hoặc fallback legacy thì gọi `ProcessLegacyPositionsByType(...)`.
- `ProcessLegacyPositionsByType(...)` hiện có nhánh:
  - `net_profit < -max_loss_amount` hoặc single lỗ quá nửa max risk => close `severe_risk_guard`.
  - `positions_count == 4 && net_profit > 0` => close basket recovery.
  - `positions_count == 1 && age_seconds > 1800 && net_profit > 0` => close `legacy_stale_profitable_single`, không phân biệt lời nhỏ/hòa vốn/lời tốt.
- `ProcessBreakoutProtectPositionsByType(...)` cùng điều kiện single stale profit nhưng `MovePositionsSL(... weighted_avg_open_price ...)`, không close ngay.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace logic hiện tại và root cause close nhầm</name>
  <files>mql5/AureusProvider_v2.mq5, .planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md</files>
  <action>Trước khi đọc/viết report, chạy GitNexus theo CLAUDE.md: `gitnexus_context({name: "ProcessLegacyPositionsByType"})` hoặc `gitnexus_query({query: "legacy_stale_profitable_single ProcessLegacyPositionsByType"})`. Ghi kết quả caller/callee/execution flow liên quan vào report để chứng minh luồng và blast radius phân tích; không cần `gitnexus_impact` vì task này không sửa code. Sau đó đọc lại các vùng liên quan trong `mql5/AureusProvider_v2.mq5`: `CalculatePositionGroupCosts`, `ClosePositionTickets`, `ProcessLegacyPositionsByType`, `ProcessBreakoutProtectPositionsByType`, `ProcessPositionsByType`, `ManagePositionProfitBreakEvent`. Viết phần report giải thích luồng gọi, biến đầu vào, công thức `net_profit`, và root cause: điều kiện `net_profit > 0` quá rộng nên mọi single position quá 30 phút chỉ cần lời ròng dương là bị close, không đo độ mạnh lợi nhuận, không đo MFE/current trend, không phân biệt hồi hòa vốn với lời tốt. Không sửa source.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
src = Path('D:/Aureus/mql5/AureusProvider_v2.mq5').read_text(encoding='utf-8', errors='ignore')
assert 'void ProcessLegacyPositionsByType' in src
assert 'legacy_stale_profitable_single' in src
assert 'positions_count == 1 && age_seconds > 1800 && net_profit > 0' in src
print('ok')
PY</automated>
  </verify>
  <done>Report có section root cause nêu đúng nhánh `legacy_stale_profitable_single`, điều kiện đóng lệnh, vì sao nó đóng cả lệnh lời tốt, và kết quả GitNexus context/query gồm caller/callee/execution flow của `ProcessLegacyPositionsByType`.</done>
</task>

<task type="auto">
  <name>Task 2: Phân loại 3 trạng thái cần nhận dạng</name>
  <files>.planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md</files>
  <action>Xây ma trận nhận dạng 3 case người dùng yêu cầu, dựa trên dữ liệu hiện có trong provider: `positions_count`, `age_seconds`, `net_profit`, `weighted_avg_open_price`, giá Bid/Ask hiện tại, symbol tick value/point, current SL/TP nếu đọc được. Case 1 sideway không có lợi nhuận: age lớn, net_profit quanh 0 hoặc dương rất nhỏ, distance/pips nhỏ, không đạt ngưỡng `InpBEProfitTarget`. Case 2 âm rồi hồi hòa/chớm lời: cần proxy bằng age lớn + net_profit vừa dương/near breakeven + profit chưa đạt ngưỡng bảo vệ; nêu hạn chế vì provider hiện không lưu MFE/MAE nên không biết chắc quá khứ âm nếu chỉ nhìn snapshot hiện tại. Case 3 lời tốt: net_profit đạt ngưỡng rõ như `>= InpBEProfitTarget` hoặc theo R/money threshold, nên không close market; ưu tiên move SL về breakeven/cost hoặc trailing structure giống breakout/trend profiles. Ghi rõ dữ liệu thiếu nếu muốn phân biệt chuẩn: MFE/MAE hoặc max_drawdown/min_profit_since_open theo ticket.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('D:/Aureus/.planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md')
assert p.exists(), 'missing report'
t = p.read_text(encoding='utf-8')
for needle in ['sideway', 'hòa', 'lời tốt', 'MFE', 'MAE', 'InpBEProfitTarget']:
    assert needle in t, needle
print('ok')
PY</automated>
  </verify>
  <done>Report có bảng/ma trận 3 case, điều kiện nhận dạng, hành động đề xuất, và hạn chế dữ liệu hiện tại.</done>
</task>

<task type="auto">
  <name>Task 3: Đề xuất solution an toàn và tiêu chí fix nếu được duyệt</name>
  <files>.planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md</files>
  <action>Viết phần solution proposal theo thứ tự khuyến nghị. Phương án tối thiểu an toàn: thay nhánh legacy single stale profit từ `ClosePositionTickets` sang chỉ close khi `0 < net_profit < small_profit_threshold` hoặc `net_profit < positions_count * InpBEProfitTarget / 2`; nếu `net_profit >= InpBEProfitTarget` thì HOLD hoặc move SL về breakeven/weighted avg (không close). Phương án tốt hơn: tách legacy thành behavior tương tự `breakout_protect` cho single stale profit: lời nhỏ/hòa vốn thì `MOVE_SL` hoặc close nhẹ tùy mục tiêu; lời tốt thì trailing SL, không close market. Phương án chuẩn nhất: thêm tracking per-ticket MFE/MAE để phân biệt âm-rồi-hòa với lời tốt thật, nhưng nêu đây là thay đổi lớn hơn. Kết luận rõ: không implement trong quick này; nếu user duyệt fix thì plan sau phải edit `ProcessLegacyPositionsByType` và có GitNexus impact trước edit.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('D:/Aureus/.planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md')
t = p.read_text(encoding='utf-8')
for needle in ['Khuyến nghị', 'không implement', 'GitNexus', 'ProcessLegacyPositionsByType', 'ClosePositionTickets', 'MovePositionsSL']:
    assert needle in t, needle
print('ok')
PY</automated>
  </verify>
  <done>Report có khuyến nghị cụ thể, tradeoff, pseudo-condition đủ để user duyệt fix sau, và không tạo code modification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 provider runtime → trade server | Logic quản lý position có thể gửi lệnh close/modify thật sang broker nếu executor sửa code. |
| Report/plan → future code edit | Báo cáo sai có thể dẫn đến fix đóng lệnh sai trong phase sau. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260517-sf2-01 | Tampering | `ProcessLegacyPositionsByType` | mitigate | Plan này chỉ phân tích, không sửa source; mọi fix sau phải có GitNexus impact và xác nhận người dùng. |
| T-260517-sf2-02 | Denial of Service | `ClosePositionTickets` repeated close attempts | accept | Ngoài scope report; có guard market closed sẵn, không thay đổi runtime. |
| T-260517-sf2-03 | Repudiation | Management close reason | mitigate | Report phải trích đúng reason `legacy_stale_profitable_single` để future fix giữ reason/log rõ. |
</threat_model>

<verification>
Chạy automated verify từng task. Sau đó đọc report và kiểm tra: có root cause, có 3-case classification, có solution proposal, có GitNexus context/query caller/callee/flow cho `ProcessLegacyPositionsByType`, không có thay đổi source ngoài report.
</verification>

<success_criteria>
- Báo cáo tiếng Việt markdown tồn tại tại `.planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-REPORT.md`.
- Báo cáo nêu đúng luồng `ManagePositionProfitBreakEvent -> ProcessPositionsByType -> ProcessLegacyPositionsByType`.
- Báo cáo ghi kết quả GitNexus context/query caller/callee/execution flow cho `ProcessLegacyPositionsByType` trước khi phân tích.
- Báo cáo giải thích vì sao `net_profit > 0` làm close cả lệnh lời tốt.
- Báo cáo phân biệt 3 case: sideway không lợi nhuận, âm rồi hòa/chớm lời, lời tốt.
- Báo cáo đề xuất fix an toàn nhưng không sửa code trong task này.
</success_criteria>

<output>
After completion, create `.planning/quick/260517-sf2-ph-n-ti-ch-ha-m-processlegacypositionsby/260517-sf2-SUMMARY.md`
</output>
