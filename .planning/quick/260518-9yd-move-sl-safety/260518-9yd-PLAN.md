---
phase: 260518-9yd-move-sl-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
autonomous: true
requirements:
  - QUICK-260518-9YD
must_haves:
  truths:
    - "MovePositionsSL never downgrades any individual ticket SL: BUY skip when proposed_sl_price <= current SL, SELL skip when proposed_sl_price >= current SL while current SL > 0."
    - "MovePositionsSL prechecks broker stop/freeze distance for each ticket and skips invalid/frozen modify attempts before PositionModify."
    - "MovePositionsSL treats TRADE_RETCODE_NO_CHANGES as safe skip, logs retcode/reason for failed modifies, and only returns true when at least one ticket was actually modified."
    - "Conservative and Basket Escape strategy semantics stay unchanged; no new SL movement path is added outside existing MovePositionsSL callers."
    - "AureusProvider_v2.mq5 builds with 0 errors and 0 warnings using mql5/Build_Rules.md."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Shared per-ticket MovePositionsSL safety guard for all existing SL modify callers"
      contains: "MovePositionsSL"
    - path: "mql5/AureusProvider_v2_compile.log"
      provides: "MQL5 compile proof"
      contains: "0 errors, 0 warnings"
  key_links:
    - from: "ProcessLegacyPositionsByType"
      to: "MovePositionsSL"
      via: "legacy_stale_single_breakeven_protect call"
      pattern: "MovePositionsSL\(symbol, magic, target_type, PROFILE_LEGACY"
    - from: "ProcessTrendRunnerPositionsByType"
      to: "MovePositionsSL"
      via: "trend_structure_trailing call"
      pattern: "MovePositionsSL\(symbol, magic, target_type, PROFILE_TREND_RUNNER"
    - from: "ProcessBreakoutProtectPositionsByType"
      to: "MovePositionsSL"
      via: "breakout stale breakeven and structure protection calls"
      pattern: "MovePositionsSL\(symbol, magic, target_type, PROFILE_BREAKOUT_PROTECT"
---

<objective>
Tối ưu shared SL modify safety trong `MovePositionsSL` cho `ProcessPositionsByType` callers.

Purpose: chặn downgrade SL theo từng ticket, giảm INVALID_STOPS/FROZEN spam bằng stop/freeze precheck, giữ surgical và không đổi semantic Conservative/Basket Escape.
Output: `mql5/AureusProvider_v2.mq5` có guard an toàn trong `MovePositionsSL`, build MQL5 sạch.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/mql5/Build_Rules.md
@D:/Aureus/.planning/quick/260518-91x-breakout-sl-safety/260518-91x-SUMMARY.md
@D:/Aureus/.planning/quick/260518-91x-breakout-sl-safety/260518-91x-VERIFICATION.md
@D:/Aureus/mql5/AureusProvider_v2.mq5

Assumptions:
- Quick task scope is shared safety only; do not add new SL behavior to Conservative/Basket Escape.
- `MovePositionsSL` is best surgical target because Legacy, Trend Runner, and Breakout Protect already route SL moves through it.
- No DB changes; DB E2E not required.

Interfaces:
```mql5
bool MovePositionsSL(string symbol,
                     long magic,
                     ENUM_POSITION_TYPE target_type,
                     string profile,
                     const ulong &tickets[],
                     double net_profit,
                     int age_seconds,
                     double proposed_sl_price,
                     string action,
                     string reason,
                     string primitive)
```

Existing callers:
```mql5
MovePositionsSL(symbol, magic, target_type, PROFILE_LEGACY, tickets, net_profit, age_seconds, weighted_avg_open_price, "MOVE_SL", "legacy_stale_single_breakeven_protect", "P-08");
MovePositionsSL(symbol, magic, target_type, PROFILE_TREND_RUNNER, tickets, net_profit, age_seconds, proposed_sl_price, "TRAIL_SL", "trend_structure_trailing", "P-10/P-11/P-12/P-13");
MovePositionsSL(symbol, magic, target_type, PROFILE_BREAKOUT_PROTECT, tickets, net_profit, age_seconds, weighted_avg_open_price, "MOVE_SL", "breakout_stale_single_breakeven_protect", "P-12/P-13/P-15");
MovePositionsSL(symbol, magic, target_type, PROFILE_BREAKOUT_PROTECT, tickets, net_profit, age_seconds, proposed_sl_price, "TRAIL_SL", "breakout_structure_protection", "P-10/P-11/P-12/P-13");
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate MovePositionsSL edit with GitNexus impact</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <action>Before editing `MovePositionsSL`, attempt official GitNexus impact per project rule: `gitnexus_impact({target: "MovePositionsSL", direction: "upstream"})`. If official tool unavailable in harness, document exact unavailable attempt in SUMMARY, then run CLI fallback from `D:/Aureus`: `npx gitnexus impact MovePositionsSL --direction upstream --repo Aureus`. If CLI cannot resolve symbol, manually document fallback blast radius from source: direct callers are `ProcessLegacyPositionsByType`, `ProcessTrendRunnerPositionsByType`, `ProcessBreakoutProtectPositionsByType`; upstream route is `ManagePositionProfitBreakEvent -> ProcessPositionsByType -> profile-specific Process* -> MovePositionsSL`; risk is Medium because shared SL modify behavior changes for existing move callers only. Do not edit symbols other than `MovePositionsSL` unless absolutely needed; if adding a tiny helper, run GitNexus impact for that symbol name too before edit. Report blast radius before making code change.</action>
  <verify>
    <automated>cd D:/Aureus && npx gitnexus impact MovePositionsSL --direction upstream --repo Aureus</automated>
  </verify>
  <done>Impact attempt and fallback blast radius are recorded in execution notes/SUMMARY before any source edit; no HIGH/CRITICAL warning ignored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-ticket no-downgrade and broker distance guard inside MovePositionsSL</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - BUY ticket with existing `POSITION_SL > 0` and `proposed_sl_price <= current_sl` is skipped before `trade.PositionModify`.
    - SELL ticket with existing `POSITION_SL > 0` and `proposed_sl_price >= current_sl` is skipped before `trade.PositionModify`.
    - BUY/SELL ticket with `proposed_sl_price` too close to current bid/ask using `SYMBOL_TRADE_STOPS_LEVEL` or `SYMBOL_TRADE_FREEZE_LEVEL` is skipped before `trade.PositionModify`.
    - `TRADE_RETCODE_NO_CHANGES` after modify failure is treated as safe skip, not counted as success.
    - Other modify failures log ticket, retcode, `RetcodeToReason(retcode)`, and `trade.ResultComment()`.
  </behavior>
  <action>Modify only `MovePositionsSL` unless a tiny local helper is necessary. For each ticket, after `PositionSelectByTicket(tickets[i])`, read per-ticket `POSITION_SL`, `POSITION_TP`, `POSITION_PRICE_CURRENT` or symbol bid/ask. Normalize `proposed_sl_price` once as current code does. Add per-ticket no-downgrade guard: for BUY skip when current SL > 0 and proposed SL <= current SL; for SELL skip when current SL > 0 and proposed SL >= current SL. Add stop/freeze precheck using broker points: `SYMBOL_POINT`, `SYMBOL_TRADE_STOPS_LEVEL`, `SYMBOL_TRADE_FREEZE_LEVEL`; required distance points is max(stops, freeze). Compare proposed SL against current market side suitable for SL validity: BUY SL must be below current bid by required distance; SELL SL must be above current ask by required distance. If precheck fails, skip that ticket and Print debug-safe reason with ticket/current_sl/proposed_sl/bid/ask/required points. Preserve existing `LogManagementDecision(...)` only for actual successful modify. On failed `PositionModify`, capture `trade.ResultRetcode()`, map with `RetcodeToReason(retcode)`, and if retcode is `TRADE_RETCODE_NO_CHANGES`/10025 skip quietly enough with explicit no-change log; for all other retcodes print retcode/reason/comment. Keep function return `success_count > 0`. Do not add SL calls to Conservative or Basket Escape. Do not refactor adjacent Process* functions.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p=Path('D:/Aureus/mql5/AureusProvider_v2.mq5')
s=p.read_text(encoding='utf-8')
start=s.index('bool MovePositionsSL(')
end=s.index('bool FindStructureSLTarget', start)
fn=s[start:end]
checks={
 'buy_no_downgrade': 'POSITION_TYPE_BUY' in fn and 'proposed_sl_price <= current_sl' in fn,
 'sell_no_downgrade': 'POSITION_TYPE_SELL' in fn and 'proposed_sl_price >= current_sl' in fn,
 'stops_level': 'SYMBOL_TRADE_STOPS_LEVEL' in fn,
 'freeze_level': 'SYMBOL_TRADE_FREEZE_LEVEL' in fn,
 'retcode_reason': 'RetcodeToReason(retcode)' in fn,
 'no_changes': 'TRADE_RETCODE_NO_CHANGES' in fn or '10025' in fn,
 'success_only': 'return success_count > 0' in fn,
}
missing=[k for k,v in checks.items() if not v]
assert not missing, missing
print('MovePositionsSL static safety checks passed')
PY</automated>
  </verify>
  <done>`MovePositionsSL` skips per-ticket SL downgrades, skips broker stop/freeze-invalid targets before modify, handles no-change retcode as non-success safe skip, logs retcode-aware failures, and existing strategy callers remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Build, scope-check, and protect unrelated dirty files</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5, D:/Aureus/mql5/AureusProvider_v2_compile.log</files>
  <action>Compile exactly per `mql5/Build_Rules.md` adapted to v2 file: `cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\AureusProvider_v2.mq5" /log:"D:\Aureus\mql5\AureusProvider_v2_compile.log""`. Fix until compile log has 0 errors and 0 warnings. If compile command fails to run, read `D:/Aureus/RUN_SERVICES.md` before retry/diagnosis. Before any commit, attempt official `gitnexus_detect_changes()` per project rule. If official tool unavailable in harness, document exact unavailable attempt, then fallback scope verification with `git diff -- D:/Aureus/mql5/AureusProvider_v2.mq5` and `git status --short`; confirm only expected source change is `mql5/AureusProvider_v2.mq5` and compile artifact may update. Do not stage or commit unrelated dirty/untracked files: `AGENTS.md`, `CLAUDE.md`, `mql5/AureusProvider_v2.ex5`, `services/aureus-signal/scripts/snapshots/`, `stable/`. No DB E2E needed because no database code/schema touched.</action>
  <verify>
    <automated>cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\AureusProvider_v2.mq5" /log:"D:\Aureus\mql5\AureusProvider_v2_compile.log""</automated>
    <automated>python - <<'PY'
from pathlib import Path
log=Path('D:/Aureus/mql5/AureusProvider_v2_compile.log').read_text(errors='ignore')
assert '0 errors, 0 warnings' in log, log[-1000:]
print('MQL5 build clean')
PY</automated>
    <automated>git -C D:/Aureus status --short</automated>
  </verify>
  <done>Compile log reports 0 errors and 0 warnings; GitNexus detect_changes official attempt or documented fallback confirms expected scope; unrelated dirty/untracked files remain untouched and unstaged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 broker state -> MovePositionsSL | Live position SL/TP/current price and symbol trade constraints can change between management loop and modify call. |
| Strategy decision -> trade server modify | Proposed SL crosses from local strategy logic into broker trade server request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260518-9YD-01 | Tampering | `MovePositionsSL` | mitigate | Per-ticket no-downgrade guard blocks lower protection for BUY and worse protection for SELL before `PositionModify`. |
| T-260518-9YD-02 | Denial of Service | `MovePositionsSL` broker modify loop | mitigate | Stop/freeze distance precheck skips requests likely to return INVALID_STOPS/FROZEN and reduces repeated broker rejection spam. |
| T-260518-9YD-03 | Repudiation | `MovePositionsSL` failure path | mitigate | Retcode-aware logs include ticket, retcode, mapped reason, and broker comment for failed modify attempts. |
| T-260518-9YD-04 | Elevation of Privilege | Conservative/Basket Escape routing | accept | Scope explicitly avoids adding new SL movement behavior to profiles that currently hold/close only; no new authority path. |
</threat_model>

<verification>
1. Static safety assertion passes for `MovePositionsSL` guard tokens.
2. MQL5 compile produces `D:/Aureus/mql5/AureusProvider_v2_compile.log` with `0 errors, 0 warnings`.
3. GitNexus impact attempted before edit; detect_changes attempted before commit, with documented fallback if official tool unavailable.
4. `git status --short` shows unrelated dirty/untracked files not staged or modified by this work.
</verification>

<success_criteria>
- `MovePositionsSL` is safer for every existing caller without changing caller strategy semantics.
- Per-ticket SL downgrades are skipped for BUY and SELL.
- Broker stop/freeze-invalid SL modifications are skipped before trade server call.
- Modify failure logs include retcode/reason/comment, and no-change retcode is not counted as success.
- `mql5/AureusProvider_v2.mq5` builds clean with 0 errors and 0 warnings.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260518-9yd-move-sl-safety/260518-9yd-SUMMARY.md`.
</output>
