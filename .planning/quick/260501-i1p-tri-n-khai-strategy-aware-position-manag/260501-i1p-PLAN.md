---
phase: quick-260501-i1p-tri-n-khai-strategy-aware-position-manag
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
  - .planning/quick/260501-i1p-tri-n-khai-strategy-aware-position-manag/260501-i1p-SUMMARY.md
  - .planning/STATE.md
autonomous: true
requirements:
  - STRAT-MGMT-01
  - STRAT-MGMT-02
  - STRAT-MGMT-03
  - STRAT-MGMT-04
  - STRAT-MGMT-05
  - STRAT-MGMT-06
  - STRAT-MGMT-07
  - STRAT-MGMT-08
must_haves:
  truths:
    - "Provider quản lý vị thế theo group identity symbol + magic + direction, không trộn strategy hoặc hướng giao dịch."
    - "Magic được resolve qua input mapping sang profile; magic không match dùng default conservative và log rõ fallback/profile."
    - "Chỉ có built-in profiles conservative, trend_runner, breakout_protect, basket_escape; không có external config, JSON, DSL, hot reload."
    - "Rule global single profitable age > 30m close bị vô hiệu hóa; CLOSE chỉ xảy ra theo profile rõ ràng sau khi đã ưu tiên HOLD/MOVE_SL/TRAIL_SL."
    - "Decision action log có đủ symbol, magic, direction, profile, action, reason, positions_count, net_profit, age_seconds và ticket/action target khi có."
    - "Unsupported symbol, history cooldown, CLOSE_ORDER, REQUEST_ORDERS, REQUEST_BACKFILL_COUNT giữ nguyên hành vi."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Strategy-aware profile resolver, decision engine, action logging, và updated position management"
      contains: "InpMagicManagementProfiles"
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Default conservative + built-in profile names trend_runner/breakout_protect/basket_escape"
      contains: "PROFILE_CONSERVATIVE"
    - path: ".planning/quick/260501-i1p-tri-n-khai-strategy-aware-position-manag/260501-i1p-SUMMARY.md"
      provides: "GSD summary với bằng chứng compile/static checks"
    - path: ".planning/STATE.md"
      provides: "Quick task history updated"
  key_links:
    - from: "mql5/AureusProvider_v2.mq5::ManagePositionProfitBreakEvent"
      to: "mql5/AureusProvider_v2.mq5::ProcessPositionsByType"
      via: "group scan passes symbol, magic, direction, aggregate state"
      pattern: "ProcessPositionsByType\(symbol, magic, type"
    - from: "mql5/AureusProvider_v2.mq5::ProcessPositionsByType"
      to: "profile resolver"
      via: "magic -> profile mapping without hard-coded magic branches"
      pattern: "Resolve.*Profile.*magic"
    - from: "mql5/AureusProvider_v2.mq5::ProcessPositionsByType"
      to: "decision action log"
      via: "all HOLD/MOVE_SL/TRAIL_SL/CLOSE decisions emit required fields"
      pattern: "Decision.*symbol=.*magic=.*direction=.*profile=.*action=.*reason=.*positions_count=.*net_profit=.*age_seconds"
    - from: "mql5/Build_Rules.md"
      to: "MetaEditor compile"
      via: "terminal64/MetaEditor64 compile command produces 0 errors 0 warnings"
      pattern: "MetaEditor64.exe.*AureusProvider_v2.mq5"
---

<objective>
Triển khai strategy-aware position management trong `mql5/AureusProvider_v2.mq5` theo Architecture Addendum mới trong `.planning/REQUIREMENTS.md`.

Purpose: Gỡ rule quản lý lệnh global đang áp dụng một policy cho mọi strategy, chuyển sang profile theo magic để hành vi quản lý phù hợp từng nhóm strategy nhưng vẫn giữ scope nhỏ, built-in, dễ compile/test.
Output: Một thay đổi surgical trong provider MQL5, không đụng database, có compile MetaEditor 0 errors/0 warnings, static evidence cho mapping/log/no stale close, GSD summary và STATE.md được cập nhật.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/REQUIREMENTS.md
@D:/Aureus/mql5/AureusProvider_v2.mq5
@D:/Aureus/mql5/Build_Rules.md

<interfaces>
Key current code contracts from `mql5/AureusProvider_v2.mq5`:

```mql5
input string InpSymbols = "USDJPY,EURUSD,GBPUSD,AUDUSD";
input double InpBEProfitTarget = 20.0;
input bool InpDebugMode = false;

bool IsForexPair(string symbol);
bool IsImbalanceUp(string symbol, ENUM_TIMEFRAMES timeframe, int index);
bool IsImbalanceDown(string symbol, ENUM_TIMEFRAMES timeframe, int index);

void ProcessPositionsByType(string symbol,
                            long magic,
                            ENUM_POSITION_TYPE target_type,
                            const ulong &tickets[],
                            double total_profit,
                            double total_volume,
                            double weighted_price_sum,
                            datetime earliest_open_time);

void ManagePositionProfitBreakEvent();
```

Current `ManagePositionProfitBreakEvent()` already groups by symbol + magic + direction and calls `ProcessPositionsByType(...)`; preserve that group identity. Current global stale-close block inside `ProcessPositionsByType()` must be removed/disabled for all strategies:

```mql5
if(net_profit > 0 && positions_count == 1 && TimeCurrent() - earliest_open_time > 1800)
  {
   Print(log_prefix, "Closing stale profitable single position.");
   for(int i = 0; i < positions_count; i++)
      trade.PositionClose(tickets[i]);
   return;
  }
```

Do not change command handlers or behaviors for unsupported symbol, history cooldown, `ExecuteCloseOrder`, `ExecuteRequestOrders`, `DoBackfillCountForSymbol`, `ProcessSingleCommand` routing for `REQUEST_BACKFILL_COUNT`, or order request/event semantics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add built-in management profile contracts and magic mapping resolver</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <action>Before editing, run GitNexus impact analysis for `ProcessPositionsByType` and `ManagePositionProfitBreakEvent` if the MQL5 symbols are indexed. Report direct callers/affected process/risk. If GitNexus cannot resolve MQL5 symbols, explicitly document fallback evidence in the summary by citing local call graph from the file: `ManagePositionProfitBreakEvent -> ProcessPositionsByType`, plus timer/on-tick caller if found. Then add a new input string near existing inputs: `InpMagicManagementProfiles = "607000:breakout_protect;2603000:trend_runner;1391000:basket_escape"`. Add small built-in profile constants/names for exactly `conservative`, `trend_runner`, `breakout_protect`, `basket_escape`; implement parser/resolver helpers that map magic to profile from the input string, trim spaces, reject unknown profile names to default `conservative`, and return whether fallback was used. Do not add external config, JSON file parsing, rule DSL, hot reload, or hard-coded `if magic == ...` branches in decision logic.</action>
  <verify>
    <automated>Static check: inspect D:/Aureus/mql5/AureusProvider_v2.mq5 and confirm it contains `InpMagicManagementProfiles`, the three mapped example magics only inside the input default string, built-in profile names `trend_runner`, `breakout_protect`, `basket_escape`, default `conservative`, and no file/JSON/DSL/hot-reload implementation.</automated>
  </verify>
  <done>Magic-to-profile resolution exists, unknown/invalid mappings safely fall back to conservative, and decision logic is profile-driven rather than hard-coded by magic.</done>
</task>

<task type="auto">
  <name>Task 2: Replace global close rule with profile-aware HOLD/MOVE_SL/TRAIL_SL/CLOSE decisions and audit logs</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <action>Update `ProcessPositionsByType` only as much as needed. Preserve existing symbol + magic + direction group inputs and aggregate calculations. Remove/disable the global rule that closes a single profitable position after age > 1800 seconds for every strategy. Add profile-aware decision flow: calculate `profile`, `direction`, `positions_count`, `net_profit`, `age_seconds`; always prefer non-close actions first. Keep existing imbalance-based SL movement as MOVE_SL/TRAIL_SL behavior where applicable before any close. Use conservative default profile to HOLD or MOVE_SL only, with CLOSE allowed only for severe existing risk guard (for example current max loss logic) if kept. Implement `trend_runner` so profitable single positions are not closed merely due to age; it may HOLD or MOVE_SL/TRAIL_SL. Implement `breakout_protect` with any time-stop/tighten behavior scoped only to this profile, not global; prefer tightening/moving SL before close. Implement `basket_escape` so multi-position group can close when net recovery/profit threshold is reached, preserving the existing basket recovery intent. Every decision/action path must log one consistent decision line including at least: symbol, magic, direction, profile, action, reason, positions_count, net_profit, age_seconds, and ticket/target SL when applicable. Do not alter unsupported symbol filtering, history cooldown helpers, `CLOSE_ORDER`, `REQUEST_ORDERS`, or `REQUEST_BACKFILL_COUNT` code paths.</action>
  <verify>
    <automated>Static check: confirm no remaining global condition equivalent to `net_profit > 0 && positions_count == 1 && TimeCurrent() - earliest_open_time > 1800` closes positions outside a profile-specific branch; confirm decision logs include all required field names; confirm `ProcessPositionsByType` resolves profile by magic and contains no direct magic-number branches; confirm `ManagePositionProfitBreakEvent` still groups by symbol + magic + direction.</automated>
  </verify>
  <done>Position management behavior is strategy/profile-aware, global stale profitable single close is gone, close decisions are conservative/profile-specific, and logs are audit-ready.</done>
</task>

<task type="auto">
  <name>Task 3: Compile, run static safeguards, and update planning artifacts</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5, D:/Aureus/.planning/quick/260501-i1p-tri-n-khai-strategy-aware-position-manag/260501-i1p-SUMMARY.md, D:/Aureus/.planning/STATE.md</files>
  <action>Compile exactly per `D:/Aureus/mql5/Build_Rules.md` using MetaEditor64, adapted to target `D:/Aureus/mql5/AureusProvider_v2.mq5`, and loop until the compile log has 0 errors and 0 warnings. Run static checks for: profile mapping input exists; default conservative and three built-in profiles exist; no hard-coded magic branches in decision logic; no external config/JSON/DSL/hot reload; no global stale profitable single close remains; required decision log fields exist; forbidden code paths (`ExecuteCloseOrder`, `ExecuteRequestOrders`, `DoBackfillCountForSymbol`, `REQUEST_BACKFILL_COUNT`, history cooldown functions, unsupported symbol checks) were not intentionally modified. Run `gitnexus_detect_changes()` before any commit/finish; if MCP tool is unavailable, document fallback by reviewing `git diff -- mql5/AureusProvider_v2.mq5 .planning/STATE.md` and confirming touched symbols/files. Create the quick summary with assumptions, changed behavior, compile evidence, static check evidence, GitNexus/fallback evidence, and no-database-impact note. Update `.planning/STATE.md` Quick Tasks Completed with this quick task and status.</action>
  <verify>
    <automated>cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\AureusProvider_v2.mq5" /log:"D:\Aureus\mql5\AureusProvider_v2_compile.log"" && static checks over D:/Aureus/mql5/AureusProvider_v2.mq5 confirm mapping/log/no global stale close/no external config, then GitNexus detect_changes or documented fallback diff review passes.</automated>
  </verify>
  <done>MetaEditor compile is 0 errors/0 warnings, static safeguards pass, summary and STATE are updated, and database behavior remains untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 input parameters -> provider decision engine | `InpMagicManagementProfiles` is operator-provided text parsed at provider startup/runtime decision time. |
| Provider decision engine -> broker trade operations | Profile decisions can move SL or close positions through `trade.PositionModify`/`trade.PositionClose`. |
| Gateway commands -> provider command handlers | Existing OPEN/CLOSE/REQUEST commands remain out of scope and must not regress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260501-i1p-01 | Tampering | `InpMagicManagementProfiles` parser | mitigate | Accept only `magic:profile` pairs where magic parses to non-zero long and profile is one of built-in names; invalid entries fall back to conservative and log fallback. |
| T-260501-i1p-02 | Repudiation | Position management decisions | mitigate | Emit decision log with symbol, magic, direction, profile, action, reason, positions_count, net_profit, age_seconds, and target ticket/SL when applicable. |
| T-260501-i1p-03 | Elevation of privilege | Profile decision close path | mitigate | No hard-coded magic branches; CLOSE only through profile-specific rules or retained severe risk guard, with HOLD/MOVE_SL/TRAIL_SL evaluated first. |
| T-260501-i1p-04 | Denial of service | Unsupported profile/malformed mapping | mitigate | Parser must not fail provider init; unknown profile/malformed pair uses conservative default instead of stopping command processing. |
| T-260501-i1p-05 | Tampering | Existing command/history flows | mitigate | Do not modify unsupported symbol behavior, history cooldown, CLOSE_ORDER, REQUEST_ORDERS, REQUEST_BACKFILL_COUNT; verify by diff/static review. |
</threat_model>

<verification>
- MetaEditor compile command from `D:/Aureus/mql5/Build_Rules.md` adapted to `AureusProvider_v2.mq5` returns 0 errors and 0 warnings.
- Static checks confirm mapping input and built-in profiles are present.
- Static checks confirm no external config/JSON/DSL/hot reload was added.
- Static checks confirm no global single profitable age > 30m close remains outside profile-specific logic.
- Static checks confirm decision logs have required audit fields.
- Diff review confirms unsupported symbol/history cooldown/CLOSE_ORDER/REQUEST_ORDERS/REQUEST_BACKFILL_COUNT behavior was not changed.
- `gitnexus_detect_changes()` runs before finish; if unavailable for MQL5, summary records fallback evidence and impacted symbols.
</verification>

<success_criteria>
- `mql5/AureusProvider_v2.mq5` compiles with 0 errors and 0 warnings.
- Management grouping remains symbol + magic + direction.
- Magic mapping example is present as input string: `607000:breakout_protect;2603000:trend_runner;1391000:basket_escape`.
- Default profile is conservative; built-in profiles are exactly conservative/trend_runner/breakout_protect/basket_escape for this scope.
- No hard-coded magic branches exist in decision logic.
- Global stale profitable single close is removed/disabled for all strategies.
- HOLD/MOVE_SL/TRAIL_SL are preferred before CLOSE.
- Decision action logs include required audit fields.
- No database behavior is touched.
- GSD summary and STATE.md are updated.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260501-i1p-tri-n-khai-strategy-aware-position-manag/260501-i1p-SUMMARY.md`.
</output>
