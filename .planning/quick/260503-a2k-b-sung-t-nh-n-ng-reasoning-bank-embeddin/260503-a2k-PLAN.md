---
phase: 260503-a2k-b-sung-t-nh-n-ng-reasoning-bank-embeddin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-db-writer/migrations/add_reasoning_entries_embeddings.sql
  - services/aureus-trader/reasoning_embeddings.py
  - services/aureus-trader/journal.py
  - services/aureus-trader/scripts/backfill_reasoning_embeddings.py
  - services/aureus-trader/scripts/verify_reasoning_bank_embeddings_e2e.py
  - services/aureus-trader/tests/test_reasoning_embeddings.py
  - services/aureus-trader/tests/test_journal.py
autonomous: true
requirements:
  - QUICK-260503-A2K
must_haves:
  truths:
    - "Executor xác minh service embedding tại http://localhost:8005 hoạt động trước khi gọi production code."
    - "Bảng aureus_reasoning_entries có pgvector/vector columns cho reasoning, prompt/context raw text nếu có, và index similarity."
    - "Reasoning Bank tự tạo embedding khi lưu reasoning mới, nhưng lỗi embedding không chặn journal lifecycle."
    - "Có semantic similarity search trả về reasoning entries gần nghĩa theo vector distance."
    - "Backfill chỉ tạo embedding từ text thật: reasoning_text và raw prompt/context nếu đã tồn tại trong DB/event payload/journal/signal snapshot; không bao giờ embed prompt_digest hoặc input_context_hash."
    - "Nếu raw prompt/context chưa được lưu, plan thêm capture field tối thiểu cho future entries và đánh dấu old prompt/context backfill unavailable."
  artifacts:
    - path: "services/aureus-db-writer/migrations/add_reasoning_entries_embeddings.sql"
      provides: "pgvector extension, embedding columns, optional raw_prompt_text/raw_context_text capture columns, vector indexes, similarity SQL function nếu cần"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
    - path: "services/aureus-trader/reasoning_embeddings.py"
      provides: "Embedding client, endpoint discovery, health/link verification, semantic search helpers, embeddable text source selection"
      exports: ["ReasoningEmbeddingClient", "verify_embedding_service", "semantic_search_reasoning_entries"]
    - path: "services/aureus-trader/scripts/backfill_reasoning_embeddings.py"
      provides: "Discovery + backfill CLI cho reasoning_text và raw prompt/context text nếu có; hash/digest fields reported unavailable for semantic backfill"
    - path: "services/aureus-trader/scripts/verify_reasoning_bank_embeddings_e2e.py"
      provides: "DB/runtime e2e kiểm tra endpoint 8005, migration, insert, future raw text capture, backfill, semantic search"
  key_links:
    - from: "services/aureus-trader/reasoning_embeddings.py"
      to: "http://localhost:8005"
      via: "health/link discovery then embedding POST"
      pattern: "localhost:8005"
    - from: "services/aureus-trader/journal.py"
      to: "services/aureus-trader/reasoning_embeddings.py"
      via: "non-blocking embedding creation after aureus_reasoning_entries insert"
      pattern: "ReasoningEmbeddingClient|embed"
    - from: "services/aureus-trader/scripts/backfill_reasoning_embeddings.py"
      to: "aureus_reasoning_entries"
      via: "SELECT rows with missing embeddings then UPDATE vector columns only from real text columns"
      pattern: "reasoning_embedding|prompt_embedding|context_embedding"
    - from: "services/aureus-trader/scripts/backfill_reasoning_embeddings.py"
      to: "aureus_reasoning_entries.prompt_digest|aureus_reasoning_entries.input_context_hash"
      via: "explicit exclusion from semantic embedding source selection"
      pattern: "prompt_digest|input_context_hash"
    - from: "services/aureus-trader/scripts/verify_reasoning_bank_embeddings_e2e.py"
      to: "semantic_search_reasoning_entries"
      via: "query embedding plus vector distance query"
      pattern: "semantic_search_reasoning_entries"
---

<objective>
Bổ sung Reasoning Bank embedding tối thiểu: client cho embedding model ở `http://localhost:8005`, migration pgvector/vector columns, semantic similarity search, discovery nguồn prompt/context raw text, backfill embedding cho text thật đang có.

Purpose: Reasoning Bank không chỉ lưu lifecycle decision, mà còn truy vấn lại các quyết định gần nghĩa bằng embedding, không tạo embedding rác từ hash/digest không có nghĩa ngữ nghĩa.
Output: Migration SQL, embedding client/helper, journal wiring non-blocking, source-discovery/backfill CLI, e2e DB/runtime verification.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@RUN_SERVICES.md
@.planning/quick/260502-vw9-clone-project-n-y-v-v-ph-n-t-ch-t-m-hi-u/260502-vw9-REASONING-BANK-REPORT.md
@.planning/quick/260502-wur-th-c-hi-n-implement-theo-plan-planning-q/260502-wur-SUMMARY.md
@.planning/quick/260502-wur-th-c-hi-n-implement-theo-plan-planning-q/260502-wur-VERIFICATION.md
@services/aureus-db-writer/migrations/add_reasoning_entries.sql
@services/aureus-trader/journal.py
@services/aureus-trader/scripts/verify_reasoning_bank_db_e2e.py

<interfaces>
Existing `aureus_reasoning_entries` columns from MVP migration: `id`, `trace_id`, `trade_journal_id`, `signal_snapshot_id`, `strategy_id`, `strategy_name`, `symbol`, `timeframe`, `direction`, `reasoning_source`, `prompt_digest`, `decision_digest`, `input_context_hash`, `reasoning_text`, `decision_action`, `confidence`, `active_signals`, `context_filters`, `ticket`, `pending_order_id`, `entry_time`, `exit_time`, `success`, `reward`, `pnl`, `pnl_pips`, `result`, `created_at`, `evaluated_at`, `updated_at`.

Existing journal insertion point: `TradeJournalManager.on_strategy_match()` inserts `aureus_reasoning_entries` after `aureus_trade_journal` insert. Embedding creation must be added there as best-effort only, never returning False because embedding service is down.

Checker constraint: `prompt_digest` and `input_context_hash` are identifiers only. They have no semantic meaning and MUST NOT be embedded as prompt/context. Prompt/context embedding source must be raw text from DB/event payload/journal/signal snapshot, or be recorded as unavailable for historical rows. If no raw prompt/context source exists, add minimal nullable capture columns/source fields for future entries and backfill only `reasoning_text` for old rows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo embedding client và khám phá contract endpoint 8005</name>
  <files>services/aureus-trader/reasoning_embeddings.py, services/aureus-trader/tests/test_reasoning_embeddings.py</files>
  <behavior>
    - `verify_embedding_service("http://localhost:8005")` thử health endpoints phổ biến (`/health`, `/v1/models`, `/docs` nếu cần) và xác nhận service reachable.
    - Nếu contract embedding chưa biết, client thử an toàn các endpoint phổ biến theo thứ tự cấu hình được: `/v1/embeddings` OpenAI-compatible với body `{ "input": text, "model": model }`, sau đó `/embed` với `{ "text": text }`; dừng khi nhận vector số.
    - Client chuẩn hóa response phổ biến: OpenAI `data[0].embedding`, direct `embedding`, hoặc `vector`.
    - Khi service lỗi/unreachable, raise exception rõ trong verify path; journal path sau này sẽ bắt lỗi và log warning.
    - `semantic_search_reasoning_entries(conn, query_text, limit, client)` tạo query embedding rồi tìm bằng pgvector distance, không hardcode kết quả.
    - Helper chọn source embedding chỉ nhận text thật: `reasoning_text`, raw prompt text, raw context text; reject/skip `prompt_digest`, `decision_digest`, `input_context_hash`.
  </behavior>
  <action>Trước khi sửa symbol mới/chạm symbol hiện có, dùng GitNexus theo CLAUDE.md: nếu sửa `TradeJournalManager.on_strategy_match` ở task sau thì chạy `gitnexus_impact({target: "TradeJournalManager.on_strategy_match", direction: "upstream"})`; với file mới thì không cần impact symbol. Tạo `reasoning_embeddings.py` chứa `ReasoningEmbeddingClient(base_url="http://localhost:8005", model=None, timeout=10)`, `verify_embedding_service(base_url)`, `semantic_search_reasoning_entries(conn, query_text, limit=10, client=None)`, và source-selection helper không bao giờ embed hash/digest fields. Dùng stdlib `urllib.request` hoặc dependency đã có; không thêm dashboard, LLM judge, Telegram, hoặc strategy retrieval injection. Test unit mock HTTP response, SQL call, và case hash/digest bị skip; không cần gọi service thật trong unit.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-trader && python -m pytest tests/test_reasoning_embeddings.py -q</automated>
  </verify>
  <done>Client verify được link `http://localhost:8005`, tạo embedding vector float list từ contract phát hiện được, semantic search dùng pgvector distance, hash/digest source bị reject/skip trong tests, và unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thêm pgvector migration, raw prompt/context capture tối thiểu, và journal wiring non-blocking</name>
  <files>services/aureus-db-writer/migrations/add_reasoning_entries_embeddings.sql, services/aureus-trader/journal.py, services/aureus-trader/tests/test_journal.py</files>
  <behavior>
    - Trước khi schema/code final, khám phá raw prompt/context sources hiện có trong DB/event payload/journal/signal snapshot: đọc `on_strategy_match` input payload, `aureus_trade_journal` fields, signal snapshot row/json nếu có, và existing `aureus_reasoning_entries` columns.
    - Migration idempotent: `CREATE EXTENSION IF NOT EXISTS vector`, thêm `reasoning_embedding vector`, `prompt_embedding vector`, `context_embedding vector`, metadata columns cần thiết như `embedding_model`, `embedded_at` nếu hữu ích.
    - Nếu raw prompt/context chưa tồn tại trong schema, migration thêm nullable capture columns tối thiểu như `prompt_text`/`context_text` hoặc tên theo convention hiện có; không backfill chúng từ digest/hash.
    - Index similarity dùng ivfflat/hnsw nếu pgvector hỗ trợ; nếu môi trường không hỗ trợ index type đó, migration vẫn chạy được hoặc fallback rõ bằng SQL tương thích.
    - `TradeJournalManager.on_strategy_match()` sau khi insert reasoning entry sẽ best-effort tạo embedding cho `reasoning_text` và raw prompt/context text nếu available từ payload/snapshot/capture columns; mọi exception chỉ log warning và vẫn return True.
    - Không embed `prompt_digest`, `decision_digest`, hoặc `input_context_hash`; chúng chỉ dùng để link/audit.
  </behavior>
  <action>Chạy GitNexus impact trước khi sửa `TradeJournalManager.on_strategy_match` và báo blast radius trong SUMMARY: `gitnexus_impact({target: "TradeJournalManager.on_strategy_match", direction: "upstream"})`. Tạo migration riêng thay vì sửa migration MVP để giữ lịch sử. Trong task này, executor phải ghi kết quả discovery nguồn raw prompt/context vào SUMMARY: nguồn nào có text thật, nguồn nào không có. Sửa tối thiểu `journal.py`: import helper/client, sau `INSERT ... RETURNING id` dùng returned reasoning entry id để update embedding columns best-effort từ source text thật. Nếu raw prompt/context chưa có trong event payload hoặc DB/snapshot, chỉ capture nullable future fields khi có giá trị mới và để historical prompt/context embedding NULL. Update test journal để chứng minh embedding failure non-blocking, raw prompt/context future capture nếu có source, và digest/hash không bị gửi vào client.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-trader && python -m pytest tests/test_journal.py tests/test_reasoning_embeddings.py -q</automated>
  </verify>
  <done>Migration thêm vector schema idempotent và minimal raw prompt/context capture nếu cần; journal tạo embedding từ `reasoning_text` và raw prompt/context text nếu có, bỏ qua khi service lỗi, không embed digest/hash, existing Reasoning Bank MVP tests vẫn pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tạo source-discovery backfill và DB/runtime e2e verification</name>
  <files>services/aureus-trader/scripts/backfill_reasoning_embeddings.py, services/aureus-trader/scripts/verify_reasoning_bank_embeddings_e2e.py, services/aureus-trader/tests/test_reasoning_embeddings.py</files>
  <behavior>
    - Backfill kết nối DB theo `AUREUS_DB_DSN` hoặc `config.load_config()`, apply migration, discover raw prompt/context text sources trong `aureus_reasoning_entries`, linked journal row, linked signal snapshot/json payload nếu tồn tại.
    - Backfill chọn rows thiếu embedding, gọi service 8005, update `reasoning_embedding` từ `reasoning_text`, `prompt_embedding` từ raw prompt text nếu có, `context_embedding` từ raw context text nếu có.
    - Nếu chỉ có `prompt_digest` hoặc `input_context_hash`, backfill in/report `unavailable_raw_prompt_context` và để `prompt_embedding`/`context_embedding` NULL; không tạo embedding từ hash/digest.
    - Backfill có `--limit`, in số rows updated và số rows skipped/unavailable; không xóa data.
    - E2E runtime kiểm tra service `http://localhost:8005` reachable, apply migration, insert trade + reasoning entry test với reasoning_text và raw context/prompt nếu capture source hỗ trợ, backfill/generate embedding, gọi semantic search bằng query gần nghĩa, assert trả về row test, cleanup test data.
    - Nếu command lỗi vì service/DB chưa chạy, executor đọc `RUN_SERVICES.md` và khởi động đúng service trước khi chạy lại.
  </behavior>
  <action>Tạo script backfill và e2e dựa theo pattern `scripts/verify_reasoning_bank_db_e2e.py`. E2E phải thật sự dùng Postgres/pgvector theo CLAUDE.md vì schema DB đổi. Test phải cover negative case: legacy row có `prompt_digest`/`input_context_hash` nhưng không có raw prompt/context thì script không gọi embed cho digest/hash và báo unavailable. Không thêm cron, dashboard, API endpoint mới, hay retrieval injection vào trading flow. Trước khi commit/summarize, chạy `gitnexus_detect_changes()` để xác nhận scope chỉ nằm ở Reasoning Bank embedding files.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-trader && python scripts/verify_reasoning_bank_embeddings_e2e.py</automated>
  </verify>
  <done>E2E PASS chứng minh endpoint 8005 hoạt động, pgvector schema tồn tại, embedding được ghi vào DB từ text thật, legacy prompt/context hash-only rows không bị embedded và được report unavailable, semantic similarity search tìm được reasoning entry test, cleanup sạch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aureus-trader → embedding service 8005 | Local HTTP call nhận text thật từ DB/event payload và trả vector; service có thể down hoặc trả malformed JSON. |
| aureus-trader → Postgres | Vector payload và similarity query ghi/đọc DB; cần parameterized queries. |
| event payload → reasoning bank | Strategy event có text/context không tin cậy đi vào DB và embedding service. |
| hash/digest identifiers → embedding source selection | Identifier có thể bị nhầm thành semantic text; phải bị loại khỏi embedding source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260503-a2k-01 | T/I | `ReasoningEmbeddingClient` response parsing | mitigate | Validate response là list số hữu hạn trước khi update DB; reject malformed vector. |
| T-260503-a2k-02 | D | `http://localhost:8005` dependency | mitigate | Journal path bắt exception và log warning; e2e verify link riêng để phát hiện runtime issue. |
| T-260503-a2k-03 | I | raw reasoning/prompt/context sent to local embedding service | accept | Service là localhost theo yêu cầu; chỉ gửi text thật đã được Reasoning Bank capture, không gửi secrets/env; không embed identifier hashes. |
| T-260503-a2k-04 | T/E | semantic search SQL | mitigate | Use asyncpg parameter binding; no string interpolation for query text/vector/limit except validated integer limit. |
| T-260503-a2k-05 | I/T | prompt/context source discovery | mitigate | Treat `prompt_digest`, `decision_digest`, `input_context_hash` as non-semantic identifiers; tests verify they are never passed to embedding client. |
</threat_model>

<verification>
1. Unit: `cd D:/Aureus/services/aureus-trader && python -m pytest tests/test_reasoning_embeddings.py tests/test_journal.py -q`
2. Runtime link: `cd D:/Aureus/services/aureus-trader && python scripts/verify_reasoning_bank_embeddings_e2e.py`
3. DB e2e bắt buộc vì đổi schema: script e2e phải apply migration và assert vector columns/search hoạt động trên Postgres thật.
4. Source correctness: tests/e2e phải chứng minh `prompt_digest` và `input_context_hash` không bao giờ được embedded; old prompt/context hash-only rows được report unavailable.
5. GitNexus: impact analysis trước khi sửa `TradeJournalManager.on_strategy_match`; `gitnexus_detect_changes()` trước khi kết thúc.
</verification>

<success_criteria>
- `http://localhost:8005` được verify bằng script e2e, không chỉ hardcode URL.
- `aureus_reasoning_entries` có vector columns và pgvector extension/index/search hoạt động.
- Reasoning entry mới có embedding best-effort từ `reasoning_text` và raw prompt/context text nếu có; embedding service lỗi không làm hỏng journal lifecycle.
- Backfill điền embedding cho rows thiếu từ `reasoning_text` và raw prompt/context text đang tồn tại; không embed `prompt_digest`, `decision_digest`, hoặc `input_context_hash`.
- Nếu raw prompt/context chưa được stored cho old rows, backfill đánh dấu unavailable và giữ prompt/context embeddings NULL; future capture columns/source fields nullable được thêm tối thiểu nếu cần.
- Semantic similarity search trả về row test dựa trên query gần nghĩa.
- Không thêm LLM judge, dashboard, Telegram, hoặc retrieval injection ngoài semantic search verification.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/quick/260503-a2k-b-sung-t-nh-n-ng-reasoning-bank-embeddin/260503-a2k-SUMMARY.md` bằng tiếng Việt.
</output>
