---
phase: "260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - ".planning/quick/260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio/260503-o5w-REPORT.md"
  - "mql5/AureusProvider_v2.mq5"
autonomous: true
requirements:
  - "QUICK-260503-O5W"
must_haves:
  truths:
    - "Người đọc hiểu `ManagePositionDecision breakout_protect HOLD breakout_profit_below_protection_threshold` nghĩa là nhóm lệnh breakout_protect chưa đạt ngưỡng profit để bảo vệ SL."
    - "Người đọc thấy nguyên nhân log lặp với BTCUSD/ETHUSD là `ManagePositionProfitBreakEvent()` chạy theo timer, gom từng group symbol+magic+direction, rồi gọi `LogManagementDecision()` mỗi vòng khi net_profit còn dưới `positions_count * InpBEProfitTarget / 2`."
    - "Report có source evidence từ `mql5/AureusProvider_v2.mq5` và summary quick trước cho logic suppress HOLD hiện có."
    - "Source luôn suppress repeated HOLD reason `breakout_profit_below_protection_threshold` bằng key hiện có symbol+magic+direction+reason; CLOSE/error/non-HOLD logs giữ nguyên."
  artifacts:
    - path: ".planning/quick/260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio/260503-o5w-REPORT.md"
      provides: "Báo cáo ý nghĩa log, root cause spam, source evidence, validation và source fix đã áp dụng"
      contains: "breakout_profit_below_protection_threshold"
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Nguồn quản lý position với surgical HOLD suppression bắt buộc"
      contains: "ShouldSuppressRepeatedHoldDecisionLog"
  key_links:
    - from: "mql5/AureusProvider_v2.mq5"
      to: "LogManagementDecision"
      via: "ProcessBreakoutProtectPositionsByType HOLD branch"
      pattern: "breakout_profit_below_protection_threshold"
    - from: "mql5/AureusProvider_v2.mq5"
      to: "ShouldSuppressRepeatedHoldDecisionLog"
      via: "required fix expands existing reason allowlist"
      pattern: "reason != \"profile_fallback\" && reason != \"legacy_no_rule_matched\""
---

# Quick Plan 260503-o5w: Phân tích log ManagePositionDecision breakout HOLD và stop spam

## Objective

Phân tích chính xác ý nghĩa log `ManagePositionDecision symbol=BTCUSD/ETHUSD profile=breakout_protect action=HOLD reason=breakout_profit_below_protection_threshold`, xác minh bằng source, tạo report, rồi áp dụng fix nhỏ bắt buộc: thêm reason này vào once/key suppression hiện có, không đụng CLOSE/error/non-HOLD logs.

Purpose: loại bỏ nhầm lẫn giữa log HOLD bình thường và lỗi quản lý position; giảm spam MT5 journal mà không che log quan trọng.

Output: report markdown và one-line allowlist change trong `mql5/AureusProvider_v2.mq5`.

## Execution Context

- `D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md`
- `D:/Aureus/.claude/get-shit-done/templates/summary.md`

## Context

- `D:/Aureus/CLAUDE.md`
- `D:/Aureus/.planning/STATE.md`
- `D:/Aureus/mql5/AureusProvider_v2.mq5`
- `D:/Aureus/.planning/quick/260503-neg-fix-spam-managepositiondecision-hold-pro/260503-neg-SUMMARY.md`

### Source facts already found

- `HoldDecisionLogState` key gồm `symbol`, `magic`, `direction`, `reason`.
- `ShouldSuppressRepeatedHoldDecisionLog(...)` hiện chỉ suppress repeated HOLD cho `profile_fallback` và `legacy_no_rule_matched`.
- `LogManagementDecision(...)` giữ format `[ManagePositionDecision] symbol=... action=... reason=...` và gọi suppression trước `PrintFormat`.
- `ProcessBreakoutProtectPositionsByType(...)` log `HOLD` với reason `breakout_profit_below_protection_threshold` khi `net_profit <= positions_count * InpBEProfitTarget / 2`.
- `InpBEProfitTarget` default là `20.0`, nên threshold default là `positions_count * 10.0` USD.
- Previous quick `260503-neg` quyết định chỉ suppress repeated HOLD theo key `symbol+magic+direction+reason`, giữ nguyên CLOSE/error/non-HOLD logs.

## Tasks

<task type="auto">
  <name>Task 1: Xác minh meaning và root cause bằng source</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5, D:/Aureus/.planning/quick/260503-neg-fix-spam-managepositiondecision-hold-pro/260503-neg-SUMMARY.md</files>
  <action>Đọc đúng các vùng source liên quan: `InpBEProfitTarget`, `ShouldSuppressRepeatedHoldDecisionLog`, `LogManagementDecision`, `ProcessBreakoutProtectPositionsByType`, `ProcessPositionsByType`, `ManagePositionProfitBreakEvent`, và timer/callsite nếu cần. Kết luận phải nêu rõ: log không phải lỗi; đó là HOLD decision của profile `breakout_protect`; root cause lặp là management loop chạy liên tục và condition vẫn true; BTCUSD/ETHUSD xuất hiện vì đang có open position groups thuộc configured symbols/profile mapping/magic tương ứng. Không suy đoán ngoài source; nếu thiếu runtime account data thì ghi limitation.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p=Path('D:/Aureus/mql5/AureusProvider_v2.mq5')
s=p.read_text(encoding='utf-8', errors='ignore')
need=['InpBEProfitTarget','ShouldSuppressRepeatedHoldDecisionLog','LogManagementDecision','ProcessBreakoutProtectPositionsByType','breakout_profit_below_protection_threshold','ManagePositionProfitBreakEvent']
missing=[x for x in need if x not in s]
assert not missing, missing
print('source evidence anchors present')
PY</automated>
  </verify>
  <done>Có ghi chú evidence đầy đủ để report giải thích được meaning + root cause lặp, gồm threshold formula và call path.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo report chính và source fix decision</name>
  <files>D:/Aureus/.planning/quick/260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio/260503-o5w-REPORT.md</files>
  <action>Tạo report tiếng Việt, markdown, không HTML. Nội dung bắt buộc: `Kết luận ngắn`, `Log này nghĩa là gì`, `Vì sao lặp liên tục`, `Source evidence` với file và line/function names, `BTCUSD/ETHUSD vì sao xuất hiện`, `Fix recommendation`, `Validation plan`, `Limitations`. Report phải giải thích fix sẽ được áp dụng bắt buộc ở Task 3: thêm `breakout_profit_below_protection_threshold` vào allowlist suppress repeated HOLD trong helper hiện có; không thay behavior quản lý lệnh; không suppress CLOSE/error/non-HOLD; không commit `mql5/AureusProvider_v2.ex5` hoặc `stable/`.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p=Path('D:/Aureus/.planning/quick/260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio/260503-o5w-REPORT.md')
s=p.read_text(encoding='utf-8')
need=['breakout_profit_below_protection_threshold','ProcessBreakoutProtectPositionsByType','LogManagementDecision','ShouldSuppressRepeatedHoldDecisionLog','BTCUSD','ETHUSD','Fix recommendation','Limitations']
missing=[x for x in need if x not in s]
assert not missing, missing
assert '<' not in s or 'http' in s
print('report assertions passed')
PY</automated>
  </verify>
  <done>Report tồn tại, giải thích đúng ý nghĩa/root cause bằng source evidence, nêu fix bắt buộc và phạm vi bảo toàn CLOSE/error/non-HOLD logs.</done>
</task>

<task type="auto">
  <name>Task 3: Surgical source fix và validation</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5, D:/Aureus/.planning/quick/260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio/260503-o5w-REPORT.md</files>
  <action>Trước khi edit phải cố chạy GitNexus impact cho symbol `ShouldSuppressRepeatedHoldDecisionLog` hoặc `LogManagementDecision`; nếu MQL5 symbol không indexed/CLI unavailable thì ghi limitation vào report. Edit duy nhất trong body `ShouldSuppressRepeatedHoldDecisionLog`: thêm `breakout_profit_below_protection_threshold` vào allowlist reason của HOLD suppression, dùng cùng key `symbol+magic+direction+reason`. Không sửa threshold, không sửa quản lý position, không suppress `CLOSE`, `TRAIL_SL`, `MOVE_SL`, close failure, market guard, `ORDER_FAILED`, hoặc unknown profile logs. Sau edit, chạy source assertions và `git diff --check`. Nếu MetaEditor có trong PATH thì compile; nếu không có, ghi limitation vào report. Trước commit nếu có commit, chạy GitNexus detect changes; nếu tool unavailable, ghi limitation. Không stage/commit `D:/Aureus/mql5/AureusProvider_v2.ex5` hoặc `D:/Aureus/stable/`.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
import re
p=Path('D:/Aureus/mql5/AureusProvider_v2.mq5')
s=p.read_text(encoding='utf-8', errors='ignore')
m=re.search(r'bool\s+ShouldSuppressRepeatedHoldDecisionLog\s*\([^)]*\)\s*\{(?P<body>.*?)\n\}', s, re.S)
assert m, 'ShouldSuppressRepeatedHoldDecisionLog body not found'
body=m.group('body')
assert 'breakout_profit_below_protection_threshold' in body, 'missing breakout HOLD allowlist in helper body'
assert 'action != "HOLD"' in body, 'helper must preserve non-HOLD gate'
assert 'profile_fallback' in body and 'legacy_no_rule_matched' in body, 'existing HOLD suppress reasons must remain'
assert 'PrintFormat("[ManagePositionDecision]' in s, 'ManagePositionDecision log print must remain'
print('required fix helper-body assertions passed')
PY
git -C D:/Aureus diff --check -- mql5/AureusProvider_v2.mq5 .planning/quick/260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio/260503-o5w-REPORT.md</automated>
  </verify>
  <done>Repeated HOLD `breakout_profit_below_protection_threshold` chỉ log lần đầu theo key hiện có; CLOSE/error/non-HOLD logs không đổi; validation source assertions và diff check pass hoặc limitation được ghi trong report cho tool ngoài source.</done>
</task>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|---|---|
| MT5 journal/log sink | Provider writes local terminal logs; change can hide operational signal if suppression too broad. |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260503-o5w-01 | Repudiation | `LogManagementDecision` | mitigate | Suppress only one repeated HOLD reason via existing key; preserve CLOSE/error/non-HOLD logs. |
| T-260503-o5w-02 | Denial of Service | MT5 journal | mitigate | Reduce repeated low-value HOLD spam for stable below-threshold condition. |
| T-260503-o5w-03 | Tampering | MQL5 management logic | accept | No trade decision thresholds or close/SL logic should change; verification checks narrow source diff. |

## Verification

1. Report assertions pass.
2. Source fix assertion confirms `breakout_profit_below_protection_threshold` exists inside `ShouldSuppressRepeatedHoldDecisionLog`.
3. `git -C D:/Aureus diff --check -- mql5/AureusProvider_v2.mq5 .planning/quick/260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio/260503-o5w-REPORT.md` passes.
4. If MetaEditor exists, MQL5 compile passes; else report states compile limitation.
5. If GitNexus tools can inspect MQL5 symbols, impact and detect changes run; else report states exact limitation.
6. `mql5/AureusProvider_v2.ex5` and `stable/` remain uncommitted.

## Success Criteria

- User can answer: log means below protection profit threshold, not failure.
- User can trace repeated logs to exact call path and formula.
- Report gives source-backed root cause for BTCUSD/ETHUSD repetition.
- Required fix is surgical and validation-backed: repeated HOLD `breakout_profit_below_protection_threshold` is suppressed by existing helper key; CLOSE/error/non-HOLD logs stay visible.

## Output

After completion, create `D:/Aureus/.planning/quick/260503-o5w-ki-m-tra-ngh-a-log-managepositiondecisio/260503-o5w-SUMMARY.md`.
