---
phase: 260506-qy2-pivot-sl-time-order
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/orders.py
  - services/aureus-signal/tests/test_pivot_sl.py
autonomous: true
requirements:
  - QY2-01
must_haves:
  truths:
    - "Khi sl_mode == 'PIVOT_POINT', valid pivots được chọn theo thứ tự thời gian gần hiện tại nhất sau khi lọc hợp lệ, không theo giá lớn nhất/bé nhất."
    - "BUY và SELL đều chọn pivot gần hiện tại nhất thỏa điều kiện 5-candle filter và pivot_index."
    - "Regression tests chứng minh logic cũ theo price-sort hoặc theo thứ tự swing_points đầu vào bị thay bằng time-sort rõ ràng."
  artifacts:
    - path: "services/aureus-signal/engine/orders.py"
      provides: "PIVOT_POINT SL selection sorted by pivot time recency"
      contains: "PIVOT_POINT"
    - path: "services/aureus-signal/tests/test_pivot_sl.py"
      provides: "Regression tests for time-based PIVOT_POINT SL selection"
      contains: "pivot_point"
  key_links:
    - from: "services/aureus-signal/engine/orders.py::_find_pivot_for_sl_candidates"
      to: "services/aureus-signal/engine/orders.py::_calculate_sl_tp"
      via: "candidate price and time metadata passed into PIVOT_POINT branch"
      pattern: "_find_pivot_for_sl_candidates\(side, state_obj\)"
    - from: "services/aureus-signal/tests/test_pivot_sl.py"
      to: "services/aureus-signal/engine/orders.py::_calculate_sl_tp"
      via: "unit tests call PIVOT_POINT branch"
      pattern: "_calculate_sl_tp"
---

<objective>
Sửa PIVOT_POINT stop-loss để chọn pivot gần hiện tại nhất theo thời gian sau khi lọc hợp lệ, không chọn theo giá cao nhất cho BUY hoặc thấp nhất cho SELL, và không phụ thuộc thứ tự `state_obj.swing_points` đầu vào.

Purpose: Đảm bảo SL theo pivot phản ánh swing gần hiện tại nhất, đúng yêu cầu giao dịch.
Output: Logic chọn pivot trong `orders.py` và regression tests trong `test_pivot_sl.py`.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@D:/Aureus/services/aureus-signal/engine/orders.py
@D:/Aureus/services/aureus-signal/tests/test_pivot_sl.py

Key current behavior:
- `orders.py::_find_pivot_for_sl_candidates` scans `state_obj.swing_points` in reverse order and returns pivot prices only.
- `orders.py::_calculate_sl_tp` currently builds `valid_pivots: List[float]`, then runs `valid_pivots.sort(reverse=('BUY' in side))`, causing BUY to prefer highest price and SELL to prefer lowest price.
- Existing tests `test_pivot_point_buy_sorts_valid_pivots_descending_by_price`, `test_pivot_point_sell_sorts_valid_pivots_ascending_by_price`, and `test_pivot_point_pivot_index_applies_after_price_sort` encode old behavior and must be changed.
- Checker requirement: `valid_pivots` must be explicitly sorted by pivot time before applying `pivot_index`. Existing/reversed `swing_points` order is not acceptable because input can be unordered.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unordered swing_points regression tests for time-recency pivot selection</name>
  <files>services/aureus-signal/tests/test_pivot_sl.py</files>
  <behavior>
    - BUY case: `swing_points` input is intentionally unordered; LL pivots include newest valid pivot that is not highest price; pivot_index=1 must select newest valid pivot by `t`, not highest price and not list/reverse-list order.
    - SELL case: `swing_points` input is intentionally unordered; HH pivots include newest valid pivot that is not lowest price; pivot_index=1 must select newest valid pivot by `t`, not lowest price and not list/reverse-list order.
    - pivot_index case: after 5-candle filter, explicitly sorted valid pivots by `t` descending make pivot_index=2 select second newest valid pivot by time, not second by price or input order.
  </behavior>
  <action>Before editing test methods, run GitNexus impact analysis for affected test symbols if MCP is available: `gitnexus_impact({target: "TestPivotSL", direction: "upstream"})` or most specific test class/function name shown by GitNexus. If GitNexus MCP is unavailable, document limitation in SUMMARY and continue because test-only symbol edits have local blast radius. Update existing price-sort tests to assert time-recency behavior with deliberately unordered `swing_points`. Ensure test data would fail if implementation only relies on current reversed `swing_points` traversal. Do not add broad new fixtures or refactor unrelated tests.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_pivot_sl.py -x</automated>
  </verify>
  <done>Tests fail before production fix because old `valid_pivots.sort(reverse=('BUY' in side))` selects by price, and any implementation relying only on reversed `swing_points` input order also fails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry pivot time metadata and sort valid pivots before pivot_index</name>
  <files>services/aureus-signal/engine/orders.py</files>
  <behavior>
    - `_find_pivot_for_sl_candidates` returns or internally supplies each candidate with price and pivot time metadata from swing point field `t`.
    - `_calculate_sl_tp` filters candidates with existing 5-candle rules, then sorts `valid_pivots` by pivot time `t` descending after filtering.
    - `pivot_index` indexes sorted valid pivots after time sort; no BUY/SELL price sort remains.
  </behavior>
  <action>Before editing production symbols, run GitNexus impact analysis: `gitnexus_impact({target: "_find_pivot_for_sl_candidates", direction: "upstream"})` and `gitnexus_impact({target: "_calculate_sl_tp", direction: "upstream"})`; report direct callers, affected processes, risk. Modify only PIVOT_POINT branch. Carry pivot time metadata through candidate filtering so `valid_pivots` contains enough data to sort by time, for example `{"price": pivot_price, "t": pivot_time}` or equivalent typed tuple. After 5-candle filter accepts a candidate, append price+time to `valid_pivots`; then run an explicit `valid_pivots.sort(key=lambda p: p["t"], reverse=True)` or equivalent descending time sort; only after that apply `pivot_index`. Remove price-based `valid_pivots.sort(reverse=('BUY' in side))`. Preserve 5-candle invalidation, offset_pips handling, pivot_index semantics, logging, and order rejection behavior. Do not rely on existing/reversed `state_obj.swing_points` order. Do not change public return contract unless tests and all direct callers prove safe.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_pivot_sl.py -x</automated>
  </verify>
  <done>PIVOT_POINT SL selects newest valid pivot by `t` for BUY and SELL, with pivot_index applied after validity filtering and explicit descending time sort; old price-sort and input-order-dependent behavior gone.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused verification and GitNexus change check</name>
  <files>services/aureus-signal/engine/orders.py, services/aureus-signal/tests/test_pivot_sl.py</files>
  <action>Run focused tests. Then run `gitnexus_detect_changes()` if MCP is available; if unavailable, document limitation and use `git diff -- services/aureus-signal/engine/orders.py services/aureus-signal/tests/test_pivot_sl.py` as safe fallback. Confirm no database behavior changed; database E2E not required.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && pytest tests/test_pivot_sl.py -x</automated>
  </verify>
  <done>Focused PIVOT_POINT tests pass, changed files limited to planned files, SUMMARY records GitNexus impact/change results or tool limitation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy config/state_obj -> SL calculation | Strategy config and swing point state feed order risk controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-QY2-01 | Tampering | `orders.py::_calculate_sl_tp` PIVOT_POINT branch | mitigate | Preserve existing numeric parsing/filtering and add regression tests so invalid recent candle breaks still reject pivots before selection. |
| T-QY2-02 | Denial of Service | `orders.py::_find_pivot_for_sl_candidates` | accept | Candidate list already bounded by existing swing_points state; change only ordering/selection, no new external input loop. |
| T-QY2-03 | Repudiation | PIVOT_POINT logging | mitigate | Keep existing reject and selected pivot logs so selected pivot can be audited. |
</threat_model>

<verification>
Overall checks:
- `cd D:/Aureus/services/aureus-signal && pytest tests/test_pivot_sl.py -x`
- GitNexus impact run before production symbol edit, or documented MCP limitation.
- `gitnexus_detect_changes()` before completion, or documented MCP limitation plus focused git diff fallback.
</verification>

<success_criteria>
- No PIVOT_POINT branch sorts valid pivots by price.
- Valid pivots are explicitly sorted newest-to-oldest by pivot time `t` after 5-candle filtering.
- BUY, SELL, unordered swing_points, and pivot_index regression tests prove time-recency behavior.
- No DB files or DB behavior touched.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260506-qy2-pivot-sl-time-order/260506-qy2-SUMMARY.md`.
</output>
