# Quick Task 260425-bs6 Plan

## Objective
Thực hiện cải thiện TPO signal theo khuyến nghị trong quick task `260425-bnh`: single-pass profile build và full-block cache cho closed H1/M30 buckets.

## Assumptions
- Giữ nguyên output contract hiện tại của TPO: `POC`, `VAH`, `VAL`, `shape`, `shape_confidence_pct`, `shape_scores_pct`.
- D1 vẫn dùng semantic hiện tại là UTC day (`now_ts - now_ts % 86400`), không đổi sang session day trong task này.
- Không triển khai full incremental histogram ở bước này; chỉ tối ưu duplicate recompute/cache theo Giai đoạn 1 đã đề xuất.

## GitNexus Impact
- `_build_profile`: LOW risk; d=1 indexed caller `_compute_tf`, d=2 `calculate` (index có vẻ cũ so với source hiện tại).
- `TPOSignal`: LOW risk; no upstream impact reported.
- `calculate`: lookup hit symbol cùng tên ở `volume_sma.py`, không đại diện TPO.
- `_compute_sliding` và `_build_tpo_block`: GitNexus target not found, khả năng index chưa nhận private methods mới; scoped change vẫn nằm trong `TPOSignal` và sẽ verify bằng tests + diff.

## Steps
1. Refactor TPO block build single-pass
   - `_build_levels_and_counts()` chạy một lần cho mỗi `_build_tpo_block()`.
   - Derive POC/VAH/VAL từ cùng `levels/counts` dùng cho shape classifier.
   - Verify: unit tests hiện có vẫn pass và có test đếm call.

2. Cache full closed bucket block
   - `state.tpo_cache` lưu full block dict cho closed H1/M30 buckets thay vì tuple profile.
   - `_compute_sliding()` check cache hit trước khi slice session, dùng cached block ngay nếu bucket closed.
   - Verify: test monkeypatch `_build_tpo_block` để cache hit không rebuild.

3. Preserve current bucket behavior
   - Current H1/M30 bucket luôn rebuild từ M1 session hiện tại để phản ánh candle mới.
   - Verify: test current bucket update khi thêm candle mới.

4. Run verification
   - `pytest services/aureus-signal/tests/test_tpo_signal.py services/aureus-notifier/tests/test_formatters.py services/aureus-signal/tests/test_indicator_snapshot.py -q`

## Files
- `services/aureus-signal/engine/signals/tpo.py`
- `services/aureus-signal/tests/test_tpo_signal.py`
- `.planning/quick/260425-bs6-th-c-hi-n-c-i-thi-n-tpo-signal-theo-summ/260425-bs6-SUMMARY.md`
- `.planning/STATE.md`

## Success Criteria
- TPO output giữ đủ `POC/VAH/VAL/shape/shape_confidence_pct/shape_scores_pct`.
- `_build_levels_and_counts()` không bị gọi 2 lần cho cùng một TPO block.
- Closed H1/M30 bucket cache hit không gọi rebuild block/session.
- Current H1/M30 bucket vẫn update khi candle mới đến.
- Target tests pass.
