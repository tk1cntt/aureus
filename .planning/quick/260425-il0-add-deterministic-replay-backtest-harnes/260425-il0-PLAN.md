---
phase: quick-260425-il0-add-deterministic-replay-backtest-harnes
plan: 260425-il0
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/tpo_replay.py
  - services/aureus-signal/tests/test_tpo_replay.py
autonomous: true
requirements:
  - QUICK-260425-IL0
must_haves:
  truths:
    - "TPO calibration replay can run deterministically on an in-memory candle/context fixture without touching live signal wiring."
    - "Replay report includes setup counts for va_rejection, va_breakout_acceptance, and trend_pullback, split by side and validity."
    - "Replay report includes regime breakdown for at least trend, range, high_volatility, and low_volatility labels present in input data."
    - "Replay report includes threshold sensitivity for configured score thresholds and shows emitted tag counts after conflict suppression."
    - "Production behavior remains unchanged: no DB writes, no migrations/schema edits, no seed strategy changes, no trade execution runtime wiring."
  artifacts:
    - path: "services/aureus-signal/engine/signals/tpo_replay.py"
      provides: "Pure in-memory deterministic TPO replay/backtest harness and report aggregation helpers"
      exports: ["replay_tpo_calibration"]
    - path: "services/aureus-signal/tests/test_tpo_replay.py"
      provides: "Unit coverage for deterministic replay reports, regime breakdown, threshold sensitivity, and production-scope guardrails"
  key_links:
    - from: "services/aureus-signal/engine/signals/tpo_replay.py"
      to: "services/aureus-signal/engine/signals/tpo_detectors.py"
      via: "detector classes called against fixture contexts only"
      pattern: "VARejectionDetector|VABreakoutAcceptanceDetector|TrendPullbackDetector"
    - from: "services/aureus-signal/engine/signals/tpo_replay.py"
      to: "services/aureus-signal/engine/signals/tpo_strategy.py"
      via: "tpo_strategy_tags_from_candidates for threshold sensitivity tag emission"
      pattern: "tpo_strategy_tags_from_candidates"
    - from: "services/aureus-signal/tests/test_tpo_replay.py"
      to: "services/aureus-signal/engine/signals/tpo_replay.py"
      via: "pytest imports replay_tpo_calibration and asserts exact deterministic report fields"
      pattern: "replay_tpo_calibration"
---

<objective>
Tạo deterministic replay/backtest harness thuần in-memory cho calibration TPO setup, dùng các detector và strategy tag bridge đã có để sinh report đo setup counts, regime breakdown và threshold sensitivity.

Purpose: Hoàn tất slice replay/calibration trước production, giúp đánh giá edge/threshold mà không thay đổi runtime live signal, DB, schema, seed strategies hoặc trade execution.
Output: `tpo_replay.py` và test tương ứng chứng minh replay deterministic, report đủ số liệu yêu cầu, và scope không đụng production behavior.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/quick/260425-ekl-l-n-k-hoa-ch-tri-n-khai-signal-tpo-theo-/260425-ekl-TPO-IMPLEMENTATION-PLAN.md
@D:/Aureus/.planning/quick/260425-evw-nghi-n-c-u-tpo-implementation-plan-v-i-t/260425-evw-NEXT-STEPS-REPORT.md
@D:/Aureus/.planning/quick/260425-gib-implement-first-tpo-detector-varejection/260425-gib-SUMMARY.md
@D:/Aureus/.planning/quick/260425-i1g-implement-remaining-deterministic-tpo-de/260425-i1g-SUMMARY.md
@D:/Aureus/.planning/quick/260425-ic5-implement-tpo-strategy-tag-bridge-and-se/260425-ic5-SUMMARY.md
@D:/Aureus/services/aureus-signal/engine/signals/tpo_detectors.py
@D:/Aureus/services/aureus-signal/engine/signals/tpo_strategy.py
@D:/Aureus/services/aureus-signal/tests/test_tpo_detectors.py
@D:/Aureus/services/aureus-signal/tests/test_tpo_strategy_signal.py

<interfaces>
Existing detector candidate contract from `tpo_detectors.py`:
```python
candidate = {
    "setup": "va_rejection" | "va_breakout_acceptance" | "trend_pullback",
    "side": "long" | "short" | None,
    "valid": bool,
    "score": float,
    "entry_zone": list[float] | None,
    "invalidation": float | None,
    "reasons": list[str],
}
```

Existing detector call signatures:
```python
VARejectionDetector().detect(context, previous_close, current_close)
VABreakoutAcceptanceDetector().detect(context, current_close, acceptance_closes)
TrendPullbackDetector().detect(context, previous_close, current_close)
```

Existing strategy bridge signature from `tpo_strategy.py`:
```python
def tpo_strategy_tags_from_candidates(candidates, min_score=0.75):
    return {"tags": tags, "suppressed": bool, "reasons": list[str], "debug": list[dict]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing replay calibration contract tests</name>
  <files>services/aureus-signal/tests/test_tpo_replay.py</files>
  <behavior>
    - Same in-memory replay fixture run twice returns identical report dictionaries.
    - Report contains `setup_counts` with counts by setup, side, valid, and invalid for all three TPO setup families.
    - Report contains `regime_breakdown` keyed by input regime labels and nested setup counts.
    - Report contains `threshold_sensitivity` for thresholds such as 0.75 and 0.85, including emitted tag counts and conflict suppression counts from `tpo_strategy_tags_from_candidates`.
    - Tests assert no production files are imported or required: no DB/session, no migrations/schema, no seed strategy mutation, no trade execution runtime path.
  </behavior>
  <action>Create `test_tpo_replay.py` first. Build small deterministic fixture rows as dictionaries with `context`, `previous_close`, `current_close`, optional `acceptance_closes`, and `regime`. Include at least one valid candidate for each setup family and at least one invalid/conflicting/low-score scenario so threshold sensitivity and suppression are observable. Import only the new planned `replay_tpo_calibration` symbol from `engine.signals.tpo_replay`; do not import database, seed strategy, journal, MT5, dispatch, or execution runtime modules. Keep test helpers local and explicit.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests/test_tpo_replay.py -q</automated>
  </verify>
  <done>Tests fail only because `engine.signals.tpo_replay` / `replay_tpo_calibration` is not implemented yet; test file clearly encodes deterministic report contract and production-scope guardrails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure in-memory TPO replay report harness</name>
  <files>services/aureus-signal/engine/signals/tpo_replay.py, services/aureus-signal/tests/test_tpo_replay.py</files>
  <behavior>
    - `replay_tpo_calibration(rows, thresholds=(0.75,))` returns a deterministic plain dict with no timestamps generated at runtime and no external I/O.
    - For each row, detector candidates are produced using existing detector signatures and aggregated without mutating input rows.
    - `setup_counts` includes every setup family and separates long/short/invalid counts.
    - `regime_breakdown` groups counts by `row["regime"]`, using `unknown` only when regime is missing.
    - `threshold_sensitivity` calls `tpo_strategy_tags_from_candidates(candidates, min_score=threshold)` per threshold and records emitted tag counts plus suppressed conflict count.
  </behavior>
  <action>Before editing any existing symbol, attempt GitNexus impact analysis and report blast radius. Preferred implementation should avoid editing existing detector/bridge symbols entirely; if existing symbols must be changed, run impact for each modified function/class/method and document direct callers, affected processes, and risk. Create a new `tpo_replay.py` module only. Use existing `VARejectionDetector`, `VABreakoutAcceptanceDetector`, `TrendPullbackDetector`, and `tpo_strategy_tags_from_candidates`; do not add live signal wiring, CLI side effects, DB writes, schema/migrations, seed strategy changes, or trade execution imports. Keep rows as plain dictionaries and return a serializable report dict. If a row needs breakout acceptance closes, read `acceptance_closes` from the row and default to an empty list. Keep threshold ordering stable by preserving the input threshold order and using string keys only if needed for JSON-safe report output.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests/test_tpo_replay.py services/aureus-signal/tests/test_tpo_detectors.py services/aureus-signal/tests/test_tpo_strategy_signal.py -q</automated>
  </verify>
  <done>Replay tests pass together with existing detector and strategy bridge tests; implementation is new-module, deterministic, in-memory, and does not alter production runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Verify no production behavior or persistence scope changed</name>
  <files>services/aureus-signal/engine/signals/tpo_replay.py, services/aureus-signal/tests/test_tpo_replay.py</files>
  <action>Run focused verification and scope guardrails. Attempt GitNexus `detect_changes()` before commit if MCP/CLI is available and document result; if unavailable or CLI command is unsupported, use the fallback scoped diff command below. Confirm changed files are limited to `tpo_replay.py` and `test_tpo_replay.py`. Do not run DB migrations or e2e DB tests because this plan must not touch persistence; if any DB/schema/persistence file appears in diff, stop and revise before finishing.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests/test_tpo_replay.py services/aureus-signal/tests/test_tpo_detectors.py services/aureus-signal/tests/test_tpo_strategy_signal.py -q</automated>
    <automated>cd D:/Aureus && git diff --name-only -- services/aureus-signal/engine/signals services/aureus-signal/tests services/aureus-signal/engine/strategies db prisma migrations</automated>
  </verify>
  <done>Focused tests pass; GitNexus detect_changes attempt is documented or fallback diff shows only the new replay harness/test files; no DB/schema/migration/seed strategy/trade execution runtime files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory fixture → replay harness | Untrusted fixture dictionaries enter detector/report aggregation code. |
| replay harness → strategy tag bridge | Candidate dictionaries are filtered into tags for calibration report only. |
| replay harness → production runtime | Boundary must remain closed: no live signal wiring, DB writes, seed changes, or trade execution imports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260425-il0-01 | Tampering | `replay_tpo_calibration` input rows | mitigate | Do not mutate input rows; copy/read values into local candidate lists and assert determinism in tests. |
| T-260425-il0-02 | Repudiation | Calibration report counts | mitigate | Include deterministic setup/regime/threshold breakdown derived from fixture rows; tests assert exact repeated output. |
| T-260425-il0-03 | Information Disclosure | Replay helper imports | accept | Fixture-only in-memory helper handles no secrets/PII and must not import DB/session/runtime modules. |
| T-260425-il0-04 | Denial of Service | Large fixture rows | accept | Harness is non-production test/calibration utility; no network endpoint or live runtime path is introduced. |
| T-260425-il0-05 | Elevation of Privilege | Production behavior crossing | mitigate | Scope guardrail requires no DB/schema/migration/seed strategy/trade execution file changes and no live wiring. |
</threat_model>

<verification>
Overall verification commands:

```bash
cd D:/Aureus && python -m pytest services/aureus-signal/tests/test_tpo_replay.py services/aureus-signal/tests/test_tpo_detectors.py services/aureus-signal/tests/test_tpo_strategy_signal.py -q
cd D:/Aureus && git diff --name-only -- services/aureus-signal/engine/signals services/aureus-signal/tests services/aureus-signal/engine/strategies db prisma migrations
```

GitNexus requirement: before committing, attempt `gitnexus_detect_changes()` if MCP is available and document result in summary. If unavailable, document the CLI/fallback result and include the scoped `git diff --name-only` output. Do not put pseudo GitNexus tool calls inside shell verification.
</verification>

<success_criteria>
- A single pure in-memory replay module exists at `services/aureus-signal/engine/signals/tpo_replay.py`.
- `replay_tpo_calibration` reports setup counts, regime breakdown, and threshold sensitivity deterministically from the same input rows.
- Existing TPO detector and strategy bridge tests still pass.
- No production behavior changes: no live signal wiring, no DB writes, no schema/migrations, no seed strategy changes, no trade execution runtime edits.
- If any existing function/class/method is edited, GitNexus impact analysis is run first and blast radius is documented; preferred outcome is additive new symbols/files only.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260425-il0-add-deterministic-replay-backtest-harnes/260425-il0-SUMMARY.md`.
</output>
