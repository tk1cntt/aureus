---
plan_id: 260423-t4w
type: quick
objective: "Tối ưu payload OPEN_ORDER gửi sang MT5, loại bỏ field nặng/không cần thiết (đặc biệt signal_snapshot) khỏi luồng execution"
scope: "services/aureus-trader/order_builder.py, services/aureus-trader/dispatcher.py, tests liên quan order payload"
autonomous: true
---

<objective>
Giảm kích thước payload từ `aureus-trader` sang MT5 bằng cách chỉ giữ các field cần thiết cho execution OPEN_ORDER, tránh gửi `signal_snapshot` và metadata nặng không phục vụ MT5 đặt lệnh.

Purpose: Giảm overhead Redis pub/sub + MT5 bridge parsing, giảm rủi ro timeout/lag khi burst lệnh.
Output: Payload OPEN_ORDER tối giản cho MT5, vẫn giữ đủ dữ liệu cho journal nội bộ nếu cần.
</objective>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/services/aureus-trader/main.py
@D:/Aureus/services/aureus-trader/order_builder.py
@D:/Aureus/services/aureus-trader/dispatcher.py
@D:/Aureus/services/aureus-trader/tests/test_order_builder.py
@D:/Aureus/services/aureus-trader/tests/test_dispatcher.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Khóa contract payload OPEN_ORDER tối giản bằng test</name>
  <files>D:/Aureus/services/aureus-trader/tests/test_order_builder.py, D:/Aureus/services/aureus-trader/tests/test_dispatcher.py</files>
  <behavior>
    - Test 1: Payload publish sang `aureus:mt5:commands` chỉ chứa field execution cần thiết (`type, symbol, cmd_id, direction, order_type, volume, price, sl, tp, magic, comment`, và field conditional như `tp_rr_ratio`, `size_mode`, `risk_amount` khi có).
    - Test 2: `signal_snapshot`, `score_breakdown`, `weights_snapshot` và metadata không cần cho MT5 không xuất hiện trong message publish sang MT5.
    - Test 3: Nếu cần journal correlation, dữ liệu bổ sung được giữ nội bộ trader flow (không ép vào payload MT5).
  </behavior>
  <action>Trước khi sửa bất kỳ symbol nào, chạy GitNexus impact bắt buộc cho các symbol dự kiến sửa: `build_order_command` và `OrderDispatcher.dispatch_order`, báo blast radius (d=1/d=2) vào output thực thi. Sau đó cập nhật test để thể hiện rõ contract payload tối giản, tránh thay đổi ngoài phạm vi OPEN_ORDER path.</action>
  <verify>
    <automated>pytest D:/Aureus/services/aureus-trader/tests/test_order_builder.py D:/Aureus/services/aureus-trader/tests/test_dispatcher.py -x</automated>
  </verify>
  <done>Test fail trước khi sửa logic và định nghĩa rõ payload contract mới cho OPEN_ORDER.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thu gọn payload ở điểm build lệnh và publish sang MT5</name>
  <files>D:/Aureus/services/aureus-trader/order_builder.py, D:/Aureus/services/aureus-trader/dispatcher.py</files>
  <behavior>
    - Test 1: `build_order_command` không đưa `signal_snapshot` và metadata nặng vào object gửi MT5.
    - Test 2: `dispatch_order` publish đúng payload đã tối giản, vẫn giữ đường xử lý ACK/ORDER_OPENED/ORDER_FAILED hiện tại.
  </behavior>
  <action>Chỉnh tối thiểu tại đường OPEN_ORDER→MT5: loại các field nặng/không cần execution khỏi command payload. Nếu dispatcher/journal cần dữ liệu bổ sung để ghi nhận nội bộ, giữ chúng ở context nội bộ hoặc map lại từ event, không chuyển nguyên khối snapshot sang MT5 command channel. Không refactor các module khác.</action>
  <verify>
    <automated>pytest D:/Aureus/services/aureus-trader/tests/test_order_builder.py D:/Aureus/services/aureus-trader/tests/test_dispatcher.py -x</automated>
  </verify>
  <done>Payload OPEN_ORDER gửi MT5 không còn `signal_snapshot` và các field nặng; luồng dispatch/journal không vỡ test.</done>
</task>

<task type="auto">
  <name>Task 3: Regression focused cho trader execution pipeline</name>
  <files>D:/Aureus/services/aureus-trader/tests/test_e2e_mt5_orders.py, D:/Aureus/services/aureus-trader/tests/test_e2e_trader.py</files>
  <action>Chạy regression tối thiểu trên luồng execution MT5 để xác nhận thay đổi payload không làm hỏng flow đặt lệnh/retry/ack. Chỉ chỉnh expectation test nếu đang assert các field đã bị loại bỏ theo contract mới.</action>
  <verify>
    <automated>pytest D:/Aureus/services/aureus-trader/tests/test_e2e_mt5_orders.py D:/Aureus/services/aureus-trader/tests/test_e2e_trader.py -x</automated>
  </verify>
  <done>Luồng OPEN_ORDER end-to-end vẫn pass với payload nhỏ hơn và không chứa snapshot nặng.</done>
</task>

</tasks>

<verification>
- Contract test xác nhận payload OPEN_ORDER chỉ còn field execution cần thiết.
- Regression trader/MT5 pass sau khi loại bỏ `signal_snapshot` khỏi payload gửi MT5.
- Trong bước thực thi, có log/ghi nhận kết quả GitNexus impact trước edit các symbol chính.
</verification>

<success_criteria>
1. Message trên channel `aureus:mt5:commands` không còn `signal_snapshot` và metadata nặng không phục vụ execution.
2. MT5 execution path (ACK/ORDER_OPENED/ORDER_FAILED + retry) hoạt động bình thường.
3. Thay đổi giới hạn trong phạm vi tối thiểu: order_builder/dispatcher và test liên quan trực tiếp.
</success_criteria>
