---
title: "StructureSignal M5 - On-the-Fly Resampling (Phương án A)"
status: ready-to-implement
created: 2026-06-17
parent_milestone: v1.7
must_haves:
  truths:
    - "StructureSignal(M1) depends on M1 swing_points persisted in state_obj.swing_points via PivotSignal"
    - "CISDMultiTFSignal resamples M1->HTF on-the-fly mỗi calculate() call, không persist state"
    - "PivotSignal sử dụng ZigZagPro (stateful engine, stored in state_obj.zigzag_engine)"
    - "get_confirmed_pivots(buffers_up, buffers_dn, types, times) -> list raw pivot dicts"
    - "label_pivots_pro(raw_pivots) -> labeled list with HH/LL/LH/HL types"
    - "StructureSignal logic: detect_breakout(scan h>price or l<price), find_opposing_extreme(opposing LL/HH), find_ob_zone(extreme candle in window), check_mitigation(touch after breakout)"
    - "resample_to_tf(df, 'M5') returns DataFrame with columns [t, o, h, l, c] or None"
    - "signal_factory.py dùng Python 3.7+ dict insertion order - structure_m5 MUST sau structure_processor và trước sweep_processor"
    - "ZigZagPro.update() signature: update(df, timeframe, sub_candles_by_tf, incremental) -> buffers dict"
    - "structure.py có 2 code paths: _calculate_old_path (pandas iloc) và _calculate_optimized_path (numpy arrays). Chỉ extract từ numpy path."
    - "cisd_mtf.py pattern: completed = ht_df.iloc[:-1] để exclude forming candle trước khi scan"
  artifacts:
    - "services/aureus-signal/engine/signals/structure_common.py (shared helpers)"
    - "services/aureus-signal/engine/signals/structure_m5.py (StructureSignalM5 class)"
    - "services/aureus-signal/engine/signal_factory.py (registration updated)"
  key_links:
    - "services/aureus-signal/engine/signals/cisd_mtf.py (resampling pattern reference)"
    - "services/aureus-signal/engine/signals/resampler.py (resample_to_tf utility)"
    - "services/aureus-signal/engine/signals/structure.py (logic source to extract from)"
    - "services/aureus-signal/engine/signals/pivots.py (ZigZag integration pattern)"
    - "services/aureus-signal/engine/common/zigzag_pro2.py (ZigZagPro, get_confirmed_pivots, label_pivots_pro)"
tasks:
  - id: 1
    title: "Extract shared structure detection logic to structure_common.py"
    files:
      - "services/aureus-signal/engine/signals/structure_common.py (create)"
      - "services/aureus-signal/engine/signals/structure.py (update numpy path only)"
    action:
      - "Extract from _calculate_optimized_path and _process_choch_numpy ONLY (numpy path). Leave _calculate_old_path and _process_choch (pandas path) unchanged."
      - "Extract detect_breakout(h_values, l_values, pivot_price, start_idx, is_bullish) -> breakout_idx or None"
      - "Extract find_opposing_extreme(points, pivot_idx, breakout_t, is_bullish) -> zone_base_idx or -1"
      - "Extract find_ob_zone(points, pivot_idx, is_bullish, t_values, o_values, h_values, l_values, c_values, t_map) -> OB dict or None"
      - "Extract verify_mitigation(obs, t_values, o_values, h_values, l_values, c_values, state_obj) -> updates ob.mitigated in-place"
      - "All functions use numpy arrays as input (np.ndarray for h_values, l_values, etc.)"
      - "Functions must be pure (no self references), accept arrays/dicts as params"
      - "Update structure.py _calculate_optimized_path to call shared helpers instead of inline logic"
      - "DO NOT touch _calculate_old_path or _process_choch (pandas path) — it will be deprecated via shadow mode"
    verify:
      - "File structure_common.py exists with 4 extracted functions"
      - "All functions have type hints and no class dependencies"
      - "Import test: from .structure_common import detect_breakout, find_opposing_extreme, find_ob_zone, verify_mitigation"
      - "structure.py _calculate_optimized_path still produces same output (shadow mode validates against old path)"
    done: false

  - id: 2
    title: "Implement StructureSignalM5 with on-the-fly resampling"
    files:
      - "services/aureus-signal/engine/signals/structure_m5.py (create)"
    action:
      - "Create StructureSignalM5(BaseSignal) with signal_type = SignalType.EVENT"
      - "TAG constants: TAG_CHOCH_M5_UP='choch_m5_up', TAG_CHOCH_M5_DN='choch_m5_down', TAG_BOS_M5_UP='bos_m5_up', TAG_BOS_M5_DN='bos_m5_down'"
      - "__init__: create private self._zz_m5 = ZigZagPro(ext_period=5, min_amplitude=100, ...) - separate instance"
      - "  TODO: ZigZagPro params (ext_period=5, min_amplitude=100) copied from M1. M5 candles have larger range — may need tuning after initial testing."
      - "create inner list self._swing_points_m5 = [] for transient M5 pivots"
      - "create inner list self._obs_m5 = [] for M5 order blocks"
      - "In calculate(df, state_obj):"
      - "  1. ht_df = resample_to_tf(df, 'M5'); return None if None or < 5 candles"
      - "  2. Exclude forming candle: df_m5_completed = ht_df.iloc[:-1] (pattern from cisd_mtf.py line 86)"
      - "  3. Convert to numpy arrays: t_values, h_values, l_values, o_values, c_values from df_m5_completed"
      - "  4. Build t_map = {int(t): i for i, t in enumerate(t_values)}"
      - "  5. Run self._zz_m5.update(df_m5_completed, timeframe='M5', incremental=True) -> buffers"
      - "  6. raw_pivots = get_confirmed_pivots(buffers); labeled = label_pivots_pro(raw_pivots)"
      - "  7. Store self._swing_points_m5 = labeled (transient, not in state_obj)"
      - "  8. Loop pivots with type in ['HH','LL'], call detect_breakout/find_opposing_extreme/find_ob_zone from structure_common"
      - "  9. Mark is_choch/is_bos on pivot dicts, emit tags to state_obj.transient_signals[tag_m5]"
      - "  10. Store transient_signals['ob_state_m5'] with active_obs list from self._obs_m5"
      - "  11. Call verify_mitigation from structure_common on self._obs_m5 list with M5 numpy arrays"
      - "DO NOT modify state_obj.swing_points, state_obj.zigzag_engine, or state_obj.obs"
    verify:
      - "Class instantiates without error"
      - "calculate() returns None if M5 data < 5 candles"
      - "Forming candle excluded: df_m5_completed has len(ht_df) - 1 rows"
      - "Tags match pattern choch_m5_up/down, bos_m5_up/down in transient_signals"
      - "transient_signals['ob_state_m5'] contains active_obs list"
      - "state_obj.swing_points and state_obj.obs remain unchanged (M1 only)"
    done: false

  - id: 3
    title: "Register StructureSignalM5 in signal_factory.py"
    files:
      - "services/aureus-signal/engine/signal_factory.py"
    action:
      - "Add import: from engine.signals.structure_m5 import StructureSignalM5"
      - "Insert 'structure_m5': StructureSignalM5() AFTER 'structure_processor' (line 149) and BEFORE 'sweep_processor' (line 150)"
      - "Rationale: structure_m5 is analogous to structure_processor (both detect CHoCH/BOS), must complete before sweep checks OB touches"
    verify:
      - "Import statement present in imports section"
      - "signal_set dict contains 'structure_m5' key between 'structure_processor' and 'sweep_processor'"
      - "Factory instantiation test: create_signal_set('XAUUSD') returns dict with structure_m5 key"
    done: false

post_implementation:
  - "Run npx gitnexus analyze to update code intelligence index"
  - "Test e2e with live M1 data stream, verify M5 tags emitted correctly in transient_signals"
  - "Verify transient_signals contains 'ob_state_m5' key with proper structure"
  - "Confirm M1 structure (choch_up/down, bos_up/down) still works unchanged"
  - "Monitor ZigZagPro M5 params — min_amplitude=100 may be too small for M5 candles, tune if needed"
