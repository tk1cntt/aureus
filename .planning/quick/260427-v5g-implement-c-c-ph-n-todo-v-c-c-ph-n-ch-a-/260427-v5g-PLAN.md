---
phase: 260427-v5g-implement-c-c-ph-n-todo-v-c-c-ph-n-ch-a-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/orders.py
  - services/aureus-signal/engine/snapshot_utils.py
  - services/aureus-signal/tests/test_entry_price_methods.py
  - services/aureus-signal/tests/test_pivot_sl.py
  - services/aureus-signal/tests/test_decision_trace_schema.py
  - services/aureus-signal/unittest/test_orders_events.py
autonomous: true
requirements:
  - QUICK-260427-V5G
must_haves:
  truths:
    - "ORDER plan không còn fallback im lặng sang CURRENT hoặc FIXED_PIPS khi entry/SL method không tính được."
    - "PIVOT_POINT SL dùng pivot_index sau khi lọc pivot hợp lệ; nếu thiếu pivot tương ứng thì warning và reject order."
    - "PULLBACK_50 và ENTRY_PIVOT_LIMIT tạo entry từ swing point LL/HH chưa broken đúng side, hoặc reject nếu thiếu/không hợp lệ."
    - "ENTRY_PIVOT_LIMIT được snapshot validation chấp nhận để seed strategies dùng đúng entry method."
    - "Trước khi sửa symbol phải có GitNexus impact; trước commit phải có GitNexus detect_changes."
  artifacts:
    - path: "services/aureus-signal/engine/orders.py"
      provides: "Order entry/SL calculation and ORDER_REJECTED path for incomplete plans"
      contains: "_calculate_entry_price"
    - path: "services/aureus-signal/engine/snapshot_utils.py"
      provides: "VALID_ENTRY_METHODS includes ENTRY_PIVOT_LIMIT"
      contains: "ENTRY_PIVOT_LIMIT"
    - path: "services/aureus-signal/tests/test_entry_price_methods.py"
      provides: "New regression coverage for entry methods and no-fallback behavior"
      contains: "ENTRY_PIVOT_LIMIT"
    - path: "services/aureus-signal/tests/test_pivot_sl.py"
      provides: "New regression coverage for PIVOT_POINT pivot_index/no-fallback behavior"
      contains: "pivot_index"
    - path: "services/aureus-signal/tests/test_decision_trace_schema.py"
      provides: "New regression coverage that rejection payload remains traceable/schema-compatible"
      contains: "ORDER_REJECTED"
  key_links:
    - from: "services/aureus-signal/engine/orders.py::_calculate_entry_price"
      to: "services/aureus-signal/engine/orders.py::process_triggers"
      via: "computed_entry None must reject before float(computed_entry)"
      pattern: "computed_entry is None"
    - from: "services/aureus-signal/engine/snapshot_utils.py::VALID_ENTRY_METHODS"
      to: "services/aureus-signal/engine/orders.py::_build_order_plan_snapshot"
      via: "entry_method validation must preserve ENTRY_PIVOT_LIMIT"
      pattern: "ENTRY_PIVOT_LIMIT"
    - from: "services/aureus-signal/engine/orders.py::_calculate_sl_tp"
      to: "services/aureus-signal/engine/orders.py::_find_pivot_for_sl_candidates"
      via: "PIVOT_POINT SL filters pivots then applies pivot_index"
      pattern: "pivot_index"
    - from: "services/aureus-signal/tests/test_entry_price_methods.py"
      to: "services/aureus-signal/engine/orders.py::_calculate_entry_price"
      via: "pytest imports SimulatedTradeManager and asserts None/rejection for invalid entry methods"
      pattern: "_calculate_entry_price"
    - from: "services/aureus-signal/tests/test_pivot_sl.py"
      to: "services/aureus-signal/engine/orders.py::_calculate_sl_tp"
      via: "pytest builds state_obj swing_points and asserts pivot_index/no-pivot behavior"
      pattern: "_calculate_sl_tp"
---

<objective>
Hoàn thiện các TODO/chưa hoàn thiện trong `services/aureus-signal/engine/orders.py` theo locked decisions: lỗi entry/SL/TP phải log warning và reject order, không retry, không fallback im lặng sang CURRENT/FIXED_PIPS.

Purpose: Tránh mở lệnh sai thời điểm hoặc sai risk plan khi order plan không đủ dữ liệu hợp lệ.
Output: `orders.py` có entry/SL behavior hoàn chỉnh, `ENTRY_PIVOT_LIMIT` được validation chấp nhận, tests mới khóa no-fallback semantics.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260427-v5g-implement-c-c-ph-n-todo-v-c-c-ph-n-ch-a-/260427-v5g-CONTEXT.md
@D:/Aureus/.planning/quick/260427-v5g-implement-c-c-ph-n-todo-v-c-c-ph-n-ch-a-/260427-v5g-RESEARCH.md
@D:/Aureus/services/aureus-signal/engine/orders.py
@D:/Aureus/services/aureus-signal/engine/snapshot_utils.py
@D:/Aureus/services/aureus-signal/unittest/test_orders_events.py

<locked_decisions>
- D-01: Implement toàn bộ TODO/chưa hoàn thiện trong `orders.py`, chỉ sửa file khác khi bắt buộc để test hoặc tương thích.
- D-02: Nếu có lỗi broker/order lifecycle: log warning và reject order.
- D-03: Không retry order submission vì order chỉ có ý nghĩa tức thì; retry làm mất tính đúng thời điểm vào lệnh.
- D-04: Nếu thay đổi chạm tới database hoặc luồng tạo/sửa data, phải chạy DB E2E để xác nhận chỉnh sửa và tạo data thành công.
- D-05: Giữ thay đổi tối thiểu, theo pattern hiện có, không mở rộng scope ngoài nhu cầu của `orders.py`.
</locked_decisions>

<interfaces>
Key contracts executor must preserve:

From `orders.py`:
```python
async def process_triggers(self, symbol, triggers, state_obj, ai_validator=None, execution_mode="simulated", recent_candles=None)
def _calculate_sl_tp(self, trigger, state_obj, config, entry_price_override=None, recent_candles=None)
def _calculate_entry_price(self, side, state_obj, entry_method="CURRENT", entry_value=None)
def _entry_pullback_50(self, side, state_obj, fallback)
def _entry_pivot_limit(self, side, state_obj, fallback)
def _find_pivot_for_sl_candidates(self, side, state_obj) -> List[float]
```

Runtime swing point shape used by pivot logic:
```python
{"price": float-like, "is_high": bool, "type": "LL" | "HH", "broken": bool, "t": int-like}
```

Reject flow to reuse in `process_triggers`:
```python
self._persist_rejection(state_obj, reason_payload)
await self.r.xadd(f"aureus:stream:{symbol}:orders", {"type": "ORDER_REJECTED", "data": json.dumps(reason_payload)})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo regression tests cho no-fallback entry/SL behavior</name>
  <files>services/aureus-signal/tests/test_entry_price_methods.py, services/aureus-signal/tests/test_pivot_sl.py, services/aureus-signal/tests/test_decision_trace_schema.py</files>
  <behavior>
    - Invalid/unknown entry_method không được resolve thành current close; process-level flow phải reject order plan thay vì tạo order.
    - OB_EDGE/EMA_TOUCH/FIXED_OFFSET/PULLBACK_50/ENTRY_PIVOT_LIMIT khi thiếu dữ liệu hợp lệ trả None hoặc dẫn tới ORDER_REJECTED, không fallback CURRENT.
    - PIVOT_POINT không có pivot hợp lệ trả `(None, None)` hoặc làm process-level rejection, không fallback FIXED_PIPS.
    - PIVOT_POINT `pivot_index=2` chọn pivot hợp lệ thứ hai sau filter, không luôn chọn pivot đầu tiên.
    - `ENTRY_PIVOT_LIMIT` nằm trong `VALID_ENTRY_METHODS` và không bị normalize về CURRENT.
    - `ORDER_REJECTED` payload cho plan incomplete vẫn có trace/schema fields cần thiết để audit.
  </behavior>
  <action>Các file `services/aureus-signal/tests/test_entry_price_methods.py`, `services/aureus-signal/tests/test_pivot_sl.py`, và `services/aureus-signal/tests/test_decision_trace_schema.py` hiện chưa tồn tại trong repo; tạo mới các file này với pytest tests cụ thể, không chỉ tham chiếu đường dẫn. Trước khi sửa production symbols, chạy GitNexus impact ở Task 2; với test files mới chưa có symbol trong index thì ghi rõ trong summary. Dùng helper/fake tối thiểu dựa trên pattern trong `services/aureus-signal/unittest/test_orders_events.py` nếu cần fake Redis/state; không tạo test framework mới. Tests phải encode D-01/D-02/D-03: warning + reject/no order, không retry, không fallback. Nếu dùng direct helper call, assert `None` cho failure path; nếu dùng `process_triggers`, assert không append `simulated_orders` và có stream `ORDER_REJECTED`/state rejection.</action>
  <verify>
    <automated>python -m pytest services/aureus-signal/tests/test_entry_price_methods.py services/aureus-signal/tests/test_pivot_sl.py services/aureus-signal/tests/test_decision_trace_schema.py -q</automated>
  </verify>
  <done>Ba test files mới tồn tại, tests mô tả đầy đủ D-01/D-02/D-03, fallback cũ không còn được kỳ vọng, và test run ban đầu fail đúng chỗ nếu production code chưa sửa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement entry và PIVOT_POINT SL no-fallback trong orders.py</name>
  <files>services/aureus-signal/engine/orders.py, services/aureus-signal/engine/snapshot_utils.py</files>
  <behavior>
    - `_calculate_entry_price` chỉ trả current close cho method `CURRENT`; unknown method hoặc helper failure log warning và trả `None`.
    - `process_triggers` reject ngay khi `computed_entry is None`, trước `_calculate_sl_tp` và trước `float(computed_entry)`, bằng existing `_persist_rejection` + `ORDER_REJECTED` stream; không retry per D-03.
    - `_entry_pullback_50`: BUY dùng LL gần nhất chưa broken, midpoint giữa LL price và trigger candle high; SELL dùng HH gần nhất chưa broken, midpoint giữa HH price và trigger candle low; nếu thiếu pivot/candle hoặc entry sai side với current close thì warning + `None`.
    - `_entry_pivot_limit`: BUY dùng LL gần nhất chưa broken dưới current; SELL dùng HH gần nhất chưa broken trên current; nếu thiếu/không hợp lệ thì warning + `None`.
    - `_calculate_sl_tp` PIVOT_POINT áp dụng `pivot_index` sau lọc 5-candle; nếu không đủ candidate thì warning và return `(None, None)`, không fallback FIXED_PIPS.
    - `VALID_ENTRY_METHODS` chấp nhận `ENTRY_PIVOT_LIMIT` để strategy configs không bị normalize về CURRENT.
  </behavior>
  <action>Chạy GitNexus gates trước khi sửa: `gitnexus_impact` upstream cho `SimulatedTradeManager.process_triggers`, `_calculate_entry_price`, `_calculate_sl_tp`, `_entry_pullback_50`, `_entry_pivot_limit`, và `VALID_ENTRY_METHODS`; báo blast radius/risk trong execution summary. Implement tối thiểu theo research, giữ pattern hiện có và không đụng DB writer/journal để tránh DB E2E theo D-04. Khi reject entry failure, dùng `reason_code="ORDER_PLAN_INCOMPLETE"` hoặc reason phụ rõ ràng trong payload như `entry_method`/`entry_error` nếu không phá tests hiện có. Không thêm retry/backoff. Không refactor unrelated code.</action>
  <verify>
    <automated>python -m pytest services/aureus-signal/tests/test_entry_price_methods.py services/aureus-signal/tests/test_pivot_sl.py services/aureus-signal/tests/test_decision_trace_schema.py -q</automated>
  </verify>
  <done>Mọi TODO trong `orders.py` được xử lý đầy đủ; no-fallback behavior hoạt động ở helper và process-level; `ENTRY_PIVOT_LIMIT` không còn bị normalize; không có path tạo order với `computed_entry=None`.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy verification mở rộng và GitNexus scope gate</name>
  <files>services/aureus-signal/engine/orders.py, services/aureus-signal/engine/snapshot_utils.py, services/aureus-signal/tests/test_entry_price_methods.py, services/aureus-signal/tests/test_pivot_sl.py, services/aureus-signal/tests/test_decision_trace_schema.py, services/aureus-signal/unittest/test_orders_events.py</files>
  <action>Chạy targeted unit tests mới và broader order-event tests đang tồn tại. Sau khi tests pass, chạy `gitnexus_detect_changes()` trước khi commit theo CLAUDE.md để xác nhận chỉ ảnh hưởng expected symbols/flows. Nếu có thay đổi DB/schema/db-writer/journal persistence ngoài kế hoạch, dừng lại và chạy DB E2E theo D-04; nếu không chạm DB/data-write path thì ghi rõ DB E2E không áp dụng vì thay đổi chỉ ở order validation/rejection runtime + unit tests.</action>
  <verify>
    <automated>python -m pytest services/aureus-signal/tests/test_entry_price_methods.py services/aureus-signal/tests/test_pivot_sl.py services/aureus-signal/tests/test_decision_trace_schema.py services/aureus-signal/unittest/test_orders_events.py -q</automated>
    <automated>gitnexus_detect_changes(scope="all")</automated>
  </verify>
  <done>Targeted tests mới và broader existing order-event tests pass; GitNexus detect_changes xác nhận scope đúng; summary nêu rõ impact gates đã chạy, risk level, và DB E2E có/không áp dụng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy config -> order planner | Strategy/order_plan input có thể thiếu hoặc sai entry_method/sl/tp values |
| runtime market state -> order creation | `state_obj` swing_points/candles/emas/obs có thể thiếu hoặc chứa value không parse được |
| order planner -> Redis order stream | Chỉ order plan hợp lệ mới được publish ORDER_OPEN/ORDER_PENDING; invalid plan phải ORDER_REJECTED |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260427-v5g-01 | Tampering | `_build_order_plan_snapshot` / `VALID_ENTRY_METHODS` | mitigate | Validate `ENTRY_PIVOT_LIMIT` explicitly instead of silently defaulting to CURRENT. |
| T-260427-v5g-02 | Tampering | `_calculate_entry_price` | mitigate | Unknown/failed method returns `None` + warning and process-level rejection; no fallback to current market price. |
| T-260427-v5g-03 | Tampering | `_calculate_sl_tp` PIVOT_POINT | mitigate | Apply `pivot_index` only after pivot filter; missing pivot returns `(None, None)` and rejects order. |
| T-260427-v5g-04 | Denial of Service | `process_triggers` | mitigate | Guard `computed_entry is None` before `float()` casts to avoid runtime crash from invalid plan. |
| T-260427-v5g-05 | Repudiation | rejection flow | mitigate | Persist rejection and publish `ORDER_REJECTED` with trace_id/symbol/strategy/reason for auditability. |
</threat_model>

<verification>
Overall checks:
1. `python -m pytest services/aureus-signal/tests/test_entry_price_methods.py services/aureus-signal/tests/test_pivot_sl.py services/aureus-signal/tests/test_decision_trace_schema.py -q`
2. `python -m pytest services/aureus-signal/unittest/test_orders_events.py -q`
3. `gitnexus_detect_changes(scope="all")` before any commit.
4. If any command fails due to service/environment setup, consult `D:/Aureus/RUN_SERVICES.md` before retrying.
</verification>

<success_criteria>
- All TODOs/chưa hoàn thiện identified in research for `orders.py` are implemented without scope expansion.
- Invalid entry method, failed entry helper, and missing PIVOT_POINT pivot reject order with warning instead of fallback.
- `pivot_index` selection is deterministic after pivot filtering.
- `ENTRY_PIVOT_LIMIT` strategy configs reach order calculation unchanged.
- Test files referenced by this plan either exist before execution (`test_orders_events.py`) or are explicitly created by Task 1 (`test_entry_price_methods.py`, `test_pivot_sl.py`, `test_decision_trace_schema.py`).
- Tests pass and GitNexus gates are documented in summary.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260427-v5g-implement-c-c-ph-n-todo-v-c-c-ph-n-ch-a-/260427-v5g-SUMMARY.md`.
</output>
