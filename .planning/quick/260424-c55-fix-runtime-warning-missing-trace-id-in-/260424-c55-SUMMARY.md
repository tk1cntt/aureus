# Quick Task 260424-c55 Summary

## Objective
Fix runtime warning `on_order_opened: missing trace_id in event` khi trace đã có ở `on_strategy_match`.

## Changes

### 1) `services/aureus-trader/dispatcher.py`
- Trong nhánh `ORDER_OPENED`, bổ sung resolve trace_id theo thứ tự:
  1. `order.trace_id`
  2. `strategy_payload.trace_id`
  3. `strategy_payload.data.trace_id`
  4. `final.trace_id` (fallback)
- Trước khi gọi `on_strategy_match`, nếu có `trace_id` thì đồng bộ vào cả `strategy_payload.trace_id` và `strategy_payload.data.trace_id` (khi có data dict).
- Sau khi await `on_strategy_match`, nếu trace ban đầu rỗng thì lấy lại từ `strategy_payload`/`strategy_payload.data`, rồi gán vào `final["trace_id"]`.

### 2) `services/aureus-trader/tests/test_dispatcher.py`
- Thêm test `test_dispatch_order_resolves_trace_id_from_strategy_event_data`.
- Thêm test `test_dispatch_order_does_not_override_existing_order_opened_trace_id`.
- Đảm bảo focused scenario trace propagation được cover đầy đủ.

## Verification
- Focused:
  - `pytest services/aureus-trader/tests/test_dispatcher.py -k "trace_id and order_opened" -x` ✅
- Regression:
  - `pytest services/aureus-trader/tests/test_dispatcher.py -x` ✅ (21 passed)
- Runtime log check sau restart `aureus-trader-dev`:
  - Không còn warning `on_order_opened: missing trace_id in event` ở các lệnh mới sau fix.
  - Có lỗi runtime khác còn tồn tại: `NotNullViolationError: signal_schema_version` trong `aureus_trade_signal_snapshots` (ngoài scope quick task này).

## Commit
- `0c90e36` — `fix(trader): propagate trace_id into ORDER_OPENED payload`

## Notes
- Impact analysis đã chạy trước khi sửa:
  - `dispatch_order`: CRITICAL (đã giữ scope sửa tối thiểu)
  - `on_order_opened`: LOW
- CLI không có `gitnexus detect_changes`; scope được kiểm soát bằng `git status` + `git diff` trước commit.