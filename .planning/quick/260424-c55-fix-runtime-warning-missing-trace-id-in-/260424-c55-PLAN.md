---
phase: quick-260424-c55
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/dispatcher.py
  - services/aureus-trader/journal.py
  - services/aureus-trader/tests/test_dispatcher.py
autonomous: true
requirements:
  - QUICK-260424-C55
must_haves:
  truths:
    - "Sau khi on_strategy_match chạy xong, on_order_opened không còn warning thiếu trace_id."
    - "trace_id được propagate xuyên suốt dispatcher -> journal theo đúng thứ tự runtime."
    - "Fix không làm hỏng hành vi hiện có của dispatcher ở các nhánh đã có test."
  artifacts:
    - path: "services/aureus-trader/tests/test_dispatcher.py"
      provides: "TDD RED->GREEN cho bug thiếu trace_id và regression liên quan"
    - path: "services/aureus-trader/dispatcher.py"
      provides: "Logic resolve/forward trace_id sau on_strategy_match trước khi gọi on_order_opened"
    - path: "services/aureus-trader/journal.py"
      provides: "Điểm nhận trace_id ổn định cho ghi log/journal ORDER_OPENED"
  key_links:
    - from: "services/aureus-trader/dispatcher.py::dispatch_order"
      to: "services/aureus-trader/journal.py::on_strategy_match"
      via: "await on_strategy_match để finalize trace context"
      pattern: "await self\.journal\.on_strategy_match\(strategy_payload\)"
    - from: "services/aureus-trader/dispatcher.py::dispatch_order"
      to: "services/aureus-trader/journal.py::on_order_opened"
      via: "forward payload đã có trace_id"
      pattern: "on_order_opened"
---

<objective>
Fix runtime warning thiếu `trace_id` trong `on_order_opened` khi `trace_id` chỉ xuất hiện sau `on_strategy_match`.

Purpose: Khôi phục chuỗi journal nhất quán strategy_match -> order_opened để log/audit không bị đứt.
Output: 1 fix tối thiểu ở luồng dispatcher/journal + test TDD + regression + verify runtime bằng restart service và kiểm tra log ngắn.
</objective>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/services/aureus-trader/dispatcher.py
@D:/Aureus/services/aureus-trader/journal.py
@D:/Aureus/services/aureus-trader/tests/test_dispatcher.py
@D:/Aureus/RUN_SERVICES.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — viết test tái hiện missing trace_id sau strategy match</name>
  <files>services/aureus-trader/tests/test_dispatcher.py</files>
  <behavior>
    - Test 1: Khi ORDER_OPENED vào dispatcher với trace_id ban đầu rỗng, nhưng on_strategy_match tạo/điền trace_id, payload chuyển sang on_order_opened phải có trace_id.
    - Test 2: Nếu trace_id có sẵn từ trước thì vẫn giữ nguyên, không bị ghi đè sai.
  </behavior>
  <action>Thêm test focused mô phỏng đúng thứ tự runtime hiện tại (strategy_match chạy trước order_opened trong nhánh ORDER_OPENED), xác nhận hiện trạng fail (RED) khi trace_id chưa được resolve đúng điểm.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -k "trace_id and order_opened" -x</automated>
  </verify>
  <done>Có test RED tái hiện cảnh báo runtime missing trace_id theo luồng dispatcher/journal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — sửa dispatcher/journal để propagate trace_id đúng thứ tự</name>
  <files>services/aureus-trader/dispatcher.py, services/aureus-trader/journal.py</files>
  <behavior>
    - dispatch_order chỉ gọi on_order_opened sau khi trace context đã được cập nhật từ on_strategy_match.
    - on_order_opened nhận payload có trace_id hợp lệ trong case trace được tạo muộn.
    - Không mở rộng scope sang logic ngoài trace_id flow.
  </behavior>
  <action>Chỉnh sửa tối thiểu ở các điểm nối dispatcher/journal: resolve trace_id sau await on_strategy_match và truyền lại payload/metadata đã cập nhật trước khi ghi ORDER_OPENED; giữ nguyên style code hiện có, không thêm abstraction mới.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -k "trace_id and order_opened" -x</automated>
  </verify>
  <done>Test RED ở Task 1 chuyển GREEN, warning missing trace_id không còn xảy ra trong luồng được tái hiện.</done>
</task>

<task type="auto">
  <name>Task 3: Regression + runtime verify bằng restart service và log check ngắn</name>
  <files>services/aureus-trader/tests/test_dispatcher.py</files>
  <action>Chạy regression test dispatcher để chặn side effects; sau đó restart service trader theo RUN_SERVICES.md và kiểm tra log ngắn sau restart để xác nhận không còn warning missing trace_id trong on_order_opened ở scenario có strategy_match trước đó.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -x && (docker compose -f docker-compose.dev.yml restart aureus-trader || docker-compose -f docker-compose.dev.yml restart aureus-trader) && (docker compose -f docker-compose.dev.yml logs --tail=120 aureus-trader || docker-compose -f docker-compose.dev.yml logs --tail=120 aureus-trader)</automated>
  </verify>
  <done>Regression pass và log sau restart không còn runtime warning missing trace_id tại on_order_opened trong luồng strategy_match -> order_opened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| order event -> dispatcher | Event đầu vào có thể thiếu trace_id ở thời điểm nhận |
| dispatcher -> journal | Sai thứ tự propagate trace_id gây warning runtime và đứt audit chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260424-c55-01 | T | services/aureus-trader/dispatcher.py | mitigate | Chỉ forward dữ liệu sang on_order_opened sau khi trace context được finalize từ on_strategy_match |
| T-quick-260424-c55-02 | R | services/aureus-trader/journal.py | mitigate | Đảm bảo trace_id hiện diện trong record ORDER_OPENED để không mất khả năng truy vết |
| T-quick-260424-c55-03 | D | services/aureus-trader/tests/test_dispatcher.py | accept | Không thay đổi kiến trúc xử lý event; kiểm soát bằng focused test + regression + runtime log check |
</threat_model>

<verification>
- RED/GREEN focused: `pytest services/aureus-trader/tests/test_dispatcher.py -k "trace_id and order_opened" -x`
- Regression: `pytest services/aureus-trader/tests/test_dispatcher.py -x`
- Runtime verify: restart `aureus-trader` theo `RUN_SERVICES.md`, rồi check log tail ngắn để xác nhận warning đã biến mất.
</verification>

<success_criteria>
- Tái hiện được lỗi bằng test RED, sau fix test chuyển GREEN.
- Regression dispatcher pass toàn bộ.
- Sau restart service, log ngắn không còn warning missing trace_id ở on_order_opened cho luồng strategy_match -> order_opened.
</success_criteria>

<output>
After completion, create `.planning/quick/260424-c55-fix-runtime-warning-missing-trace-id-in-/260424-c55-SUMMARY.md`
</output>
