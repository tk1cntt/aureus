---
phase: quick-260425-i1g-implement-remaining-deterministic-tpo-de
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/tpo_detectors.py
  - services/aureus-signal/tests/test_tpo_detectors.py
autonomous: true
requirements:
  - QUICK-260425-I1G
must_haves:
  truths:
    - "VABreakoutAcceptanceDetector trả candidate valid long khi giá đóng trên VAH, acceptance được xác nhận 1-2 nến và POC M30/H1 không giảm."
    - "VABreakoutAcceptanceDetector trả candidate valid short khi giá đóng dưới VAL, acceptance được xác nhận 1-2 nến và POC M30/H1 không tăng."
    - "TrendPullbackDetector trả candidate valid long khi D1 bullish/neutral-up hoặc giá trên D1 POC, pullback về H1 POC/VAL và M30 reclaim/rejection xác nhận."
    - "TrendPullbackDetector trả candidate valid short theo logic đối xứng quanh D1 POC, H1 POC/VAH và M30 reject/reclaim."
    - "Cả hai detector trả invalid/no side khi thiếu D1/H1/M30 context, history_guard stale/missing, conflict obvious hoặc chỉ có shape hỗ trợ mà thiếu price relation."
    - "Mọi candidate dict có reasons metadata giải thích điều kiện chính; không thêm strategy tags, scorer/bridge, seed strategies, DB/schema/migrations, trade execution hoặc production wiring."
  artifacts:
    - path: "services/aureus-signal/engine/signals/tpo_detectors.py"
      provides: "VABreakoutAcceptanceDetector và TrendPullbackDetector cùng candidate dict contract deterministic"
    - path: "services/aureus-signal/tests/test_tpo_detectors.py"
      provides: "Unit tests long/short/invalid/conflict/reasons cho hai detector còn lại"
  key_links:
    - from: "services/aureus-signal/tests/test_tpo_detectors.py"
      to: "services/aureus-signal/engine/signals/tpo_detectors.py"
      via: "import VABreakoutAcceptanceDetector, TrendPullbackDetector và assert candidate contract"
      pattern: "from engine\.signals\.tpo_detectors import .*VABreakoutAcceptanceDetector.*TrendPullbackDetector"
    - from: "TPOContextBuilder-shaped context"
      to: "services/aureus-signal/engine/signals/tpo_detectors.py"
      via: "detect(context, previous_closes, current_close) đọc context['timeframes'], context['bias'], history_guard và poc_shift/price levels"
      pattern: "context\.get\(\"timeframes\"\)"
---

<objective>
Implement only the two remaining deterministic TPO detectors from Slice 3: `VABreakoutAcceptanceDetector` and `TrendPullbackDetector`.

Purpose: Complete the detector-only layer started by `VARejectionDetector` while preserving the architecture boundary from the TPO implementation plan: indicators/context/history produce facts, detectors produce explainable setup candidates, and no strategy/scorer/production wiring is added in this quick task.

Output: Updated `tpo_detectors.py` plus focused tests in `test_tpo_detectors.py` covering long, short, invalid, conflict handling, and reasons metadata for both new detectors.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260425-ekl-l-n-k-hoa-ch-tri-n-khai-signal-tpo-theo-/260425-ekl-TPO-IMPLEMENTATION-PLAN.md
@D:/Aureus/.planning/quick/260425-gib-implement-first-tpo-detector-varejection/260425-gib-PLAN.md
@D:/Aureus/services/aureus-signal/engine/signals/tpo_detectors.py
@D:/Aureus/services/aureus-signal/tests/test_tpo_detectors.py

<interfaces>
Existing detector output contract used by `VARejectionDetector` and required for the two new detectors:
```python
{
    "setup": "va_breakout_acceptance" | "trend_pullback",
    "side": "long" | "short" | None,
    "valid": bool,
    "score": float,
    "entry_zone": [float, float] | None,
    "invalidation": float | None,
    "reasons": list[str],
}
```

Existing context fixture shape in `test_tpo_detectors.py`:
```python
{
    "timeframes": {
        "D1": {"poc": float, "vah": float, "val": float, "shape": str | None, "price_location": str, "distance_to_poc_ticks": float, "va_width": float, ...},
        "H1": {...},
        "M30": {...},
    },
    "bias": {"d1": "bullish" | "bearish" | "neutral" | "neutral-up" | "neutral-down"},
    "history_guard": {"is_stale": bool, "stale_timeframes": list[str], "missing_timeframes": list[str]},  # optional
}
```

Existing `VARejectionDetector` pattern in `tpo_detectors.py`:
- `detect(context, previous_close, current_close)` validates required D1/H1/M30 context and optional history_guard.
- Invalid candidate uses `valid=False`, `side=None`, `score=0.0`, `entry_zone=None`, `invalidation=None`, non-empty `reasons`.
- Shape is a score/reason modifier only, never the sole gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detector tests for breakout acceptance and trend pullback</name>
  <files>services/aureus-signal/tests/test_tpo_detectors.py</files>
  <behavior>
    - VABreakoutAcceptanceDetector long valid: current close is above H1 or M30 VAH, acceptance closes for 1-2 following candles do not close back below that VAH, POC shift for H1/M30 is not down, result is valid long with setup `va_breakout_acceptance` and reasons mentioning accepted above VAH.
    - VABreakoutAcceptanceDetector short valid: current close is below H1 or M30 VAL, acceptance closes do not close back above that VAL, POC shift for H1/M30 is not up, result is valid short with reasons mentioning accepted below VAL.
    - VABreakoutAcceptanceDetector invalid/conflict: missing D1/H1/M30 or stale/missing history_guard returns invalid; long with bearish D1 bias or short with bullish D1 bias returns invalid; supportive shape alone without breakout/acceptance remains invalid.
    - TrendPullbackDetector long valid: D1 bias bullish/neutral-up or current price above D1 POC, pullback touches/returns to H1 POC or VAL zone, M30 has reclaim/rejection confirmation, result is valid long with setup `trend_pullback` and reasons.
    - TrendPullbackDetector short valid: D1 bias bearish/neutral-down or current price below D1 POC, pullback touches/returns to H1 POC or VAH zone, M30 has reject/reclaim confirmation, result is valid short with reasons.
    - TrendPullbackDetector invalid/conflict: missing required context/history guard invalid; obvious D1 side conflict invalid; shape-only support without D1 trend + H1 pullback + M30 confirmation invalid.
  </behavior>
  <action>Extend the existing `test_tpo_detectors.py` without replacing VARejectionDetector tests. Update the import to include `VABreakoutAcceptanceDetector` and `TrendPullbackDetector`. Add small fixture helpers only if needed, keeping the same lightweight style. Tests must use candidate dicts/reasons metadata only; do not reference strategy tags, scorer/bridge, seed strategies, DB/schema/migrations, trade execution, or live wiring. If changing an existing test function rather than only adding new tests/helpers/imports, first run/attempt GitNexus impact analysis for that test symbol and document blast radius in the execution summary.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests/test_tpo_detectors.py -q</automated>
  </verify>
  <done>Tests cover long valid, short valid, invalid missing/stale context, conflict handling, shape-not-sole-gate, and non-empty reasons for both remaining detectors; tests fail before implementation only because detector classes/logic are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement VABreakoutAcceptanceDetector</name>
  <files>services/aureus-signal/engine/signals/tpo_detectors.py</files>
  <behavior>
    - `detect(context, current_close, acceptance_closes)` consumes TPOContextBuilder-shaped context and a list/tuple of 1-2 subsequent acceptance closes.
    - Long condition: current close breaks above H1 or M30 VAH; every acceptance close remains at/above the same VAH; selected timeframe `poc_shift` is not `down`; D1 bias is not bearish.
    - Short condition: current close breaks below H1 or M30 VAL; every acceptance close remains at/below the same VAL; selected timeframe `poc_shift` is not `up`; D1 bias is not bullish.
    - Missing D1/H1/M30 context, malformed levels, stale/missing history_guard when present, missing acceptance closes, POC shift conflict, or D1 side conflict returns invalid with reasons.
    - Shape M30/H1 can add a small score/reason for continuation but cannot validate without breakout + acceptance + POC shift checks.
  </behavior>
  <action>Add `VABreakoutAcceptanceDetector` to `tpo_detectors.py` near `VARejectionDetector`, matching the existing simple class style. You may add private module-level helper functions only if they reduce duplication without creating a framework. Before editing existing `VARejectionDetector` methods or any existing class/function, run/attempt `gitnexus_impact({target: "VARejectionDetector", direction: "upstream"})` or the exact modified symbol and report direct callers/affected flows/risk; preferred path is additive changes only. Candidate contract must be exactly the dict shape in this plan. Do not add strategy tags, scorer/bridge, seed strategy templates, DB/schema/migrations, trade execution, or production wiring.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests/test_tpo_detectors.py -q</automated>
  </verify>
  <done>`VABreakoutAcceptanceDetector` passes its long/short/invalid/conflict/reasons tests while all existing VARejectionDetector tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement TrendPullbackDetector and verify scope</name>
  <files>services/aureus-signal/engine/signals/tpo_detectors.py, services/aureus-signal/tests/test_tpo_detectors.py</files>
  <behavior>
    - `detect(context, previous_close, current_close)` consumes TPOContextBuilder-shaped context.
    - Long condition: D1 bias is bullish/neutral-up or current close is above D1 POC; price has pulled back to H1 POC or H1 VAL zone using previous/current relation or near-POC/near-VAL distance fields; M30 confirms with VAL reclaim or lower-value rejection; D1 bias is not bearish.
    - Short condition: D1 bias is bearish/neutral-down or current close is below D1 POC; price has pulled back to H1 POC or H1 VAH zone; M30 confirms with VAH reject or upper-value rejection; D1 bias is not bullish.
    - Missing D1/H1/M30 context, malformed levels, stale/missing history_guard when present, absent H1 pullback, absent M30 confirmation, or obvious side conflict returns invalid with reasons.
    - Shape is metadata/score modifier only; it never creates a valid pullback without D1 trend/context, H1 pullback, and M30 confirmation.
  </behavior>
  <action>Add `TrendPullbackDetector` with the same candidate dict contract and invalid candidate semantics. Keep logic deterministic and O(1) over D1/H1/M30 only. If you refactor shared validation out of `VARejectionDetector`, run/attempt GitNexus impact analysis for every modified existing symbol before editing and report blast radius; otherwise keep changes additive. After implementation, run GitNexus detect_changes per `CLAUDE.md` and document the result; if the GitNexus tool/CLI is unavailable, document the failed attempt and use the fallback scoped git diff command below. Do not touch DB/schema/migrations, seed strategies, strategy tags, scorer/bridge, trade execution, or production integration files.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests/test_tpo_detectors.py services/aureus-signal/tests/test_tpo_context.py services/aureus-signal/tests/test_tpo_history.py services/aureus-signal/tests/test_tpo_signal.py -q</automated>
    <automated>git -C D:/Aureus diff --name-only -- services/aureus-signal/engine/signals services/aureus-signal/engine/strategies services/aureus-signal/tests db prisma migrations</automated>
  </verify>
  <done>All focused TPO tests pass; GitNexus detect_changes was attempted/documented before commit or fallback scoped git diff was used; changed files are limited to `tpo_detectors.py` and `test_tpo_detectors.py`; no strategy/DB/trade/live wiring files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TPOContextBuilder output → deterministic detectors | Detectors consume in-memory derived market context; malformed/missing/stale context must not create valid candidates. |
| Detector candidate → future scorer/strategy layers | This quick task stops at candidate dicts; candidates must not become live strategy tags or trade execution payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260425-i1g-01 | Tampering | `VABreakoutAcceptanceDetector.detect` context and acceptance input | mitigate | Validate D1/H1/M30 levels, require 1-2 acceptance closes, reject stale/missing history_guard and POC shift conflicts with explicit reasons. |
| T-260425-i1g-02 | Tampering | `TrendPullbackDetector.detect` context input | mitigate | Validate D1/H1/M30 levels and require all three semantic legs: D1 trend/context, H1 pullback zone, M30 confirmation. |
| T-260425-i1g-03 | Elevation of Privilege | Future strategy/trade boundary | mitigate | Do not emit strategy tags, seed strategies, scorer output, bridge payloads, trade execution objects, or production wiring in this plan. |
| T-260425-i1g-04 | Information Disclosure | Detector reasons metadata | accept | Reasons contain only derived market facts and detector rule explanations; no secrets or PII. |
| T-260425-i1g-05 | Denial of Service | Detector runtime | mitigate | Keep deterministic O(1) checks over D1/H1/M30 and at most 1-2 acceptance closes; no candle/history scans. |
</threat_model>

<verification>
Run focused verification from repo root:
```bash
cd D:/Aureus && python -m pytest services/aureus-signal/tests/test_tpo_detectors.py services/aureus-signal/tests/test_tpo_context.py services/aureus-signal/tests/test_tpo_history.py services/aureus-signal/tests/test_tpo_signal.py -q
```

Before any commit, run or attempt GitNexus change detection per `CLAUDE.md` and document the result in the summary:
```text
gitnexus_detect_changes(scope="all")
```
If GitNexus is unavailable, document that failure and run fallback scope check:
```bash
git -C D:/Aureus diff --name-only -- services/aureus-signal/engine/signals services/aureus-signal/engine/strategies services/aureus-signal/tests db prisma migrations
```

If any existing function/class/method is edited instead of only adding new classes/helpers/tests/imports, first run/attempt GitNexus impact analysis for that exact symbol and include blast radius, direct callers, affected processes, and risk level in the execution summary.
</verification>

<success_criteria>
- `VABreakoutAcceptanceDetector` valid long and valid short cases pass.
- `VABreakoutAcceptanceDetector` invalid cases cover missing/stale context, D1 conflict, POC shift conflict, failed acceptance, and shape-only support.
- `TrendPullbackDetector` valid long and valid short cases pass.
- `TrendPullbackDetector` invalid cases cover missing/stale context, D1 conflict, missing H1 pullback, missing M30 confirmation, and shape-only support.
- Every valid and invalid candidate includes non-empty `reasons`.
- Shape is never a sole gate and never required when required price/context conditions are valid.
- No DB/schema/migration changes.
- No seed strategy changes.
- No strategy tag emission, scorer/bridge, or trade execution changes.
- No production/live signal wiring.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260425-i1g-implement-remaining-deterministic-tpo-de/260425-i1g-SUMMARY.md`.
</output>
