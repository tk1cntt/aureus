---
phase: 260430-s08-update-mql5-closeallpositions-mq5-to-als
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/CloseAllPositions.mq5
autonomous: true
requirements:
  - QUICK-260430-S08
must_haves:
  truths:
    - "Running CloseAllPositions closes matching open positions and also deletes matching pending orders."
    - "Pending order deletion respects the existing symbol, magic number, comment, and buy/sell direction filters where those filters are applicable."
    - "The change is surgical: no generated compile logs or .ex5 artifacts are committed."
  artifacts:
    - path: "mql5/CloseAllPositions.mq5"
      provides: "Script behavior for closing positions and cancelling pending orders"
      contains: "Trade.OrderDelete"
  key_links:
    - from: "mql5/CloseAllPositions.mq5::OnStart"
      to: "mql5/CloseAllPositions.mq5::DeletePendingOrder"
      via: "pending order iteration after/alongside position close flow"
      pattern: "OrdersTotal\\(\\).*OrderGetTicket|DeletePendingOrder"
---

<objective>
Update `mql5/CloseAllPositions.mq5` so the script cancels/deletes pending orders in addition to closing open positions.

Purpose: The current script only closes positions, leaving BUY_LIMIT/SELL_LIMIT/BUY_STOP/SELL_STOP/BUY_STOP_LIMIT/SELL_STOP_LIMIT orders active after emergency cleanup.
Output: One surgical MQL5 change plus verification notes in the execution summary.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@D:/Aureus/mql5/Build_Rules.md
@D:/Aureus/mql5/CloseAllPositions.mq5

<interfaces>
Existing script shape from `mql5/CloseAllPositions.mq5`:
```mql5
#include <Trade/Trade.mqh>
CTrade *Trade;

input bool OnlyCurrentSymbol = false;
input ENUM_ORDER_TYPES PositionTypeFilter = ALL_ORDERS;
input bool OnlyMagicNumber = false;
input int MagicNumber = 0;
input bool OnlyWithComment = false;
input string MatchingComment = "";
input int Delay = 0;
input int Retries = 10;
input bool AsyncMode = false;

void OnStart();
void CloseOrder(ulong ticket = 0);
double CaclulateCommission();
```
Use MQL5 pending order APIs such as `OrdersTotal()`, `OrderGetTicket(i)`, `OrderSelect(ticket)`, `OrderGetInteger(ORDER_TYPE)`, `OrderGetString(ORDER_SYMBOL)`, `OrderGetInteger(ORDER_MAGIC)`, `OrderGetString(ORDER_COMMENT)`, and `CTrade::OrderDelete(ticket)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run GitNexus impact attempt and inspect pending-order touchpoints</name>
  <files>mql5/CloseAllPositions.mq5</files>
  <action>Before editing any MQL5 symbol, attempt GitNexus impact analysis for the relevant entry/helper symbols: `OnStart`, `CloseOrder`, and any new helper such as `DeletePendingOrder` if it already exists. Use `gitnexus_impact({target: "OnStart", direction: "upstream"})` and `gitnexus_impact({target: "CloseOrder", direction: "upstream"})` if GitNexus MCP is available. If MQL5 symbols are not indexed or the MCP tool is unavailable, document the failed/unavailable impact attempt in the summary and proceed only after confirming the file is a standalone script. Do not modify any other files.</action>
  <verify>
    <automated>git status --short -- mql5/CloseAllPositions.mq5</automated>
  </verify>
  <done>Impact attempt is documented with blast radius or fallback reason; no code has been changed yet except none expected.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add pending order cancellation to CloseAllPositions</name>
  <files>mql5/CloseAllPositions.mq5</files>
  <action>Modify only `mql5/CloseAllPositions.mq5`. Extend the script so `OnStart()` also scans `OrdersTotal()` and deletes pending orders using `Trade.OrderDelete(ticket)` with retry/delay behavior consistent with `CloseOrder()`. Add a focused helper, for example `DeletePendingOrder(ulong ticket)`, only if it keeps the existing `OnStart()` simple. Apply existing filters to pending orders: `OnlyCurrentSymbol` checks `ORDER_SYMBOL`; `OnlyMagicNumber` checks `ORDER_MAGIC`; `OnlyWithComment` checks `ORDER_COMMENT`; `PositionTypeFilter == ONLY_BUY` should delete only pending BUY order types (`ORDER_TYPE_BUY_LIMIT`, `ORDER_TYPE_BUY_STOP`, `ORDER_TYPE_BUY_STOP_LIMIT`); `PositionTypeFilter == ONLY_SELL` should delete only pending SELL order types (`ORDER_TYPE_SELL_LIMIT`, `ORDER_TYPE_SELL_STOP`, `ORDER_TYPE_SELL_STOP_LIMIT`). Do not apply open-position-only profit/loss, close percentage, or position point filters to pending orders because pending orders have no floating position P/L or close volume. Keep the existing position close behavior intact and avoid broad refactors, formatting churn, or changes outside this file.</action>
  <verify>
    <automated>git diff -- mql5/CloseAllPositions.mq5</automated>
  </verify>
  <done>`CloseAllPositions.mq5` contains a pending-order iteration path and uses `Trade.OrderDelete` for matching pending orders while preserving existing position close logic.</done>
</task>

<task type="auto">
  <name>Task 3: Compile or syntax-check without committing generated artifacts</name>
  <files>mql5/CloseAllPositions.mq5</files>
  <action>Run compile/syntax verification if MetaEditor is available using the command pattern from `mql5/Build_Rules.md`, adjusted to this script: `cmd /c ""E:\\Openclaw\\MetaTrader5\\MetaEditor64.exe" /compile:"D:\\Aureus\\mql5\\CloseAllPositions.mq5" /log:"D:\\Aureus\\mql5\\CloseAllPositions_compile.log""`. If the compiler path is unavailable in this environment, document that clearly in the summary and at minimum inspect the diff for MQL5 syntax issues. After compilation, remove or leave untracked generated `CloseAllPositions_compile.log` and `CloseAllPositions.ex5`; do not commit any `.log` or `.ex5` artifacts. Run GitNexus change detection before any commit if committing is requested later: `gitnexus_detect_changes()` when available, with fallback documentation if unavailable.</action>
  <verify>
    <automated>cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\CloseAllPositions.mq5" /log:"D:\Aureus\mql5\CloseAllPositions_compile.log"" || true</automated>
    <automated>git status --short -- mql5/CloseAllPositions.mq5 mql5/CloseAllPositions.ex5 mql5/CloseAllPositions_compile.log</automated>
  </verify>
  <done>Compile succeeds with no errors/warnings, or compiler unavailability is documented; generated log/ex5 artifacts are not staged or committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User/script inputs -> MT5 trade account | Script inputs determine which live positions/orders are modified. |
| Script -> broker trade server | `CTrade` close/delete requests affect account state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260430-s08-01 | Tampering | Pending order deletion filters | mitigate | Reuse existing symbol/magic/comment/direction filters for pending orders so the script does not delete unrelated orders. |
| T-260430-s08-02 | Denial of Service | Retry loop for `OrderDelete` | mitigate | Match existing bounded `Retries` and `Delay` behavior; no unbounded loops. |
| T-260430-s08-03 | Repudiation | Close/delete audit trail | mitigate | Print total pending orders and delete errors with ticket and `GetLastError()` similar to existing close error logging. |
</threat_model>

<verification>
Overall checks:
1. `git diff -- mql5/CloseAllPositions.mq5` shows only surgical script changes.
2. Compile via MetaEditor command from `mql5/Build_Rules.md` adjusted for `CloseAllPositions.mq5`, if available.
3. `git status --short -- mql5/CloseAllPositions.mq5 mql5/CloseAllPositions.ex5 mql5/CloseAllPositions_compile.log` confirms only source remains relevant and generated artifacts are not committed.
</verification>

<success_criteria>
- Matching pending orders are deleted/cancelled by the script.
- Existing open-position closing behavior remains unchanged.
- Filters for current symbol, magic number, comment, and BUY/SELL direction apply consistently to pending orders.
- Compile/syntax verification is attempted and documented.
- Scope stays limited to `mql5/CloseAllPositions.mq5` and planning/summary artifacts.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260430-s08-update-mql5-closeallpositions-mq5-to-als/260430-s08-SUMMARY.md`.
</output>
