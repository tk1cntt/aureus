---
phase: 260503-b25-reasoning-bank-strategy-telegram
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/reasoning_embeddings.py
  - services/aureus-trader/tests/test_reasoning_embeddings.py
  - services/aureus-notifier/formatters.py
  - services/aureus-notifier/rate_limiter.py
  - services/aureus-notifier/tests/test_formatters.py
  - services/aureus-notifier/tests/test_rate_limiter.py
  - services/aureus-notifier/scripts/verify_reasoning_bank_strategy_telegram_e2e.py
autonomous: true
requirements:
  - QUICK-260503-B25
must_haves:
  truths:
    - "Telegram STRATEGY_MATCH hiển thị Reasoning Bank insight bổ sung theo đúng strategy đang trigger."
    - "Reasoning Bank chỉ enrich message Telegram, không filter/block/skip/order-gate execute order."
    - "Retrieval/statistics Reasoning Bank được scope theo strategy_name; symbol/direction được dùng làm scope phụ khi có."
    - "Nếu Reasoning Bank/embedding/DB lỗi hoặc không có dữ liệu, STRATEGY_MATCH vẫn enqueue/gửi Telegram bình thường với phần insight unavailable."
    - "Không có thay đổi vào dispatch order, validator, MT5 command, hoặc strategy execution gate."
  artifacts:
    - path: "services/aureus-trader/reasoning_embeddings.py"
      provides: "Strategy-scoped Reasoning Bank retrieval/statistics helper for Telegram enrichment"
      exports: ["fetch_strategy_reasoning_insights"]
    - path: "services/aureus-notifier/formatters.py"
      provides: "STRATEGY_MATCH Telegram formatter renders optional Reasoning Bank section"
      contains: "Reasoning Bank"
    - path: "services/aureus-notifier/rate_limiter.py"
      provides: "Dispatcher enriches STRATEGY_MATCH message with Reasoning Bank insights before formatting"
      contains: "fetch_strategy_reasoning_insights"
    - path: "services/aureus-notifier/scripts/verify_reasoning_bank_strategy_telegram_e2e.py"
      provides: "DB/runtime E2E proving strategy-scoped insights appear in Telegram text without changing order gating"
  key_links:
    - from: "services/aureus-notifier/rate_limiter.py"
      to: "services/aureus-trader/reasoning_embeddings.py"
      via: "optional import/call for STRATEGY_MATCH enrichment only"
      pattern: "fetch_strategy_reasoning_insights"
    - from: "services/aureus-trader/reasoning_embeddings.py"
      to: "aureus_reasoning_entries"
      via: "strategy_name WHERE clause plus optional symbol/direction predicates/statistics"
      pattern: "strategy_name.*symbol.*direction"
    - from: "services/aureus-notifier/formatters.py"
      to: "event.data.reasoning_bank"
      via: "format_strategy_match optional section"
      pattern: "reasoning_bank"
    - from: "services/aureus-trader/dispatcher.py"
      to: "services/aureus-trader/orders.py or MT5 dispatch"
      via: "no code change allowed; verify git diff has no order-gating edits"
      pattern: "NO_MODIFY"
---

<objective>
Bổ sung Reasoning Bank vào thông tin strategy khi gửi Telegram STRATEGY_MATCH.

Purpose: giúp người đọc Telegram thấy bài học/thống kê Reasoning Bank theo đúng strategy đang trigger, nhưng tuyệt đối không dùng Reasoning Bank để filter/block/skip/gate execute order.
Output: helper retrieval/statistics scope strategy_name, Telegram formatter/enrichment, unit tests, DB/runtime E2E.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@RUN_SERVICES.md
@.planning/quick/260502-wur-th-c-hi-n-implement-theo-plan-planning-q/260502-wur-SUMMARY.md
@.planning/quick/260503-a2k-b-sung-t-nh-n-ng-reasoning-bank-embeddin/260503-a2k-SUMMARY.md
@services/aureus-trader/reasoning_embeddings.py
@services/aureus-trader/journal.py
@services/aureus-notifier/formatters.py
@services/aureus-notifier/rate_limiter.py

<interfaces>
Existing Reasoning Bank table columns from prior summaries include: `id`, `trace_id`, `trade_journal_id`, `signal_snapshot_id`, `strategy_id`, `strategy_name`, `symbol`, `timeframe`, `direction`, `reasoning_source`, `reasoning_text`, `decision_action`, `confidence`, `active_signals`, `context_filters`, `success`, `reward`, `pnl`, `pnl_pips`, `result`, `created_at`, `evaluated_at`, `updated_at`, `reasoning_embedding`, `prompt_embedding`, `context_embedding`, `prompt_text`, `context_text`, `embedding_model`, `embedded_at`.

Existing Telegram strategy path: `RateLimitedDispatcher.enqueue()` formats `STRATEGY_MATCH` using `format_strategy_match(event)`, then dispatches via strategy Telegram bot. Existing formatter reads `event["data"]` fields: `strategy`, `strategy_id`, `side`, `entry_type`, `sl/sl_absolute`, `tp/tp_absolute`, `size_value`, `entry_price`, `order_plan`, `reason_code`.

Hard business rule: Reasoning Bank enrichment must live only in notifier/Telegram formatting path. Do not change `services/aureus-trader/dispatcher.py`, `services/aureus-trader/validator.py`, `services/aureus-signal/engine/strategy_executor.py`, order command builders, MT5 gateway, or any branch that decides whether order executes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strategy-scoped Reasoning Bank insight helper</name>
  <files>services/aureus-trader/reasoning_embeddings.py, services/aureus-trader/tests/test_reasoning_embeddings.py</files>
  <behavior>
    - `fetch_strategy_reasoning_insights(conn, strategy_name, symbol=None, direction=None, limit=5, query_text=None, client=None)` requires non-empty `strategy_name` and always filters DB by `strategy_name = $1`.
    - Optional `symbol` and `direction` narrow retrieval/statistics when provided; missing optional scope still keeps strategy_name scope.
    - Helper returns structured dict for Telegram: `strategy_name`, `symbol`, `direction`, `sample_size`, `success_rate`, `avg_reward`, `avg_pnl_pips`, `recent_lessons`, `similar_lessons`.
    - `recent_lessons` come from latest evaluated/recent reasoning rows for that strategy scope; trim each lesson for Telegram.
    - If `query_text` and embeddings exist, `similar_lessons` uses semantic search with same `strategy_name`/optional symbol/direction WHERE scope; never searches across unrelated strategies.
    - Empty DB result returns valid unavailable/empty insight object, not exception.
  </behavior>
  <action>Run GitNexus before symbol edit per project rule: `gitnexus_impact({target: "semantic_search_reasoning_entries", direction: "upstream"})` if reusing/modifying that function, and `gitnexus_impact({target: "ReasoningEmbeddingClient", direction: "upstream"})` if changing class behavior. Prefer adding new helper without changing existing public behavior. Use asyncpg parameter binding only; validate limit 1..20. Do not add schema/migration unless executor proves existing columns cannot support stats; if DB schema change becomes necessary, stop and ask because quick plan intentionally avoids new DB changes. Unit tests must assert generated SQL/args include `strategy_name` filter and optional `symbol`/`direction`, and assert strategy A does not receive strategy B rows.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-trader && python -m pytest tests/test_reasoning_embeddings.py -q</automated>
  </verify>
  <done>Helper returns strategy-specific statistics/lessons, optional symbol/direction narrowing works, semantic retrieval if used remains strategy-scoped, no hash/digest embedding behavior regresses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Reasoning Bank into STRATEGY_MATCH Telegram only</name>
  <files>services/aureus-notifier/formatters.py, services/aureus-notifier/rate_limiter.py, services/aureus-notifier/tests/test_formatters.py, services/aureus-notifier/tests/test_rate_limiter.py</files>
  <behavior>
    - `format_strategy_match(event)` renders optional `<b>Reasoning Bank</b>` section only when `event["data"]["reasoning_bank"]` exists.
    - Section displays strategy-scoped stats: sample size, win/success rate if available, avg reward/pips if available, 1-3 recent/similar lessons.
    - All text is HTML-escaped and final message remains <= 4095 chars; if too long, trim Reasoning Bank lessons first, not trade plan core fields.
    - `RateLimitedDispatcher.enqueue()` enriches only `event["type"] == "STRATEGY_MATCH"` before formatting; `SIGNAL_EVENT` untouched.
    - Enrichment failure logs warning and falls back to existing STRATEGY_MATCH message; enqueue count remains same.
    - No code path uses insight to return 0, skip enqueue, alter event direction/entry/sl/tp/size, or gate order execution.
  </behavior>
  <action>Run GitNexus impact before editing symbols: `gitnexus_impact({target: "format_strategy_match", direction: "upstream"})` and `gitnexus_impact({target: "RateLimitedDispatcher.enqueue", direction: "upstream"})` (fallback to method name if full target not found). Add minimal optional DB lookup in notifier dispatcher: lazily create DB pool using same env pattern as `OrderStatusReporter._get_db_pool()` or reuse a small local helper; call `fetch_strategy_reasoning_insights` with `strategy_name` from `data.strategy` or `data.strategy_name`, `symbol` from event/data, `direction` from `data.side`/`data.direction`, and `query_text` from `data.reason_code` or `data.reasoning`. Keep enrichment inside notifier only. Do not modify trader dispatcher/order execution files. Tests must monkeypatch helper/DB pool and prove: success enriches formatter, failure still enqueues original STRATEGY_MATCH, SIGNAL_EVENT does not call helper, output contains strategy-specific Reasoning Bank section.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-notifier && python -m pytest tests/test_formatters.py tests/test_rate_limiter.py -q</automated>
  </verify>
  <done>Telegram strategy message includes Reasoning Bank section when available; no available insight or DB error preserves existing message/enqueue behavior; no order execution/gating file modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add DB/runtime E2E proving strategy scope and no execute gating</name>
  <files>services/aureus-notifier/scripts/verify_reasoning_bank_strategy_telegram_e2e.py, services/aureus-notifier/tests/test_rate_limiter.py</files>
  <behavior>
    - E2E connects to Postgres using `DATABASE_URL` or `AUREUS_DB_DSN`, applies existing Reasoning Bank migrations if test environment needs them, and inserts test `aureus_reasoning_entries` rows for two different strategies with same symbol/direction.
    - E2E formats/enqueues a STRATEGY_MATCH for strategy A and asserts Telegram text includes only strategy A insights, not strategy B lessons.
    - E2E asserts enqueue returns 1 even when Reasoning Bank lookup is unavailable/fails.
    - E2E asserts no source files related to order execution were modified in this plan: `services/aureus-trader/dispatcher.py`, `services/aureus-trader/validator.py`, `services/aureus-signal/engine/strategy_executor.py`, and order builder files must be absent from git diff.
    - If DB command fails because services are down, executor reads `RUN_SERVICES.md`, starts required DB/Redis/service, and reruns.
  </behavior>
  <action>Create script modeled after existing E2E scripts but scoped to notifier Telegram enrichment. Use fake Telegram sender and direct dispatcher/formatter call; do not send real Telegram messages. Because this task touches DB data, run true DB E2E per CLAUDE.md with cleanup by unique trace_id prefix. Before finish, run `gitnexus_detect_changes()` if MCP exists; if only CLI exists and command unavailable, record exact limitation in SUMMARY, then use `git diff --name-only` to prove no execution-gating files changed. Do not commit unless user/orchestrator explicitly asks.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-notifier && python scripts/verify_reasoning_bank_strategy_telegram_e2e.py</automated>
  </verify>
  <done>DB/runtime E2E passes, same-symbol same-direction rows from other strategy never appear in strategy A Telegram insight, lookup failure does not reduce enqueue count, and diff proves order execution path untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier → Postgres | Telegram notifier reads Reasoning Bank data to enrich message; DB may be down or contain untrusted text. |
| DB text → Telegram HTML | reasoning_text/lessons become Telegram HTML; must escape and trim. |
| Reasoning Bank → strategy event path | Insight must remain informational only and cannot affect order execution or enqueue/drop decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260503-b25-01 | I | `format_strategy_match` | mitigate | HTML-escape all Reasoning Bank lesson/stat text before Telegram output. |
| T-260503-b25-02 | D | `RateLimitedDispatcher.enqueue` DB enrichment | mitigate | Catch lookup exceptions and fall back to original STRATEGY_MATCH message/enqueue. |
| T-260503-b25-03 | T | `fetch_strategy_reasoning_insights` SQL | mitigate | Use asyncpg parameter binding and bounded limit; no string interpolation for strategy/symbol/direction/query text. |
| T-260503-b25-04 | I/T | Cross-strategy retrieval | mitigate | Mandatory `strategy_name` WHERE filter; unit and E2E prove strategy B lessons do not appear in strategy A Telegram. |
| T-260503-b25-05 | E/T | Execute-order gating creep | mitigate | Plan forbids editing trader dispatcher/validator/strategy executor/order builders; tests assert enrichment failure still enqueues; diff check verifies no order-gating files changed. |
</threat_model>

<verification>
1. Unit trader helper: `cd D:/Aureus/services/aureus-trader && python -m pytest tests/test_reasoning_embeddings.py -q`
2. Unit notifier: `cd D:/Aureus/services/aureus-notifier && python -m pytest tests/test_formatters.py tests/test_rate_limiter.py -q`
3. DB/runtime E2E: `cd D:/Aureus/services/aureus-notifier && python scripts/verify_reasoning_bank_strategy_telegram_e2e.py`
4. GitNexus: run impact before editing `semantic_search_reasoning_entries`/`ReasoningEmbeddingClient` if touched, `format_strategy_match`, and `RateLimitedDispatcher.enqueue`; run detect changes before finish or record tool limitation.
5. Scope guard: `git diff --name-only` must not list order execution/gating files (`services/aureus-trader/dispatcher.py`, `services/aureus-trader/validator.py`, `services/aureus-signal/engine/strategy_executor.py`, order builder files).
</verification>

<success_criteria>
- STRATEGY_MATCH Telegram message shows Reasoning Bank stats/lessons when data exists.
- Retrieval/statistics are scoped by `strategy_name`; optional `symbol`/`direction` narrowing included when available.
- Reasoning Bank failures never block Telegram enqueue and never affect execute order.
- No execute-order/gating source file changed.
- DB/runtime E2E proves strategy A does not leak strategy B reasoning into Telegram insight.
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/quick/260503-b25-h-t-i-mu-n-b-sung-ph-n-reasoning-bank-v-/260503-b25-SUMMARY.md` bằng tiếng Việt.
</output>
