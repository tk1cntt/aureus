---
phase: 260430-sm5-base-tr-n-managepositionprofitbreakevent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
autonomous: true
requirements:
  - QUICK-260430-SM5
must_haves:
  truths:
    - "Provider quản lý profit/breakeven cho mọi symbol trong InpSymbols, không chỉ chart _Symbol."
    - "Các lệnh được quản lý theo đúng nhóm symbol + magic + direction, không trộn BUY/SELL, không trộn strategy, không trộn symbol."
    - "Logic quản lý được base theo ManagePositionProfitBreakEvent()/ProcessPositionsByType() trong CISD_Slope_EA_v6.39_Final nhưng đã bỏ các giả định EA chart-symbol."
    - "Không ảnh hưởng manual trade, magic khác, symbol ngoài InpSymbols, hoặc side ngược chiều."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Provider-safe profit/breakeven position management"
      contains: "ManagePositionProfitBreakEvent"
  key_links:
    - from: "mql5/AureusProvider_v2.mq5::OnTimer"
      to: "mql5/AureusProvider_v2.mq5::ManagePositionProfitBreakEvent"
      via: "timer loop for all configured symbols"
      pattern: "ManagePositionProfitBreakEvent"
    - from: "mql5/AureusProvider_v2.mq5::ManagePositionProfitBreakEvent"
      to: "mql5/AureusProvider_v2.mq5::ProcessPositionsByType"
      via: "symbol + magic + direction scoped ticket groups"
      pattern: "ProcessPositionsByType"
---

<objective>
Bổ sung logic quản lý profit/breakeven vào `mql5/AureusProvider_v2.mq5`, base theo `ManagePositionProfitBreakEvent()` và `ProcessPositionsByType()` trong `mql5/CISD_Slope_EA_v6.39_Final.mq5`.

Purpose: Provider v2 đang quản lý multi-symbol và nhận lệnh từ gateway, nên logic copy từ EA phải được chuyển thành provider-safe: chạy cho toàn bộ `InpSymbols`, scope theo `symbol + magic + direction`, không dùng `_Symbol` làm identity giao dịch, không tác động strategy/symbol/side khác.

Output: `AureusProvider_v2.mq5` có hàm quản lý profit/breakeven tương tự EA, được gọi định kỳ và compile được nếu MetaEditor khả dụng.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/mql5/AureusProvider_v2.mq5
@D:/Aureus/mql5/CISD_Slope_EA_v6.39_Final.mq5
@D:/Aureus/mql5/Build_Rules.md

<interfaces>
Các contracts hiện có trong provider cần giữ nguyên:

```mql5
input string InpSymbols = "USDJPY,EURUSD,GBPUSD,AUDUSD";
SymbolContext g_contexts[];
int g_symbolCount;
CTrade trade;

int FindContextIndex(string symbol);
bool IsForexPair(string symbol);
void OnTimer();
void CheckDCAEntryConditionFromCISD(string symbol, CISDDCAState &state);
```

Source reference trong EA:

```mql5
void ManagePositionProfitBreakEvent();
void ProcessPositionsByType(ENUM_POSITION_TYPE target_type,
                            const long &tickets[],
                            double total_profit,
                            double total_volume,
                            double weighted_price_sum,
                            datetime earliest_open_time);
```

Provider invariant bắt buộc:
- Không dùng `_Symbol` để lọc/group position trong logic mới, trừ khi chỉ liên quan chart tick streaming hiện có.
- Chỉ quản lý position có `POSITION_SYMBOL` nằm trong `InpSymbols` và `POSITION_MAGIC != 0`.
- Group riêng theo `symbol + magic + POSITION_TYPE`; BUY và SELL cùng symbol/magic phải tách riêng.
- Mọi `SymbolInfoDouble`, `iLow`, `iHigh`, `iBarShift`, point/digits/tick_value phải dùng `symbol` của group, không dùng `_Symbol`, `_Point`, `_Digits`.
- Không sửa behavior gateway/order command ngoài phần gọi quản lý position cần thiết.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port profit/breakeven management with provider-safe grouping</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - Khi có nhiều positions cùng symbol + magic + BUY, logic tính count/profit/volume/weighted price và modify/close chỉ áp dụng cho tickets BUY của group đó.
    - Khi cùng symbol + magic có SELL song song, SELL được xử lý trong group riêng; BUY action không close/modify SELL và ngược lại.
    - Khi có symbol khác hoặc magic khác, group hiện tại không thấy/tác động các ticket đó.
    - Khi symbol không nằm trong InpSymbols hoặc magic == 0, logic quản lý bỏ qua hoàn toàn.
  </behavior>
  <action>Trước khi sửa symbol/hàm, tuân thủ CLAUDE.md: chạy GitNexus impact cho các symbol dự kiến chỉnh (`OnTimer`, `DoDCA` nếu đụng, và hàm mới/được thay đổi nếu GitNexus nhận diện được) rồi ghi blast radius vào summary. Trong `mql5/AureusProvider_v2.mq5`, thêm logic quản lý base theo `ManagePositionProfitBreakEvent()`/`ProcessPositionsByType()` của EA nhưng thiết kế lại signature cho provider, ví dụ `ManagePositionProfitBreakEvent()` quét tất cả positions rồi tạo các group `symbol + magic + direction`, hoặc helper `ManagePositionProfitBreakEventForGroup(symbol, magic, direction)`. Copy nguyên ý tưởng tính total_profit/volume/weighted_avg, net_profit sau commission, close khi loss/risk, dời SL theo imbalance/breakeven nếu có profit threshold; nhưng mọi phép lấy giá/point/digits/tick/swap/commission phải truyền `symbol`. Nếu cần dùng imbalance như source (`IsImbalanceUp/Down`) mà provider chưa có, thêm helper tối thiểu trong cùng file với signature nhận `symbol, timeframe, shift`; không kéo UI/EA state không liên quan. Không port các biến trạng thái chart-signal như `g_h1_buy_signal_active`, `g_last_h1_processed_bar_time`; nếu source dùng chúng thì bỏ hoặc thay bằng log provider-safe vì provider không quản lý entry signal state của EA.</action>
  <verify>
    <automated>cd D:/Aureus && git diff -- mql5/AureusProvider_v2.mq5</automated>
  </verify>
  <done>`AureusProvider_v2.mq5` có hàm quản lý profit/breakeven scoped theo `symbol + magic + direction`; code không còn giả định `_Symbol` trong logic quản lý position multi-symbol; không thêm file mới.</done>
</task>

<task type="auto">
  <name>Task 2: Wire management into provider timer for all InpSymbols</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <action>Gọi logic quản lý mới trong `OnTimer()` sau khi connection/command processing phù hợp và trước/sau DCA gate một cách rõ ràng. Đảm bảo vòng quản lý không phụ thuộc chart tick và chạy cho tất cả groups thuộc symbols trong `g_contexts`. Không thay đổi tick/candle streaming behavior. Nếu hàm mới đã tự quét toàn bộ positions thì chỉ gọi một lần mỗi timer; nếu helper theo symbol thì loop `g_contexts[i].symbol`. Giữ logging ngắn, có prefix `[ManagePositionProfitBreakEvent] [symbol:magic:side]` để trace nhưng không spam khi không có action.</action>
  <verify>
    <automated>cd D:/Aureus && git diff --check -- mql5/AureusProvider_v2.mq5</automated>
  </verify>
  <done>`OnTimer()` kích hoạt quản lý position định kỳ cho toàn bộ `InpSymbols`, độc lập chart `_Symbol`, đồng thời DCA gate hiện có vẫn chạy như trước.</done>
</task>

<task type="auto">
  <name>Task 3: Compile provider and keep generated artifacts out of git</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <action>Compile `mql5/AureusProvider_v2.mq5` nếu MetaEditor khả dụng theo `mql5/Build_Rules.md`. Dùng command tương tự: `cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\AureusProvider_v2.mq5" /log:"D:\Aureus\mql5\AureusProvider_v2_compile.log""`. Phân tích log và sửa đến khi hết compile error; warning cũng phải xử lý nếu warning do thay đổi này tạo ra. Không commit/generated artifacts: không stage `.log`, `.ex5`, snapshots, tmp, stable hoặc file ngoài `AureusProvider_v2.mq5` trừ khi user yêu cầu. Sau sửa code, chạy `gitnexus_detect_changes()` trước khi kết thúc để xác nhận phạm vi thay đổi đúng theo CLAUDE.md.</action>
  <verify>
    <automated>cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\AureusProvider_v2.mq5" /log:"D:\Aureus\mql5\AureusProvider_v2_compile.log"" || true</automated>
    <automated>cd D:/Aureus && git status --short</automated>
  </verify>
  <done>Compile được nếu MetaEditor tồn tại; nếu MetaEditor không tồn tại thì summary ghi rõ không khả dụng. Git status không stage/commit `.log` hoặc `.ex5`; thay đổi chính chỉ là `mql5/AureusProvider_v2.mq5`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider timer → MT5 trade server | Logic tự động close/modify positions dựa trên live account state. |
| Gateway strategies → provider positions | Multiple strategies/magic có thể cùng symbol và cùng/khác direction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260430-sm5-01 | Tampering | Position grouping | mitigate | Lọc và group bằng `POSITION_SYMBOL`, `POSITION_MAGIC`, `POSITION_TYPE`; skip `magic == 0` và symbol ngoài `InpSymbols`. |
| T-260430-sm5-02 | Elevation of Privilege | Close/modify operations | mitigate | Chỉ truyền tickets đã collect từ đúng group vào `trade.PositionClose/PositionModify`; không dùng `PositionSelect(symbol)` để thao tác ambiguous group. |
| T-260430-sm5-03 | Denial of Service | Timer loop | accept | Số positions thấp; loop mỗi timer theo MT5 state. Giữ logging chỉ khi có action/skip quan trọng. |
| T-260430-sm5-04 | Information Disclosure | Compile artifacts/logs | mitigate | Không commit `.log`/`.ex5`; chỉ dùng log để fix compile. |
</threat_model>

<verification>
1. `git diff --check -- mql5/AureusProvider_v2.mq5` không lỗi whitespace.
2. Compile `AureusProvider_v2.mq5` bằng MetaEditor nếu có theo `Build_Rules.md`.
3. Review diff xác nhận không có `_Symbol`, `_Point`, `_Digits` trong logic quản lý profit/breakeven mới, ngoại trừ code cũ không liên quan.
4. Review diff xác nhận không có thay đổi database và không stage/generated `.log`/`.ex5`.
5. Chạy `gitnexus_detect_changes()` trước khi hoàn tất implementation summary.
</verification>

<success_criteria>
- Provider v2 tự quản lý profit/breakeven cho positions của mọi symbol trong `InpSymbols`.
- Position management không ảnh hưởng manual trades, magic khác, symbol ngoài `InpSymbols`, hoặc direction đối nghịch.
- BUY/SELL được xử lý độc lập theo symbol + magic.
- Compile attempt được thực hiện nếu MetaEditor khả dụng; generated artifacts không được commit.
</success_criteria>

<output>
Sau khi hoàn tất, tạo `D:/Aureus/.planning/quick/260430-sm5-base-tr-n-managepositionprofitbreakevent/260430-sm5-SUMMARY.md`.
</output>
