---
phase: quick-260503-w57-update-classify-shape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/tpo.py
  - services/aureus-signal/tests/test_tpo_signal.py
autonomous: true
requirements:
  - QUICK-260503-W57
must_haves:
  truths:
    - "_classify_shape chỉ trả shape D/p/b khi điểm shape_scores_pct tốt nhất > 70.0."
    - "_classify_shape trả shape None và confidence 0.0 khi điểm tốt nhất <= 70.0 hoặc dữ liệu rỗng/zero không đủ xác nhận."
    - "Shape B không còn được emit từ classifier và không còn được score như recognized shape."
    - "Các public block TPO vẫn giữ shape_scores_pct ổn định, ưu tiên giữ key B=0.0 nếu hợp đồng test hiện tại còn phụ thuộc để giảm blast radius."
  artifacts:
    - path: "services/aureus-signal/engine/signals/tpo.py"
      provides: "TPO shape classifier threshold >70% và remove B scoring"
      contains: "SHAPES"
    - path: "services/aureus-signal/tests/test_tpo_signal.py"
      provides: "Regression tests cho threshold None và no-B behavior"
      contains: "test_tpo_classify_shape"
  key_links:
    - from: "services/aureus-signal/engine/signals/tpo.py::_classify_shape"
      to: "services/aureus-signal/engine/signals/tpo.py::_build_tpo_block"
      via: "shape, confidence_pct, scores tuple"
      pattern: "shape, confidence_pct, scores = self._classify_shape"
    - from: "services/aureus-signal/tests/test_tpo_signal.py"
      to: "TPOSignal._classify_shape"
      via: "direct fixture call"
      pattern: "sig._classify_shape"
---

<objective>
Sửa TPO `_classify_shape` đúng rule user: chỉ xác nhận shape khi best `shape_scores_pct` > 70%, còn lại `None`; bỏ shape `B` khỏi recognized output/scoring.

Purpose: Giảm false-positive shape classification và bỏ profile `B` vì khó nhận diện ổn định.
Output: Một thay đổi nhỏ ở classifier cộng regression tests.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/services/aureus-signal/engine/signals/tpo.py
@D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py
@D:/Aureus/.planning/quick/260503-ul1-s-a-classify-shape-cu-a-tpo-py-nh-n-d-ng/260503-ul1-SUMMARY.md

Important constraints:
- Trước khi sửa symbol `_classify_shape`, executor phải chạy GitNexus impact: `gitnexus_impact({target: "_classify_shape", direction: "upstream"})`, báo blast radius, rồi mới edit.
- Không chạm uncommitted unrelated user work: `mql5/AureusProvider_v2.mq5`, `services/aureus-trader/journal.py`, `mql5/AureusProvider_v2.ex5`, `stable/`.
- Không đổi database.
- Least-impact contract: hiện tests nhiều nơi đang assert `shape_scores_pct` có key `{D,B,p,b}`. Nếu không có bằng chứng hợp đồng đã an toàn để remove key B, giữ key `B` trong dict với value `0.0`, nhưng không dùng B trong `SHAPES` recognized/ranking và không bao giờ emit shape `B`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add classifier regression tests for threshold and no-B rule</name>
  <files>services/aureus-signal/tests/test_tpo_signal.py</files>
  <behavior>
    - Clear D/p/b fixture with best `shape_scores_pct` > 70.0 returns that shape and confidence > 0.0.
    - Near-tie or low-confidence fixture with best `shape_scores_pct` <= 70.0 returns `shape is None` and `confidence == 0.0` while scores remain normalized.
    - Former B-like two-peak fixture no longer returns `B`; `scores["B"] == 0.0` if B key kept for compatibility.
    - Empty/zero profile returns `shape is None`, `confidence == 0.0`, and no recognized B scoring.
  </behavior>
  <action>Update existing `_assert_scores_contract` and focused `test_tpo_classify_shape_*` tests to allow `shape is None` where threshold fails, remove assertions expecting emitted shape `B`, and add explicit regression cases for best score exactly/under 70 being unconfirmed. Keep edits surgical in TPO classifier tests only. Do not update unrelated replay/context fixture tests unless they fail because helper contract changed.</action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-signal/tests/test_tpo_signal.py -q</automated>
  </verify>
  <done>Tests encode user rules before production change: threshold >70 required, no emitted B, fallback None for unconfirmed profiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement thresholded no-B classifier</name>
  <files>services/aureus-signal/engine/signals/tpo.py, services/aureus-signal/tests/test_tpo_signal.py</files>
  <behavior>
    - `_classify_shape` returns `Tuple[Optional[str], float, Dict[str, float]]` semantics: shape is `None` when unconfirmed.
    - `SHAPES` recognized ranking excludes uppercase `B`; compatibility score dict may keep `B: 0.0`.
    - `best_shape` chosen only from D/p/b. Confirm only when `normalized[best_shape] > 70.0`; else return `(None, 0.0, normalized_scores)`.
    - Empty/zero/no-score paths return `(None, 0.0, scores)` instead of defaulting to D.
  </behavior>
  <action>Run GitNexus impact for `_classify_shape` before edit and report direct callers/risk. Then update `services/aureus-signal/engine/signals/tpo.py`: change type hint to Optional shape if needed, remove B from recognized `SHAPES`/ranking/scoring, set B compatibility score to 0.0 if keeping dict key, normalize remaining D/p/b so scores sum to 100 across recognized shapes plus B=0.0, and gate final return so top score must be strictly greater than 70.0. Remove duplicate/obsolete B confidence caps only where tied to removed B evidence; do not refactor unrelated TPO code. Update tests only as needed for new public contract.</action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-signal/tests/test_tpo_signal.py services/aureus-signal/tests/test_tpo_context.py services/aureus-signal/tests/test_indicator_snapshot.py -q</automated>
  </verify>
  <done>`_classify_shape` never emits B, emits None below/equal 70%, and existing TPO block consumers still receive stable score metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused impact and change-scope verification</name>
  <files>services/aureus-signal/engine/signals/tpo.py, services/aureus-signal/tests/test_tpo_signal.py</files>
  <action>Run focused tests plus GitNexus change detection before finishing. Use `gitnexus_detect_changes()` to confirm only expected symbols/files changed. If GitNexus says index stale, run `cd D:/Aureus && npx gitnexus analyze` first, then rerun detection. Do not stage or commit unless explicitly asked by user.</action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-signal/tests/test_tpo_signal.py services/aureus-signal/tests/test_tpo_context.py services/aureus-signal/tests/test_tpo_replay.py services/aureus-signal/tests/test_indicator_snapshot.py -q</automated>
  </verify>
  <done>Focused suite passes, GitNexus detect_changes matches expected TPO classifier/test scope, unrelated user work remains untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market data -> TPO classifier | Untrusted/noisy OHLC-derived counts influence shape metadata |
| classifier -> downstream strategy context/Telegram | Shape label consumed by other code and humans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260503-w57-01 | Tampering | `_classify_shape` inputs | mitigate | Clamp/normalize counts as existing code does; tests cover empty/zero/sparse inputs. |
| T-260503-w57-02 | Information Disclosure | TPO metadata | accept | No secrets/PII; output is market-derived signal metadata. |
| T-260503-w57-03 | Denial of Service | score normalization | mitigate | Preserve existing bounded loops and no new heavy work; focused tests exercise classifier path. |
| T-260503-w57-04 | Repudiation | emitted shape changes | mitigate | Regression tests pin None threshold and no-B contract. |
</threat_model>

<verification>
Overall checks:
- `pytest services/aureus-signal/tests/test_tpo_signal.py services/aureus-signal/tests/test_tpo_context.py services/aureus-signal/tests/test_tpo_replay.py services/aureus-signal/tests/test_indicator_snapshot.py -q`
- GitNexus impact run before editing `_classify_shape`.
- GitNexus detect_changes run before finish.
</verification>

<success_criteria>
- Best `shape_scores_pct` > 70.0 returns D/p/b.
- Best `shape_scores_pct` <= 70.0 returns `None` and confidence `0.0`.
- Uppercase `B` no longer emitted or recognized in ranking/scoring; if dict key remains for compatibility, value is always `0.0`.
- Only `services/aureus-signal/engine/signals/tpo.py` and TPO classifier tests changed.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260503-w57-update-classify-shape-ch-x-c-nh-n-shape-/260503-w57-SUMMARY.md`
</output>
