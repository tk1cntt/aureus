---
must_haves:
  - FZ_CONT_BULL has choch_up + bos_up sequence (syntax error fix)
  - FZ_CONT_BEAR has choch_down + bos_down sequence
  - FIRST_HIGH_LOW_PIVOT / FIRST_LOW_HIGH_PIVOT entry methods implemented in orders.py
  - SL uses next swing pivot or nearest LL/HH as fallback
  - DB E2E verify strategy config after seed
---

## Tasks

### Task 1: Fix FZ_CONT_BULL syntax and FZ_CONT_BEAR bos_down sequence

**files:** `services/aureus-signal/engine/strategies/seed_strategies.py`

**action:**
- FZ_CONT_BULL line 78: add missing comma after choch_up dict `}`
- Add `{"tag": "bos_down", "weight": 4.0, "required": True, "max_wait": 30}` after choch_down in FZ_CONT_BEAR sequence
- Verify entry_method names: `FIRST_HIGH_LOW_PIVOT` (BUY) and `FIRST_LOW_HIGH_PIVOT` (SELL) match the logic

**verify:** grep confirms choch_up+bos_up in BULL, choch_down+bos_down in BEAR with valid JSON

**done:** when both sequence fixes verified

### Task 2: Implement FIRST_HIGH_LOW_PIVOT and FIRST_LOW_HIGH_PIVOT entry methods

**files:** `services/aureus-signal/engine/orders.py`

**action:**
- Add to `_calculate_entry_price`: `FIRST_HIGH_LOW_PIVOT` and `FIRST_LOW_HIGH_PIVOT` cases
- `FIRST_HIGH_LOW_PIVOT` (BUY): find the LOW pivot that created the BOS high — entry at that low (LIMIT price)
- `FIRST_LOW_HIGH_PIVOT` (SELL): find the HIGH pivot that created the BOS low — entry at that high (LIMIT price)
- Look at `_entry_pivot_limit` for reference implementation pattern using swing_points
- Logic: for BUY, get the LL that corresponds to the BOS_up trigger; for SELL, get the HH that corresponds to BOS_down

**verify:** grep confirms both methods handled in _calculate_entry_price

**done:** when both entry methods are implemented

### Task 3: DB E2E verify strategy seed

**files:** `services/aureus-signal/engine/strategies/seed_strategies.py`

**action:**
- Validate JSON structure after fixes
- Run seed script against DB to verify no errors

**verify:** `python -c "import json, ast; ast.literal_eval(open('services/aureus-signal/engine/strategies/seed_strategies.py').read().split('strategies = [')[1].split(']')[0] + ']')" 2>&1 || echo "JSON_INVALID"

**done:** when validation passes