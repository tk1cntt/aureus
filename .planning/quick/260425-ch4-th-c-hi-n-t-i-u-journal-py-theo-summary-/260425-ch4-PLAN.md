# Quick Task 260425-ch4 Plan

## Objective
Thực hiện tối ưu `services/aureus-trader/journal.py` theo summary `260425-c5f`: refactor an toàn để giảm round-trip trong `on_order_opened()`, giữ nguyên dữ liệu quan trọng như `trace_id` và toàn bộ mapping trong `_build_signal_snapshot_columns()`.

## Assumptions
- Không đổi schema/index database trong task này; vì vậy không bắt buộc E2E DB migration, nhưng vẫn chạy unit tests liên quan.
- Giữ nguyên semantic snapshot conflict hiện tại: `ON CONFLICT (trade_journal_id) DO NOTHING`.
- Giữ nguyên behavior missing evaluation core fields: không insert evaluation, nhưng vẫn update journal và persist signal snapshot.
- Refactor tối thiểu trong cùng file để giảm rủi ro mất mapping; chưa tách nhiều module/repository file mới.

## GitNexus Impact
- `TradeJournalManager` → LOW, impactedCount 0.
- `on_order_opened` → LOW, impactedCount 0.
- `_build_signal_snapshot_columns` → LOW, impactedCount 2.
  - d=1: `_has_signal_payload` (index stale/older), `on_order_opened`.
  - affected modules: Tests, Aureus-trader.
- `on_order_closed` → LOW, impactedCount 0.

## Steps
1. Refactor `on_order_opened()` SQL flow
   - Replace `UPDATE ... execute` + later `SELECT` with `UPDATE ... RETURNING id, strategy_name, symbol, active_signals, context_filters, timeframe`.
   - Keep `trace_id`, `ticket`, `entry_time`, score fields, `signal_schema_version`, and snapshot mapping unchanged.
   - Verify existing journal tests pass.

2. Add transaction boundary for opened lifecycle writes
   - Within one DB acquire, use transaction for journal update + optional evaluation insert + signal snapshot insert.
   - Preserve rule: missing evaluation core skips evaluation insert but does not block snapshot.

3. Improve ORDER_CLOSED fire-and-forget visibility
   - In `dispatcher.py`, wrap `asyncio.create_task(self.journal.on_order_closed(event))` with done callback that logs exceptions.
   - Avoid changing event payload semantics.

4. Verification
   - Run targeted tests:
     - `pytest services/aureus-trader/tests/test_journal.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_dispatcher.py -q`
   - Run GitNexus detect changes if available; fallback to git diff/status if CLI still lacks command.

## Files
- `services/aureus-trader/journal.py`
- `services/aureus-trader/dispatcher.py`
- `services/aureus-trader/tests/test_journal.py` (only if expected query assertions need update)
- `.planning/quick/260425-ch4-th-c-hi-n-t-i-u-journal-py-theo-summary-/260425-ch4-SUMMARY.md`
- `.planning/STATE.md`

## Success Criteria
- `on_order_opened()` happy path reduces one DB round-trip by using `UPDATE ... RETURNING`.
- `trace_id` and `_build_signal_snapshot_columns()` mapped data are preserved.
- Missing evaluation core fields still allow snapshot persistence.
- ORDER_CLOSED async task exceptions are logged.
- Target tests pass.
