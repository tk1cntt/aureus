---
phase: quick-260430-ouw
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
autonomous: true
requirements:
  - QUICK-260430-OUW
must_haves:
  truths:
    - "AureusProvider_v2 chỉ gọi DoDCA khi xuất hiện xác nhận CISD LTF tương đương CheckSignalsAndDraw_Stateful: bullish breakout gọi DoDCA(1), bearish breakout gọi DoDCA(-1)."
    - "Provider tự tính đủ state CISD tối thiểu để xác định điều kiện vào DCA, không cần copy UI/drawing/panel/Telegram/signal alert từ EA nguồn."
    - "Executor dừng ngay nếu mql5/AureusProvider_v2.mq5 hoặc mql5/CISD_Slope_EA_v6.39_Final.mq5 không tồn tại trong current worktree; không tạo file thay thế và không chuyển sang AureusProvider.mq5."
    - "Socket JSON, ACK/NACK, ORDER_OPENED/ORDER_FAILED, streaming candle/tick hiện có không đổi semantic."
    - "mql5/AureusProvider_v2.mq5 compile bằng MetaEditor với 0 errors và 0 warnings."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Provider-local helper/call pattern adapting CheckSignalsAndDraw_Stateful DCA entry-condition logic plus minimal CISD state/helpers."
      contains: "DoDCA(1)"
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Bearish DCA trigger parity with source function, guarded against duplicate every-tick calls."
      contains: "DoDCA(-1)"
    - path: "mql5/AureusProvider_v2_compile.log"
      provides: "Compile log generated by MetaEditor verification."
      contains: "0 error(s), 0 warning(s)"
  key_links:
    - from: "mql5/CISD_Slope_EA_v6.39_Final.mq5::CheckSignalsAndDraw_Stateful"
      to: "mql5/AureusProvider_v2.mq5::CheckDCAEntryConditionFromCISD"
      via: "copy/adapt only DCA gate: setup state, H1/M5 signal state, ltf scan start, last trade signal time, bullish/bearish confirmation checks"
      pattern: "DoDCA\\((1|-1)\\)"
    - from: "mql5/AureusProvider_v2.mq5::CheckDCAEntryConditionFromCISD"
      to: "mql5/AureusProvider_v2.mq5::DoDCA"
      via: "call existing DoDCA(1/-1) only when the copied CISD condition transitions true for the current chart symbol on a new qualifying closed bar/setup"
      pattern: "DoDCA\\((1|-1)\\)"
    - from: "mql5/AureusProvider_v2.mq5::ExecuteOpenOrder"
      to: "mql5/AureusProvider_v2.mq5::DoDCA"
      via: "preserve existing post-market-order DCA path while adding separate CISD-derived DCA gate"
      pattern: "DoDCA\\(direction == \"BUY\" \\? 1 : -1\\)"
    - from: "mql5/AureusProvider_v2.mq5"
      to: "E:/Openclaw/MetaTrader5/MetaEditor64.exe"
      via: "MetaEditor compile command from Build_Rules.md"
      pattern: "0 errors?[, ]+0 warnings?"
---

<objective>
Bổ sung logic điều kiện thực hiện DoDCA từ `CheckSignalsAndDraw_Stateful` trong EA nguồn sang `mql5/AureusProvider_v2.mq5`.

Purpose: Provider đã có `DoDCA`, nhưng cần thêm gate vào DCA theo cùng điều kiện xác nhận CISD LTF của `CheckSignalsAndDraw_Stateful`, để DCA không chỉ phụ thuộc vào post-market-order path hiện tại.
Output: Một thay đổi surgical trong `mql5/AureusProvider_v2.mq5`, chỉ gồm state/helper tối thiểu để tính điều kiện DCA và lời gọi `DoDCA(1/-1)` tương ứng.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@mql5/CISD_Slope_EA_v6.39_Final.mq5
@mql5/AureusProvider_v2.mq5
@mql5/Build_Rules.md

<prechecks>
Before dependency mapping, before editing, and before compile, executor must run from the current worktree:
```bash
test -f mql5/AureusProvider_v2.mq5 && test -f mql5/CISD_Slope_EA_v6.39_Final.mq5
```
If either file is missing in the current worktree, stop and report the missing repo-relative path. Do not create replacement files, do not switch to `mql5/AureusProvider.mq5`, and do not use absolute source/target paths as a workaround.
</prechecks>

<source_logic>
Relevant source slices already identified:
- `mql5/CISD_Slope_EA_v6.39_Final.mq5:3068-3342` contains `CheckSignalsAndDraw_Stateful`.
- DCA calls are inside closed-bar confirmation branches only:
  - Bullish: `s_bear_setup.active && close_i > s_bear_setup.priceLevel`, then `i == 1 && time_i > g_last_trade_signal_time`, then `DoDCA(1)`.
  - Bearish: `s_bull_setup.active && close_i < s_bull_setup.priceLevel`, then `i == 1 && time_i > g_last_trade_signal_time`, then `DoDCA(-1)`.
- Setup discovery to copy/adapt:
  - `g_h1_signalType == -1 && isBullish_i && wasBearish_ip1` sets `s_bull_setup`.
  - `g_h1_signalType == 1 && isBearish_i && wasBullish_ip1` sets `s_bear_setup`.
- Minimal state/dependencies visible in source:
  - `SetupInfo` struct at `mql5/CISD_Slope_EA_v6.39_Final.mq5:29-34`.
  - globals around `mql5/CISD_Slope_EA_v6.39_Final.mq5:395-400`: `g_h1_signalType`, `g_m5_signalType`, `g_h1_signalTime`, `g_ltf_scan_start_time`, `g_last_trade_signal_time`.
  - H1 signal scan setup around `mql5/CISD_Slope_EA_v6.39_Final.mq5:3831-3871` may be needed if target does not already compute these fields.
</source_logic>

<target_constraints>
- Scope is surgical: modify only `mql5/AureusProvider_v2.mq5`.
- Do not copy UI/drawing/panel/Telegram/alert/label/arrow/screenshot/strategy execution code from `CheckSignalsAndDraw_Stateful`.
- Preserve provider socket/JSON/ACK/NACK/streaming semantics and existing order terminal-event behavior.
- Existing `DoDCA` in target is around `mql5/AureusProvider_v2.mq5:1181-1430`; existing post-market-order call is around `mql5/AureusProvider_v2.mq5:1818-1819`.
</target_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Xác định blast radius và map dependency tối thiểu</name>
  <files>mql5/AureusProvider_v2.mq5, mql5/CISD_Slope_EA_v6.39_Final.mq5</files>
  <action>Đầu tiên chạy precheck trong current worktree: `test -f mql5/AureusProvider_v2.mq5 && test -f mql5/CISD_Slope_EA_v6.39_Final.mq5`. Nếu thiếu bất kỳ file nào, dừng và báo path thiếu; không tạo file, không checkout/switch sang file khác, không dùng `mql5/AureusProvider.mq5`. Sau precheck, trước khi sửa symbol nào, chạy GitNexus impact theo project rule cho các symbol dự kiến chạm: `DoDCA`, `ExecuteOpenOrder`, và tên helper mới nếu sẽ thêm helper. Nếu GitNexus không index được MQL5 symbols hoặc trả về không có symbol, ghi rõ fallback trong summary và dùng direct searches trong hai file nguồn/đích để map callers/usages của `DoDCA`, `ExecuteOpenOrder`, `OnTimer`, `ProcessIncomingCommands`, `CheckSignalsAndDraw_Stateful`. Báo blast radius ngắn gọn trước khi edit: direct callers, affected execution flows nếu có, risk level. Không bỏ qua HIGH/CRITICAL nếu GitNexus báo.</action>
  <verify>
    <automated>test -f mql5/AureusProvider_v2.mq5 && test -f mql5/CISD_Slope_EA_v6.39_Final.mq5</automated>
    <automated>npx gitnexus --help</automated>
    <automated>python - <<'PY'
from pathlib import Path
for p in [Path('mql5/AureusProvider_v2.mq5'), Path('mql5/CISD_Slope_EA_v6.39_Final.mq5')]:
    text = p.read_text(encoding='utf-8', errors='ignore')
    assert 'DoDCA' in text
print('fallback-search-inputs-ok')
PY</automated>
  </verify>
  <done>Executor đã xác nhận hai file repo-relative tồn tại trong current worktree, có blast radius hoặc documented fallback nếu GitNexus thiếu MQL5 coverage, và biết chính xác đoạn source cần copy/adapt trước khi sửa target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thêm helper provider-local cho CISD DCA gate chống duplicate</name>
  <files>mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - Khi có bearish setup active và nến đóng mới của current chart symbol phá lên priceLevel giống source, provider gọi existing `DoDCA(1)` đúng một lần cho qualifying setup/bar đó.
    - Khi có bullish setup active và nến đóng mới của current chart symbol phá xuống priceLevel giống source, provider gọi existing `DoDCA(-1)` đúng một lần cho qualifying setup/bar đó.
    - Nếu chưa tới `g_ltf_scan_start_time`, thiếu H1 signal, symbol không phải current chart symbol đang tính gate, hoặc `time_i <= g_last_trade_signal_time`, provider không gọi DCA.
    - Không gọi lặp lại mỗi tick cho cùng một setup/bar; chỉ gọi lại khi có new qualifying setup/bar làm condition transition true.
    - Không có code mới nào gửi Telegram, vẽ object/chart label/arrow/panel, hoặc thay đổi JSON ACK/NACK/ORDER events.
  </behavior>
  <action>Trước khi edit, chạy lại precheck trong current worktree: `test -f mql5/AureusProvider_v2.mq5 && test -f mql5/CISD_Slope_EA_v6.39_Final.mq5`. Nếu thiếu bất kỳ file nào, dừng; không tạo file và không chuyển sang `mql5/AureusProvider.mq5`. Trong `mql5/AureusProvider_v2.mq5`, thêm một helper provider-local có tên rõ ràng như `CheckDCAEntryConditionFromCISD()` hoặc tương đương. Helper này phải tự tính điều kiện vào DCA của `CheckSignalsAndDraw_Stateful` bằng state/data đã copy/adapt tối thiểu trong provider: `SetupInfo`, static bull/bear setup state, H1 signal type/time, LTF scan start, last trade signal time, start_bar/full-scan handling, loop trên closed bars, setup discovery, confirmation branch. Call site phải nằm trong timer/tick path hiện có phù hợp để chạy trên current chart symbol mà không block socket; call site chỉ gọi helper, còn helper chỉ gọi existing `DoDCA(1)` hoặc `DoDCA(-1)` khi condition transition true cho current chart symbol. Anti-duplicate bắt buộc: cập nhật/so sánh `g_last_trade_signal_time` hoặc provider-local equivalent theo closed bar/setup time để không gọi `DoDCA` lặp lại mỗi tick khi vẫn cùng setup/bar. Không thay đổi implementation của `DoDCA` ngoài incidental compile fix nếu thực sự cần; không thêm UI/drawing/Telegram/panel/alert/screenshot/message formatting; không copy `SignalAnalysis`, `TriggerCISDAlerts`, `AttemptTradeExecution`, candle structure analysis, range checklist; không đổi wire protocol, parse JSON, ACK/NACK, PushOrderOpened/Failed/Closed, candle/tick streaming.</action>
  <verify>
    <automated>test -f mql5/AureusProvider_v2.mq5 && test -f mql5/CISD_Slope_EA_v6.39_Final.mq5</automated>
    <automated>python - <<'PY'
from pathlib import Path
import subprocess
text = Path('mql5/AureusProvider_v2.mq5').read_text(encoding='utf-8', errors='ignore')
diff = subprocess.check_output(['git', 'diff', '--', 'mql5/AureusProvider_v2.mq5'], text=True, errors='ignore')
# Required gate symbols may already exist elsewhere, so validate the target file for presence and the diff for the new helper/callsite scope.
assert 'DoDCA(1)' in text, 'missing bullish DCA gate'
assert 'DoDCA(-1)' in text, 'missing bearish DCA gate'
assert ('CheckDCAEntryConditionFromCISD' in text or 'CISD' in text and 'DCA' in text), 'missing provider-local CISD DCA helper/call pattern'
assert ('CheckDCAEntryConditionFromCISD' in diff or ('CISD' in diff and 'DCA' in diff)), 'diff does not show new/changed CISD DCA helper or callsite'
# Forbidden source behaviors are checked only in newly added/changed lines, not across the whole target file.
added_changed = '\n'.join(line[1:] for line in diff.splitlines() if line.startswith('+') and not line.startswith('+++'))
for forbidden in ['DrawConfirmationLine', 'DrawConfirmationLabel', 'DrawSignalArrow', 'TriggerCISDAlerts', 'SendTelegram', 'AttemptTradeExecution', 'SignalAnalysis']:
    assert forbidden not in added_changed, f'unrelated source behavior copied in diff/helper/callsite: {forbidden}'
for required in ['SetupInfo', 'g_ltf_scan_start_time', 'g_last_trade_signal_time']:
    assert required in text, f'missing required DCA gate data: {required}'
print('dca-gate-diff-scoped-static-checks-ok')
PY</automated>
  </verify>
  <done>`mql5/AureusProvider_v2.mq5` có provider-local helper/call pattern tính CISD DCA entry-condition từ copied state/data tối thiểu, gọi existing `DoDCA(1/-1)` chỉ khi condition transition true cho current chart symbol, không gọi lặp mỗi tick cho cùng setup/bar, không copy behavior ngoài DCA condition, và không thay đổi provider protocol semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Compile MetaEditor và kiểm tra scope thay đổi</name>
  <files>mql5/AureusProvider_v2.mq5, mql5/AureusProvider_v2_compile.log</files>
  <action>Trước compile, chạy lại precheck trong current worktree: `test -f mql5/AureusProvider_v2.mq5 && test -f mql5/CISD_Slope_EA_v6.39_Final.mq5`. Nếu thiếu bất kỳ file nào, dừng; không tạo file và không chuyển sang `mql5/AureusProvider.mq5`. Compile target theo `mql5/Build_Rules.md` bằng MetaEditor64.exe, sửa đến khi 0 errors và 0 warnings, nhưng bắt buộc resolve absolute Windows path của current worktree tại execution time; không hardcode `D:\Aureus` vì executor có thể chạy trong worktree khác. Command Git Bash chuẩn: `WT_WIN=$(pwd -W) && cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"${WT_WIN}\mql5\AureusProvider_v2.mq5" /log:"${WT_WIN}\mql5\AureusProvider_v2_compile.log""`. PowerShell equivalent cũng được nếu dùng `Resolve-Path` cho current worktree. Sau compile, chạy GitNexus detect changes theo project rule; nếu GitNexus không map MQL5 changes, ghi fallback và dùng `git diff -- mql5/AureusProvider_v2.mq5` để xác nhận scope chỉ là provider DCA gate. Không commit trừ khi user/orchestrator yêu cầu riêng.</action>
  <verify>
    <automated>test -f mql5/AureusProvider_v2.mq5 && test -f mql5/CISD_Slope_EA_v6.39_Final.mq5</automated>
    <automated>WT_WIN=$(pwd -W) && cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"${WT_WIN}\mql5\AureusProvider_v2.mq5" /log:"${WT_WIN}\mql5\AureusProvider_v2_compile.log""</automated>
    <automated>python - <<'PY'
from pathlib import Path
log_path = Path.cwd() / 'mql5' / 'AureusProvider_v2_compile.log'
log = log_path.read_text(encoding='utf-8', errors='ignore')
low = log.lower()
assert ('0 error' in low or '0 errors' in low), log[-2000:]
assert ('0 warning' in low or '0 warnings' in low), log[-2000:]
print('metaeditor-compile-clean')
PY</automated>
  </verify>
  <done>MetaEditor compile log chứng minh 0 errors/0 warnings; detect_changes hoặc fallback diff xác nhận chỉ `mql5/AureusProvider_v2.mq5` bị thay đổi đúng scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway→Provider command socket | Untrusted JSON commands vẫn đi qua parser/ACK/NACK hiện có; plan không thay đổi boundary này. |
| Provider→Broker trade API | DCA gate có thể tạo thêm trade qua `DoDCA`; cần giữ mọi broker limit/volume/time/profit guards trong `DoDCA`. |
| Market data→DCA condition | OHLC/time series từ MT5 được dùng để quyết định DCA; cần tránh dùng bar chưa đóng sai cách và tránh duplicate trên cùng setup/bar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260430-ouw-01 | Tampering | `AureusProvider_v2.mq5::ProcessIncomingCommands` / socket JSON | accept | Scope không sửa command parsing hoặc ACK/NACK semantics; static/diff checks phải xác nhận không đổi protocol functions ngoài incidental compile fixes. |
| T-260430-ouw-02 | Denial of Service | New CISD DCA gate in timer path | mitigate | Helper phải dùng bounded scan giống source (`start_bar`, fallback 12 bars), chạy trên current chart symbol, chống duplicate bằng signal/bar time, và không block socket/streaming loop. |
| T-260430-ouw-03 | Elevation of Privilege | `DoDCA` trade execution | mitigate | Gate chỉ gọi existing `DoDCA(1/-1)` khi copied CISD condition transitions true; không bypass existing position count, net profit, time spacing, volume normalization, broker min/max checks. |
| T-260430-ouw-04 | Repudiation | ORDER events / logs | mitigate | Không thay đổi `PushOrderOpened`, `PushOrderFailed`, `PushOrderClosed`; compile/diff verification must preserve terminal event semantics. |
</threat_model>

<verification>
- Prechecks prove `mql5/AureusProvider_v2.mq5` and `mql5/CISD_Slope_EA_v6.39_Final.mq5` exist in the executor's current worktree before mapping, editing, and compile.
- Static checks prove BUY and SELL DCA gates exist in target, helper/call pattern is provider-local, and unrelated source UI/Telegram/drawing/signal execution code was not copied.
- MetaEditor compile command from `mql5/Build_Rules.md` completes with 0 errors and 0 warnings.
- GitNexus impact/detect_changes requirements are attempted; if MQL5 symbols are unavailable, fallback searches/diff are documented in summary.
</verification>

<success_criteria>
- `mql5/AureusProvider_v2.mq5` contains adapted CISD DCA entry-condition logic from `CheckSignalsAndDraw_Stateful`.
- Executor never creates replacement source/target files and never switches scope to `mql5/AureusProvider.mq5` if current-worktree prechecks fail.
- `DoDCA(1)` is reachable only from bullish LTF confirmation equivalent to source for current chart symbol and a new qualifying setup/bar.
- `DoDCA(-1)` is reachable only from bearish LTF confirmation equivalent to source for current chart symbol and a new qualifying setup/bar.
- No repeated `DoDCA` call occurs every tick without a new qualifying setup/bar.
- No UI/drawing/panel/Telegram/unrelated signal rendering behavior is copied.
- Provider socket/JSON/ACK/NACK/streaming behavior remains intact.
- `mql5/AureusProvider_v2_compile.log` in the executor current worktree shows 0 errors and 0 warnings; compile/log paths are resolved from the current worktree, not hardcoded to `D:\Aureus`.
</success_criteria>

<output>
After completion, create `.planning/quick/260430-ouw-b-sung-th-m-logic-th-c-hi-n-dodca-ha-m-c/260430-ouw-SUMMARY.md`.
</output>
