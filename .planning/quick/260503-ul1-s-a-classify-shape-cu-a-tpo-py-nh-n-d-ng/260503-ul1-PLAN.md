---
phase: quick-260503-ul1-s-a-classify-shape-cu-a-tpo-py-nh-n-d-ng
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/tpo.py
  - services/aureus-signal/tests/test_tpo_signal.py
autonomous: true
requirements:
  - QUICK-260503-UL1
must_haves:
  truths:
    - "_classify_shape trả D khi POC gần giữa candle và khoảng VAH/VAL quanh POC cân bằng theo phần trăm candle range."
    - "_classify_shape trả p khi POC nằm upper 1/3 candle."
    - "_classify_shape trả b khi POC nằm lower 1/3 candle."
    - "_classify_shape trả B khi có hai POC peak gần bằng nhau về định lượng và tách qua hai vùng candle."
    - "shape_scores_pct vẫn đủ keys D/B/p/b và tổng xấp xỉ 100 cho profile hợp lệ."
  artifacts:
    - path: "services/aureus-signal/engine/signals/tpo.py"
      provides: "Quy tắc classify shape D/P/b/B theo POC position, VA balance, dual POC"
      contains: "def _classify_shape"
    - path: "services/aureus-signal/tests/test_tpo_signal.py"
      provides: "Regression tests cho rule D/P/b/B"
      contains: "test_tpo_classify_shape_"
  key_links:
    - from: "services/aureus-signal/engine/signals/tpo.py::_build_tpo_block"
      to: "services/aureus-signal/engine/signals/tpo.py::_classify_shape"
      via: "shape, confidence_pct, scores = self._classify_shape(levels, counts, poc_idx)"
      pattern: "_classify_shape\(levels, counts, poc_idx\)"
    - from: "services/aureus-signal/tests/test_tpo_signal.py"
      to: "services/aureus-signal/engine/signals/tpo.py::TPOSignal._classify_shape"
      via: "_classify_fixture calls sig._classify_shape"
      pattern: "sig\._classify_shape"
---

<objective>
Sửa `_classify_shape` trong TPO signal để shape D/p/b/B bám đúng rule người dùng: D theo POC ở giữa và VAH/VAL cân bằng quanh POC, p/b theo POC upper/lower 1/3, B theo dual near-equal POC peaks tách vùng.

Purpose: loại bỏ classify dựa quá nhiều vào mass/tail hiện tại, làm shape phản ánh vị trí POC và cấu trúc peak rõ hơn.
Output: code classifier đã sửa và test regression chạy được.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/services/aureus-signal/engine/signals/tpo.py
@D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py

Relevant current interfaces:

```python
class TPOSignal(BaseSignal):
    def _build_tpo_block(self, session_df: pd.DataFrame, symbol: str = "") -> Optional[Dict[str, Any]]:
        # calls: shape, confidence_pct, scores = self._classify_shape(levels, counts, poc_idx)

    def _classify_shape(self, levels: List[float], counts: List[int], poc_idx: int) -> Tuple[str, float, Dict[str, float]]:
        # returns: (shape, confidence_pct, {"D": pct, "B": pct, "p": pct, "b": pct})
```

Assumptions executor must keep explicit:
- Candle range for classifier is `levels[0]` to `levels[-1]`; range percent can be computed by normalized index `idx / (n - 1)` because levels are price bins.
- Upper 1/3 means normalized POC position `>= 2/3`; lower 1/3 means `<= 1/3`; middle region means between 1/3 and 2/3.
- Existing public contract stays unchanged: return shape in `{"D", "B", "p", "b"}`, confidence 0-100, scores percent keys D/B/p/b.
- Must run GitNexus impact before editing `_classify_shape`, per project rule.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for requested TPO shape rules</name>
  <files>services/aureus-signal/tests/test_tpo_signal.py</files>
  <behavior>
    - D case: balanced profile with POC near middle returns `"D"`, D score highest, confidence meaningful.
    - p case: profile with POC in upper 1/3 returns `"p"` even if lower/upper mass heuristics would conflict.
    - b case: profile with POC in lower 1/3 returns `"b"`.
    - B case: profile with two separated near-equal peaks across two candle regions returns `"B"`; unequal or adjacent peaks must not become confident B.
  </behavior>
  <action>Before editing tests, run `gitnexus_impact({target: "TPOSignal._classify_shape", direction: "upstream"})` if MCP is available; if MCP unavailable, run `npx gitnexus impact TPOSignal._classify_shape --direction upstream` or record tool unavailability in SUMMARY. Add focused pytest cases near existing `_classify_fixture` tests. Do not add database tests; this quick task changes pure signal logic only. Keep helper signature unchanged unless needed for explicit `poc_idx` fixture. If helper is extended, preserve current callers.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && python -m pytest tests/test_tpo_signal.py -q</automated>
  </verify>
  <done>New tests fail against old classifier for at least one requested rule before code change, then pass after Task 2. Existing TPO tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework _classify_shape scoring around POC position, VA balance, and dual POC</name>
  <files>services/aureus-signal/engine/signals/tpo.py</files>
  <behavior>
    - Empty/zero profile keeps current safe output: shape D, confidence 0, zero scores.
    - D score uses normalized POC distance from candle middle plus VAH/VAL balance around POC by candle range percent. Compute value-area span from counts using existing `self.value_area_pct` target: include bins around POC by descending counts until target mass, derive `val_idx`/`vah_idx`, compare `(poc_idx - val_idx)/(n-1)` vs `(vah_idx - poc_idx)/(n-1)`.
    - p score is driven by POC in upper third (`poc_pos >= 2/3`), not by tail mass alone.
    - b score is driven by POC in lower third (`poc_pos <= 1/3`).
    - B score requires two local peaks with peak balance near equal, separation across regions, and valley between them lower than weaker peak; if peak ratio is not near-equal or separation too small, B must stay low.
  </behavior>
  <action>Modify only `_classify_shape` and any tiny local helper inside it if needed. Keep return type and score normalization contract. Prefer deterministic numeric thresholds visible in code: e.g. middle proximity, third boundaries, peak_balance, valley_depth, separation. Do not rename symbols. Do not change `_build_tpo_block`, `_calculate_distr`, or downstream context/Telegram code. Preserve confidence/data_quality cap behavior where compatible, but make best shape selection follow requested rules above. After edits, run `gitnexus_detect_changes()` or CLI equivalent before SUMMARY/commit handoff.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && python -m pytest tests/test_tpo_signal.py -q</automated>
  </verify>
  <done>`_classify_shape` returns D/p/b/B for requested synthetic cases, existing shape contract tests pass, no unrelated files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market data -> signal classifier | External price/count-derived data enters `_classify_shape`; output affects strategy context but does not execute orders directly here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260503-ul1-01 | T | `_classify_shape` counts/levels input | mitigate | Clamp `poc_idx`, keep empty/zero guards, normalize scores to bounded 0-100. |
| T-260503-ul1-02 | D | `_classify_shape` large counts arrays | accept | Existing profile builder caps levels; quick change must stay O(n^2) only for bounded peak candidates or better. |
| T-260503-ul1-03 | I | shape scores output | accept | No secrets/PII; output is derived market metadata. |
</threat_model>

<verification>
Run full focused test:

```bash
cd D:/Aureus/services/aureus-signal && python -m pytest tests/test_tpo_signal.py -q
```

Run GitNexus scope check before finishing:

```bash
npx gitnexus detect-changes
```

If command fails because CLI differs, use available GitNexus MCP `gitnexus_detect_changes()` and record result.
</verification>

<success_criteria>
- `services/aureus-signal/engine/signals/tpo.py::_classify_shape` implements requested D/p/b/B rules without changing public return contract.
- `services/aureus-signal/tests/test_tpo_signal.py` contains focused tests for D middle+balanced VA, p upper third, b lower third, B dual near-equal separated peaks.
- `python -m pytest tests/test_tpo_signal.py -q` passes from `D:/Aureus/services/aureus-signal`.
- GitNexus impact and detect-changes requirements are satisfied or explicitly recorded as unavailable.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260503-ul1-s-a-classify-shape-cu-a-tpo-py-nh-n-d-ng/260503-ul1-SUMMARY.md`.
</output>
