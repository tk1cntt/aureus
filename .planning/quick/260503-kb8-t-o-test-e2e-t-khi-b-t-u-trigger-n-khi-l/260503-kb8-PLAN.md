---
phase: quick-260503-kb8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py
autonomous: true
requirements:
  - QUICK-260503-KB8
must_haves:
  truths:
    - "E2E bắt đầu từ trigger/dispatch path, không gọi trực tiếp TradeJournalManager.on_strategy_match hoặc insert trực tiếp aureus_reasoning_entries."
    - "E2E dùng runtime Postgres thật qua AUREUS_DB_DSN và tạo record thật trong aureus_reasoning_entries."
    - "Record aureus_reasoning_entries được link tới aureus_trade_journal và aureus_trade_signal_snapshots sau ORDER_OPENED từ dispatch path."
  artifacts:
    - path: "services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py"
      provides: "Runtime DB E2E script proving trigger-to-reasoning-entry persistence"
      exports: ["run_e2e", "main"]
  key_links:
    - from: "services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py"
      to: "OrderDispatcher.dispatch_order"
      via: "script creates order with strategy_event then drives ACK and ORDER_OPENED through dispatcher wait responses"
      pattern: "dispatch_order"
    - from: "OrderDispatcher._prepare_journal_event"
      to: "TradeJournalManager.on_strategy_match"
      via: "dispatch success path persists triggered journal row from strategy_event before on_order_opened"
      pattern: "on_strategy_match"
    - from: "TradeJournalManager.on_order_opened"
      to: "aureus_reasoning_entries"
      via: "runtime DB update links trade_journal_id, signal_snapshot_id, generated reasoning_text"
      pattern: "aureus_reasoning_entries"
---

<objective>
Tạo E2E test chứng minh luồng bắt đầu từ trigger/dispatch path rồi lưu thành công record thật vào `aureus_reasoning_entries`.

Purpose: Bổ sung bằng chứng DB runtime cho luồng gần thực tế hơn script reuse cũ, tránh false positive do gọi trực tiếp journal hoặc insert thẳng Reasoning Bank.
Output: Một script E2E trong `services/aureus-trader/scripts/` chạy qua WSL `.venv`, dùng runtime Postgres, tự cleanup trace_id test.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/RUN_SERVICES.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260503-g9l-t-o-reasoning-text-sau-khi-aureus-trade-/260503-g9l-SUMMARY.md
@D:/Aureus/services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py
@D:/Aureus/services/aureus-trader/dispatcher.py
@D:/Aureus/services/aureus-trader/journal.py

Key existing contracts:
- `OrderDispatcher.dispatch_order(order: dict)` publishes order, waits ACK, waits final result, then calls `_prepare_journal_event()` and `journal.on_order_opened()` for `ORDER_OPENED`.
- `_prepare_journal_event()` calls `self.journal.on_strategy_match(strategy_payload)` when order contains `strategy_event`; this is trigger/journal path executor must use.
- Existing `verify_reasoning_bank_reuse_e2e.py` has reusable schema setup/cleanup pattern, DSN pattern, journal lifecycle assertions. New script may copy small helpers, but must start from dispatcher path instead of direct journal calls.
- Runtime commands must run through WSL per `RUN_SERVICES.md`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trigger-to-reasoning-entry DB E2E script</name>
  <files>services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py</files>
  <behavior>
    - Script opens real asyncpg pool using `AUREUS_DB_DSN` or config fallback.
    - Script creates unique `trace_id`, inserts only prerequisite `aureus_trades` row, then uses `OrderDispatcher.dispatch_order(order)` with `strategy_event` and fake ACK/ORDER_OPENED responses to drive trigger path.
    - Script must not call `TradeJournalManager.on_strategy_match()` or insert/update `aureus_reasoning_entries` directly; dispatcher owns that path.
    - Script queries joined `aureus_reasoning_entries`, `aureus_trade_journal`, `aureus_trade_signal_snapshots` and asserts one linked record with non-empty generated `reasoning_text`, non-null `trade_journal_id`, non-null `signal_snapshot_id`, expected strategy/symbol/direction/timeframe/snapshot data.
    - Script cleanup deletes rows by `trace_id` in reverse dependency order in `finally`.
  </behavior>
  <action>Create `services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py`. Use patterns from `verify_reasoning_bank_reuse_e2e.py` for `_ensure_schema`, `_cleanup`, `load_config`, asyncpg pool, and final PASS print. Add local fake Redis class with `publish()` capturing command payload; instantiate `OrderDispatcher(fake_redis, TraderConfig(ack_timeout=0.1, result_timeout=0.1, max_retries=0), journal_manager=TradeJournalManager(pool))`. Monkeypatch or assign dispatcher `_wait_for_response` to return `ACK` then `ORDER_OPENED` for same `cmd_id`. Build order containing `cmd_id`, `trace_id`, symbol/direction/order fields, `strategy_event` with `type=STRATEGY_MATCH`, nested `data`, `active_signals`, `context_filters`, and `signal_snapshot`. Call only `await dispatcher.dispatch_order(order)` as trigger path. Do not touch production files. Do not alter execute-order gating.</action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && AUREUS_DB_DSN=postgresql://aureus:aureus_password@localhost:5433/aureus ./.venv/bin/python services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py"</automated>
  </verify>
  <done>Command exits 0 and prints `PASS reasoning bank trigger-to-entry DB E2E trace_id=...`; database query assertions prove exactly one real `aureus_reasoning_entries` row linked to journal and signal snapshot from dispatcher-triggered path.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DB runtime and scope</name>
  <files>services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py</files>
  <action>Run services if DB unavailable using `wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && bash ./scripts/dev-service.sh"`, then rerun E2E command from Task 1. After script passes, run GitNexus change detection per project rule if tool exists; if unavailable, record exact failure in summary and use `git diff -- services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py` as fallback scope proof. Since only new test script touched, no production-symbol `gitnexus_impact` required.</action>
  <verify>
    <automated>npx gitnexus detect-changes --scope all --repo Aureus</automated>
    <automated>wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && AUREUS_DB_DSN=postgresql://aureus:aureus_password@localhost:5433/aureus ./.venv/bin/python services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py"</automated>
  </verify>
  <done>Detect-changes result or documented command failure exists in summary; E2E passes against runtime DB; no production behavior files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script -> runtime Postgres | Test script writes disposable rows to dev DB using provided DSN. |
| Fake MT5 response -> dispatcher | Test-controlled ACK/ORDER_OPENED data enters journal persistence path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-kb8-01 | Tampering | Runtime DB rows | mitigate | Use unique `trace_id` prefix and cleanup by trace_id in `finally`; only insert test rows. |
| T-kb8-02 | Repudiation | E2E evidence | mitigate | Print PASS with trace_id and assert joined DB facts before cleanup. |
| T-kb8-03 | Information Disclosure | DSN/secrets | mitigate | Read DSN from env/config; do not print password or connection string. |
| T-kb8-04 | Denial of Service | Dispatcher wait loop | mitigate | Use fake `_wait_for_response` with short timeouts and `max_retries=0`; no real Redis/MT5 dependency. |
</threat_model>

<verification>
- `wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && AUREUS_DB_DSN=postgresql://aureus:aureus_password@localhost:5433/aureus ./.venv/bin/python services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py"` passes.
- `npx gitnexus detect-changes --scope all --repo Aureus` run or exact tool failure recorded.
- `git diff -- services/aureus-trader/scripts/verify_reasoning_bank_trigger_to_entry_e2e.py` shows only new test script if GitNexus detect unavailable.
</verification>

<success_criteria>
- Single new E2E script exists under `services/aureus-trader/scripts/`.
- Test starts from `OrderDispatcher.dispatch_order()` with `strategy_event`, not direct journal call and not direct `aureus_reasoning_entries` insert.
- Runtime DB contains a real `aureus_reasoning_entries` row during assertions, linked to journal and snapshot IDs.
- Script cleans test rows after pass/fail.
- No production behavior changed.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260503-kb8-t-o-test-e2e-t-khi-b-t-u-trigger-n-khi-l/260503-kb8-SUMMARY.md`
</output>
