---
quick: 260501-720-fix-mt5-processincomingcommands-to-handl
type: quick
mode: execute
autonomous: true
files_modified:
  - mql5/AureusProvider_v2.mq5
requirements:
  - QUICK-260501-720
must_haves:
  truths:
    - "MT5 provider xử lý mọi JSON command hoàn chỉnh có trong một lần g_socket.Receive(), không dừng ở command đầu tiên."
    - "Hai hoặc nhiều OPEN_ORDER/CLOSE_ORDER/REQUEST_* JSON nối liền trong cùng raw buffer đều được dispatch theo thứ tự xuất hiện."
    - "JSON chưa đủ nếu có fragment cuối buffer không được xử lý sai; executor phải xác nhận Receive() hiện tại có trả frame hoàn chỉnh hay không trước khi chọn cách xử lý phần dư."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "ProcessIncomingCommands loop tách và dispatch từng JSON object trong raw socket read"
      contains: "ProcessIncomingCommands"
  key_links:
    - from: "g_socket.Receive()"
      to: "ExecuteOpenOrder/ExecuteCloseOrder/ExecuteRequestOrders/ExecuteTradeHistoryRequest/backfill handlers"
      via: "mỗi JSON object đã tách được dispatch qua handler hiện hữu"
      pattern: "Receive.*Extract.*ExecuteOpenOrder"
---

<objective>
Sửa nhanh `mql5/AureusProvider_v2.mq5` để `ProcessIncomingCommands()` không coi một socket read là đúng một command. Khi Gateway gửi nhiều JSON order/command nối liền trong cùng buffer, provider phải tách toàn bộ object JSON hoàn chỉnh và xử lý lần lượt, tránh bỏ sót lệnh gây `ACK_TIMEOUT_NO_PROVIDER_RESPONSE`.

Purpose: giữ thay đổi surgical trong provider MQL5, tái sử dụng các handler hiện có (`ExecuteOpenOrder`, `ExecuteCloseOrder`, request/backfill handlers) thay vì viết lại execution logic.
Output: một patch trong `mql5/AureusProvider_v2.mq5` và summary quick task.
</objective>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/mql5/AureusProvider_v2.mq5
@D:/Aureus/mql5/AureusProvider.mq5

Các điểm đã đọc:
- `AureusProvider_v2.mq5` hiện có `ProcessIncomingCommands()` đọc `string raw = g_socket.Receive();` rồi dispatch theo `StringFind(raw, ...)` và `return` ngay sau handler đầu tiên. Đây là nguyên nhân có thể bỏ các JSON object phía sau khi nhiều command bị concat trong cùng buffer.
- Legacy `AureusProvider.mq5` cũng xử lý một raw command/lần đọc, chỉ dùng để so sánh pattern backfill cũ; không sửa legacy trừ khi executor chứng minh cần thiết.
- Project instruction yêu cầu tiếng Việt, thay đổi surgical, và trước khi sửa symbol phải chạy GitNexus impact nếu symbol indexed. MQL5 symbol có thể không indexed; nếu GitNexus không tìm thấy `ProcessIncomingCommands`, ghi rõ fallback trong summary trước khi sửa.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phân tích impact và xác nhận contract socket/command hiện tại</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <action>Trước khi sửa `ProcessIncomingCommands`, chạy GitNexus impact cho symbol `ProcessIncomingCommands` theo project rule. Nếu GitNexus báo symbol MQL5 không indexed hoặc không có tool trong môi trường executor, ghi rõ fallback: symbol only called from `OnTimer()` trong file provider theo code hiện tại, risk chủ yếu là dispatch command MT5. Đọc nhanh `mql5/AureusSocketLib.mqh` để xác nhận `Receive()` trả raw string như thế nào: có delimiter newline hay chỉ bytes hiện có. Không thay đổi socket lib nếu không bắt buộc; mục tiêu là sửa parser/dispatcher trong provider.</action>
  <verify>
    <automated>cd D:/Aureus && test -f mql5/AureusProvider_v2.mq5 && test -f mql5/AureusSocketLib.mqh</automated>
  </verify>
  <done>Blast radius/fallback được ghi trong summary; executor biết rõ `Receive()` có framing gì trước khi implement; phạm vi sửa vẫn giới hạn ở `mql5/AureusProvider_v2.mq5` trừ khi phát hiện blocker thật sự.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tách raw buffer thành từng JSON object hoàn chỉnh và dispatch lần lượt</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - Buffer `{"type":"OPEN_ORDER",...}{"type":"OPEN_ORDER",...}` phải tạo 2 command object và gọi dispatch cho cả 2 theo thứ tự.
    - Buffer có whitespace/newline giữa các object vẫn xử lý đủ object.
    - Object có nested braces trong field array/object nếu có ở request/backfill không được cắt sớm; tách bằng brace depth và bỏ qua braces nằm trong JSON string.
    - Nếu buffer chỉ có 1 JSON object thì behavior cũ giữ nguyên.
  </behavior>
  <action>Refactor surgical trong `AureusProvider_v2.mq5`: tách phần dispatch hiện có của `ProcessIncomingCommands()` thành helper nội bộ, ví dụ `void ProcessSingleCommand(const string &raw)`, giữ nguyên thứ tự check command nhưng sửa lỗi cấu trúc hiện tại quanh `REQUEST_BACKFILL_COUNT`/`REQUEST_ORDERS` nếu cần để mỗi command độc lập được xử lý đúng. Thêm helper nhỏ để scan `raw` từ `g_socket.Receive()` bằng brace-depth: bắt đầu tại `{`, tăng/giảm depth với `{`/`}`, theo dõi `inString` và escape `\` để không tính braces trong chuỗi; khi depth về 0 thì lấy substring object và gọi `ProcessSingleCommand(commandRaw)`. Bỏ qua whitespace/noise ngoài object và log debug số command xử lý khi `InpDebugMode`. Không đổi execution semantics của `ExecuteOpenOrder`, dedup, ACK/NACK, DCA, hay order sizing.</action>
  <verify>
    <automated>cd D:/Aureus && python - <<'PY'
from pathlib import Path
p=Path('mql5/AureusProvider_v2.mq5')
s=p.read_text(encoding='utf-8', errors='ignore')
assert 'void ProcessIncomingCommands()' in s
assert 'ProcessSingleCommand' in s, 'expected dispatch helper'
assert s.count('g_socket.Receive()') >= 1
assert 'ExecuteOpenOrder(command' in s or 'ExecuteOpenOrder(raw)' in s
assert 'depth' in s and 'inString' in s, 'expected brace-depth JSON splitter'
print('static checks passed')
PY</automated>
  </verify>
  <done>`ProcessIncomingCommands()` xử lý tất cả JSON object hoàn chỉnh trong một raw socket read; mọi handler command hiện hữu vẫn được gọi qua cùng logic; không có thay đổi ngoài phạm vi provider parser/dispatcher.</done>
</task>

<task type="auto">
  <name>Task 3: Verify compile/static và ghi summary quick task</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5, D:/Aureus/.planning/quick/260501-720-fix-mt5-processincomingcommands-to-handl/260501-720-SUMMARY.md</files>
  <action>Chạy kiểm tra phù hợp môi trường: nếu có MetaEditor/MT5 compiler CLI thì compile `AureusProvider_v2.mq5`; nếu không có compiler trong môi trường, chạy static checks ở Task 2 và ghi rõ compile thủ công cần thực hiện trong MT5. Sau thay đổi, chạy `git diff -- mql5/AureusProvider_v2.mq5` để tự review chỉ có parser/dispatcher đổi. Nếu có GitNexus tool, chạy `gitnexus_detect_changes()` trước khi kết thúc; nếu không có, ghi fallback `git diff --stat`/manual scope check vào summary.</action>
  <verify>
    <automated>cd D:/Aureus && git diff --check -- mql5/AureusProvider_v2.mq5 && git diff -- mql5/AureusProvider_v2.mq5 --stat</automated>
  </verify>
  <done>Không có whitespace diff error; summary nêu rõ các command concat đã được xử lý ra sao, impact/fallback GitNexus, compile/static verification, và đường dẫn file đã sửa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway TCP -> MT5 provider | `g_socket.Receive()` là input mạng; provider phải không bỏ sót command và không parse nhầm object concat. |
| Parsed command -> trade execution | JSON command sau khi tách đi vào ACK/order side effects qua handler hiện hữu. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260501-720-01 | Tampering | ProcessIncomingCommands JSON splitter | mitigate | Tách object bằng brace-depth có trạng thái string/escape để tránh cắt sai payload có braces trong chuỗi. |
| T-260501-720-02 | Denial of Service | ProcessIncomingCommands loop | mitigate | Chỉ xử lý object hoàn chỉnh trong raw hiện tại; không thêm vòng lặp vô hạn trên socket; scan tuyến tính theo StringLen(raw). |
| T-260501-720-03 | Repudiation | ACK/result dispatch | mitigate | Giữ nguyên `cmd_id`, dedup, ACK/NACK trong handler hiện hữu; không bypass `ExecuteOpenOrder`/`ExecuteCloseOrder`. |
</threat_model>

<verification>
- Static: `git diff --check -- mql5/AureusProvider_v2.mq5` pass.
- Static parser evidence: file có helper dispatch single command và splitter brace-depth/string-aware.
- Compile: ưu tiên compile trong MetaEditor nếu môi trường executor có; nếu không có, ghi rõ manual compile required trong summary.
</verification>

<success_criteria>
- Một socket read chứa nhiều JSON command nối liền không còn chỉ xử lý command đầu tiên.
- Handler command hiện hữu tiếp tục chịu trách nhiệm ACK/NACK/order result, nên không phá idempotency và terminal event gate.
- Thay đổi chỉ chạm `mql5/AureusProvider_v2.mq5` và summary quick task.
</success_criteria>

<output>
Sau completion, tạo `D:/Aureus/.planning/quick/260501-720-fix-mt5-processincomingcommands-to-handl/260501-720-SUMMARY.md`.
</output>
