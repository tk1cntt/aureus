---
phase: quick-260520-9kx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
  - services/aureus-signal/**/*.py
autonomous: true
requirements:
  - QUICK-260520-9KX
must_haves:
  truths:
    - "Executor identifies exact git commit/diff and exact changed path that caused ORDER_CLOSED to lose cmd_id/trace_id after MT5 rebuild."
    - "Executor proves root cause with runtime logs and DB evidence before editing."
    - "ORDER_OPENED ticket/cmd_id linkage survives until ORDER_CLOSED or correct fill event emits usable strategy context; no random fallback added."
    - "Notifier no longer skips valid ORDER_CLOSED events because trace_id/cmd_id was dropped by regression path."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "MT5 event emission and pending/position mapping fix if regression is in provider"
    - path: "services/aureus-signal"
      provides: "Backend journal/notifier mapping fix if regression is in Python services"
    - path: ".planning/quick/260520-9kx-ph-n-ti-ch-la-i-log-cu-a-git-va-review-x/260520-9kx-SUMMARY.md"
      provides: "Evidence, culprit commit, fix summary, verification output"
  key_links:
    - from: "ORDER_OPENED cmd_id/ticket log"
      to: "ORDER_CLOSED event payload"
      via: "same MT5 ticket must retain resolvable cmd_id/trace_id or documented intended linkage"
      pattern: "ORDER_OPENED.*cmd_id=.*ticket=.* then ORDER_CLOSED.*ticket=.*(cmd_id|trace_id)"
    - from: "ORDER_CLOSED event"
      to: "notifier strategy context"
      via: "journal/backend resolver must find existing order context without guess fallback"
      pattern: "skip ORDER_CLOSED unresolved strategy context"
---

# Objective

Fix regression where ORDER_CLOSED arrives without cmd_id/trace_id after MT5 rebuild, causing notifier to skip close notification and journal today to have no FILLED/CLOSED rows.

Purpose: user reports previous build worked; must inspect git history/diff and exact runtime evidence, not guess or add broad fallback.

Output: surgical fix plus evidence trail naming culprit commit/feature, blast radius, and verification.

# Execution Context

- `D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md`
- `D:/Aureus/.claude/get-shit-done/templates/summary.md`
- `D:/Aureus/CLAUDE.md`
- `D:/Aureus/.planning/STATE.md`
- `D:/Aureus/.planning/quick/260519-w3p-ph-n-ti-ch-la-i-file-mql5-aureusprovider/`

# Context

Observed evidence from current conversation:

- Services all Up, gateway healthy.
- Journal today has 0 rows; `aureus_trades` today all SENT (161), no FILLED/CLOSED.
- Trader logs show `ORDER_OPENED` with cmd_id/ticket, then later `ORDER_CLOSED` without cmd_id/trace_id.
- Example: `ORDER_OPENED cmd_id=ord-a6f81ac43dfe ticket=1657600189` then `Event without cmd_id type=ORDER_CLOSED symbol=BTCUSD ticket=1657600189 trace_id= pending_count=0`.
- Notifier logs skip `ORDER_CLOSED` unresolved strategy context because trace_id blank.
- No `ORDER_FILLED` in last 3h.
- User says MT5 was rebuilt and requires no guessing/no random fallback; inspect git log/diff and exact regression.

Recent likely related commits from STATE/git history:

- `add3910 fix(260519-w3p): preserve pending fill mapping when history order type is unavailable`
- `4c62426 docs(quick-260518-9yd): Move SL safety`
- `eb164e8 fix(260518-9yd): guard MovePositionsSL per ticket`
- Prior pending-order lifecycle commits: `5ccf0eb`, `cb42dee`, `c43467d`, `2a79c31`.

Hard rules:

- Do not edit before exact failing path and culprit diff are identified.
- Do not add fallback based on symbol/ticket/strategy guesses unless evidence proves it restores existing intended invariant.
- Before modifying any function/class/method, run `gitnexus_impact({target: "SYMBOL", direction: "upstream"})` and record direct callers, affected processes, risk.
- Before commit-ready, run `gitnexus_detect_changes()` and verify affected symbols/flows match expected scope.
- If fix touches DB/journal backend, run DB E2E with real database and prove data created/updated per `D:/Aureus/CLAUDE.md`.
- If command fails, read `D:/Aureus/RUN_SERVICES.md` before retrying service commands.

# Tasks

## Task 1: Prove regression source from git diff plus runtime path

Type: auto

Files:

- `mql5/AureusProvider_v2.mq5`
- `services/aureus-signal/**/*.py`
- `D:/Aureus/.planning/quick/260519-w3p-ph-n-ti-ch-la-i-file-mql5-aureusprovider/*`

Action:

1. Inspect git history before editing:
   - `git log --oneline --decorate -- mql5/AureusProvider_v2.mq5 services/aureus-signal`
   - `git show --stat add3910`
   - `git show add3910 -- mql5/AureusProvider_v2.mq5 services/aureus-signal`
   - compare against last known working candidate before MT5 rebuild using `git diff <known-good>..HEAD -- mql5/AureusProvider_v2.mq5 services/aureus-signal`.
2. Inspect prior quick task output under `D:/Aureus/.planning/quick/260519-w3p-ph-n-ti-ch-la-i-file-mql5-aureusprovider/` and extract what was changed, why, and what was verified.
3. Use GitNexus for debugging navigation before code assumptions:
   - `gitnexus_query({query: "ORDER_CLOSED cmd_id trace_id pending fill mapping notifier unresolved strategy context"})`
   - `gitnexus_context({name: "suspect symbol from query"})` for each suspect entry point.
4. Correlate runtime evidence with source path:
   - Locate producer of `ORDER_OPENED`, producer of `ORDER_CLOSED`, pending mapping storage/removal, backend event resolver, journal update, notifier skip branch.
   - Use logs/DB evidence from running services to prove where cmd_id/trace_id is lost: provider event emit vs gateway/trader parse vs journal/notifier resolver.
5. Stop and report if evidence shows multiple plausible root causes with no decisive proof. Do not edit until one exact root cause is proven.

Verify:

- Automated: `git log --oneline -- mql5/AureusProvider_v2.mq5 services/aureus-signal`
- Automated: `git show add3910 -- mql5/AureusProvider_v2.mq5 services/aureus-signal`
- Automated: run relevant log/DB inspection commands that reproduce stated evidence, then capture exact lines showing `ORDER_OPENED` ticket/cmd_id and later `ORDER_CLOSED` same ticket without cmd_id/trace_id.

Done:

- Culprit commit/diff hunk and exact function/path are named.
- Evidence chain shows where data existed and where it vanished.
- No code has been edited before this proof exists.

## Task 2: Apply surgical invariant-restoring fix with GitNexus impact guard

Type: auto

Files:

- `mql5/AureusProvider_v2.mq5`
- `services/aureus-signal/**/*.py`

Action:

1. Before editing each selected symbol, run `gitnexus_impact({target: "<symbol>", direction: "upstream"})`. Record blast radius: direct callers, affected processes, risk level. If HIGH/CRITICAL, stop and report before editing.
2. Fix only proven failing path:
   - If regression is in `mql5/AureusProvider_v2.mq5`, restore intended mapping lifetime so close/fill event for same MT5 ticket emits the existing cmd_id/trace_id context. Do not invent context from unrelated orders.
   - If regression is in backend, restore resolver to use existing persisted invariant only when exact key match exists, such as ticket/pending_order_id/cmd_id already recorded by ORDER_OPENED or pending-fill mapping. Do not add symbol/time/strategy fallback.
   - Preserve market order path and pending order path semantics separately.
   - Keep edits minimal; no refactor, no broad logging spam, no unrelated formatting.
3. Add/adjust focused automated test if suitable symbol is Python. If MQL5-only, add command-level verification/build and log assertion rather than Python unit fake that misses provider behavior.

Verify:

- Automated: `npx gitnexus analyze` only if GitNexus warns index stale.
- Automated: relevant unit test command for touched Python module if backend touched.
- Automated: MQL5 build command per `D:/Aureus/mql5/Build_Rules.md` if `mql5/AureusProvider_v2.mq5` touched.
- Automated: targeted source/log assertion proves no new broad fallback exists: search changed diff for symbol/time fallback logic and explain why none was added or why exact-key invariant restoration is valid.

Done:

- Fix touches only culprit path.
- Impact output recorded for every modified symbol.
- Market path and pending path still compile/test.
- No random fallback or speculative behavior added.

## Task 3: End-to-end verification with logs, DB, and change-scope guard

Type: auto

Files:

- `D:/Aureus/.planning/quick/260520-9kx-ph-n-ti-ch-la-i-log-cu-a-git-va-review-x/260520-9kx-SUMMARY.md`

Action:

1. Run service/build verification needed by touched area. If any service command fails, read `D:/Aureus/RUN_SERVICES.md` and retry using documented command.
2. Reproduce or simulate exact lifecycle with evidence:
   - `ORDER_OPENED` has cmd_id/ticket.
   - Later close/fill event for same ticket has trace_id/cmd_id or backend resolves it from exact persisted key.
   - Notifier no longer logs skip for valid `ORDER_CLOSED` due to blank strategy context.
   - Journal/trade tables move beyond SENT when lifecycle event is valid.
3. If fix touches DB/journal backend, run real DB E2E: create/update trade lifecycle data and verify rows in `aureus_trade_journal` and/or `aureus_trades` changed correctly.
4. Run `gitnexus_detect_changes()` before commit-ready and confirm only expected symbols/execution flows changed.
5. Create summary with: culprit commit, culprit hunk, failing invariant, fix, impact blast radius, verification commands/results, DB E2E result if applicable, and remaining risk.

Verify:

- Automated: `gitnexus_detect_changes()`
- Automated: DB query showing corrected lifecycle state if backend DB touched.
- Automated: log query showing no `skip ORDER_CLOSED unresolved strategy context` for tested valid close event.

Done:

- Runtime evidence proves valid close/fill event resolves strategy context.
- DB state verified if backend touched.
- GitNexus change detection matches intended scope.
- Summary exists at `D:/Aureus/.planning/quick/260520-9kx-ph-n-ti-ch-la-i-log-cu-a-git-va-review-x/260520-9kx-SUMMARY.md`.

# Threat Model

## Trust Boundaries

| Boundary | Description |
|---|---|
| MT5 provider to gateway/trader | MT5 emits order lifecycle events consumed by backend; malformed/missing ids can corrupt lifecycle. |
| Backend event resolver to DB | Event payload updates journal/trade rows; wrong matching can attach close to wrong strategy. |
| DB to notifier | Notifier trusts resolved strategy context for Telegram close message. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260520-9KX-01 | Tampering | ORDER_CLOSED resolver | mitigate | Use only exact existing keys such as ticket/pending_order_id/cmd_id/trace_id proven by git/runtime evidence; no symbol/time fallback. |
| T-260520-9KX-02 | Repudiation | lifecycle logs | mitigate | Keep command id/ticket/trace id visible in verification logs so open-to-close chain is auditable. |
| T-260520-9KX-03 | Information disclosure | logs/summary | accept | Logs contain trading ids but no secret credentials; do not paste env secrets in summary. |
| T-260520-9KX-04 | Denial of service | provider/backend lifecycle path | mitigate | Surgical fix only; preserve existing event flow and avoid blocking retries or broad DB scans. |

# Verification

Overall checks:

- Culprit commit and exact hunk identified before editing.
- GitNexus impact run before modifying any symbol.
- MQL5 build passes if provider touched.
- Python tests pass if backend touched.
- Real DB E2E passes if journal/backend DB touched.
- `gitnexus_detect_changes()` confirms expected scope before commit-ready.

# Success Criteria

- Valid order lifecycle no longer gets stuck as SENT only when close/fill event arrives.
- `ORDER_CLOSED` for ticket previously opened with cmd_id resolves strategy context through exact intended invariant.
- Notifier sends/permits close handling instead of skipping valid `ORDER_CLOSED` because trace_id blank.
- No broad fallback, no guessed matching, no unrelated refactor.

# Output

After completion, create `D:/Aureus/.planning/quick/260520-9kx-ph-n-ti-ch-la-i-log-cu-a-git-va-review-x/260520-9kx-SUMMARY.md`.
