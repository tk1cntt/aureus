---
phase: quick-260424-fo9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
  - services/aureus-trader/journal.py
autonomous: true
requirements:
  - QUICK-260424-FO9
must_haves:
  truths:
    - "ORDER_OPENED không còn gây NotNullViolationError cho cột signal_schema_version khi ghi aureus_trade_signal_snapshots."
    - "Giá trị signal_schema_version được persist ổn định (lấy từ event nếu có, fallback mặc định nếu thiếu)."
    - "Fix không làm hỏng flow persist evaluation/snapshot hiện có."
  artifacts:
    - path: "services/aureus-trader/tests/test_signal_snapshot_pipeline.py"
      provides: "TDD RED->GREEN tái hiện và chặn hồi quy lỗi NotNullViolation signal_schema_version"
    - path: "services/aureus-trader/journal.py"
      provides: "Logic persist signal_schema_version vào snapshot insert với fallback an toàn"
  key_links:
    - from: "services/aureus-trader/journal.py::on_order_opened"
      to: "aureus_trade_signal_snapshots.signal_schema_version"
      via: "INSERT snapshot khi ORDER_OPENED"
      pattern: "INSERT INTO aureus_trade_signal_snapshots"
---

<objective>
Fix lỗi runtime `NotNullViolationError: signal_schema_version` tại bước ghi `aureus_trade_signal_snapshots` trong luồng `on_order_opened`.

Purpose: Khôi phục tính toàn vẹn dữ liệu snapshot và ngăn crash runtime do vi phạm NOT NULL.
Output: TDD test tái hiện lỗi + patch tối thiểu trong journal để luôn ghi được `signal_schema_version` đúng contract.
</objective>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@D:/Aureus/services/aureus-trader/journal.py
@D:/Aureus/services/aureus-trader/dispatcher.py
@D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py
@D:/Aureus/RUN_SERVICES.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — tái hiện lỗi NotNullViolation cho signal_schema_version</name>
  <files>services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <behavior>
    - Test 1: ORDER_OPENED thiếu `signal_schema_version` vẫn phải persist snapshot thành công (không nổ NotNullViolation).
    - Test 2: ORDER_OPENED có `signal_schema_version` thì giá trị đó phải đi vào câu INSERT snapshot.
  </behavior>
  <action>Thêm test focused vào `on_order_opened` theo style test hiện có để tái hiện bug (RED): case thiếu `signal_schema_version` làm fail tại snapshot insert; và case có giá trị thì assertion đúng vị trí insert args/query.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "signal_schema_version" -x</automated>
  </verify>
  <done>Có test RED tái hiện đúng lỗi runtime `NotNullViolationError` liên quan `signal_schema_version`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — patch tối thiểu journal snapshot insert để luôn có signal_schema_version</name>
  <files>services/aureus-trader/journal.py</files>
  <behavior>
    - `on_order_opened` lấy `signal_schema_version` từ event nếu có.
    - Nếu thiếu/null/rỗng, dùng fallback mặc định ổn định (ví dụ `sig-v2.0.0`) trước khi INSERT snapshot.
    - Không mở rộng scope sang logic không liên quan.
  </behavior>
  <action>Chỉnh đúng điểm INSERT `aureus_trade_signal_snapshots`: bổ sung cột/value `signal_schema_version` và normalize fallback ngay trong `on_order_opened`; giữ nguyên hành vi các field snapshot/evaluation khác.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "signal_schema_version" -x</automated>
  </verify>
  <done>Test RED chuyển GREEN; snapshot insert không còn đường đi tới giá trị null cho `signal_schema_version`.</done>
</task>

<task type="auto">
  <name>Task 3: Regression + verify runtime DB e2e theo rule dự án</name>
  <files>services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <action>Chạy regression test trader liên quan snapshot/journal; sau đó verify runtime với DB thật bằng cách restart service trader trong WSL theo RUN_SERVICES.md, quan sát log để xác nhận không còn NotNullViolation cho signal_schema_version.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py services/aureus-trader/tests/test_journal.py -x</automated>
  </verify>
  <done>Regression pass; verify e2e với DB thật xác nhận tạo data snapshot thành công và `signal_schema_version` không null; log runtime không còn NotNullViolationError tương ứng.</done>
</task>

</tasks>
