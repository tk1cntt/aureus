---
phase: quick-260430-oa4-copy-dodca-mql5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
autonomous: true
requirements:
  - QUICK-260430-OA4
must_haves:
  truths:
    - "AureusProvider_v2.mq5 contains DoDCA logic copied from CISD_Slope_EA_v6.39_Final.mq5 with the dependent variables/functions needed to compile and run it."
    - "DCA logic can be invoked only from existing provider order/position flow without changing unrelated gateway streaming behavior."
    - "MetaEditor compile for AureusProvider_v2.mq5 finishes with 0 errors and 0 warnings."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Target provider with DoDCA and required dependency helpers/variables"
      contains: "void DoDCA(int order_type_signal)"
    - path: "mql5/AureusProvider_v2_compile.log"
      provides: "MetaEditor build log proving 0 errors and 0 warnings"
      contains: "0 error(s), 0 warning(s)"
  key_links:
    - from: "mql5/AureusProvider_v2.mq5::DoDCA"
      to: "mql5/CISD_Slope_EA_v6.39_Final.mq5::DoDCA"
      via: "copied logic with provider-safe dependency adaptation"
      pattern: "void DoDCA\\(int order_type_signal\\)"
    - from: "mql5/AureusProvider_v2.mq5::DoDCA"
      to: "CTrade trade"
      via: "trade.Buy/trade.Sell/trade.PositionModify"
      pattern: "trade\\.(Buy|Sell|PositionModify)"
---

<objective>
Copy hàm `DoDCA` từ `mql5/CISD_Slope_EA_v6.39_Final.mq5` sang `mql5/AureusProvider_v2.mq5`, đồng thời bổ sung đúng các biến phụ thuộc và helper xử lý giá trị biến phụ thuộc cần thiết để provider compile sạch và không kéo theo refactor ngoài phạm vi.

Purpose: Khôi phục DCA position-management logic trong provider target mà trước đó đang có comment cho biết helper CISD đã bị remove vì thiếu dependency.
Output: `mql5/AureusProvider_v2.mq5` có `DoDCA` và dependency tối thiểu, build MetaEditor 0 errors/0 warnings.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@mql5/Build_Rules.md
@mql5/CISD_Slope_EA_v6.39_Final.mq5
@mql5/AureusProvider_v2.mq5

<interfaces>
Source contracts observed in `mql5/CISD_Slope_EA_v6.39_Final.mq5`:
```mql5
struct PositionInfo
  {
   ulong             ticket;
   long              open_time;
   double            open_price;
  };

double commission_per_lot = 8.0;
int max_loss_amount = 100;
int buffer_profit = 5;

void DoDCA(int order_type_signal);
```
Target already has:
```mql5
#include <Trade/Trade.mqh>
CTrade trade;
bool IsForexPair(string symbol);
PositionInfo; // already exists near the removed CISD helper comment
double commission_per_lot = 8.0;
int max_loss_amount = 100;
```

Build command from `mql5/Build_Rules.md` must keep the absolute Windows MetaEditor compile path, but executor must first verify the target exists in the current worktree/repo:
```bash
test -f mql5/AureusProvider_v2.mq5 && test -f mql5/CISD_Slope_EA_v6.39_Final.mq5
cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\AureusProvider_v2.mq5" /log:"D:\Aureus\mql5\AureusProvider_v2_compile.log""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map DoDCA dependency surface before editing</name>
  <files>mql5/CISD_Slope_EA_v6.39_Final.mq5, mql5/AureusProvider_v2.mq5</files>
  <action>Before editing, run a current-worktree file-existence precheck for `mql5/CISD_Slope_EA_v6.39_Final.mq5` and `mql5/AureusProvider_v2.mq5`; stop if either is missing because all later commands depend on these repo-relative paths. Before editing any MQL5 symbol, attempt GitNexus impact analysis for symbols that will be modified or introduced: `DoDCA`, `PositionInfo`, `commission_per_lot`, `max_loss_amount`, `trade`, and any chosen call-site symbol such as `ExecuteOpenOrder` or `OnTradeTransaction` if it is edited. Report direct callers/affected processes/risk level. GitNexus may not index MQL5 symbols; if any symbol is unavailable, explicitly note that and perform direct dependency/caller search in both MQL5 files for `DoDCA`, `PositionInfo`, `commission_per_lot`, `max_loss_amount`, `buffer_profit`, `SendTradeExecutionToTelegram`, `g_short_term`, `g_long_term`, and `GetSymbolTradeState`. Determine the minimal dependency set to copy/adapt. Avoid bringing UI/Telegram/signal-panel dependencies into provider unless DoDCA cannot compile without them; prefer removing or provider-safe logging for non-provider side effects from the copied function.</action>
  <verify>
    <automated>test -f mql5/CISD_Slope_EA_v6.39_Final.mq5 && test -f mql5/AureusProvider_v2.mq5</automated>
    <automated>grep -n "DoDCA\|PositionInfo\|commission_per_lot\|max_loss_amount\|buffer_profit\|SendTradeExecutionToTelegram\|g_short_term\|g_long_term\|GetSymbolTradeState" mql5/CISD_Slope_EA_v6.39_Final.mq5 mql5/AureusProvider_v2.mq5</automated>
  </verify>
  <done>Executor has verified both source and target exist in the current worktree/repo, has a concrete list of DoDCA dependencies, confirms which are already present in AureusProvider_v2, and documents any GitNexus unavailable symbols plus direct-search fallback before editing.</done>
</task>

<task type="auto">
  <name>Task 2: Copy DoDCA with provider-safe dependencies only</name>
  <files>mql5/AureusProvider_v2.mq5</files>
  <action>Re-run `test -f mql5/AureusProvider_v2.mq5` immediately before editing to ensure the target file exists in the executor's current worktree/repo. Insert `void DoDCA(int order_type_signal)` into `mql5/AureusProvider_v2.mq5` at the existing placeholder around the comment `The copied CISD position-management helpers were removed...`. Copy the source logic from `mql5/CISD_Slope_EA_v6.39_Final.mq5` lines around `DoDCA`, but adapt only what is necessary for this provider: keep use of existing `PositionInfo`, `trade`, `IsForexPair`, `commission_per_lot`, and `max_loss_amount`; add `buffer_profit` if missing; avoid duplicate declarations already present; replace source-only Telegram/panel dependency `SendTradeExecutionToTelegram(..., g_short_term, g_long_term)` with a provider-local `Print`/debug log so the target does not require CISD UI/signal globals. If wiring DoDCA into execution is required, add a narrowly scoped call only in an existing order/position-management path where same-symbol/same-direction open positions already exist; do not change JSON formats, socket behavior, ACK/NACK semantics, risk sizing, or unrelated order execution logic.</action>
  <verify>
    <automated>test -f mql5/AureusProvider_v2.mq5</automated>
    <automated>grep -n "void DoDCA\|buffer_profit\|SendTradeExecutionToTelegram\|g_short_term\|g_long_term" mql5/AureusProvider_v2.mq5</automated>
  </verify>
  <done>`mql5/AureusProvider_v2.mq5` exists in the current worktree/repo, contains exactly one `DoDCA` implementation, has all compile-required dependencies, has no unresolved CISD-only `SendTradeExecutionToTelegram/g_short_term/g_long_term` references, and avoids unrelated refactors.</done>
</task>

<task type="auto">
  <name>Task 3: Build and fix until MetaEditor is clean</name>
  <files>mql5/AureusProvider_v2.mq5, mql5/AureusProvider_v2_compile.log</files>
  <action>Before compiling, run a current-worktree precheck that `mql5/AureusProvider_v2.mq5` exists. Compile the target using the absolute Windows MetaEditor command from `mql5/Build_Rules.md`, adapted to `AureusProvider_v2.mq5`; keep the absolute compile/log paths because Build_Rules requires them. Analyze `mql5/AureusProvider_v2_compile.log` if produced in the repo, or `D:/Aureus/mql5/AureusProvider_v2_compile.log` when MetaEditor writes to the absolute Build_Rules path, and fix only errors/warnings introduced or exposed by the DoDCA copy. Repeat until the build log reports 0 errors and 0 warnings. Before finishing, run `gitnexus_detect_changes()` if available; if unavailable in the environment, state that and use `git diff -- mql5/AureusProvider_v2.mq5` to confirm only expected MQL5 target changes were made. Do not commit unless explicitly requested by the user.</action>
  <verify>
    <automated>test -f mql5/AureusProvider_v2.mq5</automated>
    <automated>cmd /c ""E:\Openclaw\MetaTrader5\MetaEditor64.exe" /compile:"D:\Aureus\mql5\AureusProvider_v2.mq5" /log:"D:\Aureus\mql5\AureusProvider_v2_compile.log""</automated>
    <automated>grep -n "0 error(s), 0 warning(s)" mql5/AureusProvider_v2_compile.log D:/Aureus/mql5/AureusProvider_v2_compile.log</automated>
  </verify>
  <done>Current-worktree target precheck passes, MetaEditor build log for `AureusProvider_v2.mq5` shows 0 errors and 0 warnings, and final change scope is limited to the planned target file plus compile log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway command → MT5 provider | Incoming order command/state can lead to trade operations in `AureusProvider_v2.mq5`. |
| MT5 account positions → DoDCA | Live position data is used to compute additional DCA orders and TP updates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260430-oa4-01 | Tampering | `DoDCA` order sizing and TP updates | mitigate | Copy source logic carefully but clamp `volume_to_open` with broker min/max/step before `trade.Buy/Sell`, preserving source safeguards. |
| T-260430-oa4-02 | Denial of Service | MetaEditor/runtime compile path | mitigate | Precheck `mql5/AureusProvider_v2.mq5` exists in the current worktree/repo, then build with MetaEditor and fix to 0 errors/0 warnings so provider cannot fail due to missing dependency symbols. |
| T-260430-oa4-03 | Elevation of Privilege | DCA triggering from provider flow | mitigate | If adding a call site, restrict it to same-symbol/same-direction existing position conditions; do not loosen existing symbol allow-list or trade-enabled checks. |
| T-260430-oa4-04 | Repudiation | DCA actions | mitigate | Preserve/source-add `Print` diagnostics with `[DoDCA]` prefix for condition checks, new order, TP modification, and failure reasons. |
</threat_model>

<verification>
Overall checks:
1. Current-worktree precheck passes: `test -f mql5/CISD_Slope_EA_v6.39_Final.mq5 && test -f mql5/AureusProvider_v2.mq5`.
2. `DoDCA` exists in `mql5/AureusProvider_v2.mq5`.
3. No unresolved CISD-only UI/Telegram globals are referenced by the target provider.
4. `mql5/AureusProvider_v2_compile.log` or `D:/Aureus/mql5/AureusProvider_v2_compile.log` contains `0 error(s), 0 warning(s)` after compiling with MetaEditor.
5. Diff scope is surgical: target MQL5 provider changes only, plus compile log if generated.
</verification>

<success_criteria>
- Required source and target files exist in the executor's current worktree/repo before dependency mapping, editing, and compiling.
- `mql5/AureusProvider_v2.mq5` includes provider-safe DoDCA copied/adapted from `mql5/CISD_Slope_EA_v6.39_Final.mq5`.
- Required DoDCA variables/helpers are present without duplicate-symbol compile failures.
- No unrelated refactor or socket/order JSON behavior changes are introduced.
- MetaEditor compile returns 0 errors and 0 warnings.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260430-oa4-copy-h-m-dodca-t-mql5-cisd-slope-ea-v6-3/260430-oa4-SUMMARY.md`.
</output>
