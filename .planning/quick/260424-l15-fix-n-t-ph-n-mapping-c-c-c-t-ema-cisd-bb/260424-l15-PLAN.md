---
phase: quick-260424-l15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/journal.py
  - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
autonomous: true
requirements:
  - QUICK-260424-L15
must_haves:
  truths:
    - "Khi journal lưu signal snapshot cho ORDER_OPENED, các cột ema_*, cisd_*, bb_* trong DB đều được map đầy đủ thay vì null ngoài ý muốn."
    - "E2E test với DB thật xác nhận insert thành công và dữ liệu đọc lại từ DB khớp mapping kỳ vọng cho ema_*, cisd_*, bb_*"
    - "Sau restart service, dữ liệu snapshot mới phát sinh trong DB thực tế vẫn giữ đầy đủ các cột ema_*, cisd_*, bb_*"
  artifacts:
    - path: "services/aureus-trader/journal.py"
      provides: "Logic mapping snapshot -> payload insert cho các nhóm cột ema_*, cisd_*, bb_*"
    - path: "services/aureus-trader/tests/test_signal_snapshot_pipeline.py"
      provides: "Bộ test TDD + E2E DB thật để verify insert/readback các cột ema_*, cisd_*, bb_*"
    - path: "RUN_SERVICES.md"
      provides: "Quy trình restart service chuẩn để verify runtime sau fix"
  key_links:
    - from: "services/aureus-trader/journal.py::on_order_opened"
      to: "aureus_trade_signal_snapshots"
      via: "insert snapshot payload đã map đủ ema_*/cisd_*/bb_*"
      pattern: "aureus_trade_signal_snapshots|signal_snapshot"
    - from: "services/aureus-trader/tests/test_signal_snapshot_pipeline.py"
      to: "Timescale/Postgres test database"
      via: "E2E insert + select verify readback"
      pattern: "INSERT|SELECT|ema_|cisd_|bb_"
---

<objective>
Fix nốt mapping signal snapshot để các cột `ema_*`, `cisd_*`, `bb_*` được lưu đầy đủ vào DB.

Purpose: Loại bỏ tình trạng snapshot thiếu dữ liệu chỉ báo, đảm bảo dữ liệu đánh giá/đối soát sau này tin cậy.
Output: Sửa mapping tại journal + test E2E DB thật xác nhận lưu thành công + quy trình restart service và kiểm tra DB thực tế sau fix.
</objective>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/services/aureus-trader/journal.py
@D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py
@D:/Aureus/RUN_SERVICES.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN cho mapping ema_*, cisd_*, bb_* trong pipeline snapshot</name>
  <files>services/aureus-trader/tests/test_signal_snapshot_pipeline.py, services/aureus-trader/journal.py</files>
  <behavior>
    - Test 1: Snapshot đầu vào có đủ field EMA/CISD/BB thì payload ghi DB phải chứa đầy đủ giá trị tương ứng, không rơi null sai.
    - Test 2: Mapping đúng tên cột DB cho từng nhóm ema_*, cisd_*, bb_* theo schema hiện hành.
    - Test 3: Không làm regress các field snapshot khác đã hoạt động trước đó.
  </behavior>
  <action>Viết test tái hiện lỗi mapping thiếu dữ liệu cho các cột ema_*, cisd_*, bb_* (RED), sau đó sửa tối thiểu trong `journal.py` để map đúng và đủ trước khi insert (GREEN). Chỉ chạm phần mapping liên quan, không mở rộng refactor ngoài phạm vi quick task.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "ema or cisd or bb" -x</automated>
  </verify>
  <done>Test mapping mới pass, chứng minh payload insert đã mang đủ dữ liệu ema_*, cisd_*, bb_*.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E với DB thật để xác nhận insert/readback thành công</name>
  <files>services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <behavior>
    - E2E test kết nối DB thật, chạy flow tạo snapshot và insert vào `aureus_trade_signal_snapshots`.
    - Query lại bản ghi vừa insert và assert tất cả cột ema_*, cisd_*, bb_* có dữ liệu đúng theo input test.
    - Test fail nếu bất kỳ cột mục tiêu bị null sai hoặc map lệch giá trị.
  </behavior>
  <action>Tạo/hoàn thiện case E2E dùng DB thật theo rule trong `CLAUDE.md` cho thay đổi liên quan database. Test cần tự xác nhận cả insert thành công lẫn dữ liệu readback đúng ở các cột ema_*, cisd_*, bb_*.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "e2e and snapshot and db" -x</automated>
  </verify>
  <done>Có bằng chứng tự động rằng DB thật nhận đúng dữ liệu cho toàn bộ cột ema_*, cisd_*, bb_* của bản ghi test.</done>
</task>

<task type="auto">
  <name>Task 3: Restart service và kiểm tra DB thực tế sau fix</name>
  <files>services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <action>Thực hiện restart service trader theo hướng dẫn trong `RUN_SERVICES.md`, chạy lại flow sinh snapshot thực tế, rồi query DB để kiểm tra bản ghi mới có dữ liệu đầy đủ cho các cột ema_*, cisd_*, bb_*. Giữ kiểm tra tập trung đúng phạm vi quick task.</action>
  <verify>
    <automated>cd /d/Aureus && (docker compose -f docker-compose.dev.yml restart aureus-trader || docker-compose -f docker-compose.dev.yml restart aureus-trader) && pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "runtime and snapshot and db" -x</automated>
  </verify>
  <done>Sau restart, kiểm chứng tự động xác nhận bản ghi snapshot mới trong DB thực tế vẫn map đủ ema_*, cisd_*, bb_*.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trader event payload -> journal mapping | Input snapshot có thể thiếu/khác key làm lệch mapping sang cột DB |
| Journal insert -> PostgreSQL/TimescaleDB | Nếu map sai sẽ ghi null/nhầm cột, gây mất toàn vẹn dữ liệu đánh giá |
| Runtime service -> production-like DB state | Restart/runtime có thể bộc lộ khác biệt so với unit test nếu chưa verify DB thật |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260424-l15-01 | T | services/aureus-trader/journal.py | mitigate | Chuẩn hóa mapping ema_*/cisd_*/bb_* đúng key nguồn và đích trước khi insert |
| T-quick-260424-l15-02 | R | services/aureus-trader/tests/test_signal_snapshot_pipeline.py | mitigate | E2E DB thật bắt buộc insert+readback để tạo bằng chứng dữ liệu đã ghi đúng |
| T-quick-260424-l15-03 | D | services/aureus-trader runtime flow | accept | Giới hạn quick scope, kiểm soát bằng restart service + verify DB bản ghi mới sau fix |
</threat_model>

<verification>
- Focused mapping tests: `pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "ema or cisd or bb" -x`
- E2E DB thật: `pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "e2e and snapshot and db" -x`
- Runtime after restart: restart `aureus-trader` theo `RUN_SERVICES.md` rồi chạy `pytest ... -k "runtime and snapshot and db" -x`
</verification>

<success_criteria>
- Mapping trong journal không còn bỏ sót dữ liệu cho các cột ema_*, cisd_*, bb_*.
- Có test E2E DB thật pass, xác nhận insert thành công và readback đúng dữ liệu cột mục tiêu.
- Sau restart service, kiểm tra DB thực tế xác nhận bản ghi snapshot mới vẫn đầy đủ các cột ema_*, cisd_*, bb_*.
</success_criteria>

<output>
After completion, create `.planning/quick/260424-l15-fix-n-t-ph-n-mapping-c-c-c-t-ema-cisd-bb/260424-l15-SUMMARY.md`
</output>
