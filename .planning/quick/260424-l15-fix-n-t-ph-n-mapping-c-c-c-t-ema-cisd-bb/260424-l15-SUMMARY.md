# Quick Task 260424-l15 Summary

## Objective
Fix mapping runtime để dữ liệu `ema_*`, `bb_*`, `cisd_*` được đi từ upstream producer vào `strategy_event.data.signal_snapshot` và persist đầy đủ xuống `aureus_trade_signal_snapshots`.

## Impact Analysis (Required)
- `publish_strategy_match` (services/aureus-signal/engine/signal_event_publisher.py)
  - Command: `npx gitnexus impact --repo Aureus --direction upstream publish_strategy_match`
  - Risk: **CRITICAL**
  - d=1: `run_strategy_executor`
  - d=2: `main_executor.py`
- `_process_candle_work_item` (services/aureus-signal/engine/live_engine.py)
  - Command: `npx gitnexus impact --repo Aureus --direction upstream _process_candle_work_item`
  - Risk: **LOW**
- `run_strategy_executor` (services/aureus-signal/engine/strategy_executor.py)
  - Command: `npx gitnexus impact --repo Aureus --direction upstream run_strategy_executor`
  - Risk: **LOW**

## Changes

### 1) `services/aureus-signal/engine/live_engine.py`
- Bổ sung `indicator_snapshot` vào payload stream `aureus:stream:{symbol}:signals`.
- Dùng lại `signal_payload["indicator_snapshot"]` cho `SIGNAL_EVENT` (tránh tính lại không cần thiết).
- Import `build_indicator_snapshot_for_telegram` ở module scope để luôn có dữ liệu indicator snapshot trong luồng aggregator -> executor.

### 2) `services/aureus-signal/engine/strategy_executor.py`
- Trước khi gọi `publish_strategy_match`, inject:
  - `res["indicator_snapshot"] = payload.get("indicator_snapshot") if dict else {}`
- Mục tiêu: truyền indicator snapshot từ stream payload sang strategy match publisher.

### 3) `services/aureus-signal/engine/signal_event_publisher.py`
- Thêm các helper nhỏ, tối thiểu:
  - `_build_signal_snapshot_from_indicator_snapshot(...)`
  - `_merge_signal_snapshots(...)`
  - `_resolve_strategy_match_signal_snapshot(...)`
- Logic mới:
  - Base snapshot = `normalized_signal_snapshot` (hoặc fallback `active_signals` như cũ).
  - Derive thêm các key DB-critical từ `indicator_snapshot`:
    - EMA: `ema_21`, `ema_34`, `ema_55`, `ema_89`, `ema_100`, `ema_200`
    - BB: `bb_m1_up/dn`, `bb_m5_up/dn`, `bb_m15_up/dn`, `bb_m30_up/dn`, `bb_h1_up/dn`
    - CISD MTF: `cisd_m5`, `cisd_m15`, `cisd_m30`, `cisd_h1` (upper-case string)
  - Merge theo nguyên tắc không overwrite giá trị đã có trong base snapshot.

### 4) `services/aureus-signal/tests/test_signal_event_publisher.py`
- Thêm test xác nhận `publish_strategy_match` derive được indicator fields vào `data.signal_snapshot`.
- Thêm test xác nhận khi conflict thì giữ giá trị đã có sẵn trong `normalized_signal_snapshot`.

## Verification

### Unit/contract tests
- `pytest services/aureus-signal/tests/test_signal_event_publisher.py -x` ✅ (8 passed)
- `pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "mapping_from_json_string_active_signals or boundary_pre_open" -x` ✅

### E2E DB thật (theo yêu cầu DB change)
- `pytest services/aureus-trader/tests/test_signal_snapshot_pipeline.py -k "e2e_db_real_persists_ema_cisd_bb_columns" -x` ✅

### Runtime restart + DB kiểm tra thực tế
- Restart:
  - `wsl -d Aureus -e bash -lc "docker restart aureus-trader-dev aureus-signal-dev"` ✅
- Flow runtime thật để tạo row mới:
  - Chạy script publish `STRATEGY_MATCH` + `ACK` + `ORDER_OPENED` qua Redis (trace: `manual-runtime-1777022207`) ✅
- DB check:
  - Query bản ghi theo trace_id runtime cho thấy các cột mục tiêu đã có dữ liệu:
    - `ema_21 = 3345.12`
    - `ema_55 = 3338.4`
    - `bb_m15_up = 3352.55`
    - `bb_m15_dn = 3331.12`
    - `cisd_m15 = -1`
    - `signal_schema_version = sig-v2.0.0`
    - `snapshot_type = object`

## Notes
- Runtime rows cũ trước patch vẫn thiếu indicator keys (đúng kỳ vọng lịch sử).
- Bằng chứng runtime mới theo trace manual xác nhận pipeline sau patch đã persist đủ dữ liệu cột mục tiêu.
- Scope giữ tối thiểu đúng quick objective: chỉ bổ sung data propagation upstream -> strategy_match payload, không thay đổi business decision logic.
