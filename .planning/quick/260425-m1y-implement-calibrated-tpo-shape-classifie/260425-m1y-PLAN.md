---
phase: 260425-m1y-implement-calibrated-tpo-shape-classifie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/tpo.py
  - services/aureus-signal/tests/test_tpo_signal.py
autonomous: true
requirements:
  - REQ-LQO-01
  - REQ-LQO-02
  - REQ-LQO-03
  - REQ-LQO-04
  - REQ-LQO-05
must_haves:
  truths:
    - "_classify_shape phân loại D/B/p/b bằng heuristic geometry rõ ràng, không chỉ normalize score cũ."
    - "Sparse/empty/insufficient profile không tạo confidence cao gây hiểu nhầm."
    - "B-shape chỉ confidence cao khi có hai peak tách biệt và valley đủ sâu."
    - "shape_confidence_pct phản ánh top-1/top-2 margin và data-quality cap, không được xem là xác suất thống kê."
    - "Outlier/wick và tick_size boundary có hành vi ổn định qua unit fixtures."
  artifacts:
    - path: "services/aureus-signal/engine/signals/tpo.py"
      provides: "Calibrated deterministic TPO shape classifier core trong _classify_shape"
      contains: "def _classify_shape"
    - path: "services/aureus-signal/tests/test_tpo_signal.py"
      provides: "Unit/edge fixtures cho D, B, p, b, sparse, outlier, tick_size và distribution contract"
      contains: "test_tpo_classify_shape"
  key_links:
    - from: "services/aureus-signal/engine/signals/tpo.py::_build_tpo_block"
      to: "services/aureus-signal/engine/signals/tpo.py::_classify_shape"
      via: "shape, confidence_pct, scores = self._classify_shape(levels, counts, poc_idx)"
      pattern: "_classify_shape\(levels, counts, poc_idx\)"
    - from: "services/aureus-signal/tests/test_tpo_signal.py"
      to: "services/aureus-signal/engine/signals/tpo.py::_classify_shape"
      via: "direct fixture calls on TPOSignal._classify_shape"
      pattern: "_classify_shape"
---

<objective>
Implement calibrated deterministic heuristic core cho `_classify_shape` theo requirements đã khóa từ `260425-lqo-REQUIREMENTS.md`, chỉ trong phạm vi hàm classifier và tests liên quan.

Purpose: Giảm false confidence của TPO shape bằng maturity/data-quality gate, metric geometry rõ cho D/B/p/b, peak separation/valley depth cho B-shape, confidence margin top-1/top-2, và robustness với sparse/outlier/tick_size mà không động tới Telegram, scoring, database, migrations hoặc schema.
Output: `_classify_shape` được cải thiện và test suite liên quan chứng minh các fixture D/B/p/b/sparse/outlier/tick_size pass.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/quick/260425-lqo-d-a-va-o-report-na-y-planning-quick-2604/260425-lqo-REQUIREMENTS.md
@D:/Aureus/.planning/quick/260425-lqo-d-a-va-o-report-na-y-planning-quick-2604/260425-lqo-ARCHITECTURE-ADVISORY.md
@D:/Aureus/services/aureus-signal/engine/signals/tpo.py
@D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py

<interfaces>
Existing production contract:
```python
def _classify_shape(self, levels: List[float], counts: List[int], poc_idx: int) -> Tuple[str, float, Dict[str, float]]
```

Current caller contract in `_build_tpo_block` must remain unchanged:
```python
shape, confidence_pct, scores = self._classify_shape(levels, counts, poc_idx)
return {
    "POC": poc,
    "VAH": vah,
    "VAL": val,
    "shape": shape,
    "shape_confidence_pct": confidence_pct,
    "shape_scores_pct": scores,
}
```

Allowed output contract for this quick task:
- `shape` remains one of `{"D", "B", "p", "b"}`.
- `shape_confidence_pct` remains a float in `[0.0, 100.0]`.
- `shape_scores_pct` remains a dict with exactly keys `D/B/p/b` and percentage-like values summing to about `100.0` for non-empty profiles.
- For empty/zero profiles, preserve safe low-confidence behavior; do not introduce new Telegram-visible states like `unknown`, `forming`, or `insufficient_data` unless existing tests/contracts already support them. In this quick scope, represent unknown/insufficient as low/capped confidence rather than widening the runtime contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add calibrated classifier fixture tests before changing runtime</name>
  <files>D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py</files>
  <behavior>
    - D fixture: balanced bell-like distribution around POC returns `D` with higher confidence than ambiguous/sparse cases.
    - B fixture: two separated prominent peaks with a deep valley returns `B`; balanced-but-lumpy adjacent local maxima must not become high-confidence `B`.
    - p fixture: upper-heavy distribution or lower-tail excess returns `p` consistently.
    - b fixture: lower-heavy distribution or upper-tail excess returns `b` consistently.
    - Sparse/empty/zero-count fixtures: return safe low/capped confidence and valid `shape_scores_pct` contract without high confidence.
    - Outlier/wick fixture: one distant low-count outlier bin must not dominate shape/confidence.
    - tick_size boundary fixture: equivalent profile levels at small/large tick spacing must keep stable shape classification.
    - Confidence margin fixture: near-tie top-1/top-2 profiles must have capped/lower confidence than clear winners.
  </behavior>
  <action>
    Run GitNexus exploration only if needed, but do not edit production yet. Extend `test_tpo_signal.py` with focused unit tests that call `TPOSignal._classify_shape(levels, counts, poc_idx)` directly. Keep tests self-contained using explicit synthetic `levels/counts`; do not add database, Telegram, scoring, migration, or schema tests. Encode REQ-LQO-01 through REQ-LQO-05 expectations from the locked requirements. Do not require a new public state string; use the existing contract and assert low/capped confidence for unknown/insufficient/sparse cases. Use simple helper fixtures inside the test file only if they reduce duplication without adding abstraction.
  </action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-signal/tests/test_tpo_signal.py -q</automated>
  </verify>
  <done>Tests initially capture D/B/p/b, sparse/empty, outlier, tick_size, and margin behavior; failures clearly identify current classifier gaps before runtime modification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement calibrated deterministic heuristic inside _classify_shape only</name>
  <files>D:/Aureus/services/aureus-signal/engine/signals/tpo.py</files>
  <behavior>
    - REQ-LQO-01: D score uses symmetry around POC and compactness/profile quality; p/b use upper/lower mass/excess with skew as one component, not sole evidence.
    - REQ-LQO-02: B score requires two non-adjacent or sufficiently separated peaks plus valley depth; close local maxima in a balanced profile cannot produce high-confidence B.
    - REQ-LQO-03: Empty, zero, too few usable bins, too low total count, or sparse profiles receive low/capped confidence through a data-quality factor.
    - REQ-LQO-04: final `shape_confidence_pct` is capped/derived from winner score, top-1/top-2 margin, and data-quality; low margin means lower confidence even if the normalized winner is first.
    - REQ-LQO-05: outlier bins have bounded influence; level spacing/tick_size changes should not flip obvious fixture shapes.
  </behavior>
  <action>
    Before editing, run GitNexus impact analysis for `_classify_shape` per project rule and report direct callers, affected processes, and risk level in the execution summary. Then modify only `TPOSignal._classify_shape` in `tpo.py`; do not change `_build_tpo_block`, Telegram formatters, strategy scoring, database code, migrations, schema, or public TPO block keys. Implement small local calculations inside the function: usable-bin/total-count data-quality factor, symmetry around POC, compactness or concentration around POC, peak detection with prominence, minimum separation by bin distance, valley depth ratio between top peaks, bounded upper/lower excess/tail metrics, normalized score dict for `D/B/p/b`, and final confidence using top-1/top-2 margin plus data-quality cap. Keep output keys exactly `D`, `B`, `p`, `b`; round percentages consistently with current style. If all raw scores collapse, return a safe low-confidence D with a valid percentage distribution rather than introducing a new runtime state.
  </action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-signal/tests/test_tpo_signal.py -q</automated>
  </verify>
  <done>`_classify_shape` passes the new fixtures and preserves existing TPO block output contract; no files outside `tpo.py` and `test_tpo_signal.py` are modified.</done>
</task>

<task type="auto">
  <name>Task 3: Run focused regression checks and GitNexus change-scope verification</name>
  <files>D:/Aureus/services/aureus-signal/engine/signals/tpo.py, D:/Aureus/services/aureus-signal/tests/test_tpo_signal.py</files>
  <action>
    Run the existing relevant TPO test suite plus the new classifier fixtures. Because scope explicitly excludes database and this change does not touch persistence, do not run DB e2e unless tests reveal a DB-related regression. If GitNexus MCP/CLI tools are available, run `gitnexus_detect_changes()` before commit/summary and record whether only `_classify_shape` and test symbols changed. If GitNexus tools are unavailable in the execution environment, record that limitation clearly and use `git diff -- services/aureus-signal/engine/signals/tpo.py services/aureus-signal/tests/test_tpo_signal.py` plus pytest as the fallback. Do not modify Telegram, scoring, database, migrations, schema, or unrelated formatting while fixing test failures.
  </action>
  <verify>
    <automated>cd D:/Aureus && pytest services/aureus-signal/tests/test_tpo_signal.py -q</automated>
  </verify>
  <done>Focused tests pass; change-scope verification confirms only the intended classifier/test files changed; summary documents impact analysis, test commands, and GitNexus detect_changes result or limitation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Market OHLCV/profile input → TPO classifier | `levels/counts/poc_idx` are derived from market data and can be sparse, outlier-heavy, or malformed by upstream data quality issues. |
| Classifier output → indicator snapshot/Telegram consumers | `shape_confidence_pct` may be interpreted by humans as probability if confidence is too high or poorly calibrated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260425-m1y-01 | T | `_classify_shape` input profile | mitigate | Validate empty/zero/low usable-bin conditions inside classifier and cap confidence via data-quality factor. |
| T-260425-m1y-02 | I | `shape_confidence_pct` | mitigate | Compute confidence from top-1/top-2 margin plus data quality; tests assert sparse/near-tie profiles do not report high confidence. |
| T-260425-m1y-03 | D | Outlier/tick_size sensitivity | mitigate | Bound outlier influence through robust geometry metrics and add explicit outlier/tick_size tests. |
| T-260425-m1y-04 | E | Strategy scoring path | accept | Scope does not connect shape to scoring; plan forbids scoring changes and verifies no scoring files changed. |
</threat_model>

<verification>
- Primary automated command: `cd D:/Aureus && pytest services/aureus-signal/tests/test_tpo_signal.py -q`
- Scope verification: GitNexus impact analysis before editing `_classify_shape`; GitNexus detect_changes before commit/summary if available, otherwise explicit fallback diff limited to `tpo.py` and `test_tpo_signal.py`.
- Contract verification: existing tests `test_tpo_block_includes_shape_confidence_and_scores` and `test_tpo_classify_shape_returns_valid_probability_distribution` still pass.
</verification>

<success_criteria>
- REQ-LQO-01: Metric definitions for D/B/p/b exist in `_classify_shape` implementation and are covered by direct unit fixtures.
- REQ-LQO-02: B fixture requires peak separation and valley depth; adjacent/lumpy negative case does not become high-confidence B.
- REQ-LQO-03: Empty/zero/sparse/insufficient profiles have low or capped confidence.
- REQ-LQO-04: Top-1/top-2 margin affects confidence; near ties have lower confidence than clear winners.
- REQ-LQO-05: Outlier/wick and tick_size boundary fixtures pass.
- Scope guardrail: no Telegram, strategy scoring, database, migration, schema, or unrelated files are modified.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260425-m1y-implement-calibrated-tpo-shape-classifie/260425-m1y-SUMMARY.md`.
</output>
