---
phase: quick-260520-wy4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/gateway/trader_gateway.py
  - services/aureus-signal/journal.py
  - services/aureus-signal/tests/test_trade_lifecycle.py
  - services/aureus-signal/tests/e2e/test_trade_lifecycle_db.py
  - mql5/AureusProvider_v2.mq5
autonomous: true
requirements:
  - QUICK-260520-WY4
must_haves:
  truths:
    - "Người dùng biết chính xác commit/change nào làm lifecycle ORDER_OPENED không cập nhật FILLED/CLOSED/journal, dựa trên git history/diff và runtime logs/DB."
    - "ORDER_OPENED có cmd_id/ticket phải rời trạng thái SENT bằng đúng link lifecycle hiện có, không dùng fallback rộng hoặc đoán trace."
    - "aureus_trade_journal có row được tạo/cập nhật khi lifecycle thật đi qua ORDER_OPENED -> ORDER_FILLED/ORDER_CLOSED với DB thật."
    - "Quick 260520-9kx được kiểm chứng: fix close cmd_id/trace mapping chưa đủ nếu link hỏng nằm ở ORDER_OPENED -> DB status/journal lifecycle."
  artifacts:
    - path: "services/aureus-signal/gateway/trader_gateway.py"
      provides: "Gateway nhận event MT5 và dispatch lifecycle sang journal/trade status."
    - path: "services/aureus-signal/journal.py"
      provides: "DB lifecycle persistence cho aureus_trades/aureus_trade_journal."
    - path: "services/aureus-signal/tests/test_trade_lifecycle.py"
      provides: "Targeted regression test cho ORDER_OPENED/ORDER_FILLED/ORDER_CLOSED correlation không fallback rộng."
    - path: "services/aureus-signal/tests/e2e/test_trade_lifecycle_db.py"
      provides: "DB E2E test dùng database thật để chứng minh lifecycle tạo/cập nhật aureus_trades/aureus_trade_journal."
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "MT5 event producer cho ORDER_OPENED/ORDER_FILLED/ORDER_CLOSED."
  key_links:
    - from: "mql5/AureusProvider_v2.mq5"
      to: "services/aureus-signal/gateway/trader_gateway.py"
      via: "ORDER_OPENED/ORDER_FILLED/ORDER_CLOSED JSON events with cmd_id/ticket/trace_id fields"
      pattern: "ORDER_OPENED|ORDER_FILLED|ORDER_CLOSED"
    - from: "services/aureus-signal/gateway/trader_gateway.py"
      to: "services/aureus-signal/journal.py"
      via: "event handler calls journal lifecycle method with exact correlation keys"
      pattern: "on_order_opened|on_order_filled|on_order_closed"
    - from: "services/aureus-signal/journal.py"
      to: "aureus_trades/aureus_trade_journal"
      via: "SQL update/insert using trace_id/cmd_id/ticket without broad fallback"
      pattern: "aureus_trades|aureus_trade_journal"
---

<objective>
Phân tích lại git log, diff gần đây, runtime logs, DB evidence để xác định đúng điểm gãy làm 125 row aureus_trades hôm nay kẹt SENT và aureus_trade_journal hôm nay 0 row dù log có ORDER_OPENED cmd_id/ticket lúc 16:18.

Purpose: sửa đúng lifecycle link hỏng, không đoán, không thêm fallback rộng, không che lỗi bằng mapping may rủi.
Output: code fix surgical tại đúng symbol/file gây lỗi, kèm bằng chứng DB E2E thật tạo/cập nhật data.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@D:/Aureus/RUN_SERVICES.md
@D:/Aureus/.planning/quick/260520-9kx-ph-n-ti-ch-la-i-log-cu-a-git-va-review-x/260520-9kx-SUMMARY.md
@D:/Aureus/.planning/quick/260519-w3p-ph-n-ti-ch-la-i-file-mql5-aureusprovider/260519-w3p-SUMMARY.md
@D:/Aureus/services/aureus-signal/gateway/trader_gateway.py
@D:/Aureus/services/aureus-signal/journal.py
@D:/Aureus/services/aureus-signal/tests/test_trade_lifecycle.py
@D:/Aureus/services/aureus-signal/tests/e2e/test_trade_lifecycle_db.py
@D:/Aureus/mql5/AureusProvider_v2.mq5

Facts from current runtime evidence:
- Date 2026-05-20. aureus_trade_journal today 0 rows.
- aureus_trades today has SENT=125, no FILLED/CLOSED.
- Logs show ORDER_OPENED with cmd_id/ticket at 16:18, but no ORDER_FILLED/ORDER_CLOSED in recent tail.
- Prior quick 260520-9kx fixed close cmd_id/trace mapping in provider and gateway, but live journal still empty.
- User requires git history/diff/log + runtime logs/DB evidence before edits. No guessing/random fallback.

Project constraints:
- Mọi trao đổi bằng tiếng Việt, không dùng HTML tags.
- Nếu command lỗi, đọc D:/Aureus/RUN_SERVICES.md trước khi retry service-related command.
- Trước khi sửa function/class/method nào, phải chạy GitNexus impact upstream cho symbol đó và báo blast radius.
- Trước commit-ready, phải chạy GitNexus detect_changes.
- Nếu chạm backend DB/journal/trades, phải chạy E2E với database thật xác nhận tạo/cập nhật data.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dựng timeline regression từ git history, runtime logs, DB thật</name>
  <files>D:/Aureus/.planning/quick/260520-wy4-ph-n-ti-ch-la-i-log-cu-a-git-va-review-x/260520-wy4-PLAN.md</files>
  <action>Không sửa code ở task này. Dùng git log/git show/git diff để so sánh các commit gần bug: dcb95f4, 4fe9793, 0d1a6c5, 9637c91, add3910 và các commit lifecycle liên quan 5ccf0eb/cb42dee/c43467d nếu cần. Kiểm tra runtime logs gateway/provider hiện có và query DB thật cho hôm nay bằng docker exec psql command trong verify, không phụ thuộc helper script ngoài kế hoạch. Query bắt buộc: count aureus_trade_journal hôm nay, grouped status aureus_trades hôm nay, sample rows SENT có cmd_id/ticket/trace_id, và timestamp quanh ORDER_OPENED 16:18. Xác định lifecycle point đầu tiên fail: MT5 không gửi ORDER_FILLED/CLOSED, gateway nhận nhưng không dispatch, journal reject/update miss, hay DB query correlation sai. Bắt buộc ghi lại bằng chứng cụ thể trong response execution, gồm commit/change nghi vấn và vì sao 260520-9kx chưa đủ nếu missing link là ORDER_OPENED -> DB status/journal lifecycle.</action>
  <verify>
    <automated>git -C D:/Aureus log --oneline -20 -- services/aureus-signal/gateway/trader_gateway.py services/aureus-signal/journal.py mql5/AureusProvider_v2.mq5 && git -C D:/Aureus show --stat --oneline 4fe9793 0d1a6c5 9637c91 add3910</automated>
    <automated>docker exec aureus-postgres psql -U aureus -d aureus -v ON_ERROR_STOP=1 -c "SELECT count(*) AS journal_today FROM aureus_trade_journal WHERE created_at::date = CURRENT_DATE;" -c "SELECT status, count(*) FROM aureus_trades WHERE created_at::date = CURRENT_DATE GROUP BY status ORDER BY status;" -c "SELECT id, status, trace_id, cmd_id, ticket, created_at, updated_at FROM aureus_trades WHERE created_at::date = CURRENT_DATE AND status = 'SENT' ORDER BY created_at DESC LIMIT 10;"</automated>
  </verify>
  <done>Root cause narrowed to one exact lifecycle link with git evidence + log evidence + DB evidence; no code changed yet; if docker/service command fails, read RUN_SERVICES.md before retry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix đúng symbol gãy sau GitNexus impact</name>
  <files>services/aureus-signal/gateway/trader_gateway.py, services/aureus-signal/journal.py, services/aureus-signal/tests/test_trade_lifecycle.py, mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - ORDER_OPENED event có cmd_id/ticket/trace_id hợp lệ cập nhật đúng DB lifecycle thay vì để aureus_trades.status = SENT.
    - ORDER_FILLED/ORDER_CLOSED event nếu có đủ correlation key phải update đúng row journal/trade; nếu thiếu key bắt buộc phải log lỗi rõ, không dùng broad fallback.
    - Fix giữ nguyên intent của 260520-9kx: close command correlation exact, không phá mapping cmd_id/trace_id vừa sửa.
  </behavior>
  <action>Trước khi sửa bất kỳ function/class/method nào, chạy GitNexus theo đúng CLAUDE.md: gitnexus_query cho symptom ORDER_OPENED SENT journal empty, gitnexus_context cho suspect symbol, gitnexus_impact({target: suspect, direction: "upstream"}) cho từng symbol sẽ sửa, rồi báo blast radius (direct callers, affected processes, risk). Chỉ sửa symbol/file chứng minh là gãy từ Task 1. Không thêm fallback rộng theo ticket-only/cmd_id-only nếu bằng chứng không chứng minh an toàn. Không refactor lan rộng. Nếu lỗi nằm ở provider MQL5, build theo D:/Aureus/mql5/Build_Rules.md. Nếu lỗi nằm ở gateway/journal, thêm hoặc cập nhật services/aureus-signal/tests/test_trade_lifecycle.py để tái hiện event ORDER_OPENED có cmd_id/ticket nhưng DB không rời SENT; test phải assert exact correlation key và assert không dùng broad fallback khi thiếu key.</action>
  <verify>
    <automated>python -m pytest D:/Aureus/services/aureus-signal/tests/test_trade_lifecycle.py -x</automated>
    <automated>docker exec aureus-postgres psql -U aureus -d aureus -v ON_ERROR_STOP=1 -c "SELECT count(*) AS journal_today FROM aureus_trade_journal WHERE created_at::date = CURRENT_DATE;" -c "SELECT status, count(*) FROM aureus_trades WHERE created_at::date = CURRENT_DATE GROUP BY status ORDER BY status;"</automated>
  </verify>
  <done>Fix surgical tại đúng symbol; targeted test file exists and passes; GitNexus impact đã chạy và blast radius đã báo; không có fallback rộng; nếu MQL5 đổi thì build pass theo Build_Rules.md.</done>
</task>

<task type="auto">
  <name>Task 3: DB E2E thật và commit-ready scope check</name>
  <files>services/aureus-signal/gateway/trader_gateway.py, services/aureus-signal/journal.py, services/aureus-signal/tests/e2e/test_trade_lifecycle_db.py, mql5/AureusProvider_v2.mq5</files>
  <action>Chạy E2E với database thật để tạo/cập nhật data lifecycle: tạo hoặc cập nhật services/aureus-signal/tests/e2e/test_trade_lifecycle_db.py để seed trade SENT có trace_id/cmd_id, bắn event ORDER_OPENED thật qua handler/gateway path, xác nhận aureus_trades không còn kẹt SENT và aureus_trade_journal có row. Test phải cleanup data test bằng trace_id/cmd_id unique trước và sau chạy để deterministic. Nếu flow cần service đang chạy mà command fail, đọc RUN_SERVICES.md rồi chạy đúng cách. Sau verify, chạy gitnexus_detect_changes() trước trạng thái commit-ready và xác nhận scope chỉ gồm symbol/file dự kiến. Không commit trừ khi user/orchestrator yêu cầu.</action>
  <verify>
    <automated>python -m pytest D:/Aureus/services/aureus-signal/tests/e2e/test_trade_lifecycle_db.py -x</automated>
    <automated>docker exec aureus-postgres psql -U aureus -d aureus -v ON_ERROR_STOP=1 -c "SELECT trace_id, status FROM aureus_trades WHERE trace_id LIKE 'e2e-lifecycle-%' ORDER BY created_at DESC LIMIT 5;" -c "SELECT trace_id, event_type, created_at FROM aureus_trade_journal WHERE trace_id LIKE 'e2e-lifecycle-%' ORDER BY created_at DESC LIMIT 10;"</automated>
    <automated>git -C D:/Aureus diff -- services/aureus-signal/gateway/trader_gateway.py services/aureus-signal/journal.py services/aureus-signal/tests/test_trade_lifecycle.py services/aureus-signal/tests/e2e/test_trade_lifecycle_db.py mql5/AureusProvider_v2.mq5</automated>
  </verify>
  <done>DB E2E thật chứng minh row được tạo/cập nhật; detect_changes xác nhận scope expected; response có before/after DB counts và exact failing lifecycle point đã fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 provider -> gateway | JSON event từ MT5 đi vào backend, cần correlation key chính xác. |
| gateway -> database | Event lifecycle cập nhật aureus_trades/aureus_trade_journal trong DB thật. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260520-WY4-01 | Tampering | MT5 lifecycle event correlation | mitigate | Không dùng fallback rộng; update DB chỉ bằng trace_id/cmd_id/ticket được chứng minh đúng từ log/git/DB. |
| T-260520-WY4-02 | Repudiation | journal lifecycle persistence | mitigate | Giữ log lỗi rõ khi thiếu key hoặc update miss; E2E DB xác nhận insert/update thật. |
| T-260520-WY4-03 | Information Disclosure | runtime logs and DB samples | mitigate | Khi báo kết quả, chỉ nêu identifiers cần debug; không dump secrets/env. |
| T-260520-WY4-04 | Denial of Service | gateway/journal handler | accept | Scope là bug lifecycle, không đổi throughput/retry. Targeted tests bảo vệ không tạo loop/fallback spam. |
</threat_model>

<verification>
- Git history/diff đã review trước edit.
- Runtime logs và DB thật đã kiểm chứng trước edit bằng docker exec psql deterministic, không phụ thuộc query_trade_lifecycle_today.py.
- GitNexus impact chạy trước mọi symbol edit.
- Targeted tests pass: services/aureus-signal/tests/test_trade_lifecycle.py.
- DB E2E thật pass: services/aureus-signal/tests/e2e/test_trade_lifecycle_db.py, có row tạo/cập nhật.
- gitnexus_detect_changes chạy trước commit-ready.
</verification>

<success_criteria>
- Exact failing lifecycle point được nêu rõ: producer, gateway dispatch, journal persistence, hay DB correlation.
- Fix surgical, không đoán, không fallback rộng.
- aureus_trades test row chuyển khỏi SENT đúng lifecycle.
- aureus_trade_journal có row sau ORDER_OPENED/FILLED/CLOSED path được E2E kích hoạt.
- 260520-9kx được đối chiếu và xác nhận phần nào đã sửa, phần nào chưa giải quyết bug hiện tại.
</success_criteria>

<output>
Sau khi hoàn tất, tạo summary theo execute-plan tại D:/Aureus/.planning/quick/260520-wy4-ph-n-ti-ch-la-i-log-cu-a-git-va-review-x/260520-wy4-SUMMARY.md
</output>
