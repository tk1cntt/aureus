---
phase: 260503-n24-update-khi-n-o-g-p-l-i-market-close-c-a-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
autonomous: true
requirements:
  - QUICK-260503-N24
must_haves:
  truths:
    - "Khi một symbol gặp lỗi market-close, toàn bộ xử lý quản lý/DCA cho đúng symbol đó dừng im lặng trong guard window."
    - "Symbol khác vẫn tiếp tục xử lý bình thường; không có global stop khi chỉ một symbol đóng cửa."
    - "Lần đầu phát hiện market-close của một symbol có log; các lần sau trong guard window không spam warning/guard/HOLD."
    - "Các lỗi close không phải market-close vẫn được log như hiện tại, không bị che mất."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Symbol-scoped market-close guard state and early skip for provider processing"
      contains: "MarketClosedCloseGuardState"
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Close failure handling that sets guard only for MARKET_CLOSED retcode"
      contains: "TRADE_RETCODE_MARKET_CLOSED"
  key_links:
    - from: "ClosePositionTickets"
      to: "SetMarketClosedCloseGuard"
      via: "market-close retcode and pre-close unavailable check"
      pattern: "TRADE_RETCODE_MARKET_CLOSED|IsSymbolCloseAvailableNow"
    - from: "ManagePositionProfitBreakEvent"
      to: "ProcessPositionsByType"
      via: "symbol+magic+direction grouped processing"
      pattern: "ProcessPositionsByType\\(symbol, magic, type"
    - from: "ProcessPositionsByType"
      to: "IsMarketClosedCloseGuardActive"
      via: "early skip before profile fallback and management logs"
      pattern: "IsMarketClosedCloseGuardActive\\(symbol, magic"
---

<objective>
Cập nhật `mql5/AureusProvider_v2.mq5` để market-close guard dừng toàn bộ xử lý cho đúng symbol gặp lỗi, không dừng symbol khác.

Purpose: Broker có symbol đóng cửa theo phiên và symbol chạy cả tuần. Market-close của một symbol không được làm provider ngừng xử lý symbol khác, nhưng cũng không được spam close/log cho symbol đã đóng.
Output: Guard scoped theo symbol, skip im lặng sau lần phát hiện đầu, và giữ log lỗi close không phải market-close.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260503-mjm-market-closed-guard-v-n-spam-log-hold-pr/260503-mjm-SUMMARY.md
@D:/Aureus/mql5/AureusProvider_v2.mq5

Assumptions:
- Scope chỉ `mql5/AureusProvider_v2.mq5`.
- "Stop tất cả xử lý của symbol đó" nghĩa là skip provider position-management/DCA work for that symbol while guard active, not only skip same magic/direction close group.
- Must not introduce global market-closed flag.
- Must keep non-market-close close failure logging.

Relevant current contracts:
```mql5
struct MarketClosedCloseGuardState
  {
   string            symbol;
   long              magic;
   string            direction;
   datetime          guard_until;
  };

bool IsMarketClosedCloseGuardActive(string symbol, long magic, string direction, datetime &guardUntil)
void SetMarketClosedCloseGuard(string symbol, long magic, string direction)
bool IsSymbolCloseAvailableNow(string symbol, string &reason)
bool ClosePositionTickets(string symbol, long magic, string pos_type_str, string profile, const ulong &tickets[], double net_profit, int age_seconds, string reason, string primitive)
void ProcessPositionsByType(string symbol, long magic, ENUM_POSITION_TYPE target_type, const ulong &tickets[], double total_profit, double total_volume, double weighted_price_sum, datetime earliest_open_time)
void ManagePositionProfitBreakEvent()
void DoDCA(int order_type_signal, string symbol, long magic)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make market-close guard symbol-wide, not group-only</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - When any close attempt/pre-check marks `EURUSD` market-closed, later provider work for `EURUSD` skips before management profile fallback, HOLD logs, close attempts, and DCA attempts.
    - When `EURUSD` is guarded, `XAUUSD` remains eligible for management and DCA.
    - Guard log emits only when a guard is first created or extended; active guard checks stay silent.
  </behavior>
  <action>Before editing, run GitNexus impact for symbols you will modify, at minimum `SetMarketClosedCloseGuard`, `IsMarketClosedCloseGuardActive`, `ProcessPositionsByType`, `ManagePositionProfitBreakEvent`, and any DCA entrypoint touched. If GitNexus cannot find MQL5 symbols, document limitation in SUMMARY and proceed with source checks. Change guard lookup semantics so an active guard for a symbol blocks all processing for that symbol while preserving stored magic/direction for first-detection log context. Prefer adding a helper such as `IsSymbolMarketClosedCloseGuardActive(symbol, guardUntil)` that scans `g_marketClosedCloseGuards` by symbol only, rather than replacing all existing group-scoped helpers if group checks still useful. Use symbol-wide helper at top of `ProcessPositionsByType` and before provider DCA processing for same symbol. Do not add global stop. Do not add repeated PrintFormat in active guard path.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('D:/Aureus/mql5/AureusProvider_v2.mq5')
s = p.read_text(encoding='utf-8', errors='ignore')
assert 'MarketClosedCloseGuardState' in s
assert 'SetMarketClosedCloseGuard' in s
assert 'TRADE_RETCODE_MARKET_CLOSED' in s or '10018' in s
assert 'IsSymbolMarketClosedCloseGuardActive' in s, 'expected symbol-wide guard helper'
idx = s.index('void ProcessPositionsByType')
body = s[idx:s.index('//+------------------------------------------------------------------+', idx)]
assert body.index('IsSymbolMarketClosedCloseGuardActive') < body.index('ResolveManagementProfile')
print('symbol-wide market-close guard source assertions passed')
PY</automated>
  </verify>
  <done>`AureusProvider_v2.mq5` has symbol-wide active guard check. Guarded symbol skips management/DCA quietly. Other symbols not matching guarded symbol continue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep first detection logging and non-market-close failures visible</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5</files>
  <behavior>
    - First market-close detection logs `[MarketClosedCloseGuard] Set guard ... reason=MARKET_CLOSED` once when state is created or extended.
    - Repeated active-guard skip does not log `[ManagePositionDecision]`, close failed warning, or guard warning.
    - Non-market-close `trade.PositionClose` failures still print `[ManagePositionProfitBreakEvent] Close failed ...` with retcode/reason/comment.
  </behavior>
  <action>Audit `ClosePositionTickets` ordering. Keep close-unavailable pre-check before `LogManagementDecision(... "CLOSE" ...)`. Keep `PrintFormat("[ManagePositionProfitBreakEvent] Close failed ...")` for all failed close retcodes, but ensure market-closed retcode sets symbol guard and returns. If needed, move market-closed detection after failure log only once; do not suppress non-market-close failures. Add no new warning spam in guard-active returns.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('D:/Aureus/mql5/AureusProvider_v2.mq5')
s = p.read_text(encoding='utf-8', errors='ignore')
close_idx = s.index('bool ClosePositionTickets')
close_body = s[close_idx:s.index('bool MovePositionsSL', close_idx)]
assert close_body.index('IsSymbolCloseAvailableNow') < close_body.index('LogManagementDecision') < close_body.index('trade.PositionClose')
assert '[ManagePositionProfitBreakEvent] Close failed' in close_body
assert 'ResultRetcode' in close_body and 'ResultComment' in close_body
assert close_body.index('[ManagePositionProfitBreakEvent] Close failed') < close_body.index('TRADE_RETCODE_MARKET_CLOSED')
# Active guard paths must be silent: no PrintFormat before first return after symbol-wide/group guard check.
first_guard = close_body.index('Is')
first_return = close_body.index('return false', first_guard)
assert 'PrintFormat' not in close_body[first_guard:first_return]
print('logging and failure visibility source assertions passed')
PY</automated>
  </verify>
  <done>First market-close detection log remains, repeated guarded processing stays quiet, and non-market-close close failures remain observable.</done>
</task>

<task type="auto">
  <name>Task 3: Verify scope, compile path, and GitNexus change detection</name>
  <files>D:/Aureus/mql5/AureusProvider_v2.mq5, D:/Aureus/.planning/quick/260503-n24-update-khi-n-o-g-p-l-i-market-close-c-a-/260503-n24-SUMMARY.md</files>
  <action>Run `git diff --check -- mql5/AureusProvider_v2.mq5`. If MetaEditor CLI exists, compile `mql5/AureusProvider_v2.mq5`; if not, document compile limitation in SUMMARY. Run GitNexus detect changes before commit per `D:/Aureus/CLAUDE.md`; if CLI/MCP command is unavailable or MQL5 symbols are not indexed, document exact limitation and confirm only expected file changed via `git diff -- mql5/AureusProvider_v2.mq5`. Create quick SUMMARY with blast-radius notes, verification output, limitations, and files changed. Do not commit untracked generated `mql5/AureusProvider_v2.ex5` or `stable/` unless user explicitly asks.</action>
  <verify>
    <automated>git -C D:/Aureus diff --check -- mql5/AureusProvider_v2.mq5</automated>
    <automated>git -C D:/Aureus status --short</automated>
  </verify>
  <done>Diff check passes. GitNexus detect/limitation documented. Only intended tracked source plus planning summary changed; generated/untracked files not included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Broker trade session state → provider close logic | Broker/MT5 market availability and retcode decide whether provider should attempt close or skip symbol. |
| Provider symbol guard → multi-symbol processing | Guard state for one symbol must not block unrelated symbols. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260503-N24-01 | D | `ManagePositionProfitBreakEvent` / DCA loops | mitigate | Use symbol-scoped active guard so repeated market-close retries cannot spam CPU/logs for closed symbol. |
| T-260503-N24-02 | D | multi-symbol provider processing | mitigate | Match guard by exact symbol only; never add global guard. Verify other symbol paths remain reachable. |
| T-260503-N24-03 | R | close failure logging | mitigate | Preserve retcode/reason/comment logs for non-market-close failures so real close errors remain auditable. |
</threat_model>

<verification>
Overall checks:
- Source assertions prove symbol-wide guard helper exists and runs before profile/HOLD/close work.
- Source assertions prove pre-close availability check occurs before close decision and broker close call.
- Source assertions prove non-market-close failures still log retcode/reason/comment.
- `git diff --check -- mql5/AureusProvider_v2.mq5` passes.
- GitNexus impact/detect completed or exact MQL5 limitation documented.
</verification>

<success_criteria>
- Market-close encountered for one symbol stops all provider processing for that symbol during guard window.
- Other symbols continue; no global stop.
- First detection logs guard set; repeated guarded cycles silent.
- Non-market-close close failures still visible.
- Scope limited to `mql5/AureusProvider_v2.mq5` plus quick SUMMARY.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260503-n24-update-khi-n-o-g-p-l-i-market-close-c-a-/260503-n24-SUMMARY.md`.
</output>
