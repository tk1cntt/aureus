---
phase: 260519-w3p-ph-n-ti-ch-la-i-file-mql5-aureusprovider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
autonomous: true
requirements:
  - QUICK-260519-W3P
must_haves:
  truths:
    - "Repository root is confirmed as D:/Aureus and mql5/AureusProvider_v2.mq5 exists there before execution."
    - "Git history for mql5/AureusProvider_v2.mq5 is inspected before code change."
    - "Recent edits causing current provider/reconciliation failure are identified with commit/file evidence."
    - "Root cause is fixed surgically in AureusProvider_v2 MQL5 provider/reconciliation path."
    - "MQL5 provider builds after fix using mql5/Build_Rules.md."
    - "GitNexus impact runs before editing modified symbols and detect_changes runs before any commit."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Fixed MT5 provider/reconciliation behavior"
      contains: "root-cause fix in affected symbol(s)"
    - path: "mql5/Build_Rules.md"
      provides: "Required MQL5 build procedure"
    - path: "git history for mql5/AureusProvider_v2.mq5"
      provides: "Regression evidence from recent commits"
  key_links:
    - from: "D:/Aureus repository root"
      to: "mql5/AureusProvider_v2.mq5"
      via: "pre-flight file existence check before git/history/build commands"
      pattern: "test -f mql5/AureusProvider_v2.mq5"
    - from: "git log/diff for mql5/AureusProvider_v2.mq5"
      to: "root-cause symbol(s) in mql5/AureusProvider_v2.mq5"
      via: "regression comparison before edit"
      pattern: "git log --follow|git diff"
    - from: "root-cause symbol(s)"
      to: "GitNexus impact analysis"
      via: "gitnexus_impact before modification"
      pattern: "gitnexus_impact({target: \"<symbol>\", direction: \"upstream\"})"
    - from: "mql5/AureusProvider_v2.mq5"
      to: "MQL5 build output"
      via: "mql5/Build_Rules.md"
      pattern: "0 error"
---

# Objective

Phân tích lại `mql5/AureusProvider_v2.mq5`, git history, log runtime đã có nếu hữu ích, xác định commit/thay đổi gây lỗi hiện tại, rồi fix đúng root cause trong provider/reconciliation path.

Purpose: Khôi phục hành vi provider/reconciliation ổn định, không revert mù, không sửa lan man.
Output: `mql5/AureusProvider_v2.mq5` được fix, build MQL5 pass, phạm vi ảnh hưởng được GitNexus xác nhận.

# Execution context

- `D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md`
- `D:/Aureus/.claude/get-shit-done/templates/summary.md`

# Context

- `D:/Aureus/.planning/STATE.md`
- `D:/Aureus/CLAUDE.md`
- `D:/Aureus/mql5/AureusProvider_v2.mq5`
- `D:/Aureus/mql5/Build_Rules.md`
- Recent relevant quick history from STATE:
  - `260512-q15`: pending order trace_id/cmd_id mapping
  - `260513-8z8`: pending fill not sent to gateway analysis
  - `260513-ccy`: pending placement/fill observability
  - `260513-s4k`: Invalid JSON fix
  - `260517-tne`, `260518-91x`, `260518-9yd`: recent SL/profit management safety changes

# Tasks

## Task 1: Điều tra regression từ git history và log

Type: auto

Files:
- `mql5/AureusProvider_v2.mq5`
- `mql5/Build_Rules.md`
- runtime logs already present in repo/worktree if relevant, for example service logs/snapshots under `services/aureus-signal/scripts/snapshots/` or existing captured logs

Action:
- Pre-flight: run from repository root `D:/Aureus`; confirm `pwd` resolves to `/d/Aureus` or equivalent and `test -f mql5/AureusProvider_v2.mq5` passes before any plan work. If this check fails, stop and report path/worktree mismatch instead of changing scope or path.
- Inspect `git status` first and do not overwrite user changes.
- Inspect `git log --follow --oneline -- mql5/AureusProvider_v2.mq5` and focused diffs for recent commits touching provider/reconciliation behavior, especially quick tasks listed in context.
- Compare current `mql5/AureusProvider_v2.mq5` against recent good/bad commits using `git diff <commit>..HEAD -- mql5/AureusProvider_v2.mq5` and `git show <commit>:mql5/AureusProvider_v2.mq5` only as needed.
- Read current runtime logs already observed or present if useful. If command errors occur, consult `RUN_SERVICES.md` before retrying service commands.
- Identify suspect symbol(s) and exact regression mechanism before editing. Use GitNexus exploration/debug flow where available: query provider/reconciliation/fill symptom, then context for suspect symbol(s).
- Report assumptions and blast-radius plan before code change.

Verify:
- Automated: `test -f mql5/AureusProvider_v2.mq5`
- Automated: `git log --follow --oneline -- mql5/AureusProvider_v2.mq5 | head -30`
- Automated: `git diff --stat HEAD~10..HEAD -- mql5/AureusProvider_v2.mq5`
- Automated: targeted log/diff command output names suspect commit(s), suspect symbol(s), and expected failing path.

Done:
- Repository root and primary file existence are confirmed before execution.
- Suspect change is tied to concrete commit/diff lines or current uncommitted lines.
- Root-cause hypothesis names affected provider/reconciliation symbol(s) and why failure occurs.
- No source file edited yet.

## Task 2: Run GitNexus impact, then fix root cause surgically

Type: auto

Files:
- `mql5/AureusProvider_v2.mq5`

Action:
- Before editing each function/class/method/symbol, run `gitnexus_impact({target: "<symbolName>", direction: "upstream"})` and report blast radius: direct callers, affected processes, risk level.
- If GitNexus reports stale index, run `npx gitnexus analyze` from `D:/Aureus` before impact and preserve embeddings if `.gitnexus/meta.json` shows embeddings count greater than 0.
- Fix only root cause in `mql5/AureusProvider_v2.mq5`. Preserve market order path, existing pending order mapping semantics, magic/symbol/direction scoping, and recent SL safety behavior unless evidence proves those lines are root cause.
- Do not add broad fallback, placeholder, hardcoded workaround, or speculative refactor.
- Keep log lines useful for reconciliation diagnosis but avoid spam.
- If fix touches database-related backend code unexpectedly, stop and require DB E2E verification per `CLAUDE.md`; default target is MQL5 only.

Verify:
- Automated: `git diff -- mql5/AureusProvider_v2.mq5`
- Automated: `gitnexus_impact({target: "<each_modified_symbol>", direction: "upstream"})` completed before edit and risk handled.
- Automated: affected logic can be traced from incoming command/order event to reconciliation/gateway notification path without missing trace_id/cmd_id/pending_order_id linkage where relevant.

Done:
- `mql5/AureusProvider_v2.mq5` contains minimal fix only.
- Direct callers or dependent flows from GitNexus d=1 are still compatible or updated if same file scope requires it.
- Root cause described in final summary with before/after behavior.

## Task 3: Build, verify scope, and prepare commit-safe output

Type: auto

Files:
- `mql5/AureusProvider_v2.mq5`
- `mql5/Build_Rules.md`

Action:
- Build/compile MQL5 exactly per `mql5/Build_Rules.md`.
- If build command fails due environment/service setup, read `RUN_SERVICES.md` before retrying.
- Run focused static checks/searches for changed symbol names and reconciliation log/event fields.
- Run `gitnexus_detect_changes()` before any commit or commit-ready handoff. Do not commit unless user explicitly asks.
- Confirm worktree diff only includes expected file(s). Preserve existing unrelated modifications/untracked files from starting status.

Verify:
- Automated: MQL5 build command from `mql5/Build_Rules.md` completes with `0 error` or executor records exact environment blocker after following `RUN_SERVICES.md`.
- Automated: `git diff --check -- mql5/AureusProvider_v2.mq5`
- Automated: `gitnexus_detect_changes()` reports only expected symbols/flows affected.
- Automated: `git status --short` reviewed for unrelated pre-existing changes.

Done:
- Build passes or environment blocker is explicit and not code-related.
- GitNexus detect_changes completed before any commit-ready state.
- Final summary includes root cause, fix, build result, detect_changes result, and absolute path to modified file.

# Threat model

## Trust boundaries

| Boundary | Description |
|---|---|
| gateway to MT5 provider | JSON/order commands and lifecycle events cross into MQL5 execution path. |
| MT5 terminal to gateway | ORDER_OPENED/ORDER_FILLED/ORDER_FAILED/reconciliation events leave provider and update backend state. |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260519-w3p-01 | Tampering | incoming order/reconciliation fields | mitigate | Preserve existing validation/parsing; do not trust missing/blank trace_id/cmd_id/pending_order_id; keep scoped matching by symbol/magic/direction where applicable. |
| T-260519-w3p-02 | Repudiation | lifecycle event logs | mitigate | Keep non-spam diagnostic logs sufficient to prove order id mapping and notification path. |
| T-260519-w3p-03 | Denial of service | repeated reconciliation/SL management loops | mitigate | Avoid retry spam; preserve recent HOLD/move-SL safety guards unless root cause requires targeted adjustment. |
| T-260519-w3p-04 | Information disclosure | provider logs | accept | Logs are local/runtime diagnostics; do not add secrets or account credentials. |

# Verification

- Repository root `D:/Aureus` confirmed and `mql5/AureusProvider_v2.mq5` exists before execution.
- Git history reviewed for `mql5/AureusProvider_v2.mq5` with suspect commit(s) identified.
- GitNexus impact run before symbol edits.
- Root cause fixed in provider/reconciliation path only.
- MQL5 build per `mql5/Build_Rules.md` passes or blocker documented.
- `gitnexus_detect_changes()` run before any commit-ready handoff.

# Success criteria

- Primary file path `mql5/AureusProvider_v2.mq5` is validated from repository root before work starts.
- Current lỗi no longer reproduced by traced provider/reconciliation path.
- No market path regression from pending/reconciliation fix.
- No unrelated files modified by executor.
- Final output gives exact changed symbols, blast radius, build result, and detect_changes scope.

# Output

After completion, create `D:/Aureus/.planning/quick/260519-w3p-ph-n-ti-ch-la-i-file-mql5-aureusprovider/260519-w3p-SUMMARY.md`.
