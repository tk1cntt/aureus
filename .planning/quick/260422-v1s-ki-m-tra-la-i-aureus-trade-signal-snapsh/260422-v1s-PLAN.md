---
plan_id: 260422-v1s
type: quick
objective: "Kiểm tra và sửa pipeline snapshot để dữ liệu signal map đúng vào các cột hiện có của bảng snapshot, chấm dứt trạng thái tất cả signal NULL"
scope: "services/aureus-signal + services/aureus-db-writer + test pipeline snapshot với DB"
autonomous: true
---

<objective>
Xác nhận nguyên nhân vì sao dữ liệu signal không đi vào đúng cột trong bảng snapshot và sửa mapping end-to-end để các cột signal nhận giá trị đúng thay vì toàn NULL.

Purpose: Bảo toàn chất lượng dữ liệu evaluation/reporting của Phase 55; tránh đánh giá chiến lược sai do snapshot rỗng.
Output: Mapping snapshot thống nhất giữa object state → payload insert → schema DB + test e2e với database chứng minh dữ liệu được ghi đúng.
</objective>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/services/aureus-db-writer/schema.sql
@D:/Aureus/services/aureus-signal/engine/snapshot_utils.py
@D:/Aureus/services/aureus-signal/engine/state_snapshot.py
@D:/Aureus/services/aureus-signal/engine/live_engine.py
@D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tái hiện lỗi NULL và khóa expected mapping bằng test pipeline snapshot</name>
  <files>D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <behavior>
    - Test 1: Khi state có dữ liệu indicator/context/signals hợp lệ, row trong bảng snapshot phải có giá trị tương ứng (không NULL) ở các cột đang tồn tại trong schema.
    - Test 2: Mapping key payload insert phải khớp tên cột DB thực tế (không dùng key legacy hoặc key không tồn tại).
  </behavior>
  <action>Bổ sung/chỉnh test để tái hiện đúng triệu chứng hiện tại “all signal null”, sau đó khóa contract mapping giữa snapshot payload và cột DB. Test phải assert theo các cột thực tế của `aureus_signal_snapshots` trong `schema.sql` (bao gồm nhóm atr/ema/vol/htf_trend/market_regime/session/events/active_obs/swing_label và nhóm mở rộng phase 7).</action>
  <verify>
    <automated>pytest D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py -x</automated>
  </verify>
  <done>Có test fail trước khi sửa logic và test mô tả rõ contract mapping cột snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sửa mapping snapshot trong pipeline build/insert để khớp schema hiện tại</name>
  <files>D:/Aureus/services/aureus-signal/engine/snapshot_utils.py, D:/Aureus/services/aureus-signal/engine/state_snapshot.py, D:/Aureus/services/aureus-signal/engine/live_engine.py</files>
  <behavior>
    - Test 1: `build_snapshot`/`StateSnapshot.to_db_row` tạo payload đúng key và đúng nguồn dữ liệu từ state.
    - Test 2: `insert_single_snapshot`/`batch_insert_snapshots` ghi đúng cột và trả về row có giá trị thực, không bị NULL đồng loạt do lệch mapping.
  </behavior>
  <action>Đồng bộ mapping theo đúng schema hiện có: kiểm tra nhất quán tên field giữa `build_snapshot`, `StateSnapshot.to_db_row`, và câu lệnh INSERT/ON CONFLICT. Nếu phát hiện lệch giữa tên bảng user nêu (`aureus_trade_signal_snapshots`) và bảng hệ thống đang dùng (`aureus_signal_snapshots`), giữ đúng bảng thực tế đang vận hành và ghi rõ trong code comment ngắn tại điểm mapping để tránh nhầm lẫn lần sau.</action>
  <verify>
    <automated>pytest D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py -x</automated>
  </verify>
  <done>Test mapping pass; payload insert và schema không còn lệch; snapshot mới không còn tình trạng tất cả cột signal NULL.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy kiểm chứng e2e với database cho luồng ghi snapshot</name>
  <files>D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py, D:/Aureus/RUN_SERVICES.md</files>
  <action>Thực thi e2e test có kết nối DB thật cho pipeline snapshot theo hướng dẫn service trong `RUN_SERVICES.md`, sau đó truy vấn xác nhận ít nhất một bản ghi mới có giá trị non-null ở các cột signal chính. Đây là bắt buộc theo quy định project với mọi chỉnh sửa liên quan database.</action>
  <verify>
    <automated>pytest D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py -x -m e2e</automated>
  </verify>
  <done>Chứng minh được bằng test e2e + query rằng data snapshot map đúng cột DB và được tạo thành công.</done>
</task>

</tasks>

<verification>
- Test pipeline snapshot pass ở mức unit/integration.
- E2E với database pass và có bản ghi snapshot non-null ở các cột signal chính.
- Không còn mismatch mapping giữa schema.sql và payload insert.
</verification>

<success_criteria>
1. Lỗi “tất cả signal NULL” được tái hiện bằng test trước khi fix và biến mất sau khi fix.
2. Dữ liệu snapshot ghi vào đúng cột hiện có trong `aureus_signal_snapshots`.
3. Có xác nhận e2e với DB thật rằng record mới được tạo với signal data hợp lệ.
</success_criteria>
