---
phase: 260512-q15-trace-cmd-pending-fill
plan: 01
subsystem: mt5-provider-trader-journal
tags: [quick, mt5, pending-order, journal, db-e2e]
dependency_graph:
  requires: [aureus_trade_journal, aureus_trade_signal_snapshots, aureus_trades]
  provides: [pending_order_fill_correlation, trace_id_fallback_update]
  affects: [mql5/AureusProvider_v2.mq5, services/aureus-trader/journal.py]
tech_stack:
  added: []
  patterns: [in-memory bounded mapping, parameterized SQL fallback, DB E2E]
key_files:
  created: []
  modified:
    - mql5/AureusProvider_v2.mq5
    - services/aureus-trader/journal.py
    - services/aureus-trader/tests/test_journal.py
    - services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py
decisions:
  - Reuse InpMaxCmdIdHistory as provider pending mapping cap.
  - Use journal row trace_id after fallback update so snapshots/reasoning keep NOT NULL trace_id.
metrics:
  duration: "~15m"
  completed_date: "2026-05-12"
---

# Phase 260512-q15 Plan 01: Trace/Cmd Pending Fill Summary

Pending limit fill correlation now survives missing MT5 history comment by using provider memory mapping plus journal SQL fallback.

## Completed Tasks

| Task | Name | Commit | Notes |
|------|------|--------|-------|
| 1 | Add failing coverage for ORDER_FILLED fallback without trace_id | c0f79fe | Unit RED proved old early return; DB E2E extended with fallback case. |
| 2 | Implement journal fallback update by pending_order_id/cmd_id | 25abd21 | `on_order_opened` accepts trace_id, pending_order_id, or cmd_id. |
| 3 | Persist provider pending mapping and run DB E2E | 5ccf0eb | Provider stores pending ticket correlation and enriches `ORDER_FILLED`; DB E2E passed. |

## What Changed

- Added provider in-memory arrays for `pending_order_id -> trace_id/cmd_id/comment/strategy`.
- Stored mapping after accepted pending `LIMIT`/`STOP` order.
- On pending fill, popped mapping by MT5 pending order ticket and sent `cmd_id` in `ORDER_FILLED`.
- Relaxed journal validation from mandatory `trace_id` to one required correlation key: `trace_id`, `pending_order_id`, or `cmd_id`.
- Returned row `trace_id` from fallback update and used it for snapshot/reasoning parent writes.
- Added unit coverage for missing trace_id fallback and missing-all-keys rejection.
- Extended real PostgreSQL lifecycle E2E with normal trace fill and fallback fill without trace_id.

## Verification

- `cd D:/Aureus && python -m pytest services/aureus-trader/tests/test_journal.py -k "pending or filled or trace_id" -q`  
  Result: `8 passed, 75 deselected`.
- `wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_journal.py -k 'pending or filled or trace_id' -q && AUREUS_DB_DSN=postgresql://aureus:aureus_password@localhost:5433/aureus ./.venv/bin/python services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py"`  
  Result: tests passed; DB E2E printed `PASS limit lifecycle DB E2E ...`.
- GitNexus gates:
  - `npx gitnexus impact on_order_opened --repo Aureus --direction upstream`: CRITICAL, direct caller `on_order_filled`; mitigated by narrow validation change + unit/DB E2E.
  - `npx gitnexus impact on_order_filled --repo Aureus --direction upstream`: LOW.
  - `PushOrderFilled` and `OnTradeTransaction`: GitNexus CLI target not found for MQL5 symbols; fallback source inspection used.
  - `npx gitnexus detect_changes --repo Aureus --scope all`: expected files only.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Snapshot/reasoning writes needed resolved journal trace_id**
- **Found during:** Task 3 DB E2E.
- **Issue:** Fallback `ORDER_FILLED` without event trace_id updated journal row, then snapshot insert used `trace_id=None`, violating NOT NULL.
- **Fix:** `UPDATE ... RETURNING` now includes row `trace_id`; snapshot and reasoning inserts use row trace id.
- **Files modified:** `services/aureus-trader/journal.py`
- **Commit:** 5ccf0eb

## Auth Gates

None.

## Known Stubs

None.

## Threat Flags

None. Threat surface matches plan threat model: MT5 provider event bus, journal SQL fallback, bounded provider memory mapping.

## Self-Check: PASSED

- Summary file exists: `D:/Aureus/.planning/quick/260512-q15-th-c-hi-n-plan-l-u-trace-id-cmd-id-cho-m/260512-q15-SUMMARY.md`.
- Commits exist: `c0f79fe`, `25abd21`, `5ccf0eb`.
- Docs artifacts intentionally not committed per quick-task constraint.
