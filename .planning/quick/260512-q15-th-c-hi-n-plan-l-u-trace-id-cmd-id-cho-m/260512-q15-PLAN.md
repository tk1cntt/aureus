---
phase: 260512-q15-trace-cmd-pending-fill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mql5/AureusProvider_v2.mq5
  - services/aureus-trader/journal.py
  - services/aureus-trader/tests/test_journal.py
  - services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py
autonomous: true
requirements:
  - QUICK-260512-Q15
must_haves:
  truths:
    - "Khi MT5 nhận pending limit order, provider lưu mapping pending_order_id -> trace_id/cmd_id trong memory."
    - "Khi pending order khớp, ORDER_FILLED có trace_id/cmd_id lấy từ mapping nếu MT5 comment/history không đủ dữ liệu."
    - "Journal update được row TRIGGERED thành EXECUTED bằng trace_id, hoặc fallback pending_order_id/cmd_id khi trace_id thiếu."
    - "DB E2E chứng minh lifecycle pending placed -> filled -> closed và case filled thiếu trace_id vẫn update đúng row."
  artifacts:
    - path: "mql5/AureusProvider_v2.mq5"
      provides: "Provider-side pending order mapping and ORDER_FILLED enrichment"
      contains: "pending_order_id"
    - path: "services/aureus-trader/journal.py"
      provides: "Journal fallback update by pending_order_id/cmd_id when trace_id missing"
      contains: "on_order_opened"
    - path: "services/aureus-trader/tests/test_journal.py"
      provides: "Unit coverage for fallback filled event without trace_id"
      contains: "test_order_filled_without_trace_id"
    - path: "services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py"
      provides: "Database E2E proof for pending_order_id/cmd_id fallback"
      contains: "ORDER_FILLED"
  key_links:
    - from: "mql5/AureusProvider_v2.mq5"
      to: "ORDER_FILLED payload"
      via: "PushOrderFilled includes mapped trace_id/cmd_id for trans.order"
      pattern: "PushOrderFilled.*trace_id"
    - from: "services/aureus-trader/journal.py"
      to: "aureus_trade_journal"
      via: "on_order_opened UPDATE WHERE trace_id OR pending_order_id OR cmd_id"
      pattern: "pending_order_id = \\$9|cmd_id = \\$11"
    - from: "services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py"
      to: "PostgreSQL aureus_trade_journal"
      via: "real asyncpg insert/update assertions"
      pattern: "trace_id.*None|pending_order_id"
---

<objective>
Thực hiện lưu trace_id/cmd_id cho MT5 pending limit order, persist mapping pending_order_id -> trace_id/cmd_id, gửi ORDER_FILLED có mapping, và backend journal fallback update bằng pending_order_id/cmd_id khi thiếu trace_id.

Purpose: fix case pending limit order khớp nhưng journal không update vì ORDER_FILLED thiếu trace_id.
Output: provider mapping, journal fallback, unit test, DB E2E fallback proof.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260512-ojj-khi-t-l-nh-limit-th-id-c-a-l-nh-limit-l-/260512-ojj-REPORT.md

Assumptions:
- Worktree root: D:/Aureus.
- Provider already sends ORDER_PENDING_PLACED and ORDER_FILLED for pending orders.
- Existing report identifies exact gap: journal SQL has fallback predicates, but on_order_opened currently returns False before SQL when trace_id missing.
- Keep surgical. No broad refactor. No schema migration unless missing local DB columns in existing E2E helper.

Required GitNexus safety:
- Before editing any function/class/method, run impact analysis and report blast radius in implementation notes:
  - gitnexus_impact({target: "PushOrderFilled", direction: "upstream"})
  - gitnexus_impact({target: "OnTradeTransaction", direction: "upstream"})
  - gitnexus_impact({target: "on_order_opened", direction: "upstream"})
  - gitnexus_impact({target: "on_order_filled", direction: "upstream"}) if modified
- If GitNexus MCP unavailable, run fallback scope discovery with `npx gitnexus status`; if CLI also unavailable, document fallback and use code search/read only.
- Before commit or final completion, run gitnexus_detect_changes({scope: "all"}). If MCP lacks detect_changes, fallback to `git status --short` plus `git diff -- mql5/AureusProvider_v2.mq5 services/aureus-trader/journal.py services/aureus-trader/tests/test_journal.py services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py` and report expected files only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for ORDER_FILLED fallback without trace_id</name>
  <files>services/aureus-trader/tests/test_journal.py, services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py</files>
  <behavior>
    - Unit: on_order_filled accepts event without trace_id when pending_order_id or cmd_id exists, normalizes position_ticket into ticket/position_id, and calls UPDATE query instead of returning False early.
    - Unit: missing all three keys trace_id, pending_order_id, cmd_id still returns False.
    - DB E2E: after on_order_pending_placed stores pending_order_id/cmd_id, on_order_filled with trace_id omitted still updates same row to EXECUTED using pending_order_id/cmd_id and stores ticket, position_id, entry_deal_ticket.
  </behavior>
  <action>Add tests first. Extend TestPendingOrderLifecycle in `services/aureus-trader/tests/test_journal.py` with a focused fallback test named containing `without_trace_id` and a negative test for no correlation keys. Update `services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py` so it proves both normal trace_id fill and fallback fill without trace_id; use distinct trace/cmd/pending ids and clean them in finally. Do not change production code in this task except if test fixture import break blocks running tests. Keep assertions on SQL arguments/query shape specific enough to catch early trace_id return regression.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-trader/tests/test_journal.py -k "pending or filled or trace_id" -q</automated>
  </verify>
  <done>Tests include fallback without trace_id and fail against current production behavior before Task 2 because on_order_opened rejects missing trace_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement journal fallback update by pending_order_id/cmd_id</name>
  <files>services/aureus-trader/journal.py, services/aureus-trader/tests/test_journal.py</files>
  <behavior>
    - ORDER_OPENED/ORDER_FILLED requires at least one correlation key: trace_id, pending_order_id, or cmd_id.
    - If trace_id exists, existing trace_id path remains unchanged.
    - If trace_id missing but pending_order_id or cmd_id exists, validation continues and SQL fallback predicates execute.
    - Ticket, price, MT5 time validation remain strict.
  </behavior>
  <action>Modify `TradeJournalManager.on_order_opened` only. Replace hard failure on missing trace_id with correlation-key validation: allow missing trace_id when `event.get("pending_order_id")` or `event.get("cmd_id")` is present. Keep existing SQL fallback predicates using `$9` pending_order_id and `$11` cmd_id. Update warning message to mention missing all correlation keys, not just missing trace_id. Do not weaken ticket/open_price/time checks. Do not alter snapshot/reasoning behavior beyond what fallback update naturally enables.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-trader/tests/test_journal.py -k "pending or filled or trace_id" -q</automated>
  </verify>
  <done>Unit tests pass. ORDER_FILLED without trace_id but with pending_order_id/cmd_id reaches UPDATE and returns True with mocked DB result.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Persist provider pending mapping and run DB E2E</name>
  <files>mql5/AureusProvider_v2.mq5, services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py</files>
  <action>Add minimal provider-side memory mapping for accepted pending orders: store `pending_order_id`, `trace_id`, `cmd_id`, and optional comment/strategy metadata when ORDER_PENDING_PLACED is produced. On `OnTradeTransaction` pending fill, before `PushOrderFilled`, look up mapping by `trans.order` and pass mapped trace_id/cmd_id into ORDER_FILLED payload when current values are empty. Remove mapping after successful ORDER_FILLED send or after fill handling to avoid stale growth. Bound mapping size using an input or small constant consistent with existing `InpMaxCmdIdHistory`; do not introduce persistence beyond provider memory. Keep existing pending detection and position_ticket/deal_ticket logic unchanged. Then run unit test and DB E2E script. If compile tooling exists for MQ5 in repo, run it; if not, document that MT5 compile is manual and verify by syntax-consistent MQL5 changes only.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-trader/tests/test_journal.py -k "pending or filled or trace_id" -q</automated>
    <automated>cd D:/Aureus && python services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py</automated>
    <automated>cd D:/Aureus && git status --short</automated>
  </verify>
  <done>Provider ORDER_FILLED can carry mapped trace_id/cmd_id for pending fills; journal fallback DB E2E passes with real database; changed files limited to plan files unless compile/test artifacts already existed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 provider -> gateway/trader event bus | ORDER_FILLED/ORDER_PENDING_PLACED payloads cross process boundary and can have missing or stale correlation keys. |
| Trader event -> PostgreSQL journal | Untrusted event fields become SQL parameters for lifecycle update. |
| Provider memory mapping -> fill event | In-memory pending mapping can accidentally attach wrong trace/cmd if key reuse/stale state not bounded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260512-q15-01 | Tampering | services/aureus-trader/journal.py::on_order_opened | mitigate | Use parameterized SQL only; update rows only with `status = 'TRIGGERED'` and matching trace_id OR pending_order_id OR cmd_id. |
| T-260512-q15-02 | Spoofing | mql5/AureusProvider_v2.mq5 pending mapping | mitigate | Match mapping strictly by MT5 pending order ticket `trans.order`; remove mapping after fill; bound mapping array. |
| T-260512-q15-03 | Repudiation | ORDER_FILLED audit fields | mitigate | Preserve `pending_order_id`, `cmd_id`, `mt5_comment`, and `entry_deal_ticket` in journal update for audit. |
| T-260512-q15-04 | Denial of Service | provider mapping growth | mitigate | Bound pending mapping by existing command history style capacity; evict old entries when limit reached. |
| T-260512-q15-05 | Information Disclosure | logs/tests | accept | Trace/cmd ids are operational identifiers, not secrets; no credentials logged. |
</threat_model>

<verification>
Run all automated checks from tasks. Because journal/database behavior changes, DB E2E is required and must pass against configured PostgreSQL. If DB connection fails, consult `D:/Aureus/RUN_SERVICES.md`, start required services, then rerun DB E2E.

Final safety checks:
- gitnexus_detect_changes({scope: "all"}) or documented fallback scope check.
- Confirm only expected files changed: `mql5/AureusProvider_v2.mq5`, `services/aureus-trader/journal.py`, `services/aureus-trader/tests/test_journal.py`, `services/aureus-trader/scripts/verify_limit_order_lifecycle_db_e2e.py`, and summary file.
</verification>

<success_criteria>
- Pending order accepted event persists `pending_order_id` plus `cmd_id` in journal.
- Provider stores pending_order_id -> trace_id/cmd_id and enriches pending fill ORDER_FILLED.
- Journal does not reject ORDER_FILLED solely because trace_id is missing when pending_order_id/cmd_id is present.
- Unit tests pass for normal and fallback pending fill lifecycle.
- DB E2E proves fallback update creates real journal EXECUTED row with ticket/position_id/entry_deal_ticket.
- No unrelated refactor or broad behavior change.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260512-q15-th-c-hi-n-plan-l-u-trace-id-cmd-id-cho-m/260512-q15-SUMMARY.md`.
</output>
