---
phase: 260526-8n0-signal-snapshot-tpo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signal_event_publisher.py
  - services/aureus-signal/engine/strategy_executor.py
  - services/aureus-signal/tests/test_signal_event_publisher.py
  - services/aureus-signal/tests/test_strategy_executor_entry_price_rejection.py
  - services/aureus-trader/journal.py
  - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
autonomous: true
requirements:
  - QUICK-260526-8N0
must_haves:
  truths:
    - "STRATEGY_MATCH Redis payload data.signal_snapshot contains full TPO blocks tpo_d0, tpo_d1, tpo_d2, tpo_d3 when indicator_snapshot has them."
    - "Existing tpo_d1 partial pass-through behavior stays intact: present values win, missing nested keys fill from indicator_snapshot, and sibling blocks are not dropped."
    - "ORDER_OPENED journal persistence maps TPO blocks into aureus_trade_signal_snapshots d0/d1/d2/d3 poc/vah/val/open/high/low/close columns."
    - "DB E2E proves a real row in aureus_trade_signal_snapshots stores non-null TPO fields after on_order_opened."
  artifacts:
    - path: "services/aureus-signal/engine/signal_event_publisher.py"
      provides: "Redis STRATEGY_MATCH signal_snapshot merge/derivation for TPO blocks"
      contains: "tpo_d0"
    - path: "services/aureus-signal/engine/strategy_executor.py"
      provides: "indicator_snapshot restoration and publish path for TPO context"
      contains: "tpo_d0"
    - path: "services/aureus-trader/journal.py"
      provides: "signal_snapshot to aureus_trade_signal_snapshots TPO column mapping"
      contains: "_build_signal_snapshot_columns"
    - path: "services/aureus-signal/tests/test_signal_event_publisher.py"
      provides: "Redis payload regression tests for full TPO blocks"
    - path: "services/aureus-trader/tests/test_signal_snapshot_pipeline.py"
      provides: "DB E2E journal persistence regression for TPO columns"
  key_links:
    - from: "services/aureus-signal/engine/strategy_executor.py"
      to: "services/aureus-signal/engine/signal_event_publisher.py"
      via: "prepare_and_publish_strategy_match passes res['indicator_snapshot'] into publish_strategy_match"
      pattern: "res\['indicator_snapshot'\].*publish_strategy_match"
    - from: "services/aureus-signal/engine/signal_event_publisher.py"
      to: "Redis aureus:signals:{symbol} STRATEGY_MATCH"
      via: "data['signal_snapshot'] includes merged TPO blocks before json publish"
      pattern: "signal_snapshot.*publish_signal_event"
    - from: "services/aureus-trader/journal.py"
      to: "aureus_trade_signal_snapshots"
      via: "_build_signal_snapshot_columns extracts tpo_d0..tpo_d3 into d0..d3 columns during on_order_opened"
      pattern: "tpo_d\{day\}|d\{day\}_"
---

<objective>
Kiểm tra và fix đường truyền signal_snapshot để TPO snapshot đầy đủ đi từ indicator snapshot → Redis STRATEGY_MATCH → trader journal → aureus_trade_signal_snapshots.

Purpose: User thấy nơi có tpo_d1 truyền được thì các nơi khác phải tương tự; cần xác nhận Redis publish/read và DB persistence không mất D0/D1/D2/D3 TPO.
Output: Regression tests + surgical fixes nếu phát hiện missing TPO trên signal_snapshot hoặc mapping DB.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/services/aureus-signal/engine/signal_event_publisher.py
@D:/Aureus/services/aureus-signal/engine/strategy_executor.py
@D:/Aureus/services/aureus-signal/engine/indicator_snapshot.py
@D:/Aureus/services/aureus-trader/journal.py
@D:/Aureus/services/aureus-signal/tests/test_signal_event_publisher.py
@D:/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py

Interfaces executor cần dùng:
- signal_event_publisher._build_signal_snapshot_from_indicator_snapshot(indicator_snapshot) currently copies tpo_d0, tpo_d1, tpo_d2, tpo_d3 from indicator_snapshot when value is dict.
- signal_event_publisher._merge_signal_snapshots(base_snapshot, fallback_snapshot) preserves existing non-null fields and fills missing nested keys.
- signal_event_publisher.publish_strategy_match(redis_client, symbol, strategy_result, active_signals=None) publishes data.signal_snapshot to Redis channel aureus:signals:{symbol}.
- strategy_executor.prepare_and_publish_strategy_match(...) sets res['indicator_snapshot'] from payload before calling publish_strategy_match.
- journal._build_signal_snapshot_columns(signal_snapshot, event) maps nested tpo_d0..tpo_d3 dict fields POC/VAH/VAL/OPEN/HIGH/LOW/CLOSE into d0_*/d1_*/d2_*/d3_* columns.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace Redis signal_snapshot TPO path and add failing regression coverage</name>
  <files>services/aureus-signal/tests/test_signal_event_publisher.py, services/aureus-signal/tests/test_strategy_executor_entry_price_rejection.py</files>
  <behavior>
    - Test 1: publish_strategy_match with indicator_snapshot containing full tpo_d0/tpo_d1/tpo_d2/tpo_d3 publishes signal_snapshot containing all four blocks with POC/VAH/VAL/OPEN/HIGH/LOW/CLOSE intact.
    - Test 2: partial normalized_signal_snapshot.tpo_d1 keeps its existing values, fills missing nested keys from indicator_snapshot, and does not drop tpo_d0/tpo_d2/tpo_d3.
    - Test 3: prepare_and_publish_strategy_match preserves payload.indicator_snapshot TPO blocks into publish_strategy_match input; this proves Redis publisher receives same TPO context executor restored.
  </behavior>
  <action>Use GitNexus first: run gitnexus_query for "signal_snapshot TPO Redis STRATEGY_MATCH", then gitnexus_impact upstream for publish_strategy_match and prepare_and_publish_strategy_match; report direct callers, affected processes, risk. Add/adjust tests before production edits. Do not add fallback guesses. Verify exact current path where tpo_d1 works, then require tpo_d0/tpo_d2/tpo_d3 same way.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && python -m pytest tests/test_signal_event_publisher.py tests/test_strategy_executor_entry_price_rejection.py -q</automated>
  </verify>
  <done>Tests fail only if TPO blocks are missing/truncated before fix; coverage proves Redis payload signal_snapshot has full D0-D3 TPO and executor passes indicator_snapshot through.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix TPO propagation only where tests show gap</name>
  <files>services/aureus-signal/engine/signal_event_publisher.py, services/aureus-signal/engine/strategy_executor.py, services/aureus-signal/tests/test_signal_event_publisher.py, services/aureus-signal/tests/test_strategy_executor_entry_price_rejection.py</files>
  <behavior>
    - Existing behavior: tpo_d1 in normalized_signal_snapshot remains compatible and wins on conflicting non-null nested values.
    - New required behavior: tpo_d0/tpo_d2/tpo_d3 follow same merge/propagation semantics as tpo_d1.
    - Redis payload remains JSON-serializable and no heavy state blobs are added.
  </behavior>
  <action>If tests from Task 1 fail, patch only minimal symbols with proven gap. Likely candidates: _build_signal_snapshot_from_indicator_snapshot, _merge_signal_snapshots, _resolve_strategy_match_signal_snapshot, _restore_indicator_snapshot_context, prepare_and_publish_strategy_match. Before editing each symbol, run gitnexus_impact({target: "symbolName", direction: "upstream"}) and record blast radius. Keep surgical; no schema changes unless DB test proves missing column mapping. Do not reintroduce removed raw signal_snapshot column.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-signal && python -m pytest tests/test_signal_event_publisher.py tests/test_strategy_executor_entry_price_rejection.py -q</automated>
  </verify>
  <done>Redis STRATEGY_MATCH data.signal_snapshot always includes full available TPO blocks from indicator_snapshot and preserves existing tpo_d1 semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify trader journal DB persistence from signal_snapshot</name>
  <files>services/aureus-trader/journal.py, services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <behavior>
    - Test 1: _build_signal_snapshot_columns maps tpo_d0/tpo_d1/tpo_d2/tpo_d3 nested dict fields into all d0/d1/d2/d3 TPO columns.
    - Test 2: on_order_opened writes a real aureus_trade_signal_snapshots row with non-null D0-D3 TPO columns using AUREUS_TEST_DB_DSN.
    - Test 3: event-level flat aliases like d1_poc still work when nested tpo_d1 absent.
  </behavior>
  <action>Use GitNexus first: gitnexus_impact upstream for _build_signal_snapshot_columns and on_order_opened before editing. If existing mapping already passes, do not edit journal.py. If gap exists, fix only mapping parity so D0-D3 nested TPO and flat aliases both work. Because database persisted trade snapshot data affected, run DB E2E with real test DB; if command fails from services not running, read D:/Aureus/RUN_SERVICES.md and start required DB/Redis stack, then rerun.</action>
  <verify>
    <automated>cd D:/Aureus/services/aureus-trader && python -m pytest tests/test_signal_snapshot_pipeline.py -q</automated>
    <automated>cd D:/Aureus/services/aureus-trader && AUREUS_RUN_RUNTIME_DB_CHECK=1 python -m pytest tests/test_signal_snapshot_pipeline.py -q -m e2e</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>aureus_trade_signal_snapshots stores D0-D3 poc/vah/val/open/high/low/close from ORDER_OPENED signal_snapshot; GitNexus detects only expected symbols/files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis STRATEGY_MATCH producer to trader consumer | JSON payload crosses process boundary; malformed or incomplete signal_snapshot can corrupt persisted trade evidence. |
| Trader event to Postgres | ORDER_OPENED event data becomes rows in aureus_trade_journal and aureus_trade_signal_snapshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260526-8N0-01 | Tampering | signal_snapshot TPO blocks | mitigate | Preserve existing merge precedence and only fill missing keys; tests assert no overwrite of present tpo_d1 values. |
| T-260526-8N0-02 | Information Disclosure | Redis payload | accept | TPO OHLC/POC/VAH/VAL are market-derived trade context already used for execution/journal; no secrets added. |
| T-260526-8N0-03 | Denial of Service | Redis publish JSON serialization | mitigate | Keep signal_snapshot limited to primitive dict TPO blocks; do not add heavy state objects. |
| T-260526-8N0-04 | Repudiation | trade snapshot DB evidence | mitigate | DB E2E verifies persisted row contains non-null D0-D3 TPO fields tied to trace_id. |
</threat_model>

<verification>
Run unit tests for Redis publisher/executor path and DB E2E for trader journal path. Before any code edit, run GitNexus impact analysis on edited symbols. Before finish/commit, run gitnexus_detect_changes() and confirm changed symbols match plan scope.
</verification>

<success_criteria>
- Redis STRATEGY_MATCH payload contains full tpo_d0, tpo_d1, tpo_d2, tpo_d3 when indicator_snapshot contains them.
- Existing tpo_d1 partial merge behavior stays unchanged.
- Trader journal maps nested TPO blocks into all d0/d1/d2/d3 persisted columns.
- Real DB E2E confirms aureus_trade_signal_snapshots row creation with TPO values.
- No unrelated refactor, no raw signal_snapshot DB column reintroduced.
</success_criteria>

<output>
After completion, create D:/Aureus/.planning/quick/260526-8n0-ha-y-ki-m-tra-xem-signal-snapshot-truy-n/260526-8n0-SUMMARY.md
</output>
