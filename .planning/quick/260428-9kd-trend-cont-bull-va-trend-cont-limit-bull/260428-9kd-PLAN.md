---
phase: quick-260428-9kd-trend-cont-bull-va-trend-cont-limit-bull
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/tests/
  - services/aureus-signal/engine/
  - services/aureus-signal/scripts/
  - services/aureus-signal/**/seed*
autonomous: true
requirements:
  - QUICK-260428-9KD
must_haves:
  truths:
    - "Xác định được vì sao TREND_CONT_LIMIT_BULL không được quan sát trigger trong khi TREND_CONT_BULL trigger trên cùng điều kiện vào lệnh."
    - "TREND_CONT_LIMIT_BULL có seed/config runtime đúng, active trong DB, và đi qua registry/evaluation/dedupe/executor flow như TREND_CONT_BULL, chỉ khác entry type/entry price."
    - "Nếu có bug, fix surgical và có test/log verification chứng minh limit strategy trigger/evaluate/dispatch đúng."
  artifacts:
    - path: "services/aureus-signal/tests/"
      provides: "Regression coverage cho TREND_CONT_BULL vs TREND_CONT_LIMIT_BULL parity và flow trigger"
    - path: "services/aureus-signal/engine/"
      provides: "Registry/evaluation/dedupe/executor logic nếu cần fix"
    - path: "services/aureus-signal/**/seed*"
      provides: "Seed declaration/runtime activation nếu root cause nằm ở config"
  key_links:
    - from: "strategy seed/config"
      to: "runtime DB active strategies"
      via: "seed/apply or DB query"
      pattern: "TREND_CONT_LIMIT_BULL"
    - from: "strategy registry/evaluator"
      to: "strategy executor dispatch"
      via: "matched strategy event"
      pattern: "trend.*cont.*limit.*bull"
---

<objective>
Điều tra và fix nếu cần vì sao `TREND_CONT_LIMIT_BULL` không thấy trigger trong khi `TREND_CONT_BULL` trigger, dù hai strategy phải chung điều kiện vào lệnh và chỉ khác entry type/entry price.

Purpose: Bảo đảm limit variant không bị mất ở seed/config, registry, evaluator, dedupe, executor, hoặc runtime DB activation.
Output: Root-cause note trong summary, fix surgical nếu có bug, regression test/log verification cho `TREND_CONT_LIMIT_BULL`.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260427-wwv-validate-and-fix-trend-cont-limit-bull-a/260427-wwv-SUMMARY.md

Project rules to enforce:
- Trao đổi và summary bằng tiếng Việt.
- Khi debug code chưa quen, ưu tiên GitNexus query/context thay vì grep mở rộng.
- Trước khi sửa bất kỳ function/class/method/symbol nào, phải chạy `gitnexus_impact({target: "<symbol>", direction: "upstream"})` và ghi blast radius trong summary.
- Trước commit phải chạy `gitnexus_detect_changes()`.
- Nếu thay đổi DB/data path/seed runtime, phải test e2e với database xác nhận data active/tạo được.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace parity path and reproduce missing TREND_CONT_LIMIT_BULL trigger</name>
  <files>services/aureus-signal/tests/, services/aureus-signal/engine/, services/aureus-signal/scripts/, services/aureus-signal/**/seed*</files>
  <behavior>
    - Cùng một synthetic/current signal snapshot thỏa điều kiện trend continuation bull phải match cả `TREND_CONT_BULL` và `TREND_CONT_LIMIT_BULL` trước bước executor, trừ khi DB/config intentionally inactive.
    - Nếu `TREND_CONT_LIMIT_BULL` không match, test/log phải chỉ ra nó rớt ở seed/config, registry lookup, condition evaluation, dedupe, executor filter, hay DB activation.
    - `TREND_CONT_LIMIT_BULL` không được bị dedupe theo cùng key với `TREND_CONT_BULL` nếu hai strategy cùng hợp lệ nhưng khác entry type/entry price.
  </behavior>
  <action>Điều tra theo flow debug bắt buộc: dùng GitNexus query cho `TREND_CONT_LIMIT_BULL`, `TREND_CONT_BULL`, strategy trigger/evaluation/dedupe/executor; đọc context của các symbol nghi ngờ. Kiểm tra seed declaration từ quick 260427-wwv, registry load, evaluator condition parity, dedupe key, executor pending/limit handling, và runtime DB activation. Tạo hoặc mở rộng regression test nhỏ nhất để reproduce: một fixture/snapshot thỏa điều kiện chung phải cho thấy `TREND_CONT_BULL` và `TREND_CONT_LIMIT_BULL` cùng được xem xét/match trước khác biệt entry. Chưa sửa production logic trong task này trừ khi test đã fail đúng nguyên nhân. Nếu cần đọc DB, dùng script/command hiện có trong repo; nếu command fail, tham khảo `RUN_SERVICES.md`.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests -k "trend_cont" -x</automated>
  </verify>
  <done>Có kết luận root cause cụ thể với bằng chứng test/log: `TREND_CONT_LIMIT_BULL` rớt tại bước nào, hoặc chứng minh code path đúng nhưng runtime DB/config inactive. Regression test fail trước fix nếu có bug code/config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply surgical fix for the failing layer</name>
  <files>services/aureus-signal/engine/, services/aureus-signal/scripts/, services/aureus-signal/**/seed*, services/aureus-signal/tests/</files>
  <behavior>
    - Sau fix, `TREND_CONT_LIMIT_BULL` giữ nguyên điều kiện vào lệnh như `TREND_CONT_BULL`; chỉ khác entry type/price calculation theo thiết kế limit.
    - Seed/runtime config phải khai báo đủ strategy code, side bull, entry type limit/pending, active flag, và không bị registry bỏ qua.
    - Dedupe/executor không được nuốt limit variant chỉ vì market variant đã trigger cùng bar/snapshot.
  </behavior>
  <action>Nếu Task 1 tìm bug, sửa đúng layer nhỏ nhất. Trước khi sửa symbol nào, chạy `gitnexus_impact` cho symbol đó và ghi direct callers/affected processes/risk. Không refactor lan rộng. Trường hợp root cause là seed/DB activation thì sửa seed/config và thêm test kiểm tra seed contract; trường hợp root cause là registry/evaluator/dedupe/executor thì sửa đúng function tương ứng và cập nhật test. Nếu thay đổi đường DB/seed runtime, chạy e2e DB để xác nhận strategy active trong database sau seed/apply.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests -k "trend_cont" -x</automated>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests -k "strategy" -x</automated>
  </verify>
  <done>Regression test pass; nếu có DB/seed change thì có log/query xác nhận `TREND_CONT_LIMIT_BULL` active trong runtime DB; blast radius GitNexus đã được ghi cho mọi symbol sửa.</done>
</task>

<task type="auto">
  <name>Task 3: Verify runtime flow and summarize evidence</name>
  <files>services/aureus-signal/tests/, .planning/quick/260428-9kd-trend-cont-bull-va-trend-cont-limit-bull/260428-9kd-SUMMARY.md</files>
  <action>Chạy verification cuối: test regression, test strategy liên quan, và nếu có DB/data-path change thì e2e database theo rule project. Chạy `gitnexus_detect_changes()` trước khi commit/finish để xác nhận scope chỉ liên quan strategy seed/evaluation/dedupe/executor/test. Summary phải nêu rõ: nguyên nhân, file/symbol đã sửa hoặc lý do không cần sửa, bằng chứng `TREND_CONT_LIMIT_BULL` trigger/active, command đã chạy, và so sánh nó với `TREND_CONT_BULL`.</action>
  <verify>
    <automated>cd D:/Aureus && python -m pytest services/aureus-signal/tests -k "trend_cont or strategy" -x</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>Summary tồn tại, có command output chính, có GitNexus detect_changes scope, và kết luận rõ có/không có bug cùng trạng thái runtime của `TREND_CONT_LIMIT_BULL`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB/config → strategy runtime | Strategy seed/runtime activation quyết định code nào được evaluate/dispatch. |
| Signal snapshot → evaluator/executor | Indicator/signal data quyết định match và order request; dữ liệu thiếu/sai có thể gây trigger sai hoặc mất trigger. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260428-9KD-01 | Tampering | strategy seed/runtime DB activation | mitigate | Test seed contract và, nếu sửa seed/DB path, e2e DB query xác nhận `TREND_CONT_LIMIT_BULL` active với đúng entry type. |
| T-260428-9KD-02 | Denial of Service | dedupe/executor flow | mitigate | Regression test đảm bảo market variant không nuốt limit variant khi cả hai cùng hợp lệ. |
| T-260428-9KD-03 | Repudiation | runtime diagnosis/log evidence | mitigate | Summary ghi rõ command/log/query chứng minh trigger path và root cause. |
</threat_model>

<verification>
- `python -m pytest services/aureus-signal/tests -k "trend_cont" -x` pass.
- `python -m pytest services/aureus-signal/tests -k "strategy" -x` pass hoặc executor ghi rõ subset phù hợp hơn nếu test name trong repo khác.
- Nếu seed/DB/data path thay đổi: chạy e2e DB seed/apply/query theo command hiện có, hoặc đọc `RUN_SERVICES.md` khi service command lỗi.
- `gitnexus_detect_changes()` trước commit/finish xác nhận scope đúng.
</verification>

<success_criteria>
- Có bằng chứng xác định layer gây mất trigger: seed/config, registry, evaluator, dedupe, executor, hoặc runtime DB activation.
- Nếu bug tồn tại, đã fix surgical và regression pass.
- `TREND_CONT_LIMIT_BULL` được chứng minh active/evaluated/triggered trong điều kiện parity với `TREND_CONT_BULL`, khác entry type/price theo thiết kế.
- Không thay đổi lan rộng ngoài phạm vi trend continuation strategy trigger.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260428-9kd-trend-cont-bull-va-trend-cont-limit-bull/260428-9kd-SUMMARY.md`
</output>
