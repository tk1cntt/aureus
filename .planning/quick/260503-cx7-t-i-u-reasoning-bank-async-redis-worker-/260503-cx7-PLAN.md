---
phase: 260503-cx7-reasoning-bank-async-redis-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/main.py
  - services/aureus-trader/journal.py
  - services/aureus-trader/reasoning_embeddings.py
  - services/aureus-trader/reasoning_embedding_worker.py
  - services/aureus-trader/tests/test_journal.py
  - services/aureus-trader/tests/test_reasoning_embedding_queue.py
  - services/aureus-trader/scripts/verify_reasoning_embedding_worker_e2e.py
autonomous: true
requirements:
  - QUICK-260503-CX7
must_haves:
  truths:
    - "STRATEGY_MATCH journal path writes journal/reasoning rows without calling embedding HTTP service inline."
    - "Reasoning Bank embedding work is enqueued to Redis from real trader runtime path and processed by separate worker/subscriber."
    - "Embedding service slowness/failure cannot block MT5 ORDER_OPENED or ORDER_CLOSED lifecycle handlers."
    - "Existing execute-order gating logic remains untouched."
    - "DB/Redis runtime verification proves worker updates aureus_reasoning_entries embeddings from queued job."
  artifacts:
    - path: "services/aureus-trader/main.py"
      provides: "Runtime wiring passes existing Redis client into TradeJournalManager construction"
      contains: "TradeJournalManager(db_pool, redis_client=r)"
    - path: "services/aureus-trader/journal.py"
      provides: "Journal writes reasoning row and enqueues async embedding job only after insert"
      contains: "enqueue_reasoning_embedding_job"
    - path: "services/aureus-trader/reasoning_embeddings.py"
      provides: "Redis queue helpers plus existing embedding worker-safe functions"
      exports: ["enqueue_reasoning_embedding_job", "ReasoningEmbeddingWorker"]
    - path: "services/aureus-trader/reasoning_embedding_worker.py"
      provides: "Standalone Redis subscriber/stream worker entrypoint"
      exports: ["main"]
    - path: "services/aureus-trader/tests/test_journal.py"
      provides: "Journal STRATEGY_MATCH coverage proving runtime enqueue is not silently skipped when Redis is wired"
    - path: "services/aureus-trader/tests/test_reasoning_embedding_queue.py"
      provides: "Unit coverage for enqueue path and non-blocking failure behavior"
    - path: "services/aureus-trader/scripts/verify_reasoning_embedding_worker_e2e.py"
      provides: "DB/Redis runtime integration verification with strict assertions"
  key_links:
    - from: "services/aureus-trader/main.py"
      to: "services/aureus-trader/journal.py"
      via: "passes live Redis client `r` into TradeJournalManager so STRATEGY_MATCH runtime cannot silently skip queue"
      pattern: "TradeJournalManager\(db_pool, redis_client=r\)"
    - from: "services/aureus-trader/journal.py"
      to: "Redis reasoning embedding queue"
      via: "enqueue_reasoning_embedding_job after aureus_reasoning_entries insert"
      pattern: "enqueue_reasoning_embedding_job"
    - from: "services/aureus-trader/reasoning_embedding_worker.py"
      to: "services/aureus-trader/reasoning_embeddings.py"
      via: "worker imports and runs ReasoningEmbeddingWorker"
      pattern: "ReasoningEmbeddingWorker"
    - from: "services/aureus-trader/reasoning_embeddings.py"
      to: "aureus_reasoning_entries"
      via: "embed_reasoning_entry updates vector columns inside worker only"
      pattern: "embed_reasoning_entry"
---

<objective>
Tối ưu Reasoning Bank embedding thành async Redis worker để STRATEGY_MATCH/journal không gọi embedding trực tiếp.

Purpose: Giữ luồng gửi lệnh sang MT5, ORDER_OPENED, ORDER_CLOSED không bị stuck bởi embedding service chậm/lỗi.
Output: Queue helper, runtime Redis wiring, worker entrypoint, test unit, DB/Redis runtime E2E.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/CLAUDE.md
@D:/Aureus/RUN_SERVICES.md
@D:/Aureus/.planning/STATE.md
@D:/Aureus/.planning/quick/260503-cc6-implement-theo-plan-planning-quick-26050/260503-cc6-SUMMARY.md
@D:/Aureus/services/aureus-trader/main.py
@D:/Aureus/services/aureus-trader/journal.py
@D:/Aureus/services/aureus-trader/reasoning_embeddings.py

<interfaces>
From services/aureus-trader/main.py current runtime construction point:
```python
r = redis.Redis(...)
...
journal = TradeJournalManager(db_pool)
...
dispatcher = OrderDispatcher(r, config, journal_manager=journal)
```
Executor must wire existing runtime Redis client `r` into `TradeJournalManager`, e.g. `TradeJournalManager(db_pool, redis_client=r)`. Do not leave runtime STRATEGY_MATCH with `redis_client=None`.

From services/aureus-trader/journal.py current risk point:
```python
from reasoning_embeddings import ReasoningEmbeddingClient, embed_reasoning_entry, select_embedding_sources
...
sources = select_embedding_sources({...})
await embed_reasoning_entry(conn, reasoning_entry_id, sources, ReasoningEmbeddingClient())
```
Executor must remove this direct/synchronous embedding call from journal path.

From services/aureus-trader/reasoning_embeddings.py existing functions to preserve:
```python
class ReasoningEmbeddingClient:
    def embed(self, text): ...

def select_embedding_sources(row): ...
async def embed_reasoning_entry(conn, entry_id, sources, client=None): ...
```
Preserve `embed_reasoning_entry` for worker/backfill callers. Do not change execute-order gating logic or files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Redis queue contract and non-blocking enqueue tests</name>
  <files>services/aureus-trader/reasoning_embeddings.py, services/aureus-trader/tests/test_reasoning_embedding_queue.py</files>
  <behavior>
    - Test 1: `enqueue_reasoning_embedding_job(redis, entry_id, sources, trace_id)` writes one JSON job to Redis stream/list/channel with `entry_id`, `trace_id`, and selected text sources.
    - Test 2: empty source dict returns false/0 and does not enqueue.
    - Test 3: Redis publish/xadd/lpush exception is caught/logged and returns false/0 so caller can continue.
  </behavior>
  <action>Before editing symbols, run GitNexus impact for `select_embedding_sources` and `embed_reasoning_entry` with upstream direction, then report direct callers/risk in summary. Add queue helper beside existing embedding helpers. Prefer Redis Stream `XADD` if current Redis client supports it; otherwise use list `LPUSH/RPUSH` with explicit queue key constant such as `aureus:reasoning:embedding_jobs`. Helper must never call `ReasoningEmbeddingClient` or `embed_reasoning_entry`; it only serializes job. Keep existing `embed_reasoning_entry` behavior for worker/backfill.</action>
  <verify>
    <automated>wsl -d Ubuntu-24.04 -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_reasoning_embedding_queue.py -v"</automated>
  </verify>
  <done>Queue helper exists, unit tests prove enqueue contract and failure does not raise/block caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace journal inline embedding and wire Redis into runtime manager</name>
  <files>services/aureus-trader/journal.py, services/aureus-trader/main.py, services/aureus-trader/tests/test_journal.py</files>
  <behavior>
    - Test 1: `on_strategy_match` creates `aureus_reasoning_entries` and calls enqueue helper, not `ReasoningEmbeddingClient` or `embed_reasoning_entry`.
    - Test 2: enqueue exception/false result is logged but `on_strategy_match` still returns true when journal insert succeeds.
    - Test 3: real trader startup construction in `main.py` passes existing Redis client into `TradeJournalManager`, so STRATEGY_MATCH runtime path cannot silently skip queue.
    - Test 4: `on_order_opened` and `on_order_closed` tests still pass; no embedding dependency added there.
  </behavior>
  <action>Before editing `TradeJournalManager.__init__`, `TradeJournalManager.on_strategy_match`, or trader startup construction, run `gitnexus_impact({target: "TradeJournalManager", direction: "upstream"})`, `gitnexus_impact({target: "on_strategy_match", direction: "upstream"})`, and impact for the startup symbol in `main.py` if GitNexus names one; report blast radius. Change import in `journal.py` to remove `ReasoningEmbeddingClient` and `embed_reasoning_entry`; keep/use `select_embedding_sources` only if needed to build job. Add `redis_client` storage to `TradeJournalManager` and make `on_strategy_match` enqueue `{entry_id, trace_id, sources}` to Redis through helper after reasoning row insert. Runtime path must not silently skip queue: update `services/aureus-trader/main.py` where `r = redis.Redis(...)` and `journal = TradeJournalManager(db_pool)` exist, changing construction to pass live Redis client (`TradeJournalManager(db_pool, redis_client=r)` or equivalent). Keep `redis_client=None` only for isolated tests/scripts that do not exercise runtime startup, and log warning if STRATEGY_MATCH has sources but no Redis client. Add/update `services/aureus-trader/tests/test_journal.py` so one test constructs manager with fake Redis and asserts STRATEGY_MATCH enqueue happens, and one test covers `main.py`/startup wiring by monkeypatching `TradeJournalManager` or inspecting construction arguments. Do not alter execute-order gating logic, dispatch_order, order validation, or MT5 command creation. Do not call embedding HTTP service from STRATEGY_MATCH/journal path.</action>
  <verify>
    <automated>wsl -d Ubuntu-24.04 -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_journal.py services/aureus-trader/tests/test_reasoning_embedding_queue.py -v"</automated>
  </verify>
  <done>`journal.py` has no direct `ReasoningEmbeddingClient()` or `await embed_reasoning_entry(...)`; `main.py` wires live Redis into `TradeJournalManager`; STRATEGY_MATCH with Redis enqueues a job; STRATEGY_MATCH remains successful when enqueue fails; ORDER_OPENED/ORDER_CLOSED behavior unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add worker entrypoint and strict DB/Redis runtime verification</name>
  <files>services/aureus-trader/reasoning_embeddings.py, services/aureus-trader/reasoning_embedding_worker.py, services/aureus-trader/scripts/verify_reasoning_embedding_worker_e2e.py, services/aureus-trader/tests/test_reasoning_embedding_queue.py</files>
  <behavior>
    - Test 1: worker consumes one queued job, calls `embed_reasoning_entry(conn, entry_id, sources, client)`, and acknowledges/removes job only after success.
    - Test 2: embedding exception does not crash process; job remains retryable or is moved/logged per existing simple retry policy.
    - Test 3: runtime E2E creates DB reasoning row, queues job in Redis, runs worker one-shot, then asserts `reasoning_embedding` is non-null in `aureus_reasoning_entries`.
  </behavior>
  <action>Before editing/creating worker symbol, run GitNexus impact for `embed_reasoning_entry` upstream and include risk in summary. Add `ReasoningEmbeddingWorker` in `reasoning_embeddings.py` or small worker module if cleaner; it must own embedding client and DB pool in worker process, not journal process. Add `services/aureus-trader/reasoning_embedding_worker.py` with `main()` reading env vars (`AUREUS_DB_DSN`, `REDIS_URL` or existing Redis host/port env style, embedding URL/model if already used). Support one-shot mode for tests, e.g. `--once --timeout 10`. Add strict E2E script using asyncpg + Redis that fails on wrong response, missing DB update, or worker error. No inline Python in command; script file only, per RUN_SERVICES.md.</action>
  <verify>
    <automated>wsl -d Ubuntu-24.04 -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_reasoning_embedding_queue.py -v && AUREUS_DB_DSN=postgresql://aureus:aureus_password@localhost:5433/aureus REDIS_URL=redis://127.0.0.1:6380 ./.venv/bin/python services/aureus-trader/scripts/verify_reasoning_embedding_worker_e2e.py --redis-url redis://127.0.0.1:6380 --timeout 20"</automated>
  </verify>
  <done>Worker can be launched separately and processes queued embedding jobs; DB/Redis E2E proves queued job updates vector columns; embedding failure cannot block journal or MT5 lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STRATEGY_MATCH Redis -> journal DB | Untrusted event payload becomes journal/reasoning DB rows and queue job. |
| Trader runtime Redis -> TradeJournalManager | Existing Redis connection must be passed into journal manager; missing wiring causes silent queue loss. |
| Redis queue -> embedding worker | Worker consumes queued JSON that can be malformed or stale. |
| Worker -> embedding HTTP service | External local service can be slow, down, or return invalid vector. |
| Worker -> Timescale/Postgres | Worker updates `aureus_reasoning_entries` vector fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260503-cx7-01 | D | journal.py STRATEGY_MATCH | mitigate | Enqueue helper catches Redis failures and returns false; journal result remains true when DB insert succeeds. |
| T-260503-cx7-02 | T | Redis queue payload | mitigate | Worker validates job JSON, `entry_id`, and non-empty source text before embedding/update. |
| T-260503-cx7-03 | D | embedding HTTP service | mitigate | Embedding call occurs only in worker with timeout; failures logged/retryable and never block ORDER_OPENED/ORDER_CLOSED. |
| T-260503-cx7-04 | I | reasoning source payload | accept | Payload contains existing reasoning/prompt/context already persisted in DB; no new external exposure beyond local Redis. |
| T-260503-cx7-05 | E | execute-order gating | mitigate | Plan explicitly forbids touching gating/dispatch files; verify with diff/name check. |
| T-260503-cx7-06 | R | main.py runtime wiring | mitigate | Test proves trader startup passes live Redis client into `TradeJournalManager`; summary records evidence so enqueue cannot be silently absent in runtime STRATEGY_MATCH path. |
</threat_model>

<verification>
Overall checks:
1. `journal.py` no longer imports or instantiates `ReasoningEmbeddingClient` and no longer awaits `embed_reasoning_entry`.
2. `main.py` constructs `TradeJournalManager` with live Redis client used by `OrderDispatcher`, not `redis_client=None`.
3. `embed_reasoning_entry` remains available only for worker/backfill/direct scripts, not STRATEGY_MATCH journal path.
4. Unit tests cover enqueue success/failure, journal non-blocking behavior, and runtime startup Redis wiring.
5. DB/Redis runtime E2E creates row, enqueues job, runs worker once, verifies `aureus_reasoning_entries.reasoning_embedding IS NOT NULL`.
6. Run scope guard: `git diff --name-only | grep -Ei "execute.*order|order.*gate|gating|dispatch_order"` must return no unintended execute-order gating changes.
7. Before commit, run GitNexus detect changes if available: `npx gitnexus detect_changes --repo Aureus`; if CLI lacks command, record fallback `git status` + `git diff --name-only` + impact evidence.
</verification>

<success_criteria>
- STRATEGY_MATCH/journal path performs DB insert + Redis enqueue only; no direct embedding HTTP call.
- Trader runtime wires Redis into `TradeJournalManager`, so STRATEGY_MATCH cannot silently skip queue because manager lacks Redis.
- Separate worker/subscriber processes queued jobs and updates `aureus_reasoning_entries` embeddings.
- Embedding service down/slow cannot block MT5 ORDER_OPENED/ORDER_CLOSED or execute-order dispatch path.
- Execute-order gating logic unchanged.
- Runtime DB/Redis verification passes with strict assertions.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260503-cx7-t-i-u-reasoning-bank-async-redis-worker-/260503-cx7-SUMMARY.md`
</output>
