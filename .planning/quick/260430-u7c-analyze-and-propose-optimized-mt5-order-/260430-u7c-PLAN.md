---
phase: quick-260430-u7c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md
autonomous: true
requirements:
  - QUICK-260430-U7C
must_haves:
  truths:
    - "Báo cáo giải thích được vì sao latency hiện tại có thể lên tới khoảng 30s và order phía sau bị block."
    - "Báo cáo phân biệt phần cần giữ và phần đang over-complicated trong pipeline trader/gateway/MT5."
    - "Báo cáo đưa ra các option kiến trúc cụ thể để đạt mục tiêu send-to-MT5 1-2s và tránh block order khác."
    - "Báo cáo đánh giá trực tiếp ý tưởng one process per order và kết luận có nên dùng hay không."
    - "Báo cáo khuyến nghị option tốt nhất kèm lộ trình triển khai theo phase và safety constraints."
  artifacts:
    - path: ".planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md"
      provides: "Vietnamese architecture recommendation report for optimized MT5 order dispatch"
      min_lines: 80
  key_links:
    - from: "services/aureus-trader/dispatcher.py"
      to: ".planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md"
      via: "Evidence for sequential dispatch, ACK/result wait, retry/backoff, and head-of-line blocking"
      pattern: "dispatch_loop|dispatch_order|_wait_for_response|result_timeout|ack_timeout"
    - from: "services/aureus-gateway/main.py"
      to: ".planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md"
      via: "Evidence for Redis COMMANDS_CHANNEL to TCP writer bridge"
      pattern: "run_command_subscriber|aureus:mt5:commands|writer\.drain"
    - from: "mql5/AureusProvider_v2.mq5"
      to: ".planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md"
      via: "Evidence for OnTimer command polling, ACK/NACK, dedup, duplicate strategy lock, OrderSend/result semantics"
      pattern: "ProcessIncomingCommands|ExecuteOpenOrder|SendACK|SendNACK|RecordCmdId|PushOrderOpened|PushOrderFailed"
---

<objective>
Tạo báo cáo phân tích và đề xuất kiến trúc tối ưu luồng dispatch order MT5 để giảm send-to-MT5 latency về mục tiêu 1-2s, đồng thời tránh việc một order timeout/retry block các order khác.

Purpose: Người dùng cần quyết định kiến trúc trước khi sửa code, đặc biệt quanh trade-off giữa worker pool/state machine/per-symbol queue và ý tưởng one process per order.
Output: `.planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md` bằng tiếng Việt, chỉ report, không sửa source code, không database, không commit.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260430-tqm-analyze-order-dispatch-flow-after-build-/260430-tqm-REPORT.md
@.planning/quick/260424-sbg-ph-n-ti-ch-nguy-n-nh-n-timeout-cu-a-lu-n/260424-sbg-REPORT.md
@services/aureus-trader/main.py
@services/aureus-trader/dispatcher.py
@services/aureus-trader/order_builder.py
@services/aureus-trader/config.py
@services/aureus-gateway/main.py
@services/aureus-trader/tests/test_dispatcher.py
@mql5/AureusProvider_v2.mq5

<interfaces>
Key source contracts/evidence the executor must use directly:

- `services/aureus-trader/main.py`: `run_trader()` starts `dispatcher.dispatch_loop()` and `dispatcher.event_listener()` as separate asyncio tasks, then converts `STRATEGY_MATCH` to order command and calls `dispatcher.enqueue_order(order_cmd)`.
- `services/aureus-trader/dispatcher.py`: `dispatch_loop()` does `lpop` then `await self.dispatch_order(order)`, so one dispatcher instance dispatches outbound orders sequentially. `dispatch_order()` publishes to `COMMANDS_CHANNEL`, waits ACK/NACK using `ack_timeout`, then waits final result using `result_timeout`; retry sleeps use exponential backoff.
- `services/aureus-gateway/main.py`: `run_command_subscriber()` subscribes to `aureus:mt5:commands`, finds `active_connections[symbol]`, writes payload to each TCP writer, and awaits `writer.drain()`.
- `mql5/AureusProvider_v2.mq5`: `OnTimer()` calls `ProcessIncomingCommands()`, which receives socket JSON and calls `ExecuteOpenOrder(raw)`. `ExecuteOpenOrder()` sends NACK for invalid/duplicate/strategy existing/trade disabled paths, sends ACK before `OrderCheck`/`OrderSend`, records cmd id, then pushes `ORDER_OPENED`/`ORDER_FAILED` result. Symbol not in `InpSymbols` currently returns silently because `SendNACK(..., "SYMBOL_NOT_ALLOWED")` is commented.
- Previous evidence: `260430-tqm-REPORT.md` already reconstructed the pipeline and established head-of-line blocking. `260424-sbg-REPORT.md` documented real timeout pattern with default `ack_timeout=5`, `result_timeout=30`, `max_retries=3`, and `NACK:DUPLICATE` after retry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build latency/blocking diagnosis and architecture option matrix</name>
  <files>.planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md</files>
  <action>
    Create the report in Vietnamese. Do not edit source files and do not change database. Base the diagnosis on the listed context files, especially `dispatcher.py`, `aureus-gateway/main.py`, `AureusProvider_v2.mq5`, `260430-tqm-REPORT.md`, and `260424-sbg-REPORT.md`.

    The report must include these sections:
    1. Executive summary: current architecture is simple but suffers head-of-line blocking; target is not true parallel MT5 execution, but 1-2s command handoff to gateway/MT5 plus safe async result handling.
    2. Current flow and latency budget: show pipeline `STRATEGY_MATCH -> build_order_command -> enqueue Redis list -> dispatch_loop -> COMMANDS_CHANNEL -> gateway TCP writer -> MT5 OnTimer/ProcessIncomingCommands -> ExecuteOpenOrder -> ACK/result`. Explain exact latency/blocking points: empty-queue sleep up to 0.5s, sequential `await dispatch_order`, ACK wait default 5s, result wait default 30s, retry/backoff ~27s for repeated ACK timeout, result timeout ~30s after ACK, gateway `writer.drain()`, MT5 single-threaded `OnTimer`, and silent no-NACK symbol path.
    3. Necessary vs over-complicated: keep idempotent `cmd_id`, payload allowlist, Redis channel, ACK/NACK/result semantics, provider-side symbol/magic/direction duplicate lock, journal correlation. Identify over-complication/risks: global FIFO queue waiting for final result before next send, retry in same dispatch loop, treating result timeout as simple rejection, ambiguous duplicate after ACK loss, using `ORDER_OPENED` semantics for pending if still present, and one global queue for unrelated symbols.
    4. Architecture options to target 1-2s send-to-MT5 and non-blocking per-order handling. Include at least these options with pros/cons/risks/test needs:
       - Option A: minimal hardening only: faster NACK for silent paths, shorter command handoff metrics, keep sequential. Explain why insufficient for non-blocking.
       - Option B: bounded in-flight dispatcher worker pool/semaphore, publish multiple orders but cap concurrency. Include why this reduces head-of-line blocking but still needs locks and correlation.
       - Option C: per-symbol or per symbol+magic+direction lanes with one in-flight per lane, lanes run concurrently. Explain this as preferred balance for trading safety.
       - Option D: state-machine dispatcher: publish/register pending immediately, do not wait for final result in queue loop; event listener transitions states and journal/result handling async. Explain highest architecture cleanliness but more implementation scope.
       - Option E: one process per order. Evaluate user's idea directly: benefits (fault isolation, no Python await blocking across orders), risks (process explosion, startup overhead, duplicate Redis consumers, hard correlation, MT5 terminal remains single-threaded per EA/account, harder idempotency/reconcile/observability), conclusion: not recommended as primary architecture; use bounded tasks/lanes instead.
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('D:/Aureus/.planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md')
text = p.read_text(encoding='utf-8')
required = [
    '30s', '1-2s', 'head-of-line', 'dispatch_loop', 'dispatch_order',
    'ack_timeout', 'result_timeout', 'writer.drain', 'OnTimer',
    'one process per order', 'symbol', 'magic', 'direction',
    'idempotency', 'ACK', 'result', 'recommended'
]
missing = [s for s in required if s not in text]
assert not missing, missing
assert len(text.splitlines()) >= 80
PY</automated>
  </verify>
  <done>Report exists, is Vietnamese, cites concrete code evidence, and answers why current flow can take ~30s plus where blocking is introduced; it also separates necessary vs over-complicated parts and compares concrete architecture options including one-process-per-order.</done>
</task>

<task type="auto">
  <name>Task 2: Recommend best architecture with phased implementation and safety constraints</name>
  <files>.planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md</files>
  <action>
    Extend the same report with a clear recommendation section. Recommend one best option, not a vague list. Preferred recommendation should be justified against the current code: `per-symbol + magic + direction lane scheduler` combined with bounded in-flight async state machine, unless the analysis finds a stronger reason otherwise. Keep it implementation-ready but still report-only.

    The recommendation must include:
    1. Why the best option can meet 1-2s send-to-MT5 latency: dispatch loop should publish/register pending quickly and continue to next independent lane instead of waiting up to `result_timeout`.
    2. Safety constraints that any implementation must preserve:
       - lane/lock identity: `symbol + magic + direction` for strategy entry/DCA isolation.
       - idempotency: deterministic `cmd_id`, Redis dedup, provider dedup, duplicate ACK/recovery handling.
       - ACK/result semantics: ACK only means command accepted by provider, final result means order side effect, result timeout after ACK is ambiguous and must not be classified as simple business reject.
       - no unbounded concurrency; bounded in-flight limit and per-lane single active command.
       - reconcile on ambiguous timeout by querying MT5 positions/orders/history using `cmd_id`, `trace_id`, `symbol`, `magic`, direction/comment where possible.
       - preserve provider duplicate strategy lock and `InpSymbols` validation; silent no-response must become explicit NACK in a later implementation task.
    3. Phased implementation steps:
       - Phase 1: observability and explicit NACK/no-response fixes; add latency metrics for enqueue age, publish-to-ACK, ACK-to-result, gateway write latency.
       - Phase 2: dispatcher lane scheduler with bounded concurrent in-flight tasks; tests simulating ACK timeout on lane A while lane B still publishes within 1-2s.
       - Phase 3: result state machine and reconcile for `RESULT_TIMEOUT_AFTER_ACK` / `ACK_LOST_DUPLICATE_RECOVERY`.
       - Phase 4: optional optimization only if measured bottleneck remains at gateway/MT5, e.g. per-symbol gateway writers or provider event improvements.
    4. Explicitly state what not to do now: do not spawn one OS process per order; do not remove idempotency/retry; do not allow multiple same symbol+magic+direction entries; do not treat MT5 as fully parallel just because Python is async.
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('D:/Aureus/.planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md')
text = p.read_text(encoding='utf-8')
required = [
    'Khuyến nghị', 'per-symbol', 'magic', 'direction', 'bounded',
    'state machine', 'RESULT_TIMEOUT_AFTER_ACK', 'ACK_LOST_DUPLICATE_RECOVERY',
    'reconcile', 'không spawn', 'one OS process per order', 'Phase 1', 'Phase 2', 'Phase 3'
]
missing = [s for s in required if s not in text]
assert not missing, missing
PY</automated>
  </verify>
  <done>Report gives a single best recommended architecture with concrete phased steps and safety constraints covering symbol/magic/direction lock, idempotency, bounded concurrency, ACK/result semantics, and timeout reconciliation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis signal pub/sub -> trader | Untrusted/duplicated strategy events can produce duplicate order intents. |
| Trader -> Redis COMMANDS_CHANNEL -> gateway | Command handoff can be delayed/lost/duplicated; wrong concurrency can amplify risk. |
| Gateway TCP -> MT5 provider | Socket delivery and `writer.drain()` can delay command forwarding; MT5 terminal/EA is not a general worker pool. |
| MT5 provider -> order side effects | ACK/result semantics cross from messaging into actual broker side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260430-u7c-01 | D | `OrderDispatcher.dispatch_loop` | mitigate | Report must identify head-of-line blocking and recommend bounded lanes/state machine rather than global sequential wait. |
| T-260430-u7c-02 | T/R | `cmd_id` ACK/result correlation | mitigate | Report must preserve idempotency and define explicit ambiguous timeout classes plus reconcile. |
| T-260430-u7c-03 | E/T | Concurrent order dispatch | mitigate | Report must require `symbol+magic+direction` lock and bounded in-flight concurrency; no unbounded process/task spawning. |
| T-260430-u7c-04 | R/I | Observability gaps | mitigate | Report must require correlation logs/metrics at enqueue, publish, gateway write, ACK, result, timeout, and provider receive. |
</threat_model>

<verification>
Run the two automated Python checks in the tasks. Additionally verify manually by reading the report and confirming it directly answers all five required questions from the user constraints. No source code changes, no database changes, no git commit.
</verification>

<success_criteria>
- `.planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-REPORT.md` exists.
- Report is in Vietnamese.
- Report explains current 30s latency/blocking causes with file/function evidence.
- Report separates necessary pieces from over-complicated pieces.
- Report evaluates multiple architecture options for 1-2s MT5 handoff and non-blocking order handling.
- Report evaluates one process per order and clearly recommends for/against it.
- Report gives one best option with phased implementation steps and safety constraints: symbol/magic/direction lane lock, idempotency, ACK/result semantics, bounded concurrency, and reconcile.
- No source code files are modified.
- No database changes are made.
- No commit is created.
</success_criteria>

<output>
After completion, create `.planning/quick/260430-u7c-analyze-and-propose-optimized-mt5-order-/260430-u7c-SUMMARY.md` summarizing the report-only work.
</output>
