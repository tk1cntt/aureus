---
phase: quick-260424-a6a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-trader/dispatcher.py
  - services/aureus-trader/tests/test_dispatcher.py
autonomous: true
requirements:
  - QUICK-260424-A6A
must_haves:
  truths:
    - "Khi dispatch_order xử lý ORDER_OPENED, trace_id dùng cho journal không còn là None."
    - "trace_id được lấy sau khi await self.journal.on_strategy_match(strategy_payload) nếu đây là điểm trace_id được tạo/chuẩn hóa."
    - "Luồng hiện tại không bị regression ở nhánh ORDER_FAILED/ACK."
  artifacts:
    - path: "services/aureus-trader/dispatcher.py"
      provides: "Sửa thứ tự lấy trace_id trong nhánh ORDER_OPENED để lấy giá trị sau on_strategy_match"
    - path: "services/aureus-trader/tests/test_dispatcher.py"
      provides: "Test chứng minh trace_id không None sau khi on_strategy_match được await"
  key_links:
    - from: "services/aureus-trader/dispatcher.py::dispatch_order"
      to: "TradeJournalManager.on_strategy_match"
      via: "await call + cập nhật payload/trace_id hậu xử lý"
      pattern: "await self.journal.on_strategy_match\(strategy_payload\)"
    - from: "services/aureus-trader/dispatcher.py::dispatch_order"
      to: "TradeJournalManager.on_order_opened"
      via: "trace_id đã được resolve trước khi ghi order_opened"
      pattern: "on_order_opened"
---

<objective>
Fix nhanh lỗi `trace_id = None` trong luồng ORDER_OPENED bằng cách chỉnh đúng thứ tự xử lý: chỉ đọc/đồng bộ `trace_id` sau khi `await self.journal.on_strategy_match(strategy_payload)` hoàn tất.

Purpose: Đảm bảo journal liên kết strategy_match -> order_opened bằng trace_id hợp lệ, không ghi record thiếu định danh.
Output: 1 chỉnh sửa tối thiểu trong dispatcher + test tự động tái hiện và chặn lỗi hồi quy.
</objective>

<context>
@D:/Aureus/services/aureus-trader/dispatcher.py
@D:/Aureus/services/aureus-trader/tests/test_dispatcher.py
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Viết test tái hiện bug trace_id None theo thứ tự xử lý hiện tại</name>
  <files>services/aureus-trader/tests/test_dispatcher.py</files>
  <behavior>
    - Test 1: Với ORDER_OPENED, khi strategy payload ban đầu chưa có trace_id nhưng on_strategy_match tạo/điền trace_id, giá trị trace_id dùng ở bước tiếp theo phải khác None.
    - Test 2: on_order_opened nhận payload có trace_id đã resolve, không phải None.
  </behavior>
  <action>Thêm test focused cho dispatch_order bằng mock journal để mô phỏng việc trace_id chỉ có sau await on_strategy_match; test phải fail trước khi sửa production code.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -k "trace_id and strategy_match" -x</automated>
  </verify>
  <done>Có test RED tái hiện đúng lỗi trace_id None do lấy quá sớm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sửa dispatcher để resolve trace_id sau await on_strategy_match</name>
  <files>services/aureus-trader/dispatcher.py</files>
  <behavior>
    - dispatch_order không dùng trace_id lấy trước khi await on_strategy_match trong nhánh ORDER_OPENED.
    - Sau await on_strategy_match, dispatcher đọc lại trace_id từ nguồn đã được journal chuẩn hóa/cập nhật rồi mới dùng cho bước journal/order tiếp theo.
    - Không đổi hành vi các nhánh khác ngoài phạm vi bug này.
  </behavior>
  <action>Chỉnh sửa tối thiểu trong nhánh ORDER_OPENED: dời/cập nhật điểm lấy trace_id sang sau await self.journal.on_strategy_match(strategy_payload); giữ nguyên cấu trúc hiện có, không thêm abstraction mới.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -k "trace_id and strategy_match" -x</automated>
  </verify>
  <done>Test Task 1 chuyển GREEN và trace_id trong luồng ORDER_OPENED không còn None.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy regression test dispatcher để xác nhận fix không gây side effects</name>
  <files>services/aureus-trader/tests/test_dispatcher.py</files>
  <action>Chạy lại toàn bộ test dispatcher để xác nhận các nhánh ACK/ORDER_FAILED/retry vẫn pass sau fix thứ tự trace_id.</action>
  <verify>
    <automated>cd /d/Aureus && pytest services/aureus-trader/tests/test_dispatcher.py -x</automated>
  </verify>
  <done>Toàn bộ test dispatcher pass, fix được xác nhận là atomic và không regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis/order event -> dispatcher | Event input có thể thiếu trace_id tại thời điểm đầu xử lý |
| dispatcher -> journal manager | Sai thứ tự await có thể làm mất liên kết trace_id giữa strategy_match và order_opened |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-260424-a6a-01 | T | services/aureus-trader/dispatcher.py | mitigate | Chỉ lấy/dùng trace_id sau await on_strategy_match để tránh ghi dữ liệu chưa hoàn chỉnh |
| T-quick-260424-a6a-02 | R | services/aureus-trader/dispatcher.py | mitigate | Đảm bảo trace_id được propagate đầy đủ sang bước on_order_opened để audit trail không đứt |
| T-quick-260424-a6a-03 | D | services/aureus-trader/tests/test_dispatcher.py | accept | Quick fix không thay đổi kiến trúc queue/retry; kiểm soát bằng regression test focused |
</threat_model>

<verification>
- Repro + fix test: `pytest services/aureus-trader/tests/test_dispatcher.py -k "trace_id and strategy_match" -x`
- Dispatcher regression: `pytest services/aureus-trader/tests/test_dispatcher.py -x`
</verification>

<success_criteria>
- Lỗi trace_id None ở luồng ORDER_OPENED được tái hiện bằng test và đã được sửa.
- dispatcher chỉ dùng trace_id sau khi await on_strategy_match hoàn tất.
- Test focused và regression dispatcher đều pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260424-a6a-v-n-l-i-trace-id-none-trace-id-c-ta-o-ra/260424-a6a-SUMMARY.md`
</output>
