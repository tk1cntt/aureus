---
quick_id: 260507-8i3
mode: quick-full
status: planned
must_haves:
  truths:
    - aureus_trade_signal_snapshots persists D1 POC, D1 VAH, and D1 VAL when source indicator/signal snapshot contains D1 TPO values.
    - Database schema supports the new persisted D1 POC/VAH/VAL fields.
    - Database/e2e verification creates or reads a real aureus_trade_signal_snapshots row with non-null D1 POC/VAH/VAL values.
  artifacts:
    - Migration/schema change if columns do not already exist.
    - Source mapping/persistence change for D1 POC/VAH/VAL.
    - Regression test for D1 POC/VAH/VAL persistence.
    - Executor summary.
    - Verification report.
  key_links:
    - source D1 TPO payload -> snapshot persistence insert/upsert via mapped POC/VAH/VAL fields
    - snapshot persistence -> aureus_trade_signal_snapshots via D1 POC/VAH/VAL columns
    - DB/e2e verification -> real snapshot row with non-null D1 POC/VAH/VAL query
    - .planning/STATE.md
    - RUN_SERVICES.md
---

# Quick Task 260507-8i3 Plan

## Goal

Persist D1 POC, VAH, and VAL data into `aureus_trade_signal_snapshots`, with schema support and DB/e2e proof that new snapshot rows store non-null values.

## Tasks

### Task 1 — Inspect existing snapshot schema and source D1 TPO payload

- **files**: `services/**`, database migrations/schema files, tests near trade signal snapshot persistence.
- **action**: Find `aureus_trade_signal_snapshots` write path and existing TPO/D1 indicator payload names. Confirm whether columns for D1 POC/VAH/VAL exist. Use GitNexus query/context; before editing any symbol, run `gitnexus_impact({target: "<symbol>", direction: "upstream"})` and record blast radius.
- **verify**: Document exact source field names and destination column names in summary.
- **done**: Clear mapping contract exists: source D1 POC/VAH/VAL -> `aureus_trade_signal_snapshots` columns.

### Task 2 — Add schema and persistence mapping

- **files**: Migration/schema file if needed; snapshot persistence source file(s); focused tests.
- **action**: Add minimal columns if absent, following project naming conventions. Wire D1 POC/VAH/VAL from existing indicator/signal snapshot data into insert/upsert payload. Add regression test asserting values reach DB insert payload or persistence layer.
- **verify**: Run focused pytest for the discovered snapshot persistence test path, expected form: `wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest <snapshot_persistence_test_path> -q"`; executor must replace `<snapshot_persistence_test_path>` with the actual test file/function it adds or updates.
- **done**: Source and schema persist D1 POC/VAH/VAL without changing unrelated snapshot fields.

### Task 3 — Run DB/e2e verification and commit code

- **files**: Changed source, migration/schema, test files.
- **action**: Run DB/e2e verification through WSL per `RUN_SERVICES.md`, creating or selecting a real `aureus_trade_signal_snapshots` row with non-null D1 POC/VAH/VAL. Run GitNexus detect changes if available; if unavailable, document CLI/MCP blocker. Commit code changes atomically. Write summary to `.planning/quick/260507-8i3-persist-d1-tpo-snapshot/260507-8i3-SUMMARY.md`.
- **verify**: DB query output shows non-null D1 POC/VAH/VAL; commit succeeds.
- **done**: Code committed, summary written, DB proof captured.
