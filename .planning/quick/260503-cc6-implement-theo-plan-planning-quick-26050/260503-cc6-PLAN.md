---
phase: 260503-cc6-implement-reasoning-bank-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-db-writer/migrations/add_reasoning_entries_join_indexes.sql
  - services/aureus-trader/reasoning_embeddings.py
  - services/aureus-trader/journal.py
  - services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py
autonomous: true
requirements:
  - QUICK-260503-CC6
must_haves:
  truths:
    - "Reasoning Bank reads strategy, lifecycle, outcome, and signal snapshot context by joining aureus_reasoning_entries to aureus_trade_journal and aureus_trade_signal_snapshots."
    - "aureus_reasoning_entries still exists and keeps memory-specific text, digest/hash, embeddings, source semantics, confidence/action, timestamps, and join ids."
    - "Write path no longer depends on duplicated lifecycle/outcome fields in aureus_reasoning_entries for Reasoning Bank queries."
    - "execute-order gating behavior and files remain untouched."
    - "DB/runtime E2E creates journal, signal snapshot, reasoning entry, then queries joined Reasoning Bank data successfully."
  artifacts:
    - path: "services/aureus-db-writer/migrations/add_reasoning_entries_join_indexes.sql"
      provides: "Safe FK/index formalization for trade_journal_id and signal_snapshot_id joins without deleting aureus_reasoning_entries"
    - path: "services/aureus-trader/reasoning_embeddings.py"
      provides: "Joined Reasoning Bank read model for insight fetch and semantic search"
    - path: "services/aureus-trader/journal.py"
      provides: "Reasoning entry write path preserving memory-specific fields and join ids while stopping nonessential duplicate lifecycle/outcome updates"
    - path: "services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py"
      provides: "Runtime DB E2E for journal + snapshot + reasoning join and query behavior"
  key_links:
    - from: "services/aureus-trader/reasoning_embeddings.py"
      to: "aureus_trade_journal"
      via: "LEFT JOIN on aureus_reasoning_entries.trade_journal_id"
      pattern: "LEFT JOIN aureus_trade_journal"
    - from: "services/aureus-trader/reasoning_embeddings.py"
      to: "aureus_trade_signal_snapshots"
      via: "LEFT JOIN on aureus_reasoning_entries.signal_snapshot_id"
      pattern: "LEFT JOIN aureus_trade_signal_snapshots"
    - from: "services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py"
      to: "services/aureus-trader/journal.py"
      via: "TradeJournalManager lifecycle calls"
      pattern: "on_strategy_match.*on_order_opened.*on_order_closed"
---

<objective>
Tối ưu Reasoning Bank để reuse data từ `aureus_trade_journal` và `aureus_trade_signal_snapshots` theo plan `260503-bvr-REASONING-BANK-DATA-OPTIMIZATION.md`.

Purpose: giảm duplicate/drift trong Reasoning Bank read path, giữ `aureus_reasoning_entries` làm memory index mỏng, không đổi execute-order gating.

Output: migration join indexes/FK, joined read model trong `reasoning_embeddings.py`, write-path cleanup giữ memory fields + join ids, DB/runtime E2E xác nhận create/query đúng.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Aureus/.planning/STATE.md
@D:/Aureus/CLAUDE.md
@D:/Aureus/.planning/quick/260503-bvr-ki-m-tra-2-b-ng-aureus-trade-journal-v-a/260503-bvr-REASONING-BANK-DATA-OPTIMIZATION.md
@D:/Aureus/services/aureus-db-writer/migrations/add_trade_journal.sql
@D:/Aureus/services/aureus-db-writer/migrations/add_trade_evaluations.sql
@D:/Aureus/services/aureus-db-writer/migrations/add_reasoning_entries.sql
@D:/Aureus/services/aureus-db-writer/migrations/add_reasoning_entries_embeddings.sql
@D:/Aureus/services/aureus-trader/journal.py
@D:/Aureus/services/aureus-trader/reasoning_embeddings.py
@D:/Aureus/services/aureus-trader/scripts/verify_reasoning_bank_db_e2e.py
@D:/Aureus/services/aureus-trader/scripts/verify_reasoning_bank_embeddings_e2e.py

<constraints>
- Không xoá bảng `aureus_reasoning_entries`.
- Không drop duplicate columns trong task này.
- Không thay đổi execute-order gating files/logic. Trước khi sửa, grep/query để xác định files gating và tránh touch.
- `aureus_reasoning_entries` phải giữ `id`, `trace_id`, `trade_journal_id`, `signal_snapshot_id`, `reasoning_source`, `decision_action`, `confidence`, `reasoning_text`, `prompt_text`, `context_text`, `prompt_digest`, `decision_digest`, `input_context_hash`, `reasoning_embedding`, `prompt_embedding`, `context_embedding`, `embedding_model`, `embedded_at`, `created_at`, `evaluated_at`, `updated_at`.
- Telegram/read model mặc định không expose raw `prompt_text`/`context_text`.
- Mọi thay đổi liên quan DB phải có DB/runtime E2E thật xác nhận tạo data và query đúng.
- Trước khi sửa symbol/function/class/method, chạy `gitnexus_impact({target: "<symbol>", direction: "upstream"})`, ghi blast radius trong summary. Nếu tool báo index stale, chạy `npx gitnexus analyze` từ `D:/Aureus` rồi retry.
- Trước commit/chốt, chạy `gitnexus_detect_changes()` và xác nhận scope chỉ gồm Reasoning Bank/journal/migration/E2E files mong đợi.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Formalize Reasoning Bank join storage without destructive schema changes</name>
  <files>services/aureus-db-writer/migrations/add_reasoning_entries_join_indexes.sql, services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py</files>
  <behavior>
    - Migration adds safe indexes for `aureus_reasoning_entries.trade_journal_id` and `aureus_reasoning_entries.signal_snapshot_id` if missing.
    - Migration adds FK constraints only if safe/idempotent, with orphan-tolerant precheck or `NOT VALID` then validation only when valid pattern matches existing migration style.
    - Migration never drops `aureus_reasoning_entries` and never drops duplicate columns.
    - E2E test can apply/use schema, insert lifecycle data, and query join keys.
  </behavior>
  <action>Read existing migration style, then create idempotent migration `add_reasoning_entries_join_indexes.sql` for `trade_journal_id`/`signal_snapshot_id` indexes and safe FK formalization from `aureus_reasoning_entries` to `aureus_trade_journal(id)` and `aureus_trade_signal_snapshots(id)`. Do not alter execute-order gating. Start E2E script skeleton `verify_reasoning_bank_reuse_e2e.py` with setup/cleanup helpers and assertions that fail until joined read model exists. Before editing any function in existing scripts copied/adapted, run GitNexus impact for that symbol. New script symbols do not need prior impact, but any reused/modified existing symbol does.</action>
  <verify>
    <automated>python services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py --help</automated>
    <automated>git diff -- services/aureus-db-writer/migrations/add_reasoning_entries_join_indexes.sql services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py</automated>
  </verify>
  <done>Migration is idempotent and non-destructive; E2E script exists with DB setup/cleanup path and initial assertions for journal + snapshot + reasoning joins; no gating files touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move Reasoning Bank reads to joined journal and signal snapshot model</name>
  <files>services/aureus-trader/reasoning_embeddings.py, services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py</files>
  <behavior>
    - `fetch_strategy_reasoning_insights()` filters/groups by `aureus_trade_journal.strategy_name`, `symbol`, and `direction` when join ids exist.
    - Stats derive success/reward/outcome from `aureus_trade_journal.result`, `pnl_pips`, and `pnl`, not duplicated reasoning lifecycle fields.
    - `semantic_search_reasoning_entries()` keeps embedding/text from `aureus_reasoning_entries` but returns display/scope fields from joined journal and timeframe/signal metadata from joined snapshot.
    - Raw `prompt_text` and `context_text` are not returned to Telegram/default insight callers.
  </behavior>
  <action>Run GitNexus impact for `fetch_strategy_reasoning_insights` and `semantic_search_reasoning_entries`, report d=1 callers and affected processes in task summary before edits. Refactor SQL in `reasoning_embeddings.py` to use `aureus_reasoning_entries re LEFT JOIN aureus_trade_journal tj ON tj.id = re.trade_journal_id LEFT JOIN aureus_trade_signal_snapshots ts ON ts.id = re.signal_snapshot_id`. Keep backward compatibility only where rows lack join ids via `COALESCE(tj.field, re.field)` for scope display/filter if existing callers need it; do not introduce new feature behavior. Ensure reward uses `COALESCE(tj.pnl_pips, tj.pnl)` and success uses `CASE WHEN tj.result = 'WIN' THEN TRUE WHEN tj.result = 'LOSS' THEN FALSE ELSE NULL END`. Extend E2E script to assert joined fields win over intentionally divergent duplicate fields in reasoning entries.</action>
  <verify>
    <automated>python services/aureus-trader/scripts/verify_reasoning_bank_embeddings_e2e.py</automated>
    <automated>python services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py</automated>
  </verify>
  <done>Reasoning Bank read queries use joined journal/snapshot as source of truth; semantic search still uses reasoning embeddings; default insight output excludes raw prompt/context; E2E proves joined fields are used.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stop nonessential duplicate lifecycle updates while preserving memory fields and runtime E2E</name>
  <files>services/aureus-trader/journal.py, services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py</files>
  <behavior>
    - `on_strategy_match()` still creates `aureus_trade_journal` and `aureus_reasoning_entries` with memory fields and `trade_journal_id`/`trace_id`.
    - `on_order_opened()` still creates `aureus_trade_signal_snapshots` and links `aureus_reasoning_entries.signal_snapshot_id`.
    - `on_order_closed()` still closes journal and keeps memory lifecycle metadata such as `evaluated_at`, but Reasoning Bank query correctness no longer depends on duplicated outcome fields in reasoning entries.
    - No execute-order gating code or behavior changes.
  </behavior>
  <action>Run GitNexus impact for `TradeJournalManager.on_strategy_match`, `TradeJournalManager.on_order_opened`, and `TradeJournalManager.on_order_closed`; report direct callers, affected processes, and risk. In `journal.py`, keep writes for memory-specific fields, embeddings, `trade_journal_id`, `signal_snapshot_id`, `trace_id`, and `evaluated_at`. Stop or minimize updates of duplicate lifecycle/outcome fields in `aureus_reasoning_entries` only after E2E parity proves joined read model works; do not drop columns. Preserve execute-order gating by not editing order gating files/logic. Finish `verify_reasoning_bank_reuse_e2e.py` to run runtime lifecycle: insert/trigger `on_strategy_match`, `on_order_opened`, `on_order_closed`, assert journal row exists, snapshot row exists, reasoning row exists with memory fields + join ids, joined Reasoning Bank insight/query returns journal/snapshot values, semantic search remains searchable when embeddings service/test fixtures are available or existing embedding E2E pattern is used.</action>
  <verify>
    <automated>python services/aureus-trader/scripts/verify_reasoning_bank_db_e2e.py</automated>
    <automated>python services/aureus-trader/scripts/verify_reasoning_bank_embeddings_e2e.py</automated>
    <automated>python services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>Runtime DB E2E creates and joins journal + signal snapshot + reasoning entry; Reasoning Bank query returns joined source-of-truth values; memory fields and join ids remain in `aureus_reasoning_entries`; no execute-order gating files changed; GitNexus change detection matches expected scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB write path -> PostgreSQL | Trade/journal/snapshot/reasoning payload crosses into persistent store. |
| Reasoning Bank query -> Telegram/insight caller | Stored prompt/context/reasoning data crosses into user-facing output. |
| Embedding search input -> vector query | Query text/embedding enters semantic search SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260503-cc6-01 | Tampering | `aureus_reasoning_entries.trade_journal_id`, `signal_snapshot_id` | mitigate | Add FK/index migration and E2E assert joins point to created journal/snapshot rows. |
| T-260503-cc6-02 | Information Disclosure | `prompt_text`, `context_text` | mitigate | Keep stored for memory/search but do not expose in default insight/Telegram result projection. |
| T-260503-cc6-03 | Repudiation | lifecycle/outcome source-of-truth | mitigate | Joined read model derives outcome from `aureus_trade_journal`; E2E asserts joined values win over divergent duplicate reasoning fields. |
| T-260503-cc6-04 | Denial of Service | joined Reasoning Bank queries | mitigate | Add indexes on join ids and keep query scoped by strategy/symbol/direction/limit. |
| T-260503-cc6-05 | Elevation of Privilege | execute-order gating | accept | Scope explicitly forbids gating edits; `gitnexus_detect_changes()` and diff review verify no gating files changed. |
</threat_model>

<verification>
Run in order from `D:/Aureus` after implementation:

1. `python services/aureus-trader/scripts/verify_reasoning_bank_db_e2e.py`
2. `python services/aureus-trader/scripts/verify_reasoning_bank_embeddings_e2e.py`
3. `python services/aureus-trader/scripts/verify_reasoning_bank_reuse_e2e.py`
4. `git diff --name-only` and confirm no execute-order gating files changed.
5. `gitnexus_detect_changes()` and confirm affected scope is Reasoning Bank/journal/migration/E2E only.

If any command fails due service startup, read `D:/Aureus/RUN_SERVICES.md` and start required DB/runtime services exactly per project instruction.
</verification>

<success_criteria>
- `aureus_reasoning_entries` remains present and keeps memory-specific fields, embeddings, prompt/context/reasoning, and join ids.
- Reasoning Bank insight and semantic search read strategy/lifecycle/outcome/signal context through joins to `aureus_trade_journal` and `aureus_trade_signal_snapshots`.
- Duplicate lifecycle/outcome fields are not required for correct Reasoning Bank query results.
- DB/runtime E2E creates `aureus_trade_journal`, `aureus_trade_signal_snapshots`, `aureus_reasoning_entries`, then queries Reasoning Bank correctly through joins.
- Existing DB and embedding E2E scripts pass.
- No execute-order gating files/logic changed.
- GitNexus impact analysis and detect_changes evidence is recorded in summary.
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/quick/260503-cc6-implement-theo-plan-planning-quick-26050/260503-cc6-SUMMARY.md`.
</output>
