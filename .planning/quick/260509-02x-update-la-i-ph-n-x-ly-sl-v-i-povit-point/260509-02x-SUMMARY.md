---
quick_id: 260509-02x
phase: quick
plan: 260509-02x
type: quick-summary
status: completed
completed_at: 2026-05-08
---

# Quick Task 260509-02x Summary

## Goal

Update PIVOT_POINT SL distance guard: `_get_pivot_sl_max_distance` chỉ áp dụng cho USTEC/NAS, XAU, BTC. Các symbol khác vẫn lấy pivot SL bình thường, không bị reject bởi fallback `20 * point_size`.

## Changes

- `services/aureus-signal/engine/orders.py`
  - `_get_pivot_sl_max_distance(symbol)` đổi return type sang `Optional[float]`.
  - XAU giữ `10.0`.
  - USTEC/NAS giữ `50.0`.
  - BTC giữ `500.0`.
  - Symbol còn lại return `None`.
  - PIVOT_POINT branch chỉ reject khi `max_distance is not None and sl_distance > max_distance`.

- `services/aureus-signal/tests/test_pivot_sl.py`
  - Đổi forex test từ reject `20 * point_size` sang allow pivot SL bình thường.
  - Giữ tests reject cho XAU/USTEC/BTC.

## GitNexus Impact

```text
npx gitnexus impact _get_pivot_sl_max_distance --repo Aureus --direction upstream
risk: CRITICAL
impactedCount: 4
direct: 1
processes_affected: 11
modules_affected: Engine, Tests, Tools
```

Direct caller:

- `_calculate_sl_tp(...)`

Affected process path includes:

- `process_triggers`
- `prepare_and_publish_strategy_match`
- `run_strategy_executor`

Mitigation:

- Surgical helper change only.
- Focused PIVOT_POINT unit tests pass.

## Verification

```text
python -m pytest services/aureus-signal/tests/test_pivot_sl.py -q
23 passed in 5.77s
```

`git diff --check` passed.

GitNexus detect-changes unavailable:

```text
npx gitnexus detect-changes --repo Aureus
error: unknown command 'detect-changes'
```

## Result

PIVOT_POINT SL now keeps normal pivot value for non-special symbols. Distance guard remains only for XAU, USTEC/NAS, and BTC.
