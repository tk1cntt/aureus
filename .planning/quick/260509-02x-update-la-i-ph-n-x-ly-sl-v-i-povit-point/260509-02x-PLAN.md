---
quick_id: 260509-02x
phase: quick
plan: 260509-02x
type: quick-full
wave: 1
depends_on:
  - 260506-rkn
files_modified:
  - services/aureus-signal/engine/orders.py
  - services/aureus-signal/tests/test_pivot_sl.py
  - .planning/quick/260509-02x-update-la-i-ph-n-x-ly-sl-v-i-povit-point/260509-02x-SUMMARY.md
  - .planning/quick/260509-02x-update-la-i-ph-n-x-ly-sl-v-i-povit-point/260509-02x-VERIFICATION.md
  - .planning/STATE.md
autonomous: true
status: planned
must_haves:
  truths:
    - `_get_pivot_sl_max_distance` distance guard must apply only to USTEC/NAS, XAU, and BTC symbols.
    - Other symbols must keep normal PIVOT_POINT SL selected from pivot price, without 20 * point_size distance rejection.
    - Existing XAU/USTEC/BTC distance limits must remain unchanged.
    - Change must be covered by unit tests for non-special symbol pass-through and existing special-symbol guard behavior.
  artifacts:
    - Minimal source change in `services/aureus-signal/engine/orders.py`.
    - Updated tests in `services/aureus-signal/tests/test_pivot_sl.py`.
    - Summary and verification reports.
    - STATE.md update and commit.
  key_links:
    - `services/aureus-signal/engine/orders.py` -> `_get_pivot_sl_max_distance(symbol)`
    - `services/aureus-signal/engine/orders.py` -> `_calculate_sl_tp(...)` PIVOT_POINT branch
    - `services/aureus-signal/tests/test_pivot_sl.py` -> PIVOT_POINT distance tests
---

# Quick Task 260509-02x Plan

## Goal

Update PIVOT_POINT SL logic so `_get_pivot_sl_max_distance` only enforces max-distance guard for USTEC/NAS, XAU, and BTC. Other symbols must continue using normal pivot-point SL value instead of rejecting by fallback `20 * point_size` threshold.

## Assumptions

- User wrote `POVIT_POINT`; intended `PIVOT_POINT` SL mode in `services/aureus-signal/engine/orders.py`.
- “Các symbol còn lại vẫn lấy giá trị PIVOT POINT bình thường” means no max-distance rejection for other symbols.
- Keep existing hard limits: XAU `10.0`, USTEC/NAS `50.0`, BTC `500.0`.

## GitNexus Impact

Before edit:

```text
npx gitnexus impact _get_pivot_sl_max_distance --repo Aureus --direction upstream
risk: CRITICAL
impactedCount: 4
direct: _calculate_sl_tp
processes_affected: 11
modules_affected: Engine, Tests, Tools
```

Blast radius:

- Direct caller: `_calculate_sl_tp(...)` PIVOT_POINT branch.
- Indirect path: `process_triggers`, `prepare_and_publish_strategy_match`, `run_strategy_executor`.
- Risk reason: order SL/TP calculation feeds strategy execution flow.
- Mitigation: surgical helper change + focused PIVOT_POINT tests.

## Tasks

<task type="auto">
  <name>Change pivot SL max-distance semantics</name>
  <files>
    services/aureus-signal/engine/orders.py
  </files>
  <action>
    Change `_get_pivot_sl_max_distance(symbol)` to return a numeric threshold only for XAU, USTEC/NAS, and BTC. Return `None` for other symbols. Update `_calculate_sl_tp(...)` PIVOT_POINT branch to skip distance rejection when threshold is `None`.
  </action>
  <verify>
    Existing XAU/USTEC/BTC thresholds unchanged. EURUSD or other non-special symbols no longer reject solely because distance exceeds `20 * point_size`.
  </verify>
  <done>
    Non-special symbols keep PIVOT_POINT SL normally.
  </done>
</task>

<task type="auto">
  <name>Update unit tests</name>
  <files>
    services/aureus-signal/tests/test_pivot_sl.py
  </files>
  <action>
    Replace old forex rejection expectation with a pass-through expectation. Keep XAU, USTEC, BTC rejection tests.
  </action>
  <verify>
    Run focused PIVOT_POINT tests.
  </verify>
  <done>
    Tests encode intended symbol-specific guard behavior.
  </done>
</task>

<task type="auto">
  <name>Verify and document</name>
  <files>
    .planning/quick/260509-02x-update-la-i-ph-n-x-ly-sl-v-i-povit-point/260509-02x-SUMMARY.md
    .planning/quick/260509-02x-update-la-i-ph-n-x-ly-sl-v-i-povit-point/260509-02x-VERIFICATION.md
    .planning/STATE.md
  </files>
  <action>
    Run focused tests, attempt GitNexus detect-changes, write summary/verification, update STATE.md, commit expected files only.
  </action>
  <verify>
    Tests pass, artifacts exist, STATE row added, commit created.
  </verify>
  <done>
    Quick task completed with atomic commit.
  </done>
</task>
