---
phase: "39.1"
plan: "03"
subsystem: aureus-signal
tags: [signal-history, truncate-limit, state]
dependency_graph:
  requires: []
  provides: "Signal history truncate limit increased to 500"
  affects:
    - services/aureus-signal/engine/state.py
tech_stack:
  added: []
  patterns: "Single-line threshold change"
key_files:
  created: []
  modified:
    - services/aureus-signal/engine/state.py
decisions:
  - "Changed truncate limit from 240 to 500 per decision D-03 from CONTEXT.md"
  - "No other changes needed — truncation mechanism (append + pop(0)) remains identical"
metrics:
  duration_minutes: 2
  completed_date: "2026-04-10"
---

# Phase 39.1 Plan 03: Increase Signal History Truncate Limit to 500

## One-liner

Increased `log_signal_normalize` bounded list truncate threshold from 240 to 500 in `state.py`, doubling the lookback buffer before events are evicted, reducing auto-reset frequency for strategy sequence matching.

## Tasks Completed

| # | Task | Commit | Files Changed |
|---|------|--------|---------------|
| 1 | Change truncate limit from 240 to 500 | `ed98370` | `state.py` (1 line) |
| 2 | Run full test suite for regressions | `ed98370` | No test changes needed |

## Execution Details

### Task 1: Change truncate limit

Single-line change in `services/aureus-signal/engine/state.py`, line 228:
- **Before:** `if len(self.log_signal_normalize) > 240:`
- **After:** `if len(self.log_signal_normalize) > 500:`

Verification:
- `py_compile` passes on `state.py`
- Grep confirms `> 500` on line 228
- No other hardcoded 240 references exist for this purpose (the only `240` in engine code is `M4: 240` in `outside_bar.py`, a timeframe constant)

### Task 2: Run test suite

All 16 tests in `test_template_strategy.py` pass (0.51s). The truncate limit is only used when adding events to the log, and tests use small numbers of events (<10), so no test was affected by the change.

## Deviations from Plan

None - plan executed exactly as written.

## Threat Surface Scan

Per threat register T-39.1-04 and T-39.1-05: Redis stream payload increases from ~318KB to ~660KB per symbol. With 8 symbols, total ~5.3MB vs ~2.5MB previously. Well within Redis stream capacity. No new endpoints, auth paths, or file access patterns introduced.

## Known Stubs

None — this is a single-line threshold change, no stubs created.

## Self-Check: PASSED

- File exists: `services/aureus-signal/engine/state.py` confirmed
- Commit exists: `ed98370` confirmed
- py_compile passes
- All 16 tests pass
