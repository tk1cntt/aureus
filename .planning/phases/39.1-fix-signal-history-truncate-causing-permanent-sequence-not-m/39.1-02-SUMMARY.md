---
phase: "39.1"
plan: "02"
type: execute
subsystem: aureus-signal
tags: [logging, sequence-matching, observability]
dependency:
  requires: []
  provides: [INFO-level SEQUENCE_NOT_MATCHED logging]
  affects: [services/aureus-signal/engine/strategies/registry.py]
tech-stack:
  added: []
  patterns: [selective log level upgrade]
key-files:
  created: []
  modified:
    - services/aureus-signal/engine/strategies/registry.py
decisions:
  - "Additive logger.info alongside existing logger.debug (not replacement) to preserve verbose detail"
metrics:
  duration: "5min"
  completed: "2026-04-10T13:00:00Z"
  tasks_completed: 2
  tasks_total: 2
---

# Phase 39.1 Plan 02: Upgrade SEQUENCE_NOT_MATCHED logging to INFO level

## One-liner

Added selective INFO-level logging for SEQUENCE_NOT_MATCHED rejection in registry.py evaluate_all, enabling operators to detect strategy stall conditions without enabling verbose debug logging.

## Objective

Upgrade SEQUENCE_NOT_MATCHED rejection logging from DEBUG to INFO level so operators can see why strategies stall without enabling verbose debug logging.

## Tasks Completed

| Task | Name | Commit | Files Modified |
|------|------|--------|----------------|
| 1 | Upgrade SEQUENCE_NOT_MATCHED logging to INFO level | d610651 | services/aureus-signal/engine/strategies/registry.py |
| 2 | Verify existing tests pass and no new log output breaks behavior | (verified, no code change needed) | - |

## Changes Made

### registry.py -- evaluate_all method (line 348-357)

Added conditional `logger.info` call after existing `logger.debug` for the SEQUENCE_NOT_MATCHED rejection path. The INFO log includes diagnostic fields: `matched_steps`, `total_steps`, `current_step_index`, `missing_required_tags`, `mismatch_reason`.

The existing `logger.debug` call is preserved -- this is additive, not a replacement. All other rejection reason codes remain at DEBUG level only.

## Verification Results

- **py_compile:** PASSED on registry.py
- **logger.info with SEQUENCE_NOT_MATCHED:** PRESENT (line 350)
- **Only SEQUENCE_NOT_MATCHED triggers INFO:** CONFIRMED -- guarded by `if intent_reason == "SEQUENCE_NOT_MATCHED"`
- **Existing tests:** 16 failures were pre-existing (TemplateStrategy attribute changes unrelated to this plan). Zero new failures introduced.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None. T-39.1-03 (Availability / INFO logging volume) assessed in plan as accept risk -- 6 strategies x 8 symbols x 1 candle = 48 INFO messages per candle, manageable at M15 intervals.

## Self-Check: PASSED

- registry.py file exists: CONFIRMED
- Commit d610651 exists: CONFIRMED
- SUMMARY.md file created: CONFIRMED
