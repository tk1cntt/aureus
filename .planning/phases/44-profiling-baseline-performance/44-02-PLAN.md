---
phase: 44-profiling-baseline-performance
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - services/aureus-signal/engine/signals/structure.py
  - services/aureus-signal/tests/test_ob_numpy.py
  - services/aureus-signal/tests/test_structure_replay_regression.py
autonomous: true
requirements:
  - PH44-PERF
  - PH44-PARITY
must_haves:
  truths:
    - "Structure processor giảm latency trung bình tối thiểu 40% trên replay dataset chuẩn (điều kiện tương đương)."
    - "Tối ưu A (array access) không làm lệch parity so với old path trên bộ replay contract."
    - "Có evidence kiểm thử tự động để quyết định promote mode từ shadow sang on."
  artifacts:
    - path: services/aureus-signal/engine/signals/structure.py
      provides: A-first optimization (array access) giữ nguyên logic trading conditions
    - path: services/aureus-signal/tests/test_structure_replay_regression.py
      provides: Replay regression benchmark + parity gate 40%
    - path: services/aureus-signal/tests/test_ob_numpy.py
      provides: Regression coverage cho OB/swing behavior sau tối ưu
  key_links:
    - from: services/aureus-signal/tests/test_structure_replay_regression.py
      to: services/aureus-signal/engine/signals/structure.py
      via: replay old vs new path with deterministic dataset
      pattern: "replay|parity|avg_ms"
    - from: optimization branch in structure.py
      to: mode on/shadow from plan 44-01
      via: shared runtime branch execution
      pattern: "off|shadow|on"
---

<objective>
Triển khai tối ưu A (data-access array-centric) cho StructureSignal và chốt gate hiệu năng >=40% mà vẫn giữ strict parity.

Purpose: Đạt mục tiêu latency phase 44 theo quyết định người dùng, sau khi safety layer đã có ở Plan 01.
Output: Hot-path structure tối ưu + replay regression chứng minh parity/perf.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-profiling-baseline-performance/44-CONTEXT.md
@.planning/phases/44-profiling-baseline-performance/44-RESEARCH.md
@.planning/phases/44-profiling-baseline-performance/44-VALIDATION.md
@.planning/phases/44-profiling-baseline-performance/44-01-SUMMARY.md
@services/aureus-signal/engine/signals/structure.py
@services/aureus-signal/tests/test_ob_numpy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo replay regression test cho parity + performance gate</name>
  <files>services/aureus-signal/tests/test_structure_replay_regression.py</files>
  <read_first>
- services/aureus-signal/engine/signals/structure.py
- services/aureus-signal/tests/test_structure_parity_shadow.py
- services/aureus-signal/tests/test_ob_numpy.py
- .planning/phases/44-profiling-baseline-performance/44-CONTEXT.md
- .planning/phases/44-profiling-baseline-performance/44-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: Replay dataset chuẩn chạy old/new cho output contract identical (per D-01).
    - Test 2: Đo avg_ms path old và optimized trong cùng điều kiện replay; optimized phải giảm >=40% (per D-04).
    - Test 3: Nếu parity mismatch xuất hiện, test phải fail kèm diff context để chặn promote.
  </behavior>
  <action>Trước khi sửa symbol chính trong `structure.py`, chạy `gitnexus_impact({target: "StructureSignal._emit", direction: "upstream"})` để nắm blast radius theo CLAUDE.md. Sau đó tạo file `test_structure_replay_regression.py` dùng dataset replay cố định (khóa symbol + time-window theo context phase 44), chạy benchmark so sánh old path và optimized path trong cùng process để giảm nhiễu. Assert cứng `optimized_avg_ms <= old_avg_ms * 0.60` (tương đương giảm >=40% per D-04) và assert parity full contract; test thất bại nếu mismatch hoặc không đạt ngưỡng.</action>
  <acceptance_criteria>
- File `test_structure_replay_regression.py` tồn tại và có assertion gate `<= 0.60` cho avg_ms.
- Test parity trong file này so đầy đủ contract output, không chỉ so count/shape.
- Output fail message có `symbol`, `t` hoặc diff field để debug nhanh.
  </acceptance_criteria>
  <verify>
    <automated>pytest services/aureus-signal/tests/test_structure_replay_regression.py -q -x</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>Replay regression gate cho D-01 và D-04 sẵn sàng trước khi tối ưu code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tối ưu hot-path StructureSignal theo A-first (array access)</name>
  <files>services/aureus-signal/engine/signals/structure.py, services/aureus-signal/tests/test_ob_numpy.py</files>
  <read_first>
- services/aureus-signal/engine/signals/structure.py
- services/aureus-signal/tests/test_structure_replay_regression.py
- services/aureus-signal/tests/test_ob_numpy.py
- .planning/phases/44-profiling-baseline-performance/44-CONTEXT.md
- .planning/phases/44-profiling-baseline-performance/44-RESEARCH.md
  </read_first>
  <action>Trước khi sửa symbol chính trong `structure.py`, chạy `gitnexus_impact({target: "StructureSignal._emit", direction: "upstream"})` để xác nhận direct callers/risk depth d=1 theo CLAUDE.md. Refactor nội bộ `structure.py` theo hướng A (per D-02): thay các điểm đọc DataFrame row-by-row (`iloc`, `iterrows`) bằng truy cập mảng (`to_numpy()`/array index), giữ nguyên điều kiện breakout/mitigation/OB, giữ nguyên side-effects lên `state.transient_signals`, `state.obs`, `state.swing_points`. Không triển khai incremental delta scan (B) trong plan này. Cập nhật `test_ob_numpy.py` để khóa các edge-case OB/swing dễ drift dtype/index khi chuyển array.</action>
  <acceptance_criteria>
- `structure.py` giảm phụ thuộc truy cập DataFrame row-wise ở hot path, thay bằng array access.
- `test_ob_numpy.py` có assertion cho edge-case biên liên quan OB/swing không bị lệch sau tối ưu.
- Không thêm dependency mới; chỉ dùng pandas/numpy hiện có.
  </acceptance_criteria>
  <verify>
    <automated>pytest services/aureus-signal/tests/test_ob_numpy.py services/aureus-signal/tests/test_structure_replay_regression.py -q -x</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>Tối ưu A hoàn tất, parity giữ nguyên và pass regression replay/perf gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataFrame market window → array hot-path | Chuyển kiểu truy cập dữ liệu có thể tạo lệch logic do dtype/index drift |
| structure output → strategy downstream | Sai parity sẽ tác động tín hiệu và quyết định giao dịch kế tiếp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | T | array access optimization | mitigate | Khóa parity bằng replay contract tests trước/sau và comparator full fields |
| T-44-05 | D | replay performance gate | mitigate | Dùng dataset cố định + assert cứng `optimized <= 0.60 * old` để chặn degrade |
| T-44-06 | R | perf evidence integrity | mitigate | Test fail message bắt buộc có context symbol/time/diff để audit quyết định promote |
</threat_model>

<verification>
- pytest services/aureus-signal/tests/test_structure_replay_regression.py -q -x
- pytest services/aureus-signal/tests/test_ob_numpy.py -q -x
- pytest services/aureus-signal/tests -k "structure and (parity or shadow or replay_regression or ob_numpy)" -q -x
</verification>

<success_criteria>
- D-02 được triển khai đầy đủ: tối ưu A (array access), không lẫn B.
- D-04 đạt gate: avg_ms structure giảm >=40% trên replay chuẩn trong test tự động.
- Không có parity drift trên contract output và side-effects state quan trọng.
</success_criteria>

<output>
After completion, create `.planning/phases/44-profiling-baseline-performance/44-02-SUMMARY.md`
</output>
