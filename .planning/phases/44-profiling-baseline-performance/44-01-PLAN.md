---
phase: 44-profiling-baseline-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signals/structure.py
  - services/aureus-signal/tests/test_structure_parity_shadow.py
  - services/aureus-signal/tests/test_structure_mode_flag.py
autonomous: true
requirements:
  - PH44-PARITY
  - PH44-MODE
must_haves:
  truths:
    - "User vận hành có thể bật/tắt tối ưu structure bằng mode off|shadow|on mà không đổi behavior mặc định."
    - "Shadow mode luôn so sánh old/new output contract và tự fallback old path khi mismatch."
    - "Parity checker bao phủ đầy đủ transient_signals, obs, swing_points để tránh mismatch ngầm."
  artifacts:
    - path: services/aureus-signal/engine/signals/structure.py
      provides: Feature flag runtime mode + shadow comparator + fallback old path
    - path: services/aureus-signal/tests/test_structure_mode_flag.py
      provides: Test mode off|shadow|on và default safety
    - path: services/aureus-signal/tests/test_structure_parity_shadow.py
      provides: Test parity contract + mismatch fallback behavior
  key_links:
    - from: services/aureus-signal/engine/signals/structure.py
      to: AUREUS_STRUCTURE_OPT_MODE
      via: env parsing and runtime branch selection
      pattern: "AUREUS_STRUCTURE_OPT_MODE"
    - from: services/aureus-signal/engine/signals/structure.py
      to: old/new structure execution paths
      via: shadow compare then fallback on mismatch
      pattern: "shadow|fallback|mismatch"
---

<objective>
Thiết lập runtime-safety layer cho tối ưu StructureSignal theo quyết định người dùng: mode off|shadow|on, shadow parity đầy đủ, mismatch thì fallback old path.

Purpose: Khóa correctness trước khi tăng tốc để bảo toàn logic trading hiện tại (D-01, D-03).
Output: Runtime mode control + parity/fallback tests sẵn sàng cho bước tối ưu hiệu năng kế tiếp.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-profiling-baseline-performance/44-CONTEXT.md
@.planning/phases/44-profiling-baseline-performance/44-RESEARCH.md
@.planning/phases/44-profiling-baseline-performance/44-VALIDATION.md
@services/aureus-signal/engine/signals/structure.py
@services/aureus-signal/engine/state.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dựng contract tests cho mode và parity trước khi chỉnh runtime</name>
  <files>services/aureus-signal/tests/test_structure_mode_flag.py, services/aureus-signal/tests/test_structure_parity_shadow.py</files>
  <read_first>
- services/aureus-signal/engine/signals/structure.py
- services/aureus-signal/engine/state.py
- .planning/phases/44-profiling-baseline-performance/44-CONTEXT.md
- .planning/phases/44-profiling-baseline-performance/44-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `AUREUS_STRUCTURE_OPT_MODE` chỉ chấp nhận `off|shadow|on`, giá trị invalid phải fallback `off` (per D-03).
    - Test 2: Ở mode `shadow`, old/new cùng chạy và comparator so đầy đủ contract `transient_signals`, `obs`, `swing_points` (per D-01, D-03).
    - Test 3: Khi comparator phát hiện mismatch, kết quả runtime phải dùng old path và phát tín hiệu mismatch (per D-03).
  </behavior>
  <action>Trước khi sửa symbol chính trong `structure.py`, chạy `gitnexus_impact({target: "StructureSignal._emit", direction: "upstream"})` để chụp blast radius caller/process liên quan theo CLAUDE.md. Sau đó tạo hai file test mới theo Wave-0 trong 44-VALIDATION.md: `test_structure_mode_flag.py` và `test_structure_parity_shadow.py`. Dùng fixture state/candle tối thiểu nhưng deterministic; assertion phải kiểm tra rõ mode branch, đầy đủ contract fields, và fallback behavior. Không viết test hiệu năng ở task này.</action>
  <acceptance_criteria>
- `services/aureus-signal/tests/test_structure_mode_flag.py` tồn tại và có test cho 3 mode `off|shadow|on` + invalid fallback.
- `services/aureus-signal/tests/test_structure_parity_shadow.py` tồn tại và có assertion cho contract gồm `transient_signals`, `obs`, `swing_points`.
- Có ít nhất 1 test mismatch xác nhận runtime chọn old path thay vì new path.
  </acceptance_criteria>
  <verify>
    <automated>pytest services/aureus-signal/tests/test_structure_mode_flag.py services/aureus-signal/tests/test_structure_parity_shadow.py -q -x</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>Test contract cho D-03 đầy đủ, fail trước khi runtime implementation được thêm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement runtime mode off|shadow|on với auto-fallback</name>
  <files>services/aureus-signal/engine/signals/structure.py</files>
  <read_first>
- services/aureus-signal/engine/signals/structure.py
- services/aureus-signal/engine/state.py
- services/aureus-signal/tests/test_structure_mode_flag.py
- services/aureus-signal/tests/test_structure_parity_shadow.py
- .planning/phases/44-profiling-baseline-performance/44-CONTEXT.md
  </read_first>
  <action>Trước khi sửa symbol chính trong `structure.py`, chạy `gitnexus_impact({target: "StructureSignal._emit", direction: "upstream"})` để xác nhận direct callers/risk depth d=1 theo CLAUDE.md. Trong `structure.py`, bổ sung runtime flag `AUREUS_STRUCTURE_OPT_MODE` với giá trị hợp lệ `off|shadow|on` (per D-03). Triển khai dual-path execution cho `shadow`: chạy old path + optimized candidate path, compare full contract output (`transient_signals`, `obs`, `swing_points` và các metadata OB quan trọng), nếu mismatch thì log mismatch context (symbol, t, diff_fields) và trả kết quả old path ngay. Ở mode `off` luôn chạy old path; mode `on` chạy optimized path nhưng vẫn giữ guard fallback khi comparator báo mismatch ở runtime để tránh phá pipeline (per D-01, D-03). Không đổi schema output và không đổi semantic trading conditions.</action>
  <acceptance_criteria>
- `structure.py` có parser mode env với whitelist `off|shadow|on`.
- Có nhánh `shadow` chạy cả hai path và có comparator contract-level, không so sánh hời hợt theo tag đơn lẻ.
- Có log mismatch chứa tối thiểu `symbol`, `t`, `diff_fields` và fallback old path.
- Mode mặc định an toàn là `off` khi env thiếu/invalid.
  </acceptance_criteria>
  <verify>
    <automated>pytest services/aureus-signal/tests/test_structure_mode_flag.py services/aureus-signal/tests/test_structure_parity_shadow.py -q -x</automated>
    <automated>gitnexus_detect_changes()</automated>
  </verify>
  <done>Runtime safety D-03 hoàn tất và test parity/mode xanh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| market candle/state → structure processor | Input runtime không tin cậy đi vào logic branch old/new có thể tạo mismatch |
| structure processor → downstream signal pipeline | Output structure ảnh hưởng CHOCH/OB/sweep và strategy match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | T | structure parity comparator | mitigate | So sánh full contract (`transient_signals`, `obs`, `swing_points`) và log diff_fields rõ ràng |
| T-44-02 | D | shadow dual-path runtime | mitigate | Fallback old path tức thời khi mismatch để không làm gián đoạn pipeline |
| T-44-03 | R | mismatch observability | mitigate | Log mismatch bắt buộc chứa symbol + timestamp + trường lệch để truy vết |
</threat_model>

<verification>
- pytest services/aureus-signal/tests/test_structure_mode_flag.py services/aureus-signal/tests/test_structure_parity_shadow.py -q -x
- pytest services/aureus-signal/tests -k "structure and (mode_flag or parity or shadow)" -q -x
</verification>

<success_criteria>
- D-03 được triển khai đầy đủ: mode `off|shadow|on`, shadow compare, mismatch fallback old path.
- Không có thay đổi schema output structure hoặc side-effects ngoài nhánh runtime safety.
- Hai test file Wave-0 trong 44-VALIDATION.md đã tồn tại và pass.
</success_criteria>

<output>
After completion, create `.planning/phases/44-profiling-baseline-performance/44-01-SUMMARY.md`
</output>
