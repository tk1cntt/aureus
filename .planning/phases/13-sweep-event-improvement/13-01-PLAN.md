---
wave: 1
depends_on: []
files_modified: ["d:\\Aureus\\services\\aureus-signal\\engine\\signals\\sweep.py", "d:\\Aureus\\services\\aureus-signal\\engine\\signals\\structure.py", "d:\\Aureus\\services\\aureus-signal\\engine\\event_filter.py", "d:\\Aureus\\services\\aureus-signal\\engine\\live_engine.py", "d:\\Aureus\\services\\aureus-signal\\engine\\event_policy.py", "d:\\Aureus\\services\\aureus-signal\\engine\\state.py", "d:\\Aureus\\services\\aureus-signal\\tests\\test_sweep_o1.py", "d:\\Aureus\\services\\aureus-signal\\tests\\test_sweep_integration_live_engine.py", "d:\\Aureus\\services\\aureus-signal\\unittest\\test_event_filter.py"]
autonomous: false
---

# 13-01: Sweep Event Improvement (Status-based + Centralized AI Trigger Policy)

<objective>
Chuẩn hóa `sweep` theo status machine `PENDING/TOUCHED/SWEEP/BROKEN_PENDING/STOP_HUNT/DEAD`, cutover sang status-based event contract, đồng thời gom AI trigger về một policy tập trung tại `live_engine`.

`transient_signals` hiện được dùng cho:
1. `has_structural_event(...)` trong `live_engine.py` để quyết định snapshot/redis sync.
2. `state_snapshot.py` / `snapshot_utils.py` để serialize-persist `events`.

`has_structural_event` giữ trách nhiệm snapshot gate, không chịu trách nhiệm AI trigger.
</objective>

<tasks>

<task>
<description>
Refactor `sweep.py` theo status-based contract và bỏ AI trigger phân tán.
</description>
<read_first>
- `d:\Aureus\services\aureus-signal\engine\signals\sweep.py`
- `d:\Aureus\.planning\archive\v1.1-signal-optimization\phases\13-sweep-event-improvement\13-CONTEXT.md`
</read_first>
<action>
1. Chuẩn hóa transition:
   - `BROKEN_PENDING` -> `STOP_HUNT` khi reclaim trong 1-2 nến.
   - `BROKEN_PENDING` -> `DEAD` nếu sau 2 nến vẫn outside OB.
2. Emit `transient_signals` theo status-key (`sweep_stop_hunt`, `sweep_dead`, ...).
3. Xóa các call `request_ai_update(...)` trực tiếp trong `sweep.py`.
</action>
<acceptance_criteria>
- Không còn gọi `request_ai_update` trong `sweep.py`.
- Không còn literal `OB_STATE_CHANGE`.
- Có status-based sweep tags mới trong emission path.
</acceptance_criteria>
</task>

<task>
<description>
Loại trigger AI phân tán khỏi `structure.py`, chỉ giữ domain event emit.
</description>
<read_first>
- `d:\Aureus\services\aureus-signal\engine\signals\structure.py`
</read_first>
<action>
1. Giữ nguyên CHOCH/OB event emit vào `transient_signals`.
2. Gỡ các call trực tiếp `request_ai_update("CHOCH")` và `request_ai_update("OB_INTERACTION")` khỏi signal layer.
</action>
<acceptance_criteria>
- `structure.py` không gọi trực tiếp `request_ai_update(...)` trong signal processing path.
</acceptance_criteria>
</task>

<task>
<description>
Thêm policy tập trung để map event theo tick -> AI triggers và tích hợp vào `live_engine`.
</description>
<read_first>
- `d:\Aureus\services\aureus-signal\engine\live_engine.py`
- `d:\Aureus\services\aureus-signal\engine\event_filter.py`
</read_first>
<action>
1. Tạo `engine/event_policy.py` với hàm evaluate tick events từ `transient_signals`.
2. Trả về danh sách AI trigger event codes (vd `STOP_HUNT`, `CHOCH`, `OB_INTERACTION`).
3. Trong `live_engine`, sau `execute_signals_for_candle(...)`, dùng policy này để gọi `state.request_ai_update(...)` tại một chỗ duy nhất.
4. Giữ `has_structural_event(...)` làm snapshot/redis sync gate; không nhúng AI policy vào hàm này.
</action>
<acceptance_criteria>
- Runtime path chỉ còn một điểm gọi `request_ai_update` cho event-driven pulse flow.
- `has_structural_event` vẫn pass role snapshot gate.
</acceptance_criteria>
</task>

<task>
<description>
Deprecate `request_ai_update` theo 2 pha an toàn.
</description>
<read_first>
- `d:\Aureus\services\aureus-signal\engine\state.py`
- `d:\Aureus\services\aureus-signal\engine\live_engine.py`
</read_first>
<action>
1. Phase A: giữ `request_ai_update` như compatibility API, nhưng chỉ còn `live_engine` gọi.
2. Phase B: sau khi ổn định callsites, đổi tên/internalize hoặc xóa API cũ.
</action>
<acceptance_criteria>
- Signal files không còn gọi `request_ai_update`.
- Có roadmap cleanup rõ ràng cho API cũ.
</acceptance_criteria>
</task>

<task>
<description>
Update tests cho contract mới.
</description>
<read_first>
- `d:\Aureus\services\aureus-signal\tests\test_sweep_o1.py`
- `d:\Aureus\services\aureus-signal\tests\test_sweep_integration_live_engine.py`
- `d:\Aureus\services\aureus-signal\unittest\test_event_filter.py`
</read_first>
<action>
1. Đổi expected tags sang status-based sweep tags.
2. Bổ sung/điều chỉnh assertions để xác nhận AI trigger được phát từ engine policy (không từ signal internals).
3. Giữ test `event_filter` tập trung vào snapshot gating behavior.
</action>
<acceptance_criteria>
- Test files liên quan pass với contract mới.
- Không còn assertion phụ thuộc AI trigger phân tán trong signal files.
</acceptance_criteria>
</task>

</tasks>

<verification>
## Verification
1. `pytest tests/test_sweep_o1.py -q`
2. `pytest tests/test_sweep_integration_live_engine.py -q`
3. `pytest unittest/test_event_filter.py -q`
4. `pytest tests/test_sweep_o1.py tests/test_sweep_integration_live_engine.py unittest/test_event_filter.py -q`
5. Manual grep checks:
   - `services/aureus-signal/engine/signals/sweep.py` không còn `request_ai_update`.
   - `services/aureus-signal/engine/signals/structure.py` không còn call trực tiếp `request_ai_update` trong signal path.
   - `services/aureus-signal/engine/live_engine.py` có một orchestration point cho AI trigger.
</verification>
