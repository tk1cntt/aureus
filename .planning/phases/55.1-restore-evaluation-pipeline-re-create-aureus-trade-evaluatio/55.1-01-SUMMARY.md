---
phase: 55.1-restore-evaluation-pipeline
plan: 01
status: completed
---
# Phase 55.1 Plan 01: Restore Evaluation Pipeline - Summary

**Status:** Completed 2026-06-10

## What was restored

1. **Migration SQL** - Created `services/aureus-db-writer/migrations/restore_add_trade_evaluations.sql` with:
   - `aureus_trade_evaluations` table with all Phase 55 schema requirements
   - Unique constraint on (trade_journal_id, score_version)
   - JSONB constraints for score_breakdown/weights_snapshot
   - Indexes for symbol/timeframe/evaluated_at queries

2. **INSERT path in journal.py** - Added evaluation record persistence in `on_order_opened`:
   - Extracts scoring fields: score_total, score_breakdown, weights_snapshot, missing_data_policy, score_version
   - Inserts after journal update, before signal snapshot insert
   - Graceful skip if score_total is None
   - ON CONFLICT DO NOTHING for idempotency
   - Error handling with warning logs

## Commits

- `7a5dc88` - feat(55.1): restore evaluation table migration
- `b535b62` - feat(55.1): restore evaluation INSERT path in on_order_opened

## Files Created/Modified

- `services/aureus-db-writer/migrations/restore_add_trade_evaluations.sql` (new)
- `services/aureus-trader/journal.py` (modified - evaluation INSERT path added)

## Requirements Restored

- EVAL-01: Schema DB chuẩn hóa evaluation
- EVAL-02: Pipeline ingestion/compute/persist đầy đủ theo trade
- EVAL-03: Backfill/recompute giữ lịch sử version
- EVAL-04: Data quality guards (idempotency, uniqueness)
- EVAL-RUNTIME-01-04: Runtime schema parity

## Next Steps

Run migration on database to create table:
```bash
psql "$DATABASE_URL" -f services/aureus-db-writer/migrations/restore_add_trade_evaluations.sql
```

Verify with tests:
```bash
pytest services/aureus-trader/tests/test_evaluation_pipeline.py -q
```
