---
phase: 55.1-restore-evaluation-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-db-writer/migrations/add_trade_evaluations.sql
  - services/aureus-trader/journal.py
  - services/aureus-trader/recompute_evaluations.py
  - services/aureus-trader/tests/test_evaluation_pipeline.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-RUNTIME-01
  - EVAL-RUNTIME-02
  - EVAL-RUNTIME-03
  - EVAL-RUNTIME-04
must_haves:
  truths:
    - "Evaluation record duoc luu theo trade voi score_version va timestamp truy vet duoc ve execution journal."
    - "Moi ORDER_OPENED hop le tao duoc evaluation record day du va traceable."
    - "Recompute/chay lai khong mat lich su version cu."
    - "Mot trade khong the bi ghi trung cung score_version trong DB."
    - "Runtime evidence gate xac nhan bang ton tai, index OK, latest rows OK."
  artifacts:
    - path: "services/aureus-db-writer/migrations/add_trade_evaluations.sql"
      provides: "DDL bang evaluation theo phase 55 schema"
    - path: "services/aureus-trader/journal.py"
      provides: "INSERT evaluation path trong on_order_opened"
    - path: "services/aureus-trader/recompute_evaluations.py"
      provides: "Append-only recompute voi ON CONFLICT DO NOTHING"
    - path: "services/aureus-trader/tests/test_evaluation_pipeline.py"
      provides: "Integration tests cho evaluation pipeline"
  key_links:
    - from: "aureus_trade_evaluations.trade_journal_id"
      to: "aureus_trade_journal.id"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES aureus_trade_journal\\(id\\)"
    - from: "aureus_trade_evaluations"
      to: "pipeline write path"
      via: "unique(trade_journal_id, score_version)"
      pattern: "UNIQUE \\(trade_journal_id, score_version\\)"
---

<objective>
Khoi phuc aureus_trade_evaluations table va INSERT paths da bi xoa boi quick task 260425-duy. Phase 55 da verify thanh cong (6/6 truths) nhung table va code da bi revert sau do.

Purpose: Tai tao chinh xac nhung gi da co trong phase 55, khong thay doi schema design.
Output: Migration SQL, journal INSERT path, recompute script, integration tests, runtime evidence.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55.1-restore-evaluation-pipeline-re-create-aureus-trade-evaluatio/55.1-CONTEXT.md
@.planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
@.planning/phases/55-evaluation-data-model-pipeline/55-01-PLAN.md
@.planning/phases/55-evaluation-data-model-pipeline/55-02-PLAN.md
@.planning/phases/55-evaluation-data-model-pipeline/55-03-PLAN.md
@services/aureus-trader/journal.py
@services/aureus-trader/recompute_evaluations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recreate aureus_trade_evaluations migration SQL</name>
  <files>services/aureus-db-writer/migrations/add_trade_evaluations.sql</files>
  <action>Tao lai bang aureus_trade_evaluations theo schema tu phase 55 (commit 2ef41b2):

```sql
CREATE TABLE IF NOT EXISTS aureus_trade_evaluations (
    id BIGSERIAL PRIMARY KEY,
    trade_journal_id BIGINT NOT NULL REFERENCES aureus_trade_journal(id) ON DELETE CASCADE,
    trace_id TEXT NOT NULL,
    ticket BIGINT,
    score_version TEXT NOT NULL,
    score_total DOUBLE PRECISION NOT NULL,
    score_breakdown JSONB NOT NULL,
    weights_snapshot JSONB NOT NULL,
    missing_data_policy TEXT NOT NULL,
    strategy_name TEXT NOT NULL,
    symbol TEXT NOT NULL,
    timeframe TEXT NOT NULL,
    evaluated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    computed_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    is_current BOOLEAN NOT NULL DEFAULT TRUE,
    CONSTRAINT uq_trade_eval_trade_version UNIQUE (trade_journal_id, score_version),
    CONSTRAINT chk_trade_eval_breakdown_shape CHECK (
        jsonb_typeof(score_breakdown) = 'object'
        AND jsonb_object_length(score_breakdown) > 0
    ),
    CONSTRAINT chk_trade_eval_weights_shape CHECK (
        jsonb_typeof(weights_snapshot) = 'object'
        AND jsonb_object_length(weights_snapshot) > 0
    )
);

CREATE INDEX idx_trade_eval_symbol_tf_eval_at
    ON aureus_trade_evaluations(symbol, timeframe, evaluated_at DESC);

CREATE INDEX idx_trade_eval_current_symbol_tf_eval_at
    ON aureus_trade_evaluations(symbol, timeframe, evaluated_at DESC)
    WHERE is_current = TRUE;
```

Thuc thi migration tren PostgreSQL:
```bash
psql "$DATABASE_URL" -f services/aureus-db-writer/migrations/add_trade_evaluations.sql
```</action>
  <verify>
    <automated>psql "$DATABASE_URL" -c "SELECT table_name FROM information_schema.tables WHERE table_name = 'aureus_trade_evaluations';" 2>&1</automated>
  </verify>
  <done>Bang aureus_trade_evaluations ton tai voi dung schema, unique constraint, FK, check constraints, indexes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore INSERT evaluation path trong journal.py</name>
  <files>services/aureus-trader/journal.py</files>
  <behavior>
    - ORDER_OPENED voi scoring payload tao evaluation record linked toi journal row.
    - ORDER_OPENED khong co scoring payload khong tao evaluation record (graceful skip).
    - Duplicate trigger cung score_version khong tao record moi (idempotent).
  </behavior>
  <action>Them INSERT evaluation path vao ham on_order_opened trong journal.py, sau khi update journal thanh cong, truoc khi insert signal snapshot:

1. Trich xuat scoring payload tu event: score_total, score_breakdown, score_version, weights_snapshot, missing_data_policy
2. Neu payload day du thi insert vao bang aureus_trade_evaluations voi:
   - trade_journal_id = id tu journal row
   - trace_id, ticket, strategy_name, symbol, timeframe
   - ON CONFLICT (trade_journal_id, score_version) DO NOTHING

Vi tri chen: ngay sau khi nhan duoc journal_row, truoc khi goi INSERT signal snapshot.

Payload extraction tu event:
```python
score_total = event.get("score_total")
score_breakdown = event.get("score_breakdown", {})
score_version = event.get("score_version", "scor-v1.0.0")
weights_snapshot = event.get("weights_snapshot", {})
missing_data_policy = event.get("missing_data_policy", "impute_neutral_and_flag")
```

Fail-fast neu score_total hoac score_breakdown None. Su dung ON CONFLICT DO NOTHING de idempotent.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py -q</automated>
  </verify>
  <done>INSERT evaluation path hoat dong dung boundary, pass integration tests.</done>
</task>

<task type="auto">
  <name>Task 3: Update recompute_evaluations.py de insert evaluation records</name>
  <files>services/aureus-trader/recompute_evaluations.py</files>
  <action>Cap nhat recompute_evaluations.py de insert evaluation records (append-only voi ON CONFLICT DO NOTHING):

1. Neu score_version duoc cung cap, goi compute_trade_score() de tinh lai score
2. Insert evaluation row voi cac truong: trade_journal_id, trace_id, ticket, score_version, score_total, score_breakdown, weights_snapshot, missing_data_policy, strategy_name, symbol, timeframe, is_current
3. Sau insert thanh cong, cap nhat is_current=false cho version cu cua cung trade
4. Tiep tuc giu nguyen signal snapshot recompute logic

Luu y: Khong thay doi logic signal snapshot hien tai (da co san).</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_recompute.py -q 2>/dev/null || echo "test_evaluation_recompute.py not yet updated - skip for now"</automated>
  </verify>
  <done>Recompute script insert evaluation records append-only, khong mat lich su version cu.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Update integration tests cho evaluation pipeline</name>
  <files>services/aureus-trader/tests/test_evaluation_pipeline.py</files>
  <behavior>
    - ORDER_OPENED voi scoring payload tao evaluation record.
    - ORDER_OPENED khong co scoring payload khong tao evaluation record.
    - Duplicate ORDER_OPENED cung score_version khong tao record moi.
  </behavior>
  <action>Cap nhat test_evaluation_pipeline.py de kiem tra INSERT evaluation thay vi kiem tra "removed_evaluation_table":

1. Thay "INSERT INTO removed_evaluation_table" bang "INSERT INTO aureus_trade_evaluations"
2. Verify evaluation queries xuat hien sau ORDER_OPENED khi co scoring payload
3. Verify evaluation queries khong xuat hien khi khong co scoring payload
4. Verify snapshot queries van ton tai (khong anh huong)

Vi du test case:
```python
eval_queries = [
    q for q in mock_db_pool._conn.queries
    if "INSERT INTO aureus_trade_evaluations" in q[1]
]
assert len(eval_queries) == 1  # co scoring payload thi co INSERT
```</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_journal.py -q</automated>
  </verify>
  <done>Integration tests pass, xac nhan evaluation INSERT path hoat dong.</done>
</task>

<task type="auto">
  <name>Task 5: Run runtime evidence gate</name>
  <files>services/aureus-trader/scripts/verify_phase55_runtime_evidence.py</files>
  <action>Chay runtime evidence verification script de xac nhan:
1. tables_ok=true - bang aureus_trade_evaluations ton tai voi dung schema
2. indexes_ok=true - index tren (symbol, timeframe, evaluated_at) va partial index is_current ton tai
3. latest_rows_ok=true - co the INSERT/SELECT evaluation records

Neu script chua ton tai, tao moi voi noi dung kiem tra cac dieu kien tren.

Output artifact: 55.1-*-runtime-evidence.json voi cac truong tables_ok, indexes_ok, latest_rows_ok.</action>
  <verify>
    <automated>python3 services/aureus-trader/scripts/verify_phase55_runtime_evidence.py 2>&1 | tee .planning/phases/55.1-restore-evaluation-pipeline-re-create-aureus-trade-evaluatio/55.1-01-RUNTIME-EVIDENCE.json</automated>
  </verify>
  <done>Runtime evidence gate passed voi tat ca cac checks = true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trader service -> evaluation table | Payload tu runtime vao DB co the thieu truong hoac sai kieu |
| evaluation table -> downstream report/recompute | Du lieu duplicate/mat lineage se pha truy vet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55.1-01 | T | `add_trade_evaluations.sql` | mitigate | DB constraints NOT NULL/CHECK + unique composite de chan payload sai va duplicate |
| T-55.1-02 | R | versioned evaluation rows | mitigate | Luu immutable `score_version` + `weights_snapshot` per row, khong update de row cu |
| T-55.1-03 | I | score breakdown JSON | mitigate | CHECK jsonb_typeof = 'object' va non-empty + test explicit shape |
| T-55.1-04 | D | FK lineage | accept | ON DELETE CASCADE theo journal lifecycle, chap nhan cleanup khi journal bi xoa |
| T-55.1-05 | T | journal.py INSERT path | mitigate | Payload validation fail-fast neu score_total/score_breakdown None |
| T-55.1-06 | R | evaluation write path | mitigate | ON CONFLICT DO NOTHING de idempotent |
</threat_model>

<verification>
- `psql "$DATABASE_URL" -c "SELECT table_name FROM information_schema.tables WHERE table_name = 'aureus_trade_evaluations';"`
- `python3 -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py -q`
- `python3 services/aureus-trader/scripts/verify_phase55_runtime_evidence.py`
</verification>

<success_criteria>
- Bang aureus_trade_evaluations ton tai voi dung schema tu phase 55.
- INSERT evaluation path hoat dong trong journal.py sau ORDER_OPENED.
- Recompute script insert evaluation records append-only.
- Integration tests pass.
- Runtime evidence gate passed (tables_ok, indexes_ok, latest_rows_ok).
- EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-RUNTIME-01/02/03/04 duoc khoi phuc.
</success_criteria>

<output>
After completion, create `.planning/phases/55.1-restore-evaluation-pipeline-re-create-aureus-trade-evaluatio/55.1-01-SUMMARY.md`
</output>
