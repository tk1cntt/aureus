---
phase: 10
wave: 1
depends_on: []
files_modified:
  - "services/aureus-signal/engine/signals/structure.py"
  - "services/aureus-signal/engine/signals/sweep.py"
  - "services/aureus-signal/tests/test_structure_o1.py"
  - "services/aureus-signal/tests/test_sweep_o1.py"
autonomous: false
---

# Phase 10: Tái Kiến Trúc Bộ Máy Tự Động Quét Thanh Khoản

<objective>
Tháo dỡ cơ chế quản lý Target cứng nhắc của MQL5 cũ. Chuyển toàn bộ máy theo dõi trạng thái Order Block (State Machine) từ `structure.py` sang `sweep.py`. Triển khai luật xác nhận gãy OB bằng 2 nến (M1) và dọn dẹp rác Memory bằng thuật toán Tọa độ (Top 10 Closest) để lấp lỗ hổng Memory Leak.
</objective>

<requirements>
- REQ-10-01: Gỡ bỏ gánh nặng Tracking Mitigation khỏi `structure.py`.
- REQ-10-02: `structure.py` khởi tạo OB với `status` và `break_counter` thay cho `is_mitigated`.
- REQ-10-03: `sweep.py` tự quản lý State Machine 4 tầng và đếm 2 nến M1 tách rời.
- REQ-10-04: Áp dụng Garbage Collection chống tràn RAM `[ob for ob in obs if ob.get('status') != 'DEAD']`. Chỉ giữ 10 OB gần nhất.
</requirements>

<tasks>

<task id="10-01-01">
  <title>Refactor Structure Initializer (Gỡ Tracking Cũ)</title>
  <read_first>
    - d:/Aureus/services/aureus-signal/engine/signals/structure.py
    - d:/Aureus/.planning/phases/10-phan-tich-toi-uu-sweep-targets/10-CONTEXT.md
  </read_first>
  <action>
    - Xóa hoàn toàn hàm `_verify_mitigations` trong `structure.py`.
    - Xóa việc gọi `_verify_mitigations` trong hàm `calculate`.
    - Trong hàm `_process_ob`, loại bỏ khai báo `ob['is_mitigated'] = False` (nếu có). Thay bằng Khởi tạo trạng thái mới cho OB: Thêm key `"status": "PENDING"` và `"break_counter": 0`.
    - Trong hàm `_register_sweep_targets`, XÓA bỏ cơ chế lấy target phức tạp cũ (Double Sweep, FVG gap, CHOCH origin) và XÓA luôn `existing_targets[-10:]`. Thay vào đó, nó KHÔNG làm gì cả (thực tế có thể xóa luôn `_register_sweep_targets` đi vì `sweep.py` sẽ đọc trực tiếp từ `state_obj.obs`). Để an toàn API cũ, hãy fix `_register_sweep_targets` thành 1 hàm rỗng (pass) hoặc chỉ log nhẹ nhàng để không lỗi.
  </action>
  <acceptance_criteria>
    - `grep -q "def _verify_mitigations" d:/Aureus/services/aureus-signal/engine/signals/structure.py` rớt (không còn hàm này).
    - `grep -q "\"status\": \"PENDING\"" d:/Aureus/services/aureus-signal/engine/signals/structure.py` pass (chứng minh có nhúng State).
    - `pytest tests/test_structure_o1.py` chạy ổn định hoặc báo lỗi cục bộ liên quan (để fix ở task kế).
  </acceptance_criteria>
</task>

<task id="10-01-02">
  <title>Fix Structure Unit Tests</title>
  <read_first>
    - d:/Aureus/services/aureus-signal/tests/test_structure_o1.py
    - d:/Aureus/services/aureus-signal/tests/test_structure_integration_execute_signals_for_candle.py
  </read_first>
  <action>
    - Mở Test Suite của `structure.py` lên. Do ta đã chặt bỏ `_verify_mitigations` và thay đổi định dạng OB, các bài test kiểm tra Output MQL5 Parity liên quan tới cờ `is_mitigated` hoặc `sweep_targets` sẽ báo đỏ.
    - Sửa lại các Expectation (Mock Assertions) để check sự tồn tại của `status == PENDING` và `break_counter == 0` thay vì check mảng Target phức tạp.
    - Chạy full test cho Structure để đảm bảo xanh hết.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_structure_o1.py -v` executes 100% PASS.
    - `pytest tests/test_structure_integration_execute_signals_for_candle.py -v` executes 100% PASS.
  </acceptance_criteria>
</task>

<task id="10-01-03" wave="2">
  <title>Cấy State Machine và Garbage Collection vào Sweep</title>
  <read_first>
    - d:/Aureus/services/aureus-signal/engine/signals/sweep.py
    - d:/Aureus/.planning/phases/10-phan-tich-toi-uu-sweep-targets/10-RESEARCH.md
  </read_first>
  <action>
    - Mở `sweep.py`. Thêm một Method nội bộ tên `_update_ob_states(self, state_obj, candle)` vào Class SweepSignal.
    - Duyệt List Comprehension `obs = getattr(state_obj, 'obs', [])`.
    - Duyệt qua từng OB trong `obs` để áp dụng Logic Cập nhật (Nến thủng qua báo `BROKEN_PENDING`, 2 nến rời rạc báo `DEAD`, chạm râu báo `TOUCHED` / `SWEPT`).
    - Dọn OB Rác bão vệ RAM bằng đúng dòng Lệnh List Comprehension được Sếp dặn dò kĩ:
      `state_obj.obs = [ob for ob in state_obj.obs if ob.get('status', 'PENDING') != 'DEAD']`
    - Logic dọn thêm theo khoảng cách (10 Bull OB gần nhất, 10 Bear OB gần nhất): Xếp mảng theo `abs(ob_extreme - close)` và xén.
  </action>
  <acceptance_criteria>
    - `grep -q "def _update_ob_states" d:/Aureus/services/aureus-signal/engine/signals/sweep.py` pass.
    - `grep -q "status.*!=.*DEAD" d:/Aureus/services/aureus-signal/engine/signals/sweep.py` pass.
  </acceptance_criteria>
</task>

<task id="10-01-04" wave="2">
  <title>Kích hoạt Tín hiệu Stop Hunt và Fix Test Sweep</title>
  <read_first>
    - d:/Aureus/services/aureus-signal/engine/signals/sweep.py
    - d:/Aureus/services/aureus-signal/tests/test_sweep_o1.py
  </read_first>
  <action>
    - Trong hàm `calculate()` của `sweep.py`, chạy `self._update_ob_states(state_obj, candle)`.
    - Sau khi update, duyệt rớt ra các OB có `status == "SWEPT"`.
    - Ném Tag `STOP_HUNT` và đẩy Transient Signal vào Redis `request_ai_update("OB_STATE_CHANGE")`.
    - Cập nhật các Test Cases `test_sweep_o1.py` để nhồi vào `state_obj.obs` (chứa các OB PENDING) thay vì nhồi giả array `sweep_targets`.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_sweep_o1.py -v` executes 100% PASS.
    - Lệnh quét `grep -q "OB_STATE_CHANGE" d:/Aureus/services/aureus-signal/engine/signals/sweep.py` pass.
  </acceptance_criteria>
</task>

<task id="10-01-05" wave="3">
  <title>Khôi Phục Backward Compatibility Cho Dashboard</title>
  <read_first>
    - d:/Aureus/services/aureus-signal/engine/state.py
    - d:/Aureus/services/aureus-signal/engine/signals/sweep.py
  </read_first>
  <action>
    - Xóa bỏ khối logic cũ kiểm tra Hard Break bên trong vòng lặp OB của hàm `update_with_candle` ở `state.py`.
    - Chèn cơ chế Proxy Sync vào `sweep.py`:
      ```python
      is_mitigated = ob.get("status") in ["TOUCHED", "SWEPT", "BROKEN_PENDING", "DEAD"]
      if is_mitigated and not ob.get("mitigated"):
          ob["t_mitigation"] = c_t
      ob["mitigated"] = is_mitigated
      ```
  </action>
  <acceptance_criteria>
    - `grep -q "broken" d:/Aureus/services/aureus-signal/engine/state.py` fail for the OB logic.
    - Cờ mitigated xuất hiện trong `sweep.py`.
    - `pytest` pass 100%.
  </acceptance_criteria>
</task>

</tasks>

<verification>
## Verification Checklist
1. All `structure.py` obsolete code is physically removed.
2. State machine effectively processes historical candles without memory explosion (the array truncates itself gracefully).
3. The 2-candle confirmation sequence in `sweep.py` operates accurately across isolated unit tests.
4. Total test footprint is perfectly green.

Must Haves:
- List comprehension for garbage collection (`!= 'DEAD'`) is verbatim in code.
</verification>
