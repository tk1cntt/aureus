---
phase: 03
plan: 01
title: Signal FVG optimization and verification gates
wave: 1
depends_on: []
autonomous: true
requirements: [SIG-03, TST-01, TST-02, TST-03, TST-04, VAL-01, VAL-02]
files_modified:
  - services/aureus-signal/engine/signals/fvg.py
  - services/aureus-signal/engine/signals/fvg_up.py
  - services/aureus-signal/engine/signals/fvg_down.py
  - services/aureus-signal/tests/test_fvg_o1.py
  - services/aureus-signal/tests/test_fvg_integration_execute_signals_for_candle.py
  - services/aureus-signal/tests/test_signal_contract_normalization.py
  - services/aureus-signal/tests/test_fvg_integration_live_engine.py
  - .planning/phases/03-signal-fvg/03-VALIDATION.md
---

# Phase 03 Plan 01 — Signal FVG Optimization and Verification Gates

## Objective
Optimize and harden FVG signal behavior while preserving existing runtime/strategy contracts, then close all phase quality gates (unit + integration + coverage >= 80%).

<tasks>
  <task id="03-01-01" type="auto">
    <name>Harden FVG signal internals without changing public contracts</name>
    <read_first>
      - services/aureus-signal/engine/signals/fvg.py
      - services/aureus-signal/engine/signals/fvg_up.py
      - services/aureus-signal/engine/signals/fvg_down.py
      - .planning/phases/03-signal-fvg/3-CONTEXT.md
    </read_first>
    <action>
      Update FVG signal implementations to add defensive guards for malformed state entries and improve deterministic behavior. Preserve existing contract keys/tags (`fvg_bull_new`, `fvg_bear_new`, `fvg_bull_mitigated`, `fvg_bear_mitigated`, `fvg_up`, `fvg_down`) and timestamp key `t`.
    </action>
    <acceptance_criteria>
      - `fvg.py` still emits all four transient FVG event keys.
      - `fvg_up.py` still returns `tag = "fvg_up"` with `t`.
      - `fvg_down.py` still returns `tag = "fvg_down"` with `t`.
    </acceptance_criteria>
  </task>

  <task id="03-01-02" type="auto">
    <name>Add dedicated FVG unit and helper integration coverage</name>
    <read_first>
      - services/aureus-signal/tests/test_atr_o1.py
      - services/aureus-signal/tests/test_ema_integration_execute_signals_for_candle.py
      - services/aureus-signal/engine/live_engine.py
    </read_first>
    <action>
      Create `test_fvg_o1.py` and `test_fvg_integration_execute_signals_for_candle.py` to verify 3-candle detection, mitigation emit-once behavior, signal history tag/timestamp contract, and transient event emission through `execute_signals_for_candle`.
    </action>
    <acceptance_criteria>
      - `test_fvg_o1.py` exists and covers bullish/bearish detection + mitigation behavior.
      - `test_fvg_integration_execute_signals_for_candle.py` exists and verifies `signal_history` entries with key `t`.
      - At least one assertion verifies transient FVG event key emission.
    </acceptance_criteria>
  </task>

  <task id="03-01-03" type="auto">
    <name>Enforce factory contract and runtime-path integration for FVG</name>
    <read_first>
      - services/aureus-signal/tests/test_signal_contract_normalization.py
      - services/aureus-signal/tests/test_atr_integration_live_engine.py
      - services/aureus-signal/engine/signal_factory.py
    </read_first>
    <action>
      Extend factory contract assertions if needed and add `test_fvg_integration_live_engine.py` to validate runtime-path behavior through `run_signal_engine` using fake infra, without masking FVG factory wiring.
    </action>
    <acceptance_criteria>
      - Factory contract test explicitly verifies FVG registration behavior by feature-flag state.
      - `test_fvg_integration_live_engine.py` exists and executes `run_signal_engine` path.
      - Runtime integration test does not patch `create_signal_set` to inject FVG signal.
    </acceptance_criteria>
  </task>

  <task id="03-01-04" type="auto">
    <name>Run validation commands and update phase validation evidence</name>
    <read_first>
      - .planning/phases/03-signal-fvg/03-VALIDATION.md
      - .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Execute the Phase 03 verification commands and update `03-VALIDATION.md` with real status/results for unit, integration, runtime, and coverage gates.
    </action>
    <acceptance_criteria>
      - `03-VALIDATION.md` status table reflects executed command outcomes.
      - Coverage output for FVG signal scope is recorded and `>=80%`.
      - Phase closure gate remains blocked unless all checks are green.
    </acceptance_criteria>
  </task>
</tasks>

## Verification
- `python -m pytest services/aureus-signal/tests/test_fvg_o1.py services/aureus-signal/tests/test_fvg_integration_execute_signals_for_candle.py -q`
- `python -m pytest services/aureus-signal/tests/test_signal_contract_normalization.py -k "fvg" -q`
- `python -m pytest services/aureus-signal/tests/test_fvg_integration_live_engine.py -q`
- `python -m pytest services/aureus-signal/tests/test_fvg_o1.py services/aureus-signal/tests/test_fvg_integration_execute_signals_for_candle.py services/aureus-signal/tests/test_fvg_integration_live_engine.py --cov=services/aureus-signal/engine/signals/fvg.py --cov=services/aureus-signal/engine/signals/fvg_up.py --cov=services/aureus-signal/engine/signals/fvg_down.py --cov-report=term-missing -q`

## Success Criteria
- FVG runtime + strategy contracts remain backward-compatible.
- Dedicated FVG unit, helper integration, and runtime integration tests are present and passing.
- Coverage for FVG phase scope is `>=80%`.
- Requirements mapped in this plan are demonstrably addressed with validation evidence.
