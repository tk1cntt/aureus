---
wave: 2
depends_on: [1]
files_modified:
  - services/aureus-db-writer/tests/test_order_buffer.py
autonomous: true
requirements_addressed:
  - TRADE-02
---

# Phase 38 Plan 02: Update Order Buffer Tests for Wrapped Payload

<objective>
Cập nhật `test_order_buffer.py` để phủ wrapped payload scenarios, strict timestamp rejection, canonical payload storage, và ACK behavior theo decisions D-01 → D-11.
</objective>

<current_state>
`test_order_buffer.py` hiện tại pass flat dict trực tiếp vào `db_writer.order_buffer.append((stream, msg_id, payload))`. Các test pass nhưng không cover unwrapping step vì step đó chưa tồn tại. Sau Wave 1, cần tests mới để verify:
1. Wrapped payload được unwrap đúng
2. Missing timestamp → reject (D-06, D-07, D-08)
3. Invalid JSON data → reject
4. `order_data` được lưu vào payload column, không phải raw envelope (D-11)
5. ACK behavior cho invalid events (D-09, D-10)
</current_state>

<must_haves>
- Tests có class riêng `TestWrappedPayload`
- Tests verify `open_time` từ `order_data`, không từ top-level
- Tests reject event thiếu timestamp
- Tests verify `order_data` được lưu vào payload, không phải envelope
- Tests phủ ORDER_REJECTED skip với wrapped format
- `ack_skip_msg_ids` bug được mention trong test (NameError no longer occurs)
</must_haves>

---

## Task 2.1: Thêm class `TestWrappedPayloadUndoWrap`

<read_first>
- `services/aureus-db-writer/tests/test_order_buffer.py` (toàn bộ file — pattern test hiện tại)
- `services/aureus-db-writer/main.py` (sau khi sửa Wave 1 — `_normalize_order_payload` method)
- `services/aureus-signal/engine/orders.py` (dòng 238-241: producer format để tạo test fixtures đúng contract)
</read_first>

<acceptance_criteria>
- File `test_order_buffer.py` có class `TestWrappedPayloadUndoWrap`
- Có test `test_unwrap_json_string_data` — wrapped payload `{"type": "ORDER_OPEN", "data": json.dumps(order_dict)}` được unwrap, trace_id được đọc đúng
- Có test `test_unwrap_dict_data` — data field là dict (không phải string) vẫn unwrap đúng
- Có test `test_non_dict_data_raises` — data là string không phải JSON → ValueError hoặc reject
- Có test `test_no_data_field_uses_payload` — backward compat: payload không có `data` field → dùng toàn bộ payload làm order_data (cho test cũ)
- Mỗi test dùng pytest assert: `assert trace_id == "XAUUSD:strat_001:1234567890"` hoặc pattern grep-verifiable cụ thể
</acceptance_criteria>

<action>
Thêm class `TestWrappedPayloadUndoWrap` vào cuối file `services/aureus-db-writer/tests/test_order_buffer.py`:

```python
import json
import pytest

class TestWrappedPayloadUndoWrap:
    """Test that order_buffer correctly unwraps the 'data' field from producer envelope."""
    
    def test_unwrap_json_string_data(self, db_writer):
        """Verify trace_id is read from unwrapped data, not top-level."""
        order_data = {
            "trace_id": "XAUUSD:strat_001:1234567890",
            "symbol": "XAUUSD",
            "strategy_id": 1,
            "strategy_name": "TestStrat",
            "side": "BUY",
            "type": "MARKET",
            "entry_price": 2000.0,
            "sl": 1990.0,
            "tp": 2020.0,
            "status": "PENDING",
            "open_time": 1234567890,
            "close_time": None,
            "pnl": 0.0,
            "exit_price": None,
        }
        wrapped = {
            "type": "ORDER_OPEN",
            "data": json.dumps(order_data),
        }
        db_writer.order_buffer.append(("aureus:stream:XAUUSD:orders", "123456789-0", wrapped))
        # After process_batch, trace_id should be found (not None)
        # Assert: order was processed, no NameError, trace_id extracted
        event_type, normalized = db_writer._normalize_order_payload(wrapped)
        assert event_type == "ORDER_OPEN"
        assert normalized["trace_id"] == "XAUUSD:strat_001:1234567890"
        assert normalized["side"] == "BUY"
    
    def test_unwrap_dict_data(self, db_writer):
        """Verify data field as dict (not string) is handled."""
        order_data = {"trace_id": "EURUSD:strat_002:999", "symbol": "EURUSD", "side": "SELL", "type": "MARKET", "status": "PENDING", "open_time": 999999999, "entry_price": 1.08}
        wrapped = {"type": "ORDER_OPEN", "data": order_data}
        event_type, normalized = db_writer._normalize_order_payload(wrapped)
        assert event_type == "ORDER_OPEN"
        assert normalized["trace_id"] == "EURUSD:strat_002:999"
    
    def test_non_dict_data_raises(self, db_writer):
        """Verify non-dict data raises ValueError."""
        wrapped = {"type": "ORDER_OPEN", "data": "not a valid json"}
        with pytest.raises(ValueError, match="Failed to parse order data JSON"):
            db_writer._normalize_order_payload(wrapped)
    
    def test_no_data_field_backward_compat(self, db_writer):
        """Verify payload without 'data' field uses entire payload as order_data."""
        flat = {"trace_id": "TEST:flat:001", "symbol": "GBPUSD", "side": "BUY"}
        event_type, normalized = db_writer._normalize_order_payload(flat)
        assert normalized["trace_id"] == "TEST:flat:001"
        # event_type will be empty since flat dict has no 'type'
        assert event_type == ""
```
</action>

---

## Task 2.2: Thêm class `TestMissingTimestampRejection`

<read_first>
- `services/aureus-db-writer/main.py` (Wave 1: timestamp validation block)
- `.planning/phases/38-fix-db-writer-order-payload-parsing-for-wrapped-data-to-unblock-trade-journal-fk/38-CONTEXT.md` (D-06, D-07, D-08)
</read_first>

<acceptance_criteria>
- File `test_order_buffer.py` có class `TestMissingTimestampRejection`
- Test `test_missing_open_time_rejected` — event thiếu `open_time` → rejected_msg_ids chứa msg_id
- Test `test_empty_open_time_rejected` — `open_time: ""` → rejected
- Test `test_open_time_none_rejected` — `open_time: None` → rejected
- Test `test_valid_open_time_accepted` — `open_time: 1234567890` → xử lý bình thường
- Không có pattern `parse_ts(msg_id)` hoặc fallback timestamp nào trong code path
</acceptance_criteria>

<action>
Thêm class `TestMissingTimestampRejection` vào cuối file `test_order_buffer.py`:

```python
class TestMissingTimestampRejection:
    """Test strict timestamp validation (D-06, D-07, D-08)."""
    
    def _make_order(self, **overrides):
        base = {
            "trace_id": "XAUUSD:strat:ts",
            "symbol": "XAUUSD",
            "side": "BUY",
            "type": "MARKET",
            "status": "PENDING",
            "entry_price": 2000.0,
            "sl": 1990.0,
            "tp": 2020.0,
            "open_time": 1234567890,
        }
        base.update(overrides)
        return {"type": "ORDER_OPEN", "data": json.dumps(base)}
    
    def test_missing_open_time_rejected(self, db_writer):
        wrapped = self._make_order()
        data = json.loads(wrapped["data"])
        del data["open_time"]
        wrapped["data"] = json.dumps(data)
        event_type, order_data = db_writer._normalize_order_payload(wrapped)
        assert order_data.get("open_time") is None
    
    def test_empty_open_time_rejected(self, db_writer):
        wrapped = self._make_order(open_time="")
        _, order_data = db_writer._normalize_order_payload(wrapped)
        assert order_data.get("open_time") == ""
    
    def test_none_open_time_rejected(self, db_writer):
        wrapped = self._make_order(open_time=None)
        _, order_data = db_writer._normalize_order_payload(wrapped)
        assert order_data.get("open_time") is None
    
    def test_valid_open_time_accepted(self, db_writer):
        wrapped = self._make_order(open_time=1234567890)
        _, order_data = db_writer._normalize_order_payload(wrapped)
        assert order_data.get("open_time") == 1234567890
```
</action>

---

## Task 2.3: Thêm class `TestCanonicalPayloadStorage`

<read_first>
- `services/aureus-db-writer/main.py` (dòng `order_payload = json.dumps(order_data)` sau Wave 1)
- `.planning/phases/38-fix-db-writer-order-payload-parsing-for-wrapped-data-to-unblock-trade-journal-fk/38-CONTEXT.md` (D-11)
</read_first>

<acceptance_criteria>
- File `test_order_buffer.py` có class `TestCanonicalPayloadStorage`
- Test verify `json.dumps(order_data)` không chứa keys `"data"` của envelope (chỉ có business fields)
- Test verify `json.dumps(payload)` không còn được gọi (grep: 0 occurrences trong order_buffer section)
</acceptance_criteria>

<action>
Thêm class `TestCanonicalPayloadStorage` vào cuối file:

```python
class TestCanonicalPayloadStorage:
    """Test that payload column stores canonical data, not raw envelope (D-11)."""
    
    def test_wrapped_payload_canonical_storage(self, db_writer):
        order_data = {
            "trace_id": "XAUUSD:test:canonical",
            "symbol": "XAUUSD",
            "side": "SELL",
            "type": "MARKET",
            "status": "PENDING",
            "open_time": 1234567890,
            "entry_price": 2000.0,
        }
        wrapped = {"type": "ORDER_OPEN", "data": json.dumps(order_data)}
        
        # After normalization, stored payload should be order_data, not wrapped
        _, normalized = db_writer._normalize_order_payload(wrapped)
        stored = json.dumps(normalized)
        stored_dict = json.loads(stored)
        
        # Should NOT have envelope 'data' key
        assert "data" not in stored_dict or isinstance(stored_dict.get("data"), dict)
        assert stored_dict["trace_id"] == "XAUUSD:test:canonical"
        assert stored_dict["side"] == "SELL"
    
    def test_no_raw_envelope_in_stored_payload(self, db_writer):
        raw_envelope = {"type": "ORDER_OPEN", "data": '{"trace_id": "X"}'}
        _, normalized = db_writer._normalize_order_payload(raw_envelope)
        # The normalized payload should be the inner dict, not the envelope
        assert normalized.get("trace_id") == "X"
        # The envelope 'data' key should not appear as a raw string
        stored = json.dumps(normalized)
        assert '"data":' not in stored or '"type": "MARKET"' in stored
```
</action>

---

## Task 2.4: Thêm test cho ORDER_REJECTED skip với wrapped format

<read_first>
- `services/aureus-db-writer/main.py` (ORDER_REJECTED skip logic: `if event_type == 'ORDER_REJECTED'`)
- `services/aureus-signal/engine/orders.py` (dòng 165-171: ORDER_REJECTED format)
</read_first>

<acceptance_criteria>
- Test `test_order_rejected_skipped_wrapped` — ORDER_REJECTED event được skip và thêm vào rejected_msg_ids
- Test verify envelope `type` được dùng để check ORDER_REJECTED, không phải inner data
</acceptance_criteria>

<action>
Thêm class `TestOrderRejectedSkip` vào cuối file:

```python
class TestOrderRejectedSkip:
    """Test that ORDER_REJECTED events are skipped (not written to DB)."""
    
    def test_order_rejected_skipped_wrapped(self, db_writer):
        reason_payload = {
            "trace_id": "XAUUSD:strat:rejected",
            "symbol": "XAUUSD",
            "side": "BUY",
        }
        wrapped = {"type": "ORDER_REJECTED", "data": json.dumps(reason_payload)}
        event_type, order_data = db_writer._normalize_order_payload(wrapped)
        assert event_type == "ORDER_REJECTED"
        assert order_data["trace_id"] == "XAUUSD:strat:rejected"
        # After normalization, the PROCESSING code should check event_type == 'ORDER_REJECTED' and skip
        # This test verifies normalization works correctly for rejected events
```
</action>

---

## Task 2.5: Xác nhận các test cũ vẫn pass (backward compat)

<read_first>
- `services/aureus-db-writer/tests/test_order_buffer.py` (toàn bộ: TestOrderBufferValidTransition, TestOrderBufferInvalidTransition, TestOrderBufferMissingTraceId)
- `services/aureus-db-writer/main.py` (`_normalize_order_payload` — backward compat cho flat dict)
</read_first>

<acceptance_criteria>
- `pytest services/aureus-db-writer/tests/test_order_buffer.py -x` → exit 0, tất cả test pass
- Không có test nào fail do `_normalize_order_payload` backward compat
- Command `pytest services/aureus-db-writer/tests/test_order_buffer.py -v` liệt kê cả test cũ và test mới, tất cả PASS
</acceptance_criteria>

<action>
Sau khi thêm tất cả test classes mới, chạy:

```bash
cd services/aureus-db-writer
pytest tests/test_order_buffer.py -v
```

Xác nhận:
- Tất cả test cũ (TestOrderBufferValidTransition, TestOrderBufferInvalidTransition, TestOrderBufferMissingTraceId) vẫn PASS
- Tất cả test mới PASS
- Nếu có test cũ fail → kiểm tra `_normalize_order_payload` backward compat branch (no data field → use entire payload as order_data)
</action>

---

## Verification Criteria

1. **New test classes exist:** `grep "class Test" services/aureus-db-writer/tests/test_order_buffer.py` → chứa TestWrappedPayloadUndoWrap, TestMissingTimestampRejection, TestCanonicalPayloadStorage, TestOrderRejectedSkip
2. **All tests pass:** `pytest services/aureus-db-writer/tests/test_order_buffer.py -x` → exit 0
3. **No trace_id top-level reads:** `grep "payload.get('trace_id')" services/aureus-db-writer/tests/test_order_buffer.py` → 0 matches
4. **Wrapped test fixture format:** `grep "json.dumps(order_data)" services/aureus-db-writer/tests/test_order_buffer.py` → ít nhất 2 matches (test fixtures dùng wrapped format)
5. **Full suite:** `pytest services/aureus-db-writer/tests/ -x` → exit 0 (nếu có test files khác)
