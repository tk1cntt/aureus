---
phase: "58"
plan: "02"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: []
files_modified:
  - services/aureus-signal/tests/test_fz_cont_pipeline_e2e.py
  - services/aureus-signal/tests/test_bos_signal.py
must_haves:
  truths:
    - "FZ_CONT strategies can emit signals end-to-end with bos_up/bos_down"
    - "StructureSignal emits bos_up/bos_down when no opposing extreme exists"
    - "Sequence matching evaluates choch -> bos sequence correctly"
    - "BOSUpSignal/BOSDownSignal consumers read from transient_signals"
  artifacts:
    - path: "services/aureus-signal/tests/test_fz_cont_pipeline_e2e.py"
      provides: "End-to-end pipeline test for FZ_CONT strategies"
      min_lines: 100
    - path: "services/aureus-signal/tests/test_bos_signal.py"
      provides: "Unit test for bos_up/bos_down signal emission"
      min_lines: 60
  key_links:
    - from: "structure.py:_process_choch"
      to: "transient_signals[bos_up/bos_down]"
      via: "BOS emission when zone_base_idx == -1"
      pattern: "TAG_BOS_UP.*transient"
    - from: "bos_up.py / bos_down.py"
      to: "state_obj.transient_signals"
      via: "calculate() consumer"
      pattern: "transient_signals.get.*bos_up"
    - from: "template.py:_evaluate_sequence"
      to: "signals/events array"
      via: "record.to_dict().signals.events"
      pattern: "events.*tag.*bos_up"
---

<objective>
Verify FZ_CONT_BULL and FZ_CONT_BEAR strategies can emit signals end-to-end using choch -> bos sequence. Focus on bos_up/bos_down signal emission and the choch_up -> bos_up / choch_down -> bos_down sequence matching.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
## Pre-fixed Issues (NOT implementing again)
- **260602-riq**: `StructureSignal` already emits `bos_up`/`bos_down` when `zone_base_idx == -1` (no opposing extreme)
- **260602-riq**: `BOSUpSignal`/`BOSDownSignal` consumers already read from `transient_signals`
- **260602-riq**: `signal_factory.py` already registers bos_up/bos_down

## Key Code References

### FZ_CONT Strategy Sequences (seed_strategies.py lines 71-117)
```python
# FZ_CONT_BULL
"sequence": [
    {"tag": "choch_up", "weight": 4.0, "required": True, "max_wait": 30 },
    {"tag": "bos_up", "weight": 4.0, "required": True, "max_wait": 30 }
]
"trade_execution": {"entry_type": "LIMIT", "entry_method": "FIRST_HIGH_LOW_PIVOT", ...}

# FZ_CONT_BEAR
"sequence": [
    {"tag": "choch_down", "weight": 4.0, "required": True, "max_wait": 30 },
    {"tag": "bos_down", "weight": 4.0, "required": True, "max_wait": 30 }
]
"trade_execution": {"entry_type": "LIMIT", "entry_method": "FIRST_LOW_HIGH_PIVOT", ...}
```

### BOS Emission Logic (structure.py lines 306-330 and 711-735)
When `zone_base_idx == -1` (no opposing extreme found):
- Set `is_bos=True`, `bos_type="Up"/"Down"`
- Emit tag `bos_up` or `bos_down` with value = tag name
- Log: `[bos] BOS DETECTED: bos_up @ price`

### BOS Consumer Signals (bos_up.py / bos_down.py)
```python
class BOSUpSignal(BaseSignal):
    TAG = "bos_up"
    def calculate(self, df, state_obj, **kwargs):
        return state_obj.transient_signals.get(self.TAG)
```

### Sequence Matching Logic (template.py)
- Reads from `record.signals["events"]` array
- Each event: `{"tag": "choch_up", "t": timestamp}`
- `map_signal_to_candle_record()` connects signal -> record -> events array

## Existing Tests
- `tests/test_structure_*.py`: StructureSignal tests
- `tests/test_strategy_choch_triggers.py`: CHOCH trigger tests
- Pattern: use mock `SymbolState` with `swing_points` and `transient_signals`

## Entry Type Note
FZ_CONT strategies use LIMIT entry. The sequence matching + strategy trigger is the focus of this verification. ORDER dispatch to MT5 is NOT in scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_bos_signal.py for bos_up/bos_down emission</name>
  <files>services/aureus-signal/tests/test_bos_signal.py</files>
  <action>
Create unit test for bos_up/bos_down signal emission in StructureSignal.

1. **Test BOS emission when no opposing extreme**:
   - Set up `swing_points` with HH at t=1000, no LL after it before breakout
   - Set up `df` with candles that break above HH
   - Call `StructureSignal().calculate(df, state_obj)`
   - Assert returned tag is "bos_up" (not "choch")
   - Assert `state_obj.transient_signals["bos_up"]` is not None
   - Assert `swing_points[0]["is_bos"] == True`

2. **Test BOS emission when opposing extreme exists (should be CHOCH)**:
   - Set up `swing_points` with HH at t=1000, LL at t=1100 (opposing extreme)
   - Set up `df` with candles that break above HH but price stays above LL
   - Call `StructureSignal().calculate(df, state_obj)`
   - Assert returned tag is "choch" with value "choch_up" (not bos_up)
   - Assert `state_obj.swing_points[0]["is_choch"] == True`
   - Assert `state_obj.transient_signals["choch_up"]` is not None

3. **Test BOSDown emission**:
   - Set up `swing_points` with LL at t=1000, no HH after it before breakout
   - Set up `df` with candles that break below LL
   - Call `StructureSignal().calculate(df, state_obj)`
   - Assert returned tag is "bos_down"
   - Assert `state_obj.transient_signals["bos_down"]` is not None

4. **Test BOS consumer signal**:
   - Create mock state with `transient_signals = {"bos_up": {"tag": "bos_up", "t": 123456}}`
   - Call `BOSUpSignal().calculate(df, state_obj)`
   - Assert return value equals the bos_up signal dict
   - Same for BOSDownSignal

5. **Test signal_factory registers bos_up/bos_down**:
   - Call `create_signal_set("XAUUSD", {})`
   - Assert `"bos_up"` in signal_set
   - Assert `"bos_down"` in signal_set
   - Assert signal_set["bos_up"] is BOSUpSignal instance
   - Assert signal_set["bos_down"] is BOSDownSignal instance

**Key setup requirements**:
- `swing_points` must have format: `[{"t": int, "price": float, "type": "HH"/"LL", "is_choch": False, "is_bos": False}]`
- `df` must have enough candles (at least 5) for structure detection
- BOS emission requires: swing point broken, NO opposing extreme between pivot and breakout
</action>
  <verify>
    <automated>cd services/aureus-signal && python -m pytest tests/test_bos_signal.py -v --tb=short</automated>
  </verify>
  <done>
    BOS emission logic verified: zone_base_idx=-1 -> bos_up/bos_down emitted.
    BOS consumer signals verified: transient_signals.get(bos_up/bos_down) returns signal.
    Signal factory verified: bos_up/bos_down registered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test_fz_cont_pipeline_e2e.py for FZ_CONT strategy pipeline</name>
  <files>services/aureus-signal/tests/test_fz_cont_pipeline_e2e.py</files>
  <action>
Create integration test for FZ_CONT_BULL and FZ_CONT_BEAR strategies through the full pipeline.

1. **Test choch_up -> bos_up sequence matching**:
   - Set up `log_signal_normalize` with two records:
     - Record 1: `{"signals": {"events": [{"tag": "choch_up", "t": 1000}]}}`
     - Record 2: `{"signals": {"events": [{"tag": "bos_up", "t": 1100}]}}`
   - Set `state_obj.strategy_progress = {}`
   - Call `_evaluate_sequence()` with FZ_CONT_BULL sequence config
   - Assert `matched_steps == 2` and `score >= 8.0`
   - Assert `current_step_index == 2` (sequence complete)

2. **Test choch_down -> bos_down sequence matching**:
   - Same setup with `choch_down` and `bos_down` tags
   - Call with FZ_CONT_BEAR sequence config
   - Assert `matched_steps == 2` and `score >= 8.0`

3. **Test partial sequence (choch only)**:
   - Set up `log_signal_normalize` with only choch_up record
   - Call `_evaluate_sequence()` with FZ_CONT_BULL
   - Assert `matched_steps == 1` and `score >= 4.0`
   - Assert `current_step_index == 1` (waiting for bos_up)
   - Assert `missing_required == False` (step 1 is optional? No - bos_up is required!)
   - Wait: FZ_CONT_BULL has BOTH choch_up AND bos_up as required=True
   - So partial sequence should have `missing_required == True` (waiting for required bos_up)

4. **Test wrong order (bos before choch)**:
   - Set up `log_signal_normalize` with bos_up BEFORE choch_up
   - Call `_evaluate_sequence()`
   - BOS at step index 1 but step 0 expects choch_up -> bos should not match
   - Assert `matched_steps == 1` (only choch_up matched)

5. **Test sequence timeout/reset**:
   - Set up `log_signal_normalize` with choch_up at t=1000
   - Set `max_wait=2` on bos_up step
   - Process records where bos arrives after timeout
   - Assert sequence resets

6. **Test bos without choch (no match)**:
   - Set up `log_signal_normalize` with only bos_up record
   - Call `_evaluate_sequence()` with FZ_CONT_BULL
   - Assert `matched_steps == 0` (no choch_up -> bos_up sequence possible)

7. **Test FULL pipeline: StructureSignal -> transient -> sequence matching**:
   - Set up swing_points with HH at t=1000, LL at t=1050
   - Set up df with candles: HH at 1000, LL at 1050, then breakout above HH at 1100 (no opposing extreme after LL)
   - Call `StructureSignal().calculate()` -> should emit choch_up (LL exists)
   - Then call again with new candle that breaks HH again -> should emit bos_up (no new LL)
   - Verify `transient_signals["choch_up"]` and `transient_signals["bos_up"]` exist
   - Feed into sequence evaluator -> assert FZ_CONT_BULL matches

8. **Test LIMIT entry config**:
   - Load FZ_CONT_BULL strategy config
   - Assert `trade_execution["entry_type"] == "LIMIT"`
   - Assert `trade_execution["entry_method"] == "FIRST_HIGH_LOW_PIVOT"`

**Test FZ_CONT_BULL and FZ_CONT_BEAR separately** with clear comments.
</action>
  <verify>
    <automated>cd services/aureus-signal && python -m pytest tests/test_fz_cont_pipeline_e2e.py -v --tb=short</automated>
  </verify>
  <done>
    FZ_CONT_BULL and FZ_CONT_BEAR verified through:
    - bos_up/bos_down emission from StructureSignal
    - transient_signals wiring
    - sequence matching (choch -> bos)
    - LIMIT entry type verified in config
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full FZ_CONT + BOS test suite</name>
  <action>
Run complete test suite for FZ_CONT and BOS signal components:

```bash
cd services/aureus-signal && python -m pytest tests/test_bos_signal.py tests/test_fz_cont_pipeline_e2e.py tests/test_structure_o1.py tests/test_strategy_choch_triggers.py -v --tb=line
```

Expected:
- test_bos_signal.py: all tests pass (NEW)
- test_fz_cont_pipeline_e2e.py: all tests pass (NEW)
- test_structure_o1.py: existing tests pass
- test_strategy_choch_triggers.py: existing tests pass

Collect summary. If any test fails, investigate and fix.
</action>
  <verify>
    <automated>cd services/aureus-signal && python -m pytest tests/test_bos_signal.py tests/test_fz_cont_pipeline_e2e.py tests/test_structure_o1.py tests/test_strategy_choch_triggers.py -v --tb=line 2>&1 | tail -40</automated>
  </verify>
  <done>
    Full FZ_CONT + BOS test suite passes.
    2 strategies verified through Stage A-E pipeline.
  </done>
</task>

</tasks>

<verification>
## Pipeline Stage Verification Checklist

| Stage | Component | Verify Method | Expected |
|-------|-----------|--------------|----------|
| A | StructureSignal | Unit test with mock swing_points | choch + bos signals emitted |
| B | Signal Bridge | transient_signals populated | bos_up/bos_down in transient |
| C | Context Filter | FZ_CONT has no context filter | auto-pass |
| D | Sequence Matching | choch -> bos sequence | matched_steps=2, score=8.0 |
| E | Full Pipeline | StructureSignal -> sequence | FZ_CONT_BULL/BEAR trigger possible |

## Evidence Criteria
- test_bos_signal.py passes with BOS emission tests
- test_fz_cont_pipeline_e2e.py passes with choch->bos sequence tests
- All existing structure/choch tests still pass
</verification>

<success_criteria>
1. New bos_signal test confirms bos_up/bos_down emission when no opposing extreme
2. New fz_cont_pipeline test confirms choch -> bos sequence matching
3. All existing structure/choch tests still pass (no regression)
4. Evidence of: structure detection -> bos emission -> transient wiring -> sequence match
</success_criteria>

<output>
After completion, create `.planning/phases/58-fix-inactive-trading-strategies/58-02-SUMMARY.md`
</output>
