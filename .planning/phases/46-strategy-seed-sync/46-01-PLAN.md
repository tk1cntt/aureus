---
phase: 46-strategy-seed-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/strategies/seed_strategies.py
  - services/aureus-signal/engine/strategies/registry.py
  - services/aureus-signal/engine/live_engine.py
  - services/aureus-signal/engine/strategy_executor.py
autonomous: true
requirements:
  - PH46-01
  - PH46-02
  - PH46-03
must_haves:
  truths:
    - "Seed chiến lược từ source code được đồng bộ vào aureus_strategy_templates một cách deterministic (upsert theo name)."
    - "Assignment symbol↔strategy không bị xóa cứng; bật/tắt rollout dùng is_active để rollback nhanh."
    - "Sau khi sync, runtime reload strategy và chỉ load những assignment is_active=true."
  artifacts:
    - path: services/aureus-signal/engine/strategies/seed_strategies.py
      provides: declarative seed catalog + DB sync orchestration (upsert template, reconcile assignment)
    - path: services/aureus-signal/engine/strategies/registry.py
      provides: runtime load contract chỉ lấy assignment active
    - path: services/aureus-signal/engine/live_engine.py
      provides: startup/reload wiring gọi sync trước khi load strategy
  key_links:
    - from: services/aureus-signal/engine/strategies/seed_strategies.py
      to: aureus_strategy_templates / aureus_symbol_strategies
      via: asyncpg execute/fetch trong transaction
      pattern: "INSERT INTO aureus_strategy_templates|aureus_symbol_strategies|is_active"
    - from: services/aureus-signal/engine/live_engine.py
      to: services/aureus-signal/engine/strategies/registry.py
      via: seed_sync -> load_from_db theo symbol
      pattern: "seed_system_strategies|load_from_db"
---

<objective>
Thiết lập cơ chế đồng bộ declarative strategy seed từ source code xuống DB để rollout nhất quán, có thể bật/tắt an toàn bằng `is_active` thay vì xóa dữ liệu.

Purpose: Loại bỏ drift giữa code và DB, đảm bảo mọi lần startup/reload có cùng tập strategy theo trạng thái active/inactive.
Output: Seed sync flow thống nhất cho template + symbol assignment + runtime reload path.

Giả định rõ ràng:
- ROADMAP/REQUIREMENTS chưa có IDs chính thức cho Phase 46, tạm dùng PH46-01..03 để traceability.
- Rollback trong scope phase này là rollback trạng thái `is_active`, không rollback schema.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@services/aureus-signal/engine/strategies/seed_strategies.py
@services/aureus-signal/engine/strategies/registry.py
@services/aureus-signal/engine/live_engine.py
@services/aureus-signal/engine/strategy_executor.py
@services/aureus-db-writer/schema.sql

<interfaces>
From services/aureus-signal/engine/strategies/registry.py:
- load_from_db(db, symbol) chỉ query `ss.is_active = true` và join qua `aureus_symbol_strategies`.

From services/aureus-db-writer/schema.sql:
- `aureus_symbol_strategies(symbol, strategy_id, is_active BOOLEAN NOT NULL DEFAULT TRUE, UNIQUE(symbol, strategy_id))`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chuẩn hóa seed catalog và sync contract cho template + assignment</name>
  <files>
- services/aureus-signal/engine/strategies/seed_strategies.py
- services/aureus-signal/tests/test_strategy_seed_sync.py
  </files>
  <read_first>
- services/aureus-signal/engine/strategies/seed_strategies.py
- services/aureus-db-writer/schema.sql
- services/aureus-signal/engine/strategies/registry.py
  </read_first>
  <action>Refactor tối thiểu `seed_system_strategies` để tách rõ 2 bước deterministic: (1) upsert `aureus_strategy_templates` theo `name`; (2) reconcile `aureus_symbol_strategies` theo catalog seed và `SYMBOLS`. Không hard-delete assignment cũ; thay vào đó set `is_active=false` cho pair ngoài catalog hiện tại để giữ khả năng rollback nhanh. Khi pair quay lại catalog thì set lại `is_active=true`. Giữ nguyên style SQL/asyncpg hiện có, không introduce framework mới.</action>
  <acceptance_criteria>
- Có đường sync idempotent: chạy lại nhiều lần không tạo duplicate rows.
- Pair bị remove khỏi seed không bị delete, chỉ bị deactivate (`is_active=false`).
- Pair re-introduce được activate lại (`is_active=true`) bằng sync tiếp theo.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_seed_sync.py -q -k "idempotent or deactivate or reactivate"</automated>
  </verify>
  <done>DB phản ánh đúng seed declarative và có rollback-friendly state qua cờ is_active.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire sync vào startup và refresh/reload path</name>
  <files>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/strategy_executor.py
- services/aureus-signal/tests/test_strategy_seed_sync.py
  </files>
  <read_first>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/strategy_executor.py
- services/aureus-signal/engine/strategies/seed_strategies.py
- services/aureus-signal/engine/strategies/registry.py
  </read_first>
  <action>Trước khi `load_from_db` ở startup và trong listener `aureus:cmd:refresh_strategies`, gọi seed sync để DB luôn ở trạng thái mới nhất từ source code rồi mới reload registry. Đảm bảo reload chỉ nạp strategy active (đang được enforce ở query `is_active=true`). Không mở rộng sang thay đổi protocol Redis; chỉ chỉnh đúng call-order để tránh stale assignment sau deploy.</action>
  <acceptance_criteria>
- Startup path thực thi sync trước load strategy.
- Refresh command path thực thi sync trước reload strategy.
- Runtime sau reload không load assignment bị deactivate.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_seed_sync.py -q -k "startup_reload_order or load_active_only"</automated>
  </verify>
  <done>Runtime luôn dùng tập strategy đã sync, active/inactive nhất quán với source-of-truth.</done>
</task>

<task type="auto">
  <name>Task 3: Thêm logging/audit tối thiểu cho kết quả sync</name>
  <files>
- services/aureus-signal/engine/strategies/seed_strategies.py
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/tests/test_strategy_seed_sync.py
  </files>
  <read_first>
- services/aureus-signal/engine/strategies/seed_strategies.py
- services/aureus-signal/engine/live_engine.py
  </read_first>
  <action>Bổ sung summary log sau mỗi lần sync: số template upsert, số assignment activated/deactivated/unchanged theo symbol. Không log payload config chi tiết để tránh noise; chỉ log counters phục vụ rollback diagnosis. Giữ prefix logging theo convention hiện tại `[GLOBAL]`/`[symbol]`.</action>
  <acceptance_criteria>
- Mỗi lượt sync có 1 dòng summary định lượng dễ grep.
- Có thể phân biệt được activate vs deactivate để audit rollout.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_seed_sync.py -q -k "sync_summary_log"</automated>
  </verify>
  <done>Operator có đủ telemetry để biết sync đã thay đổi gì mà không cần query DB thủ công ngay lập tức.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source seed config -> DB templates | Config trong code đi vào DB, cần deterministic upsert để tránh drift/sai lệch rollout |
| Sync reconciliation -> runtime reload | Sai thứ tự sync/reload có thể làm runtime chạy assignment cũ hoặc vô hiệu rollout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01-01 | T | seed_strategies sync SQL | mitigate | Dùng transaction + upsert theo unique key, không delete cứng assignment |
| T-46-01-02 | D | reload path | mitigate | Enforce call-order: sync trước, load_from_db sau |
| T-46-01-03 | R | sync observability | mitigate | Log summary counters activated/deactivated/upserted cho từng lượt sync |
</threat_model>

<verification>
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_seed_sync.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_trigger_lifecycle.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/unittest/test_orders_events.py -q
</verification>

<success_criteria>
- Seed declarative trong code là source-of-truth duy nhất cho strategy templates và symbol assignments.
- Active/inactive rollout dùng `is_active` hoạt động 2 chiều (disable + re-enable) không cần delete dữ liệu.
- Startup/reload đều tiêu thụ trạng thái đã sync, giảm drift sau deploy.
</success_criteria>

<output>
After completion, create `.planning/phases/46-strategy-seed-sync/46-01-SUMMARY.md`
</output>
