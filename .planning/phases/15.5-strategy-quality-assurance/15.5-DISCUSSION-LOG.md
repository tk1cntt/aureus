# Phase 15.5: Strategy Quality Assurance - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-23
**Phase:** 15.5-strategy-quality-assurance
**Areas discussed:** Test Data Source, Baseline Scope, Strategy Coverage, Replay Tool Output

---

## Test Data Source

| Option | Description | Selected |
|--------|-------------|----------|
| Real DB only | Query `aureus_candles` directly for all tests | |
| Exported CSV only | Frozen CSV datasets, deterministic, CI-friendly | |
| Hybrid (DB + CSV) | DB for replay tool, exported CSV for unit/scenario tests | ✓ |

**User's choice:** Hybrid — "Dùng DB cho replay tool và export data hiện tại từ TimescaleDB ra file rồi dùng cho unit/scenario tests"
**Notes:** CSV files committed to repo as frozen test data. Replay tool connects to TimescaleDB directly.

**Follow-up: Export scope**

| Option | Description | Selected |
|--------|-------------|----------|
| 100 bars/scenario | Lightweight, fast tests | |
| 500 bars/scenario | Good coverage for sequence matching | |
| 2000 bars/symbol, all symbols | Full coverage across all market conditions | ✓ |

**User's choice:** 2000 bars per symbol, all symbols currently in DB

---

## Baseline Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Tier 1 — Basic | Trigger count, rate, reject count | |
| Tier 2 — Detailed | + Score distribution, signal contribution, timing by session, context filter breakdown | ✓ |
| Tier 3 — Deep | + Per-candle state dump, correlation analysis | |

**User's choice:** Tier 2 for all symbols
**Notes:** Includes score distribution, signal contribution, trigger timing by session, context filter pass/fail

---

## Strategy Coverage

| Option | Description | Selected |
|--------|-------------|----------|
| 3 seeds only | Dedicated tests for TREND_CONT, SESSION_SWEEP, ORDER_FLOW_DOM | |
| Extensible only | Framework tests any TemplateStrategy JSON config | |
| 3 seeds + extensible | Seeds have dedicated assertions + framework for custom configs | ✓ |

**User's choice:** Option C — 3 seed strategies with dedicated tests + extensible framework for custom configs
**Notes:** Seed strategies get known expected behavior assertions. Custom configs get basic validation (trigger count > 0, determinism).

---

## Replay Tool Output

| Option | Description | Selected |
|--------|-------------|----------|
| Console only | Print JSON/table to stdout | |
| JSON file | Save to `reports/` folder | |
| Console + JSON | Summary console + detail file | |
| Persist to DB | Store in TimescaleDB table | ✓ |

**User's choice:** Persist to TimescaleDB
**Notes:** Dedicated table for replay results (separate from backtest results)

**Follow-up: CLI modes**

| Option | Description | Selected |
|--------|-------------|----------|
| Single | `--symbol X --strategy Y` | ✓ |
| Batch | `--symbol X` (all strategies) | ✓ |
| Full matrix | No args (all × all) | ✓ |

**User's choice:** All 3 modes — single, batch, and full matrix
**Notes:** Replay tool supports all 3 via CLI arguments

---

## Agent's Discretion

- DB table schema for replay results
- CSV export script implementation details
- Test file organization within `tests/`

## Deferred Ideas

None — discussion stayed within phase scope
