---
phase: 43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - services/aureus-signal/engine/snapshot_utils.py
  - services/aureus-signal/engine/live_engine.py
  - services/aureus-db-writer/schema.sql
  - services/aureus-signal/tests/test_snapshot_mtf_fields.py
  - services/aureus-signal/tests/test_snapshot_null_policy.py
autonomous: true
requirements: [PH43-01, PH43-02, PH43-03, PH43-04]

must_haves:
  truths:
    - "Mỗi nến M1 persist snapshot có đủ candle_color_d1/h1/m30/m15/m5"
    - "Mỗi snapshot persist có đủ bb_m1/m5/m15/m30/h1 theo object upper/middle/lower"
    - "Khi thiếu dữ liệu TF thì field mới tương ứng là null, không phải placeholder"
    - "Field indicator cũ vẫn giữ nghĩa và không bị thay đổi contract"
  artifacts:
    - path: "services/aureus-signal/engine/snapshot_utils.py"
      provides: "build_snapshot + insert queries đã mở rộng fields mới additive"
    - path: "services/aureus-db-writer/schema.sql"
      provides: "DB schema có cột mới cho candle color và BB MTF"
      contains: "candle_color_d1"
    - path: "services/aureus-signal/tests/test_snapshot_mtf_fields.py"
      provides: "Integration-style tests cho payload mới đi qua build_snapshot"
    - path: "services/aureus-signal/tests/test_snapshot_null_policy.py"
      provides: "Regression tests bảo vệ null-first + backward compatibility"
  key_links:
    - from: "services/aureus-signal/engine/live_engine.py"
      to: "services/aureus-signal/engine/snapshot_utils.py"
      via: "build_snapshot(state, candle) mỗi M1"
      pattern: "build_snapshot\("
    - from: "services/aureus-signal/engine/snapshot_utils.py"
      to: "services/aureus-db-writer/schema.sql"
      via: "insert columns khớp schema columns"
      pattern: "candle_color_d1|bb_m1"
---

<objective>
Wire helper MTF vào snapshot persistence path và đồng bộ schema để dữ liệu mới thật sự xuống DB theo cadence M1.

Purpose: Biến logic đã khóa ở Plan 01 thành dữ liệu persisted dùng được cho downstream analytics.
Output: snapshot_utils/live_engine/schema được cập nhật đồng bộ + test xác nhận contract additive và null-first.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-CONTEXT.md
@.planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-RESEARCH.md
@.planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-01-SUMMARY.md

<interfaces>
From services/aureus-signal/engine/snapshot_utils.py:
```python
def build_snapshot(state, candle: Dict[str, Any]) -> Dict[str, Any]
async def insert_single_snapshot(db_pool, snapshot: Dict[str, Any], table_name: str = "aureus_signal_snapshots")
async def batch_insert_snapshots(db_pool, snapshots: List[Dict[str, Any]], table_name: str = "aureus_signal_snapshots")
```

Current table contract in services/aureus-db-writer/schema.sql:
```sql
CREATE TABLE IF NOT EXISTS aureus_signal_snapshots (
  time TIMESTAMPTZ NOT NULL,
  symbol TEXT NOT NULL,
  ...
  strategy_progress JSONB,
  PRIMARY KEY (time, symbol)
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mở rộng schema DB + snapshot insert contract theo additive fields</name>
  <files>services/aureus-db-writer/schema.sql, services/aureus-signal/engine/snapshot_utils.py</files>
  <read_first>
    - services/aureus-db-writer/schema.sql
    - services/aureus-signal/engine/snapshot_utils.py
    - .planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: bảng `aureus_signal_snapshots` có cột candle_color_d1/h1/m30/m15/m5 theo D-02.
    - Test 2: bảng có cột bb_m1/m5/m15/m30/h1 với object JSON `{upper,middle,lower}` theo D-04, D-06.
    - Test 3: insert_single_snapshot và batch_insert_snapshots cùng danh sách cột mới, không lệch số lượng placeholders.
  </behavior>
  <action>Cập nhật `schema.sql` để thêm cột mới nullable theo naming đã khóa (D-02, D-06), giữ additive-only (D-12). Cập nhật SQL trong `insert_single_snapshot` và `batch_insert_snapshots` để persist cùng bộ field mới ở cả 2 đường ghi (mitigation drift). Nếu chọn lưu BB dạng JSONB thì từng cột bb_<tf> dùng JSONB và value chuẩn object upper/middle/lower; không chuyển sang status rút gọn (D-04).</action>
  <acceptance_criteria>
    - schema.sql chứa đầy đủ 5 cột candle_color_* và 5 cột bb_*.
    - Cả single + batch insert queries dùng cùng danh sách cột mới.
    - Không sửa nghĩa hoặc rename field indicator cũ.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest D:/Aureus/services/aureus-signal/tests/test_snapshot_mtf_fields.py -q</automated>
  </verify>
  <done>Schema và insert contracts đồng bộ hoàn toàn, không còn risk lệch single/batch path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire build_snapshot để gắn candle color + BB MTF mỗi M1</name>
  <files>services/aureus-signal/engine/snapshot_utils.py, services/aureus-signal/engine/live_engine.py, services/aureus-signal/tests/test_snapshot_mtf_fields.py</files>
  <read_first>
    - services/aureus-signal/engine/mtf_snapshot.py
    - services/aureus-signal/engine/snapshot_utils.py
    - services/aureus-signal/engine/live_engine.py
  </read_first>
  <behavior>
    - Test 1: build_snapshot output có đủ candle_color_d1/h1/m30/m15/m5 (D-02, D-07).
    - Test 2: build_snapshot output có đủ bb_m1/m5/m15/m30/h1 (D-05, D-07).
    - Test 3: live_engine cadence không đổi — vẫn ghi mỗi M1 (D-09).
  </behavior>
  <action>Tích hợp helper từ plan 01 vào `build_snapshot()` để map field mới theo D-01..D-09. Đảm bảo uppercase values (D-03), last-closed semantics cho TF>M1 (D-08), và không chạm trigger/notifier logic ngoài scope. `live_engine.py` chỉ chỉnh tối thiểu nếu cần truyền context dữ liệu phục vụ MTF snapshot, không thay đổi luồng trigger strategy.</action>
  <acceptance_criteria>
    - build_snapshot trả đủ 10 field mới với naming chính xác.
    - Cadence ghi snapshot M1 giữ nguyên, không thêm điều kiện chờ HTF close.
    - Existing fields (`emas`, `atr_14`, `vol_sma_20`, `htf_trend`, `cisd_mtf`) không đổi nghĩa (D-12).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest D:/Aureus/services/aureus-signal/tests/test_snapshot_mtf_fields.py D:/Aureus/services/aureus-signal/tests/test_mtf_last_closed_rule.py -q</automated>
  </verify>
  <done>Snapshot payload mới xuất hiện ổn định ở nhịp M1 và không phá contract cũ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bổ sung regression test cho null-first và backward compatibility</name>
  <files>services/aureus-signal/tests/test_snapshot_null_policy.py, services/aureus-signal/tests/test_snapshot_mtf_fields.py</files>
  <read_first>
    - services/aureus-signal/tests/test_snapshot_mtf_fields.py
    - .planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: thiếu dữ liệu TF lớn thì candle_color_<tf> = null (D-10).
    - Test 2: thiếu dữ liệu TF lớn thì bb_<tf> = null (D-10).
    - Test 3: field cũ của snapshot vẫn tồn tại và pass assertions cũ (D-12).
  </behavior>
  <action>Tạo test file `test_snapshot_null_policy.py` xác nhận null-first policy cho cả candle color và BB; thêm asserts backward-compatible trong test_mtf_fields để bảo vệ additive contract per D-12. Không dùng placeholder 0/UNKNOWN để “qua test”.</action>
  <acceptance_criteria>
    - Có test explicit bắt trường hợp null cho từng nhóm field mới.
    - Test fail nếu xuất hiện sentinel values thay null.
    - Full targeted test suite phase 43 pass.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest D:/Aureus/services/aureus-signal/tests/test_snapshot_mtf_fields.py D:/Aureus/services/aureus-signal/tests/test_snapshot_null_policy.py D:/Aureus/services/aureus-signal/tests/test_mtf_candle_color.py D:/Aureus/services/aureus-signal/tests/test_mtf_bb_snapshot.py D:/Aureus/services/aureus-signal/tests/test_mtf_last_closed_rule.py -q</automated>
  </verify>
  <done>Null policy và compatibility được khóa bằng regression tests tự động.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal engine snapshot payload → DB schema | Dữ liệu JSON và text mới đi qua SQL insert, cần chống drift contract |
| in-memory indicator calc → persisted historical table | Sai semantics có thể làm hỏng analytics/replay dài hạn |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05 | Tampering | snapshot_utils single/batch SQL | mitigate | Đồng bộ cùng cột mới ở cả 2 query path + test regression so khớp count fields |
| T-43-06 | Integrity | build_snapshot MTF mapping | mitigate | Ép naming cứng `candle_color_*` và `bb_*` theo D-02, D-06; test fail khi lệch key |
| T-43-07 | Integrity | null handling | mitigate | Null-first assertions cho từng TF, cấm sentinel values theo D-10 |
| T-43-08 | Repudiation | DB historical snapshots | accept | Scope phase không thêm audit signature; chấp nhận vì nội bộ trusted pipeline |

## Severity handling
- Nếu phát hiện mismatch cột giữa schema và insert paths hoặc vi phạm D-10 null policy => HIGH, phải fix ngay trước khi kết thúc plan.
</threat_model>

<verification>
- Targeted pytest suite phase 43 pass.
- Manual grep nhanh xác nhận đủ key naming mới trong snapshot_utils + schema.sql.
- Không có thay đổi trigger strategy hoặc notifier ngoài scope phase.
</verification>

<success_criteria>
- Snapshot dữ liệu mới xuống DB theo M1 cadence với semantics ổn định.
- Không có drift giữa single insert và batch insert.
- Backward compatibility được chứng minh bằng test.
</success_criteria>

<output>
After completion, create `.planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-02-SUMMARY.md`
</output>
