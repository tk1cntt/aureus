---
phase: "15.10"
plan: "03"
title: "Execute signal field unification and missing sweep/OB event investigation"
wave: 1
depends_on: [15.10-02]
files_modified:
  - services/aureus-signal/engine/signals/trend.py
  - services/aureus-signal/engine/signals/sweep.py
  - services/aureus-signal/engine/signals/structure.py
  - services/aureus-signal/engine/live_engine.py
  - services/aureus-signal/engine/backtest_engine.py
  - services/aureus-signal/signal_computer.py
  - services/aureus-signal/engine/state.py
  - services/aureus-signal/tests/test_signal_contract_normalization.py
  - services/aureus-signal/tests/test_sweep_signal.py
autonomous: true
requirements_addressed: [INTERNAL-SIGNAL-CONSISTENCY, INTERNAL-SWEEP-OBSERVABILITY]
---

<objective>
Thực thi 2 todo pending trọng điểm của phase 15.10:
1) loại bỏ `market_regime` và chuẩn hóa toàn bộ pipeline sang `htf_trend`.
2) điều tra/fix thiếu event `sweep` và `MITIGATED` trong `signal_history_normalized` sau `BROKEN_PENDING`, bao gồm bỏ `Regime Check` đang chặn trigger hợp lệ.
</objective>

<must_haves>
- Không còn đường đọc/ghi runtime phụ thuộc `market_regime`
- `signal_history_normalized` có đủ event sweep lifecycle và event `MITIGATED` ở các case đã nêu
- Event không bị nuốt bởi guard/dedup sai (`already_swept`, `status_tag`, normalize mapping)
- Có regression tests cho `choch down + sweep` và `MITIGATED`
</must_haves>

---

<task id="15.10-03-T1" title="Migrate market_regime references to canonical htf_trend">
<read_first>
- services/aureus-signal/engine/signals/trend.py
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/signals/sweep.py
</read_first>
<action>
1. Inventory toàn bộ chỗ đọc/ghi `market_regime` trong runtime + normalize path.
2. Chuyển sang field canonical `htf_trend` với compatibility guard ngắn hạn nếu cần.
3. Cập nhật tests/contracts để đảm bảo không còn assertion dựa trên `market_regime`.
</action>
<acceptance_criteria>
- `rg "market_regime"` không còn kết quả trong runtime path mục tiêu (trừ migration note nếu có)
- Runtime output dùng nhất quán `htf_trend`
</acceptance_criteria>
</task>

<task id="15.10-03-T2" title="Trace and fix missing sweep or MITIGATED events after BROKEN_PENDING">
<read_first>
- services/aureus-signal/engine/signals/sweep.py
- services/aureus-signal/engine/signals/structure.py
- services/aureus-signal/engine/state.py
</read_first>
<action>
1. Trace state transition từ `BROKEN_PENDING` sang các trạng thái kế tiếp để tìm điểm chặn event.
2. Kiểm tra thứ tự `_update_ob_states` và khối emit event trong cùng candle.
3. Loại bỏ `Regime Check` khỏi điều kiện trigger sweep event.
4. Rà logic guard/dedup (`already_swept`, `status_tag`) và normalize mapping để tránh mất event.
</action>
<acceptance_criteria>
- Case `choch down` sweep qua OB phát sinh event đúng
- Case OB `MITIGATED` xuất hiện trong `signal_history_normalized`
- Không chỉ còn mỗi `BROKEN_PENDING` như hiện trạng lỗi
</acceptance_criteria>
</task>

<task id="15.10-03-T3" title="Add regression coverage and verify output contract">
<read_first>
- services/aureus-signal/tests/test_sweep_signal.py
- services/aureus-signal/tests/test_signal_contract_normalization.py
</read_first>
<action>
1. Bổ sung test cho case `choch down + sweep` và `MITIGATED`.
2. Verify output `signal_history_normalized` có đủ event theo contract.
3. Chạy detect changes để xác nhận phạm vi thay đổi đúng kỳ vọng.
</action>
<acceptance_criteria>
- Test case mới pass ổn định
- Không có regression ở contract normalization hiện tại
- Scope thay đổi tập trung đúng engine signal + tests liên quan
</acceptance_criteria>
</task>

---

<verification>
1. `pytest tests/test_sweep_signal.py -q`
2. `pytest tests/test_signal_contract_normalization.py -q`
3. `pytest tests/test_decision_trace_schema.py tests/test_state_snapshot.py -q`
4. `rg -n "market_regime|htf_trend|BROKEN_PENDING|MITIGATED|sweep" services/aureus-signal/engine services/aureus-signal/tests`
5. `gitnexus_detect_changes(scope="all")`
</verification>
