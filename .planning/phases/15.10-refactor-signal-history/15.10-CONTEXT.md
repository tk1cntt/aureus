# Phase 15.10: Refactor signal-history contract (reopened) — Context

**Gathered:** 2026-03-27
**Status:** Planned (Reopened)

<domain>
## Phase Boundary

Ổn định contract `signal_history/log_signal` sau rollback cục bộ để loại bỏ mismatch giữa state layer và runtime call-sites.

**In scope:**
- Chốt contract canonical cho `log_signal`
- Đồng bộ call-sites live/backtest/precompute theo contract này
- Xác nhận compatibility ở snapshot/consumer path

**Out of scope:**
- Refactor toàn phần sang typed polymorphic models trên mọi tầng
- Thay đổi chiến lược trading/business rules
- Tối ưu performance ngoài phạm vi contract drift
</domain>

<problem_statement>
## Current Symptom

- `SymbolState.log_signal` đang ở legacy signature (`tag`, `timestamp`, `value`, `data`).
- Nhiều đường gọi runtime vẫn dùng metadata kwargs (`category`, `explain`, `inputs`).
- Drift này có nguy cơ gây lỗi runtime hoặc mất nhất quán payload.
</problem_statement>

<hypotheses>
## Working Hypotheses

1. Khôi phục/chuẩn hóa contract metadata-aware ở state layer là cách ít phá vỡ nhất.
2. Regression chính xuất phát từ mismatch call-site chứ không phải logic signal calculators.
3. Nếu snapshot normalize hỗ trợ cả legacy + new shape thì migration sẽ an toàn.
</hypotheses>

<investigation_path>
## Investigation Path

- Checkpoint A: xác định canonical signature tại state layer.
- Checkpoint B: reconcile call-sites (`live_engine`, `backtest_engine`, `signal_computer`).
- Checkpoint C: verify consumer/snapshot path (`ai_validator`, `state_snapshot`).
- Checkpoint D: chạy test + smoke runtime để xác nhận không drift còn sót.
</investigation_path>

<expected_outputs>
## Expected Outputs for Next Steps

- Canonical contract mô tả rõ và nhất quán.
- Danh sách call-sites đã reconcile hoàn tất.
- Verification evidence cho tests contract/snapshot + smoke runtime.
</expected_outputs>
