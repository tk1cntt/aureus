---
phase: 02
plan: 01
title: Signal EMA optimization and verification gates
wave: 1
depends_on: []
autonomous: true
requirements: [SIG-02, TST-01, TST-02, TST-03, TST-04, VAL-01, VAL-02]
files_modified:
  - services/aureus-signal/engine/signals/ema.py
  - services/aureus-signal/tests/test_ema_o1.py
  - services/aureus-signal/tests/test_ema_integration_execute_signals_for_candle.py
  - .planning/phases/02-signal-ema/02-VALIDATION.md
---

# Phase 02 Plan 01 — Signal EMA Optimization and Verification Gates

## Objective
Optimize and harden EMA signal behavior while preserving existing event semantics, then close all phase quality gates (unit + integration + coverage >= 80%).

<tasks>
  <task id="02-01-01" type="auto">
    <name>Harden EMA state handling without changing output contract</name>
    <read_first>
      - services/aureus-signal/engine/signals/ema.py
      - services/aureus-signal/engine/signals/base.py
      - .planning/phases/02-signal-ema/2-CONTEXT.md
    </read_first>
    <action>
      Update `EMASignal.calculate` in `ema.py` to keep exact output contract (`tag`, optional `cross`, `period`, `value`, `t`) while hardening cached-state logic. Keep warmup and O(1) paths, but make fallback behavior explicit when cache shape is invalid or keys are missing. Preserve existing tag naming (`ema_{period}_up|down|cross_up|cross_down`) and keep state writeback keys exactly `current`, `prev`, `slope`.
    </action>
    <acceptance_criteria>
      - `services/aureus-signal/engine/signals/ema.py` contains `"current"`, `"prev"`, and `"slope"` in state writeback.
      - `services/aureus-signal/engine/signals/ema.py` still sets `res` with `"t": curr_t`.
      - `services/aureus-signal/engine/signals/ema.py` still references `self.tag_cross_up` and `self.tag_cross_down` for crossover output.
    </acceptance_criteria>
  </task>

  <task id="02-01-02" type="auto">
    <name>Add dedicated EMA integration tests for runtime signal execution path</name>
    <read_first>
      - services/aureus-signal/tests/test_atr_integration_execute_signals_for_candle.py
      - services/aureus-signal/engine/live_engine.py
      - services/aureus-signal/tests/test_ema_o1.py
    </read_first>
    <action>
      Create `services/aureus-signal/tests/test_ema_integration_execute_signals_for_candle.py` following ATR integration style. Use `WindowManager` + `execute_signals_for_candle` with `EMASignal(21)` and deterministic candles. Add assertions that EMA tags are emitted after warmup, state cache for period 21 progresses between candles, and `signal_history` contains EMA entries with `t` matching candle timestamp.
    </action>
    <acceptance_criteria>
      - `services/aureus-signal/tests/test_ema_integration_execute_signals_for_candle.py` exists.
      - The test file imports `execute_signals_for_candle`, `WindowManager`, and `EMASignal`.
      - The test file contains at least one assertion for EMA tag emission and one assertion for timestamp key `t` in signal history.
    </acceptance_criteria>
  </task>

  <task id="02-01-03" type="auto">
    <name>Expand EMA unit checks and prove gate coverage for phase closure</name>
    <read_first>
      - services/aureus-signal/tests/test_ema_o1.py
      - .planning/phases/02-signal-ema/02-VALIDATION.md
      - .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Extend `test_ema_o1.py` with edge-case assertions for fallback behavior when cached EMA state for target period is malformed or missing required keys, ensuring the pandas batch fallback path remains deterministic. Run phase verification commands from `02-VALIDATION.md` and update the validation status table/checklist to reflect real command outcomes for unit, integration, and coverage gates.
    </action>
    <acceptance_criteria>
      - `services/aureus-signal/tests/test_ema_o1.py` includes a test case that seeds malformed cache data and verifies successful EMA output.
      - `d:/Aureus/.planning/phases/02-signal-ema/02-VALIDATION.md` shows updated task statuses after command execution.
      - Coverage command output for `engine/signals/ema.py` is recorded and indicates >= 80%.
    </acceptance_criteria>
  </task>
</tasks>

## Verification
- `python -m pytest services/aureus-signal/tests/test_ema_o1.py services/aureus-signal/tests/test_ema_integration_execute_signals_for_candle.py -q`
- `python -m pytest services/aureus-signal/tests -k "ema" -q`
- `python -m pytest services/aureus-signal/tests/test_ema_o1.py services/aureus-signal/tests/test_ema_integration_execute_signals_for_candle.py --cov=services/aureus-signal/engine/signals/ema.py --cov-report=term-missing -q`

## Success Criteria
- EMA output contract remains backward-compatible with current tag/cross naming.
- Dedicated EMA unit + integration tests are present and passing.
- Coverage for EMA phase scope is >= 80%.
- Requirements mapped in this plan are demonstrably addressed with test evidence.

## must_haves
- No change to trading intent semantics for EMA classification and crossover events.
- Deterministic behavior between warmup and incremental update paths.
- Phase 02 closes only after all TST gates are green.
