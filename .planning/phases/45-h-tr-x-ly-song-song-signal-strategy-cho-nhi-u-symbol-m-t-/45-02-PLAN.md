---
phase: 45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - services/aureus-signal/engine/strategy_executor.py
  - services/aureus-signal/engine/orders.py
  - services/aureus-signal/tests/test_strategy_trigger_lifecycle.py
  - services/aureus-signal/unittest/test_orders_events.py
autonomous: true
requirements:
  - PH45-04
  - PH45-05
must_haves:
  truths:
    - "Strategy executor xử lý song song theo symbol và vẫn đảm bảo đúng snapshot của cùng candle."
    - "Trigger/order bị dedupe strict theo symbol+candle thông qua trace_id nhất quán."
    - "Không phát sinh duplicate trigger/order khi có retry hoặc replay entry."
  artifacts:
    - path: services/aureus-signal/engine/strategy_executor.py
      provides: Strategy worker per-symbol đọc stream signals và evaluate độc lập
    - path: services/aureus-signal/engine/orders.py
      provides: Dedupe trace_id strict symbol+candle cho trigger lifecycle
    - path: services/aureus-signal/tests/test_strategy_trigger_lifecycle.py
      provides: Contract test snapshot-cùng-candle cho strategy trigger
    - path: services/aureus-signal/unittest/test_orders_events.py
      provides: Unit tests trace_id dedupe strict
  key_links:
    - from: services/aureus-signal/engine/strategy_executor.py
      to: aureus:stream:{symbol}:signals
      via: per-symbol executor consumer
      pattern: "aureus:stream:{symbol}:signals"
    - from: services/aureus-signal/engine/strategy_executor.py
      to: services/aureus-signal/engine/orders.py
      via: process_triggers với trace_id cùng candle
      pattern: "trace_id|origin_timestamp"
---

<objective>
Mở rộng song song sang strategy executor theo per-symbol worker và khóa chặt consistency trigger/order theo snapshot cùng candle + trace_id strict.

Purpose: Hoàn tất yêu cầu song song đồng thời signal + strategy mà vẫn bảo toàn idempotency giao dịch.
Output: Strategy runtime song song per-symbol + test consistency snapshot/trace dedupe.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-CONTEXT.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-RESEARCH.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VALIDATION.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-01-SUMMARY.md
@services/aureus-signal/engine/strategy_executor.py
@services/aureus-signal/engine/orders.py

<interfaces>
From services/aureus-signal/engine/strategy_executor.py:
```python
async def run_strategy_executor(db_pool=None, redis_client=None)
async def emit_registry_rejections(redis_client: Any, symbol: str, enriched_rejections: list[dict])
```

From services/aureus-signal/engine/orders.py:
```python
class SimulatedTradeManager:
    async def process_triggers(self, symbol: str, triggers: List[Dict[str, Any]], state_obj: Any, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Viết test consistency snapshot-cùng-candle và trace_id strict</name>
  <files>services/aureus-signal/tests/test_strategy_trigger_lifecycle.py, services/aureus-signal/unittest/test_orders_events.py</files>
  <read_first>
- services/aureus-signal/engine/strategy_executor.py
- services/aureus-signal/engine/orders.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-CONTEXT.md
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: Strategy evaluation chỉ chấp nhận payload snapshot có cùng `t` của candle đang xử lý (per D-07).
    - Test 2: trace_id tạo theo `symbol:strategy_id:origin_timestamp` và dedupe strict (per D-08).
    - Test 3: Replay/retry cùng trigger không sinh thêm order event mới.
  </behavior>
  <action>Mở rộng test lifecycle để khóa D-07/D-08: payload signal phải giữ `t` đồng nhất giữa `current_signal` và `signals_snapshot`; nếu lệch candle thì reject có reason rõ ràng. Bổ sung unit test orders để đảm bảo trace_id strict theo symbol+candle và chống duplicate khi cùng strategy trigger lặp lại. Không thay đổi policy kinh doanh khác ngoài snapshot/trace consistency.</action>
  <acceptance_criteria>
- `test_strategy_trigger_lifecycle.py` có case mismatch candle timestamp bị chặn.
- `test_orders_events.py` có case duplicate trace_id không phát sinh order mới.
- Test phản ánh đúng format trace_id `symbol:strategy_id:origin_timestamp`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_trigger_lifecycle.py /d/Aureus/services/aureus-signal/unittest/test_orders_events.py -q -x</automated>
  </verify>
  <done>Consistency contracts D-07 và D-08 được khóa bằng test trước khi đổi runtime executor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strategy executor per-symbol worker + strict trace lifecycle</name>
  <files>services/aureus-signal/engine/strategy_executor.py, services/aureus-signal/engine/orders.py</files>
  <read_first>
- services/aureus-signal/engine/strategy_executor.py
- services/aureus-signal/engine/orders.py
- services/aureus-signal/tests/test_strategy_trigger_lifecycle.py
- services/aureus-signal/unittest/test_orders_events.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-01-SUMMARY.md
  </read_first>
  <action>Cập nhật `strategy_executor.py` theo mô hình router + worker per-symbol giống signal layer (D-02, D-03), nhưng giữ strict FIFO trong mỗi symbol (D-05). Trước khi `evaluate_all`/`process_triggers`, assert payload snapshot cùng candle t (D-07); mismatch thì reject + ack entry để tránh treo queue. Cập nhật `orders.py` để dùng trace_id strict symbol+candle xuyên suốt và tăng guard dedupe cho replay/retry (D-08), không đổi format event downstream hiện có. Tuyệt đối không fallback direction từ strategy name trong phần này (tuân theo decision phase 36 đã chốt).</action>
  <acceptance_criteria>
- `strategy_executor.py` có luồng per-symbol worker thay cho loop tập trung.
- Có kiểm tra bắt buộc snapshot cùng candle trước trigger processing.
- `orders.py` giữ trace_id strict và chống duplicate ở cả history key và trigger key.
- Không đổi schema event `type` + `data` hiện hành.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_trigger_lifecycle.py /d/Aureus/services/aureus-signal/unittest/test_orders_events.py /d/Aureus/services/aureus-signal/tests/test_multi_symbol.py -q -x</automated>
  </verify>
  <done>Strategy layer song song per-symbol và idempotency trace/candle đúng D-02, D-07, D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal payload stream -> strategy evaluator | Payload có thể lệch candle hoặc replay gây trigger sai |
| strategy trigger -> orders publisher | Duplicate/replay có thể tạo order trùng |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | T | strategy snapshot contract | mitigate | Assert `payload.t` đồng nhất snapshot trước evaluate, reject mismatch |
| T-45-05 | T | trace_id dedupe in orders | mitigate | Dùng trace_id strict symbol+candle + set membership check trước publish |
| T-45-06 | D | executor centralized loop | mitigate | Chuyển sang per-symbol worker để tránh symbol lỗi làm nghẽn toàn executor |
</threat_model>

<verification>
- Chạy GitNexus impact analysis upstream cho mọi symbol/hàm sẽ sửa trong plan này và ghi blast radius trước khi code.
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_trigger_lifecycle.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/unittest/test_orders_events.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_multi_symbol.py -q
- Chạy gitnexus_detect_changes() trước commit để xác nhận phạm vi thay đổi đúng plan.
</verification>

<success_criteria>
- D-02, D-07, D-08 được triển khai đầy đủ ở strategy/execution path.
- Không có duplicate order khi replay cùng trace_id.
- Snapshot mismatch không đi tiếp vào trigger processing.
</success_criteria>

<output>
After completion, create `.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-02-SUMMARY.md`
</output>
