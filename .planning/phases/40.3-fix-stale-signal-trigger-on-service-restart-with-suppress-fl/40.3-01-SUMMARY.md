# Phase 40.3: Fix Stale Signal Trigger on Service Restart — Summary

**Plan:** 40.3-01-PLAN.md
**Status:** ✅ COMPLETE
**Commit:** 47ad1de
**Date:** 2026-04-13

## What Changed

| File | Lines Added | Description |
|------|-------------|-------------|
| `services/aureus-signal/engine/live_engine.py` | +14, -4 | Suppress flag implementation |

## Implementation Details

### 6 Targeted Changes in `live_engine.py`:

1. **Snapshot restore path (line 370):** `state._signal_suppressed = True` — set after `restore_to_state()`
2. **No-snapshot path (line 406):** `state._signal_suppressed = True` — set after "No snapshot found" logger
3. **Delta loop cleanup (line 407):** `state.transient_signals = {}` — clear after delta loop, before `else:` block
4. **evaluate_all guard (line 478):** `if not getattr(state, "_signal_suppressed", False):` — skip strategy eval during warmup
5. **Gateway reset (line 704-707):** Reset flag on first gateway candle with INFO log
6. **Publish gate (line 750):** Prepend `_signal_suppressed` check to `transient_signals` gate

## Verification

| Check | Result |
|-------|--------|
| `_signal_suppressed = True` count | ✅ 2 (line 370 + 410) |
| `_signal_suppressed = False` count | ✅ 1 (line 707) |
| `not getattr(state, "_signal_suppressed", False)` count | ✅ 2 (line 478 + 750) |
| `state.transient_signals = {}` added after delta loop | ✅ 1 new (line 407) |
| Only `live_engine.py` modified | ✅ |
| Git diff scope | ✅ Single file |

## Success Criteria

- [x] `_signal_suppressed = True` set in snapshot restore path
- [x] `_signal_suppressed = True` set in no-snapshot path
- [x] `evaluate_all` call guarded with suppress check
- [x] `publish_signal_event` gate includes suppress check
- [x] `_signal_suppressed = False` reset on first gateway candle with INFO log
- [x] `transient_signals = {}` cleared after delta loop
- [x] Only `live_engine.py` modified
- [x] No other code paths affected
- [x] Ephemeral flag excluded from `to_dict()` serialization (verified by design — pattern matches `ai_update_pending`)

## How It Works

```
Service restart
  → Hydrate snapshot → _signal_suppressed = True
  → Process delta candles → signals calculated but suppressed
  → evaluate_all SKIPPED (suppressed)
  → transient_signals cleared after delta loop
  → Engine ready (still suppressed)
  → First gateway candle arrives → _signal_suppressed = False
  → Log: "[XAUUSD] Signal suppression cleared — first real-time candle received"
  → Normal operation resumes
```

## Next Steps

- Restart `aureus-signal-dev` service and verify:
  - No SIGNAL_EVENT published during warmup
  - No Telegram alerts on restart
  - "Signal suppression cleared" log appears within seconds
  - All 8 symbols process candles normally after suppression clears
