---
phase: 54-strategy-scoring-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/scoring/models.py
  - services/aureus-signal/engine/scoring/gate.py
  - services/aureus-signal/engine/scoring/normalize.py
  - services/aureus-signal/engine/scoring/compute.py
  - services/aureus-signal/tests/test_strategy_scoring_formula.py
  - services/aureus-signal/tests/test_strategy_scoring_versioning.py
  - services/aureus-signal/tests/test_strategy_scoring_breakdown.py
autonomous: true
requirements:
  - SCOR-01
  - SCOR-02
  - SCOR-04
must_haves:
  truths:
    - "Trade không đạt quality gate bị chặn trước khi tính weighted-sum."
    - "Mỗi lần tính score dùng score_version semantic và weights_snapshot bất biến."
    - "Kết quả per-trade luôn có score_breakdown JSON kèm missing_data_policy rõ ràng."
  artifacts:
    - path: "services/aureus-signal/engine/scoring/models.py"
      provides: "Pydantic contract cho ScoringResult, criterion breakdown, version snapshot"
    - path: "services/aureus-signal/engine/scoring/gate.py"
      provides: "Stage-1 quality gate với reason codes deterministic"
    - path: "services/aureus-signal/engine/scoring/compute.py"
      provides: "Stage-2 weighted-sum 4 criteria theo snapshot"
    - path: "services/aureus-signal/tests/test_strategy_scoring_formula.py"
      provides: "Test contract SCOR-01"
  key_links:
    - from: "services/aureus-signal/engine/scoring/compute.py"
      to: "services/aureus-signal/engine/scoring/models.py"
      via: "ScoringResult contract"
      pattern: "score_version|weights_snapshot|score_total"
    - from: "services/aureus-signal/engine/scoring/gate.py"
      to: "services/aureus-signal/engine/scoring/compute.py"
      via: "gate_passed flag controls compute"
      pattern: "if not gate\.passed"
---

<objective>
Chốt scoring contract lõi cho Phase 54 theo kiến trúc two-stage và bảo toàn tính tái lập/audit ngay từ lớp tính toán.

Purpose: Khóa nền tảng đúng D-01→D-06, D-09→D-13 trước khi wiring vào executor.
Output: Module scoring độc lập (models/gate/normalize/compute) + test contracts cho formula/versioning/breakdown + precision nội bộ 6 decimals.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-strategy-scoring-framework/54-CONTEXT.md
@.planning/phases/54-strategy-scoring-framework/54-RESEARCH.md
@services/aureus-signal/engine/strategies/template.py

<interfaces>
From services/aureus-signal/engine/strategies/template.py:
```python
class TemplateStrategy(BaseStrategy):
    def __init__(self, config: Dict[str, Any]):
        self.min_score = config.get("min_score_threshold", 10.0)
        self.sequence = config.get("sequence", [])
        self.context_filters = config.get("context_filters", [])
```

From services/aureus-signal/engine/strategy_executor.py:
```python
def enrich_strategy_decisions_with_contract_metadata(strategy_results: list[dict], normalized_snapshot: dict) -> list[dict]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo test contracts RED cho scoring formula/versioning/breakdown</name>
  <files>services/aureus-signal/tests/test_strategy_scoring_formula.py, services/aureus-signal/tests/test_strategy_scoring_versioning.py, services/aureus-signal/tests/test_strategy_scoring_breakdown.py</files>
  <read_first>
    - services/aureus-signal/tests/test_template_strategy.py
    - .planning/phases/54-strategy-scoring-framework/54-CONTEXT.md
    - .planning/phases/54-strategy-scoring-framework/54-RESEARCH.md
  </read_first>
  <behavior>
    - Test gate fail: input thiếu dữ liệu quality bắt buộc trả gate.passed=False và score_total=None.
    - Test versioning: cùng input + cùng score_version + cùng weights_snapshot luôn ra score_total giống nhau (deterministic).
    - Test breakdown: output có đủ 4 criteria và `missing_data_policy` khác rỗng.
  </behavior>
  <action>Viết test theo D-01, D-02, D-03, D-04, D-05, D-09, D-10, D-11, D-12 với fixture cụ thể: score_version="scor-v1.0.0", weights_snapshot={"profit_outcome":0.35,"signal_quality":0.30,"timing_quality":0.20,"volatility_session":0.15}. Assert exact keys: gate, score_version, weights_snapshot, criteria, missing_data_policy, score_total; đồng thời assert `score_total` và `criteria[*].normalized` được round internal 6 decimals.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-signal/tests/test_strategy_scoring_formula.py services/aureus-signal/tests/test_strategy_scoring_versioning.py services/aureus-signal/tests/test_strategy_scoring_breakdown.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Cả 3 file test tồn tại và có assertion chứa chuỗi "score_version".
    - Có assertion explicit cho 4 key criteria: "profit_outcome", "signal_quality", "timing_quality", "volatility_session".
    - Có assertion precision nội bộ 6 decimals cho `score_total` và `criteria[*].normalized`.
    - RED phase xác nhận test fail trước khi implement.
  </acceptance_criteria>
  <done>Test contracts mô tả đầy đủ hành vi lõi SCOR-01/02/04 và fail đúng khi chưa có implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement two-stage scoring core với immutable snapshot</name>
  <files>services/aureus-signal/engine/scoring/models.py, services/aureus-signal/engine/scoring/gate.py, services/aureus-signal/engine/scoring/normalize.py, services/aureus-signal/engine/scoring/compute.py</files>
  <read_first>
    - services/aureus-signal/engine/strategies/template.py
    - .planning/phases/54-strategy-scoring-framework/54-CONTEXT.md
    - services/aureus-signal/tests/test_strategy_scoring_formula.py
  </read_first>
  <action>Tạo package `engine/scoring` và implement: (1) `models.py` định nghĩa contract `ScoringCriterion`, `QualityGateResult`, `ScoringResultV1`; (2) `gate.py` với `evaluate_quality_gate(input_payload)->QualityGateResult`; (3) `normalize.py` chuẩn hóa mỗi criterion về [0,1] với metadata `normalization_method`; (4) `compute.py` với `compute_trade_score(input_payload, score_version, weights_snapshot)` thực thi D-01/D-02: gate trước, pass mới weighted-sum tuyến tính 4 criteria; round internal 6 decimals; bắt buộc copy snapshot bất biến vào output; luôn set `missing_data_policy` explicit (ví dụ `impute_neutral_and_flag`) theo D-10.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-signal/tests/test_strategy_scoring_formula.py services/aureus-signal/tests/test_strategy_scoring_versioning.py services/aureus-signal/tests/test_strategy_scoring_breakdown.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `models.py` export class/type chứa literal `score_version` và `weights_snapshot`.
    - `compute.py` có công thức tuyến tính đúng: `profit_outcome*0.35 + signal_quality*0.30 + timing_quality*0.20 + volatility_session*0.15` (khi dùng snapshot ví dụ).
    - Khi gate fail, output chứa `gate.passed=False` và không tính weighted sum.
    - Breakdown JSON có metadata normalization cho từng criterion.
    - Kết quả compute không thực hiện DB schema migration/new table trong plan 01; chỉ tạo scoring contract output để executor consume (chuẩn bị cho D-13).
  </acceptance_criteria>
  <done>GREEN phase: test contracts của Task 1 pass hoàn toàn, scoring core deterministic và auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy regression nhanh để bảo đảm không vỡ strategy path hiện có</name>
  <files>services/aureus-signal/tests/test_strategy_scoring_formula.py</files>
  <read_first>
    - services/aureus-signal/tests/test_template_strategy.py
    - services/aureus-signal/tests/test_strategy_scoring_formula.py
  </read_first>
  <action>Chạy smoke test hiện hữu của strategy engine và scoring tests mới để đảm bảo thay đổi không phá path cũ. Không chỉnh logic TemplateStrategy ở plan này để giữ surgical scope theo ACC-01.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-signal/tests/test_template_strategy.py services/aureus-signal/tests/test_strategy_scoring_formula.py services/aureus-signal/tests/test_strategy_scoring_versioning.py services/aureus-signal/tests/test_strategy_scoring_breakdown.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Command verify trả exit code 0.
    - Không có thay đổi file ngoài danh sách `files_modified` của plan.
  </acceptance_criteria>
  <done>Scoring core sẵn sàng để plan 02 wiring vào executor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal payload → scoring core | Dữ liệu chưa tin cậy đi vào gate/normalize/compute |
| scoring core → persisted breakdown | JSON breakdown có thể bị thiếu key hoặc sai kiểu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | T | `engine/scoring/compute.py` | mitigate | Validate schema bằng Pydantic, reject payload thiếu trường bắt buộc trước khi tính điểm |
| T-54-02 | R | `score_version` snapshot logic | mitigate | Persist immutable `score_version` + `weights_snapshot` trong output, cấm mutate in-place |
| T-54-03 | I | breakdown JSON | mitigate | Bắt buộc ghi đủ 4 criteria + `missing_data_policy`; test assert exact key set |
| T-54-04 | D | normalize path | accept | Nếu dữ liệu criterion thiếu, dùng `impute_neutral_and_flag` thay vì crash toàn pipeline |
</threat_model>

<verification>
- Unit contracts cho SCOR-01/02/04 xanh.
- Smoke test strategy hiện hữu vẫn xanh.
</verification>

<success_criteria>
- Two-stage scoring core hoạt động deterministic theo score_version snapshot.
- Breakdown JSON có đủ trường audit theo D-09→D-11.
</success_criteria>

<output>
After completion, create `.planning/phases/54-strategy-scoring-framework/54-01-SUMMARY.md`
</output>
