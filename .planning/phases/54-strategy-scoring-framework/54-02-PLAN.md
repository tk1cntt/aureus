---
phase: 54-strategy-scoring-framework
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - services/aureus-signal/engine/scoring/aggregate.py
  - services/aureus-signal/engine/strategy_executor.py
  - services/aureus-signal/tests/test_strategy_scoring_aggregate.py
  - services/aureus-signal/tests/test_strategy_scoring_e2e.py
autonomous: true
requirements:
  - SCOR-03
  - ACC-01
must_haves:
  truths:
    - "Executor tạo được score per-trade và aggregate trong cùng flow đánh giá strategy."
    - "Aggregate bị khóa đúng chiều strategy/symbol/timeframe."
    - "End-to-end flow sinh artifact audit có score_version và breakdown truy vết được."
  artifacts:
    - path: "services/aureus-signal/engine/scoring/aggregate.py"
      provides: "Aggregate scorer theo key strategy/symbol/timeframe"
    - path: "services/aureus-signal/engine/strategy_executor.py"
      provides: "Wiring scoring core vào pipeline evaluator"
    - path: "services/aureus-signal/tests/test_strategy_scoring_e2e.py"
      provides: "ACC-01 e2e guard"
  key_links:
    - from: "services/aureus-signal/engine/strategy_executor.py"
      to: "services/aureus-signal/engine/scoring/compute.py"
      via: "per-trade compute call"
      pattern: "compute_trade_score"
    - from: "services/aureus-signal/engine/strategy_executor.py"
      to: "services/aureus-signal/engine/scoring/aggregate.py"
      via: "aggregate update by strategy/symbol/timeframe"
      pattern: "aggregate.*strategy.*symbol.*timeframe"
---

<objective>
Wiring scoring core vào strategy executor để hoàn tất luồng per-trade + aggregate end-to-end cho Phase 54.

Purpose: Đạt SCOR-03 và ACC-01 trước khi qua phase schema/reporting, đồng thời phản ánh D-12/D-13 trong wiring runtime.
Output: Aggregate module + executor integration + integration tests, với precision nội bộ 6 decimals và temporary persistence contract tại executor.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-strategy-scoring-framework/54-CONTEXT.md
@services/aureus-signal/engine/strategy_executor.py
@services/aureus-signal/engine/scoring/compute.py

<interfaces>
From services/aureus-signal/engine/strategy_executor.py:
```python
async def run_strategy_executor(db_pool=None, redis_client=None)
def enrich_strategy_decisions_with_contract_metadata(strategy_results: list[dict], normalized_snapshot: dict) -> list[dict]
```

From Phase 54 Plan 01 expected contract:
```python
def compute_trade_score(input_payload: dict, score_version: str, weights_snapshot: dict) -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo aggregate contract tests và implement aggregate scorer</name>
  <files>services/aureus-signal/tests/test_strategy_scoring_aggregate.py, services/aureus-signal/engine/scoring/aggregate.py</files>
  <read_first>
    - services/aureus-signal/engine/strategy_executor.py
    - services/aureus-signal/tests/test_template_strategy.py
    - services/aureus-signal/engine/scoring/compute.py
  </read_first>
  <behavior>
    - Aggregate key bắt buộc: strategy/symbol/timeframe (D-08).
    - Aggregate score phản ánh tập per-trade score cùng score_version.
  </behavior>
  <action>Viết test RED cho SCOR-03 rồi implement `aggregate.py` với API rõ ràng `update_aggregate_score(records, strategy_name, symbol, timeframe, score_version)` trả object gồm `aggregate_score`, `trade_count`, `score_version`, `group_key`. Group key serialize đúng format `"{strategy}|{symbol}|{timeframe}"` để chống lệch dimension.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-signal/tests/test_strategy_scoring_aggregate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_strategy_scoring_aggregate.py` có assertion literal cho group key 3 chiều.
    - `aggregate.py` export function `update_aggregate_score`.
    - Test fail trước implement và pass sau implement.
  </acceptance_criteria>
  <done>Aggregate scorer ổn định, deterministic, bám đúng D-07/D-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wiring scoring per-trade + aggregate vào strategy executor</name>
  <files>services/aureus-signal/engine/strategy_executor.py, services/aureus-signal/tests/test_strategy_scoring_e2e.py</files>
  <read_first>
    - services/aureus-signal/engine/strategy_executor.py
    - services/aureus-signal/engine/scoring/compute.py
    - services/aureus-signal/engine/scoring/aggregate.py
    - .planning/phases/54-strategy-scoring-framework/54-CONTEXT.md
  </read_first>
  <action>Sửa `strategy_executor.py` để tại điểm decision enrichment gọi `compute_trade_score(...)` cho từng trade candidate, gắn fields `score_total`, `score_breakdown`, `score_version`, `weights_snapshot`, `missing_data_policy` vào decision payload theo D-11; đảm bảo `score_total` và `criteria[*].normalized` giữ precision nội bộ 6 decimals theo D-12; sau đó cập nhật aggregate qua `update_aggregate_score(...)` với key strategy/symbol/timeframe (D-07/D-08). Viết `test_strategy_scoring_e2e.py` assert end-to-end: từ signal payload qua executor có đầy đủ field audit và aggregate artifact. Trong phase này chỉ dùng temporary persistence contract ở executor, không tạo schema/table migration mới (D-13).</action>
  <verify>
    <automated>python3 -m pytest services/aureus-signal/tests/test_strategy_scoring_e2e.py services/aureus-signal/tests/test_strategy_scoring_aggregate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Trong `strategy_executor.py` có call `compute_trade_score(` và `update_aggregate_score(`.
    - Payload decision output chứa exact keys: `score_total`, `score_breakdown`, `score_version`, `weights_snapshot`, `missing_data_policy`.
    - E2E test assert precision nội bộ 6 decimals cho `score_total` và `criteria[*].normalized`.
    - E2E test assert aggregate dimension đúng strategy/symbol/timeframe.
    - Không có thay đổi schema migration/new table trong plan 02; persistence vẫn ở temporary runtime contract tại executor (D-13).
  </acceptance_criteria>
  <done>Luồng executor đã chấm điểm 2 cấp và tạo output auditable, đạt ACC-01 phạm vi phase 54.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy full signal test gate cho phase completion</name>
  <files>services/aureus-signal/tests/test_strategy_scoring_e2e.py</files>
  <read_first>
    - .planning/phases/54-strategy-scoring-framework/54-VALIDATION.md
    - services/aureus-signal/tests/test_strategy_scoring_e2e.py
  </read_first>
  <action>Chạy fast scoring-focused subset (`test_strategy_scoring_e2e.py` + `test_strategy_scoring_aggregate.py`) cho loop phản hồi nhanh ở task-level; full suite `services/aureus-signal/tests -q` giữ ở wave/phase gate theo 54-VALIDATION.md trước `/gsd-verify-work`.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-signal/tests/test_strategy_scoring_e2e.py services/aureus-signal/tests/test_strategy_scoring_aggregate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Fast scoring-focused subset pass ở task loop.
    - Full suite signal service pass tại wave/phase gate theo 54-VALIDATION.md.
    - Không có test mới bị skip/xfail ngoài cấu hình có sẵn.
  </acceptance_criteria>
  <done>Phase 54 execution ready cho verify-work với evidence tự động đầy đủ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy evaluator → scoring integration | Quyết định trade runtime nhận thêm field score có thể làm sai pipeline nếu contract mismatch |
| scoring output → downstream persistence/reporting consumers | Dữ liệu aggregate/per-trade sai dimension sẽ phá analytics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05 | T | `strategy_executor.py` integration point | mitigate | Contract assertion trong test e2e cho exact key set và required fields |
| T-54-06 | I | aggregate key builder | mitigate | Khóa format key strategy/symbol/timeframe và test literal group key |
| T-54-07 | R | version traceability | mitigate | Propagate `score_version` từ compute result vào mọi decision/aggregate outputs |
| T-54-08 | D | executor runtime path | accept | Giữ compute/aggregate lightweight; nếu scoring fail thì mark failed gate thay vì crash worker |
</threat_model>

<verification>
- Aggregate tests pass.
- E2E scoring tests pass.
- Full signal test suite pass.
</verification>

<success_criteria>
- Per-trade + aggregate score hoạt động cùng pipeline evaluator.
- Output có đủ trường traceability để phase 55/56 consume trực tiếp.
</success_criteria>

<output>
After completion, create `.planning/phases/54-strategy-scoring-framework/54-02-SUMMARY.md`
</output>
