---
phase: 06
plan: 01
title: Signal sweep conservative optimization and verification gates
wave: 1
depends_on: []
autonomous: true
requirements: [SIG-06, TST-01, TST-02, TST-03, TST-04, VAL-01, VAL-02]
files_modified:
  - services/aureus-signal/engine/signals/sweep.py
  - services/aureus-signal/tests/test_sweep_o1.py
  - services/aureus-signal/tests/test_sweep_integration_execute_signals_for_candle.py
  - services/aureus-signal/tests/test_sweep_integration_live_engine.py
  - .planning/phases/06-signal-sweep-optimization/06-VALIDATION.md
---

# Phase 06 Plan 01 — Signal Sweep Conservative Optimization and Verification Gates

## Objective
Harden sweep runtime safety and determinism while preserving signal semantics and downstream contracts, then close all phase quality gates (unit + integration + coverage >= 80%).

<tasks>
  <task id="06-01-01" type="auto">
    <name>Harden sweep signal internals without changing trading semantics</name>
    <read_first>
      - services/aureus-signal/engine/signals/sweep.py
      - services/aureus-signal/engine/signals/structure.py
      - .planning/phases/06-signal-sweep-optimization/06-CONTEXT.md
    </read_first>
    <action>
      Add defensive guards for malformed candle/target/state inputs and tighten deterministic target-iteration flow in `sweep.py`. Preserve contract tags (`sweep_bull`, `sweep_bear`), payload keys, single-trigger-per-candle behavior, and dedup semantics (`tag+price_swept+t`).
    </action>
    <acceptance_criteria>
      - Sweep output tags remain exactly `sweep_bull` and `sweep_bear`.
      - Payload keys remain backward-compatible for downstream consumers.
      - Single-trigger-per-candle + dedup behavior is preserved.
    </acceptance_criteria>
  </task>

  <task id="06-01-02" type="auto">
    <name>Add sweep unit and execute-signals integration coverage</name>
    <read_first>
      - services/aureus-signal/tests/test_signal_contract_normalization.py
      - services/aureus-signal/tests/test_fvg_integration_execute_signals_for_candle.py
      - services/aureus-signal/engine/signals/sweep.py
    </read_first>
    <action>
      Create `test_sweep_o1.py` and `test_sweep_integration_execute_signals_for_candle.py` to validate malformed-target guards, regime-gated trigger behavior, dedup at same candle/target, and signal-history contract stability through `execute_signals_for_candle`.
    </action>
    <acceptance_criteria>
      - `test_sweep_o1.py` exists and includes malformed-target + dedup assertions.
      - `test_sweep_integration_execute_signals_for_candle.py` exists and validates tag/timestamp history behavior.
      - At least one assertion verifies no duplicate emission for same candle-target pair.
    </acceptance_criteria>
  </task>

  <task id="06-01-03" type="auto">
    <name>Add runtime integration coverage for sweep in live-engine path</name>
    <read_first>
      - services/aureus-signal/tests/test_fvg_integration_live_engine.py
      - services/aureus-signal/engine/signal_factory.py
      - services/aureus-signal/engine/event_filter.py
    </read_first>
    <action>
      Add `test_sweep_integration_live_engine.py` to validate runtime loop behavior, structural-tag persistence path, and sweep producer/outlet wiring through normal signal factory registration.
    </action>
    <acceptance_criteria>
      - `test_sweep_integration_live_engine.py` exists and executes runtime path.
      - Runtime assertions confirm stable sweep contract in transient/history flow.
      - Test avoids monkeypatching that bypasses real factory integration behavior.
    </acceptance_criteria>
  </task>

  <task id="06-01-04" type="auto">
    <name>Run validation commands and update phase validation evidence</name>
    <read_first>
      - .planning/phases/06-signal-sweep-optimization/06-VALIDATION.md
      - .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Execute planned verification commands and update `06-VALIDATION.md` with real outcomes for unit, integration, runtime, and coverage gates.
    </action>
    <acceptance_criteria>
      - `06-VALIDATION.md` status table reflects executed command outcomes.
      - Coverage output for sweep phase scope is recorded.
      - Phase closure gate remains blocked unless all checks are green.
    </acceptance_criteria>
  </task>
</tasks>

## Verification
- `python -m pytest services/aureus-signal/tests/test_sweep_o1.py services/aureus-signal/tests/test_sweep_integration_execute_signals_for_candle.py -q`
- `python -m pytest services/aureus-signal/tests/test_sweep_integration_live_engine.py -q`
- `python -m pytest services/aureus-signal/tests/test_sweep_o1.py services/aureus-signal/tests/test_sweep_integration_execute_signals_for_candle.py services/aureus-signal/tests/test_sweep_integration_live_engine.py --cov=services/aureus-signal/engine/signals/sweep.py --cov-report=term-missing -q`

## Success Criteria
- Sweep signal semantics remain conservative and backward-compatible.
- Dedicated sweep unit, integration, and runtime tests are present and passing.
- Coverage for sweep phase scope meets gate requirements.
- Requirements mapped in this plan are demonstrably addressed with validation evidence.
