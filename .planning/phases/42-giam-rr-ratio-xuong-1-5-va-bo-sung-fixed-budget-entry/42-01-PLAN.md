---
phase: 42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/simulated_orders.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Backtest simulated_orders.py default RR fallback is 1.5 (not 2.0)"
    - "All 16 seed strategies have tp.value = 1.5 (no exceptions)"
    - "SESSION_SWEEP_BEAR also uses RR 1.5 (D-02, no special treatment)"
  artifacts:
    - path: "services/aureus-signal/engine/simulated_orders.py"
      provides: "Backtest TP calculation with RR 1.5 default fallback"
      min_lines: 2
    - path: "services/aureus-signal/engine/strategies/seed_strategies.py"
      provides: "16 seed strategies all with tp.value = 1.5"
      min_lines: 400
  key_links:
    - from: "services/aureus-signal/engine/strategies/seed_strategies.py"
      to: "services/aureus-signal/engine/simulated_orders.py"
      via: "tp_cfg.get('value', default) — seed provides value=1.5, fallback used if missing"
      pattern: "tp_cfg\\.get\\('value'"
---

<objective>
Giảm RR ratio mặc định xuống 1.5 cho tất cả strategy và đảm bảo consistency giữa live và backtest.

Purpose: RR 1.5 cho phép take profit sớm hơn, phù hợp scalping/intraday. Break-even ở ~40% win rate. Tất cả strategy phải đồng nhất — không có ngoại lệ (D-20).
Output: simulated_orders.py default 1.5, seed_strategies.py tất cả 16 strategy có value=1.5
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Giảm default RR ratio trong simulated_orders.py từ 2.0 xuống 1.5</name>
  <files>services/aureus-signal/engine/simulated_orders.py</files>
  <read_first>
    - services/aureus-signal/engine/simulated_orders.py
  </read_first>
  <action>
    Trong file services/aureus-signal/engine/simulated_orders.py, tìm dòng có `tp_cfg.get('value', 2.0)` trong method `_calculate_sl_tp` và thay `2.0` thành `1.5`.

    Dòng cần sửa:
    ```python
    ratio = tp_cfg.get('value', 2.0)  # ← thay 2.0 thành 1.5
    ```
    Thành:
    ```python
    ratio = tp_cfg.get('value', 1.5)  # ← default RR 1.5 per D-01, D-03
    ```

    Đây là default fallback — seed_strategies.py đã cung cấp value=1.5 cho tất cả 16 strategy, nhưng default trong simulated_orders.py cần nhất quán.
  </action>
  <acceptance_criteria>
    - grep "get('value', 1.5)" services/aureus-signal/engine/simulated_orders.py → có ít nhất 1 match
    - grep "get('value', 2.0)" services/aureus-signal/engine/simulated_orders.py → KHÔNG còn match nào
    - Kiểm tra seed_strategies.py: `grep '"value": 1.5' services/aureus-signal/engine/strategies/seed_strategies.py` → đếm được 16 occurrences (cho tất cả 16 strategy)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '"value": 1.5' services/aureus-signal/engine/strategies/seed_strategies.py && grep "get('value', 1.5)" services/aureus-signal/engine/simulated_orders.py && echo "RR 1.5 verified" || echo "FAIL"</automated>
  </verify>
  <done>
    Tất cả seed strategy có tp.value = 1.5 (16 occurrences), simulated_orders.py default cũng là 1.5
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed_strategies → DB | Strategy config được upsert — config cũ trong DB không bị ảnh hưởng (D-04) |
| config → TP calculation | tp_cfg.get('value') đọc từ config, không có injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Integrity | seed_strategies.py | mitigate | RR 1.5 hardcoded trong 16 strategy config — không có per-strategy override (D-20) |
| T-42-02 | Integrity | simulated_orders.py:_calculate_sl_tp | mitigate | Default 1.5 fallback — nếu strategy config có value thì dùng value đó, không override |
| T-42-03 | Tampering | strategy config in DB | accept | DB config cũ không bị ảnh hưởng bởi seed mới (D-04) — chỉ strategy mới seeded có RR 1.5 |
</threat_model>

<verification>
- simulated_orders.py default RR = 1.5 (không còn 2.0)
- seed_strategies.py: đếm 16 strategy, tất cả có "value": 1.5 trong tp config
- Không có strategy nào có RR khác 1.5 (D-02, D-20)
</verification>

<success_criteria>
- Default RR fallback trong simulated_orders.py = 1.5
- Tất cả 16 seed strategy có tp.value = 1.5
- SESSION_SWEEP_BEAR cũng có RR 1.5 (không ngoại lệ)
</success_criteria>

<output>
After completion, create `.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-01-SUMMARY.md`
</output>
