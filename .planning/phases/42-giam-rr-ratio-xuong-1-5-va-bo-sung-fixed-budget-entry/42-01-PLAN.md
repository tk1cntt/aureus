---
phase: 42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/simulated_orders.py
autonomous: true
requirements: [RR-01]

must_haves:
  truths:
    - "Backtest SL/TP calculation uses RR 1.5 as default ratio (not 2.0)"
    - "TP is correctly calculated as 1.5x risk distance for all strategies"
  artifacts:
    - path: "services/aureus-signal/engine/simulated_orders.py"
      provides: "Backtest SL/TP calculation with RR 1.5 default"
      contains: "ratio = tp_cfg.get('value', 1.5)"
  key_links:
    - from: "services/aureus-signal/engine/simulated_orders.py"
      to: "tp_cfg.get('value')"
      via: "_calculate_sl_tp reads RR value from strategy config"
      pattern: "tp_cfg\\.get\\('value'"
---

<objective>
Fix backtest SimulatedTradeManager default RR ratio from 2.0 to 1.5.

Purpose: The live seed_strategies.py already has RR 1.5 in all strategy configs, but simulated_orders.py (backtest mirror) still defaults to 2.0. This inconsistency means backtest results are misleading.
Output: Updated default in _calculate_sl_tp fallback
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix simulated_orders.py default RR ratio from 2.0 to 1.5</name>
  <files>services/aureus-signal/engine/simulated_orders.py</files>
  <read_first>
    - services/aureus-signal/engine/simulated_orders.py
  </read_first>
  <action>
    In services/aureus-signal/engine/simulated_orders.py, find the _calculate_sl_tp method (around line 206-208).
    Change the default TP RR ratio from 2.0 to 1.5:

    Current:
        ratio = tp_cfg.get('value', 2.0)

    Change to:
        ratio = tp_cfg.get('value', 1.5)

    This ensures the backtest SimulatedTradeManager uses the same RR 1.5 default as live seed strategies (D-01, D-03).
    The seed_strategies.py already has all 16 strategies configured with "value": 1.5 — this change makes the backtest fallback consistent.
  </action>
  <acceptance_criteria>
    - grep "value', 1.5" services/aureus-signal/engine/simulated_orders.py → matches at least 1 occurrence
    - grep "value', 2.0" services/aureus-signal/engine/simulated_orders.py → NO matches in TP calculation context
  </acceptance_criteria>
  <verify>
    <automated>grep -n "value', 1.5" services/aureus-signal/engine/simulated_orders.py && echo "RR 1.5 default confirmed" || echo "FAIL: RR 1.5 not found"</automated>
  </verify>
  <done>
    simulated_orders.py _calculate_sl_tp uses 1.5 as default RR ratio, matching live seed_strategies.py configuration
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config→calculation | Strategy TP config value flows to SL/TP price calculation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Integrity | simulated_orders.py:_calculate_sl_tp | mitigate | Default RR value hardcoded (1.5) — cannot be injected by external config; validate ratio > 0 before multiplication |
</threat_model>

<verification>
- Verify no remaining "2.0" default in simulated_orders.py TP logic
- Verify all seed strategies in seed_strategies.py already have "value": 1.5 (pre-existing, confirmed)
</verification>

<success_criteria>
- simulated_orders.py default RR ratio = 1.5
- Both live and backtest use same RR default
</success_criteria>

<output>
After completion, create `.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-01-SUMMARY.md`
</output>
