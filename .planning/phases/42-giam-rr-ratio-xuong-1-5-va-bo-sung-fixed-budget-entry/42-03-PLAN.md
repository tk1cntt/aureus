---
phase: 42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/orders.py
  - services/aureus-trader/order_builder.py
autonomous: true
requirements: [ORDER-02, ORDER-03]

must_haves:
  truths:
    - "Python orders.py detects RISK_FIXED_AMOUNT and sets volume=0, risk_amount in order snapshot"
    - "aureus-trader order_builder.py detects RISK_FIXED_AMOUNT and forwards size_mode + risk_amount to MT5 command"
    - "MT5 CalculateLotFromBudget uses real Ask/Bid for SL distance calculation"
  artifacts:
    - path: "services/aureus-signal/engine/orders.py"
      provides: "RISK_FIXED_AMOUNT detection with volume=0 placeholder and risk_amount"
      exports: ["RISK_FIXED_AMOUNT handling in process_triggers"]
    - path: "services/aureus-trader/order_builder.py"
      provides: "RISK_FIXED_AMOUNT command builder with risk_amount forwarding"
      exports: ["build_order_command"]
  key_links:
    - from: "services/aureus-signal/engine/orders.py"
      to: "services/aureus-trader/order_builder.py"
      via: "order_plan_snapshot with size_mode + risk_amount → Redis event → order_builder parses"
      pattern: "RISK_FIXED_AMOUNT"
    - from: "services/aureus-trader/order_builder.py"
      to: "mql5/AureusProvider.mq5"
      via: "TCP OPEN_ORDER command with size_mode + risk_amount"
      pattern: "size_mode.*risk_amount"
---

<objective>
Verify RISK_FIXED_AMOUNT handling in orders.py (signal engine) and order_builder.py (aureus-trader).

Purpose: Ensure Python sets volume=0 as placeholder and forwards risk_amount to MT5 for actual lot calculation. Per decisions D-07, D-16, D-17.
Output: Verification that orders.py and order_builder.py correctly handle RISK_FIXED_AMOUNT
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify RISK_FIXED_AMOUNT handling in orders.py</name>
  <files>services/aureus-signal/engine/orders.py</files>
  <read_first>
    - services/aureus-signal/engine/orders.py
  </read_first>
  <action>
    In services/aureus-signal/engine/orders.py, verify the RISK_FIXED_AMOUNT handling block exists (around lines 276-283):

    The code must:
    1. Detect size_mode == "RISK_FIXED_AMOUNT"
    2. Set risk_amount = size_value if valid, else fallback to 50.0 (D-12)
    3. Set order_plan_snapshot["risk_amount"] = risk_amount
    4. Set order_plan_snapshot["size_value"] = 0 (placeholder — MT5 calculates real lot, D-07)
    5. Log the budget mode for observability

    Expected code pattern:
        if size_mode == "RISK_FIXED_AMOUNT":
            risk_amount = size_value if isinstance(size_value, (int, float)) and size_value > 0 else 50.0
            order_plan_snapshot["risk_amount"] = risk_amount
            order_plan_snapshot["size_value"] = 0
            logger.info(...)

    If this block is missing or incorrect, add/fix it.
  </action>
  <acceptance_criteria>
    - grep 'RISK_FIXED_AMOUNT' services/aureus-signal/engine/orders.py → matches at least 1 occurrence
    - grep 'risk_amount' services/aureus-signal/engine/orders.py → matches the assignment line
    - grep 'size_value.*= 0' services/aureus-signal/engine/orders.py → confirms volume=0 placeholder
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'RISK_FIXED_AMOUNT' services/aureus-signal/engine/orders.py && grep -n '"risk_amount"' services/aureus-signal/engine/orders.py && echo "orders.py RISK_FIXED_AMOUNT confirmed" || echo "FAIL: RISK_FIXED_AMOUNT missing from orders.py"</automated>
  </verify>
  <done>
    orders.py correctly detects RISK_FIXED_AMOUNT, sets risk_amount, and zeroes size_value for MT5 lot calculation
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify RISK_FIXED_AMOUNT forwarding in order_builder.py</name>
  <files>services/aureus-trader/order_builder.py</files>
  <read_first>
    - services/aureus-trader/order_builder.py
  </read_first>
  <action>
    In services/aureus-trader/order_builder.py, verify build_order_command() handles RISK_FIXED_AMOUNT (around lines 58-64):

    The code must:
    1. Extract size_mode and risk_amount from match event data
    2. Detect size_mode == "RISK_FIXED_AMOUNT"
    3. Add "size_mode": size_mode to command
    4. Add "risk_amount": risk_amount if risk_amount else 50.0 (D-12 fallback)
    5. Set command["volume"] = 0 (placeholder for MT5)

    Expected code pattern:
        if size_mode == "RISK_FIXED_AMOUNT":
            command["size_mode"] = size_mode
            command["risk_amount"] = risk_amount if risk_amount else 50.0
            command["volume"] = 0

    If this block is missing or incorrect, add/fix it.
  </action>
  <acceptance_criteria>
    - grep 'RISK_FIXED_AMOUNT' services/aureus-trader/order_builder.py → matches at least 1 occurrence
    - grep '"risk_amount"' services/aureus-trader/order_builder.py → matches in RISK_FIXED_AMOUNT block
    - grep '"volume".*0' services/aureus-trader/order_builder.py → confirms volume=0 placeholder
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'RISK_FIXED_AMOUNT' services/aureus-trader/order_builder.py && grep -n '"risk_amount"' services/aureus-trader/order_builder.py && echo "order_builder.py RISK_FIXED_AMOUNT confirmed" || echo "FAIL: RISK_FIXED_AMOUNT missing from order_builder.py"</automated>
  </verify>
  <done>
    order_builder.py correctly forwards size_mode and risk_amount to MT5 OPEN_ORDER command
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orders.py→Redis stream | order_plan_snapshot with risk_amount serialized to JSON |
| order_builder.py→MT5 TCP | OPEN_ORDER command with risk_amount forwarded over TCP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Integrity | orders.py:process_triggers | mitigate | risk_amount validated as int/float > 0 with $50 fallback (D-12) — prevents negative or zero budget from causing division by zero on MT5 |
| T-42-06 | Integrity | order_builder.py:build_order_command | mitigate | risk_amount null-safe fallback (risk_amount if risk_amount else 50.0) — ensures MT5 always receives valid dollar amount |
| T-42-07 | Tampering | Redis stream → aureus-trader | accept | Internal Redis, no external access — risk_amount cannot be tampered in transit |
</threat_model>

<verification>
- orders.py: RISK_FIXED_AMOUNT detected, risk_amount set, size_value=0
- order_builder.py: size_mode + risk_amount forwarded in OPEN_ORDER command
- Both layers use $50 fallback when risk_amount is null (D-12)
</verification>

<success_criteria>
- orders.py handles RISK_FIXED_AMOUNT with volume=0 placeholder
- order_builder.py forwards risk_amount in MT5 command
- $50 fallback applied when risk_amount missing in both layers
</success_criteria>

<output>
After completion, create `.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-03-SUMMARY.md`
</output>
