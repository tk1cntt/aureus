---
phase: 42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/signal_event_publisher.py
  - services/aureus-signal/engine/strategies/registry.py
  - services/aureus-signal/engine/snapshot_utils.py
autonomous: true
requirements: [ORDER-02, STRAT-03]

must_haves:
  truths:
    - "RISK_FIXED_AMOUNT strategies propagate risk_amount through every layer"
    - "Strategy match events include risk_amount field when size_mode is RISK_FIXED_AMOUNT"
    - "Registry evaluate_all output includes risk_amount in the trigger payload"
  artifacts:
    - path: "services/aureus-signal/engine/strategies/registry.py"
      provides: "risk_amount in evaluate_all accepted output (line ~489)"
      exports: ["risk_amount in order_plan output"]
    - path: "services/aureus-signal/engine/signal_event_publisher.py"
      provides: "risk_amount in publish_strategy_match payload"
      exports: ["publish_strategy_match"]
    - path: "services/aureus-signal/engine/snapshot_utils.py"
      provides: "RISK_FIXED_AMOUNT in VALID_SIZE_MODES"
      contains: "RISK_FIXED_AMOUNT"
  key_links:
    - from: "services/aureus-signal/engine/strategies/registry.py"
      to: "services/aureus-signal/engine/signal_event_publisher.py"
      via: "registry returns risk_amount → publisher includes it in STRATEGY_MATCH event"
      pattern: "risk_amount"
    - from: "services/aureus-signal/engine/signal_event_publisher.py"
      to: "services/aureus-trader/order_builder.py"
      via: "Redis STRATEGY_MATCH event contains risk_amount → order_builder parses it"
      pattern: "risk_amount"
---

<objective>
Verify RISK_FIXED_AMOUNT propagation through all signal pipeline layers: registry → publisher → snapshot_utils.

Purpose: Ensure risk_amount ($50 budget) flows from strategy config through to the Redis event so downstream (aureus-trader → MT5) can calculate lot size. Per decisions D-05, D-06, D-15, D-16, D-17.
Output: Verification that all layers correctly forward risk_amount and VALID_SIZE_MODES includes RISK_FIXED_AMOUNT
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify RISK_FIXED_AMOUNT in VALID_SIZE_MODES (snapshot_utils.py)</name>
  <files>services/aureus-signal/engine/snapshot_utils.py</files>
  <read_first>
    - services/aureus-signal/engine/snapshot_utils.py
  </read_first>
  <action>
    In services/aureus-signal/engine/snapshot_utils.py, verify line 72 contains "RISK_FIXED_AMOUNT" in VALID_SIZE_MODES:

    VALID_SIZE_MODES = ("FIXED_UNITS", "FIXED_LOT", "RISK_PERCENT", "RISK_FIXED_AMOUNT")

    This is the validation gate that prevents RISK_FIXED_AMOUNT from being rejected as an invalid size_mode (D-05).
    If the tuple is missing "RISK_FIXED_AMOUNT", add it.
  </action>
  <acceptance_criteria>
    - grep "RISK_FIXED_AMOUNT" services/aureus-signal/engine/snapshot_utils.py → matches at least 1 occurrence
    - grep 'VALID_SIZE_MODES.*RISK_FIXED_AMOUNT' services/aureus-signal/engine/snapshot_utils.py → confirms it is in the tuple
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'RISK_FIXED_AMOUNT' services/aureus-signal/engine/snapshot_utils.py && echo "RISK_FIXED_AMOUNT in VALID_SIZE_MODES confirmed" || echo "FAIL: RISK_FIXED_AMOUNT missing"</automated>
  </verify>
  <done>
    VALID_SIZE_MODES tuple includes "RISK_FIXED_AMOUNT" — no validation rejection
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify risk_amount in registry.py evaluate_all output</name>
  <files>services/aureus-signal/engine/strategies/registry.py</files>
  <read_first>
    - services/aureus-signal/engine/strategies/registry.py
  </read_first>
  <action>
    In services/aureus-signal/engine/strategies/registry.py, verify that evaluate_all() includes risk_amount in the accepted output dict (around line 489):

    The accepted.append() block must contain:
        "risk_amount": order_plan.get("risk_amount"),

    This ensures risk_amount is available in the trigger payload for downstream layers (D-17).
    If missing, add it to the accepted dict.
  </action>
  <acceptance_criteria>
    - grep '"risk_amount"' services/aureus-signal/engine/strategies/registry.py → matches at least 1 occurrence in evaluate_all
    - The accepted dict includes risk_amount key
  </acceptance_criteria>
  <verify>
    <automated>grep -n '"risk_amount"' services/aureus-signal/engine/strategies/registry.py && echo "risk_amount in registry confirmed" || echo "FAIL: risk_amount missing from registry"</automated>
  </verify>
  <done>
    registry.py evaluate_all output includes risk_amount from order_plan
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify risk_amount in signal_event_publisher.py STRATEGY_MATCH event</name>
  <files>services/aureus-signal/engine/signal_event_publisher.py</files>
  <read_first>
    - services/aureus-signal/engine/signal_event_publisher.py
  </read_first>
  <action>
    In services/aureus-signal/engine/signal_event_publisher.py, verify publish_strategy_match() includes risk_amount in the data dict (around line 72):

    The data dict must contain:
        "risk_amount": strategy_result.get("risk_amount"),

    This ensures risk_amount is forwarded to the Redis STRATEGY_MATCH event for aureus-trader to consume (D-15).
    If missing, add it to the data dict.
  </action>
  <acceptance_criteria>
    - grep '"risk_amount"' services/aureus-signal/engine/signal_event_publisher.py → matches at least 1 occurrence in publish_strategy_match
    - The event data dict includes risk_amount key
  </acceptance_criteria>
  <verify>
    <automated>grep -n '"risk_amount"' services/aureus-signal/engine/signal_event_publisher.py && echo "risk_amount in publisher confirmed" || echo "FAIL: risk_amount missing from publisher"</automated>
  </verify>
  <done>
    signal_event_publisher.py publish_strategy_match includes risk_amount in the Redis event payload
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry→publisher | risk_amount value flows between Python services via function return |
| publisher→Redis | risk_amount serialized to JSON in Redis pub/sub event |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | Integrity | registry.py:evaluate_all | mitigate | risk_amount read from order_plan.get() — None-safe; downstream handles null with $50 fallback (D-12) |
| T-42-03 | Tampering | signal_event_publisher.py:publish_strategy_match | mitigate | risk_amount forwarded as-is from strategy_result; no mutation — tampering detected by downstream volume guard |
| T-42-04 | Information Disclosure | Redis pub/sub channel | accept | risk_amount is dollar amount ($50), not PII or credential — low-value target, internal network only |
</threat_model>

<verification>
- All 3 files verified: snapshot_utils (VALID_SIZE_MODES), registry (risk_amount in output), publisher (risk_amount in event)
- Full propagation chain confirmed: strategy config → registry → publisher → Redis event
</verification>

<success_criteria>
- VALID_SIZE_MODES includes RISK_FIXED_AMOUNT (no validation rejection)
- risk_amount present in registry evaluate_all accepted output
- risk_amount present in publish_strategy_match event data
- End-to-end propagation verified via grep checks
</success_criteria>

<output>
After completion, create `.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-02-SUMMARY.md`
</output>
