---
phase: 42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - mql5/AureusProvider.mq5
autonomous: true
requirements: [ORDER-04]

must_haves:
  truths:
    - "MT5 CalculateLotFromBudget uses real Ask/Bid for SL distance (not requested price)"
    - "Lot = riskAmount / (slDistance * contractSize) with correct contract sizes"
    - "Budget auto-adjust increases when lot < min volume, decreases when lot > max volume"
    - "Lot is normalized to SYMBOL_VOLUME_STEP and clamped between MIN/MAX"
    - "$50 fallback applied when riskAmount null or <= 0"
  artifacts:
    - path: "mql5/AureusProvider.mq5"
      provides: "CalculateLotFromBudget with auto-adjust and MT5-side lot calc"
      exports: ["CalculateLotFromBudget()", "ExecuteOpenOrder()"]
      min_lines: 90
    - path: "mql5/AureusProvider.mq5"
      provides: "Contract sizes hardcoded matching Python defaults"
      contains: "contractSize = 100.0"  // XAUUSD
  key_links:
    - from: "mql5/AureusProvider.mq5:ExecuteOpenOrder"
      to: "mql5/AureusProvider.mq5:CalculateLotFromBudget"
      via: "RISK_FIXED_AMOUNT detected → CalculateLotFromBudget called with symbol, direction, price, sl, riskAmount"
      pattern: "CalculateLotFromBudget\\(symbol, direction, price, sl, riskAmount\\)"
    - from: "mql5/AureusProvider.mq5:CalculateLotFromBudget"
      to: "SymbolInfoDouble(symbol, SYMBOL_ASK/BID)"
      via: "Uses actual market price, not requested price"
      pattern: "SymbolInfoDouble.*SYMBOL_ASK"
---

<objective>
Verify MT5-side lot calculation in AureusProvider.mq5: CalculateLotFromBudget and ExecuteOpenOrder.

Purpose: MT5 is responsible for calculating actual lot size from budget ($50), SL distance, and real-time Ask/Bid price. This is the core of the FIXED_BUDGET mechanism (D-08, D-09, D-10, D-11, D-12, D-18, D-19).
Output: Verification that CalculateLotFromBudget correctly implements the formula with auto-adjust and guards
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CalculateLotFromBudget formula and contract sizes</name>
  <files>mql5/AureusProvider.mq5</files>
  <read_first>
    - mql5/AureusProvider.mq5
  </read_first>
  <action>
    In mql5/AureusProvider.mq5, verify CalculateLotFromBudget() function (lines 869-960):

    1. **Real price usage (D-08):** Uses SYMBOL_ASK for BUY, SYMBOL_BID for SELL — not entryPriceRequested parameter
    2. **SL distance:** MathAbs(actualEntry - slRequested)
    3. **Contract sizes (D-10):** Verify these defaults match Python:
       - XAUUSD/GOLD: 100.0
       - XAGUSD/SILVER: 5000.0
       - USTEC/NAS100/US30/DJ30: 1.0
       - US500/SPX: 1.0
       - BTC: 1.0
       - ETH: 1.0
       - Default (forex): 100000.0
    4. **Formula (D-09):** lot = riskAmount / (slDistance * contractSize)
    5. **Fallback (D-12):** If slDistance <= 0, return SYMBOL_VOLUME_MIN
  </action>
  <acceptance_criteria>
    - grep "SymbolInfoDouble.*SYMBOL_ASK" mql5/AureusProvider.mq5 → in CalculateLotFromBudget
    - grep "SymbolInfoDouble.*SYMBOL_BID" mql5/AureusProvider.mq5 → in CalculateLotFromBudget
    - grep "contractSize = 100.0" mql5/AureusProvider.mq5 → XAUUSD contract size
    - grep "contractSize = 5000.0" mql5/AureusProvider.mq5 → XAGUSD contract size
    - grep "lot = riskAmount / riskPerLot" mql5/AureusProvider.mq5 → formula present
    - grep "SYMBOL_VOLUME_MIN" mql5/AureusProvider.mq5 → fallback guard present
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'SymbolInfoDouble.*SYMBOL_ASK' mql5/AureusProvider.mq5 | head -5 && grep -n 'contractSize = 100.0' mql5/AureusProvider.mq5 && grep -n 'lot = riskAmount / riskPerLot' mql5/AureusProvider.mq5 && echo "MT5 lot calc formula confirmed" || echo "FAIL: MT5 lot calc incomplete"</automated>
  </verify>
  <done>
    CalculateLotFromBudget uses real Ask/Bid, correct contract sizes, and proper formula
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify budget auto-adjust (D-18, D-19) and volume normalization</name>
  <files>mql5/AureusProvider.mq5</files>
  <read_first>
    - mql5/AureusProvider.mq5
  </read_first>
  <action>
    In mql5/AureusProvider.mq5, verify the budget auto-adjust logic in CalculateLotFromBudget:

    **D-18 (lot < min volume):**
    - Loop up to 20 iterations
    - Doubles adjustedBudget each iteration (×2)
    - Recalculates lot until >= minVol
    - Logs budget adjustment for observability

    **D-19 (lot > max volume):**
    - Loop up to 20 iterations
    - Halves adjustedBudget each iteration (/2)
    - Recalculates lot until <= maxVol
    - Logs budget adjustment for observability

    **D-11 (normalization):**
    - Normalizes lot to SYMBOL_VOLUME_STEP using MathFloor
    - Final clamp between SYMBOL_VOLUME_MIN and SYMBOL_VOLUME_MAX

    Verify all three guards are present and correct.
  </action>
  <acceptance_criteria>
    - grep "adjustedBudget \*= 2" mql5/AureusProvider.mq5 → auto-increase for min volume
    - grep "adjustedBudget /= 2" mql5/AureusProvider.mq5 → auto-decrease for max volume
    - grep "SYMBOL_VOLUME_STEP" mql5/AureusProvider.mq5 → step normalization
    - grep "MathFloor.*stepVol" mql5/AureusProvider.mq5 → floor to step
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'adjustedBudget' mql5/AureusProvider.mq5 | head -10 && grep -n 'SYMBOL_VOLUME_STEP' mql5/AureusProvider.mq5 && echo "Auto-adjust + normalization confirmed" || echo "FAIL: Auto-adjust or normalization missing"</automated>
  </verify>
  <done>
    Budget auto-adjust works for both min and max volume violations, lot normalized to step
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify ExecuteOpenOrder RISK_FIXED_AMOUNT detection and integration</name>
  <files>mql5/AureusProvider.mq5</files>
  <read_first>
    - mql5/AureusProvider.mq5
  </read_first>
  <action>
    In mql5/AureusProvider.mq5, verify ExecuteOpenOrder() function (lines 965-1196):

    1. **Parse fields:** size_mode and risk_amount from JSON command (lines 978-979)
    2. **Detection:** calcVolumeOnMT5 = (StringFind(StringToUpper(sizeMode), "RISK_FIXED_AMOUNT") >= 0 && riskAmount > 0)
    3. **Validation:** volume check skipped when calcVolumeOnMT5 is true (line 985)
    4. **Lot calculation:** volume = CalculateLotFromBudget(symbol, direction, price, sl, riskAmount) when calcVolumeOnMT5 && sl > 0
    5. **Logging:** PrintFormat logs the budget → lot conversion

    Verify the integration point correctly connects RISK_FIXED_AMOUNT detection to lot calculation.
  </action>
  <acceptance_criteria>
    - grep 'ParseJSONString.*"size_mode"' mql5/AureusProvider.mq5 → size_mode parsed
    - grep 'ParseJSONDouble.*"risk_amount"' mql5/AureusProvider.mq5 → risk_amount parsed
    - grep 'RISK_FIXED_AMOUNT' mql5/AureusProvider.mq5 → detection in ExecuteOpenOrder
    - grep 'CalculateLotFromBudget' mql5/AureusProvider.mq5 → lot calc called in ExecuteOpenOrder
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'RISK_FIXED_AMOUNT' mql5/AureusProvider.mq5 && grep -n 'CalculateLotFromBudget' mql5/AureusProvider.mq5 && echo "ExecuteOpenOrder integration confirmed" || echo "FAIL: ExecuteOpenOrder integration missing"</automated>
  </verify>
  <done>
    ExecuteOpenOrder detects RISK_FIXED_AMOUNT, calls CalculateLotFromBudget, and uses calculated volume
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP command → MT5 EA | OPEN_ORDER command with risk_amount from external service |
| MT5 lot calculation → OrderSend | Calculated volume must be within broker limits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-08 | Integrity | AureusProvider.mq5:CalculateLotFromBudget | mitigate | slDistance > 0 guard prevents division by zero; contractSize fallback chain prevents zero division; lot clamped to SYMBOL_VOLUME_MIN/MAX (D-11) |
| T-42-09 | Integrity | AureusProvider.mq5:ExecuteOpenOrder | mitigate | calcVolumeOnMT5 requires both valid size_mode AND riskAmount > 0 — dual guard prevents accidental lot calculation with zero budget |
| T-42-10 | Repudiation | AureusProvider.mq5:CalculateLotFromBudget | mitigate | PrintFormat logs full calculation details (budget, entry, SL, distance, contract, lot) — full audit trail for lot decisions |
| T-42-11 | Denial of Service | Budget auto-adjust loop | mitigate | Max 20 iterations per direction — prevents infinite loop; lot always has final clamp as safety net |
</threat_model>

<verification>
- CalculateLotFromBudget uses real Ask/Bid (D-08)
- Formula: lot = riskAmount / (slDistance * contractSize) (D-09)
- Contract sizes match Python defaults (D-10)
- Step normalization and MIN/MAX clamp (D-11)
- $50 fallback for null/zero riskAmount (D-12)
- Auto-adjust for min volume violation (D-18)
- Auto-adjust for max volume violation (D-19)
- ExecuteOpenOrder correctly integrates RISK_FIXED_AMOUNT detection
</verification>

<success_criteria>
- All 5 verification checks pass via grep
- MT5 lot calculation fully functional with auto-adjust guards
- ExecuteOpenOrder correctly routes RISK_FIXED_AMOUNT to CalculateLotFromBudget
</success_criteria>

<output>
After completion, create `.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-04-SUMMARY.md`
</output>
