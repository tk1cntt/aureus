---
phase: 55-evaluation-data-model-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-db-writer/migrations/add_trade_evaluations.sql
  - services/aureus-db-writer/tests/test_evaluation_migration.py
  - services/aureus-trader/tests/test_evaluation_pipeline.py
  - services/aureus-trader/tests/test_signal_snapshot_migration.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-04
  - SIGNAL-SNAPSHOT-01
must_haves:
  truths:
    - "Evaluation record được lưu theo trade với score_version và timestamp truy vết được về execution journal."
    - "Signal snapshot đầy đủ của trade được lưu ở bảng riêng để mở rộng mà không phá schema cốt lõi."
    - "Một trade không thể bị ghi trùng cùng score_version trong DB."
    - "Record thiếu breakdown/context bắt buộc bị chặn bởi guardrails schema."
  artifacts:
    - path: "services/aureus-db-writer/migrations/add_trade_evaluations.sql"
      provides: "DDL bảng evaluation bám lineage journal + unique/idempotency constraints"
    - path: "services/aureus-db-writer/tests/test_evaluation_migration.py"
      provides: "Test constraints EVAL-01/EVAL-04"
    - path: "services/aureus-trader/tests/test_signal_snapshot_migration.py"
      provides: "Test schema/index cho bảng signal snapshot"
    - path: "services/aureus-trader/tests/test_evaluation_pipeline.py"
      provides: "Test contract DB write shape theo trigger boundary"
  key_links:
    - from: "aureus_trade_evaluations.trade_journal_id"
      to: "aureus_trade_journal.id"
      via: "FK ON DELETE CASCADE"
      pattern: "REFERENCES aureus_trade_journal\(id\)"
    - from: "aureus_trade_evaluations"
      to: "pipeline write path"
      via: "unique(trade_journal_id, score_version)"
      pattern: "UNIQUE \(trade_journal_id, score_version\)"
    - from: "aureus_trade_signal_snapshots.trade_journal_id"
      to: "aureus_trade_journal.id"
      via: "FK signal lineage"
      pattern: "REFERENCES aureus_trade_journal\(id\)"
---

<objective>
Thiết kế và khóa schema evaluation chuẩn hóa bám Trade Execution Journal, kèm guardrails DB-level để chống duplicate/sai shape dữ liệu ngay tại persistence layer.

Purpose: Đảm bảo nền dữ liệu ổn định cho ingestion/backfill/report ở các plan sau, đúng D-01→D-06.
Output: Migration SQL bảng evaluation + bảng signal snapshots + test constraints migration + test scaffold pipeline write.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
@.planning/phases/55-evaluation-data-model-pipeline/55-RESEARCH.md
@services/aureus-db-writer/migrations/add_trade_journal.sql
@services/aureus-trader/journal.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo RED tests cho schema evaluation + signal snapshot constraints</name>
  <files>services/aureus-db-writer/tests/test_evaluation_migration.py, services/aureus-trader/tests/test_signal_snapshot_migration.py, services/aureus-trader/tests/test_evaluation_pipeline.py</files>
  <read_first>
    - services/aureus-db-writer/tests
    - services/aureus-trader/tests/test_journal.py
    - .planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
  </read_first>
  <behavior>
    - Insert record hợp lệ với trade_journal_id + score_version thành công.
    - Insert trùng (trade_journal_id, score_version) bị reject.
    - Insert signal snapshot hợp lệ (cisd/ema/candle context) thành công.
    - Insert thiếu score_breakdown hoặc missing_data_policy bị reject bởi NOT NULL/CHECK.
    - Insert signal snapshot thiếu payload hoặc trùng (trade_journal_id, signal_schema_version) bị reject.
  </behavior>
  <action>Viết test theo D-01, D-02, D-05, D-06 và SIGNAL-SNAPSHOT-01 với giá trị cụ thể: score_version="scor-v1.0.0", strategy_name="london_breakout", symbol="XAUUSD", timeframe="M15", score_total=0.7825, missing_data_policy="impute_neutral_and_flag", signal_schema_version="sig-v1.0.0", cisd_direction="bull", ema21=3345.12, ema55=3338.40. RED trước: assert duplicate key lỗi cho unique composite, assert lỗi constraint khi score_breakdown='{}'::jsonb nếu schema check yêu cầu criteria không rỗng, assert lỗi khi signal_snapshot='{}'::jsonb hoặc duplicate (trade_journal_id, signal_schema_version). Không assert cột `ema21_above_ema55` vì field này tính downstream.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_evaluation_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Có test explicit cho unique(trade_journal_id, score_version).
    - Có test explicit cho FK về aureus_trade_journal(id).
    - Có test explicit cho score_breakdown/missing_data_policy constraints.
    - RED xác nhận fail khi migration chưa có.
  </acceptance_criteria>
  <done>Test contracts cho EVAL-01/EVAL-04 đã mô tả rõ và fail đúng trước implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement migration evaluation + signal snapshot schema journal-linked</name>
  <files>services/aureus-db-writer/migrations/add_trade_evaluations.sql</files>
  <read_first>
    - services/aureus-db-writer/migrations/add_trade_journal.sql
    - .planning/phases/55-evaluation-data-model-pipeline/55-RESEARCH.md
  </read_first>
  <action>Tạo migration `add_trade_evaluations.sql` theo D-01, D-02, D-03, D-04, D-05, D-06 và SIGNAL-SNAPSHOT-01: (1) bảng `aureus_trade_evaluations` append-only gồm cột bắt buộc `trade_journal_id BIGINT`, `trace_id TEXT`, `ticket BIGINT`, `score_version TEXT`, `score_total DOUBLE PRECISION`, `score_breakdown JSONB`, `weights_snapshot JSONB`, `missing_data_policy TEXT`, `strategy_name TEXT`, `symbol TEXT`, `timeframe TEXT`, `evaluated_at TIMESTAMPTZ DEFAULT now()`, `computed_at TIMESTAMPTZ DEFAULT now()`, `is_current BOOLEAN DEFAULT TRUE`; FK tới `aureus_trade_journal(id)`, unique `(trade_journal_id, score_version)`, index `(symbol, timeframe, evaluated_at DESC)` + partial index `WHERE is_current=true`, check JSON object non-empty cho breakdown/snapshot. (2) bảng mới `aureus_trade_signal_snapshots` gồm `trade_journal_id BIGINT`, `trace_id TEXT`, `ticket BIGINT`, `strategy_name TEXT`, `symbol TEXT`, `timeframe TEXT`, `signal_schema_version TEXT`, `signal_snapshot JSONB`, typed columns `cisd_direction TEXT`, `ema21 DOUBLE PRECISION`, `ema55 DOUBLE PRECISION`, `created_at TIMESTAMPTZ DEFAULT now()`; FK tới `aureus_trade_journal(id)`, unique `(trade_journal_id, signal_schema_version)`, index `(symbol, timeframe, created_at DESC)`, index `(symbol, timeframe, cisd_direction, created_at DESC)`, BRIN index `(created_at)` cho scan time-range volume lớn, check JSON object non-empty cho signal_snapshot. Không tạo cột `ema21_above_ema55`; field này tính downstream bằng pandas/SQL expression từ ema21 và ema55.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_evaluation_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Migration có đầy đủ score/context/version/timestamp fields của EVAL-01.
    - Có bảng `aureus_trade_signal_snapshots` lưu full signal snapshot + typed analytics columns cho query nhanh trên volume lớn (kèm bộ index theo symbol/timeframe/cisd/time-range).
    - Không tạo idempotency key rời lineage; uniqueness dựa trade_journal_id + score_version (D-05/D-06).
    - DDL thể hiện append-version history (không overwrite row cũ) cho EVAL-03 readiness theo D-04.
  </acceptance_criteria>
  <done>Schema evaluation được khóa ở DB-level, test constraints pass.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy verification subset cho migration + quality guards</name>
  <files>services/aureus-db-writer/tests/test_evaluation_migration.py, services/aureus-trader/tests/test_evaluation_pipeline.py</files>
  <read_first>
    - services/aureus-db-writer/tests/test_evaluation_migration.py
    - services/aureus-trader/tests/test_evaluation_pipeline.py
  </read_first>
  <action>Chạy lại subset tests sau implement để xác nhận migration/schema guardrails ổn định và không phá test journal hiện hữu.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_journal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Tất cả test command pass.
    - Không phát sinh thay đổi ngoài `files_modified` plan 01.
  </acceptance_criteria>
  <done>Plan 01 hoàn tất và sẵn sàng cho wiring pipeline/recompute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trader service → evaluation table | Payload từ runtime vào DB có thể thiếu trường hoặc sai kiểu |
| evaluation table → downstream report/recompute | Dữ liệu duplicate/mất lineage sẽ phá truy vết |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | T | `add_trade_evaluations.sql` | mitigate | DB constraints NOT NULL/CHECK + unique composite để chặn payload sai và duplicate |
| T-55-02 | R | versioned evaluation rows | mitigate | Lưu immutable `score_version` + `weights_snapshot` per row, không update đè row cũ |
| T-55-03 | I | score breakdown JSON | mitigate | CHECK jsonb_typeof = 'object' và non-empty + test explicit shape |
| T-55-04 | D | FK lineage | accept | ON DELETE CASCADE theo journal lifecycle, chấp nhận cleanup khi journal bị xóa |
</threat_model>

<verification>
- `python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_evaluation_pipeline.py -q`
- `python3 -m pytest services/aureus-trader/tests/test_journal.py -q`
</verification>

<success_criteria>
- Có schema evaluation chuẩn hóa, journal-linked, có uniqueness/idempotency guardrails ở DB level.
- Test constraints xác nhận duplicate/sai shape không thể persist.
</success_criteria>

<output>
After completion, create `.planning/phases/55-evaluation-data-model-pipeline/55-01-SUMMARY.md`
</output>
