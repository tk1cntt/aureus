---
phase: 55-evaluation-data-model-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - services/aureus-trader/journal.py
  - services/aureus-trader/dispatcher.py
  - services/aureus-trader/tests/test_evaluation_pipeline.py
  - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
autonomous: true
requirements:
  - EVAL-02
  - EVAL-04
  - SIGNAL-SNAPSHOT-01
must_haves:
  truths:
    - "Sau khi ORDER_OPENED thành công, trade có evaluation record persisted kèm traceable lineage."
    - "Sau khi ORDER_OPENED thành công, trade có signal snapshot record persisted đầy đủ để mở rộng về sau."
    - "Evaluation persist không chạy trước ORDER_OPENED."
    - "Pipeline ghi đủ score/breakdown/version/context cho mỗi trade hợp lệ."
  artifacts:
    - path: "services/aureus-trader/journal.py"
      provides: "Hook persist evaluation linked tới journal lifecycle"
    - path: "services/aureus-trader/dispatcher.py"
      provides: "Trigger boundary ORDER_OPENED → journal integration"
    - path: "services/aureus-trader/tests/test_evaluation_pipeline.py"
      provides: "Integration tests cho ingestion/compute/persist evaluation path"
    - path: "services/aureus-trader/tests/test_signal_snapshot_pipeline.py"
      provides: "Integration tests cho persist signal snapshot path"
  key_links:
    - from: "OrderDispatcher.dispatch_order"
      to: "TradeJournalManager.on_order_opened"
      via: "ORDER_OPENED event path"
      pattern: "if final.get\(\"type\"\) == \"ORDER_OPENED\""
    - from: "TradeJournalManager.on_order_opened"
      to: "aureus_trade_evaluations"
      via: "insert evaluation after execution confirm"
      pattern: "INSERT INTO aureus_trade_evaluations"
    - from: "TradeJournalManager.on_order_opened"
      to: "aureus_trade_signal_snapshots"
      via: "insert signal snapshot after execution confirm"
      pattern: "INSERT INTO aureus_trade_signal_snapshots"
---

<objective>
Wiring ingestion/compute/persist evaluation + signal snapshot vào execution lifecycle để mỗi order mở thành công đều có record đầy đủ và truy vết được nguồn.

Purpose: Hoàn thành EVAL-02 + SIGNAL-SNAPSHOT-01 với boundary trigger đúng D-07/D-08, không tạo orphan records.
Output: Runtime persist path trong trader + integration tests pipeline cho evaluation và signal snapshot.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
@.planning/phases/55-evaluation-data-model-pipeline/55-RESEARCH.md
@services/aureus-trader/journal.py
@services/aureus-trader/dispatcher.py
@services/aureus-signal/engine/strategy_executor.py

<interfaces>
From services/aureus-trader/dispatcher.py:
```python
if final.get("type") == "ORDER_OPENED":
    if self.journal:
        trace_id = order.get("trace_id", "")
        if trace_id:
            final["trace_id"] = trace_id
        await self.journal.on_order_opened(final)
```

From services/aureus-signal/engine/strategy_executor.py:
```python
decision["score_total"]
decision["score_breakdown"]
decision["score_version"]
decision["weights_snapshot"]
decision["missing_data_policy"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo RED integration tests cho trigger boundary và persist payload/signal snapshot</name>
  <files>services/aureus-trader/tests/test_evaluation_pipeline.py, services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <read_first>
    - services/aureus-trader/tests/test_journal.py
    - services/aureus-trader/dispatcher.py
    - .planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
  </read_first>
  <behavior>
    - ORDER_OPENED với trace_id hợp lệ tạo row evaluation linked tới journal row.
    - ORDER_OPENED với trace_id hợp lệ tạo row signal snapshot linked tới journal row.
    - STRATEGY_MATCH (trước ORDER_OPENED) không tạo row evaluation/signal snapshot.
    - Payload persist chứa score_total/score_breakdown/score_version/weights_snapshot/missing_data_policy.
    - Payload persist signal snapshot chứa cisd_direction, ema21, ema55 và snapshot JSONB đầy đủ; `ema21_above_ema55` không lưu vật lý, tính downstream từ ema21/ema55.
  </behavior>
  <action>Viết test theo D-07, D-08 với scenario cụ thể: trace_id="tr-55-001", ticket=123456789, score_version="scor-v1.0.0", score_total=0.801234, signal_schema_version="sig-v1.0.0", cisd_direction="bull", ema21=3345.12, ema55=3338.40. Assert insert chỉ xảy ra sau `on_order_opened`; assert lineage fields (`trade_journal_id`, `trace_id`, `ticket`) luôn có giá trị trên cả evaluation/signal snapshot; assert duplicate trigger cùng score_version hoặc signal_schema_version không tạo thêm record mới (EVAL-04).</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Có test explicit chứng minh boundary: pre-open = 0 rows, post-open = 1 row.
    - Có test explicit cho payload fields từ scoring contract.
    - RED xác nhận fail trước khi wiring code.
  </acceptance_criteria>
  <done>Test contracts EVAL-02 rõ ràng và fail đúng trước implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement persist evaluation + signal snapshot trong on_order_opened theo journal lineage</name>
  <files>services/aureus-trader/journal.py, services/aureus-trader/dispatcher.py</files>
  <read_first>
    - services/aureus-trader/journal.py
    - services/aureus-trader/dispatcher.py
    - services/aureus-trader/tests/test_evaluation_pipeline.py
  </read_first>
  <action>Implement theo D-01, D-02, D-07, D-08: mở rộng `TradeJournalManager.on_order_opened` để sau khi update `aureus_trade_journal` thành công thì insert evaluation row vào `aureus_trade_evaluations` bằng dữ liệu scoring đã có trong event/order payload (score_total, score_breakdown, score_version, weights_snapshot, missing_data_policy, strategy_name, symbol, timeframe) và insert signal snapshot row vào `aureus_trade_signal_snapshots` (signal_schema_version, signal_snapshot JSONB, cisd_direction, ema21, ema55). Nếu thiếu score_version thì dùng fallback "scor-v1.0.0"; dùng `ON CONFLICT (trade_journal_id, score_version) DO NOTHING` và `ON CONFLICT (trade_journal_id, signal_schema_version) DO NOTHING` để idempotent đúng D-05/D-06; không persist cột dẫn xuất `ema21_above_ema55`; giữ nguyên dispatcher trigger chỉ ở ORDER_OPENED, không thêm trigger sớm hơn.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py services/aureus-trader/tests/test_journal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Code path persist chỉ chạy khi journal update thành công ở trạng thái EXECUTED.
    - Có mapping lineage rõ trade_journal_id/trace_id/ticket trên row evaluation.
    - Không thêm pipeline persist ở STRATEGY_MATCH hoặc trước MT5 success.
  </acceptance_criteria>
  <done>Pipeline persist runtime hoạt động đúng boundary và pass test integration.</done>
</task>

<task type="auto">
  <name>Task 3: Regression subset cho trader lifecycle + evaluation ingest</name>
  <files>services/aureus-trader/tests/test_evaluation_pipeline.py, services/aureus-trader/tests/test_journal.py</files>
  <read_first>
    - services/aureus-trader/tests/test_journal.py
    - services/aureus-trader/tests/test_evaluation_pipeline.py
  </read_first>
  <action>Chạy regression subset để xác nhận bổ sung evaluation persist không phá behavior journal hiện hữu.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_journal.py services/aureus-trader/tests/test_evaluation_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Exit code 0 cho command verify.
    - Không phát sinh sửa file ngoài `files_modified` plan 02.
  </acceptance_criteria>
  <done>Plan 02 hoàn tất, dữ liệu evaluation được ghi ổn định theo execution lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 event stream → trader persist | Event có thể replay hoặc thiếu field scoring |
| trader journal update → evaluation insert | Nếu mapping sai sẽ mất traceability hoặc tạo orphan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-05 | T | `journal.py:on_order_opened` | mitigate | Validate fields bắt buộc trước insert và reject payload thiếu lineage/scoring core |
| T-55-06 | R | evaluation write path | mitigate | Persist immutable score_version/weights_snapshot + trace_id/ticket để audit lại source |
| T-55-07 | D | replay events | mitigate | `ON CONFLICT (trade_journal_id, score_version) DO NOTHING` + test duplicate replay |
| T-55-08 | E | dispatcher trigger boundary | accept | Boundary chỉ dùng event nội bộ đã ACK; không mở thêm external write surface ở plan này |
</threat_model>

<verification>
- `python3 -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py -q`
- `python3 -m pytest services/aureus-trader/tests/test_journal.py -q`
</verification>

<success_criteria>
- Mỗi ORDER_OPENED hợp lệ tạo được evaluation record đầy đủ và traceable.
- Không tạo evaluation orphan trước ORDER_OPENED hoặc khi replay event trùng.
</success_criteria>

<output>
After completion, create `.planning/phases/55-evaluation-data-model-pipeline/55-02-SUMMARY.md`
</output>
