---
phase: 55-evaluation-data-model-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - 55-01
  - 55-02
  - 55-03
files_modified:
  - services/aureus-db-writer/migrations/add_trade_evaluations.sql
  - services/aureus-db-writer/tests/test_evaluation_migration.py
  - services/aureus-trader/tests/test_signal_snapshot_migration.py
  - scripts/dev-service.sh
  - RUN_SERVICES.md
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - SIGNAL-SNAPSHOT-01
must_haves:
  truths:
    - "Phase 55 chỉ được coi là complete khi migration đã apply trên runtime DB dev, không chỉ pass unit test."
    - "Bảng aureus_trade_evaluations và aureus_trade_signal_snapshots phải tồn tại thực tế trong TimescaleDB dev."
    - "Pipeline journal -> persist phải tạo được record thật trong DB sau sự kiện ORDER_OPENED hợp lệ."
  artifacts:
    - path: "services/aureus-db-writer/tests/test_evaluation_migration.py"
      provides: "Schema verification cho bảng/index/constraints của phase 55"
    - path: "services/aureus-trader/tests/test_signal_snapshot_migration.py"
      provides: "Verification signal snapshot migration contract"
    - path: "RUN_SERVICES.md"
      provides: "Runbook verify runtime migration + DB checks"
---

<objective>
Đóng gap xác minh runtime cho phase 55: bảo đảm migration đã apply trên môi trường dev thật và dữ liệu evaluation/signal snapshot được persist thành công end-to-end.

Purpose: Loại bỏ tình huống “test pass nhưng DB runtime chưa có bảng phase 55”.
Output: Checklist + test + runbook đảm bảo schema/runtime parity trước khi mark complete.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
@.planning/phases/55-evaluation-data-model-pipeline/55-RESEARCH.md
@services/aureus-db-writer/migrations/add_trade_evaluations.sql
@services/aureus-trader/journal.py
@RUN_SERVICES.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bổ sung verification test cho runtime schema parity</name>
  <files>services/aureus-db-writer/tests/test_evaluation_migration.py, services/aureus-trader/tests/test_signal_snapshot_migration.py</files>
  <read_first>
    - services/aureus-db-writer/tests/test_evaluation_migration.py
    - services/aureus-trader/tests/test_signal_snapshot_migration.py
    - services/aureus-db-writer/migrations/add_trade_evaluations.sql
  </read_first>
  <behavior>
    - Assert đầy đủ 2 bảng phase 55 tồn tại.
    - Assert unique constraints và index chính tồn tại đúng tên/shape.
    - Assert JSONB shape checks tồn tại và hợp lệ.
  </behavior>
  <action>Viết/siết test migration để fail nếu thiếu bảng hoặc thiếu index/constraint trên DB test runtime context.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Test fail ngay khi schema phase 55 chưa được apply đầy đủ.
    - Test pass khi schema đúng với migration SQL thực tế.
  </acceptance_criteria>
  <done>Migration tests trở thành gate bắt buộc cho schema parity.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime DB verification gate trong runbook</name>
  <files>RUN_SERVICES.md, scripts/dev-service.sh</files>
  <read_first>
    - RUN_SERVICES.md
    - scripts/dev-service.sh
  </read_first>
  <action>Thêm bước verify runtime DB sau restart services: query existence 2 bảng phase 55 + query count mới nhất để xác nhận persist pipeline hoạt động.</action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('aureus_trade_evaluations','aureus_trade_signal_snapshots');\""</automated>
  </verify>
  <acceptance_criteria>
    - Runbook có command kiểm tra schema runtime rõ ràng.
    - Có command kiểm tra dữ liệu mới nhất trong 2 bảng sau khi pipeline chạy.
  </acceptance_criteria>
  <done>Ops có checklist rõ để tránh mark complete khi runtime DB chưa sẵn sàng.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end persistence verification and sign-off</name>
  <files>services/aureus-trader/journal.py</files>
  <read_first>
    - services/aureus-trader/journal.py
    - services/aureus-db-writer/migrations/add_trade_evaluations.sql
  </read_first>
  <action>Chạy flow thực tế (ORDER_OPENED payload có scoring + signal fields), xác nhận insert thành công vào aureus_trade_evaluations và aureus_trade_signal_snapshots với symbol/timeframe/trace_id tương ứng.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q</automated>
    <manual>Query DB runtime để xác nhận có bản ghi mới nhất theo created_at/evaluated_at.</manual>
  </verify>
  <acceptance_criteria>
    - Có bằng chứng DB runtime có row mới cho cả evaluations và signal snapshots.
    - Không còn trạng thái “phase complete nhưng runtime schema thiếu”.
  </acceptance_criteria>
  <done>Phase 55 được xác nhận theo chuẩn code + runtime đầy đủ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL -> runtime DB | Migration có thể tồn tại trong repo nhưng chưa apply trên DB dev |
| Runtime events -> journal persistence | Payload hợp lệ nhưng schema thiếu sẽ làm persistence fail silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-13 | R | Phase completion evidence | mitigate | Bắt buộc evidence từ DB runtime (table existence + latest row query) |
| T-55-14 | D | Missing runtime schema | mitigate | Migration parity tests + runbook gate trước sign-off |
| T-55-15 | T | Partial payload persistence | mitigate | E2E checks với trace_id xác thực cả evaluation và signal snapshot |
</threat_model>

<verification>
- `python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py -q`
- `python3 -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q`
- Runtime DB check:
  - `SELECT tablename FROM pg_tables ... IN ('aureus_trade_evaluations','aureus_trade_signal_snapshots')`
  - `SELECT trace_id, symbol, timeframe, created_at FROM aureus_trade_signal_snapshots ORDER BY created_at DESC LIMIT 5`
  - `SELECT trace_id, symbol, timeframe, evaluated_at FROM aureus_trade_evaluations ORDER BY evaluated_at DESC LIMIT 5`
</verification>

<success_criteria>
- Không còn false-positive hoàn thành phase khi DB runtime chưa có schema.
- Có evidence runtime persistence thật cho phase 55 (evaluation + snapshot).
</success_criteria>

<output>
After completion, create `.planning/phases/55-evaluation-data-model-pipeline/55-04-SUMMARY.md`
</output>
