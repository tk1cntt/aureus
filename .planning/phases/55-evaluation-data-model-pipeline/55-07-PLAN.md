---
phase: 55-evaluation-data-model-pipeline
plan: 07
type: execute
wave: 7
depends_on:
  - 55-06
files_modified:
  - services/aureus-trader/journal.py
  - services/aureus-trader/tests/test_evaluation_pipeline.py
  - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
  - services/aureus-trader/recompute_evaluations.py
  - services/aureus-trader/scripts/verify_phase55_runtime_evidence.py
  - RUN_SERVICES.md
autonomous: true
requirements:
  - EVAL-02
  - EVAL-04
  - EVAL-RUNTIME-03
  - EVAL-RUNTIME-04
must_haves:
  truths:
    - "Mỗi ORDER_OPENED hợp lệ đều tạo evaluation record đầy đủ; không còn nhánh silent-skip khi thiếu scoring core fields."
    - "Nếu payload scoring không hợp lệ, pipeline fail rõ ràng với error path có thể kiểm chứng (không chỉ warning log)."
    - "Có bằng chứng runtime DB thật cho evaluation + signal snapshot (table/index/latest rows) theo checklist sign-off."
    - "Timeframe lineage cho recompute có policy rõ: nguồn chuẩn ưu tiên schema lineage, fallback tạm thời phải có cảnh báo và evidence kiểm soát."
  artifacts:
    - path: "services/aureus-trader/journal.py"
      provides: "Runtime invariant enforcement cho evaluation payload tại ORDER_OPENED"
    - path: "services/aureus-trader/tests/test_evaluation_pipeline.py"
      provides: "Contract tests ép invariant đầy đủ/thiếu payload"
    - path: "services/aureus-trader/recompute_evaluations.py"
      provides: "Timeframe lineage handling có guardrail + explicit warning path"
    - path: "services/aureus-trader/scripts/verify_phase55_runtime_evidence.py"
      provides: "Tạo runtime evidence machine-readable cho DB sign-off"
  key_links:
    - from: "services/aureus-trader/dispatcher.py"
      to: "services/aureus-trader/journal.py:on_order_opened"
      via: "ORDER_OPENED event path"
      pattern: "await self.journal.on_order_opened(final)"
    - from: "services/aureus-trader/journal.py:on_order_opened"
      to: "aureus_trade_evaluations"
      via: "mandatory insert after payload contract validation"
      pattern: "INSERT INTO aureus_trade_evaluations"
    - from: "services/aureus-trader/scripts/verify_phase55_runtime_evidence.py"
      to: "aureus_trade_evaluations,aureus_trade_signal_snapshots"
      via: "runtime SQL evidence"
      pattern: "SELECT COUNT\(\*\), MAX\(evaluated_at\)|MAX\(created_at\)"
---

<objective>
Đóng các gap verification còn mở của Phase 55 bằng cách khóa invariant runtime cho evaluation payload/journal persist contract và tạo evidence runtime DB bắt buộc, truy xuất được bằng command tự động.

Purpose: Hoàn tất EVAL-02 theo đúng contract “mỗi trade ORDER_OPENED hợp lệ có evaluation record đầy đủ”, đồng thời chốt EVAL-RUNTIME-03/04 bằng bằng chứng DB thật thay vì chứng cứ test-only.
Output: Invariant code + test coverage âm/dương + runtime evidence script/checklist.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
@.planning/phases/55-evaluation-data-model-pipeline/55-RESEARCH.md
@.planning/phases/55-evaluation-data-model-pipeline/55-VERIFICATION.md
@.planning/phases/55-evaluation-data-model-pipeline/55-06-SUMMARY.md
@services/aureus-trader/journal.py
@services/aureus-trader/recompute_evaluations.py
@services/aureus-trader/tests/test_evaluation_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Khóa invariant payload cho ORDER_OPENED (không cho silent-skip persist evaluation)</name>
  <files>services/aureus-trader/tests/test_evaluation_pipeline.py, services/aureus-trader/tests/test_signal_snapshot_pipeline.py, services/aureus-trader/journal.py</files>
  <read_first>
    - services/aureus-trader/journal.py
    - services/aureus-trader/tests/test_evaluation_pipeline.py
    - .planning/phases/55-evaluation-data-model-pipeline/55-VERIFICATION.md
  </read_first>
  <action>Thực hiện theo D-07/D-08 và gap EVAL-02: thêm test RED cho 2 nhánh bắt buộc (1) ORDER_OPENED hợp lệ có đủ `score_total`, `score_breakdown`, `weights_snapshot`, `missing_data_policy`, `score_version` thì luôn insert đúng 1 row vào `aureus_trade_evaluations`; (2) nếu thiếu bất kỳ core field nào thì `on_order_opened` phải đi vào fail path rõ ràng (raise exception domain-specific hoặc return error result có code cố định) và KHÔNG được chỉ warning + skip persist. Sau đó implement trong `journal.py` validation bắt buộc trước insert, giữ idempotent `ON CONFLICT` theo D-05/D-06, và đảm bảo traceable lineage (`trade_journal_id`, `trace_id`, `ticket`) luôn có khi persist thành công.</action>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-trader/tests/test_evaluation_pipeline.py /d/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Không còn test nào chấp nhận behavior "thiếu payload nhưng vẫn coi pipeline pass".
    - Có test explicit chứng minh ORDER_OPENED hợp lệ luôn tạo evaluation row.
    - Có test explicit chứng minh payload thiếu field đi vào fail path rõ ràng và assert được.
  </acceptance_criteria>
  <done>Invariant runtime cho persist contract được cưỡng bức bằng test + code, loại bỏ silent-skip gap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Siết timeframe lineage guardrail trong recompute và thêm evidence cảnh báo fallback</name>
  <files>services/aureus-trader/recompute_evaluations.py, services/aureus-trader/tests/test_evaluation_pipeline.py, services/aureus-trader/tests/test_evaluation_recompute.py</files>
  <read_first>
    - services/aureus-trader/recompute_evaluations.py
    - .planning/REQUIREMENTS.md
    - .planning/phases/55-evaluation-data-model-pipeline/55-VERIFICATION.md
  </read_first>
  <action>Đóng gap EVAL-RUNTIME-03: cập nhật recompute để ưu tiên timeframe từ lineage schema/journal canonical source; chỉ dùng fallback khi không còn nguồn hợp lệ và phải phát cảnh báo có cấu trúc (bao gồm `trace_id`, `trade_journal_id`, fallback_source) để kiểm soát drift. Bổ sung test assert ưu tiên nguồn canonical, và test assert fallback path có warning/event rõ ràng (không silent default). Không thay đổi semantics append-only/versioned hiện có của recompute.</action>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-trader/tests/test_evaluation_pipeline.py /d/Aureus/services/aureus-trader/tests/test_evaluation_recompute.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Recompute không mặc định `M1` silent khi thiếu timeframe.
    - Fallback path (nếu còn) có evidence machine-checkable để theo dõi và loại bỏ ở phase tiếp theo.
    - Không phá idempotency/append-only contract của EVAL-03/EVAL-04.
  </acceptance_criteria>
  <done>Timeframe lineage governance được chuyển từ "needs human" sang có guardrail code + test chứng minh.</done>
</task>

<task type="auto">
  <name>Task 3: Tạo runtime evidence gate tự động cho sign-off verification</name>
  <files>services/aureus-trader/scripts/verify_phase55_runtime_evidence.py, RUN_SERVICES.md</files>
  <read_first>
    - RUN_SERVICES.md
    - .planning/phases/55-evaluation-data-model-pipeline/55-VERIFICATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Tạo script CLI `verify_phase55_runtime_evidence.py` chạy các SQL bắt buộc cho EVAL-RUNTIME-04 và xuất JSON artifact gồm: table presence (`aureus_trade_evaluations`, `aureus_trade_signal_snapshots`), index presence, `COUNT(*)` + `MAX(evaluated_at/created_at)`, và mẫu 5 row mới nhất với `trace_id,symbol,timeframe,score_version/signal_schema_version`. Cập nhật `RUN_SERVICES.md` để dùng script này như gate sign-off chính thức; yêu cầu command trả exit code != 0 khi thiếu table/index hoặc không có row runtime mới.</action>
  <verify>
    <automated>python3 /d/Aureus/services/aureus-trader/scripts/verify_phase55_runtime_evidence.py --dsn "$AUREUS_DB_DSN" --output /d/Aureus/.planning/phases/55-evaluation-data-model-pipeline/55-07-runtime-evidence.json</automated>
  </verify>
  <acceptance_criteria>
    - Có một command duy nhất tạo evidence runtime DB đầy đủ cho verifier.
    - Evidence artifact lưu được và đọc được ở path phase 55.
    - Gate fail rõ ràng khi thiếu schema/index/data mới, tránh false-positive completion.
  </acceptance_criteria>
  <done>Runtime evidence từ DB thật trở thành kiểm chứng bắt buộc, không phụ thuộc xác nhận thủ công rời rạc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ORDER_OPENED event payload → journal persist | Payload thiếu core fields có thể làm mất evaluation record nếu không cưỡng bức invariant |
| recompute lineage source → evaluation rows | Timeframe drift gây aggregate/report sai nghĩa |
| runtime DB state → verification sign-off | Thiếu evidence runtime tạo false completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-23 | T | `journal.py` payload validation | mitigate | Reject payload thiếu scoring core fields; test âm/dương bắt buộc cho ORDER_OPENED contract |
| T-55-24 | R | recompute timeframe lineage | mitigate | Ưu tiên canonical lineage source + structured warning khi fallback |
| T-55-25 | R | phase sign-off evidence | mitigate | Script runtime evidence trả JSON + non-zero exit khi thiếu schema/index/data |
| T-55-26 | D | runtime verification workflow | mitigate | Gate tự động hóa command-level thay cho checklist thủ công phân tán |
</threat_model>

<verification>
- `python3 -m pytest /d/Aureus/services/aureus-trader/tests/test_evaluation_pipeline.py /d/Aureus/services/aureus-trader/tests/test_signal_snapshot_pipeline.py /d/Aureus/services/aureus-trader/tests/test_evaluation_recompute.py -q`
- `python3 /d/Aureus/services/aureus-trader/scripts/verify_phase55_runtime_evidence.py --dsn "$AUREUS_DB_DSN" --output /d/Aureus/.planning/phases/55-evaluation-data-model-pipeline/55-07-runtime-evidence.json`
- `python3 -c "import json; p='/d/Aureus/.planning/phases/55-evaluation-data-model-pipeline/55-07-runtime-evidence.json'; d=json.load(open(p,'r',encoding='utf-8')); assert d['tables_ok'] and d['indexes_ok'] and d['latest_rows_ok']"`
</verification>

<success_criteria>
- Gap FAILED trong 55-VERIFICATION về EVAL-02 được đóng bằng invariant runtime + test contract bắt buộc.
- Gaps NEEDS HUMAN cho EVAL-RUNTIME-03/04 được chuyển thành evidence path có thể chạy tự động.
- Có artifact runtime evidence JSON để verifier đối chiếu trực tiếp.
</success_criteria>

<output>
After completion, create `.planning/phases/55-evaluation-data-model-pipeline/55-07-SUMMARY.md`
</output>
