---
phase: 55-evaluation-data-model-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 55-01
  - 55-02
files_modified:
  - services/aureus-trader/recompute_evaluations.py
  - services/aureus-trader/tests/test_evaluation_recompute.py
  - services/aureus-trader/tests/test_evaluation_pipeline.py
  - services/aureus-trader/tests/test_signal_snapshot_recompute.py
  - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
autonomous: true
requirements:
  - EVAL-03
  - SIGNAL-SNAPSHOT-01
must_haves:
  truths:
    - "Khi đổi score_version/weights, hệ thống có thể recompute/backfill mà không mất lịch sử version cũ."
    - "Signal snapshot schema mới có thể backfill/recompute mà không mất lịch sử snapshot cũ."
    - "Sau recompute, truy vấn vẫn xác định được record current cho mỗi trade."
    - "Recompute chạy lặp lại không tạo duplicate cùng trade+score_version."
  artifacts:
    - path: "services/aureus-trader/recompute_evaluations.py"
      provides: "Batch recompute/backfill command append-version rows"
    - path: "services/aureus-trader/tests/test_evaluation_recompute.py"
      provides: "Tests đảm bảo history retention + idempotent recompute"
    - path: "services/aureus-trader/tests/test_signal_snapshot_recompute.py"
      provides: "Tests đảm bảo signal snapshot history retention + idempotent recompute"
    - path: "services/aureus-trader/tests/test_evaluation_pipeline.py"
      provides: "Shared fixtures/assertions cho evaluation rows"
    - path: "services/aureus-trader/tests/test_signal_snapshot_pipeline.py"
      provides: "Shared fixtures/assertions cho signal snapshot rows"
  key_links:
    - from: "recompute_evaluations.py"
      to: "aureus_trade_journal"
      via: "select executed trades theo time window"
      pattern: "FROM aureus_trade_journal"
    - from: "recompute_evaluations.py"
      to: "aureus_trade_evaluations"
      via: "append row per score_version + upsert current flag"
      pattern: "INSERT INTO aureus_trade_evaluations"
    - from: "recompute_evaluations.py"
      to: "aureus_trade_signal_snapshots"
      via: "append row per signal_schema_version"
      pattern: "INSERT INTO aureus_trade_signal_snapshots"
---

<objective>
Xây pipeline backfill/recompute theo score_version để tính lại evaluation + signal snapshot khi đổi trọng số/schema mà vẫn giữ đầy đủ lịch sử và đảm bảo idempotent.

Purpose: Hoàn thành EVAL-03 + SIGNAL-SNAPSHOT-01 với cơ chế append-only versioned history bám lineage journal (D-03/D-04).
Output: Script recompute batch + tests history/idempotency cho evaluation và signal snapshot.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
@.planning/phases/55-evaluation-data-model-pipeline/55-RESEARCH.md
@services/aureus-signal/engine/scoring/compute.py
@services/aureus-trader/journal.py

<interfaces>
From services/aureus-signal/engine/scoring/compute.py:
```python
compute_trade_score(input_payload: dict, score_version: str, weights_snapshot: dict) -> dict
```

Expected evaluation uniqueness:
```sql
UNIQUE (trade_journal_id, score_version)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo RED tests cho recompute append-version history (evaluation + signal snapshot)</name>
  <files>services/aureus-trader/tests/test_evaluation_recompute.py, services/aureus-trader/tests/test_signal_snapshot_recompute.py, services/aureus-trader/tests/test_evaluation_pipeline.py, services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <read_first>
    - services/aureus-trader/tests/test_evaluation_pipeline.py
    - .planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
    - .planning/phases/55-evaluation-data-model-pipeline/55-RESEARCH.md
  </read_first>
  <behavior>
    - Recompute với score_version mới tạo row evaluation mới, giữ nguyên row version cũ.
    - Recompute với signal_schema_version mới tạo row signal snapshot mới, giữ nguyên snapshot version cũ.
    - Recompute lặp lại cùng score_version/signal_schema_version không tạo duplicate.
    - Sau recompute, chỉ row evaluation mới nhất của trade có is_current=true.
  </behavior>
  <action>Viết test với dataset cụ thể: trade_journal_id=101 đã có row scor-v1.0.0; chạy recompute scor-v1.1.0 với weights_snapshot={"profit_outcome":0.30,"signal_quality":0.35,"timing_quality":0.20,"volatility_session":0.15}. Assert row count tăng +1, row scor-v1.0.0 vẫn tồn tại, rerun scor-v1.1.0 không tăng count, state `is_current` chuyển đúng từ version cũ sang version mới.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_recompute.py services/aureus-trader/tests/test_signal_snapshot_recompute.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Có test explicit giữ lịch sử version cũ.
    - Có test explicit idempotency khi rerun cùng version.
    - RED xác nhận fail trước implement recompute script.
  </acceptance_criteria>
  <done>Test contracts cho EVAL-03 hoàn chỉnh và fail đúng trước implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recompute/backfill script theo batch window</name>
  <files>services/aureus-trader/recompute_evaluations.py</files>
  <read_first>
    - services/aureus-trader/journal.py
    - services/aureus-signal/engine/scoring/compute.py
    - services/aureus-trader/tests/test_evaluation_recompute.py
  </read_first>
  <action>Tạo script `recompute_evaluations.py` theo D-03, D-04, D-05, D-06 nhận tham số `--score-version`, `--signal-schema-version`, `--start`, `--end`, `--batch-size` (default 500). Với mỗi batch trade EXECUTED/CLOSED trong `aureus_trade_journal`, tính score qua `compute_trade_score`, insert append-only vào `aureus_trade_evaluations` với `ON CONFLICT (trade_journal_id, score_version) DO NOTHING`; đồng thời tạo row signal snapshot version mới vào `aureus_trade_signal_snapshots` với `ON CONFLICT (trade_journal_id, signal_schema_version) DO NOTHING`. Sau insert evaluation thành công, set `is_current=false` cho version cũ cùng trade và `is_current=true` cho row version mới. Không update đè score_version cũ, không dùng key idempotency rời journal lineage; không persist cột dẫn xuất `ema21_above_ema55`, field này tính downstream bằng pandas/SQL expression.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_recompute.py services/aureus-trader/tests/test_signal_snapshot_recompute.py services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Script hỗ trợ backfill theo time window và chunking batch-size.
    - Recompute đảm bảo append-only version history, không overwrite bản cũ.
    - Idempotent khi rerun cùng score_version theo unique composite hiện có.
  </acceptance_criteria>
  <done>Pipeline recompute/backfill chạy ổn định, giữ lịch sử và pass test.</done>
</task>

<task type="auto">
  <name>Task 3: Verification full subset cho evaluation phase scope</name>
  <files>services/aureus-trader/tests/test_evaluation_recompute.py, services/aureus-trader/tests/test_evaluation_pipeline.py, services/aureus-db-writer/tests/test_evaluation_migration.py</files>
  <read_first>
    - services/aureus-trader/tests/test_evaluation_recompute.py
    - services/aureus-trader/tests/test_evaluation_pipeline.py
    - services/aureus-db-writer/tests/test_evaluation_migration.py
  </read_first>
  <action>Chạy full subset test cho schema + persist + recompute để chốt readiness trước Phase 56.</action>
  <verify>
    <automated>python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py services/aureus-trader/tests/test_evaluation_recompute.py services/aureus-trader/tests/test_signal_snapshot_recompute.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Command verify pass toàn bộ.
    - Không có thay đổi file ngoài `files_modified` plan 03.
  </acceptance_criteria>
  <done>Phase 55 data model + pipeline + recompute được xác nhận hoàn chỉnh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recompute CLI input → DB queries | Input window/version có thể sai hoặc bị lạm dụng gây recompute ngoài phạm vi |
| recompute scorer → evaluation persistence | Tính lại sai hoặc overwrite sai sẽ phá audit trail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-09 | T | `recompute_evaluations.py` inputs | mitigate | Validate tham số bắt buộc (`score_version`, start/end) và batch-size giới hạn >0 |
| T-55-10 | R | historical evaluation rows | mitigate | Append-only per score_version + giữ immutable weights_snapshot/computed_at cho audit |
| T-55-11 | D | recompute rerun | mitigate | `ON CONFLICT DO NOTHING` theo unique composite để chống duplicate khi replay job |
| T-55-12 | E | recompute execution scope | accept | Script chạy nội bộ ops path, không expose endpoint public trong phase này |
</threat_model>

<verification>
- `python3 -m pytest services/aureus-trader/tests/test_evaluation_recompute.py -q`
- `python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_evaluation_recompute.py -q`
</verification>

<success_criteria>
- Recompute/backfill theo score_version chạy được và giữ lịch sử version cũ.
- Rerun idempotent không tạo duplicate rows.
</success_criteria>

<output>
After completion, create `.planning/phases/55-evaluation-data-model-pipeline/55-03-SUMMARY.md`
</output>
