---
phase: 55-evaluation-data-model-pipeline
plan: 06
type: execute
wave: 6
depends_on:
  - 55-05
files_modified:
  - services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql
  - services/aureus-db-writer/tests/test_signal_snapshot_migration.py
  - services/aureus-trader/journal.py
  - services/aureus-trader/recompute_evaluations.py
  - services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py
  - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
  - services/aureus-trader/tests/test_signal_snapshot_recompute.py
  - services/aureus-trader/tests/test_signal_snapshot_storage_policy.py
autonomous: true
requirements:
  - SIGNAL-SNAPSHOT-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-RUNTIME-01
  - EVAL-RUNTIME-03
  - EVAL-RUNTIME-04
requirements_addressed:
  - SIGNAL-SNAPSHOT-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-RUNTIME-01
  - EVAL-RUNTIME-03
  - EVAL-RUNTIME-04
must_haves:
  truths:
    - "Snapshot lưu đủ canonical fields để resume state + explainability + reporting, đồng thời vẫn persist full raw `signal_snapshot` JSONB theo mô hình hybrid của SIGNAL-SNAPSHOT-01."
    - "Các field debug/legacy/derived (`ema21_above_ema55`, `debug_payload`, `raw_reasoning`) bị loại khỏi hot typed physical columns; không tạo cột vật lý riêng cho các field này."
    - "Retention TTL + archive policy rõ ràng: query hot nhanh cho dữ liệu gần đây và vẫn giữ lịch sử archive cho audit/recompute."
    - "Backfill/recompute chạy idempotent với schema tối ưu mới, không làm mất record version cũ."
    - "Có bằng chứng E2E trên DB dev thật: migration tồn tại, insert runtime thành công, và dữ liệu mới xuất hiện ở bảng snapshot."
  artifacts:
    - path: "services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql"
      provides: "DDL canonical hot typed columns + full raw signal_snapshot JSONB + archive table + retention policy + indexes"
    - path: "services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py"
      provides: "Backfill/rewrite canonical typed fields từ dữ liệu hiện hữu; vẫn giữ full raw signal_snapshot JSONB"
    - path: "services/aureus-trader/recompute_evaluations.py"
      provides: "Compatibility path với schema snapshot mới trong recompute"
    - path: "services/aureus-trader/tests/test_signal_snapshot_storage_policy.py"
      provides: "TDD coverage cho hybrid policy: raw JSONB preserved + typed hot pruning + retention + archive"
  key_links:
    - from: "services/aureus-trader/journal.py"
      to: "aureus_trade_signal_snapshots"
      via: "INSERT canonical typed fields + full raw signal_snapshot JSONB tại ORDER_OPENED"
      pattern: "INSERT INTO aureus_trade_signal_snapshots"
    - from: "services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py"
      to: "aureus_trade_signal_snapshots_archive"
      via: "archive-before-prune theo TTL"
      pattern: "INSERT INTO aureus_trade_signal_snapshots_archive"
    - from: "services/aureus-trader/recompute_evaluations.py"
      to: "aureus_trade_signal_snapshots"
      via: "idempotent upsert theo (trade_journal_id, signal_schema_version)"
      pattern: "ON CONFLICT \(trade_journal_id, signal_schema_version\) DO"
---

<objective>
Tối ưu lưu trữ `aureus_trade_signal_snapshots` theo mô hình hybrid của SIGNAL-SNAPSHOT-01: giữ full raw `signal_snapshot` JSONB để audit/recompute, đồng thời chỉ giữ tập typed hot columns canonical phục vụ resume/explainability/reporting và loại bỏ debug/legacy/derived khỏi hot physical columns.

Purpose: Giảm phình dữ liệu typed hot path và drift semantics nhưng vẫn bảo toàn traceability/audit với full raw snapshot + journal lineage theo D-01, D-02, D-05, D-06, D-07, D-08.
Output: Migration policy + runtime writer/recompute compatibility + backfill script + test suite + runtime DB verification evidence.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
@.planning/phases/55-evaluation-data-model-pipeline/55-RESEARCH.md
@services/aureus-db-writer/migrations/add_trade_evaluations.sql
@services/aureus-trader/journal.py
@services/aureus-trader/recompute_evaluations.py
@RUN_SERVICES.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Khóa hybrid storage contract (raw JSONB preserved + typed hot canonical) bằng RED tests</name>
  <files>services/aureus-trader/tests/test_signal_snapshot_storage_policy.py, services/aureus-db-writer/tests/test_signal_snapshot_migration.py, services/aureus-trader/tests/test_signal_snapshot_pipeline.py</files>
  <read_first>
    - services/aureus-trader/tests/test_signal_snapshot_pipeline.py
    - services/aureus-trader/tests/test_signal_snapshot_recompute.py
    - services/aureus-db-writer/tests/test_signal_snapshot_migration.py
    - .planning/phases/55-evaluation-data-model-pipeline/55-CONTEXT.md
  </read_first>
  <behavior>
    - Canonical typed hot columns bắt buộc: trade_journal_id, trace_id, ticket, strategy_name, symbol, timeframe, signal_schema_version, cisd_direction, ema21, ema55, created_at.
    - Bắt buộc persist full raw `signal_snapshot` JSONB object theo SIGNAL-SNAPSHOT-01.
    - Không tạo typed physical columns cho field debug/legacy/derived: ema21_above_ema55, debug_payload, raw_reasoning.
    - TTL mặc định 90 ngày ở bảng hot; row quá TTL phải được archive trước khi prune.
    - Archive table giữ lineage + full raw `signal_snapshot` JSONB để audit/recompute.
  </behavior>
  <action>Tạo test RED với giá trị cố định: `trace_id='tr-55-storage-001'`, `symbol='XAUUSD'`, `timeframe='M15'`, `signal_schema_version='sig-v2.0.0'`, `cisd_direction='bull'`, `ema21=3345.12`, `ema55=3338.40`, `created_at='2026-04-01T00:00:00Z'`. Test phải assert migration có cột `signal_snapshot JSONB` và policy TTL 90 ngày + archive-before-prune; assert pipeline vẫn persist full raw `signal_snapshot` JSONB nhưng reject typed-column attempt cho `debug_payload`/`raw_reasoning`; assert không có typed physical column `ema21_above_ema55` (theo SIGNAL-SNAPSHOT-01) và duplicate `(trade_journal_id, signal_schema_version)` bị chặn theo D-05/D-06.</action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_signal_snapshot_storage_policy.py services/aureus-db-writer/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - Có test explicit cho hybrid policy: raw JSONB phải tồn tại + typed hot columns canonical phải đúng.
    - Có test explicit cho danh sách field không được thêm vào typed physical columns (`ema21_above_ema55`, `debug_payload`, `raw_reasoning`).
    - Có test explicit cho TTL=90 ngày và archive-before-prune.
    - RED fail trước khi migration/runtime path được cập nhật.
  </acceptance_criteria>
  <done>Storage policy hybrid được đặc tả rõ bằng test checkable, loại bỏ mâu thuẫn giữa raw JSONB và hot typed pruning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement migration SQL + runtime/backfill compatibility theo hybrid policy</name>
  <files>services/aureus-db-writer/migrations/optimize_trade_signal_snapshots_storage.sql, services/aureus-trader/journal.py, services/aureus-trader/recompute_evaluations.py, services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py</files>
  <read_first>
    - services/aureus-db-writer/migrations/add_trade_evaluations.sql
    - services/aureus-trader/journal.py
    - services/aureus-trader/recompute_evaluations.py
    - RUN_SERVICES.md
  </read_first>
  <action>Implement theo D-01, D-02, D-05, D-06, D-07, D-08 với giá trị cụ thể: (1) migration `optimize_trade_signal_snapshots_storage.sql` tạo/chuẩn hóa bảng hot và bảng `aureus_trade_signal_snapshots_archive`; bắt buộc giữ cột `signal_snapshot JSONB` lưu full raw object + canonical typed hot columns đã khóa ở Task 1; không thêm typed physical columns cho debug/legacy/derived fields. (2) thêm retention policy SQL dùng ngưỡng `interval '90 days'`: archive rows cũ sang bảng archive rồi delete khỏi hot table theo batch `LIMIT 5000`/lần chạy. (3) cập nhật `journal.py` để persist canonical typed fields + full raw `signal_snapshot` JSONB sau `ORDER_OPENED` thành công; map lineage bắt buộc `trade_journal_id`, `trace_id`, `ticket`. (4) cập nhật `recompute_evaluations.py` để tương thích schema mới, vẫn idempotent với `ON CONFLICT (trade_journal_id, signal_schema_version) DO NOTHING`. (5) tạo script `backfill_signal_snapshots_canonical.py` nhận tham số `--start`, `--end`, `--batch-size` (default 1000), đọc snapshot cũ, chuẩn hóa typed canonical fields, archive dữ liệu theo TTL, và giữ lại full raw `signal_snapshot` JSONB; rerun-safe theo unique key hiện có.</action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_signal_snapshot_storage_policy.py services/aureus-db-writer/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py services/aureus-trader/tests/test_signal_snapshot_recompute.py -q"</automated>
  </verify>
  <acceptance_criteria>
    - Migration thể hiện rõ hybrid schema: full raw `signal_snapshot` JSONB + canonical typed hot columns + archive table + TTL 90 ngày.
    - Backfill script chạy lại cùng window không tạo duplicate rows.
    - Runtime persist path chỉ ghi sau ORDER_OPENED, không ghi trước trigger boundary.
    - Recompute/backfill không overwrite version cũ, chỉ append/idempotent theo unique key.
  </acceptance_criteria>
  <done>Schema và pipeline tương thích hybrid storage policy mới, vừa giữ raw JSONB vừa tối ưu typed hot path cho vận hành dài hạn.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy DB runtime verification E2E theo chuẩn WSL/.venv</name>
  <files>services/aureus-trader/tests/test_signal_snapshot_storage_policy.py, services/aureus-trader/tests/test_signal_snapshot_pipeline.py, services/aureus-trader/tests/test_signal_snapshot_recompute.py</files>
  <read_first>
    - RUN_SERVICES.md
    - services/aureus-trader/tests/test_signal_snapshot_storage_policy.py
    - services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py
  </read_first>
  <action>Thực thi verification runtime bắt buộc cho DB thật: (1) chạy subset test signal snapshot/evaluation liên quan bằng `.venv`; (2) chạy backfill dry window `--start 2026-03-01 --end 2026-04-22 --batch-size 1000`; (3) query runtime DB trong container `aureus_timescaledb_dev` để xác nhận bảng/index tồn tại và có row mới nhất; (4) query 5 row mới nhất gồm `trace_id,symbol,timeframe,signal_schema_version,created_at` từ hot table và archive table; (5) query 5 row mới nhất để chứng minh `signal_snapshot` JSONB tồn tại và là object không rỗng ở cả hot/archive path.</action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_signal_snapshot_storage_policy.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py services/aureus-trader/tests/test_signal_snapshot_recompute.py services/aureus-trader/tests/test_evaluation_recompute.py -q && ./.venv/bin/python services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py --start 2026-03-01 --end 2026-04-22 --batch-size 1000 && docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('aureus_trade_signal_snapshots','aureus_trade_signal_snapshots_archive');\" && docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT indexname FROM pg_indexes WHERE schemaname='public' AND tablename IN ('aureus_trade_signal_snapshots','aureus_trade_signal_snapshots_archive') ORDER BY indexname;\" && docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT trace_id,symbol,timeframe,signal_schema_version,created_at FROM aureus_trade_signal_snapshots ORDER BY created_at DESC LIMIT 5;\" && docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT trace_id,symbol,timeframe,signal_schema_version,archived_at FROM aureus_trade_signal_snapshots_archive ORDER BY archived_at DESC LIMIT 5;\" && docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT jsonb_typeof(signal_snapshot) AS snapshot_type, COUNT(*) FROM aureus_trade_signal_snapshots GROUP BY 1;\" && docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT jsonb_typeof(signal_snapshot) AS snapshot_type, COUNT(*) FROM aureus_trade_signal_snapshots_archive GROUP BY 1;\""</automated>
  </verify>
  <acceptance_criteria>
    - Command verify trả exit code 0 và có kết quả cho cả hot + archive tables.
    - Có bằng chứng row mới nhất chứa đầy đủ canonical typed fields để reporting/explainability.
    - Có bằng chứng full raw `signal_snapshot` JSONB vẫn được persist ở hot và archive.
    - Có bằng chứng runtime DB thật, không chỉ unit test/migration file.
  </acceptance_criteria>
  <done>Phase 55 có bằng chứng E2E DB cho hybrid signal snapshot policy: raw JSONB preserved + hot typed columns optimized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trader runtime payload → snapshot persistence | Payload có thể chứa field debug không mong muốn hoặc thiếu lineage bắt buộc |
| hot snapshot table → archive/prune job | Job retention sai có thể xóa nhầm dữ liệu chưa archive |
| recompute/backfill script → production-like DB | Rerun hoặc window sai có thể tạo drift/duplicate dữ liệu |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-19 | T | `journal.py` snapshot insert | mitigate | Whitelist typed canonical fields + giữ full raw `signal_snapshot` JSONB theo hybrid contract; không tạo typed columns cho debug/derived fields |
| T-55-20 | R | retention/archive workflow | mitigate | Archive-before-prune transaction + evidence query hot/archive row counts |
| T-55-21 | D | backfill/recompute rerun | mitigate | Unique lineage key + `ON CONFLICT ... DO NOTHING` + rerun tests |
| T-55-22 | I | explainability/reporting payload | accept | Chấp nhận lưu full raw `signal_snapshot` JSONB trong trust boundary private DB để phục vụ audit/recompute, không expose trực tiếp ra external boundary |
</threat_model>

<verification>
- `wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_signal_snapshot_storage_policy.py services/aureus-db-writer/tests/test_signal_snapshot_migration.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py services/aureus-trader/tests/test_signal_snapshot_recompute.py -q"`
- `wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python services/aureus-trader/scripts/backfill_signal_snapshots_canonical.py --start 2026-03-01 --end 2026-04-22 --batch-size 1000"`
- `wsl -d Aureus -e bash -lc "docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT COUNT(*), MAX(created_at) FROM aureus_trade_signal_snapshots;\""`
- `wsl -d Aureus -e bash -lc "docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT COUNT(*), MAX(archived_at) FROM aureus_trade_signal_snapshots_archive;\""`
- `wsl -d Aureus -e bash -lc "docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT jsonb_typeof(signal_snapshot), COUNT(*) FROM aureus_trade_signal_snapshots GROUP BY 1;\""`
- `wsl -d Aureus -e bash -lc "docker exec -i aureus_timescaledb_dev psql -U aureus -d aureus -c \"SELECT jsonb_typeof(signal_snapshot), COUNT(*) FROM aureus_trade_signal_snapshots_archive GROUP BY 1;\""`
</verification>

<success_criteria>
- `aureus_trade_signal_snapshots` tuân thủ hybrid contract SIGNAL-SNAPSHOT-01: giữ full raw `signal_snapshot` JSONB + canonical typed hot columns.
- Không có typed physical columns cho debug/legacy/derived fields (`ema21_above_ema55`, `debug_payload`, `raw_reasoning`).
- Có retention TTL 90 ngày + archive policy chạy được và có bằng chứng dữ liệu archive thật.
- Backfill/recompute tương thích schema mới, idempotent rerun, không mất lịch sử version.
- Test + runtime E2E DB verification đều pass theo chuẩn WSL + `.venv`.
</success_criteria>

<output>
After completion, create `.planning/phases/55-evaluation-data-model-pipeline/55-06-SUMMARY.md`
</output>
