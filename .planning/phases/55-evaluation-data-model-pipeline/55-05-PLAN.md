---
phase: 55-evaluation-data-model-pipeline
plan: 05
type: execute
wave: 5
depends_on:
  - 55-01
  - 55-02
  - 55-03
  - 55-04
files_modified:
  - services/aureus-db-writer/migrations/add_trade_evaluations.sql
  - services/aureus-trader/recompute_evaluations.py
  - services/aureus-trader/journal.py
  - services/aureus-db-writer/tests/test_evaluation_migration.py
  - services/aureus-trader/tests/test_signal_snapshot_migration.py
  - services/aureus-trader/tests/test_evaluation_recompute.py
  - services/aureus-trader/tests/test_signal_snapshot_recompute.py
  - RUN_SERVICES.md
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - SIGNAL-SNAPSHOT-01
  - EVAL-RUNTIME-01
  - EVAL-RUNTIME-02
  - EVAL-RUNTIME-03
  - EVAL-RUNTIME-04
must_haves:
  truths:
    - "Không chấp nhận false-positive phase complete khi runtime DB chưa sẵn sàng."
    - "Validation JSONB phải tương thích PostgreSQL runtime hiện tại, không dùng expression gây lỗi apply migration."
    - "Recompute phải lấy timeframe từ lineage schema chuẩn; fallback JSON chỉ là cơ chế tạm và có deadline loại bỏ."
  artifacts:
    - path: "services/aureus-db-writer/migrations/add_trade_evaluations.sql"
      provides: "Compatibility-safe constraints cho JSONB shape"
    - path: "services/aureus-trader/recompute_evaluations.py"
      provides: "Recompute query không phụ thuộc cột không tồn tại"
    - path: "services/aureus-trader/journal.py"
      provides: "Nguồn timeframe chuẩn trong lifecycle persist"
---

<objective>
Khóa chặt runtime parity cho Phase 55 bằng cách xử lý triệt để 2 vấn đề kiến trúc:
(1) migration JSONB constraints không tương thích runtime PG,
(2) recompute phụ thuộc timeframe thiếu trong journal lineage.

Output: migration + recompute + tests + runbook đạt chuẩn runtime evidence (schema + data).
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/55-evaluation-data-model-pipeline/55-04-PLAN.md
@services/aureus-db-writer/migrations/add_trade_evaluations.sql
@services/aureus-trader/recompute_evaluations.py
@services/aureus-trader/journal.py
@RUN_SERVICES.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chuẩn hóa JSONB constraints theo runtime compatibility</name>
  <files>services/aureus-db-writer/migrations/add_trade_evaluations.sql, services/aureus-db-writer/tests/test_evaluation_migration.py, services/aureus-trader/tests/test_signal_snapshot_migration.py</files>
  <action>
    - Chốt một biểu thức CHECK tương thích runtime PG hiện tại cho non-empty JSON object.
    - Cập nhật migration tests để assert đúng expression final (tránh assert vào function không tồn tại trong runtime).
    - Verify migration apply clean trên DB dev từ trạng thái chưa có bảng.
  </action>
  <verify>
    <automated>python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py -q</automated>
    <manual>Apply migration vào aureus_timescaledb_dev và kiểm tra bảng/index tồn tại.</manual>
  </verify>
  <acceptance_criteria>
    - Migration apply thành công trên runtime DB hiện tại.
    - Constraints không còn gây lỗi function/subquery unsupported.
  </acceptance_criteria>
  <done>Migration constraints tương thích runtime DB và được chứng minh bằng test + apply thành công trên DB dev.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ổn định recompute timeframe lineage (bridge approach)</name>
  <files>services/aureus-trader/recompute_evaluations.py, services/aureus-trader/tests/test_evaluation_recompute.py, services/aureus-trader/tests/test_signal_snapshot_recompute.py</files>
  <action>
    - Sửa query recompute để không tham chiếu cột không tồn tại trong aureus_trade_journal.
    - Bridge tạm thời: dùng nguồn timeframe khả dụng an toàn và deterministic cho runtime hiện tại.
    - Gắn TODO migration lineage để loại bỏ fallback JSON ở iteration tiếp theo trong cùng phase.
  </action>
  <verify>
    <automated>python3 -m pytest services/aureus-trader/tests/test_evaluation_recompute.py services/aureus-trader/tests/test_signal_snapshot_recompute.py -q</automated>
    <manual>Chạy recompute thực tế với window ngắn và xác nhận không lỗi UndefinedColumn.</manual>
  </verify>
  <acceptance_criteria>
    - Recompute chạy thành công trên DB dev runtime.
    - Có row mới ở cả evaluations và signal snapshots.
  </acceptance_criteria>
  <done>Recompute runtime ổn định, không lỗi cột thiếu và vẫn giữ hành vi append/idempotent theo contract phase 55.</done>
</task>

<task type="auto">
  <name>Task 3: Runtime sign-off gate bắt buộc trước mark complete</name>
  <files>RUN_SERVICES.md, services/aureus-trader/journal.py</files>
  <action>
    - Bổ sung checklist cứng: schema existence + index existence + latest row evidence.
    - Đảm bảo pipeline persist sau ORDER_OPENED ghi được data thật cho trace_id mới.
  </action>
  <verify>
    <automated>wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus && ./.venv/bin/python -m pytest services/aureus-trader/tests/test_evaluation_pipeline.py services/aureus-trader/tests/test_signal_snapshot_pipeline.py -q"</automated>
    <manual>
      - SELECT tablename ... ('aureus_trade_evaluations','aureus_trade_signal_snapshots')
      - SELECT indexname FROM pg_indexes ...
      - SELECT COUNT(*), MAX(evaluated_at) ...
      - SELECT COUNT(*), MAX(created_at) ...
      - SELECT top 5 rows (trace_id/symbol/timeframe/version)
    </manual>
  </verify>
  <acceptance_criteria>
    - Không còn trạng thái “test pass nhưng runtime rỗng schema/data”.
    - Có evidence SQL đầy đủ trong summary artifact.
  </acceptance_criteria>
  <done>Sign-off phase 55 chỉ được chấp nhận khi có đầy đủ chứng cứ runtime schema/index/data theo checklist bắt buộc.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
|-----------|----------|-----------|------------|
| T-55-16 | D | Migration runtime mismatch | Compatibility-safe CHECK + apply verify trên DB thật |
| T-55-17 | T | Timeframe semantic drift | Bridge deterministic + task chuẩn hóa lineage tiếp theo |
| T-55-18 | R | False completion evidence | Runtime sign-off gate bắt buộc với SQL proof |
</threat_model>

<verification>
- `python3 -m pytest services/aureus-db-writer/tests/test_evaluation_migration.py services/aureus-trader/tests/test_signal_snapshot_migration.py -q`
- `python3 -m pytest services/aureus-trader/tests/test_evaluation_recompute.py services/aureus-trader/tests/test_signal_snapshot_recompute.py -q`
- Runtime DB evidence SQL (schema + indexes + latest rows)
</verification>

<success_criteria>
- Migration phase 55 apply được trên runtime DB dev không lỗi compatibility.
- Recompute phase 55 chạy thực tế và persist dữ liệu vào 2 bảng mục tiêu.
- Sign-off phase 55 có bằng chứng runtime đầy đủ, không chỉ test output.
</success_criteria>

<output>
After completion, create `.planning/phases/55-evaluation-data-model-pipeline/55-05-SUMMARY.md`
</output>
