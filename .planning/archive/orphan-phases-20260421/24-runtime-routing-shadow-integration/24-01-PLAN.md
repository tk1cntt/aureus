---
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/live_engine.py
  - services/aureus-signal/tests/test_live_engine_shadow_mode.py
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
---

# Phase 24: Runtime Routing & Shadow Integration

<objective>
Implement dynamic provider mode routing (`redis_primary`, `ta_shadow`, `ta_primary`) within the `live_engine.py` and ensure TradingAgents shadow mode evaluates asynchronously without blocking the primary candle pipeline.
</objective>

<task>
<read_first>
- `services/aureus-signal/engine/live_engine.py`
- `d:\Aureus\.planning\phases\24-runtime-routing-shadow-integration\24-CONTEXT.md`
- `d:\Aureus\.planning\phases\24-runtime-routing-shadow-integration\24-RESEARCH.md`
</read_first>
<action>
1. **Dynamic Config**: In `services/aureus-signal/engine/live_engine.py`, instantiate `tradingagents_provider = TradingAgentsProvider(endpoint=os.getenv("TA_ENDPOINT", "http://aureus-tradingagents:8000"))` (or similar) prior to the main loop if not already present. Remember the TradingAgents adapter handles its own HTTP connection pooling.
2. **Feature Flag Check**: Just before the AI evaluation blocks in the inner `execute_signals_for_candle` or `brain_worker`, read `provider_mode = await flags.get("provider_mode", "redis_primary")`.
3. **Execution Routing**: 
   - If `redis_primary`: proceed as normal with Redis OHLCV. (This fulfills default behavior requirement).
   - If `ta_primary`: Wait for `get_decision` from TradingAgentsProvider synchronously and inject the signals.
   - If `ta_shadow`: Proceed as normal with Redis OHLCV, but spawn an async background task (`asyncio.create_task(shadow_execute_pulse(...))`) that calls TradeAgents, computes the decision without blocking, and records the answer to the Redis key `aureus:ai:shadow:{symbol}`.
4. **Metrics**: Add logging such as `logger.info(f"[{symbol}] [SHADOW] TradingAgents decision computed in background...")` when shadow pulse finishes.
</action>
<acceptance_criteria>
- `services/aureus-signal/engine/live_engine.py` contains `provider_mode = await flags.get("provider_mode"` or similar routing logic.
- `services/aureus-signal/engine/live_engine.py` contains background task wrapper (e.g., `asyncio.create_task`) for the shadow mode execution.
</acceptance_criteria>
</task>

<task>
<read_first>
- `services/aureus-signal/tests/test_live_engine_shadow_mode.py` (NEW)
- `services/aureus-signal/engine/live_engine.py`
</read_first>
<action>
1. Create `services/aureus-signal/tests/test_live_engine_shadow_mode.py`.
2. Mock the `aureus:config:provider_mode` Redis key as `ta_shadow`.
3. Assert that when the engine runs one tick, it does not block on a mocked `TradingAgentsProvider.get_decision()` that sleeps for 2 seconds.
4. Verify that `aureus:ai:shadow:{symbol}` receives the mocked output eventually.
</action>
<acceptance_criteria>
- `services/aureus-signal/tests/test_live_engine_shadow_mode.py` exists.
- `pytest services/aureus-signal/tests/test_live_engine_shadow_mode.py` exits 0.
</acceptance_criteria>
</task>

<verification>
### Goal Backward Verification
- **ROUT-01**: Supported modes covered via `provider_mode`.
- **ROUT-02**: Handled by background task offloading for `ta_shadow`, persisting to `aureus:ai:shadow:{symbol}`.

### Run tests
```bash
pytest services/aureus-signal/tests/test_live_engine_shadow_mode.py -v
```
</verification>
