---
wave: 1
depends_on: []
files_modified: [
  "services/aureus-signal/engine/providers/tradingagents.py",
  "services/aureus-signal/tests/test_tradingagents_adapter.py"
]
autonomous: true
requirements: [ADPT-01, ADPT-02, ADPT-03, ADPT-04]
---

# Plan 01: TradingAgents Adapter Implementation

## Objective
Implement TradingAgents decision provider adapter with parsing, config mapping, TTL caching, and safe fallback.

## must_haves
- Mapped symbol strings
- TTL in-memory cache
- Exception catching to return `None`
- Implement `DecisionProvider` interface

## Tasks

### [Task 1] Scaffold Test Suite
<read_first>
- `services/aureus-signal/engine/providers/base.py`
</read_first>
<acceptance_criteria>
- `services/aureus-signal/tests/test_tradingagents_adapter.py` is present and contains tests for mapping, caching and fallback logic.
</acceptance_criteria>
<action>
Create `services/aureus-signal/tests/test_tradingagents_adapter.py` with unit tests covering initial setup, caching logic (via time mock), symbol mapping load, and exception safety (ensuring `None` is returned on simulated failure).
</action>

### [Task 2] Implement Provider Adapter
<read_first>
- `services/aureus-signal/engine/providers/base.py`
- `services/aureus-signal/symbols.json`
</read_first>
<acceptance_criteria>
- `services/aureus-signal/engine/providers/tradingagents.py` is created.
- Defines `TradingAgentsProvider` inheriting from `DecisionProvider`.
- It implements `get_decision` returning `DecisionSignal` or `None`.
- Catches network/timeout exceptions, returning `None`.
</acceptance_criteria>
<action>
Create `TradingAgentsProvider` in `services/aureus-signal/engine/providers/tradingagents.py`. 
Add `setup()` to load symbols mapping from `services/aureus-signal/symbols.json`.
Implement an in-memory TTL dictionary (default 15s) inside `get_decision` to prevent excess polling.
Wrap outbound API calls inside `try/except` capturing standard exceptions like connection/timeouts to return `None` gracefully.
</action>
