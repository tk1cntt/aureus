---
phase: 08
plan: 01
title: Signal Volume SMA Optimization and Validation Gates
wave: 1
depends_on: []
autonomous: true
requirements: [SIG-08, TST-01, TST-02, TST-03, TST-04, VAL-01, VAL-02]
files_modified:
  - services/aureus-signal/engine/signals/volume_sma.py
  - services/aureus-signal/tests/test_volume_sma_o1.py
  - services/aureus-signal/tests/test_volume_sma_integration_execute_signals_for_candle.py
  - services/aureus-signal/tests/test_volume_sma_integration_live_engine.py
  - .planning/phases/08-signal-volume-sma/08-VALIDATION.md
---

# Phase 08 Plan 01 — Signal Volume SMA Optimization and Verification Gates

## Objective
Tối ưu hóa tín hiệu `volume_sma`, đổi cơ chế ghi State sang Dynamic properties, áp dụng bộ lọc Spike Threshold 1.5x (chống spam) và xử lý Zero Imputation cho NaN Volume. Đóng gói đầy đủ Regression Gates (Unit + Integration + Live Engine).

<tasks>
  <task id="08-01-01" type="auto">
    <name>Harden volume_sma.py logic by adding spike_threshold and defensive zero-imputations</name>
    <read_first>
      - services/aureus-signal/engine/signals/volume_sma.py
      - .planning/phases/08-signal-volume-sma/08-CONTEXT.md
    </read_first>
    <action>
      Update `volume_sma.py` `__init__` to accept `spike_threshold: float = 1.5`. Modify state injection to use `setattr(state_obj, f"vol_sma_{self.period}", sma_val)`. Handle missing volume by using `fillna(0)` on `recent_vol`. Return metadata ONLY if `current_vol > sma_val * self.spike_threshold`.
    </action>
    <acceptance_criteria>
      - `volume_sma.py` contains `setattr(state_obj, f"vol_sma_{self.period}", sma_val)`.
      - `volume_sma.py` contains `fillna(0)` before calculating mean.
      - Return payload is exact: `{"tag": f"vol_sma_{self.period}", "value": round(sma_val, 2), "current_vol": ...}` but only emitted if condition clears.
    </acceptance_criteria>
  </task>

  <task id="08-01-02" type="auto">
    <name>Add volume_sma unit regression coverage</name>
    <read_first>
      - services/aureus-signal/engine/signals/volume_sma.py
    </read_first>
    <action>
      Create `test_volume_sma_o1.py` with mock pandas dataframes to assert default initialization behaviors, the zero-imputation logic for missing records, spike threshold bypass controls (ensuring empty return when `curr < sma * 1.5`), and correct dynamic attribute mutations on the base State object.
    </action>
    <acceptance_criteria>
      - `test_volume_sma_o1.py` exists with tests verifying `fillna(0)`.
      - Test assertions prove the Signal returns None on non-spike volumes while state object gets correctly modified inside memory.
    </acceptance_criteria>
  </task>

  <task id="08-01-03" type="auto">
    <name>Add execute-signals integration coverage for Volume SMA</name>
    <read_first>
      - services/aureus-signal/tests/test_pivots_integration_execute_signals_for_candle.py
      - services/aureus-signal/engine/signal_factory.py
    </read_first>
    <action>
      Create `test_volume_sma_integration_execute_signals_for_candle.py` relying on existing test factory paradigms to run 30+ candles. Check that `signal_history` output contract tracks Spike emits accurately but suppresses non-threshold noise while the base object keeps track of standard SMAs.
    </action>
    <acceptance_criteria>
      - `test_volume_sma_integration_execute_signals_for_candle.py` checks for `execute_signals_for_candle` response pipeline array.
      - Pytest must be able to assert valid dictionary fields `tag` and `current_vol` in output.
    </acceptance_criteria>
  </task>

  <task id="08-01-04" type="auto">
    <name>Add live_engine runtime coverage and full phase valuation</name>
    <read_first>
      - services/aureus-signal/tests/test_atr_integration_live_engine.py
      - .planning/phases/08-signal-volume-sma/08-VALIDATION.md
    </read_first>
    <action>
      Create `test_volume_sma_integration_live_engine.py` simulating full engine async Redis pubsub behavior verifying state_payload injection. Run all tests and update `08-VALIDATION.md` coverage numbers > 80%.
    </action>
    <acceptance_criteria>
      - `test_volume_sma_integration_live_engine.py` runs green.
      - `08-VALIDATION.md` has table reflecting run stats and `cov_rate`.
    </acceptance_criteria>
  </task>
</tasks>

## Verification
- `python -m pytest services/aureus-signal/tests/test_volume_sma_o1.py -q`
- `python -m pytest services/aureus-signal/tests/test_volume_sma_integration_execute_signals_for_candle.py -q`
- `python -m pytest services/aureus-signal/tests/test_volume_sma_integration_live_engine.py -q`
- `python -m pytest services/aureus-signal/tests/test_volume_sma_o1.py services/aureus-signal/tests/test_volume_sma_integration_execute_signals_for_candle.py services/aureus-signal/tests/test_volume_sma_integration_live_engine.py --cov=services/aureus-signal/engine/signals/volume_sma.py --cov-report=term-missing`

## Success Criteria
- Tín hiệu Volume SMA không xả rác sự kiện, hoạt động đúng threshold spikes.
- Tests (Unit, Validation, Runtime, Factory) coverage trên 80%.
- Không thay đổi scope hay dependency engine hiện tại.
