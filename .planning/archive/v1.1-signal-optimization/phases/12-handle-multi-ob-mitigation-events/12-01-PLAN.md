---
wave: 1
depends_on: []
files_modified: ["d:\\Aureus\\services\\aureus-signal\\engine\\signals\\structure.py", "d:\\Aureus\\services\\aureus-signal\\tests\\test_structure_integration_execute_signals_for_candle.py", "d:\\Aureus\\services\\aureus-signal\\unittest\\test_event_filter.py"]
autonomous: false
---

# 12-01: Enforce Deterministic Last-Event-Wins for OB Mitigation

<objective>
Chuẩn hóa rõ ràng semantics `last-event-wins` cho `ob_bull_mitigated` / `ob_bear_mitigated` trong cùng candle, giữ backward compatibility contract hiện tại và bổ sung test chứng minh hành vi deterministic.
</objective>

<tasks>

<task>
<description>
Refactor `_verify_mitigations` để biểu đạt rõ deterministic `last-event-wins` mà không đổi schema event.
</description>
<read_first>
- `d:\Aureus\services\aureus-signal\engine\signals\structure.py` (hàm `_verify_mitigations` và nơi set `transient_signals`)
- `d:\Aureus\services\aureus-signal\engine\state.py` (dedup semantics của `request_ai_update`)
- `d:\Aureus\.planning\archive\v1.1-signal-optimization\phases\12-handle-multi-ob-mitigation-events\12-CONTEXT.md` (locked decision D-01..D-07)
</read_first>
<action>
Sửa hàm `_verify_mitigations` trong `structure.py` theo hướng explicit semantics:
1. Khởi tạo collector nội bộ cho event cuối cùng mỗi phía trong tick hiện tại:
   - `latest_bull_mitigation = None`
   - `latest_bear_mitigation = None`
2. Khi tìm thấy mitigation với `c_t == latest_t`, gán collector tương ứng về chính `ob` vừa chạm.
3. Sau khi sweep xong toàn bộ OB, chỉ ghi vào `state_obj.transient_signals` bằng 2 key legacy hiện có:
   - `state_obj.transient_signals['ob_bull_mitigated'] = latest_bull_mitigation` nếu collector khác `None`
   - `state_obj.transient_signals['ob_bear_mitigated'] = latest_bear_mitigation` nếu collector khác `None`
4. KHÔNG tạo key mới kiểu list aggregate (`ob_*_mitigated_events`).
5. Giữ nguyên trigger `request_ai_update("OB_INTERACTION")` khi mitigation vừa xảy ra tại `latest_t`.
</action>
<acceptance_criteria>
- `grep -q "latest_bull_mitigation" d:\Aureus\services\aureus-signal\engine\signals\structure.py` exits 0.
- `grep -q "latest_bear_mitigation" d:\Aureus\services\aureus-signal\engine\signals\structure.py` exits 0.
- `grep -q "ob_bull_mitigated_events" d:\Aureus\services\aureus-signal\engine\signals\structure.py` exits 1.
- `grep -q "ob_bear_mitigated_events" d:\Aureus\services\aureus-signal\engine\signals\structure.py` exits 1.
</acceptance_criteria>
</task>

<task>
<description>
Bổ sung test để khóa hành vi last-event-wins và đảm bảo event filter vẫn pass với legacy keys.
</description>
<read_first>
- `d:\Aureus\services\aureus-signal\tests\test_structure_integration_execute_signals_for_candle.py`
- `d:\Aureus\services\aureus-signal\unittest\test_event_filter.py`
- `d:\Aureus\services\aureus-signal\engine\event_filter.py`
</read_first>
<action>
Thêm test cases:
1. Trong `test_structure_integration_execute_signals_for_candle.py`, thêm 1 integration test tạo tình huống nhiều OB mitigation cùng tick và assert:
   - chỉ có key legacy `ob_bull_mitigated`/`ob_bear_mitigated`;
   - key lưu payload của event cuối cùng đúng theo thứ tự duyệt OB.
2. Trong `test_event_filter.py`, thêm assertion rằng `has_structural_event(...)` vẫn trả `True` khi chỉ có `ob_bull_mitigated` hoặc `ob_bear_mitigated`.
3. Không thêm assertion/tag cho aggregate keys mới.
</action>
<acceptance_criteria>
- `pytest tests/test_structure_integration_execute_signals_for_candle.py -q` exits 0.
- `pytest unittest/test_event_filter.py -q` exits 0.
- `pytest tests/test_sweep_integration_execute_signals_for_candle.py tests/test_structure_integration_execute_signals_for_candle.py -q` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
## Verification
1. Chạy full test commands trong acceptance criteria.
2. Dùng grep để xác nhận không xuất hiện bất kỳ key aggregate `ob_*_mitigated_events` trong code đã sửa.
3. Review thủ công `transient_signals` emission path để đảm bảo downstream (`event_filter`, snapshot) vẫn dùng contract legacy.
</verification>
