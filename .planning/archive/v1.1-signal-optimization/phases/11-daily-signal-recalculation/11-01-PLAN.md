---
wave: 1
depends_on: []
files_modified: ["d:\Aureus\services\aureus-signal\engine\live_engine.py"]
autonomous: false
---

# 11-01: Implement Daily Signal Recalculation (Garbage Collection)

<objective>
Thêm cơ chế rà soát thời gian thực vào vòng lặp Live Engine để tự động gọi `recalculate_all_signals` xử lý Garbage Collection cho 1500 nến M1 vào đúng 0:00 UTC (5h sáng GMT+7).
</objective>

<tasks>

<task>
<description>
Bổ sung `Daily Reset Cron Task` vào `live_engine.py` để clear memory.
</description>
<read_first>
- `d:\Aureus\services\aureus-signal\engine\live_engine.py` (Live Engine loop hiện tại)
- `d:\Aureus\.planning\phases\11-daily-signal-recalculation\11-CONTEXT.md`
</read_first>
<action>
Mở `live_engine.py`.
Trong class `LiveEngine`, tạo một hàm async mới tên là `_daily_gc_loop(self)`.
Hàm này sử dụng `while self.active:` kết hợp `await asyncio.sleep(60)` để mỗi phút check 1 lần giờ hệ thống (`datetime.datetime.now(datetime.timezone.utc)`).
Nếu giờ là `0` và phút là `0` (hoặc `0 <= minute <= 1`) và chưa reset trong ngày hôm nay:
- Ghi log: `logger.info("Triggering Daily Signal Recalculation (1500 candles GC)")`
- Chạy vòng lặp duyệt qua tất cả `symbol` đang có trong `self.window_manager.states`:
  - Phát event nội bộ hoặc trực tiếp gọi hàm `self.command_processor.start_recalculation(symbol)` (hoặc hàm tương tự dùng để recalculate tín hiệu). *Lưu ý: Đọc file `live_engine.py` để dùng đúng hàm mà hệ thống hiện đang dùng để Recalculate 1500 candles khi vừa khởi động bot*.
- Cập nhật cờ `last_gc_date = now.date()`.

Sau đó sửa hàm `start(self)` hoặc `run(self)` của `LiveEngine` để tạo Task chạy ngầm hàm `_daily_gc_loop` này cùng với các tiến trình stream dữ liệu.
</action>
<acceptance_criteria>
`grep -q "_daily_gc_loop" d:\Aureus\services\aureus-signal\engine\live_engine.py && grep -q "hour == 0 and" d:\Aureus\services\aureus-signal\engine\live_engine.py` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
## Verification
1. Viết Unit test hoặc Manual mock system time để xác nhận `_daily_gc_loop` kích hoạt đúng 0:00 UTC.
2. Xác nhận hàm recalculate được gọi và không gây block thread asyncio chính của WebSocket/Redis Stream.
</verification>
