# Phase 15.16: choch-strategy-mismatch - Context

**Gathered:** 2026-04-01
**Status:** Ready for planning

<domain>
## Phase Boundary

Phân tích data `data-test.json` xem có gì bất thường không. Với dữ liệu có rất nhiều event CHOCH nhưng tại sao không thấy strategy nào được kích hoạt. Dùng data để làm đầu vào backtest và kiểm tra lại các strategy hoạt động.

</domain>

<decisions>
## Implementation Decisions

### Phạm vi "bất thường" cần ưu tiên
- **D-01:** So sánh `CHOCH event count` vs `strategy trigger count` theo từng strategy VÀ theo từng symbol/timeframe/session để khoanh vùng nguyên nhân do cấu hình strategy chung, thiếu hụt hay bị chặn bởi filter.

### Nguồn backtest cho phase này
- **D-02:** Sử dụng trực tiếp `data-test.json` replayed thông qua config của `seed_strategies`.
- **D-03:** Bắt buộc áp dụng 1 Control Benchmark Test (tắt hoàn toàn gating/sequence filters) để đánh giá xem nguyên nhân không kích hoạt là do bị loại nến bởi filter hay lỗi ở parser đầu lọt.

### Mức độ điều tra nguyên nhân không kích hoạt
- **D-04:** Audit toàn diện từ luồng Data → Strategy Gating Path (`context_filters`/sequence) → Mapping Tag Normalization (`event_policy` ↔ strategy tags) để bù đắp rủi ro tag lệch (drift) ngay sau Phase 15.15.

### Đầu ra mong muốn
- **D-05:** Báo cáo chẩn đoán sự cố với số liệu chứng minh lượng CHOCH thất thoát.
- **D-06:** Patch code vá lỗi và cung cấp test case khẳng định strategy có thể kích hoạt thành công (Triggers > 0) trên base `data-test.json`.
</decisions>

<canonical_refs>
## Canonical References

### Data & Tagging Baseline
- `services/aureus-signal/engine/event_policy.py` — Chuẩn hóa trigger mapping tags. Hệ thống strategy filter phải khớp hoàn toàn với tags trả ra ở đây.
- `services/aureus-signal/engine/strategies/seed_strategies.py` — Chứa conditions và setup context filters của strategy.
- `data-test.json` — Dataset nguồn chứa raw target event để replay.

</canonical_refs>

<specifics>
## Specific Ideas

Yêu cầu mọi debug action và output đều mang căn cứ định lượng (vd: 100 CHOCH event sinh ra -> mong đợi ít nhất > 0 Trade Intents, tại sao tỷ lệ này đang là 0%). 
Không sửa code dựa trên cảm tính.

</specifics>

<code_context>
## Existing Code Insights

### Established Patterns
- Sequence matching phụ thuộc vào các `trigger_conditions` list tags (ví dụ `["bullish_choch", ...]`). Chỉ một sai lệch tag nhỏ giữa `transient_signals` -> `event_policy` -> `strategy config` cũng có thể âm thầm bỏ qua trigger.
- Mọi logic sinh intent trong framework Version 1.2 đều phải pass qua `TemplateStrategy.evaluate_sequence` và `context_filters`.
</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed completely focused within debugging the trigger pipeline.
</deferred>

---

*Phase: 15.16-choch-strategy-mismatch*
*Context gathered: 2026-04-01*
