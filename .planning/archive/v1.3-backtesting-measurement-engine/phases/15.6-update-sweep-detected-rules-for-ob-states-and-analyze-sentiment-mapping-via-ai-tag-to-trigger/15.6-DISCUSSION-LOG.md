# Phase 15.6: SWEEP Rules + Sentiment Mapping - Discussion Log

> **Audit trail only.** Do not use as input to planning/execution agents.
> Final scope and decisions are captured in `15.6-CONTEXT.md`.

**Date:** 2026-03-24
**Phase:** 15.6-update-sweep-detected-rules-for-ob-states-and-analyze-sentiment-mapping-via-ai-tag-to-trigger
**Areas discussed:** Sweep status-tag policy, runtime warning analysis, sentiment mapping interpretation, logging hot-reload, mitigation-age gate correction

---

## 1) Sweep detected rule by OB state

| Option | Description | Selected |
|--------|-------------|----------|
| Keep old sweep tagging | No state-aware distinction | |
| State-aware sweep tag generation | Reflect OB lifecycle in emitted sweep status tags | ✓ |

**Outcome:** Chọn state-aware rule và đã cập nhật code/test cho `SweepSignal`.

---

## 2) `missing origin_timestamp` warning

| Option | Description | Selected |
|--------|-------------|----------|
| Ignore warning | Chấp nhận noisy log | |
| Analyze call-path and identify root-cause | Trace live path and document exact failure condition | ✓ |

**Outcome:** Đã truy vết live path và chốt root-cause liên quan timestamp có thể tồn tại key nhưng giá trị `None`.

---

## 3) Sentiment mapping via `_AI_TAG_TO_TRIGGER`

| Option | Description | Selected |
|--------|-------------|----------|
| Add policy override sentiment | Force sentiment from trigger mix | |
| Keep AI sentiment output as-is | Treat sentiment as model prediction result | ✓ |

**User decision:** "Nếu đây là kết quả dự đoán từ AI thì không cần sửa"

**Outcome:** Không thay đổi logic sentiment runtime.

---

## 4) Logging settings hot-reload

| Option | Description | Selected |
|--------|-------------|----------|
| Keep restart-required logging config | Manual restart after log config edits | |
| Add runtime hot-reload | Auto-apply log setting updates without restart | ✓ |

**Outcome:** Đã hoàn tất triển khai + kiểm chứng cho hot-reload flow.

---

## 5) Mitigation age gate (user-confirmed correction)

| Option | Description | Selected |
|--------|-------------|----------|
| Gate only non-TOUCHED mitigated states | `if mitigated and current_status != "TOUCHED"` | |
| Gate all mitigated states with strict window | `if mitigated` và chỉ cho emit khi `0 < age <= 300s` | ✓ |

**Outcome:** User đã sửa trực tiếp `sweep.py`:
- Gate áp dụng cho mọi status mitigated.
- `continue` đã bật lại khi age ngoài khoảng hợp lệ.
- Log đã giữ `ob_idx` để trace thứ tự OB trigger.

---

## 6) Naming cleanup for sweep lifecycle + trigger tags (latest user patch)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep legacy naming (`DEAD`, alias tags) | Retain old state/tag compatibility layer | |
| Standardize naming (`CLEAN_BREAKOUT`, canonical tags) | Align state machine + event filter/policy on new labels | ✓ |

**Outcome:**
- `_update_ob_states` đổi terminal state từ `DEAD` -> `CLEAN_BREAKOUT`.
- `event_filter.STRUCTURAL_TAGS` đã bỏ alias cũ (`sweep_swept_*`, `sweep_dead_*`, ...).
- `event_policy._AI_TAG_TO_TRIGGER` đã bỏ map `sweep_touched_*`.
- Retest cần sửa 1 assertion unit cũ (`sweep_swept_bull`) sang `sweep_touched_bull`; sau đó toàn bộ test mục tiêu pass.

---

## 7) Canonical status-tag regression after git-diff audit

| Option | Description | Selected |
|--------|-------------|----------|
| Keep dynamic `status.lower()+suffix` tagging | Risk emitting non-canonical tags like `touched_bull` | |
| Use explicit status-to-tag mapping | Enforce canonical sweep lifecycle tags per state | ✓ |

**Outcome:**
- Regression identified in `SweepSignal.calculate`: `TOUCHED` emitted `touched_bull` instead of `sweep_touched_bull`.
- Patched to explicit mapping for `TOUCHED`, `SWEEP`, `BROKEN_PENDING`, `STOP_HUNT`, `CLEAN_BREAKOUT`.
- Full affected retest set passed in Docker (`test_sweep_o1`, `test_event_filter`, two sweep integrations).

---

## Deferred

- Replay scenario nhiều OB active để xác nhận giảm lặp signal thực tế.
