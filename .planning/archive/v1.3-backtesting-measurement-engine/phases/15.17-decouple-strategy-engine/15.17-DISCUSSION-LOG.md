# Phase 15.17: decouple-strategy-engine - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the analysis.

**Date:** 2026-04-01T21:35:34+07:00
**Phase:** 15.17-decouple-strategy-engine
**Mode:** direct-input

## Explicit Decisions Provided by User

User bypassed the interactive questions and mapped out the exact solution topology:

- **Khối 1: aureus-signal-aggregator**
  - Đọc stream giá, tính toán Indicators/Structure.
  - Vẫn giữ `aureus:state` và các thông tin state khác, ghi đè file JSON lên Redis.
  - Quản lý việc lưu Snapshot xuống TimescaleDB & GC tracking_vars.
  - Xuất ra Stream chứa signal riêng cho mỗi symbol (`aureus:stream:XAUUSD:signals`).
- **Khối 2: aureus-strategy-executor**
  - Lắng nghe stream tín hiệu, chạy bất đồng bộ. Tách riêng logic `evaluate_all` (khoảng từ dòng 692 trở xuống).
  - **Strategy Evaluation**: Quét `TemplateStrategy` vs `log_signal_normalize`.
  - **Observability**: Đóng gói version metadata, snapshot ra quyết định, và publish orders_rejected (cho observability).
  - **Trade & AI**: Chạy `trade_manager.update_orders`, `process_triggers`, đẩy sang `ai_queue` nếu cần AI approval.

No additional assumptions were needed. Added constraints per user follow-up detailing exact functional assignments.
