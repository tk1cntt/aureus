---
phase: 05
plan: 01
title: Signal session optimization and verification gates
wave: 1
depends_on: []
autonomous: true
requirements: [SIG-05, TST-01, TST-02, TST-03, TST-04, VAL-01, VAL-02]
files_modified:
  - services/aureus-signal/engine/signals/session.py
  - services/aureus-signal/tests/test_session_o1.py
  - services/aureus-signal/tests/test_session_integration_execute_signals_for_candle.py
  - services/aureus-signal/tests/test_session_integration_live_engine.py
  - .planning/phases/05-signal-session-optimization/05-VALIDATION.md
---

# Phase 05 Plan 01 — Signal Session Optimization and Verification Gates

## Objective
Harden session signal correctness and determinism while preserving downstream contracts and trading intent, then close all phase quality gates (unit + integration + coverage >= 80%).

<tasks>
  <task id="05-01-01" type="auto">
    <name>Harden session signal internals without changing runtime contracts</name>
    <read_first>
      - services/aureus-signal/engine/signals/session.py
      - services/aureus-signal/engine/state.py
      - .planning/phases/05-signal-session-optimization/05-CONTEXT.md
    </read_first>
    <action>
      Add defensive guards and deterministic boundary handling in `session.py` for malformed inputs and session window transitions. Preserve contract outputs (`tag = market_session`), `current_session` updates, and `tracking_vars['session_hlo']` compatibility.
    </action>
    <acceptance_criteria>
      - Session tag contract remains `market_session`.
      - `state_obj.current_session` update semantics remain backward-compatible.
      - `tracking_vars['session_hlo']` structure remains compatible.
    </acceptance_criteria>
  </task>

  <task id="05-01-02" type="auto">
    <name>Add dedicated session unit and execute-signals integration coverage</name>
    <read_first>
      - services/aureus-signal/tests/test_ema_integration_execute_signals_for_candle.py
      - services/aureus-signal/engine/live_engine.py
      - services/aureus-signal/engine/signals/session.py
    </read_first>
    <action>
      Create `test_session_o1.py` and `test_session_integration_execute_signals_for_candle.py` to validate session classification boundaries, deterministic state updates, and signal history contract behavior through `execute_signals_for_candle`.
    </action>
    <acceptance_criteria>
      - `test_session_o1.py` exists and covers at least boundary + DST-sensitive classification behavior.
      - `test_session_integration_execute_signals_for_candle.py` exists and verifies signal-history timestamp/tag contract.
      - At least one assertion verifies stable `current_session` progression.
    </acceptance_criteria>
  </task>

  <task id="05-01-03" type="auto">
    <name>Add runtime integration coverage for session in live-engine path</name>
    <read_first>
      - services/aureus-signal/tests/test_fvg_integration_live_engine.py
      - services/aureus-signal/engine/signal_factory.py
      - services/aureus-signal/engine/state_snapshot.py
    </read_first>
    <action>
      Add `test_session_integration_live_engine.py` to validate `run_signal_engine` runtime-path behavior and persisted session state contract, without bypassing normal signal factory wiring.
    </action>
    <acceptance_criteria>
      - `test_session_integration_live_engine.py` exists and executes runtime path.
      - Runtime assertions verify session persistence contract stability.
      - Test does not patch signal factory in a way that masks real registration behavior.
    </acceptance_criteria>
  </task>

  <task id="05-01-04" type="auto">
    <name>Run validation commands and update phase validation evidence</name>
    <read_first>
      - .planning/phases/05-signal-session-optimization/05-VALIDATION.md
      - .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Execute planned verification commands and update `05-VALIDATION.md` with actual outcomes for unit, integration, runtime, and coverage gates.
    </action>
    <acceptance_criteria>
      - `05-VALIDATION.md` status table reflects executed command outcomes.
      - Coverage output for session phase scope is recorded.
      - Phase closure gate remains blocked unless all checks are green.
    </acceptance_criteria>
  </task>
</tasks>

## Verification
- `python -m pytest services/aureus-signal/tests/test_session_o1.py services/aureus-signal/tests/test_session_integration_execute_signals_for_candle.py -q`
- `python -m pytest services/aureus-signal/tests/test_session_integration_live_engine.py -q`
- `python -m pytest services/aureus-signal/tests/test_session_o1.py services/aureus-signal/tests/test_session_integration_execute_signals_for_candle.py services/aureus-signal/tests/test_session_integration_live_engine.py --cov=services/aureus-signal/engine/signals/session.py --cov-report=term-missing -q`

## Success Criteria
- Session signal behavior remains contract-compatible with downstream strategy/judge/snapshot consumers.
- Dedicated session unit, integration, and runtime tests are present and passing.
- Coverage for session phase scope meets gate requirements.
- Requirements mapped in this plan are demonstrably addressed with validation evidence.
