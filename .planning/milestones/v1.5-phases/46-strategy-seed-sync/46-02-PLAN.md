---
phase: 46-strategy-seed-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - services/aureus-signal/tests/test_strategy_seed_sync.py
  - services/aureus-signal/scripts/strategy_seed_sync_rollback.py
  - services/aureus-signal/scripts/strategy_seed_sync_dryrun.py
  - .planning/phases/46-strategy-seed-sync/46-ROLLBACK-RUNBOOK.md
autonomous: true
requirements:
  - PH46-04
  - PH46-05
must_haves:
  truths:
    - "Có test tự động chứng minh enable/disable rollout bằng is_active an toàn và idempotent."
    - "Có dry-run để xem trước thay đổi sync trước khi apply trong môi trường thật."
    - "Có rollback runbook rõ ràng để restore trạng thái active/inactive khi deploy lỗi."
  artifacts:
    - path: services/aureus-signal/tests/test_strategy_seed_sync.py
      provides: regression suite cho sync semantics + reload behavior
    - path: services/aureus-signal/scripts/strategy_seed_sync_dryrun.py
      provides: preview chênh lệch activate/deactivate trước khi áp dụng
    - path: services/aureus-signal/scripts/strategy_seed_sync_rollback.py
      provides: rollback tool theo snapshot trạng thái is_active
    - path: .planning/phases/46-strategy-seed-sync/46-ROLLBACK-RUNBOOK.md
      provides: operator procedure chuẩn cho rollback/forward-fix
  key_links:
    - from: services/aureus-signal/scripts/strategy_seed_sync_dryrun.py
      to: services/aureus-signal/engine/strategies/seed_strategies.py
      via: cùng thuật toán reconcile ở chế độ preview
      pattern: "deactivate|reactivate|unchanged|dry-run"
    - from: services/aureus-signal/scripts/strategy_seed_sync_rollback.py
      to: aureus_symbol_strategies.is_active
      via: restore assignment state từ snapshot table/file
      pattern: "UPDATE aureus_symbol_strategies SET is_active"
---

<objective>
Khóa chất lượng vận hành cho seed sync bằng bộ test, dry-run preview, và rollback runbook để có thể triển khai/thu hồi an toàn.

Purpose: Giảm rủi ro rollout strategy khi thay đổi seed code và đảm bảo recovery nhanh nếu có sai lệch runtime.
Output: Verification bundle + công cụ rollback thực thi được + runbook thao tác chuẩn.

Giả định rõ ràng:
- Rollback theo trạng thái assignment (`is_active`) là đủ cho scope phase này; không yêu cầu rollback dữ liệu candle/signal.
- Runbook markdown được chấp nhận vì user đã yêu cầu rõ “rollback/runbook”.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-strategy-seed-sync/46-01-SUMMARY.md
@services/aureus-signal/engine/strategies/seed_strategies.py
@services/aureus-signal/engine/live_engine.py
@services/aureus-signal/engine/strategy_executor.py
@services/aureus-signal/engine/strategies/registry.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hoàn thiện regression tests cho sync + reload + rollback semantics</name>
  <files>
- services/aureus-signal/tests/test_strategy_seed_sync.py
- services/aureus-signal/engine/strategies/seed_strategies.py
- services/aureus-signal/engine/live_engine.py
  </files>
  <read_first>
- services/aureus-signal/tests/ (pattern hiện có)
- services/aureus-signal/engine/strategies/seed_strategies.py
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/strategy_executor.py
  </read_first>
  <action>Tạo/hoàn thiện `test_strategy_seed_sync.py` bao phủ các cases bắt buộc: idempotent sync; deactivate khi strategy bị rút khỏi catalog; reactivate khi thêm lại; startup/reload call-order (sync trước load); và load active-only. Dùng DB fixture/mocking theo style test hiện có, không tạo harness mới phức tạp.</action>
  <acceptance_criteria>
- Tối thiểu 5 test case tương ứng 5 hành vi cốt lõi phía trên.
- Có ít nhất 1 test chứng minh rollback semantics (reactivate từ trạng thái trước đó).
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_seed_sync.py -q -x</automated>
  </verify>
  <done>Seed sync behavior được khóa bằng test để tránh regression khi thay đổi strategy catalog tương lai.</done>
</task>

<task type="auto">
  <name>Task 2: Tạo công cụ dry-run và rollback executable cho operator</name>
  <files>
- services/aureus-signal/scripts/strategy_seed_sync_dryrun.py
- services/aureus-signal/scripts/strategy_seed_sync_rollback.py
- services/aureus-signal/tests/test_strategy_seed_sync.py
  </files>
  <read_first>
- services/aureus-signal/engine/strategies/seed_strategies.py
- services/aureus-db-writer/schema.sql
- RUN_SERVICES.md
  </read_first>
  <action>Tạo 2 script nhẹ: `strategy_seed_sync_dryrun.py` và `strategy_seed_sync_rollback.py`. Chốt cơ chế snapshot bằng JSON file theo timestamp tại `services/aureus-signal/scripts/snapshots/seed-sync-{YYYYmmdd-HHMMSS}.json` (không dùng bảng tạm). CLI contract cụ thể: dry-run hỗ trợ `--output <path>` để ghi diff JSON và tuyệt đối không mutate DB; rollback hỗ trợ `--snapshot <path> --apply` (thiếu `--apply` thì chỉ preview), validate file tồn tại + schema hợp lệ (`symbol`,`strategy_id`,`is_active`), lỗi phải exit code != 0 kèm message rõ. Không thêm service mới; giữ mức script vận hành đơn giản.</action>
  <acceptance_criteria>
- Dry-run cho ra diff có cấu trúc, không mutate DB.
- Rollback script restore được trạng thái is_active về snapshot chỉ định.
- Cả hai script có `--help` và exit code rõ ràng.
  </acceptance_criteria>
  <verify>
    <automated>python3 /d/Aureus/services/aureus-signal/scripts/strategy_seed_sync_dryrun.py --help && python3 /d/Aureus/services/aureus-signal/scripts/strategy_seed_sync_rollback.py --help</automated>
  </verify>
  <done>Operator có thể preview trước rollout và rollback nhanh bằng command line.</done>
</task>

<task type="auto">
  <name>Task 3: Viết rollback runbook ngắn gọn gắn trực tiếp với command thật</name>
  <files>
- .planning/phases/46-strategy-seed-sync/46-ROLLBACK-RUNBOOK.md
- services/aureus-signal/tests/test_strategy_seed_sync.py
  </files>
  <read_first>
- RUN_SERVICES.md
- services/aureus-signal/scripts/strategy_seed_sync_dryrun.py
- services/aureus-signal/scripts/strategy_seed_sync_rollback.py
  </read_first>
  <action>Tạo `.planning/phases/46-strategy-seed-sync/46-ROLLBACK-RUNBOOK.md` gồm đúng 3 luồng: Pre-check (dry-run + snapshot id), Rollout (sync + refresh command), Rollback (chọn snapshot và restore), kèm expected outputs để người vận hành xác nhận nhanh. Đồng thời thêm/ cập nhật test `runbook_contract` trong `services/aureus-signal/tests/test_strategy_seed_sync.py` để assert runbook chứa đủ 3 section và command bắt buộc (`strategy_seed_sync_dryrun.py`, `strategy_seed_sync_rollback.py --snapshot ... --apply`). Không viết tài liệu dài; chỉ checklist command-by-command.</action>
  <acceptance_criteria>
- Runbook có command cụ thể copy-paste được.
- Mỗi bước có điều kiện pass/fail quan sát được (log/test/DB check).
- Không chứa hướng dẫn mơ hồ kiểu “kiểm tra lại hệ thống”.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_seed_sync.py -q -k "runbook_contract"</automated>
  </verify>
  <done>Rollout/rollback thao tác nhất quán, giảm phụ thuộc vào trí nhớ operator khi sự cố.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI -> production DB state | Lệnh sync/rollback có thể thay đổi diện rộng trạng thái active nếu thao tác sai |
| Dry-run output -> human decision | Nếu preview sai, quyết định rollout/rollback có thể sai theo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-01 | T | rollback script | mitigate | Bắt buộc snapshot id hợp lệ + transaction restore atomically |
| T-46-02-02 | R | operator actions | mitigate | Runbook yêu cầu lưu log command + output checkpoint mỗi bước |
| T-46-02-03 | D | bad rollout disable mass | mitigate | Dry-run bắt buộc trước apply, hiển thị diff count và symbol scope |
</threat_model>

<verification>
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_strategy_seed_sync.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_multi_symbol.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_live_engine_shadow_mode.py -q
</verification>

<success_criteria>
- Có bộ chứng cứ tự động cho toàn bộ behavior sync/active/reload/rollback.
- Có quy trình rollback rõ và thực thi được bằng script thay vì thao tác SQL thủ công ad-hoc.
- Triển khai phase 46 có thể chạy theo flow: dry-run -> apply -> verify -> rollback (nếu cần).
</success_criteria>

<output>
After completion, create `.planning/phases/46-strategy-seed-sync/46-02-SUMMARY.md`
</output>
