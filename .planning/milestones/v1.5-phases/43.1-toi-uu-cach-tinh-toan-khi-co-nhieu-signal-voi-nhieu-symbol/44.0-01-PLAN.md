---
phase: 44.0-profiling-baseline-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/live_engine.py
  - services/aureus-signal/engine/manager.py
  - services/aureus-signal/tests/test_profiling_instrumentation.py
autonomous: true
requirements:
  - PROF-01
  - PROF-02
must_haves:
  truths:
    - "Engine ghi timing theo từng signal và tổng mỗi candle mà không đổi kết quả trading."
    - "Profiling data được đẩy vào Redis stream theo symbol với giới hạn MAXLEN để không phình bộ nhớ."
    - "WindowManager có số đo riêng cho DataFrame build và integrity để tách chi phí pipeline."
  artifacts:
    - path: services/aureus-signal/engine/live_engine.py
      provides: Instrument timing trong execute_signals_for_candle + push stream aureus:profiling:{symbol}
    - path: services/aureus-signal/engine/manager.py
      provides: Timing cho DataFrame build và integrity check trong WindowManager.update
    - path: services/aureus-signal/tests/test_profiling_instrumentation.py
      provides: Regression tests đảm bảo profiling additive và resilient
  key_links:
    - from: services/aureus-signal/engine/live_engine.py
      to: aureus:profiling:{symbol}
      via: redis_client.xadd(maxlen=10000)
      pattern: "xadd\(f\"aureus:profiling:\{symbol\}\""
    - from: services/aureus-signal/engine/live_engine.py
      to: logging output
      via: summary mỗi 100 candles
      pattern: "\[PROFILING\]"
---

<objective>
Thiết lập baseline instrumentation cho signal engine đúng phạm vi phase 44.0: đo thời gian per-signal/per-candle và ghi dữ liệu profiling để dùng cho phase tối ưu sau.

Purpose: Tạo số liệu thật cho PROF-01/02 mà không thay đổi logic trading.
Output: Instrumentation trong live_engine + manager, kèm test bảo vệ hành vi.
</objective>

<context>
@D:/Aureus/.planning/ROADMAP.md
@D:/Aureus/.planning/phases/44.0-profiling-baseline-performance/44.0-CONTEXT.md
@D:/Aureus/.planning/phases/44.0-profiling-baseline-performance/44.0-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instrument execute_signals_for_candle cho PROF-01 + PROF-02</name>
  <files>services/aureus-signal/engine/live_engine.py, services/aureus-signal/tests/test_profiling_instrumentation.py</files>
  <read_first>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/signal_factory.py
- .planning/phases/44.0-profiling-baseline-performance/44.0-CONTEXT.md
  </read_first>
  <action>Thêm đo thời gian bằng time.perf_counter_ns() bên trong execute_signals_for_candle (per D-Location/Timing trong CONTEXT): đo từng signal và _total; giữ nguyên flow xử lý signal hiện tại, không đổi output/side effects trading. Đẩy payload vào Redis stream aureus:profiling:{symbol} với maxlen=10000, bọc try/except để lỗi profiling không làm crash vòng candle. Log summary mỗi 100 candles theo format [PROFILING] với timestamp + avg_ms + total_ms.</action>
  <acceptance_criteria>
- Có timing per-signal và _total trong execute_signals_for_candle.
- Có xadd vào aureus:profiling:{symbol} với maxlen=10000.
- Có guard try/except bao quanh nhánh profiling write/log.
- Không thay đổi semantic kết quả signal so với trước instrumentation.
  </acceptance_criteria>
  <verify>
    <automated>pytest services/aureus-signal/tests/test_profiling_instrumentation.py -k "signal_results or profiling_records_timing or redis_failure" -q -x</automated>
  </verify>
  <done>PROF-01/02 cho nhánh execute_signals_for_candle hoàn tất với test pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument WindowManager.update để tách chi phí DataFrame/integrity</name>
  <files>services/aureus-signal/engine/manager.py, services/aureus-signal/tests/test_profiling_instrumentation.py</files>
  <read_first>
- services/aureus-signal/engine/manager.py
- services/aureus-signal/tests/test_profiling_instrumentation.py
- .planning/phases/44.0-profiling-baseline-performance/44.0-CONTEXT.md
  </read_first>
  <action>Thêm timing perf_counter_ns cho DataFrame build và integrity metadata trong WindowManager.update; log [PROFILING] mỗi 100 candles/symbol theo quyết định locked trong CONTEXT. Không đổi logic window append, integrity semantics, hoặc return path.</action>
  <acceptance_criteria>
- manager.py có timing tách riêng df_build_ns và integrity_ns.
- Log xuất hiện theo chu kỳ 100 candles và chứa window_size.
- Không phát sinh thay đổi hành vi nghiệp vụ ngoài profiling additive.
  </acceptance_criteria>
  <verify>
    <automated>pytest services/aureus-signal/tests/test_profiling_instrumentation.py -k "window_manager_times_df_build or logs_every_100_candles" -q -x</automated>
  </verify>
  <done>Window-level instrumentation hoàn tất và có test xác nhận.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hoàn thiện regression + overhead guard cho baseline profiling</name>
  <files>services/aureus-signal/tests/test_profiling_instrumentation.py</files>
  <read_first>
- services/aureus-signal/tests/test_profiling_instrumentation.py
- .planning/phases/44.0-profiling-baseline-performance/44.0-CONTEXT.md
  </read_first>
  <action>Hoàn thiện test bundle để chứng minh instrumentation là additive: không đổi kết quả signal, không crash khi Redis lỗi, và overhead nằm trong ngưỡng đã chốt (<0.1ms/candle theo CONTEXT). Không thêm test liên quan optimization structure.</action>
  <acceptance_criteria>
- Test suite bao phủ: parity output, Redis failure resilience, log cadence 100 candles, overhead budget.
- Mọi test mới chạy độc lập dưới 60s.
  </acceptance_criteria>
  <verify>
    <automated>pytest services/aureus-signal/tests/test_profiling_instrumentation.py -q -x</automated>
  </verify>
  <done>Baseline profiling có bộ kiểm chứng tự động đầy đủ cho PROF-01/02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candle processing → profiling instrumentation | Input runtime đi qua nhánh đo thời gian, cần đảm bảo không ảnh hưởng output trading |
| engine → redis stream | Ghi profiling I/O có thể lỗi hoặc chậm, cần không block execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44.0-01 | T | live_engine profiling payload | mitigate | Serialize payload rõ field + guard try/except trước xadd |
| T-44.0-02 | D | Redis profiling write path | mitigate | Lỗi ghi stream chỉ warning, không raise ra candle loop |
| T-44.0-03 | R | profiling logs | mitigate | Format log chuẩn [PROFILING] kèm symbol/t để truy vết |
</threat_model>

<verification>
- pytest services/aureus-signal/tests/test_profiling_instrumentation.py -q -x
</verification>

<success_criteria>
- PROF-01 hoàn tất: đo được per-signal + total timing trong runtime path chính.
- PROF-02 hoàn tất: dữ liệu được aggregate vào Redis stream theo symbol, bounded memory.
- Không có task nào đụng tối ưu logic structure (scope 44.4 đã loại bỏ).
</success_criteria>

<output>
After completion, create `D:/Aureus/.planning/phases/44.0-profiling-baseline-performance/44.0-01-SUMMARY.md`
</output>
