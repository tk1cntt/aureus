---
phase: "39.1"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/state.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Signal history truncate limit is 500 (was 240)"
    - "Truncation still works — events beyond 500 are popped from front"
    - "Redis stream payload size increases from ~318KB to ~660KB per symbol (acceptable)"
  artifacts:
    - path: "services/aureus-signal/engine/state.py"
      provides: "Truncate limit in log_signal_normalize_add method"
      contains: "len(self.log_signal_normalize) > 500"
  key_links:
    - from: "services/aureus-signal/engine/state.py"
      to: "log_signal_normalize list"
      via: "append + pop(0) with limit=500"
      pattern: "len\\(self\\.log_signal_normalize\\) > 500"
---

<objective>
Increase signal history truncate limit from 240 to 500 in state.py to provide more margin before auto-reset (D-01) is needed.

Purpose: 240 records causes stall after ~4h on M15. Increasing to 500 doubles the buffer, reducing auto-reset frequency and giving strategies more lookback context for 3-5 step sequences.
Output: Single-line change in state.py from 240 to 500.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39.1-fix-signal-history-truncate-causing-permanent-sequence-not-m/39.1-CONTEXT.md
@services/aureus-signal/engine/state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change truncate limit from 240 to 500 in state.py</name>
  <files>services/aureus-signal/engine/state.py</files>
  <read_first>services/aureus-signal/engine/state.py</read_first>
  <action>
In state.py, line 228, change the truncate limit:

FROM:
```python
        if len(self.log_signal_normalize) > 240:
```

TO:
```python
        if len(self.log_signal_normalize) > 500:
```

This is per decision D-03. No other changes needed. The truncation mechanism (append + pop(0)) remains identical — only the threshold changes.

Verify with grep that no other file references the hardcoded value 240 for this purpose:
- Check: `grep -r "240" services/aureus-signal/engine/` to confirm no other references to this limit exist
  </action>
  <verify>
    <automated>cd services/aureus-signal && python -c "import py_compile; py_compile.compile('engine/state.py', doraise=True)" && echo "py_compile OK" && grep -n "len(self.log_signal_normalize) > 500" engine/state.py</automated>
  </verify>
  <done>
    py_compile passes on state.py. Grep confirms `len(self.log_signal_normalize) > 500` on line 228. No other references to hardcoded 240 limit in engine code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm no regressions</name>
  <files>services/aureus-signal/engine/state.py</files>
  <read_first>services/aureus-signal/tests/test_template_strategy.py</read_first>
  <action>
Run the full template_strategy test suite to confirm the truncate limit change doesn't affect any test behavior. The truncate limit is only used when adding events to the log, and tests use small numbers of events (<10), so no test should be affected by changing 240→500.
  </action>
  <verify>
    <automated>cd services/aureus-signal && python -m pytest tests/test_template_strategy.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests pass. py_compile passes. Truncate limit confirmed at 500.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis stream → state.py log | Data from Redis pub/sub, size bounded by truncate limit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-04 | Denial of Service | Redis stream payload size | accept | Payload increases from ~318KB to ~660KB per symbol. With 8 symbols, total ~5.3MB vs ~2.5MB previously. Well within Redis stream capacity. Monitor via existing Redis memory metrics. |
| T-39.1-05 | Resource Exhaustion | Memory usage of log_signal_normalize | accept | 500 records vs 240 — bounded growth, not unbounded. Each record ~1.3KB average → 650KB max per symbol. Acceptable for 8 symbols. |
</threat_model>

<verification>
- py_compile on state.py passes
- Grep confirms `> 500` in state.py
- No other hardcoded 240 references in engine code
- All existing tests pass
</verification>

<success_criteria>
- state.py line 228: `len(self.log_signal_normalize) > 500` (not 240)
- Truncation mechanism unchanged: append + pop(0)
- py_compile passes
- All existing tests pass
- No other references to 240 limit found in engine code
</success_criteria>

<output>
After completion, create `.planning/phases/39.1-fix-signal-history-truncate-causing-permanent-sequence-not-m/39.1-03-SUMMARY.md`
</output>
