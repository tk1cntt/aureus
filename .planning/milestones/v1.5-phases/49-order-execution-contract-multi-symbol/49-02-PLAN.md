---
phase: 49-order-execution-contract-multi-symbol
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - services/aureus-nautilus-node/settings.py
  - services/aureus-nautilus-node/execution_client.py
  - services/aureus-nautilus-node/tests/test_execution_client_policy.py
  - services/aureus-nautilus-node/tests/test_execution_multi_symbol_contract.py
autonomous: true
requirements: [ORDER-01, PH45-06]
must_haves:
  truths:
    - "Execution client poll được nhiều stream orders theo symbol thay vì mặc định singleton XAUUSD."
    - "Mọi mismatch symbol giữa stream và payload bị reject reason code SYMBOL_STREAM_MISMATCH."
    - "Whitelist guardrail vẫn enforce cho multi-symbol runtime."
  artifacts:
    - path: "services/aureus-nautilus-node/settings.py"
      provides: "Multi-symbol whitelist defaults và validation"
    - path: "services/aureus-nautilus-node/execution_client.py"
      provides: "Discover/poll multi-stream với per-stream last_id"
    - path: "services/aureus-nautilus-node/tests/test_execution_multi_symbol_contract.py"
      provides: "Regression tests cho stream discovery và hardcode removal"
  key_links:
    - from: "services/aureus-nautilus-node/settings.py"
      to: "services/aureus-nautilus-node/execution_client.py"
      via: "ExecutionRiskPolicy symbol_whitelist"
      pattern: "symbol_whitelist"
    - from: "services/aureus-nautilus-node/execution_client.py"
      to: "services/aureus-nautilus-node/tests/test_execution_client_policy.py"
      via: "symbol-stream mismatch guard"
      pattern: "SYMBOL_STREAM_MISMATCH"
---

<objective>
Loại bỏ hardcode/fallback thiên lệch XAUUSD trong execution consume path và khóa hành vi multi-symbol theo stream pattern thật.

Purpose: Đóng gap `multi-symbol streams -> _poll_loop` để PH45-06 có hiệu lực vận hành thực tế.
Output: Settings + discovery/policy path + regression tests multi-symbol.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-order-execution-contract-multi-symbol/49-CONTEXT.md
@services/aureus-nautilus-node/settings.py
@services/aureus-nautilus-node/execution_client.py
@services/aureus-nautilus-node/tests/test_execution_client_policy.py

<interfaces>
From services/aureus-nautilus-node/settings.py:
```python
class NautilusNodeSettings:
    symbol_whitelist: List[str]
    order_stream_pattern: str
```

From services/aureus-nautilus-node/execution_client.py:
```python
async def _discover_order_streams(self) -> Dict[str, str]
@staticmethod
def _extract_stream_symbol(stream_str: str) -> str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chuẩn hóa settings multi-symbol, bỏ default singleton XAUUSD</name>
  <files>services/aureus-nautilus-node/settings.py, services/aureus-nautilus-node/execution_client.py</files>
  <read_first>
    - .planning/phases/49-order-execution-contract-multi-symbol/49-CONTEXT.md
    - services/aureus-nautilus-node/settings.py
    - services/aureus-nautilus-node/execution_client.py
  </read_first>
  <behavior>
    - Test 1: from_env yêu cầu whitelist multi-symbol hợp lệ thay vì fallback singleton ngầm.
    - Test 2: _discover_order_streams không tự chèn `aureus:stream:XAUUSD:orders` khi không cần thiết.
  </behavior>
  <action>Trước khi sửa `NautilusNodeSettings.from_env` và `_discover_order_streams`, chạy `gitnexus_impact({target: "NautilusNodeSettings.from_env", direction: "upstream"})` và `gitnexus_impact({target: "_discover_order_streams", direction: "upstream"})`, ghi blast radius vào SUMMARY. Sau đó cập nhật defaults/policy để whitelist và stream discovery mang semantics multi-symbol thực tế (per D-04, D-06), xóa nhánh hardcode fallback stream `aureus:stream:XAUUSD:orders` trong execution path, nhưng vẫn giữ guard lỗi cấu hình rõ ràng qua validate settings thay vì silently fallback.</action>
  <acceptance_criteria>
    - settings.py không còn default literal `XAUUSD` cho whitelist runtime chính.
    - execution_client.py không còn `discovered.add("aureus:stream:XAUUSD:orders")`.
    - `_discover_order_streams` vẫn trả map stream->last_id deterministic.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest D:/Aureus/services/aureus-nautilus-node/tests/test_execution_client_policy.py -q -x</automated>
  </verify>
  <done>Execution client discover/poll path vận hành theo multi-symbol thật, không bias XAUUSD.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Khóa guard stream/payload symbol mismatch + bổ sung test contract mới</name>
  <files>services/aureus-nautilus-node/tests/test_execution_client_policy.py, services/aureus-nautilus-node/tests/test_execution_multi_symbol_contract.py</files>
  <read_first>
    - .planning/phases/49-order-execution-contract-multi-symbol/49-VALIDATION.md
    - services/aureus-nautilus-node/tests/test_execution_client_policy.py
    - services/aureus-nautilus-node/execution_client.py
  </read_first>
  <behavior>
    - Test 1: stream `...:EURUSD:orders` với payload symbol `XAUUSD` bị reject `SYMBOL_STREAM_MISMATCH`.
    - Test 2: poll nhiều stream giữ per-stream last_id độc lập.
  </behavior>
  <action>Mở rộng test matrix theo 49-VALIDATION Wave 0 bằng file mới `test_execution_multi_symbol_contract.py` để chứng minh D-04/D-05/D-06: discover đa stream, cursor độc lập, mismatch reject reason code ổn định, và không tái xuất hiện fallback XAUUSD.</action>
  <acceptance_criteria>
    - Có file `test_execution_multi_symbol_contract.py` với case assert `SYMBOL_STREAM_MISMATCH`.
    - Có case assert per-stream last_id cập nhật độc lập cho >=2 symbols.
    - Bộ test policy + test mới chạy xanh.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest D:/Aureus/services/aureus-nautilus-node/tests/test_execution_client_policy.py D:/Aureus/services/aureus-nautilus-node/tests/test_execution_multi_symbol_contract.py -q</automated>
  </verify>
  <done>PH45-06 có evidence tự động cho multi-symbol consume path và mismatch guardrails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis stream namespace -> execution poll loop | Stream key và payload có thể không đồng nhất symbol |
| Env settings -> runtime policy | Cấu hình sai có thể mở đường consume sai symbol |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | S/T | stream symbol routing | mitigate | Enforce stream/payload symbol match, reject `SYMBOL_STREAM_MISMATCH` với counter metrics |
| T-49-05 | E | whitelist policy | mitigate | Validate `symbol_whitelist` bắt buộc hợp lệ, không fallback singleton âm thầm |
| T-49-06 | D | poll loop fairness | mitigate | Maintain per-stream `last_id` cursor và regression tests >=2 streams |
</threat_model>

<verification>
- `python3 -m pytest D:/Aureus/services/aureus-nautilus-node/tests/test_execution_client_policy.py -q -x`
- `python3 -m pytest D:/Aureus/services/aureus-nautilus-node/tests/test_execution_multi_symbol_contract.py -q`
- Run `gitnexus_detect_changes({scope: "all"})` before final commit; result must stay within `files_modified` of this plan.
</verification>

<success_criteria>
- Không còn hardcode/fallback XAUUSD trong settings + execution discovery path.
- Reject `SYMBOL_STREAM_MISMATCH` hoạt động ổn định và có test khóa.
- Multi-symbol poll path có test chứng minh cursor độc lập theo stream.
</success_criteria>

<output>
After completion, create `.planning/phases/49-order-execution-contract-multi-symbol/49-02-SUMMARY.md`
</output>
