---
phase: 49-order-execution-contract-multi-symbol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-nautilus-node/execution_client.py
  - services/aureus-nautilus-node/tests/test_execution_client.py
  - services/aureus-nautilus-node/tests/test_execution_risk_controls.py
autonomous: true
requirements: [ORDER-01, ORDER-02, PH45-05]
must_haves:
  truths:
    - "ORDER_OPEN thiếu trace_id/symbol/side/qty bị reject với reason code ổn định."
    - "ORDER_OPEN hợp lệ tạo được ENTRY + contingent SL/TP theo đúng side và qty."
    - "trace_id duplicate bị chặn deterministic, không phát sinh order mới."
  artifacts:
    - path: "services/aureus-nautilus-node/execution_client.py"
      provides: "Contract validation + idempotency path cho ORDER_OPEN"
    - path: "services/aureus-nautilus-node/tests/test_execution_client.py"
      provides: "Regression tests cho critical fields/reason codes"
    - path: "services/aureus-nautilus-node/tests/test_execution_risk_controls.py"
      provides: "Regression tests cho contingent orders + duplicate trace_id"
  key_links:
    - from: "services/aureus-signal/engine/orders.py"
      to: "services/aureus-nautilus-node/execution_client.py"
      via: "Redis stream ORDER_OPEN payload"
      pattern: "trace_id|symbol|side|qty"
    - from: "services/aureus-nautilus-node/execution_client.py"
      to: "services/aureus-nautilus-node/tests/test_execution_risk_controls.py"
      via: "unit tests for generate/reject logic"
      pattern: "DUPLICATE_TRACE_ID|ORDER_OPEN_MISSING_CRITICAL_FIELD"
---

<objective>
Khóa contract-first cho ORDER_OPEN tại biên consume để không reject sai dữ liệu hợp lệ và không chấp nhận payload thiếu critical fields.

Purpose: Đóng lõi gap `orders.py -> execution_client.py` bằng validation deterministic và idempotency strict theo trace_id.
Output: Validation/runtime logic và test contracts cho ORDER-01/02 + PH45-05.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-order-execution-contract-multi-symbol/49-CONTEXT.md
@.planning/phases/49-order-execution-contract-multi-symbol/49-RESEARCH.md
@services/aureus-signal/engine/orders.py
@services/aureus-nautilus-node/execution_client.py
@services/aureus-nautilus-node/tests/test_execution_client.py
@services/aureus-nautilus-node/tests/test_execution_risk_controls.py

<interfaces>
From services/aureus-signal/engine/orders.py:
```python
trace_id = f"{symbol}:{strat_id}:{origin_t}"
await self.r.xadd(f"aureus:stream:{symbol}:orders", {
    "type": stream_type,
    "data": json.dumps(order)
})
```

From services/aureus-nautilus-node/execution_client.py:
```python
def _validate_and_build_orders(
    self,
    data: Dict[str, Any],
    *,
    stream_symbol: str = "",
) -> Tuple[bool, str, List[Dict[str, Any]], List[str]]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chuẩn hóa validation critical fields + qty alias tại execution boundary</name>
  <files>services/aureus-nautilus-node/execution_client.py, services/aureus-nautilus-node/tests/test_execution_client.py</files>
  <read_first>
    - .planning/phases/49-order-execution-contract-multi-symbol/49-CONTEXT.md
    - services/aureus-nautilus-node/execution_client.py
    - services/aureus-nautilus-node/tests/test_execution_client.py
  </read_first>
  <behavior>
    - Test 1: ORDER_OPEN thiếu trace_id/symbol/side/qty trả ORDER_OPEN_MISSING_CRITICAL_FIELD.
    - Test 2: Payload có quantity nhưng thiếu qty được normalize sang qty và không bị reject sai.
    - Test 3: Optional fields entry_policy/expiry_policy/backfill_status fallback có increment metrics tương ứng.
  </behavior>
  <action>Trước khi sửa `_validate_and_build_orders`, chạy `gitnexus_impact({target: "_validate_and_build_orders", direction: "upstream"})` và ghi blast radius vào SUMMARY. Sau đó thêm adapter tối thiểu để canonical volume về `qty` nhưng vẫn hỗ trợ alias `quantity` (per D-07), giữ reject cứng khi cả hai đều thiếu (per D-01), và không đổi semantics fallback optional fields ngoài metric evidence (per D-02, D-09). Không thay đổi format stream publish ở producer để giữ minimum-change wiring (per D-10).</action>
  <acceptance_criteria>
    - Trong execution_client.py có logic đọc `qty` ưu tiên, fallback `quantity` có kiểm soát.
    - Reason code `ORDER_OPEN_MISSING_CRITICAL_FIELD` vẫn giữ nguyên cho thiếu critical fields.
    - Metric `order_open_optional_fallback_total` tiếp tục tăng khi thiếu optional fields.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest D:/Aureus/services/aureus-nautilus-node/tests/test_execution_client.py -q -x</automated>
  </verify>
  <done>ORDER_OPEN contract consume path chấp nhận đúng dữ liệu hợp lệ theo D-01/D-02/D-07, không tạo false reject do lệch key qty/quantity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Khóa idempotency strict và contingent generation theo ORDER_OPEN</name>
  <files>services/aureus-nautilus-node/execution_client.py, services/aureus-nautilus-node/tests/test_execution_risk_controls.py</files>
  <read_first>
    - .planning/phases/49-order-execution-contract-multi-symbol/49-CONTEXT.md
    - services/aureus-nautilus-node/execution_client.py
    - services/aureus-nautilus-node/tests/test_execution_risk_controls.py
  </read_first>
  <behavior>
    - Test 1: Payload hợp lệ tạo ENTRY + STOP_LOSS + TAKE_PROFIT đúng side đảo chiều.
    - Test 2: Duplicate trace_id trả `DUPLICATE_TRACE_ID` và không gọi generate_order lần 2.
  </behavior>
  <action>Trước khi sửa idempotency/contingent path trong `execution_client.py`, chạy `gitnexus_impact({target: "_validate_and_build_orders", direction: "upstream"})` và ghi blast radius vào SUMMARY. Sau đó giữ nguyên cơ chế `_seen_trace_ids` reject duplicate trace_id (per D-01, PH45-05), đồng thời đảm bảo path generate contingent orders vẫn dùng qty canonical sau normalize để ORDER-02 không regress. Không thêm mode/queue mới, chỉ sửa trong execution client hiện có (per D-10).</action>
  <acceptance_criteria>
    - Test duplicate trace_id pass với reason `DUPLICATE_TRACE_ID`.
    - Test contingent generation pass với đủ 3 orders khi có sl/tp.
    - Không xuất hiện branch mới bypass `_seen_trace_ids`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest D:/Aureus/services/aureus-nautilus-node/tests/test_execution_risk_controls.py -q -x</automated>
  </verify>
  <done>PH45-05 giữ strict idempotency và ORDER-02 vẫn tạo đầy đủ contingent orders theo contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal producer -> execution client | Untrusted ORDER_OPEN payload đi từ Redis stream vào consumer |
| runtime payload -> order generation | Dữ liệu chưa validate đi vào lệnh ENTRY/SL/TP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | T | `execution_client._validate_and_build_orders` | mitigate | Enforce critical fields trace_id/symbol/side/qty, reject machine-readable `ORDER_OPEN_MISSING_CRITICAL_FIELD` |
| T-49-02 | R | `execution_client` idempotency | mitigate | Reject `DUPLICATE_TRACE_ID` trước generate_order, assert bằng unit test duplicate path |
| T-49-03 | D | contingent generation path | mitigate | Chỉ generate orders sau validate pass + qty numeric > 0; block invalid qty/notional |
</threat_model>

<verification>
- `python3 -m pytest D:/Aureus/services/aureus-nautilus-node/tests/test_execution_client.py -q -x`
- `python3 -m pytest D:/Aureus/services/aureus-nautilus-node/tests/test_execution_risk_controls.py -q -x`
- Run `gitnexus_detect_changes({scope: "all"})` before final commit; result must stay within `files_modified` of this plan.
</verification>

<success_criteria>
- Không còn false reject khi payload dùng `quantity` alias hợp lệ.
- ORDER_OPEN thiếu critical fields luôn reject bằng reason code ổn định.
- Duplicate trace_id không thể tạo order lần 2.
</success_criteria>

<output>
After completion, create `.planning/phases/49-order-execution-contract-multi-symbol/49-01-SUMMARY.md`
</output>
