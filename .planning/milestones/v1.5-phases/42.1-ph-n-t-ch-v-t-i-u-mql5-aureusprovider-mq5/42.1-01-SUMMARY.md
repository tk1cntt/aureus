---
phase: 42.1
plan: 01
subsystem: mql5-execution-gateway-events
tags: [mt5, gateway, rr, post-fill-finalize]
requires: [PH42.1-01, PH42.1-02, PH42.1-03]
provides: [post-fill-finalize-sequencing, modify-fail-terminal-failure, structured-postfill-logs]
affects: [mql5/AureusProvider.mq5, services/aureus-gateway/tests/test_order_events.py]
tech_stack:
  added: []
  patterns: [TDD-regression-tests, fail-fast-modify, single-terminal-event]
key_files:
  modified:
    - mql5/AureusProvider.mq5
    - services/aureus-gateway/tests/test_order_events.py
decisions:
  - Keep early ACK contract unchanged for valid OPEN_ORDER commands.
  - MARKET terminal success now requires post-fill SLTP modify success.
  - Modify failure emits ORDER_FAILED with retcode/reason trace and no retry loop.
  - Option B hardened with 3 safety layers: deterministic fill/position resolution, stop+freeze pre-modify guards, and hard single terminal-event gate (exactly one of ORDER_OPENED/ORDER_FAILED).
metrics:
  completed_at: 2026-04-16
  tasks_completed: 3
  commits: 3
---

# Phase 42.1 Plan 01: Post-fill finalize MARKET lifecycle Summary

Implemented MARKET order post-fill finalization so TP is recalculated from actual fill and ORDER_OPENED is emitted only after SLTP modify succeeds; modify failures now emit explicit ORDER_FAILED with traceable reason/retcode.

## Tasks Completed

1. Task 0 (TDD regression): Added regression tests in `services/aureus-gateway/tests/test_order_events.py` for ACK→terminal sequencing, modify-fail no-false-success behavior, and post-fill trace fields.
   - Commit: `10dcbd0`

2. Task 1: Implemented post-fill finalize in `ExecuteOpenOrder` MARKET path in `mql5/AureusProvider.mq5`.
   - Recalculate TP from filled entry and final SL using rr payload/fallback.
   - Gate ORDER_OPENED on successful SLTP modify.
   - Emit ORDER_FAILED with `POST_FILL_MODIFY_FAILED|...|modify_result=retcode:*` on failure.
   - Commit: `5df16c0`

3. Task 2: Added structured machine-greppable log marker for modify attempt in `mql5/AureusProvider.mq5`.
   - Markers present: `PF_FILL`, `PF_RECALC`, `PF_MODIFY_ATTEMPT`, `PF_MODIFY`.
   - Commit: `4fc26d6`

## Verification Results

- Task-level command:
  - `wsl -d Aureus -e bash -lc "cd /mnt/d/Aureus/.claude/worktrees/agent-a802ef96/services/aureus-gateway && /mnt/d/Aureus/.venv/bin/python -m pytest tests/test_order_events.py -q -k 'opened or failed'"`
  - Result: `4 passed, 8 deselected`

- Wave-level suites:
  - Gateway tests: `12 passed`
  - Trader tests: `102 passed`

## Deviations from Plan

### Auto-fixed Issues

1. [Rule 3 - Blocking] Worktree snapshot lacked phase planning files
- Found during: mandatory pre-check
- Fix: realigned worktree HEAD locally and synced required phase files from parent working tree snapshot into isolated worktree.
- Files affected in working tree context: `.planning/phases/42.1-.../*`, `CLAUDE.md`, `services/aureus-gateway/tests/test_order_events.py`

2. [Rule 3 - Blocking] Validation command python path mismatch in worktree context
- Found during: Task 0 verify command
- Fix: used absolute venv path `/mnt/d/Aureus/.venv/bin/python` for all WSL test commands.

3. [Rule 2 - Correctness/Security] Added explicit invalid-stops guard before modify
- Found during: Task 1 finalize design against D-06 constraints
- Fix: validate stop-distance constraints before sending SLTP modify, fail-fast to ORDER_FAILED with retcode 10016 trace.
- File: `mql5/AureusProvider.mq5`
- Commit: `5df16c0`

## Known Stubs

None.

## Threat Flags

None identified beyond the planned threat model scope.

## Self-Check: PASSED

- Confirmed summary file exists.
- Confirmed task commits exist in git history: `10dcbd0`, `5df16c0`, `4fc26d6`.
