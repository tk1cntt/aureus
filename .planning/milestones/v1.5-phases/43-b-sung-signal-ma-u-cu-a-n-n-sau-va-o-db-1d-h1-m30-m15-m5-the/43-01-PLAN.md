---
phase: 43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/mtf_snapshot.py
  - services/aureus-signal/tests/test_mtf_candle_color.py
  - services/aureus-signal/tests/test_mtf_bb_snapshot.py
  - services/aureus-signal/tests/test_mtf_last_closed_rule.py
autonomous: true
requirements: [PH43-01, PH43-02, PH43-03, PH43-04]

must_haves:
  truths:
    - "Màu nến đa TF được phân loại BULLISH/BEARISH/DOJI theo OHLC đã normalize digits"
    - "Snapshot BB đa TF luôn có đúng 3 line upper/middle/lower hoặc null khi thiếu dữ liệu"
    - "TF lớn hơn M1 luôn lấy nến đã đóng gần nhất, không lấy nến forming"
    - "Logic null-first nhất quán cho mọi TF không đủ dữ liệu"
  artifacts:
    - path: "services/aureus-signal/engine/mtf_snapshot.py"
      provides: "Helper contracts cho candle color + BB MTF + last-closed selection"
      exports: ["compute_candle_color", "get_last_closed_candle", "build_bb_payload", "build_mtf_candle_color_map"]
    - path: "services/aureus-signal/tests/test_mtf_candle_color.py"
      provides: "RED/GREEN tests cho D-01, D-02, D-03"
    - path: "services/aureus-signal/tests/test_mtf_bb_snapshot.py"
      provides: "RED/GREEN tests cho D-04, D-05, D-06, D-10"
    - path: "services/aureus-signal/tests/test_mtf_last_closed_rule.py"
      provides: "RED/GREEN tests cho D-08, D-09"
  key_links:
    - from: "services/aureus-signal/engine/mtf_snapshot.py"
      to: "services/aureus-signal/engine/signals/resampler.py"
      via: "resample_to_tf + chọn iloc[-2] cho TF> M1"
      pattern: "iloc\[-2\]|resample_to_tf"
    - from: "services/aureus-signal/engine/mtf_snapshot.py"
      to: "services/aureus-signal/tests/test_mtf_bb_snapshot.py"
      via: "assert dict bb_<tf> có đủ upper/middle/lower"
      pattern: "upper|middle|lower"
---

<objective>
Tạo nền tảng logic MTF snapshot theo TDD để khóa chặt rule nghiệp vụ trước khi wire vào DB writer path.

Purpose: Khóa sớm các quyết định D-01..D-10 bằng test tự động, giảm rủi ro sai semantics last-closed khi tích hợp persistence.
Output: Module helper MTF snapshot + bộ unit tests RED→GREEN cho candle color/BB/last-closed/null-first.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-CONTEXT.md
@.planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-RESEARCH.md
@services/aureus-signal/engine/signals/resampler.py

<interfaces>
From services/aureus-signal/engine/signals/resampler.py:
```python
def resample_to_tf(df: pd.DataFrame, tf: str) -> Optional[pd.DataFrame]:
    """The last row is the forming (incomplete) HTF candle."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Viết RED tests cho candle color MTF và null-first policy</name>
  <files>services/aureus-signal/tests/test_mtf_candle_color.py</files>
  <read_first>
    - services/aureus-signal/engine/signals/resampler.py
    - services/aureus-signal/tests
  </read_first>
  <behavior>
    - Test 1: close > open => BULLISH (uppercase) theo D-01, D-03.
    - Test 2: close < open => BEARISH (uppercase) theo D-01, D-03.
    - Test 3: close == open sau normalize digits => DOJI theo D-01.
    - Test 4: map keys bắt buộc gồm candle_color_d1/h1/m30/m15/m5 theo D-02.
    - Test 5: thiếu dữ liệu TF => field TF đó = null theo D-10.
  </behavior>
  <action>Tạo file test_mtf_candle_color.py với fixtures OHLC đơn giản cho M1/M5/H1/D1. Viết tests fail trước cho `compute_candle_color` và `build_mtf_candle_color_map` (per D-01, D-02, D-03, D-10). Không hardcode kết quả ngoài input OHLC.</action>
  <acceptance_criteria>
    - Test file mới tồn tại với tối thiểu 5 test cases nêu trong behavior.
    - Chạy test file này ở trạng thái RED trước khi implement.
    - Tên asserts chứa rõ key `candle_color_d1|h1|m30|m15|m5`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest D:/Aureus/services/aureus-signal/tests/test_mtf_candle_color.py -q</automated>
  </verify>
  <done>Test RED tạo đúng expectation cho candle color và null-first policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement helper MTF snapshot + GREEN cho candle color và last-closed</name>
  <files>services/aureus-signal/engine/mtf_snapshot.py, services/aureus-signal/tests/test_mtf_last_closed_rule.py</files>
  <read_first>
    - services/aureus-signal/tests/test_mtf_candle_color.py
    - services/aureus-signal/engine/signals/resampler.py
  </read_first>
  <behavior>
    - Test 1: TF > M1 dùng candle đã đóng gần nhất (iloc[-2] khi có forming) theo D-08.
    - Test 2: snapshot vẫn chạy mỗi M1, không đợi HTF đóng theo D-09.
    - Test 3: TF không đủ 2 rows khi cần last-closed => trả null theo D-10.
  </behavior>
  <action>Tạo module `mtf_snapshot.py` export các hàm `compute_candle_color`, `get_last_closed_candle`, `build_mtf_candle_color_map`. Implement logic uppercase + digits-normalize + last-closed semantics per D-01, D-03, D-08, D-09, D-10. Thêm file test_mtf_last_closed_rule.py xác nhận boundary behavior với dữ liệu resample.</action>
  <acceptance_criteria>
    - `mtf_snapshot.py` có đủ 3 hàm export theo must_haves.artifacts.
    - last-closed selection không dùng row forming cho TF>M1.
    - test_mtf_candle_color.py và test_mtf_last_closed_rule.py đều GREEN.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest D:/Aureus/services/aureus-signal/tests/test_mtf_candle_color.py D:/Aureus/services/aureus-signal/tests/test_mtf_last_closed_rule.py -q</automated>
  </verify>
  <done>Helper MTF hoạt động ổn định cho candle color + last-closed, không vi phạm D-01..D-03 và D-08..D-10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Viết RED→GREEN tests cho BB payload đa TF (upper/middle/lower)</name>
  <files>services/aureus-signal/tests/test_mtf_bb_snapshot.py, services/aureus-signal/engine/mtf_snapshot.py</files>
  <read_first>
    - services/aureus-signal/engine/mtf_snapshot.py
    - .planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: TF bắt buộc M1/M5/M15/M30/H1 đều có key `bb_<tf>` theo D-05, D-06.
    - Test 2: mỗi key bb_<tf> là object `{upper,middle,lower}` theo D-04.
    - Test 3: thiếu dữ liệu TF nào thì `bb_<tf>` = null theo D-10.
  </behavior>
  <action>Viết tests trước trong `test_mtf_bb_snapshot.py` rồi implement `build_bb_payload` trong `mtf_snapshot.py` để trả object BB đúng contract per D-04, D-05, D-06, D-10. Không đưa status rút gọn, không trả sentinel values.</action>
  <acceptance_criteria>
    - Có test xác nhận đủ 5 TF BB và đủ 3 line mỗi TF.
    - Có test null-case cho TF thiếu data.
    - Test BB chạy GREEN sau implement.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest D:/Aureus/services/aureus-signal/tests/test_mtf_bb_snapshot.py -q</automated>
  </verify>
  <done>BB payload đa TF đúng cấu trúc object và null-first policy theo quyết định đã khóa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Market OHLC input → mtf_snapshot helper | Dữ liệu nến từ stream là untrusted input, có thể thiếu hoặc méo shape |
| mtf_snapshot helper → snapshot builder | Contract key/value mới đi vào persistence path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | mtf_snapshot.py input OHLC | mitigate | Validate presence open/high/low/close trước khi classify; thiếu field trả null theo D-10 |
| T-43-02 | Integrity | last-closed selector | mitigate | Ép TF>M1 lấy candle closed, test boundary bắt buộc (D-08) |
| T-43-03 | Denial of Service | BB payload builder | accept | Scope chỉ in-memory mapping, không loop vô hạn; rủi ro thấp |
| T-43-04 | Elevation of Privilege | local helper module | accept | Không có auth boundary trong phase này |

## Severity handling
- Nếu test phát hiện vi phạm D-08 (forming candle bị dùng) hoặc D-04 (BB thiếu line) => coi là HIGH, dừng thực thi plan cho tới khi GREEN.
</threat_model>

<verification>
- Chạy ba file test mới phải pass.
- Không có assertion nào cho phép sentinel (`0`, `UNKNOWN`) thay null ở case thiếu data.
- Kiểm tra export functions trong mtf_snapshot.py đúng tên contract.
</verification>

<success_criteria>
- Bộ helper MTF đủ dùng cho wire DB ở kế hoạch sau.
- Các quyết định D-01..D-10 đã được khóa bằng test tự động.
- Không đụng vào trigger strategy hoặc notifier logic.
</success_criteria>

<output>
After completion, create `.planning/phases/43-b-sung-signal-ma-u-cu-a-n-n-sau-va-o-db-1d-h1-m30-m15-m5-the/43-01-SUMMARY.md`
</output>
