---
phase: 48-performance-backtest-api-wiring
plan: 03
type: execute
wave: 3
depends_on:
  - 48-01
  - 48-02
files_modified:
  - services/aureus-dashboard/api/main.py
  - services/aureus-dashboard/api/tests/test_performance_contract.py
  - services/aureus-dashboard/web/src/app/performance/page.tsx
  - services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx
autonomous: true
gap_closure: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06
  - PERF-07
  - PERF-08
must_haves:
  truths:
    - "Equity endpoint phản ánh cùng filter dataset với metrics/trades (symbol, strategy_id, timeframe, date-range) theo D-03, D-04."
    - "Invalid filter từ API được web parse đúng theo contract runtime và hiển thị error state rõ ràng theo D-05."
    - "Contract tests bắt được regression cho 2 gap đã nêu trong 48-VERIFICATION.md."
  artifacts:
    - path: "services/aureus-dashboard/api/main.py"
      provides: "Equity query áp dụng đầy đủ normalized filters"
      contains: "symbol/strategy/timeframe filter semantics được dùng trong truy vấn equity"
    - path: "services/aureus-dashboard/api/tests/test_performance_contract.py"
      provides: "Regression test cho equity filter-effect"
      contains: "assert equity response thay đổi theo filter dimensions"
    - path: "services/aureus-dashboard/web/src/app/performance/page.tsx"
      provides: "Error parser align với envelope backend thật"
      contains: "parse được top-level {error, code, details} và fallback compatible"
    - path: "services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx"
      provides: "Error-path test dùng backend envelope thật"
      contains: "mock 4xx envelope runtime shape"
  key_links:
    - from: "services/aureus-dashboard/api/main.py:get_equity_curve"
      to: "_normalize_performance_filters"
      via: "query predicates"
      pattern: "normalized filters được dùng thật trong SQL snapshot/fallback"
    - from: "services/aureus-dashboard/web/src/app/performance/page.tsx"
      to: "API error envelope"
      via: "parseErrorMessage"
      pattern: "đọc đúng top-level {error, code, details}"
---

<objective>
Đóng đúng 2 verification gaps còn lại của phase 48 (PERF-06, PERF-07, PERF-08) với thay đổi tối thiểu, không mở rộng scope.

Purpose: Khôi phục E2E consistency giữa API và web `/performance` theo quyết định khóa D-03/D-04/D-05.
Output: 1 plan gap-closure tập trung vào equity filter semantics + web error-envelope contract.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-performance-backtest-api-wiring/48-CONTEXT.md
@.planning/phases/48-performance-backtest-api-wiring/48-VERIFICATION.md
@.planning/phases/48-performance-backtest-api-wiring/48-01-SUMMARY.md
@.planning/phases/48-performance-backtest-api-wiring/48-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Đóng gap equity filter-effect trong API runtime path</name>
  <read_first>@services/aureus-dashboard/api/main.py @.planning/phases/48-performance-backtest-api-wiring/48-VERIFICATION.md</read_first>
  <files>services/aureus-dashboard/api/main.py, services/aureus-dashboard/api/tests/test_performance_contract.py</files>
  <acceptance_criteria>Endpoint `get_equity_curve` áp dụng cùng dimensions filter với metrics/trades (symbol, strategy_id, timeframe/date-range semantics) per D-03, D-04; có test regression chứng minh filter tác động dữ liệu equity.</acceptance_criteria>
  <action>Sửa đúng vùng `get_equity_curve` để dùng normalized filters trong query path (snapshot/fallback) theo gap #1 của 48-VERIFICATION.md; bổ sung test trong `test_performance_contract.py` xác nhận equity phản ánh filter-effect. Không thêm endpoint mới, không thêm analytics mới (deferred).</action>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-dashboard/api/tests/test_performance_contract.py -q -x -k "equity or filter"</automated>
  </verify>
  <done>Gap dữ liệu lệch metrics/trades/equity được đóng; requirement traceability cho PERF-06, PERF-07 chuyển sang pass path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Đồng bộ web error parser với envelope backend thật</name>
  <read_first>@services/aureus-dashboard/web/src/app/performance/page.tsx @services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx @.planning/phases/48-performance-backtest-api-wiring/48-VERIFICATION.md</read_first>
  <files>services/aureus-dashboard/web/src/app/performance/page.tsx, services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx</files>
  <acceptance_criteria>`parseErrorMessage` parse đúng envelope API top-level `{error, code, details}` và vẫn backward-compatible nếu gặp nested shape; contract test invalid-filter dùng đúng shape backend runtime theo D-05.</acceptance_criteria>
  <action>Cập nhật parser và test mock error-path để khớp backend envelope thật như verification chỉ ra; giữ nguyên UI architecture hiện tại, chỉ sửa tối thiểu phần parse/render lỗi và assertion test tương ứng.</action>
  <verify>
    <automated>npm --prefix /d/Aureus/services/aureus-dashboard/web run test:performance-contract</automated>
  </verify>
  <done>Web hiển thị lỗi invalid-filter đúng contract runtime, không phụ thuộc mock envelope sai shape.</done>
</task>

<task type="auto">
  <name>Task 3: Chạy verify bundle phase 48 cho gap closure</name>
  <read_first>@.planning/phases/48-performance-backtest-api-wiring/48-VERIFICATION.md @services/aureus-dashboard/api/tests/test_performance_contract.py @services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx</read_first>
  <files>services/aureus-dashboard/api/tests/test_performance_contract.py, services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx</files>
  <acceptance_criteria>Bundle verify cho API + web contract xanh, bám đúng 2 gaps cần closure; không phát sinh scope ngoài PERF-06/07/08.</acceptance_criteria>
  <action>Chạy lại test commands targeted cho API/web contract sau khi fix; đảm bảo evidence đủ để cập nhật verification từ `gaps_found` sang closure cho các gaps blocked IDs.</action>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-dashboard/api/tests/test_performance_contract.py -q -x && npm --prefix /d/Aureus/services/aureus-dashboard/web run test:performance-contract</automated>
  </verify>
  <done>Có bằng chứng tự động cho closure của 2 gap và giữ nguyên surgical scope phase 48.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web query params → API filter parser | Input từ user không tin cậy đi vào normalization/validation |
| API error envelope → web renderer | Contract mismatch có thể làm UI hiển thị sai trạng thái lỗi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-09 | T | `get_equity_curve` filter wiring | mitigate | Dùng normalized filters trực tiếp trong query path, không bỏ qua dimensions đã validate |
| T-48-10 | I | Cross-widget consistency | mitigate | Đồng bộ semantics filter metrics/trades/equity để tránh split-brain dataset |
| T-48-11 | R | Error envelope parsing | mitigate | Parse đúng contract top-level + test runtime-shape để chống regression |
</threat_model>

<verification>
- `python3 -m pytest /d/Aureus/services/aureus-dashboard/api/tests/test_performance_contract.py -q -x -k "equity or filter"`
- `npm --prefix /d/Aureus/services/aureus-dashboard/web run test:performance-contract`
- `python3 -m pytest /d/Aureus/services/aureus-dashboard/api/tests/test_performance_contract.py -q -x && npm --prefix /d/Aureus/services/aureus-dashboard/web run test:performance-contract`
</verification>

<success_criteria>
- Gap #1 (equity filter mismatch) đóng hoàn toàn, không còn partial wiring.
- Gap #2 (web error-envelope mismatch) đóng hoàn toàn, test bám contract backend thật.
- PERF-06, PERF-07, PERF-08 có đường verify tự động rõ ràng; traceability PERF-01..PERF-05 được preserve.
</success_criteria>

<output>
After completion, create `.planning/phases/48-performance-backtest-api-wiring/48-03-SUMMARY.md`
</output>
