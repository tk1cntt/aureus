---
phase: 48-performance-backtest-api-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - services/aureus-dashboard/web/package.json
  - services/aureus-dashboard/web/src/app/performance/page.tsx
  - services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx
autonomous: true
requirements:
  - PERF-08
must_haves:
  truths:
    - "Trang `/performance` hiển thị cards/table/chart từ cùng filter semantics với API sau phase 48 (per D-03, D-04)."
    - "Khi API trả invalid-filter error, web hiển thị trạng thái lỗi rõ ràng thay vì số liệu sai (per D-05)."
    - "Web parse số và nullability đúng contract, không vỡ render do NaN/toFixed trên null (per D-02)."
  artifacts:
    - path: "services/aureus-dashboard/web/src/app/performance/page.tsx"
      provides: "Contract-aligned fetch/query/error/render for performance page"
      contains: "shared query params + structured error handling"
    - path: "services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx"
      provides: "Contract tests for PERF-08 rendering and error states"
      contains: "render success path + invalid-filter error path"
    - path: "services/aureus-dashboard/web/package.json"
      provides: "Targeted test script to run performance page contract test quickly"
      contains: "test:performance-contract script"
  key_links:
    - from: "services/aureus-dashboard/web/src/app/performance/page.tsx"
      to: "/api/v1/performance/metrics|trades|equity-curve"
      via: "single query-source URLSearchParams"
      pattern: "same filter params passed to all three fetch calls"
    - from: "services/aureus-dashboard/web/src/app/performance/page.tsx"
      to: "structured API error envelope"
      via: "error-state rendering"
      pattern: "invalid filter shows explicit UI error"
---

<objective>
Hoàn tất wiring phía web `/performance` để consume đúng contract API phase 48 và xử lý lỗi filter nhất quán.

Purpose: Đóng PERF-08 ở mức runtime contract thực tế giữa UI và API, không chỉ build pass.
Output: Performance page render ổn định với success/error paths có test.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-performance-backtest-api-wiring/48-CONTEXT.md
@.planning/phases/48-performance-backtest-api-wiring/48-RESEARCH.md
@.planning/phases/48-performance-backtest-api-wiring/48-01-PLAN.md
@services/aureus-dashboard/web/src/app/performance/page.tsx

<interfaces>
From services/aureus-dashboard/web/src/app/performance/page.tsx:
- `MetricsResponse`, `TradesResponse`, `EquityCurveResponse` là contract local cho render.
- Fetch pattern hiện dùng `Promise.all([...metrics, trades, equity...])` với `URLSearchParams`.
- UI hiện dùng `.toFixed()` trực tiếp cho metrics, cần guard nullability theo D-02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Tạo test runner script cho contract test PERF-08</name>
  <read_first>@services/aureus-dashboard/web/package.json @.planning/phases/48-performance-backtest-api-wiring/48-VALIDATION.md</read_first>
  <files>services/aureus-dashboard/web/package.json</files>
  <action>Bổ sung script test targeted để chạy riêng `src/app/performance/__tests__/page.contract.test.tsx` (không dùng build cho vòng lặp nhanh), phục vụ Nyquist cho Task 1 per checker fix hint.</action>
  <verify>
    <automated>npm --prefix /d/Aureus/services/aureus-dashboard/web run test:performance-contract -- --help</automated>
  </verify>
  <done>Có script chạy test contract riêng cho `/performance`, sẵn sàng cho RED/GREEN nhanh hơn build full.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Viết contract tests cho `/performance` success + invalid-filter error</name>
  <read_first>@services/aureus-dashboard/web/src/app/performance/page.tsx @.planning/phases/48-performance-backtest-api-wiring/48-VALIDATION.md</read_first>
  <files>services/aureus-dashboard/web/src/app/performance/__tests__/page.contract.test.tsx</files>
  <acceptance_criteria>Test file cover ít nhất 2 luồng: (1) API success render cards/table/chart với dữ liệu hợp lệ, (2) API invalid-filter trả structured error thì UI hiển thị thông báo lỗi và không render số liệu sai.</acceptance_criteria>
  <behavior>
    - Test 1: same filter params được dùng khi gọi metrics/trades/equity.
    - Test 2: khi 1 endpoint trả 4xx structured error, page vào error state thống nhất.
  </behavior>
  <action>Tạo test contract cho PERF-08, mock fetch response cho 3 endpoint theo schema phase 48; assert đúng hành vi render + error-handling per D-03, D-05.</action>
  <verify>
    <automated>npm --prefix /d/Aureus/services/aureus-dashboard/web run test:performance-contract</automated>
  </verify>
  <done>Contract test tồn tại, chạy pass qua script targeted, và phản ánh rõ success/error behavior của performance page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cập nhật performance page theo contract API mới, thống nhất filter và nullability guards</name>
  <read_first>@services/aureus-dashboard/web/src/app/performance/page.tsx @.planning/phases/48-performance-backtest-api-wiring/48-01-PLAN.md</read_first>
  <files>services/aureus-dashboard/web/src/app/performance/page.tsx</files>
  <acceptance_criteria>Page dùng một nguồn filter params cho cả 3 endpoint (bao gồm timeframe nếu đã chuẩn hóa ở API per D-04), parse numeric/null explicit an toàn, hiển thị lỗi có cấu trúc khi filter invalid theo D-05, không còn render NaN/toFixed crash.</acceptance_criteria>
  <behavior>
    - Test 1: mỗi lần apply filter, cả 3 endpoint nhận cùng query dimensions.
    - Test 2: metrics nullability không gây runtime render error.
    - Test 3: invalid filter từ API hiển thị error state thay vì stale data.
  </behavior>
  <action>Chỉnh sửa tối thiểu `page.tsx` để đồng bộ contract với API đã chuẩn hóa (per D-01..D-05), bổ sung error-state rõ ràng, giữ kiến trúc hiện tại (parallel fetch + URL sync), không redesign UI và không thêm analytics mới (deferred).</action>
  <verify>
    <automated>npm --prefix /d/Aureus/services/aureus-dashboard/web run test:performance-contract && npm --prefix /d/Aureus/services/aureus-dashboard/web run build</automated>
  </verify>
  <done>`/performance` render ổn định cho cả success/error contract paths và tương thích API phase 48.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser UI → API responses | Response có thể thiếu/invalid fields hoặc lỗi 4xx/5xx |
| URL query params → client fetch | Query params từ user có thể không hợp lệ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | T | `page.tsx` query builder | mitigate | Dùng một nguồn `URLSearchParams` cho 3 endpoint, không tự ý tách semantics riêng từng call |
| T-48-06 | I | metric render pipeline | mitigate | Guard nullability/number coercion trước `.toFixed()` và fallback render an toàn |
| T-48-07 | R | UI error handling | mitigate | Chuẩn hóa hiển thị theo structured API error để tránh che lỗi thành dữ liệu stale |
| T-48-08 | D | repeated fetch on bad params | mitigate | Invalid params đưa vào error state deterministic, tránh retry vòng lặp vô hạn |
</threat_model>

<verification>
- `npm --prefix /d/Aureus/services/aureus-dashboard/web run test:performance-contract`
- `npm --prefix /d/Aureus/services/aureus-dashboard/web run build`
</verification>

<success_criteria>
- PERF-08 đạt đủ evidence tự động (targeted contract test + build).
- UI xử lý đúng 2 luồng: dữ liệu hợp lệ và invalid-filter error.
- Cùng filter => cards/table/chart không lệch semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/48-performance-backtest-api-wiring/48-02-SUMMARY.md`
</output>
