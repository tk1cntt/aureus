---
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/orders.py
  - services/aureus-signal/engine/strategies/seed_strategies.py
  - services/aureus-signal/tests/test_pivot_sl.py
requirements_addressed:
  - SL-01
  - SL-02
autonomous: true
---

# Phase 41-01: PIVOT_POINT SL Implementation

**Objective:** Thêm cơ chế Stop Loss PIVOT_POINT vào `_calculate_sl_tp()`, cho phép strategy đặt SL dựa trên swing point HH/LL gần nhất.

**Gap closure:** false

## Task 1: Method `_find_pivot_for_sl()` trong `SimulatedTradeManager`

<read_first>
- `services/aureus-signal/engine/orders.py` (line 409-504 — `_calculate_sl_tp()`, toàn bộ context)
- `services/aureus-signal/engine/state.py` (line 98 — `swing_points` initialization)
- `services/aureus-signal/engine/signals/pivots.py` (line 154-250 — swing_points merge logic, structure)
</read_first>

<action>
Thêm method mới `_find_pivot_for_sl(self, side: str, state_obj) -> Optional[float]` vào class `SimulatedTradeManager` trong file `services/aureus-signal/engine/orders.py`. Đặt ngay trước method `_calculate_sl_tp()` (khoảng line 409).

Logic method:
1. Lấy `swing_points` từ `state_obj` (list of dicts)
2. Nếu rỗng → return None
3. Xác định `is_high = ('SELL' in side)` — SELL cần HH, BUY cần LL
4. Duyệt ngược list `reversed(swing_points)` từ gần nhất → xa nhất
5. Với mỗi swing point `sp`:
   - Nếu `sp.get('is_high') != is_high` → skip
   - Nếu `sp.get('broken') is True` → skip
   - Nếu `is_high` và `sp.get('type', '').upper() != 'HH'` → skip
   - Nếu không `is_high` và `sp.get('type', '').upper() != 'LL'` → skip
   - Nếu tất cả pass → `return float(sp['price'])`
6. Không tìm được → return None

Code method:
```python
def _find_pivot_for_sl(self, side: str, state_obj: Any) -> Optional[float]:
    """Tìm swing point hợp lệ cho PIVOT_POINT SL.
    
    BUY → LL (Lower Low) gần nhất chưa broken.
    SELL → HH (Higher High) gần nhất chưa broken.
    
    Returns pivot price hoặc None nếu không tìm thấy.
    """
    swing_points = getattr(state_obj, 'swing_points', [])
    if not swing_points:
        return None
    
    is_high = ('SELL' in side)  # SELL cần HH, BUY cần LL
    
    for sp in reversed(swing_points):
        if sp.get('is_high') != is_high:
            continue
        if sp.get('broken') is True:
            continue
        sp_type = sp.get('type', '').upper()
        if is_high and sp_type != 'HH':
            continue
        if not is_high and sp_type != 'LL':
            continue
        return float(sp['price'])
    
    return None
```
</action>

<acceptance_criteria>
- `grep -c "_find_pivot_for_sl" services/aureus-signal/engine/orders.py` trả về >= 1
- Method signature chứa `(self, side: str, state_obj: Any) -> Optional[float]`
- Method nằm trong class `SimulatedTradeManager`
- Import `Optional` đã có trong file (kiểm tra imports đầu file)
</acceptance_criteria>

## Task 2: Branch PIVOT_POINT trong `_calculate_sl_tp()`

<read_first>
- `services/aureus-signal/engine/orders.py` (line 431-478 — section SL calculation trong `_calculate_sl_tp()`)
- `.planning/phases/41-b-sl-pivot-point/41-CONTEXT.md` (decisions section — config format, fallback behavior)
</read_first>

<action>
Sửa method `_calculate_sl_tp()` trong `services/aureus-signal/engine/orders.py`. Thêm branch mới `elif sl_mode == 'PIVOT_POINT':` sau branch `FIXED_PIPS` (khoảng line 447) và trước branch `SIGNAL_LOW/HIGH` (line 449).

Branch mới có logic:
1. Lấy `offset_pips = sl_cfg.get('offset_pips', 0)`
2. Tính `offset_distance = offset_pips * point_size`
3. Gọi `pivot_price = self._find_pivot_for_sl(side, state_obj)`
4. Nếu `pivot_price is None` → log warning, `return None, None` (reject trade)
5. Tính SL: `sl = pivot_price - offset_distance` nếu BUY, `sl = pivot_price + offset_distance` nếu SELL
6. Log debug với đầy đủ thông tin

Code branch:
```python
elif sl_mode == 'PIVOT_POINT':
    offset_pips = sl_cfg.get('offset_pips', 0)
    offset_distance = offset_pips * point_size
    
    pivot_price = self._find_pivot_for_sl(side, state_obj)
    if pivot_price is None:
        logger.warning(
            f"[{strategy_name}] [{symbol}] PIVOT_POINT SL: no valid swing point found "
            f"(side={side}, swing_points={len(getattr(state_obj, 'swing_points', []))})"
        )
        return None, None
    
    if 'BUY' in side:
        sl = pivot_price - offset_distance
    else:
        sl = pivot_price + offset_distance
    
    logger.debug(
        f"[{strategy_name}] [{symbol}] SL: entry={entry}, mode=PIVOT_POINT, "
        f"pivot_price={pivot_price}, offset_pips={offset_pips}, offset_distance={offset_distance}, "
        f"sl={sl}, side={side}"
    )
```
</action>

<acceptance_criteria>
- `grep "sl_mode == 'PIVOT_POINT'" services/aureus-signal/engine/orders.py` tìm thấy match
- Branch nằm trong method `_calculate_sl_tp()`, sau branch `FIXED_PIPS`
- Khi không tìm thấy pivot → `return None, None` (KHÔNG fallback FIXED_PIPS)
- Có log.warning khi reject, có log.debug khi tính thành công
- `offset_pips` default = 0 (optional trong config)
</acceptance_criteria>

## Task 3: Cập nhật seed_strategies.py — demo PIVOT_POINT

<read_first>
- `services/aureus-signal/engine/strategies/seed_strategies.py` (toàn bộ file — 8 strategies)
- `.planning/phases/41-b-sl-pivot-point/41-CONTEXT.md` (specifics section — config format)
</read_first>

<action>
Sửa file `services/aureus-signal/engine/strategies/seed_strategies.py`. Thay đổi SL config của 2 strategies demo sang PIVOT_POINT:

1. **TREND_CONT_BULL** (line 29): đổi `"sl": {"type": "FIXED_PIPS"}` thành `"sl": {"type": "PIVOT_POINT", "offset_pips": 5}`

2. **TREND_CONT_BEAR** (line 50): đổi `"sl": {"type": "FIXED_PIPS"}` thành `"sl": {"type": "PIVOT_POINT", "offset_pips": 5}`

Giữ nguyên FIXED_PIPS cho 6 strategies còn lại (ORDER_FLOW_BULL, ORDER_FLOW_BEAR, SESSION_SWEEP_BULL, SESSION_SWEEP_BEAR, CHOCH_CISD_BULL, CHOCH_CISD_BEAR).
</action>

<acceptance_criteria>
- `grep -A2 '"sl"' services/aureus-signal/engine/strategies/seed_strategies.py` có ít nhất 2 dòng chứa `"PIVOT_POINT"`
- TREND_CONT_BULL có `"sl": {"type": "PIVOT_POINT", "offset_pips": 5}`
- TREND_CONT_BEAR có `"sl": {"type": "PIVOT_POINT", "offset_pips": 5}`
- 6 strategies còn lại vẫn có `"type": "FIXED_PIPS"`
- Tổng số strategies = 8 (không thiếu, không thêm)
</acceptance_criteria>

## Task 4: Unit tests cho PIVOT_POINT SL

<read_first>
- `services/aureus-signal/engine/orders.py` (toàn bộ class SimulatedTradeManager)
- `services/aureus-signal/tests/` — đọc 1-2 file test existing để lấy pattern testing
</read_first>

<action>
Tạo file test mới `services/aureus-signal/tests/test_pivot_sl.py` với các test cases:

```python
"""Tests for PIVOT_POINT SL mechanism (Phase 41)."""
import os
import sys
import pytest
from unittest.mock import MagicMock

# Add engine package to path so imports work when running pytest from repo root
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))

from engine.orders import SimulatedTradeManager

class TestFindPivotForSl:
    """Tests for _find_pivot_for_sl() method."""
    
    def setup_method(self):
        self.tm = SimulatedTradeManager(MagicMock())
        self.state = MagicMock()
    
    def _make_swing_points(self, points):
        """Helper: tạo swing points list từ dict shorthand."""
        self.state.swing_points = points
    
    def test_buy_finds_nearest_ll(self):
        """BUY tìm LL gần nhất chưa broken."""
        self._make_swing_points([
            {"t": 100, "price": 2000.0, "is_high": False, "type": "LL", "broken": False},
            {"t": 200, "price": 2010.0, "is_high": True, "type": "HH"},
            {"t": 300, "price": 2005.0, "is_high": False, "type": "LL", "broken": False},
        ])
        result = self.tm._find_pivot_for_sl("BUY", self.state)
        assert result == 2005.0  # LL gần nhất (t=300)
    
    def test_sell_finds_nearest_hh(self):
        """SELL tìm HH gần nhất chưa broken."""
        self._make_swing_points([
            {"t": 100, "price": 2000.0, "is_high": True, "type": "HH", "broken": False},
            {"t": 200, "price": 1990.0, "is_high": False, "type": "LL"},
            {"t": 300, "price": 2015.0, "is_high": True, "type": "HH", "broken": False},
        ])
        result = self.tm._find_pivot_for_sl("SELL", self.state)
        assert result == 2015.0  # HH gần nhất (t=300)
    
    def test_skips_broken_pivots(self):
        """Bỏ qua pivot đã broken, tìm pivot trước đó."""
        self._make_swing_points([
            {"t": 100, "price": 2000.0, "is_high": False, "type": "LL", "broken": False},
            {"t": 200, "price": 2005.0, "is_high": False, "type": "LL", "broken": True},
        ])
        result = self.tm._find_pivot_for_sl("BUY", self.state)
        assert result == 2000.0  # Pivot t=200 bị broken, lùi về t=100
    
    def test_returns_none_if_no_swing_points(self):
        """Return None khi swing_points rỗng."""
        self.state.swing_points = []
        result = self.tm._find_pivot_for_sl("BUY", self.state)
        assert result is None
    
    def test_returns_none_if_no_matching_type(self):
        """Return None khi không có swing point cùng loại (HH cho SELL, LL cho BUY)."""
        self._make_swing_points([
            {"t": 100, "price": 2000.0, "is_high": True, "type": "HH"},
            {"t": 200, "price": 2010.0, "is_high": True, "type": "HH"},
        ])
        result = self.tm._find_pivot_for_sl("BUY", self.state)
        assert result is None  # Không có LL nào
    
    def test_skips_wrong_type(self):
        """Bỏ qua LH cho BUY, HL cho SELL (strict SMC)."""
        self._make_swing_points([
            {"t": 100, "price": 2005.0, "is_high": False, "type": "LH"},  # LH, không phải LL
            {"t": 200, "price": 2000.0, "is_high": False, "type": "LL", "broken": False},
        ])
        result = self.tm._find_pivot_for_sl("BUY", self.state)
        assert result == 2000.0  # Bỏ qua LH, lấy LL


class TestCalculateSlTpPivotPoint:
    """Tests for PIVOT_POINT branch trong _calculate_sl_tp()."""
    
    def setup_method(self):
        self.tm = SimulatedTradeManager(MagicMock())
        self.state = MagicMock()
        self.state.symbol = "XAUUSD"
        self.state.last_candle = {"t": 1000, "c": 2010.0, "h": 2020.0, "l": 2000.0}
        self.trigger = {"strategy": "TEST_STRAT", "side": "BUY"}
    
    def test_pivot_point_buy_sl_below_pivot(self):
        """BUY: SL = pivot_price - offset."""
        self.state.swing_points = [
            {"t": 900, "price": 2000.0, "is_high": False, "type": "LL", "broken": False},
        ]
        config = {"sl": {"type": "PIVOT_POINT", "offset_pips": 5}, "tp": {"type": "RR", "value": 2.0}}
        sl, tp = self.tm._calculate_sl_tp(self.trigger, self.state, config)
        
        # point_size cho XAUUSD từ symbols.json (thường 0.01)
        # SL = 2000.0 - (5 * 0.01) = 1999.95
        assert sl is not None
        assert sl < 2000.0  # SL phải dưới pivot
    
    def test_pivot_point_sell_sl_above_pivot(self):
        """SELL: SL = pivot_price + offset."""
        self.trigger["side"] = "SELL"
        self.state.swing_points = [
            {"t": 900, "price": 2020.0, "is_high": True, "type": "HH", "broken": False},
        ]
        config = {"sl": {"type": "PIVOT_POINT", "offset_pips": 5}, "tp": {"type": "RR", "value": 2.0}}
        sl, tp = self.tm._calculate_sl_tp(self.trigger, self.state, config)
        
        assert sl is not None
        assert sl > 2020.0  # SL phải trên pivot
    
    def test_pivot_point_no_pivot_rejects(self):
        """Không tìm thấy pivot → reject (None, None)."""
        self.state.swing_points = []
        config = {"sl": {"type": "PIVOT_POINT"}, "tp": {"type": "RR", "value": 2.0}}
        sl, tp = self.tm._calculate_sl_tp(self.trigger, self.state, config)
        assert sl is None
        assert tp is None
    
    def test_pivot_point_zero_offset(self):
        """offset_pips=0 → SL = đúng giá pivot (không buffer)."""
        self.state.swing_points = [
            {"t": 900, "price": 2000.0, "is_high": False, "type": "LL", "broken": False},
        ]
        config = {"sl": {"type": "PIVOT_POINT", "offset_pips": 0}, "tp": {"type": "RR", "value": 2.0}}
        sl, tp = self.tm._calculate_sl_tp(self.trigger, self.state, config)
        
        # SL = pivot_price - 0 = pivot_price
        assert sl == 2000.0
    
    def test_pivot_point_missing_offset_defaults_zero(self):
        """Không có offset_pips trong config → default = 0."""
        self.state.swing_points = [
            {"t": 900, "price": 2000.0, "is_high": False, "type": "LL", "broken": False},
        ]
        config = {"sl": {"type": "PIVOT_POINT"}, "tp": {"type": "RR", "value": 2.0}}
        sl, tp = self.tm._calculate_sl_tp(self.trigger, self.state, config)
        
        # SL = pivot_price - 0 = pivot_price (same as offset_pips=0)
        assert sl == 2000.0
    
    def test_fixed_pips_unchanged(self):
        """FIXED_PIPS vẫn hoạt động bình thường (backward compatibility)."""
        self.state.swing_points = []  # Không có swing points
        config = {"sl": {"type": "FIXED_PIPS"}, "tp": {"type": "RR", "value": 2.0}}
        sl, tp = self.tm._calculate_sl_tp(self.trigger, self.state, config)
        
        assert sl is not None  # FIXED_PIPS không cần swing points
        assert tp is not None
```
</action>

<acceptance_criteria>
- File `services/aureus-signal/tests/test_pivot_sl.py` tồn tại
- Có class `TestFindPivotForSl` với >= 6 test methods
- Có class `TestCalculateSlTpPivotPoint` với >= 5 test methods
- `pytest services/aureus-signal/tests/test_pivot_sl.py` pass tất cả
- Test `test_fixed_pips_unchanged` xác nhận backward compatibility
</acceptance_criteria>

## Task 5: Chạy tests và xác nhận

<read_first>
- Không cần đọc file nào thêm
</read_first>

<action>
1. Chạy `docker exec aureus-signal-dev pytest services/aureus-signal/tests/test_pivot_sl.py -v` để verify tests pass
2. Nếu test fails, debug và fix (thường là do import path hoặc mock setup)
3. Chạy thêm `docker exec aureus-signal-dev pytest services/aureus-signal/tests/ -k "test_fixed_pips" -v` để xác nhận backward compatibility
4. Chạy seed strategies: `docker exec aureus-signal-dev python engine/strategies/seed_strategies.py` để cập nhật DB với config mới
</action>

<acceptance_criteria>
- `docker exec aureus-signal-dev pytest services/aureus-signal/tests/test_pivot_sl.py -v` exit code 0
- Tất cả 11+ tests pass
- Không có test nào trong existing tests bị fail do thay đổi
- Seed strategies chạy thành công, log có "Seeded/Updated strategy: TREND_CONT_BULL/BEAR"
</acceptance_criteria>

## Verification Criteria

1. `_calculate_sl_tp()` xử lý đúng cả 3 mode: FIXED_PIPS, PIVOT_POINT, SIGNAL_LOW/HIGH
2. PIVOT_POINT SL nằm đúng vị trí: dưới LL cho BUY, trên HH cho SELL
3. Offset pips hoạt động: SL = pivot ± (offset * point_size)
4. Không có pivot → reject trade (return None, None)
5. Broken pivots bị bỏ qua
6. Chỉ nhận HH/LL (strict SMC), bỏ qua LH/HL
7. FIXED_PIPS không thay đổi behavior (backward compatible)
8. Seed strategies cập nhật TREND_CONT_BULL/BEAR sang PIVOT_POINT
9. Tất cả tests pass
