---
wave: 1
depends_on: []
files_modified:
  - services/aureus-db-writer/main.py
autonomous: true
requirements_addressed:
  - TRADE-02
---

# Phase 38 Plan 01: Fix Order Buffer Parsing in DB Writer

<objective>
Sửa `DBWriter.process_batch()` trong `aureus-db-writer/main.py` để unwrap wrapped payload (`type` + `data` JSON) từ Redis stream, fix 3 latent bugs, và implement strict timestamp validation theo decisions D-01 đến D-11.
</objective>

<current_state>
`order_buffer` trong `process_batch()` (dòng ~451-618) đọc fields trực tiếp từ top-level `payload` dict của Redis stream entry. Producer gửi wrapped payload: `{"type": "ORDER_OPEN", "data": json.dumps(order_dict)}`. Kết quả: `trace_id = None` → mọi order event bị skip + ACK → không có row nào trong `aureus_trades` → FK violation khi trade journal insert.
</current_state>

<must_haves>
- Unwrap `data` field trước khi đọc business fields
- Không map `type -> status` (D-04)
- Timestamp là required, không fallback (D-06, D-07, D-08)
- Invalid events được ACK + skip với log rõ ràng (D-09, D-10)
- Cột payload lưu canonical data, không lưu raw envelope (D-11)
</must_haves>

---

## Task 1.1: Thêm helper method `_normalize_order_payload`

<read_first>
- `services/aureus-db-writer/main.py` (dòng 451-620: order_buffer processing loop)
- `services/aureus-db-writer/main.py` (dòng 263-267: execution_buffer unwrapping pattern — reference implementation)
- `services/aureus-signal/engine/orders.py` (dòng 238-241, 327-330: producer envelope format)
- `.planning/phases/38-fix-db-writer-order-payload-parsing-for-wrapped-data-to-unblock-trade-journal-fk/38-CONTEXT.md`
- `.planning/phases/38-fix-db-writer-order-payload-parsing-for-wrapped-data-to-unblock-trade-journal-fk/38-RESEARCH.md`
</read_first>

<acceptance_criteria>
- File `services/aureus-db-writer/main.py` chứa method `_normalize_order_payload(self, payload)` trong class `DBWriter`
- Method trả về tuple `(event_type: str, order_data: dict)` — `event_type` là envelope type (ORDER_OPEN, ORDER_CLOSE...), `order_data` là dict đã unwrap từ `data` field
- Method xử lý 2 trường hợp: `data` là JSON string → `json.loads()`, `data` là dict → dùng trực tiếp
- Method raise `ValueError("Order payload data must be a JSON object")` nếu `data` không phải dict sau khi parse
- File `services/aureus-db-writer/main.py` có import `json` ở đầu file (nếu chưa có)
</acceptance_criteria>

<action>
Thêm private method `_normalize_order_payload(self, payload)` vào class `DBWriter` trong `services/aureus-db-writer/main.py`, đặt trước method `process_batch()`. Nội dung method:

```python
def _normalize_order_payload(self, payload):
    """Normalize wrapped order payload from Redis stream.
    
    Producer emits: {"type": "ORDER_OPEN", "data": json.dumps(order_dict)}
    Returns: (event_type, order_data) where order_data is the unwrapped dict.
    """
    event_type = payload.get('type', '')
    raw_data = payload.get('data')
    
    if raw_data is not None:
        if isinstance(raw_data, str):
            try:
                order_data = json.loads(raw_data)
            except (json.JSONDecodeError, TypeError) as e:
                raise ValueError(f"Failed to parse order data JSON: {e}")
        else:
            order_data = raw_data
        if not isinstance(order_data, dict):
            raise ValueError("Order payload data must be a JSON object")
    else:
        # No data field — treat entire payload as order_data (backward compat for tests)
        order_data = payload
    
    return event_type, order_data
```

Pattern này copy chính xác từ execution_buffer (dòng 263-267). Xử lý cả trường hợp `data` là string và dict. Nếu không có `data` field (back-compat cho test dùng flat dict), coi toàn bộ payload là order_data.
</action>

---

## Task 1.2: Sửa order_buffer processing loop — unwrap + map fields từ order_data

<read_first>
- `services/aureus-db-writer/main.py` (dòng 451-620: toàn bộ order_buffer section trong process_batch)
- `services/aureus-db-writer/main.py` (dòng 551: order_payload JSONB storage)
- `services/aureus-db-writer/schema.sql` (cột `aureus_trades` — field mapping)
</read_first>

<acceptance_criteria>
- `process_batch()` gọi `_normalize_order_payload(payload)` ngay sau khi lấy entry từ stream, trước khi đọc bất kỳ business field nào
- Biến `trace_id` được đọc từ `order_data.get('trace_id')`, không còn đọc từ `payload.get('trace_id')`
- Biến `direction` được đọc từ `order_data.get('side', order_data.get('direction', ''))`
- Biến `entry_type` được đọc từ `order_data.get('type', '')` hoặc `order_data.get('entry_type', '')` — không còn đọc `type` từ envelope
- Biến `status` được đọc từ `order_data.get('status', 'PENDING')` — không map từ envelope type
- Dòng `order_payload = json.dumps(order_data)` (không còn `json.dumps(payload)`) — lưu canonical data (D-11)
- Code không còn pattern `payload.get('trace_id')`, `payload.get('direction')`, `payload.get('entry_type')` trong order_buffer section
- Không có mapping tự động từ envelope `type` → `status`
</acceptance_criteria>

<action>
Trong `process_batch()` của `services/aureus-db-writer/main.py`, sửa order_buffer processing loop (bắt đầu từ dòng ~457):

**Bước 1 — Thêm normalize ngay đầu loop iteration:**
Thay thế đoạn code đọc fields trực tiếp từ `payload` bằng:

```python
# Normalize wrapped payload: extract data field, parse JSON
try:
    event_type, order_data = self._normalize_order_payload(payload)
except ValueError as e:
    logger.warning("Invalid order payload on %s msg %s: %s", stream, msg_id, e)
    rejected_msg_ids.append((stream, msg_id))
    continue

# Skip ORDER_REJECTED events (already handled by producer logging)
if event_type == 'ORDER_REJECTED':
    rejected_msg_ids.append((stream, msg_id))
    continue
```

**Bước 2 — Sửa tất cả field reads từ `payload` → `order_data`:**

| Dòng hiện tại (broken) | Sửa thành |
|---|---|
| `trace_id = payload.get('trace_id')` | `trace_id = order_data.get('trace_id')` |
| `direction = payload.get('direction', payload.get('side', ''))` | `direction = order_data.get('side', order_data.get('direction', ''))` |
| `entry_type = payload.get('entry_type', '')` | `entry_type = order_data.get('entry_type', order_data.get('type', ''))` |
| `status = payload.get('status', 'PENDING')` | `status = order_data.get('status', 'PENDING')` |
| `symbol = payload.get('symbol', '')` | `symbol = order_data.get('symbol', '')` |
| `strategy_id = payload.get('strategy_id')` | `strategy_id = order_data.get('strategy_id')` |
| `strategy_name = payload.get('strategy_name', '')` | `strategy_name = order_data.get('strategy_name', '')` |
| `entry_price = payload.get('entry_price')` | `entry_price = order_data.get('entry_price')` |
| `sl = payload.get('sl')` | `sl = order_data.get('sl')` |
| `tp = payload.get('tp')` | `tp = order_data.get('tp')` |
| `pnl = payload.get('pnl')` | `pnl = order_data.get('pnl')` |
| `exit_price = payload.get('exit_price')` | `exit_price = order_data.get('exit_price')` |
| `ticket = payload.get('ticket')` | `ticket = order_data.get('ticket')` |
| `magic_number = payload.get('magic_number')` | `magic_number = order_data.get('magic_number')` |
| `volume = payload.get('volume')` | `volume = order_data.get('volume')` |
| `order_payload = json.dumps(payload)` | `order_payload = json.dumps(order_data)` |

**Bước 3 — Status normalization cho state machine compatibility:**
Sau khi đọc `status` từ `order_data`, thêm normalization:

```python
# Normalize producer status values to state machine compatible values
# PENDING_AI and ACTIVE are both treated as PENDING for initial INSERT
if status in ('PENDING_AI', 'ACTIVE'):
    status = 'PENDING'
```

**Bước 4 — Giữ nguyên logic state machine, INSERT, UPDATE còn lại.** Không sửa `state_machine.validate_transition()`, INSERT query, hay ACK flow — chỉ thay đổi nguồn data.
</action>

---

## Task 1.3: Fix bug `ack_skip_msg_ids` undefined variable

<read_first>
- `services/aureus-db-writer/main.py` (dòng 515-530: state machine validation section)
- `services/aureus-db-writer/main.py` (dòng 451-470: variable declarations — `rejected_msg_ids` được khai báo ở đây)
</read_first>

<acceptance_criteria>
- File `services/aureus-db-writer/main.py` không còn string `ack_skip_msg_ids` anywhere trong order_buffer section
- Dòng ~519: `rejected_msg_ids.append((stream, msg_id))` thay cho `ack_skip_msg_ids.append((stream, msg_id))`
- Dòng ~528: `rejected_msg_ids.append((stream, msg_id))` thay cho `ack_skip_msg_ids.append((stream, msg_id))`
- Python syntax valid (không có NameError khi import module)
</acceptance_criteria>

<action>
Trong `services/aureus-db-writer/main.py`, thay thế toàn bộ 2 occurrences của `ack_skip_msg_ids` bằng `rejected_msg_ids`:

1. Dòng ~519: `ack_skip_msg_ids.append((stream, msg_id))` → `rejected_msg_ids.append((stream, msg_id))`
2. Dòng ~528: `ack_skip_msg_ids.append((stream, msg_id))` → `rejected_msg_ids.append((stream, msg_id))`

Đây là biến chưa được khai báo (chưa có `ack_skip_msg_ids: list` nào trong scope). Biến `rejected_msg_ids` đã được khai báo ở đầu order_buffer section (dòng ~456).
</action>

---

## Task 1.4: Strict timestamp validation — reject nếu thiếu `open_time`

<read_first>
- `services/aureus-db-writer/main.py` (dòng 532-545: `parse_ts()` helper closure)
- `.planning/phases/38-fix-db-writer-order-payload-parsing-for-wrapped-data-to-unblock-trade-journal-fk/38-CONTEXT.md` (D-06, D-07, D-08)
- `.planning/phases/38-fix-db-writer-order-payload-parsing-for-wrapped-data-to-unblock-trade-journal-fk/38-RESEARCH.md` (Pitfall 2, Timestamp handling section)
</read_first>

<acceptance_criteria>
- Code có kiểm tra `open_time = order_data.get('open_time')` sau normalize
- Nếu `open_time` là `None` hoặc rỗng → reject event: log warning reason="missing_timestamp", append to `rejected_msg_ids`, continue
- Không có fallback timestamp từ `msg_id` hoặc bất kỳ nguồn nào khác
- Warning log chứa string `"Rejecting order event — missing required timestamp: trace_id="`
- Event có timestamp hợp lệ được xử lý bình thường qua `parse_ts(open_time)` cho `filled_at`
</acceptance_criteria>

<action>
Trong order_buffer processing loop, sau khi normalize và đọc fields, thêm validation block trước khi INSERT:

```python
# Strict timestamp validation (D-06, D-07, D-08)
open_time = order_data.get('open_time')
if open_time is None or open_time == '':
    logger.warning(
        "Rejecting order event — missing required timestamp: trace_id=%s event_type=%s msg_id=%s",
        trace_id, event_type, msg_id
    )
    rejected_msg_ids.append((stream, msg_id))
    continue

# Parse timestamp using existing helper
filled_at = parse_ts(open_time)
```

Không thêm bất kỳ fallback nào. Đây là hard requirement theo D-07/D-08.
</action>

---

## Task 1.5: Sửa `check_xpending()` recovery — unwrap قبل khi append vào order_buffer

<read_first>
- `services/aureus-db-writer/main.py` (dòng 621-688: `check_xpending()` method)
- `services/aureus-db-writer/main.py` (dòng 670-671: nơi append wrapped payload vào order_buffer)
- `services/aureus-db-writer/main.py` (`_normalize_order_payload` method đã tạo ở Task 1.1)
</read_first>

<acceptance_criteria>
- Method `check_xpending()` unwrap `data` field trước khi append vào `self.order_buffer`
- Code block dòng ~670-671 thay vì `self.order_buffer.append((stream, msg_id, payload))` → parse `data` field, append unwrapped version
- Recovery messages từ XPENDING được xử lý đúng format như messages mới từ stream
- Không thay đổi logic cho các stream type khác (execution, position, account)
</acceptance_criteria>

<action>
Trong `check_xpending()` của `services/aureus-db-writer/main.py`, sửa đoạn xử lý order events (dòng ~670-671):

Thay thế:
```python
if event_type in ('ORDER_PENDING', 'ORDER_OPEN', 'ORDER_CLOSE'):
    self.order_buffer.append((stream, msg_id, payload))
```

Bằng:
```python
if event_type in ('ORDER_PENDING', 'ORDER_OPEN', 'ORDER_CLOSE'):
    # Unwrap data field before adding to buffer (same normalization as process_batch)
    try:
        raw_data = payload.get('data')
        if raw_data is not None:
            if isinstance(raw_data, str):
                order_data = json.loads(raw_data)
            else:
                order_data = raw_data
            if isinstance(order_data, dict):
                self.order_buffer.append((stream, msg_id, payload))
            else:
                logger.warning("XPENDING order recovery — non-dict data on %s msg %s, skipping", stream, msg_id)
        else:
            self.order_buffer.append((stream, msg_id, payload))  # back-compat
    except (json.JSONDecodeError, TypeError) as e:
        logger.warning("XPENDING order recovery — unparseable data on %s msg %s: %s", stream, msg_id, e)
```

Cách tiếp cận: unwrap trong `check_xpending()` để buffer chứa unwrapped data, `process_batch()` sẽ nhận được format đúng. Nếu parse thất bại → skip (ACK implicitly vì không append vào buffer).
</action>

---

## Verification Criteria

1. **Syntax check:** `python -m py_compile services/aureus-db-writer/main.py` → exit 0
2. **Unwrap function exists:** `grep "_normalize_order_payload" services/aureus-db-writer/main.py` → ít nhất 2 matches (definition + call site)
3. **No broken variable:** `grep "ack_skip_msg_ids" services/aureus-db-writer/main.py` → 0 matches
4. **No top-level field reads:** `grep "payload.get('trace_id')" services/aureus-db-writer/main.py` → 0 matches (trong order_buffer section)
5. **No envelope type collision:** `grep "payload.get('type', '')"` → vẫn có (cần cho event_type routing), nhưng business fields đều đọc từ `order_data`
6. **Timestamp validation:** `grep "missing required timestamp" services/aureus-db-writer/main.py` → 1 match
7. **Canonical payload:** `grep "json.dumps(order_data)" services/aureus-db-writer/main.py` → ít nhất 1 match trong order_buffer section
8. **Status normalization:** `grep "PENDING_AI" services/aureus-db-writer/main.py` → có logic normalize, không có mapping `type -> status`
