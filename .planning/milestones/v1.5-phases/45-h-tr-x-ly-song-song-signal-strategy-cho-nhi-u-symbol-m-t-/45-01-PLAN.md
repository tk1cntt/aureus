---
phase: 45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/live_engine.py
  - services/aureus-signal/engine/symbol_runtime.py
  - services/aureus-signal/tests/test_per_symbol_worker_runtime.py
  - services/aureus-signal/tests/test_out_of_order_drop_policy.py
autonomous: true
requirements:
  - PH45-01
  - PH45-02
  - PH45-03
must_haves:
  truths:
    - "Mỗi symbol được xử lý song song bởi worker riêng, không còn nghẽn toàn cục theo vòng lặp tuần tự."
    - "Trong mỗi symbol, candle được xử lý FIFO strict theo t; candle trễ/out-of-order bị drop và ack đúng."
    - "Khi quá tải, hệ thống queue per-symbol thay vì drop, ưu tiên correctness."
  artifacts:
    - path: services/aureus-signal/engine/symbol_runtime.py
      provides: Worker lifecycle per-symbol (queue, task, ingest, stop)
    - path: services/aureus-signal/engine/live_engine.py
      provides: Router stream -> queue per-symbol + FIFO/out-of-order guard
    - path: services/aureus-signal/tests/test_per_symbol_worker_runtime.py
      provides: Test song song worker isolation và backlog behavior
    - path: services/aureus-signal/tests/test_out_of_order_drop_policy.py
      provides: Test drop/ack out-of-order theo policy
  key_links:
    - from: services/aureus-signal/engine/live_engine.py
      to: services/aureus-signal/engine/symbol_runtime.py
      via: enqueue candle theo symbol
      pattern: "enqueue|worker|queue"
    - from: services/aureus-signal/engine/live_engine.py
      to: Redis streams per-symbol
      via: xreadgroup + xack routing
      pattern: "aureus:stream:{symbol}:candle"
---

<objective>
Thiết lập lõi per-symbol worker cho signal engine để xử lý đa symbol song song, giữ FIFO strict theo symbol và drop out-of-order theo quyết định người dùng.

Purpose: Gỡ bottleneck tuần tự hiện tại và bảo toàn correctness trading ở tầng signal trước khi wire đầy đủ sang strategy.
Output: Runtime worker per-symbol + test contracts cho isolation/FIFO/drop-policy.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-CONTEXT.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-RESEARCH.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VALIDATION.md
@services/aureus-signal/engine/live_engine.py
@services/aureus-signal/tests/test_multi_symbol.py

<interfaces>
From services/aureus-signal/engine/live_engine.py:
```python
def execute_signals_for_candle(signals: dict, df: Any, state: Any, symbol: str, redis_client: Any) -> None
async def run_signal_engine(...)
```

From services/aureus-signal/tests/test_multi_symbol.py:
```python
symbol_locks = {s: asyncio.Lock() for s in symbols_list}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tạo test contracts cho per-symbol worker runtime</name>
  <files>services/aureus-signal/tests/test_per_symbol_worker_runtime.py, services/aureus-signal/tests/test_out_of_order_drop_policy.py</files>
  <read_first>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/tests/test_multi_symbol.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-CONTEXT.md
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: Hai symbol chạy đồng thời không block nhau (per D-01, D-03).
    - Test 2: Trong mỗi symbol, queue xử lý FIFO strict theo candle t (per D-05).
    - Test 3: Candle có `ts_unix <= last_executed_candle_t` bị drop + ack, không reorder (per D-06).
  </behavior>
  <action>Viết trước test cho runtime per-symbol worker theo D-01/D-03/D-05/D-06. Test phải mô phỏng ít nhất 2 symbol (`XAUUSD`, `EURUSD`) với sequence candle xen kẽ; verify thứ tự xử lý trong từng symbol giữ nguyên theo t, và candle trễ bị ack/drop ngay. Không dùng global queue chung; test fail nếu xử lý lệch FIFO hoặc nếu một symbol bị block bởi symbol khác.</action>
  <acceptance_criteria>
- Có file `test_per_symbol_worker_runtime.py` với case isolation đa symbol và FIFO per-symbol.
- Có file `test_out_of_order_drop_policy.py` với case drop+ack khi `ts_unix <= last_executed_candle_t`.
- Chạy test mới phải fail khi runtime chưa có per-symbol worker.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_per_symbol_worker_runtime.py /d/Aureus/services/aureus-signal/tests/test_out_of_order_drop_policy.py -q -x</automated>
  </verify>
  <done>Contract tests khóa behavior D-01, D-03, D-05, D-06 trước khi triển khai runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement signal router + per-symbol worker queue</name>
  <files>services/aureus-signal/engine/symbol_runtime.py, services/aureus-signal/engine/live_engine.py</files>
  <read_first>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/tests/test_per_symbol_worker_runtime.py
- services/aureus-signal/tests/test_out_of_order_drop_policy.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-RESEARCH.md
  </read_first>
  <action>Tạo `symbol_runtime.py` để quản lý worker lifecycle per-symbol (queue, task loop, graceful stop) theo D-01. Cập nhật `live_engine.py` để main loop chỉ làm router: đọc stream per-symbol rồi enqueue vào queue symbol tương ứng, xử lý signal bên trong worker của symbol đó (D-02, D-03). Bắt buộc FIFO strict trong queue mỗi symbol (D-05), áp dụng guard drop out-of-order `ts_unix <= last_executed_candle_t` và `xack` ngay (D-06). Khi backlog tăng, tiếp tục enqueue/đợi xử lý, không drop vì realtime (D-04). Không đổi contract payload downstream ở task này.</action>
  <acceptance_criteria>
- `symbol_runtime.py` tồn tại và có runtime class/function quản lý queue + worker theo symbol.
- `live_engine.py` có router -> enqueue thay cho xử lý tuần tự toàn cục.
- Guard out-of-order trong luồng worker thực thi đúng điều kiện `<=` và có ack.
- Không có global lock làm serialize toàn bộ symbols.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_per_symbol_worker_runtime.py /d/Aureus/services/aureus-signal/tests/test_out_of_order_drop_policy.py /d/Aureus/services/aureus-signal/tests/test_multi_symbol.py -q -x</automated>
  </verify>
  <done>Signal engine chạy song song per-symbol với FIFO/drop-policy đúng quyết định D-01..D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis candle stream -> signal router | Payload không tin cậy đi vào runtime queue/worker |
| signal worker per-symbol -> downstream state | Sai thứ tự/ack có thể gây sai state và duplicate xử lý |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | T | live_engine out-of-order handling | mitigate | Guard `ts_unix <= last_executed_candle_t` drop+ack bắt buộc, test contract dedicated |
| T-45-02 | D | global processing loop | mitigate | Tách router + worker per-symbol để cô lập nghẽn/noisy neighbor |
| T-45-03 | R | enqueue/ack sequence | mitigate | Chỉ ack sau khi policy xử lý rõ ràng (drop hoặc xử lý xong), log symbol+t+entry_id |
</threat_model>

<verification>
- Chạy GitNexus impact analysis upstream cho mọi symbol/hàm sẽ sửa trong plan này và ghi blast radius trước khi code.
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_per_symbol_worker_runtime.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_out_of_order_drop_policy.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_multi_symbol.py -q
- Chạy gitnexus_detect_changes() trước commit để xác nhận phạm vi thay đổi đúng plan.
</verification>

<success_criteria>
- D-01..D-06 được phủ đầy đủ ở signal layer, không còn xử lý tuần tự toàn cục cho multi-symbol.
- Các test Wave 0 liên quan worker/drop-policy đều xanh.
- Không có thay đổi ngoài scope runtime song song signal.
</success_criteria>

<output>
After completion, create `.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-01-SUMMARY.md`
</output>
