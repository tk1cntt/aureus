---
phase: 45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-
plan: 03
type: execute
wave: 3
depends_on:
  - 45-01
  - 45-02
files_modified:
  - services/aureus-signal/engine/symbol_runtime.py
  - services/aureus-signal/engine/live_engine.py
  - services/aureus-signal/engine/strategy_executor.py
  - services/aureus-signal/tests/test_circuit_breaker.py
  - services/aureus-signal/tests/test_symbol_slo_rollback.py
  - services/aureus-signal/tests/test_live_engine_shadow_mode.py
autonomous: false
requirements:
  - PH45-06
  - PH45-07
must_haves:
  truths:
    - "Mỗi symbol có circuit-breaker/backlog threshold riêng; symbol lỗi bị cô lập, symbol khác vẫn xử lý."
    - "Rollout chạy đúng chuỗi Shadow -> Canary -> Full, có thể quan sát rõ theo symbol."
    - "Khi vi phạm SLO liên tiếp theo symbol, runtime tự rollback symbol đó về chế độ tuần tự an toàn."
  artifacts:
    - path: services/aureus-signal/engine/symbol_runtime.py
      provides: Per-symbol circuit-breaker, backlog/SLO counters, rollback state
    - path: services/aureus-signal/tests/test_symbol_slo_rollback.py
      provides: Contract test auto-rollback theo SLO liên tiếp
    - path: services/aureus-signal/tests/test_live_engine_shadow_mode.py
      provides: Test rollout gating shadow/canary/full theo symbol
  key_links:
    - from: services/aureus-signal/engine/symbol_runtime.py
      to: services/aureus-signal/engine/live_engine.py
      via: symbol mode flag + fallback route
      pattern: "shadow|canary|full|fallback"
    - from: services/aureus-signal/engine/symbol_runtime.py
      to: services/aureus-signal/engine/strategy_executor.py
      via: shared per-symbol health/rollout control
      pattern: "slo|lag|backlog|error_rate"
---

<objective>
Hoàn thiện safety layer cho song song multi-symbol: circuit-breaker per-symbol, backlog/SLO guard, rollout Shadow→Canary→Full và auto rollback theo symbol.

Purpose: Đảm bảo rollout an toàn theo đúng quyết định người dùng, tránh sự cố một symbol làm ảnh hưởng toàn hệ thống.
Output: Runtime guard per-symbol + test rollback/rollout + checkpoint xác nhận vận hành.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-CONTEXT.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-RESEARCH.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VALIDATION.md
@.planning/phases/44-profiling-baseline-performance/44-02-SUMMARY.md
@services/aureus-signal/common/circuit_breaker.py
@services/aureus-signal/tests/test_circuit_breaker.py

<interfaces>
From services/aureus-signal/common/circuit_breaker.py:
```python
class CircuitBreaker:
    def allow_request(self) -> bool
    def record_success(self) -> None
    def record_failure(self) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement per-symbol breaker/backlog/SLO rollback contracts + runtime</name>
  <files>services/aureus-signal/engine/symbol_runtime.py, services/aureus-signal/engine/live_engine.py, services/aureus-signal/engine/strategy_executor.py, services/aureus-signal/tests/test_circuit_breaker.py, services/aureus-signal/tests/test_symbol_slo_rollback.py</files>
  <read_first>
- services/aureus-signal/common/circuit_breaker.py
- services/aureus-signal/engine/symbol_runtime.py
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/strategy_executor.py
- services/aureus-signal/tests/test_circuit_breaker.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Breaker mở ở symbol A không dừng symbol B (per D-09).
    - Test 2: Backlog threshold theo symbol phát cảnh báo đúng ngưỡng (per D-10).
    - Test 3: Vi phạm SLO liên tiếp (lag/backlog/error-rate) thì rollback symbol đó về tuần tự (per D-12).
  </behavior>
  <action>Thiết kế runtime health per-symbol trong `symbol_runtime.py` dùng circuit breaker riêng từng symbol (D-09) và backlog threshold theo symbol (D-10). Chốt ngưỡng cụ thể (Claude discretion) để bắt đầu an toàn: `lag_p95_ms > 2000` liên tiếp 3 phút, `queue_depth > 200` liên tiếp 3 phút, hoặc `error_rate > 5%` liên tiếp 3 phút thì fallback symbol đó về mode tuần tự; thoát fallback khi cả 3 chỉ số dưới 50% ngưỡng trong 5 phút liên tiếp (hysteresis). Wire trạng thái này vào `live_engine.py` và `strategy_executor.py` để chỉ hạ mode symbol vi phạm, không hạ toàn cục (D-12). Bổ sung/điều chỉnh test breaker + test rollback để khóa contract trên.</action>
  <acceptance_criteria>
- Có state per-symbol cho breaker, backlog, SLO counters và fallback mode.
- Có test chứng minh symbol vi phạm bị rollback độc lập, symbol khác vẫn parallel.
- Ngưỡng SLO/hysteresis được khai báo rõ giá trị số trong code/config, không dùng mệnh đề mơ hồ.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_circuit_breaker.py /d/Aureus/services/aureus-signal/tests/test_symbol_slo_rollback.py -q -x</automated>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_multi_symbol.py -q -x</automated>
  </verify>
  <done>Cơ chế D-09, D-10, D-12 chạy đúng và được khóa bằng test tự động.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire rollout Shadow→Canary→Full theo symbol với auto rollback hooks</name>
  <files>services/aureus-signal/engine/live_engine.py, services/aureus-signal/tests/test_live_engine_shadow_mode.py</files>
  <read_first>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/tests/test_live_engine_shadow_mode.py
- services/aureus-signal/engine/symbol_runtime.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-RESEARCH.md
  </read_first>
  <action>Mở rộng rollout flags per-symbol theo pipeline D-11: `shadow` (chạy song song new path + baseline để so kết quả), `canary` (bật xử lý parallel thực cho subset symbol), `full` (bật toàn bộ symbol đạt SLO). Tích hợp hook rollback tự động từ task 1 để symbol đang canary/full vi phạm SLO quay về tuần tự ngay (D-12). Cập nhật test shadow mode để kiểm tra transition hợp lệ `shadow -> canary -> full` và transition lỗi `* -> fallback_serial` khi breach. Không thêm rollout mode ngoài 3 mode đã khóa bởi user.</action>
  <acceptance_criteria>
- Có mapping mode rollout per-symbol với đúng 3 trạng thái shadow/canary/full.
- Test xác minh transition hợp lệ và transition rollback khi breach.
- Không có rollback global mặc định; chỉ rollback symbol vi phạm.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_live_engine_shadow_mode.py /d/Aureus/services/aureus-signal/tests/test_symbol_slo_rollback.py -q -x</automated>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests -q</automated>
  </verify>
  <done>Rollout D-11 hoàn chỉnh và gắn auto rollback D-12 theo symbol.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Xác nhận rollout vận hành thực tế trên môi trường staging</name>
  <read_first>
- services/aureus-signal/tests/test_live_engine_shadow_mode.py
- services/aureus-signal/tests/test_symbol_slo_rollback.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VALIDATION.md
  </read_first>
  <action>Claude phải chạy đầy đủ test tự động trước checkpoint, sau đó cung cấp script/command bật mode shadow/canary/full theo symbol để user xác nhận vận hành thực tế ở staging. Dùng đúng ngưỡng SLO đã chốt ở Task 1 và không thay đổi thủ công giữa lúc verify để đảm bảo quan sát nhất quán.</action>
  <what-built>Claude đã tự động hóa rollout per-symbol (shadow/canary/full), breaker/backlog/SLO guard và auto rollback theo symbol.</what-built>
  <how-to-verify>
1) Chạy signal service ở staging với ít nhất 2 symbol active.
2) Đặt 1 symbol ở canary, 1 symbol ở full; quan sát metrics backlog/lag/error-rate theo symbol.
3) Tạo tình huống overload có kiểm soát ở 1 symbol (ví dụ feed burst) và xác nhận chỉ symbol đó rollback về tuần tự.
4) Xác nhận symbol còn lại tiếp tục xử lý parallel bình thường.
5) Kiểm tra log transition có đầy đủ mode trước/sau + lý do breach.
  </how-to-verify>
  <acceptance_criteria>
- Có bằng chứng log/metric cho chuỗi shadow->canary->full.
- Có bằng chứng rollback chỉ tác động symbol vi phạm SLO.
- Không có dấu hiệu dừng xử lý toàn cục khi một symbol lỗi.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_circuit_breaker.py /d/Aureus/services/aureus-signal/tests/test_symbol_slo_rollback.py /d/Aureus/services/aureus-signal/tests/test_live_engine_shadow_mode.py -q -x</automated>
  </verify>
  <done>User xác nhận rollout staging đạt tiêu chí acceptance hoặc trả về issue cụ thể để sửa vòng kế tiếp.</done>
  <resume-signal>Type "approved" nếu đạt, hoặc gửi danh sách issue cụ thể để Claude sửa tiếp.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| symbol runtime metrics -> rollout decisions | SLO signal sai có thể trigger rollback sai phạm vi |
| per-symbol breaker -> processing mode | Cơ chế bảo vệ có thể bị dùng như global kill-switch nếu wire sai |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-07 | D | per-symbol backlog overload | mitigate | Queue/backlog threshold per-symbol + breaker riêng để cô lập nghẽn |
| T-45-08 | E | rollout mode control | mitigate | Chỉ cho phép transition shadow/canary/full/fallback_serial theo state machine có test |
| T-45-09 | R | rollback auditability | mitigate | Ghi log bắt buộc mode_before/mode_after/symbol/slo_reason/consecutive_minutes |
</threat_model>

<verification>
- Chạy GitNexus impact analysis upstream cho mọi symbol/hàm sẽ sửa trong plan này và ghi blast radius trước khi code.
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_circuit_breaker.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_symbol_slo_rollback.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_live_engine_shadow_mode.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests -q
- Chạy gitnexus_detect_changes() trước commit để xác nhận phạm vi thay đổi đúng plan.
</verification>

<success_criteria>
- D-09..D-12 được triển khai đủ: breaker/backlog/SLO per-symbol + rollout + rollback tự động.
- Có test tự động chứng minh rollback cục bộ theo symbol, không rollback global.
- Checkpoint human-verify xác nhận hành vi rollout runtime ở staging.
</success_criteria>

<output>
After completion, create `.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-03-SUMMARY.md`
</output>
