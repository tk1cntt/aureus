---
phase: 45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/live_engine.py
  - services/aureus-signal/engine/symbol_runtime.py
  - services/aureus-signal/tests/test_per_symbol_worker_runtime.py
  - services/aureus-signal/tests/test_out_of_order_drop_policy.py
  - services/aureus-signal/tests/test_live_engine_shadow_mode.py
autonomous: true
gap_closure: true
requirements:
  - PH45-01
  - PH45-02
  - PH45-03
  - PH45-06
  - PH45-07
must_haves:
  truths:
    - "run_signal_engine thực sự dùng PerSymbolWorkerRuntime trên đường chạy production."
    - "FIFO strict và out-of-order drop policy trong symbol_runtime là enforcement path runtime thật."
    - "Signal runtime cập nhật health/rollout state per-symbol để mode transition có hiệu lực thực thi."
  artifacts:
    - path: services/aureus-signal/engine/live_engine.py
      provides: instantiate runtime, enqueue flow, health/rollout update calls
    - path: services/aureus-signal/engine/symbol_runtime.py
      provides: worker FIFO/drop policy + health manager interfaces consumed by runtime loop
    - path: services/aureus-signal/tests/test_out_of_order_drop_policy.py
      provides: regression test cho runtime path enforcement
  key_links:
    - from: services/aureus-signal/engine/live_engine.py
      to: services/aureus-signal/engine/symbol_runtime.py
      via: PerSymbolWorkerRuntime(...).enqueue(...)
      pattern: "PerSymbolWorkerRuntime|enqueue|build_symbol_work_item"
    - from: services/aureus-signal/engine/live_engine.py
      to: services/aureus-signal/engine/symbol_runtime.py
      via: SymbolRuntimeHealthManager metrics/mode update
      pattern: "update_symbol_metrics|resolve_symbol_processing_mode"
---

<objective>
Đóng gap production wiring ở signal path: runtime phải dùng PerSymbolWorkerRuntime thật, FIFO/drop policy phải là enforcement path, và health/rollout phải ảnh hưởng mode xử lý theo symbol.

Purpose: Khóa các gap blocker trong 45-VERIFICATION để PH45-01/02/03/06/07 đạt ở runtime thực tế thay vì chỉ ở unit primitive.
Output: Signal runtime wired end-to-end vào symbol_runtime với test chứng minh đường chạy thật.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-CONTEXT.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VERIFICATION.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-01-SUMMARY.md
@.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-03-SUMMARY.md
@services/aureus-signal/engine/live_engine.py
@services/aureus-signal/engine/symbol_runtime.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire PerSymbolWorkerRuntime vào run_signal_engine production loop</name>
  <files>services/aureus-signal/engine/live_engine.py, services/aureus-signal/tests/test_per_symbol_worker_runtime.py</files>
  <read_first>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/symbol_runtime.py
- services/aureus-signal/tests/test_per_symbol_worker_runtime.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
- Có instantiate PerSymbolWorkerRuntime trong run_signal_engine (grep: "PerSymbolWorkerRuntime(").
- Entry từ stream candle được route vào runtime queue theo symbol thay vì xử lý trực tiếp loop cũ (grep: "enqueue(" trong live loop).
- Test runtime path fail trước/success sau khi wire.
  </acceptance_criteria>
  <action>Trong `run_signal_engine`, khởi tạo `PerSymbolWorkerRuntime` và chuyển đường xử lý candle sang enqueue theo symbol (per D-01, D-02, D-03). Xóa/khóa nhánh xử lý trực tiếp trong main loop để tránh dual-path drift. Giữ semantics hiện tại của payload/ack và không thay đổi scope ngoài signal path. Reference gap: "PerSymbolWorkerRuntime tồn tại nhưng chưa wire vào run_signal_engine".</action>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_per_symbol_worker_runtime.py -q -x</automated>
  </verify>
  <done>Production signal path thực sự chạy qua worker runtime per-symbol thay vì loop tuần tự trước đó.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ép FIFO/out-of-order policy trong symbol_runtime thành enforcement runtime path</name>
  <files>services/aureus-signal/engine/live_engine.py, services/aureus-signal/engine/symbol_runtime.py, services/aureus-signal/tests/test_out_of_order_drop_policy.py</files>
  <read_first>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/symbol_runtime.py
- services/aureus-signal/tests/test_out_of_order_drop_policy.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-CONTEXT.md
  </read_first>
  <acceptance_criteria>
- Guard out-of-order nằm trong worker runtime path và được gọi từ production loop (grep: "ts_unix <= last_executed_candle_t").
- Không còn policy duplicate ở state.tracking_vars main loop gây lệch enforcement path.
- Test out-of-order drop policy pass với runtime wired flow.
  </acceptance_criteria>
  <action>Chuẩn hóa enforcement D-05/D-06: FIFO strict + drop out-of-order chỉ được quyết định ở `symbol_runtime` worker path, và `live_engine` chỉ route+ack theo kết quả runtime. Loại bỏ logic guard song song ở main loop nếu gây mâu thuẫn nguồn sự thật. Đảm bảo policy là runtime-path thật, không chỉ class-level contract.</action>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_out_of_order_drop_policy.py /d/Aureus/services/aureus-signal/tests/test_multi_symbol.py -q -x</automated>
  </verify>
  <done>FIFO/drop policy được enforce trong đường chạy production đã wire, closing PH45-02/03 gaps.</done>
</task>

<task type="auto">
  <name>Task 3: Wire health manager + rollout mode vào signal runtime loop</name>
  <files>services/aureus-signal/engine/live_engine.py, services/aureus-signal/engine/symbol_runtime.py, services/aureus-signal/tests/test_live_engine_shadow_mode.py</files>
  <read_first>
- services/aureus-signal/engine/live_engine.py
- services/aureus-signal/engine/symbol_runtime.py
- services/aureus-signal/tests/test_live_engine_shadow_mode.py
- .planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
- Signal loop có update_symbol_metrics/get_symbol_status/resolve mode hooks được gọi theo symbol (grep các hàm tương ứng).
- Mode transition (shadow/canary/full/fallback_serial) ảnh hưởng nhánh xử lý thực tế của signal runtime.
- Test shadow mode pass với runtime integration, không chỉ helper-level.
  </acceptance_criteria>
  <action>Gắn `SymbolRuntimeHealthManager` trực tiếp vào signal runtime loop để D-09..D-12 có hiệu lực thật: cập nhật metrics per-symbol sau mỗi work item, resolve mode trước xử lý, và áp dụng fallback_serial đúng symbol khi breach SLO. Không rollback global; mọi transition scoped theo symbol (per D-09, D-10, D-11, D-12).</action>
  <verify>
    <automated>python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_live_engine_shadow_mode.py /d/Aureus/services/aureus-signal/tests/test_symbol_slo_rollback.py -q -x</automated>
  </verify>
  <done>Signal runtime đã wire health/rollout end-to-end, closing runtime disconnection gap ở live_engine.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis candle stream -> signal runtime | Input có thể out-of-order/replay, cần policy cưỡng bức đúng nơi thực thi |
| signal runtime metrics -> rollout mode | Sai wiring có thể làm rollback không đúng symbol hoặc không kích hoạt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04-01 | T | out-of-order enforcement | mitigate | Enforce guard duy nhất ở symbol_runtime worker path, test runtime-path bắt buộc |
| T-45-04-02 | D | sequential bottleneck | mitigate | Router->per-symbol queue, isolate processing per symbol |
| T-45-04-03 | E | rollout mode bypass | mitigate | Mode resolve trong runtime loop trước xử lý mỗi symbol work item |
</threat_model>

<verification>
- Bắt buộc chạy GitNexus impact analysis cho các symbol sẽ sửa trước khi code.
- Bắt buộc chạy `gitnexus_detect_changes({scope: "staged"})` (hoặc CLI tương đương) ngay trước commit để xác nhận phạm vi thay đổi đúng expected scope.
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_per_symbol_worker_runtime.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_out_of_order_drop_policy.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_live_engine_shadow_mode.py -q
- python3 -m pytest /d/Aureus/services/aureus-signal/tests/test_symbol_slo_rollback.py -q
</verification>

<success_criteria>
- Gap 1 và gap 2 trong 45-VERIFICATION.md được đóng bằng runtime evidence.
- Signal-side phần của gap 3 được đóng: rollout/health transition có hiệu lực trên production path.
- Không thêm scope ngoài runtime wiring đã nêu.
</success_criteria>

<output>
After completion, create `.planning/phases/45-h-tr-x-ly-song-song-signal-strategy-cho-nhi-u-symbol-m-t-/45-04-SUMMARY.md`
</output>
