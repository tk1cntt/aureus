---
phase: 42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/aureus-signal/engine/snapshot_utils.py
  - services/aureus-signal/engine/strategies/registry.py
  - services/aureus-signal/engine/signal_event_publisher.py
  - services/aureus-signal/engine/orders.py
  - services/aureus-trader/order_builder.py
  - mql5/AureusProvider.mq5
autonomous: true
requirements: []

must_haves:
  truths:
    - "VALID_SIZE_MODES chứa RISK_FIXED_AMOUNT"
    - "risk_amount propagate qua registry → orders → publisher → order_builder → MT5"
    - "orders.py xử lý đúng flow mới: side → entry_price → SL/TP → validate (D-13, D-14)"
    - "orders.py set volume=0 + risk_amount khi RISK_FIXED_AMOUNT"
    - "order_builder.py forward RISK_FIXED_AMOUNT sang MT5 command"
    - "MT5 CalculateLotFromBudget dùng Ask/Bid thực tế + auto-adjust budget"
  artifacts:
    - path: "services/aureus-signal/engine/snapshot_utils.py"
      provides: "VALID_SIZE_MODES tuple với RISK_FIXED_AMOUNT"
    - path: "services/aureus-signal/engine/strategies/registry.py"
      provides: "risk_amount trong evaluate_all accepted output"
    - path: "services/aureus-signal/engine/signal_event_publisher.py"
      provides: "risk_amount trong STRATEGY_MATCH event data"
    - path: "services/aureus-signal/engine/orders.py"
      provides: "RISK_FIXED_AMOUNT handling + flow mới (side → entry → SL/TP → validate)"
    - path: "services/aureus-trader/order_builder.py"
      provides: "build_order_command forwarding RISK_FIXED_AMOUNT"
    - path: "mql5/AureusProvider.mq5"
      provides: "CalculateLotFromBudget() với auto-adjust budget"
---

<objective>
Verify toàn bộ RISK_FIXED_AMOUNT pipeline và flow SL/TP mới đã implement đúng.

Purpose: Confirm code đã tồn tại tại mọi bước: registry → orders → publisher → order_builder → MT5.
Output: SUMMARY.md xác nhận tất cả grep checks pass hoặc implement missing pieces.
</objective>

<execution_context>
@D:/Aureus/.claude/get-shit-done/workflows/execute-plan.md
@D:/Aureus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify VALID_SIZE_MODES và risk_amount propagation (registry → orders → publisher)</name>
  <files>services/aureus-signal/engine/snapshot_utils.py, services/aureus-signal/engine/strategies/registry.py, services/aureus-signal/engine/signal_event_publisher.py, services/aureus-signal/engine/orders.py</files>
  <read_first>
    - services/aureus-signal/engine/snapshot_utils.py
    - services/aureus-signal/engine/strategies/registry.py
    - services/aureus-signal/engine/signal_event_publisher.py
    - services/aureus-signal/engine/orders.py
  </read_first>
  <action>
    Kiểm tra 4 điểm:

    **snapshot_utils.py** — VALID_SIZE_MODES phải chứa "RISK_FIXED_AMOUNT":
    ```python
    VALID_SIZE_MODES = ("FIXED_UNITS", "FIXED_LOT", "RISK_PERCENT", "RISK_FIXED_AMOUNT")
    ```

    **registry.py** — Trong accepted.append() dict, phải có:
    ```python
    "risk_amount": order_plan.get("risk_amount"),
    ```

    **signal_event_publisher.py** — Trong publish_strategy_match data dict, phải có:
    ```python
    "risk_amount": strategy_result.get("risk_amount"),
    ```

    **orders.py** — Trong process_triggers, khi size_mode == "RISK_FIXED_AMOUNT":
    ```python
    order_plan_snapshot["risk_amount"] = risk_amount
    order_plan_snapshot["size_value"] = 0
    ```

    Nếu grep pass → done. Nếu grep fail → implement missing code.
  </action>
  <acceptance_criteria>
    - snapshot_utils.py: grep '"RISK_FIXED_AMOUNT"' → match trong VALID_SIZE_MODES
    - registry.py: grep '"risk_amount"' → match trong accepted dict
    - signal_event_publisher.py: grep '"risk_amount"' → match trong data dict
    - orders.py: grep '"risk_amount"' → match trong RISK_FIXED_AMOUNT block
    - orders.py: grep '"size_value".*= 0' → match volume=0 placeholder
  </acceptance_criteria>
  <verify>
    <automated>grep -n '"RISK_FIXED_AMOUNT"' services/aureus-signal/engine/snapshot_utils.py && grep -n '"risk_amount"' services/aureus-signal/engine/strategies/registry.py services/aureus-signal/engine/signal_event_publisher.py services/aureus-signal/engine/orders.py</automated>
  </verify>
  <done>
    VALID_SIZE_MODES có RISK_FIXED_AMOUNT, risk_amount present ở registry, publisher, orders
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify order_builder.py forwarding RISK_FIXED_AMOUNT</name>
  <files>services/aureus-trader/order_builder.py</files>
  <read_first>
    - services/aureus-trader/order_builder.py
  </read_first>
  <action>
    Trong build_order_command(), kiểm tra:

    **Extract fields:**
    ```python
    size_mode = data.get("size_mode", "FIXED_UNITS")
    risk_amount = data.get("risk_amount")
    ```

    **Forwarding block:**
    ```python
    if size_mode == "RISK_FIXED_AMOUNT":
        command["size_mode"] = size_mode
        command["risk_amount"] = risk_amount if risk_amount else 50.0
        command["volume"] = 0
    ```

    Nếu grep pass → done. Nếu fail → implement.
  </action>
  <acceptance_criteria>
    - grep 'RISK_FIXED_AMOUNT' services/aureus-trader/order_builder.py → match
    - grep '"risk_amount"' services/aureus-trader/order_builder.py → match fallback 50.0
    - grep '"volume".*0' services/aureus-trader/order_builder.py → match trong RISK_FIXED_AMOUNT block
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'RISK_FIXED_AMOUNT' services/aureus-trader/order_builder.py && grep -n '50.0' services/aureus-trader/order_builder.py</automated>
  </verify>
  <done>
    order_builder.py forward size_mode + risk_amount + volume=0 cho RISK_FIXED_AMOUNT
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify MT5 CalculateLotFromBudget + auto-adjust</name>
  <files>mql5/AureusProvider.mq5</files>
  <read_first>
    - mql5/AureusProvider.mq5
  </read_first>
  <action>
    Kiểm tra CalculateLotFromBudget():

    **D-08 — Giá thực tế:**
    ```mql5
    double actualEntry = (direction == "BUY") ? SymbolInfoDouble(symbol, SYMBOL_ASK) : SymbolInfoDouble(symbol, SYMBOL_BID);
    ```

    **D-09 — Công thức:**
    ```mql5
    double riskPerLot = slDistance * contractSize;
    double lot = riskAmount / riskPerLot;
    ```

    **D-10 — Contract sizes:** XAUUSD=100, XAGUSD=5000, indices=1, crypto=1, forex=100000

    **D-18 — Auto-adjust tăng budget (lot < min):**
    ```mql5
    if(lot < minVol) { adjustedBudget *= 2; ... }
    ```

    **D-19 — Auto-adjust giảm budget (lot > max):**
    ```mql5
    if(lot > maxVol) { adjustedBudget /= 2; ... }
    ```

    **D-11 — Normalize + clamp:**
    ```mql5
    lot = MathFloor(lot / stepVol) * stepVol;
    if(lot < minVol) lot = minVol;
    if(lot > maxVol) lot = maxVol;
    ```

    **ExecuteOpenOrder integration:**
    ```mql5
    bool calcVolumeOnMT5 = (StringFind(StringToUpper(sizeMode), "RISK_FIXED_AMOUNT") >= 0 && riskAmount > 0);
    if(calcVolumeOnMT5 && sl > 0) {
       volume = CalculateLotFromBudget(symbol, direction, price, sl, riskAmount);
    }
    ```
  </action>
  <acceptance_criteria>
    - grep "SymbolInfoDouble.*SYMBOL_ASK" mql5/AureusProvider.mq5 → match trong CalculateLotFromBudget
    - grep "SymbolInfoDouble.*SYMBOL_BID" mql5/AureusProvider.mq5 → match
    - grep "riskAmount / riskPerLot" mql5/AureusProvider.mq5 → công thức
    - grep "contractSize = 100.0" mql5/AureusProvider.mq5 → XAUUSD
    - grep "contractSize = 5000.0" mql5/AureusProvider.mq5 → XAGUSD
    - grep "adjustedBudget \*= 2" mql5/AureusProvider.mq5 → auto-adjust tăng
    - grep "adjustedBudget /= 2" mql5/AureusProvider.mq5 → auto-adjust giảm
    - grep "MathFloor.*stepVol" mql5/AureusProvider.mq5 → normalize
    - grep "calcVolumeOnMT5" mql5/AureusProvider.mq5 → detect
    - grep "CalculateLotFromBudget" mql5/AureusProvider.mq5 → gọi trong ExecuteOpenOrder
  </acceptance_criteria>
  <verify>
    <automated>grep -n "contractSize = 100.0\|contractSize = 5000.0\|riskAmount / riskPerLot\|adjustedBudget" mql5/AureusProvider.mq5 | head -10</automated>
  </verify>
  <done>
    CalculateLotFromBudget có đúng công thức, contract sizes, auto-adjust; ExecuteOpenOrder gọi đúng
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify flow mới side → entry → SL/TP → validate (D-13, D-14)</name>
  <files>services/aureus-signal/engine/orders.py</files>
  <read_first>
    - services/aureus-signal/engine/orders.py
  </read_first>
  <action>
    Trong process_triggers(), verify flow order:

    **D-14 — Flow mới:** Thứ tự phải là:
    1. Xác định side (BUY/SELL)
    2. Tính entry_price từ entry_method (_calculate_entry_price)
    3. Tính SL/TP với entry_price_override (_calculate_sl_tp)
    4. Validate missing_keys (sau SL/TP enrichment)

    Grep kiểm tra:
    - "entry_price_override" phải có trong lời gọi _calculate_sl_tp
    - "missing_keys" validation phải NẰM SAU SL/TP enrichment block
    - KHÔNG có "continue" sau khi sl/tp None (D-13: để missing_keys handle)

    Nếu flow sai → sửa thứ tự trong process_triggers().
  </action>
  <acceptance_criteria>
    - orders.py: grep "entry_price_override" → có trong lời gọi _calculate_sl_tp
    - orders.py: grep "missing_order_plan_keys" → nằm sau SL/TP enrichment block (sau dòng "tp_value")
    - orders.py: grep "SL_TP_CALC_FAILED" → có block nhưng KHÔNG có "continue" ngay sau đó
    - orders.py: grep "_calculate_entry_price" → gọi trước _calculate_sl_tp
  </acceptance_criteria>
  <verify>
    <automated>grep -n "entry_price_override\|missing_order_plan_keys\|SL_TP_CALC_FAILED\|_calculate_entry_price" services/aureus-signal/engine/orders.py</automated>
  </verify>
  <done>
    Flow đúng: side → entry_price → SL/TP → validate. Khi SL/TP=None → missing_keys handle rejection
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → publisher | risk_amount truyền qua dict return — không qua network |
| publisher → Redis | risk_amount serialize JSON → Redis pub/sub |
| order_builder → MT5 TCP | JSON command gửi qua TCP socket |
| MT5 EA → OrderSend | Lot tính toán → gửi order thực tế |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Integrity | orders.py:process_triggers | mitigate | risk_amount = size_value nếu > 0, fallback $50 — tránh null/zero budget |
| T-42-07 | Integrity | order_builder.py:build_order_command | mitigate | risk_amount null check với fallback 50.0 |
| T-42-09 | Integrity | CalculateLotFromBudget | mitigate | slDistance <= 0 → return SYMBOL_VOLUME_MIN; auto-adjust loop max 20 iterations |
| T-42-10 | Tampering | signal_event_publisher.py | accept | risk_amount chỉ read từ strategy_result — không write từ external |
| T-42-11 | Information Disclosure | Redis pub/sub | accept | risk_amount = 50.0 — không phải secret |
</threat_model>

<verification>
- VALID_SIZE_MODES có RISK_FIXED_AMOUNT
- risk_amount present ở registry, publisher, orders, order_builder
- order_builder forward RISK_FIXED_AMOUNT → MT5
- MT5 CalculateLotFromBudget đúng công thức, contract sizes, auto-adjust
- ExecuteOpenOrder detect RISK_FIXED_AMOUNT → gọi CalculateLotFromBudget
- Flow orders.py đúng: side → entry → SL/TP → validate (D-13, D-14)
</verification>

<success_criteria>
- Tất cả grep checks pass → code đã implement đúng
- Nếu grep fail → implement missing pieces
- SUMMARY.md xác nhận status từng task
</success_criteria>

<output>
After completion, create `.planning/phases/42-giam-rr-ratio-xuong-1-5-va-bo-sung-fixed-budget-entry/42-02-SUMMARY.md`
</output>
